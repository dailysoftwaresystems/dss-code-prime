{
  "$comment": "D-OPT-MEM2REG-LOOP-BODY-LOCAL-DEAD-PHI witness (p18 Cluster G c3): a local declared+used entirely inside a loop body (`int iv = i*2;`) is block-local; minimal SSA placed a dead phi for it at the loop header whose entry incoming was undefined → Mem2Reg aborted the release compile on ANY `while(){ int x=...; }`. Semi-pruned SSA (gate phi placement on upward-exposure) fixes it. The `release` arm is the witness; baseline always worked (debug runs no Mem2Reg). exit 20 = 0+2+4+6+8 via a mutable global (anti-fold). RED-on-disable: revert the gate in mem2reg.cpp → the release arm Mem2Reg-aborts.",
  "language": "c-subset",
  "source": "main.c",
  "exitCode": 20,
  "targets": [
    { "spec": "x86_64:pe64-x86_64-windows-exec", "artifact": "main.exe", "runOn": ["windows"] },
    { "spec": "x86_64:elf64-x86_64-linux-exec",  "artifact": "main", "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec",  "artifact": "main", "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    { "label": "release", "shippedPipeline": "release" }
  ]
}

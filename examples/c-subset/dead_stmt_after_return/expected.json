{
  "$comment": "D-CSUBSET-BLOCK-TERMINATION-LAST-REACHABLE (TF-C40, SQLite testfixture-link sub-arc): RUN witness that a non-void function whose body has a trailing null statement `;` AFTER the `return` (the `MACRO(...);` idiom — the macro body ends in `return`, the invocation carries a `;` that lowers to an empty Block as the body's LAST child) COMPILES and RUNS correctly. `pathTerminates` (src/hir/hir_verifier.hpp) now decides a block's termination by its LAST REACHABLE statement, not its literal last child, so the empty trailing Block no longer masks the real `return` terminator. gcc/clang compile this cleanly; before the fix DSS emitted a spurious H0003 (H_VerifierFailure). `f()` returns 42; the dead `;` has no runtime effect. The `release` optimizedPipeline arm witnesses the shipped optimizer over the same source (the dead null statement is pruned). All 4 targets: a front-end/verifier predicate change, target/format-agnostic. RED-ON-DISABLE: revert the Block case of pathTerminates to `return !kids.empty() && pathTerminates(src, kids.back())` and this TU no longer COMPILES (H0003).",
  "language": "c-subset",
  "source": "main.c",
  "exitCode": 42,
  "expectedStdout": "",
  "targets": [
    { "spec": "x86_64:pe64-x86_64-windows-exec", "artifact": "main.exe", "runOn": ["windows"] },
    { "spec": "x86_64:elf64-x86_64-linux-exec", "artifact": "main", "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec", "artifact": "main", "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    {"label": "full-release-like", "passes": ["ConstFold", "Mem2Reg", "CopyProp", "Cse", "Licm", "SimplifyCfg", "Dce"], "$commentByteIdentical": "MEASURED 2026-08-15 — optimized image BYTE-IDENTICAL to the baseline on: arm64:elf64-aarch64-linux-exec, x86_64:elf64-x86_64-linux-exec, x86_64:pe64-x86_64-windows-exec. Legs actually run: windows/pe64-x86_64 and linux/elf64 (x86_64 + aarch64 under qemu); the darwin/macho64 leg was NOT run. Where the image is identical the arm's exit-code/stdout comparison is x==x and cannot fail. HONEST, AND THE MANIFEST IS STILL COVERED: a SIBLING arm in this same manifest was measured DIFFERING and carries \"mustDifferFromBaseline\": true, so the example does witness the optimizer. This arm names a narrower pass set that legitimately finds nothing — which is exactly why the lever is per-ARM rather than per-manifest. The dead trailing `;` carries no runtime effect and is gone before the pipeline runs. Deliberately NOT given \"mustDifferFromBaseline\" — an honest no-op, not a missed opt-in."},
    {"label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true}
  ]
}

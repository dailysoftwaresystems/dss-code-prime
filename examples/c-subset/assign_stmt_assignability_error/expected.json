{
  "$comment": "D-SEMANTIC-ASSIGN-STMT-ASSIGNABILITY-BYPASS (negative): the assignment STATEMENT `x = f;` (int <- float) fails loud with a positioned S_TypeMismatch at the RHS (22:9), the SAME check the init/call-arg/return sites run. A valid `x = 5;` statement on the line above is unaffected, so exactly ONE diagnostic fires. RED-ON-DISABLE: remove the assignment-statement isAssignable arm (restore the bypass) -> `x = f;` is silently accepted (HIR coerce truncates) and the diagnostic set is empty -> mismatch.",
  "language": "c-subset",
  "source": "main.c",
  "targets": [ { "spec": "x86_64:elf64-x86_64-linux-exec" } ],
  "expectDiagnostics": [ { "code": "S_TypeMismatch", "line": 22, "col": 9 } ]
}

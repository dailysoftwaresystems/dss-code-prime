{
  "$comment": "FC16 (D-CSUBSET-NORETURN): a non-void function (compute) whose only non-return path is a call to a noreturn function (die — noreturn via the proto/def OR-merge) that tail-calls the shipped noreturn `exit`. The synthesized Unreachable makes the path structurally terminate; main -> compute(0) -> die(42) -> exit(42) exits 42. The `release` arm loads the shipped release.pipeline.json (shippedPipeline) so the reachable Unreachable is routed through the real optimizer composition — the required safety net.",
  "language": "c-subset",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    {
      "spec": "x86_64:pe64-x86_64-windows-exec",
      "artifact": "main.exe",
      "runOn": ["windows"]
    },
    {
      "spec": "x86_64:elf64-x86_64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"]
    },
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"],
      "emulator": "qemu-aarch64"
    },
    {
      "spec": "arm64:macho64-arm64-darwin-exec",
      "artifact": "main",
      "runOn": ["darwin"],
      "emulator": ""
    }
  ],
  "optimizedPipelines": [
    {"label": "release", "shippedPipeline": "release"}
  ]
}

{
  "$comment": "c21 (D-CSUBSET-VOLATILE-QUALIFIER): a pointer-to-volatile-POINTEE (`volatile int *p`) is the one volatile form model B cannot express; it fails loud S_VolatilePointeeNotSupported (was S_VolatileNotSupported, retired now that volatile objects/members/pointer-objects compile). Positioned at the declarator `*p`.",
  "language": "c-subset",
  "source": "main.c",
  "targets": [
    { "spec": "x86_64:elf64-x86_64-linux-exec" }
  ],
  "expectDiagnostics": [
    { "code": "S_VolatilePointeeNotSupported", "line": 12, "col": 14 }
  ]
}

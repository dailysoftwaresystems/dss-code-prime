{
  "$comment": "FC15c (`#pragma` -- C 6.10.6) runtime witness. main.c is laced with `#pragma` lines (file scope + inside main, with GCC/STDC payloads); the preprocessor consumes-and-DROPS each line with NO error (C 6.10.6p2). The program still compiles->links->runs to exit 42 (20 + 22). RED-ON-DISABLE: without the `#pragma`-consume arm each `#pragma` falls through to the unsupported-directive fail-loud (P_PreprocessorUnsupported) -> compile error, no binary. The `release` arm runs the shipped parser->semantic->codegen->native pipeline over the same source.",
  "language": "c-subset",
  "source": "main.c",
  "exitCode": 42,
  "expectedStdout": "",
  "targets": [
    {
      "spec": "x86_64:pe64-x86_64-windows-exec",
      "artifact": "main.exe",
      "runOn": ["windows"]
    },
    {
      "spec": "x86_64:elf64-x86_64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"]
    },
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"],
      "emulator": "qemu-aarch64"
    },
    {
      "spec": "arm64:macho64-arm64-darwin-exec",
      "artifact": "main",
      "runOn": ["darwin"],
      "emulator": ""
    }
  ],
  "optimizedPipelines": [
    {"label": "release", "shippedPipeline": "release"}
  ]
}

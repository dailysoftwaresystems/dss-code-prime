{
  "$comment": "D15 cycle C: the SHIPPED release pipeline must compile+run ordinary recursive control flow (rec(5)=42). The `release` arm loads release.pipeline.json ITSELF (shippedPipeline) so the corpus exercises the exact shipped composition — the RC-A I_UnreachableBlock fix (SimplifyCfg post-fold reachability) is witnessed end-to-end across all four targets.",
  "language": "c-subset",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    {
      "spec": "x86_64:pe64-x86_64-windows-exec",
      "artifact": "main.exe",
      "runOn": ["windows"]
    },
    {
      "spec": "x86_64:elf64-x86_64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"]
    },
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"],
      "emulator": "qemu-aarch64"
    },
    {
      "spec": "arm64:macho64-arm64-darwin-exec",
      "artifact": "main",
      "runOn": ["darwin"],
      "emulator": ""
    }
  ],
  "optimizedPipelines": [
    {"label": "release", "shippedPipeline": "release"}
  ]
}

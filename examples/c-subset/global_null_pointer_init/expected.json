{
  "$comment": "c80 (top-level scalar null-pointer-constant initializer; 2026-07-02): a bare file-scope `T* g = 0;` folds to compile-time zero bytes (C 6.3.2.3p3 — identical to the no-initializer .bss form). The exact sqlite construct: vfsList/unixBigLock/inodeList/sqlite3SharedCacheList/sqlite3_temp_directory/sqlite3_data_directory = 0 (6 of the 9 K_NoMatchingObjectFormat runtime-init globals). Pre-fix const-eval refused cast-to-pointer and c67's tryClassifyNullPointerConst was wired ONLY into the AGGREGATE member loop (struct{void*p;} g={0} folded; bare void* g=0 did NOT) -> runtimeInit -> K_NoMatchingObjectFormat fail-loud. FIX: the classifyGlobals cascade gains tryClassifyNullPointerConst + tryClassifyNullBaseIndexConst arms for top-level scalars (mirroring the member loop) + the asm.cpp scalar arm sizes pointer-class scalars via the DataModel chokepoint. The globals stay ordinary MUTABLE storage (the example writes through them + calls a fn-ptr). RED-ON-DISABLE: revert the cascade arms -> K_NoMatchingObjectFormat. => 42.",
  "language": "c-subset",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    { "spec": "x86_64:pe64-x86_64-windows-exec", "artifact": "main.exe", "runOn": ["windows"] },
    { "spec": "x86_64:elf64-x86_64-linux-exec", "artifact": "main", "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec", "artifact": "main", "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    {"label": "full-release-like", "passes": ["ConstFold", "Mem2Reg", "CopyProp", "Cse", "Licm", "SimplifyCfg", "Dce"]},
    {"label": "release", "shippedPipeline": "release"}
  ]
}

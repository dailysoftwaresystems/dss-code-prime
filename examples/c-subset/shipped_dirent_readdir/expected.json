{
  "$comment": "c81 (the sqlite shell.c CLI route): ship <dirent.h> — opaque DIR (typedef to the empty named struct, the stdio-FILE precedent; member access on it fails loud), the glibc-exact struct dirent (readdir returns LIBC'S struct — matching \".\" by d_name proves the 19-byte offset), opendir/readdir/closedir on real libc. ★★★ THE pe64 ARM IS THE WITNESS FOR D-RUNTIME-DSS-SHIPS-NO-IMPLEMENTATION-HALF, AND IT RUNS THE *IDENTICAL* SOURCE. Windows has no POSIX directory API in any platform image, so on `pe` these three symbols are not imported at all: `dirent.json`'s per-format `realization` map states that their bodies are PROVIDED by the shipped source file `runtime/platform/src/dirent.c` (spelled relative to `src/dss-config/`, and it is the literal value of `realization.pe.source` — ✔RE-MEASURED 2026-08-18 after this comment named a `runtime/platform/pe/…` path that has never existed on disk, in BOTH places it mentioned the file, INCLUDING inside the red-on-disable recipe below), and the driver adds that file to the build graph as an ordinary extra translation unit compiled FOR THE TARGET. That makes this one example a differential over the realization axis rather than over the source: the SAME `main.c` binds three libc imports on elf/macho and links a DSS-compiled body on pe, and all four legs must answer 42. ★★ THE d_name MATCH IS WHAT MAKES IT A LAYOUT WITNESS ON pe TOO — the shipped unit writes into the descriptor's OWN `struct dirent` (it `#include`s this very header), so finding the dot entry by string equality proves the unit and the declaration agree on the offset. MEASURED: mutate the pe `struct dirent`'s `d_name` in `dirent.json` and `runtime/platform/src/dirent.c` FAILS TO COMPILE — the layout agreement is checked by the compiler, not by review. ★ RED-ON-DISABLE for the pe arm: delete `dirent.json`'s `realization` key ⇒ the pe64 build FAILS LOUD at the HIR tier (`H_UnsupportedLoweringForKind` — the row is available on pe and names no image, so there is nothing to bind) and never reaches the link tier, so this witness does NOT depend on the still-open D-LINK-EXEC-UNDEFINED-SYMBOL-FAIL-LOUD. | ⚠ THE DARWIN CLAUSE THAT STOOD HERE WAS STALE AND SAID THE OPPOSITE OF THE TREE, so it is corrected rather than deleted. It read: 'Darwin's dirent diverges (d_seekoff/d_namlen/d_name[1024]) AND Darwin-x86_64 readdir is symbol-suffixed ($INODE64); fail-loud unavailable beats a silent wrong layout' — i.e. it recorded a decision NOT to ship dirent on macho, while this very manifest declares a `arm64:macho64-arm64-darwin-exec` target. ✔RE-MEASURED 2026-08-18 against `dirent.json`: BOTH divergences are now MODELLED, not refused. `availableObjectFormats` is [elf, macho, pe] with `library.macho` = /usr/lib/libSystem.B.dylib; the macho `struct dirent` variant declares exactly d_ino u64 / d_seekoff u64 / d_reclen u16 / d_namlen u16 / d_type u8 / d_name arr<char,1024>; and `opendir`/`readdir` carry a `linkName` variant `opendir$INODE64` / `readdir$INODE64` gated to {format: macho, arch: x86_64} (D-FFI-SHIPPED-SYMBOL-PER-TARGET-LINK-NAME). So the layout is declared per-format instead of guessed, which is what made the macho arm safe to ship. RED-ON-DISABLE: remove dirent.json -> F001A.",
  "language": "c-subset",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    { "spec": "x86_64:pe64-x86_64-windows-exec", "artifact": "main.exe", "runOn": ["windows"] },
    { "spec": "x86_64:elf64-x86_64-linux-exec",  "artifact": "main", "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec",  "artifact": "main", "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    {"label": "full-release-like", "passes": ["ConstFold", "Mem2Reg", "CopyProp", "Cse", "Licm", "SimplifyCfg", "Dce"], "mustDifferFromBaseline": true},
    {"label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true}
  ]
}

{
  "$comment": "D-OPT-VARIADIC-RELEASE-MISCOMPILE witness (p18 Cluster G c2): a static single-call variadic `vsum` is a prime inlining candidate; the release pipeline runs Inlining, and inlining a va_start-bearing callee binds its frame-relative va_start leaves (VaRegSaveAreaAddr/VaOverflowArgAreaAddr/VaHomeArgAreaAddr) to the CALLER's frame → garbage. The inlineLegalityGate now refuses it. The `release` arm is the witness FC12's baseline-only variadic corpus lacked. RED-on-disable: drop the va_start-leaf refusal in inlining.cpp → the release arm exits garbage not 42.",
  "language": "c-subset",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    { "spec": "x86_64:pe64-x86_64-windows-exec", "artifact": "main.exe", "runOn": ["windows"] },
    { "spec": "x86_64:elf64-x86_64-linux-exec",  "artifact": "main", "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec",  "artifact": "main", "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    { "label": "release", "shippedPipeline": "release" }
  ]
}

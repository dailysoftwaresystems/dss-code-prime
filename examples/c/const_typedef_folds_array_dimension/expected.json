{
  "$comment": "P68 round 8 lane ht: a `const` reached through a TYPEDEF folds in an array dimension exactly as its direct spelling does. `typedef const int CI; typedef CI CI2; CI ROWS = 4; CI2 COLS = 3; static int table[ROWS][COLS];` at FILE scope, where a dimension that does not fold is a variably modified type and refused. Before the typedef's own `const` claim was applied DSS refused exactly this while folding the direct `const int ROWS = 4;`; clang 18.1.3 folds both spellings in its default mode (-Wgnu-folding-constant) and gcc 13.3.0 refuses both, so the fold is owed and owed to both. Exit 42 iff both dimensions folded (sizeof gives 12 cells) and the argc-seeded contents sum to 66. RED-ON-DISABLE: the Pass-1.5 typedef application in the semantic tier skipped -> `ROWS` is not const, the dimension does not fold, no image.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "expectedStdout": "",
  "targets": [
    { "spec": "x86_64:pe64-x86_64-windows-exec", "artifact": "main.exe", "runOn": ["windows"] },
    { "spec": "x86_64:elf64-x86_64-linux-exec",  "artifact": "main", "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec",  "artifact": "main", "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    {"label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true}
  ]
}

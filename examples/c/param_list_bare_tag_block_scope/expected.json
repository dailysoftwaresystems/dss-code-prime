{
  "$comment": "P68 round 9 (lane `cs`) — the tag move D-C-INCOMPATIBLE-POINTER-CONVERSION-REFUSED-WHERE-EVERY-REFERENCE-WARNS's closing names: a BARE tag reference in a function DEFINITION's parameter list, with no such tag visible, declares the tag in the body's block scope (C 6.2.1p4: the list and the body's outermost block are ONE scope). ✔MEASURED 2026-09-23, each reference separately, this file compiled and RUN: gcc 13.3.0, clang 18.1.3 and mingw-w64 13.2.0 at -std=c2x build it and exit 42, each with the same four warnings at the same four positions DSS gives below (a tag declared inside a parameter list, three times; `g(p)` passing one function's `struct Q *` to the other's, once); at -std=c17 -pedantic-errors gcc, clang and mingw refuse it; MSVC 19.51 refuses it (C2037: on its reading the list's `R` is a file-scope tag that stays incomplete, so `p->a` names no member). DSS before this round's row-1 work (✔MEASURED, the lane's WSL tree at the P0 fold, ELF x86_64) read the list's tags at FILE scope — MSVC's reading — and refused `h` twice: S_NotAComposite at `p->a` and S_TypeMismatch at `h(&r)`. exit 42 = f(0) (22) + h(0) (42: the completed `R`'s member read through the recursive call, plus 22) - 22.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "expectWarnings": [
    { "code": "S_TagDeclaredInParameterList", "line": 21, "col": 21 },
    { "code": "S_TagDeclaredInParameterList", "line": 23, "col": 21 },
    { "code": "S_IncompatiblePointerConversion", "line": 23, "col": 38 },
    { "code": "S_TagDeclaredInParameterList", "line": 25, "col": 21 }
  ],
  "targets": [
    {
      "spec": "x86_64:pe64-x86_64-windows-exec",
      "artifact": "main.exe",
      "runOn": ["windows"]
    },
    {
      "spec": "x86_64:elf64-x86_64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"]
    },
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"],
      "emulator": "qemu-aarch64"
    },
    {
      "spec": "arm64:macho64-arm64-darwin-exec",
      "artifact": "main",
      "runOn": ["darwin"],
      "emulator": ""
    }
  ],
  "optimizedPipelines": [
    {"label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true}
  ]
}

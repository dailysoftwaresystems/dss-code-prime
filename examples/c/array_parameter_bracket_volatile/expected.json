{
  "$comment": "P68 round 10 (lane `cs`, D-C-AN-ARRAY-PARAMETER-DROPS-ITS-BRACKET-VOLATILE): C 6.7.6.3p7 gives an array parameter's adjusted pointer the qualifiers written inside its outermost brackets — `int p[volatile]` is `int *volatile p`, in every bracket form (`[const volatile]`, `[static volatile 2]`, `[volatile][2]`, a VLA's `[volatile n]`); C 6.7.6.3p15 keeps that qualifier out of the FUNCTION's type (both `sum` spellings are `int (int *)`); C 6.7.3p10 puts a typedef'd array's qualifier on its ELEMENT (a `volatile A` parameter points at `volatile int`, a `volatile A` local holds `volatile int`s). MEASURED 2026-09-24, this file compiled and RUN on each reference separately: gcc 13.3.0 and clang 18.1.3 at -std=c17 -pedantic-errors and -std=c2x, mingw-w64 13.2.0 at both — exit 42 with no diagnostic; MSVC 19.51 implements no bracket qualifier (C2143) and abstains. Before this round DSS built it silently and exited 1: the bracket's volatile was dropped, a parameter's qualifier stayed in the function type (the explicit spelling's initialization warned S_IncompatiblePointerConversion), and the typedef'd array's qualifier sat on the array. exit 42 = every type is C's.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    {
      "spec": "x86_64:pe64-x86_64-windows-exec",
      "artifact": "main.exe",
      "runOn": ["windows"]
    },
    {
      "spec": "x86_64:elf64-x86_64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"]
    },
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"],
      "emulator": "qemu-aarch64"
    },
    {
      "spec": "arm64:macho64-arm64-darwin-exec",
      "artifact": "main",
      "runOn": ["darwin"],
      "emulator": ""
    }
  ],
  "optimizedPipelines": [
    {
      "label": "release",
      "shippedPipeline": "release",
      "mustDifferFromBaseline": true
    }
  ]
}

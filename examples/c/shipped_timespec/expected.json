{
  "$comment": "c46 D-FFI-TIME-STRUCT-TIMESPEC: ship struct timespec from <time.h> (the sqlite frontier after c45 cleared FILENAME_MAX). sqlite os_unix `struct timespec sp;` (sqlite3.c:47379) failed S0028 (incomplete type) because time.json shipped struct tm but not timespec. Added struct timespec {tv_sec i64, tv_nsec i64} (16 bytes, flat -- byte-identical on glibc/Darwin LP64) to time.json's structs surface. Exit 42 iff sizeof==16 + both fields round-trip at distinct offsets. <time.h> is gated [elf,macho] (FULL-UNIX-build targets), so NO pe target (a pe build uses os_win). RED-ON-DISABLE: remove timespec from time.json -> S0028. The fold is at semantic time; optimized pipelines re-verify the same layout. Targets: x86_64-elf (WSL run) + arm64-elf (qemu run) + arm64-macho (compile-only, no darwin host).",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    { "spec": "x86_64:elf64-x86_64-linux-exec",  "artifact": "main", "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec",  "artifact": "main", "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    {"label": "full-release-like", "passes": ["ConstFold", "Mem2Reg", "CopyProp", "Cse", "Licm", "SimplifyCfg", "Dce"], "$commentByteIdentical": "MEASURED 2026-08-15 — optimized image BYTE-IDENTICAL to the baseline on: arm64:elf64-aarch64-linux-exec, x86_64:elf64-x86_64-linux-exec. Legs actually run: windows/pe64-x86_64 and linux/elf64 (x86_64 + aarch64 under qemu); the darwin/macho64 leg was NOT run. Where the image is identical the arm's exit-code/stdout comparison is x==x and cannot fail. HONEST: the values here live in an AGGREGATE (struct/union/array) local or global. The shipped pipeline does not promote aggregate storage, so the address-compute/store/load chain survives the pipeline unchanged. Every read and write is a member of a `struct timespec` local. Deliberately NOT given \"mustDifferFromBaseline\" — an honest no-op, not a missed opt-in."},
    {"label": "release", "shippedPipeline": "release", "$commentByteIdentical": "MEASURED 2026-08-15 — optimized image BYTE-IDENTICAL to the baseline on: arm64:elf64-aarch64-linux-exec, x86_64:elf64-x86_64-linux-exec. Legs actually run: windows/pe64-x86_64 and linux/elf64 (x86_64 + aarch64 under qemu); the darwin/macho64 leg was NOT run. Where the image is identical the arm's exit-code/stdout comparison is x==x and cannot fail. HONEST: the values here live in an AGGREGATE (struct/union/array) local or global. The shipped pipeline does not promote aggregate storage, so the address-compute/store/load chain survives the pipeline unchanged. Every read and write is a member of a `struct timespec` local. Deliberately NOT given \"mustDifferFromBaseline\" — an honest no-op, not a missed opt-in."}
  ]
}

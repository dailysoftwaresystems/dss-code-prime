{
  "$comment": "D-PP-VA-SPECIAL-IDENTIFIER-IN-A-NONVARIADIC-BODY-REFUSED-ABOVE-THE-UNION (P54 lane `ob`) — the LAST position in the C23 6.10.5p5 family. Lane `va` gave the violation a Warning everywhere OUTSIDE a `#define`; lane `vh` relaxed the `#define`/`#undef` NAME positions and the two macro-PARAMETER positions; this row is either config-declared spelling in a NON-VARIADIC replacement list where no parameter of that name exists, which DSS REFUSED OUTRIGHT — `#define OBJ __VA_ARGS__` as an Error from `handleDefine`'s own catch-all guard and `#define OBJ __VA_OPT__` as an Error from `validateVaOpt`'s `!def.isVariadic` arm. THE OPERATOR RULED 2026-09-02: ACCEPT AND WARN. ✔MEASURED 2026-09-02 (lane `ob`), each reference invoked SEPARATELY, compile-only, DEFAULT and STRICT, with the macro NEVER USED so that no reference's MEANING choice could masquerade as a refusal: every shape is rc 0 on gcc 13.3.0, gcc 13.2.0 (mingw-w64) and cl 19.51.36252, and rc 0 on clang 18.1.3 for all but one; every one of them becomes an error under `-pedantic-errors` / `/W4 /WX` (C2220). Acceptance is therefore 4-0 or 3-1 shape by shape, the disjunction decides ACCEPTANCE, and refusing put DSS ABOVE the union while going silent would leave it BELOW on the diagnostic axis — a Warning is the only answer level on both axes at once, and the strict arm stays AVAILABLE rather than imposed — ✔MEASURED that this file under `--warnings-as-errors` exits rc 1 with EIGHT errors and zero warnings, the references' `-pedantic-errors` / `/W4 /WX` posture on demand. ⚠⚠ THE BRIEF'S PREMISE 'all four references accept' WAS REFUTED BY THIS MEASUREMENT AND THE REFUTED SHAPE IS DELIBERATELY IN THE FILE: clang 18.1.3 hard-errors `missing '(' following __VA_OPT__` on a BARE va-opt spelling in a FUNCTION-LIKE non-variadic body — TWO of them here, `ADD_OPT` and `OPT_BARE_FN` — and NOT on the same spelling in an object-like body, so its rule keys on function-like-ness rather than on variadicity. The union is read construct by construct, exactly as lane `vh` found from the other side, and 3-1 for acceptance is still acceptance. ⚠ THE MEANING IS gcc's, MEASURED: the token is an ORDINARY IDENTIFIER and passes through verbatim. Two references lose code here and DSS follows neither — cl 19.51.36252 DELETES both spellings wherever they appear (`int __VA_ARGS__ = 5;` becomes `int = 5;`, C2513) and clang ELIDES a USED `__VA_OPT__( ... )` even inside a non-variadic macro. Same tie-break as `#pragma once`: the reading that silently drops what the author wrote loses. ⚠⚠ ONLY THE TWO gcc REFERENCES BUILD THIS WHOLE FILE, and the cross-check is the point: ✔MEASURED that gcc 13.3.0 (WSL) and gcc 13.2.0 (mingw) each build it UNCHANGED, exit 42, and emit EIGHT warnings at THE SAME EIGHT line:col POSITIONS declared below — 107:5 108:5 112:18 117:17 121:28 126:27 138:27 139:24, identical on both, COLUMNS included. A reference agreeing with `expectWarnings` caret for caret, not merely on the verdict. clang needs `ADD_OPT` and `OPT_BARE_FN` removed and the `d` addend zeroed, after which it exits 33 (= 42 − 9) with THREE warnings, being silent about `__VA_ARGS__` at its default settings; cl cannot build the file at all for the erasure reason above, while still voting ACCEPT on the constraint itself (C5100/C5108 are warnings). ★★ WHY `expectWarnings` AND NOT TWO INSTRUMENTS: lane `fw` landed that key in BOTH runners hours earlier in this same cycle and it is exactly the shape of this claim — the compile SUCCEEDS, the artifact is still built, spawned and checked, AND the declared codes were emitted at Warning severity at the declared positions. The sibling `preprocessor_va_special_identifier_ordinary_code`, written before the key existed, had to split the same claim across the unit suite and an exit code and says so in its own header. ⓘ THE COUNT MODEL IS THE REFERENCES': one report per SOURCE OCCURRENCE, never per expansion — the two declarators and the six replacement lists are eight occurrences, and the six USES of the macros in `main` are not; gcc reports the same eight. An implementation that re-scanned expansion products would report more and still look fail-loud, which is why the assertion is positional. Split of duties: THAT the position is accepted and warned rather than refused is pinned by Preprocessor.SpecialVariadicIdentifierFamilyHasNoSurvivingRefusals (all ten shapes of the family, and the list is now empty on the acceptance axis); the MEANING by Preprocessor.NonVariadicBodyKeepsTheSpellingVerbatim and PreprocessorVaOpt.InANonVariadicFunctionLikeMacroIsInertAndWarns / InAnObjectLikeMacroIsInertAndWarns (renamed and reversed from `FailsLoud`, not deleted); the two ruled divergences by Preprocessor.NonVariadicBodyAcceptanceDivergesFromClangByRuling; the config-drivenness by Preprocessor.NonVariadicBodyAcceptanceIsConfigDriven; that a malformed va-opt INSIDE a variadic macro is still a refusal by the four PreprocessorVaOpt.*FailsLoud tests, which are untouched; that the ARTIFACT BUILDS, RUNS and SAYS SO is pinned HERE and nowhere else. exit 42 = 5 (the catch-all in an object-like body) + 7 (the va-opt spelling in one) + 6 (the catch-all in a non-variadic function-like body) + 9 (the BARE va-opt spelling in one — the shape clang refuses) + 15 (the CONTROL's va-opt operator still FIRES) + 0 (and still ELIDES). Six claims, six addends, none removable without changing the answer. Release pipeline + all four targets, because 'DSS BUILDS AND RUNS THIS' is a per-target claim.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "$commentExpectWarnings": "EIGHT occurrences, and the count is asserted EXACTLY over this code: a ninth report of P_PreprocessorDirective anywhere in the file, or a missing one, or one that moved, is a red. Two of the eight belong to the SIBLING row (the ordinary-code declarators on 107/108, D-PP-VA-SPECIAL-IDENTIFIER-OUTSIDE-REPLACEMENT-LIST) and six to this one; they share a code and differ only in position, which is what makes a positional assertion the right instrument. ⚠ THE CONTROL DRAWS NOTHING AND THAT IS ASSERTED HERE TOO: `PICK` is a VARIADIC macro whose replacement list carries BOTH spellings, the one position 6.10.5p5 permits, and it appears nowhere in this list — an implementation that reported by TOKEN rather than by POSITION would add two entries and red this entry. ✔MEASURED caret-for-caret against gcc 13.3.0 and gcc 13.2.0 (mingw): both emit eight warnings at exactly 107:5 108:5 112:18 117:17 121:28 126:27 138:27 139:24, the same lines AND the same columns DSS reports.",
  "expectWarnings": [
    { "code": "P_PreprocessorDirective", "line": 107, "col": 5,
      "$comment": "int __VA_ARGS__ = 5; — an ordinary declarator, the SIBLING row's position" },
    { "code": "P_PreprocessorDirective", "line": 108, "col": 5,
      "$comment": "int __VA_OPT__  = 7; — the same, other spelling" },
    { "code": "P_PreprocessorDirective", "line": 112, "col": 18,
      "$comment": "(A) #define ARGS_OBJ __VA_ARGS__ — THE SHAPE THE ROW WAS FILED ON; an Error until P54" },
    { "code": "P_PreprocessorDirective", "line": 117, "col": 17,
      "$comment": "(B) #define OPT_OBJ __VA_OPT__ — refused by a different code path (validateVaOpt) for a different stated reason" },
    { "code": "P_PreprocessorDirective", "line": 121, "col": 28,
      "$comment": "(C) #define ADD_ARGS(a) ((a) + __VA_ARGS__) — a NON-VARIADIC function-like body" },
    { "code": "P_PreprocessorDirective", "line": 126, "col": 27,
      "$comment": "(D) #define ADD_OPT(a) ((a) + __VA_OPT__) — a BARE va-opt spelling in a function-like body; clang 18.1.3 refuses this one" },
    { "code": "P_PreprocessorDirective", "line": 138, "col": 27,
      "$comment": "(E) #define OPT_CALL_SHAPE(x) __VA_OPT__(x) — defined, never invoked: the DEFINITION is the claim" },
    { "code": "P_PreprocessorDirective", "line": 139, "col": 24,
      "$comment": "(F) #define OPT_BARE_FN(a) __VA_OPT__ — the second shape clang refuses, and the pin that validateVaOpt returns EARLY for a non-variadic macro rather than losing one arm" }
  ],
  "targets": [
    { "spec": "x86_64:pe64-x86_64-windows-exec", "artifact": "main.exe", "runOn": ["windows"] },
    { "spec": "x86_64:elf64-x86_64-linux-exec",  "artifact": "main",     "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec",  "artifact": "main",     "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main",     "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    {"label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true}
  ]
}

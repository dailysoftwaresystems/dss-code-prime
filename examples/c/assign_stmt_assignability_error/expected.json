{
  "$comment": "D-SEMANTIC-ASSIGN-STMT-ASSIGNABILITY-BYPASS (negative): the assignment STATEMENT `p = d;` (int* <- double) fails loud with a positioned S_TypeMismatch at the RHS (26:9), the SAME check the init/call-arg/return sites run. A valid `p = z;` statement on the line above is unaffected, so exactly ONE diagnostic fires. The witness moved twice because C admits each earlier pair: int<-float is an ordinary arithmetic conversion (D-CSUBSET-INT-FLOAT-CONVERSION), and int*<-char* is an incompatible pointer conversion every reference builds with a warning, which P68 round 9 builds the same way. A double into a pointer is no conversion C has: ✔MEASURED 2026-09-23, gcc 13.3.0, clang 18.1.3 and mingw-w64 13.2.0 at -std=c17 -pedantic-errors and -std=c2x, and MSVC 19.51 at /std:c17 and /std:clatest (C2440), each refuses it. RED-ON-DISABLE: remove the assignment-statement isAssignable arm (restore the bypass) -> `p = d;` is silently accepted and the diagnostic set is empty -> mismatch.",
  "language": "c",
  "source": "main.c",
  "targets": [ { "spec": "x86_64:elf64-x86_64-linux-exec" } ],
  "expectDiagnostics": [ { "code": "S_TypeMismatch", "line": 26, "col": 9 } ]
}

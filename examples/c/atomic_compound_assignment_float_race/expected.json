{
  "$comment": "D-C-ATOMIC-COMPOUND-ASSIGNMENT-AND-INCREMENT-ARE-A-LOAD-THEN-A-SEPARATE-STORE: the THREADED witness for FLOATING `_Atomic` objects. Every reference makes `d += 1.0` on an `_Atomic double` work under contention (MEASURED: a two-thread race over `d += 1.0`, `f += 1.0f`, `++dd` and a three-step update exits clean on gcc 13.3.0 and clang 18.1.3 at -O0/-O2 and MSVC 19.51 `/experimental:c11atomics` at /Od and /O2, three runs each), while DSS lowered NO fenced access of a floating `_Atomic` at all — every load and store was refused in mir_to_lir. A floating object's load, store and read-modify-write now go through its same-width INTEGER representation: the address is retyped, the integer atomic op the targets already realize runs, and a Bitcast crosses register classes at each end. ★ THE COMMIT COMPARES BITS: C23 7.17.7.4 defines compare-exchange over the contents of memory, so a NaN commits exactly once (a value-compare loop would HANG on arm 8, never fail) and the sign of zero survives (arm 10). ★ EXACT VALUES: every sum is an integer far below 2^53 (2^24 for float), so only a lost update can move a verdict. Also pinned: `+=`/`-=`/`*=`/`/=`/`++`/`--` values on double and float locals, and `atomic_load_explicit`/`atomic_store_explicit` on a double. ⚠ FOLD-RESISTANT: every access is a side-effecting atomic op and the NaN is computed from a mutable global. exit = 42.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "expectedStdout": "",
  "targets": [
    { "spec": "x86_64:pe64-x86_64-windows-exec", "artifact": "main.exe", "runOn": ["windows"] },
    { "spec": "x86_64:elf64-x86_64-linux-exec", "artifact": "main", "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec", "artifact": "main", "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    { "label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true }
  ]
}

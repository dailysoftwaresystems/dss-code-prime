{
  "$comment": "D-CSUBSET-DARWIN-BSD-SYMBOL-CLUSTER — strlcpy/strlcat shipped macho-only in string.json (SDK _string.h:201-202; consumers: sqlite os_unix.c proxy locking, strlcpy from :7434 + strlcat from :7443, all under __APPLE__ && SQLITE_ENABLE_LOCKING_STYLE). Runtime witness that the BSD RETURN VALUES cross the real libSystem ABI — source length 5 and total length 8, never the dest pointer — both feeding the exit arithmetic (int)(n1*n2)+2 = 5*8+2 = 42; the FFI calls are opaque to the optimizer so nothing folds, and a strcmp guard pins the write side. RED-ON-DISABLE: delete either row from src/dss-config/shippedLibs/string.json → S0001 undeclared identifier, no binary (tests/ffi RealStringJsonStrlcpyStrlcatMachoOnly is the structural twin). Single arm64-macho target → the macos-latest CI leg; the release arm runs the optimizer over the same opaque calls.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    { "label": "release", "shippedPipeline": "release" }
  ]
}

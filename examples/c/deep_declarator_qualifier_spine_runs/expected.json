{
  "$comment": "P55 D-SEMANTIC-DEPTH-CAPS-TRUNCATE-INTO-TWO-WRONG-ANSWERS (positive, runnable): the OVER-REFUSAL control for `deep_declarator_qualifier_redecl_error`. Every declaration is written twice and IDENTICALLY at a depth past one of the two caps `declaratorConstSpine` used to truncate at — five levels of nested function-pointer parameters and twenty nested parenthesized declarators — and the tower is CALLED, so the artifact witnesses the deep shapes end to end rather than only the redeclaration oracle. It is the direction that matters most on this row: the sibling half of the same defect FABRICATED an S_IncompatibleRedeclaration on a legal 17-level pointer chain, MEASURED, where all four references accept 63. ✔gcc 13.3.0, clang 18.1.3 and mingw-w64 gcc 13.2.0 all compile this source `-pedantic-errors -Wall -Wextra` and RUN it to exit 0; MSVC 19.51.36252 `/std:clatest /c` accepts it — each probed separately. The `release` arm proves the deep declarators survive the shipped optimizer: it is NOT byte-identical to the baseline on pe64 or elf64 (MEASURED) and still exits 0. exitCode 0 = ('d' == 100 delivered through five nesting levels) AND ('k' read back through the twenty-paren `char **`). RED-ON-DISABLE: make the qualifier spine refuse — or misplace a level in — a deep declarator and this stops compiling; make the removed caps come back and the negative twin stops reporting.",
  "language": "c",
  "source": "main.c",
  "exitCode": 0,
  "targets": [
    {
      "spec": "x86_64:pe64-x86_64-windows-exec",
      "artifact": "main.exe",
      "runOn": ["windows"]
    },
    {
      "spec": "x86_64:elf64-x86_64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"]
    },
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"],
      "emulator": "qemu-aarch64"
    },
    {
      "spec": "arm64:macho64-arm64-darwin-exec",
      "artifact": "main",
      "runOn": ["darwin"],
      "emulator": ""
    }
  ],
  "optimizedPipelines": [
    {"label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true}
  ]
}

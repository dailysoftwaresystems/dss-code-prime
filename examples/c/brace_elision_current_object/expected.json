{
  "$comment": "P68 round 9 (lane `cs`): the CURRENT OBJECT of a brace list (C 6.7.9p17-p20) — brace elision into arrays, structures and a union's first member, an unknown size counted in the rows elision fills, the continuation after a deep designator staying inside its aggregate (and leaving it when it is complete), a designator through a union member and through an anonymous union member, an unnamed bit-field skipped, a string and a structure value initializing their subobject whole, a braced row after elided ones, a compound literal, and file scope. ✔MEASURED 2026-09-23, each reference separately and this file compiled and RUN: gcc 13.3.0 and clang 18.1.3 at -std=c17 -pedantic-errors and -std=c2x, mingw-w64 13.2.0 at both, MSVC 19.51 at /std:c17 and /std:clatest — all exit 42 with no diagnostic. Before the placement cursor (✔MEASURED, the lane's fold-2 build) DSS refused this file: an elided list met its aggregate slot as a scalar (H_VerifierFailure / S_ExcessInitializerElements), a union list of two elements was refused, and in separate probes `{.u.i = 13, 14}` DROPPED the 13 and `{.i.a = 7, 8}` stored 8 in `o.c` — both silently. exit 42 = every element placed where C places it; any other exit counts the misplaced lines.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    {
      "spec": "x86_64:pe64-x86_64-windows-exec",
      "artifact": "main.exe",
      "runOn": ["windows"]
    },
    {
      "spec": "x86_64:elf64-x86_64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"]
    },
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"],
      "emulator": "qemu-aarch64"
    },
    {
      "spec": "arm64:macho64-arm64-darwin-exec",
      "artifact": "main",
      "runOn": ["darwin"],
      "emulator": ""
    }
  ],
  "optimizedPipelines": [
    {
      "label": "release",
      "shippedPipeline": "release",
      "mustDifferFromBaseline": true
    }
  ]
}

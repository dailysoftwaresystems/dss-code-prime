{
  "$comment": "D-CSUBSET-INT128-ICE-CONTEXT-REFUSED / D-CE-ASINT64-REJECTS-BY-WIDTH-NOT-MAGNITUDE — a 128-bit (or wide `_BitInt`) CONSTANT whose VALUE fits an int64 is admissible in every 64-bit integer-constant-expression slot: array bound (C 6.7.6.2p1), enumerator (6.7.2.2p2), bit-field width (6.7.2.1p4). MEASURED before the fix, through this same CLI on x86_64:elf64-x86_64-linux: `int a[(__int128)2 + 1];` → S_NonConstantArrayLength, `enum { EA = (__int128)7 }` → S000F, `unsigned x : (__int128)3;` → S001F; gcc 13.3.0 `-std=c2x` and clang 18.1.3 `-std=c23`, probed SEPARATELY, accept all three (and clang additionally accepts the `_BitInt(128)`/`_BitInt(200)` bounds gcc 13 cannot parse at all, which is the disjunction rule deciding it). ★ EVERY TERM ASSERTS A VALUE, NEVER MERE ACCEPTANCE: the failure this pins against is a wide constant silently contributing its LOW 64 BITS, and a bound that is merely present would pass — so array lengths are read back through `sizeof`, enumerators through arithmetic over all four, and bit-field widths through values that only fit at the declared width. The four terms are MULTIPLICATIVE, so one wrong value drives the exit away from 42 rather than being absorbed. t4 additionally writes at the LAST index of every array and reads it back, so `sizeof` and the actual allocation cannot disagree. ⓘ Two bounds (`e`, `f`) and one enumerator (`ED`) route through an intermediate that genuinely EXCEEDS 64 bits (or is negative) before coming back into range, so a fold that bailed at the first wide intermediate — rather than testing the FINAL value's magnitude — cannot produce them. ⚠ THE COMPLEMENTARY HALF IS `examples/c/c_int128_ice_slot_overflow_error`, and it is required: accepting the fitting values without still REFUSING the non-fitting ones is the silent truncation this row exists to prevent, and it would look green here. RED-ON-DISABLE: restore `asInt64`'s width test in `src/hir/const_eval_arith.hpp` (`if (p->width() <= 64) return p->asI64(); return std::nullopt;`) and this example stops COMPILING — every bound, enumerator and bit-field width above refuses. The release arm carries the same terms through the shipped optimizer, where the bounds are already frozen into the layout and the runtime reads must survive mem2reg/const-fold.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    {
      "spec": "x86_64:pe64-x86_64-windows-exec",
      "artifact": "main.exe",
      "runOn": ["windows"]
    },
    {
      "spec": "x86_64:elf64-x86_64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"]
    },
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"],
      "emulator": "qemu-aarch64"
    },
    {
      "spec": "arm64:macho64-arm64-darwin-exec",
      "artifact": "main",
      "runOn": ["darwin"],
      "emulator": ""
    }
  ],
  "optimizedPipelines": [
    {"label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true}
  ]
}

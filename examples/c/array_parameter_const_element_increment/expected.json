{
  "$comment": "P68 round 9 (lane `cs`) — the round's P0 merge blocker. C 6.7.6.3p7 adjusts an array parameter to a pointer qualified by ONLY the qualifiers written inside its bracket; the element keeps its own. So `Tcl_Obj *CONST objv[]` is a MODIFIABLE `Tcl_Obj *const *` and `objv++` is legal — sqlite's Tcl harness shape (src/test1.c, ext/session/test_session.c), which DSS refused S_ConstViolation on every leg (the refusal came from round 8's `++`/`--` const check meeting an adjustment that took the ELEMENT's const for the pointer's), so no sqlite testfixture built. ✔MEASURED 2026-09-23, this file compiled and RUN on each reference separately: gcc 13.3.0 and mingw-w64 13.2.0 at -std=c2x, clang 18.1.3 at -std=c17 -pedantic-errors and -std=c2x, and MSVC 19.51 at /std:c17 and /std:clatest all exit 42; gcc and mingw at -std=c17 -pedantic-errors refuse only the 2-D call (`int (*)[2]` into `const int (*)[2]`, the pre-C2X array-qualifier rule). Four increments, each observed through the advanced pointer: the element-const pointer array behind sqlite's macro, `const int p[]`, a typedef'd array `const A4 p` (its `const` qualifies the ELEMENT, C 6.7.3p10), and a 2-D `const int p[][2]` stepped by a row. exit 42 = 10 + 10 + 10 + 12.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    {
      "spec": "x86_64:pe64-x86_64-windows-exec",
      "artifact": "main.exe",
      "runOn": ["windows"]
    },
    {
      "spec": "x86_64:elf64-x86_64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"]
    },
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"],
      "emulator": "qemu-aarch64"
    },
    {
      "spec": "arm64:macho64-arm64-darwin-exec",
      "artifact": "main",
      "runOn": ["darwin"],
      "emulator": ""
    }
  ],
  "optimizedPipelines": [
    {"label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true}
  ]
}

{
  "$comment": "D-PP-VA-SPECIAL-IDENTIFIER-OUTSIDE-REPLACEMENT-LIST (P54 lane va) — C23 6.10.5p5 in ORDINARY PROGRAM TEXT. DSS enforced the constraint only inside a `#define`; every other position was accepted in silence. ✔MEASURED 2026-09-02, each reference invoked SEPARATELY, compile-only, over the whole position axis (declarator name, struct member, label, variadic-macro ARGUMENT, `#if` operand): gcc 13.3.0 (WSL), gcc 13.2.0 (mingw-w64), clang 18.1.3 and cl 19.51.36252 EACH diagnose and EACH exit 0 at their DEFAULT settings; only `-pedantic-errors` / `/W4 /WX` make it fatal. The union decides ACCEPTANCE, so refusing would put DSS ABOVE the union and silence left it BELOW on the diagnostic axis — DSS now WARNS and BUILDS, with `--warnings-as-errors` reproducing the references' strict arm. ⚠⚠ THIS EXACT SOURCE was cross-checked against the references: gcc 13.3.0 (WSL), gcc 13.2.0 (mingw-w64) and clang 18.1.3 each BUILD it at default settings and the program exits 42 — the same answer DSS gives. cl 19.51.36252 does NOT build it, because MSVC treats both spellings as built-in macros that EXPAND TO NOTHING (`int __VA_ARGS__ = 20;` becomes `int = 20;`, error C2513); MSVC's own diagnostic FOR THE CONSTRAINT is still only a WARNING (C5100/C5108) and it exits 0 on every shape where that erasure does not break the syntax, so its ACCEPTANCE vote is with the others and only the MEANING differs. DSS follows gcc and clang, which is the meaning it already had. ⚠ THE ROW'S OWN HEADLINE ('clang-18/clang-19/gcc-13 reject') was REFUTED: it holds only under -pedantic-errors, and taking it at face value would have shipped a refusal that makes this entry fail to build. THIS ENTRY IS RUNNABLE ON PURPOSE and deliberately declares NO `expectDiagnostics`: ✔MEASURED via sibling const_bitfield_write_warns, the runner treats any manifest carrying that key as an EXPECT-ERROR entry, asserts the compile was REJECTED and never runs the artifact — the exact verdict this shape must NOT draw. The split of duties is therefore: THAT a diagnostic is emitted, that it is a WARNING, and that it fires once per OCCURRENCE is pinned by Preprocessor.SpecialVariadicIdentifierOutsideReplacementListWarns (which asserts severity AND count); that the LEGITIMATE position and an ELIDED branch stay silent is pinned by Preprocessor.SpecialVariadicIdentifierLegitimatePositionsStaySilent; that the program is still BUILT and still computes the right answer is pinned HERE, by an artifact that executes and returns 42 on four targets. RED-ON-DISABLE, BOTH DIRECTIONS RUN: in the REMOVE direction (delete the two call sites) the four `Preprocessor.SpecialVariadic*` / `VaArgsNameAsMacroName*` unit tests go red BY NAME while THIS ENTRY STAYS GREEN — correct and deliberate, since deleting a warning leaves the program buildable and an exit-42 assertion cannot see it; in the REFUSE direction (flip the diagnostic's severity to Error) ✔MEASURED that BOTH runners fail this entry with `compile rc == 0 (rc=1)`, quoting `error[P_PreprocessorDirective]` for both spellings, so exit 42 becomes unreachable. Object md5 pinned and asserted MOVED then RETURNED in both transcripts. exit 42 = 20 (file-scope `__VA_ARGS__`) + 15 (file-scope `__VA_OPT__`) + 4 + 3 (struct members) + 0 + 0 (the legitimate `__VA_OPT__(+)` expansion and the macro-ARGUMENT position), reached through a label spelled `__VA_ARGS__`. Release pipeline + all four targets, because the claim is 'DSS BUILDS AND RUNS THIS' and that claim is per-target.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    { "spec": "x86_64:pe64-x86_64-windows-exec", "artifact": "main.exe", "runOn": ["windows"] },
    { "spec": "x86_64:elf64-x86_64-linux-exec",  "artifact": "main",     "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec",  "artifact": "main",     "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main",     "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    {"label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true}
  ]
}

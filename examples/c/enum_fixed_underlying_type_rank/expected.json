{
  "$comment": "P68 round 12 (lane `cs`, D-C-AN-ENUMS-FIXED-UNDERLYING-TYPE-LOST-ITS-NAME-IN-THE-CONVERSIONS): C23 6.7.2.2 and C 6.3.1.1p1 give an enumeration with a FIXED underlying type that type's conversion rank, and rank is by name — `enum E : long` beside `unsigned int` converts as `long` does: `unsigned long` on LLP64 (the fifth usual arithmetic conversion), `long` on LP64 (the fourth); the enumeration constant has the enumerated type; an enum with no fixed type is the `int` control; the values' signedness agrees; and two QUALIFIED clauses (`enum Q : const long`, `enum R : long volatile`) are `long` enums as well, C23 6.7.2.2 making the underlying type the unqualified version of what the clause names. MEASURED 2026-09-24, this file compiled and RUN on each reference separately: gcc 13.3.0, clang 18.1.3 and mingw-w64 13.2.0 at -std=c2x exit 42 with no diagnostic (their -std=c17 -pedantic-errors modes refuse the C23 syntax); MSVC 19.51 has no fixed underlying types (C2059) and abstains. Before this round DSS kept only the underlying KIND on the enum and typed `e + u` `unsigned int` on LLP64 and an unnamed 64-bit type on LP64. exit 42 = every type and value is C's on this target's data model.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    {
      "spec": "x86_64:pe64-x86_64-windows-exec",
      "artifact": "main.exe",
      "runOn": ["windows"]
    },
    {
      "spec": "x86_64:elf64-x86_64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"]
    },
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": ["linux"],
      "emulator": "qemu-aarch64"
    },
    {
      "spec": "arm64:macho64-arm64-darwin-exec",
      "artifact": "main",
      "runOn": ["darwin"],
      "emulator": ""
    }
  ],
  "optimizedPipelines": [
    {
      "label": "release",
      "shippedPipeline": "release",
      "mustDifferFromBaseline": true
    }
  ]
}

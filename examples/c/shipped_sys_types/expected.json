{
  "$comment": "Shipped <sys/types.h> POSIX typedefs + the FIRST subdirectory descriptor (sys/types.json). off_t/ssize_t/time_t -> i64, mode_t -> u32; exit 42 (30+2+10+0) witnesses the subdir resolution + typedef injection on the unix ABIs (elf x86-64 + arm64-qemu + macho). time_t is re-provided by sys/types.json (glibc <sys/types.h> pulls it via <bits/types/time_t.h>) and this TU does NOT include <time.h>, so time_t here is a SECOND red-on-disable edge (drop it from the descriptor -> `time_t when` fails S0006, no binary). NO windows-pe arm: this TU is a UNIX-ABI witness (off_t/ssize_t/time_t are LP64 here). ⚠ STALE CLAIM CORRECTED 2026-07-29 (TF-C90): this comment used to say `<sys/types.h>` is declared `availableObjectFormats:[elf,macho]` so a pe compile FAILS LOUD. It is NOT — `sys/types.json` declares `[elf,macho,pe]` (its own $comment records the pe/LLP64 arms added for ssize_t), so a pe `#include <sys/types.h>` resolves fine. The dedicated fail-loud availability witness shipped_header_unavailable_pe uses `<sys/time.h>`, which IS `[elf,macho]` — not this header. The pe side of THIS descriptor is witnessed instead (D-CSUBSET-SYS-TYPES-BSD-SPELLING-GROUP-ABSENT) by the unit test ShippedLibDescriptor.RealSysTypesBsdSpellingGroupPerFormat (the BSD-compat group has no pe variants, so those NAMES are absent while the header itself is available). RED-on-disable: without the resolveSystemDescriptor subdir-preserving fix, <sys/types.h> maps to a non-existent types.json and the header fails to resolve -> compile fails. The release arm confirms the full shipped pipeline.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "targets": [
    { "spec": "x86_64:elf64-x86_64-linux-exec",  "artifact": "main", "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec",  "artifact": "main", "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    { "label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true }
  ]
}

{
  "$comment": "D-C-ATOMIC-COMPOUND-ASSIGNMENT-AND-INCREMENT-ARE-A-LOAD-THEN-A-SEPARATE-STORE: the VALUE semantics of every compound operator, `++` and `--` on an `_Atomic` object through the read-modify-write they now lower to (a HIR `ReadModifyWrite`, the same AtomicCas retry loop the `atomic_fetch_*` family uses). The race witnesses prove no update is LOST; this proves each update COMPUTES the right value, because the lowering was replaced rather than patched: all ten compound operators (each checking the value yielded AND the value left), prefix (new) and postfix (old) yields, sub-word objects wrapping at their own width (signed char, unsigned short, `_Bool` including `(bool)(0 - 1)`), a 64-bit object, an enumeration, a pointer object whose step scales by the pointee, a floating operand on an integer object (C23 6.5.17.3p4's `x = x + (1.5)`: -2 becomes 0 — MEASURED gcc 13.3.0 agrees, clang 18.1.3 lowers it to `lock add` of `(int)1.5` and leaves -1, a meaning its own non-atomic `+=` does not have), E2 evaluated exactly once although the loop may re-run, a member, an element with a side-effecting index, a dereference, an `_Atomic` parameter, and an `_Atomic` local stepped in a for-update clause. ⚠ FOLD-RESISTANT: every access is a side-effecting atomic op, so the `release` arm computes the same values at run time. exit = 42.",
  "language": "c",
  "source": "main.c",
  "exitCode": 42,
  "expectedStdout": "",
  "targets": [
    { "spec": "x86_64:pe64-x86_64-windows-exec", "artifact": "main.exe", "runOn": ["windows"] },
    { "spec": "x86_64:elf64-x86_64-linux-exec", "artifact": "main", "runOn": ["linux"] },
    { "spec": "arm64:elf64-aarch64-linux-exec", "artifact": "main", "runOn": ["linux"], "emulator": "qemu-aarch64" },
    { "spec": "arm64:macho64-arm64-darwin-exec", "artifact": "main", "runOn": ["darwin"], "emulator": "" }
  ],
  "optimizedPipelines": [
    { "label": "release", "shippedPipeline": "release", "mustDifferFromBaseline": true }
  ]
}

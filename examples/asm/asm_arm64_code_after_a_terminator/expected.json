{
  "$comment": "★★ CODE AFTER A TERMINATOR ON arm64 (P68 round 8, D-ASM-INSTRUCTION-AFTER-A-TERMINATOR-REFUSED and D-ASM-CONDITIONAL-BRANCH-FALLTHROUGH-LAID-OUT-AT-THE-FUNCTION-END) — the AT&T sibling's claim on the second dialect. A line after `b`, a line after a mid-function `ret`, and a `nop` + `add` tail after the LAST `ret`, each dead and each changing the exit code if it ran; `b.ne 2f` falls into the next line. ✔MEASURED 2026-09-23: aarch64-linux-gnu-as 2.42 and clang 18.1.3 (`--target=aarch64-linux-gnu`) run this file to 42 under qemu-aarch64; DSS refused the first dead line. ★ RED-ON-DISABLE: create the anonymous blocks after the labels again and the block-plan pin reds. The `release` arm pins only that naming the release pipeline does not break the assembly path.",
  "language": "asm-arm64-gas",
  "source": "main.s",
  "exitCode": 42,
  "targets": [
    {
      "spec": "arm64:elf64-aarch64-linux-exec",
      "artifact": "main",
      "runOn": [
        "linux"
      ],
      "emulator": "qemu-aarch64"
    }
  ],
  "optimizedPipelines": [
    {
      "label": "release",
      "shippedPipeline": "release"
    }
  ]
}

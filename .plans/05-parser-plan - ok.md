# Parser Phase — Sub-Plan

> Opens parent plan #7 (`analysis-syntactic`). The piece that consumes the live `TokenStream` from the tokenizer and drives `TreeBuilder::open` / `pushToken` / `Checkpoint` from the compiled shape graph. Closes `v2-gap-catalog - tbd.md` row 1 end-to-end (operator-table data ✅, tree shape ⏳ until this phase lands).
>
> **Post-close amendment (CU2, [`08-compilation-unit-plan`](./08-compilation-unit-plan%20-%20tbd.md) §2.6 C2-L1):** the `Parser` constructor gains an optional trailing `std::unique_ptr<DiagnosticReporter> lexerDiagnostics = nullptr`. When non-null, the parser folds the tokenizer's lexer diagnostics into the builder's reporter (via `TreeBuilder::ingestDiagnostics`) before the walk, so the finished `Tree` owns lexer + parser diagnostics in one stream. Additive (defaulted param); existing callers (LSP, tests) are unchanged. Lets the LSP later stop dropping lexer diagnostics by passing its `lexDiags` through.

## 0. Status (snapshot)

| | |
|---|---|
| Status        | ✅ **PARSER PHASE CLOSED — all 7 PRs done: PA0 + PA1 + PA2 + PA3 + PA4 + PA5a + PA5b**, plus sub-workitems PA-Walker-LeftRec, PA5a-prep, PA4-followups, and an LSP cross-cutting review fix-everything pass on the integrated stack. 56 ctest suites green (post-08.55). **Post-close substrate finding 2026-05-28** (memory `project_pratt_walker_speculation_gap_2026_05_28`): the Pratt walker's primary-parsing path bypasses `stepOnce`'s `AltChoice` dispatch, so `"speculative": true` on an `expr`-shape's atom rule is silently ignored. Empirically proven: marking c-subset's `operand` `speculative: true` makes `int x = 5;` fail to parse. Blocks D5.3-typedef-compound-literal, D5.2-cycle-2 block-scope alias, and future C-style cast `(T)expr`. **Sub-cycle A** (Pratt walker speculative-atom dispatch) opens as the next parser-phase post-close substrate work — refactors `walkExpression`'s primary-parsing to route through `trySpeculativeBranch` when the atom's alt is marked speculative. Files load-bearing: `parser.cpp:691-737` (trySpeculativeBranch), `:912-916` (Pratt dispatch in stepOnce), `:1263+` (walkExpression primary), `:629-671` (WalkerSnapshot snap/commit/rollback), `grammar_schema.hpp:466-474` (isSpeculativeAlt API). Sub-cycle B (`statement`-level speculation), Sub-cycle C (cast-expressions) explicitly delimited as follow-ups. **Original PR history follows below for archival reference.** PA0 (`SchemaWalker` extraction) shipped as `c764c2a`. PA1 (schema-driven iterative RD driver with try-each-branch speculation) + `SpeculationProbe` RAII follow-up shipped. **PA3 (error recovery + diagnostic UX) shipped + 5-agent review + fix-everything round.** Schema-declared `syncTokens` + true `followSetOf(RuleId)` computed at load time (fixed-point with non-convergence guard); panic-mode recovery scans until `syncTokens ∪ FOLLOW(nearest compiled ancestor) ∪ EOF ∪ lexer-Error`, capped at `ParserConfig::maxSyncScanTokens=64`. `RecoveryStrategy::{SingleToken,PanicMode}` selects the strategy. `panicRecover` inserts an Error leaf at the first bad token + preserves trivia (whitespace/comment nodes survive recovery) + walks the parser's `frameRules` stack to find FOLLOW when the immediate `currentRule()` is a Pratt wrapper. Parser diagnostics now carry full `expected` set (rendered from `expectedSet(cursor)`) and `actual` rendered from source bytes — the existing `DiagnosticReporter::format()` clang-style renderer (already in tree) now produces useful output for every parser-emitted code. Renderer enhanced: multi-char `^^^` caret matching span length. Loader rejects `syncTokens` entries that are Eof/Error/EmptySpace-flagged kinds. New `tests/analysis/syntactic/test_parser_recovery.cpp` (8 ParserRecovery + 1 death) + 4 grammar-schema FOLLOW/syncTokens tests + 2 tree_builder pushErrorNode tests + 2 diagnostic_reporter multi-char-caret tests. 715 tests / 35 suites green. PA2 (Pratt walker for expressions) shipped + 5-agent review + fix-everything round — `DefaultPrattWalker` produces right-recursive trees wrapping infix/postfix/prefix ops in auto-interned `binaryExpr`/`unaryExpr`/`postfixExpr` rules; left-vs-right associativity encoded in the operator table only (not tree shape). Closes v2-gap-catalog row 1 end-to-end — `ParserCSubsetSmoke.FunctionBodyExpressionIsPrecedenceCorrect` pins `int main() { a + b * c; }` parsing to the precedence-correct shape via `Parser::parse()`. Loader auto-interns wrapper rules when ANY shape body (recursively scanned) declares `expr`; loader rejects user-declared shapes named `binaryExpr`/`unaryExpr`/`postfixExpr` (`C_UnknownShape` with "reserved" in the message); `FIRST(expr-rule)` augmented with prefix-op tokens so dispatch `tokInFirst` accepts bare-prefix expressions (`-a;`) without ever entering the walker. Review-fix round addressed: H4 (EOF guard before op-push — `pushOperatorToken` returns false on EOF, walker emits `P_PrematureEndOfInput` and unwinds), H2 (`parseUntilFrameDepth` post-condition — emits `P_PrematureEndOfInput` and closes remaining frames on `Done` mid-atom), M3 (reserved-wrapper-shape rejection), M4 (nested `expr` detection via recursive body scan), convention dups (`walkerFatal`/`isWalkerTrivia`/`walkerEffectiveKind` collapsed into the existing file-level helpers; `_guard`/`d` renamed; wrapper rule IDs use `well_known_names.hpp` constants), wrapper open/close + push-op-token boilerplate extracted to `Impl::openExprFrame`/`closeFrameOnce`/`pushOperatorToken` helpers. 687 tests / 34 suites green (~20 new PA2 cases atop PA1's 668). <!-- LANDING-LOG-HASHES: PA1 --><!-- /LANDING-LOG-HASHES --> |
| Predecessors  | ✅ T5 `TreeBuilder` with checkpoint/rollback; ✅ T6 `TreeCursor` for downstream consumption; ✅ v2 schema with compiled position graph + `expectedSet(cursor)` + `firstSetOf(rule)` + `isNullable(rule)`; ✅ tokenizer with live `TokenStream` + body-mode handling; ✅ **PA0 `SchemaWalker` substrate** (parser embeds its own instance, lock-step with builder's). |
| Successors    | **Phase #7.5** ([`08-compilation-unit-plan - tbd.md`](./08-compilation-unit-plan%20-%20tbd.md)) consumes per-file `Tree` output and bundles into `CompilationUnit` for semantic. The `artifactProfile` plan AP3 plumbs the profile into the `CompilationContext` that wraps parser output. |
| Scope         | **Bounded.** PA0 substrate refactor ✅. PA1: iterative RD driver from the shape graph. PA2: Pratt walker for operators. PA3: error recovery + diagnostic UX. PA4: corpus stress. PA5a/PA5b: LSP scaffolding + semantic-stub method handlers. |

### Entry checklist (must be true before PA1 starts)

- [x] `TreeBuilder::Checkpoint` shipped + tested (PR4).
- [x] `GrammarSchema::advance` / `enterRule` / `leaveRule` / `routeToRuleLeaf` shipped + tested (PR2a + PR2b review).
- [x] `firstSetOf(rule)` / `isNullable(rule)` available for FIRST/FOLLOW disambiguation (PR2a).
- [x] `OperatorTable::lookup(kind, arity)` returns precedence + associativity (PR1).
- [x] `expectedSet(cursor)` returns the set of token kinds valid at the current cursor position (PR2a).
- [x] `TokenStream` consumed by `pushToken` end-to-end (TZ3).
- [x] `bodyDefaultTokenKinds_` cursor-skip in the builder so body-mode tokens don't desync the cursor (TZ3 + TZ3 review-fix r1).
- [x] **`SchemaWalker` substrate shipped** (PA0). Parser embeds its own instance + drives lock-step with builder's; opaque move-only `Snapshot` for speculation; fatal-abort on contract violations (`enterRule(InvalidRule)`, underflow, cross-walker `restore`, throwing desync callback).
- [x] **Closed by PA1**: Does the schema's compiled shape graph carry enough information for a single recursive-descent driver to handle every alt without per-language hand-tuning? **Yes** — empirically verified on toy + c-subset smoke (22 tests green). The driver handles TokenLeaf + RuleLeaf + AltChoice (speculative and non-speculative) + End-slot with no per-language code; the only schema-side extensions PA1 needed were `nullableTail` / `nullableBranch` accessors (added under PA1's substrate diff). The c-subset call/array/postfix gaps land in PA4 corpus stress and are operator-table / shape work, not driver work.

---

## 1. Motivation

Master plan §4.6.2 says phase #7 implements `SyntaxAnalyzer` + `RecursiveDescentParser` + `PrattExpressionParser`. The v2 design pivot made that more specific:

1. **The parser is schema-driven, not hand-written per language.** A single driver walks the compiled position graph; per-language behavior comes from the schema's shape tables + operator table + alt FIRST sets.
2. **Speculation is the parser's escape hatch** for alts whose FIRST sets overlap (`speculative: true` in the schema). `TreeBuilder::Checkpoint` is the mechanism; the parser is the consumer.
3. **Error recovery is a first-class concern.** Production compilers must keep parsing past syntax errors and produce useful diagnostics. The current `pushError` machinery is the primitive; the parser layers a recovery strategy on top (panic-mode-to-FOLLOW-set, sync-token, partial subtree retention).
4. **Operator precedence is the missing piece.** `v2-gap-catalog - tbd.md` row 1 says the data shipped in PR1 but the tree shape is still wrong because nothing consumes it. The Pratt walker is that consumer.

---

## 2. Design

### 2.1 Files

```
src/analysis/syntactic/
├── CMakeLists.txt
├── parser.hpp / .cpp                # main schema-driven driver
├── pratt_walker.hpp / .cpp          # operator-table-driven expression parser
└── recovery.hpp / .cpp              # panic-mode + sync-token strategy

tests/analysis/syntactic/
├── CMakeLists.txt
├── test_parser_toy.cpp              # toy-language parser pinning
├── test_parser_c_subset.cpp         # c-subset full grammar
├── test_parser_tsql_subset.cpp      # tsql-subset full grammar
├── test_pratt_walker.cpp            # precedence + associativity unit pins
└── test_recovery.cpp                # broken-source recovery quality
```

End-to-end tests stay in `tests/core/` (the current `test_c_subset.cpp` / `test_tsql_subset.cpp`) and are extended to use the real parser instead of hand-driving `TreeBuilder::open` calls.

### 2.2 Public API (proposed)

```cpp
namespace dss {

struct ParseResult {
    Tree tree;                                  // the CST
    // diagnostics live on the tree (`tree.diagnostics()`) — no separate channel
};

class DSS_EXPORT Parser {
public:
    Parser(std::shared_ptr<SourceBuffer>        src,
           std::shared_ptr<GrammarSchema const> schema,
           TokenStream                          tokens,
           ParserConfig                         config = {}) noexcept;

    // Single-use: consumes the parser. Drives TreeBuilder from the root rule.
    [[nodiscard]] ParseResult parse() &&;
};

struct ParserConfig {
    std::size_t maxRecoveryDistance     = 64;   // tokens to scan ahead before giving up
    std::size_t maxSpeculationDepth     = 8;    // distinct from BuilderConfig::maxSpeculationDepth — bounds the parser's own checkpoint nesting
};

} // namespace dss
```

The parser owns the `TokenStream` (move-in) and constructs an internal `TreeBuilder`. Callers don't see the builder directly — that's an implementation detail and would tempt mixing modes.

### 2.3 PR breakdown

| PR  | Title                                              | Scope                                                                                                                                                                                                                                                                                                                                                                       |
|-----|----------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| PA0 | ✅ **done** — Substrate refactor: extract `SchemaWalker` | **Pure refactor; no behavior change.** New `src/core/types/schema_walker.{hpp,cpp}` owns the schema cursor + parent-cursor stack + opaque move-only speculation snapshot + one-shot desync latch — the state machine previously embedded in `TreeBuilder`. `TreeBuilder` keeps its existing public surface but delegates cursor work to an embedded `SchemaWalker walker_` member; the parser (PA1) embeds its own. Two consumers, one canonical state machine — agreement enforced by both being driven lock-step. **Contract violations are fatal-abort** (matches project discipline): `enterRule(InvalidRule)`, `leaveRule` stack underflow, `restore` from a snapshot taken by a different walker (schema pointer-identity guard), throwing desync callback. New `tests/core/test_schema_walker.cpp` (21 tests, 3 death). `Snapshot` non-default-constructible + move-only + opaque — `friend SchemaWalker::snapshot()` is the only construction path; `TreeBuilder::CheckpointSnapshot` holds `std::optional<SchemaWalker::Snapshot>` so it can be built field-by-field. `static_assert(!std::is_move_constructible_v<TreeBuilder>)` pins the `[this]`-capture lifetime of the desync emission lambda. 646 cases / 30 suites, 100% pass; pre-PA0 625 pass byte-identical. Substrate-tier 5-agent review + fix-everything round + 5th-reviewer follow-up round (3 confidence-8/10 items addressed: drop unused include, tighter contract on throwing-callback, non-default-constructible Snapshot). <!-- LANDING-LOG-HASHES: PA0 --><!-- /LANDING-LOG-HASHES --> Shipped as `c764c2a` on `feature/parser-phase` (PR #7); will squash-merge into main per the project's existing convention. |
| PA1 | ✅ **done** — Schema-driven recursive-descent driver | Iterative dispatch loop over the compiled position graph: `TokenLeaf` → expect-and-advance; `RuleLeaf` → enter / skip-nullable (gated by `skipWouldAbandonToken`); `AltChoice` (non-speculative) → FIRST-set + `routeToRuleLeaf` scan; `AltChoice` (speculative) → **try-each-branch** with full snapshot/restore (token bookmark + builder checkpoint + walker snapshot + parser frame stack + diag counter, with delta-basis desync detection); `End` → close frame. Speculation depth bounded by `ParserConfig::maxSpeculationDepth` (default 8, ≥1 enforced in ctor); per-probe budget = 16 × declared lookahead. **`SpeculationProbe` RAII guard landed (follow-up)** — the four-machine snapshot/restore is owned by a move-only nested class whose dtor restores all four state machines + the diag-counter + speculation-depth on any non-committed exit; `commit()` re-baselines the watchdog tuple. The invariant is local + structurally enforced, mirroring `TreeBuilder::Checkpoint`; no behavior change. Closes open question #5. Pratt walker is NOT plumbed in PA1 — PA2 adds the public ctor parameter + dispatch invocation. Forward-progress watchdog fatal-aborts on `(cursor, tokPos, depth)` tuple equality across iterations; speculation commit re-baselines the tuple. **Counter-mirror caveat**: `diagsEmitted` mirrors only parser-emitted recovery diagnostics, NOT builder-side `pushToken` paths (P_UnknownToken / P_SchemaCursorDesync / P_AmbiguousToken) — known + accepted gap, those codes signal informational drift, not commit-blocking errors. New files: `src/analysis/syntactic/parser.{hpp,cpp}`, `src/analysis/syntactic/pratt_walker.hpp`. Tests: 12 toy (5 happy + 3 broken + 2 dispatch-pin + 3 trivia + death) + 6 speculation (commit caseA + commit caseB + bogus + cap + no-committable + empty) + 1 c-subset smoke = 19 + 3 death = **22 parser tests**, all pre-existing 29 suites unchanged. Shipped on `feature/tokenizer-plan` after 5-agent review + fix-everything round addressing ~25 items (top: speculation `isDesynced` delta-basis (C1 from code-reviewer), PrattWalker removed from public ctor (C2), watchdog post-commit re-baseline (I5), abandon-token guard for both AltChoice + RuleLeaf nullable-skip paths (I2), null-check in ctor not parse() (type-design 1), `makeDiag` helper collapsing 8 sites (type-design 10), `expectExactCodes` whitelist expanded to catch `P_BuilderInvariant` / `P_AmbiguousToken` / `P_SchemaCursorDesync` / `P_MaxSpeculationDepth` / `P_RecoveryStalled` / `P_UncommittedCheckpoint`). Existing `test_*_subset.cpp` tests stay manual-driven; PA4 flips them. |
| PA2 | ✅ **done** — Pratt walker for expressions          | `OperatorTable::lookup(kind, arity)` drives precedence climbing via `DefaultPrattWalker`. Three arities supported: `Prefix` (open `unaryExpr` wrapper, push op, recurse at prefix's own precedence), `Infix` (open `binaryExpr` wrapper, recurse LHS at `op.prec+1` so same-prec ops don't gobble, push op, recurse RHS at `op.prec` → right-recursive tree shape), `Postfix` (open `postfixExpr` wrapper, recurse operand at `op.prec+1`, push op). Tree shape is right-recursive regardless of declared associativity — left-vs-right is encoded in the operator table for downstream semantic analysis, not in the tree's structural nesting (documented in `PrattWalker.SamePrecLeftAssocChainIsRightRecursive`). **Walker hand-off**: dispatch's `RuleLeaf` branch checks `schema->isExprRule(nextRule)` and invokes `prattWalker->walkExpression(*outer, nextRule, schema->exprMinPrecedence(nextRule))` BEFORE opening the rule (walker manages its own framing). **Schema layer**: new `CompiledRule::{isExpr,exprAtom,exprMinPrecedence}` populated at compile time; new `GrammarSchema::{isExprRule,exprAtom,exprMinPrecedence}` accessors; loader recursively scans shape bodies for `expr` and auto-interns `binaryExpr`/`unaryExpr`/`postfixExpr` when found; loader rejects user-declared shapes with those reserved names; `firstOfBody(expr)` unions in all schema-token-ids registered as Prefix operators so the dispatch's `tokInFirst` check accepts bare-prefix expressions without ever entering the walker. **Walker mechanics**: rollback-replay algorithm — take a four-machine snapshot (tokens + builder Checkpoint + `SchemaWalker::Snapshot` + frame depth + diag counter) before primary, build primary, peek for op at >= minPrec, if so rollback + re-snap + open wrapper + recursively rebuild deeper. `Impl::snapForWalker`/`rollbackForWalker`/`commitWalkerSnap` are the access points; `pushOperatorToken` returns false on EOF (H4 guard) so the walker emits `P_PrematureEndOfInput` rather than synthesizing an Eof token as the operator; `parseUntilFrameDepth` post-condition closes orphaned frames + emits `P_PrematureEndOfInput` on mid-atom EOF (H2 guard). Wrapper rule frames open a transparent path through the schema walker — wrappers have no compiled body so `enterRule(binaryExpr)` silently invalidates the cursor without firing `P_SchemaCursorDesync` (the `wasValid && !nowValid` check is skipped inside `enterRule`); `leaveRule` recovers cleanly because the saved parent is the still-valid outer frame. `ParserConfig` grew `maxExpressionDepth=256` (recursive C++-stack cap; fatal-abort on overflow) and `std::unique_ptr<PrattWalker> prattWalker` (optional override; default-constructs `DefaultPrattWalker` in `parse()`). **c-subset migration**: `expression` rule body flipped from `sequence:[operand, repeat[sequence:[binaryOp, operand]]]` to `{ "expr": { "atom": "operand" } }`; `binaryOp` rule deleted (operators driven by table). Old hand-driven `CSubsetEndToEnd.ExpressionWithMixedOpsIsLeftFolded` removed (replaced by parser-driven `ParserCSubsetSmoke.FunctionBodyExpressionIsPrecedenceCorrect` which pins the precedence-correct tree via `Parser::parse()`). **Coverage**: 12 cases in `test_pratt_walker.cpp` (mixed precedence, tight-then-loose, same-prec chains, right-assoc chains, bare/embedded prefix, bare/embedded postfix, happy-path-zero-diags, recursion-depth death) + new `MinPrecedenceFloorLeavesLowerOpsUnconsumed` pin (proves loader plumbs `exprMinPrecedence`) + `ParserCSubsetSmoke.{FunctionBodyExpressionIsPrecedenceCorrect, ParenGroupingForcesOuterPrecedence}` (paren re-entrancy: `(a + b) * c` re-triggers walker through the operand's `( expression )` branch) + `GrammarSchema.{ExprShapeAccessorsOnCSubset, WrapperRulesAbsentInNonExprSchema, LoaderRejectsReservedWrapperShapeName, ExprRuleFirstSetIncludesPrefixOperators}` + `ParserToyDeath.ZeroExpressionDepthAborts`. Shipped with 5-agent review + fix-everything round (correctness: H4 EOF op-push guard, H2 parseUntilFrameDepth post-cond; structural: M4 nested-expr detection, M3 reserved-shape rejection; convention: dup helpers collapsed, wrapper open/push-op boilerplate extracted to `Impl` helpers, well-known-names constants used at call sites). 687 tests / 34 suites green. |
| PA3 | ✅ **done** — Error recovery + diagnostic quality   | Panic-mode strategy: on unrecognized token, consume tokens until peek is in `schema->syncTokens()` ∪ `followSetOf(nearest compiled ancestor)` ∪ EOF ∪ lexer-Error, capped at `ParserConfig::maxSyncScanTokens=64`. `ParserConfig::recoveryStrategy` defaults to `RecoveryStrategy::PanicMode`; `SingleToken` is retained as the legacy single-step option for regression-bisect parity. **Schema layer**: new `"syncTokens"` JSON field (validated: must name declared kinds; loader rejects Eof/Error/EmptySpace-flagged); new `GrammarSchema::followSetOf(RuleId)` accessor, computed at load time via fixed-point over the position graph (FOLLOW(R) = ∪{ expectedSet(positions[p.nextPos()]) : p is RuleLeaf(R) }, plus FOLLOW(parent) when nextPos.nullableTail; root rule's FOLLOW stays empty); non-convergence emits `C_CircularShape` rather than silently shipping truncated FOLLOW data. **`Impl::panicRecover`** inserts an Error leaf at the bad token (via new `TreeBuilder::pushErrorNode(span)` — leaf-only insertion, no diagnostic emission, since the parser has already emitted a rich `P_UnexpectedToken`), then scans. Trivia tokens (whitespace, comments) are pushed through the builder during the scan so they survive in the tree; non-trivia is consumed silently to avoid `P_SchemaCursorDesync` per byte. **Pratt-wrapper traversal**: the parser maintains a parallel `frameRules` chain alongside `frames` so `panicRecover` can walk up to find the nearest compiled-body rule's FOLLOW (auto-interned wrapper rules `binaryExpr`/`unaryExpr`/`postfixExpr` have no compiled body, hence empty followSet). **All 6 dispatch recovery sites** now share a single `recoverAt(code, peek, expected)` helper: emit rich diagnostic with `expected` from `walker.expectedSet()` (or rule's FIRST) rendered as quoted prose (`'foo'`), `actual` rendered from `src->slice(peek.span)`, then `panicRecover`. **Diagnostic UX** — the existing `DiagnosticReporter::format()` clang-style renderer (line/column/caret already in tree from earlier work) now sees populated `expected`/`actual`/`ruleContext` fields for every parser-emitted diagnostic; renderer enhanced with multi-char `^^^` underline matching span length (single span-wide error visually highlights the entire bad region). `tools/render_diagnostic.py` deferred — the C++ renderer is sufficient and consumed by both CLI and (future PA5) LSP. **c-subset / toy / tsql-subset** got `syncTokens` arrays added. Shipped on `feature/parser-phase` with 5-agent review + fix-everything round addressing H1 (Error leaf at every recovery site, not just TokenLeaf), H2 (FOLLOW walks frame stack for Pratt wrappers), H3 (lexer-Error tokens are implicit breakpoints), M3 (syncTokens rejects EmptySpace-flagged kinds), M4 (loader diag on FOLLOW non-convergence), M5 (preserve trivia during panic scan), plus render polish (`'<eof>'` quoted consistently, `renderExpectedNames` skips InvalidSchemaToken sentinels). 715 tests / 35 suites green (+28 cases atop PA2). |
| PA4 | ✅ **done** — Real-world stress + corpus tests     | Onboarded `tests/corpus/{c-subset/mini_calc.c, tsql-subset/schema_and_dml.sql, toy/demo.toy}` driven through `Parser::parse()` by a new `tests/analysis/syntactic/test_corpus.cpp` harness (auto-discovers `tests/corpus/<lang>/` under any cwd). **Closes v2-gap-catalog rows 6, 7, 8, 12.** Schema additions: c-subset operator-table grew prefix `*` + postfix `++`/`--`/`(`/`[` with `endsAt`+`bodyRule` for the grouped variants (function calls + array indexing); new `argList` shape (optional, so `f()` parses); tsql-subset gained `--` line + `/* */` block comment modes, `tableRef` (with optional `[AS] alias`), optional column-list in `insertStmt`, and `qualifiedName` in `operand` (covers `u.Name` column refs). **Walker changes** (`src/analysis/syntactic/parser.cpp`): grouped-postfix dispatch in `DefaultPrattWalker` consults the new `Entry::endsAt`/`Entry::bodyRule`; `bodyRule` that resolves to an expr-rule routes through `prattWalker->walkExpression` so operator climb runs inside the body (e.g. `a[i + j * k]`); AltChoice→RuleLeaf scan path also routes expr-rule candidates through the walker (fixes `return f(x);` regression); new off-grammar **body-token drain** at top of `stepOnce` absorbs `StringChar`/`BracketIdChar`/`CommentChar` codepoints as leaves under the current frame without advancing the schema walker (mirrors `TreeBuilder::pushToken`'s body-mode cursor skip; positioned after the watchdog snap so it can't mask dispatch stalls; aborts loudly if a body token fails to advance). Missing-closer recovery in grouped-postfix now drops an Error leaf alongside the diagnostic so `HasError` propagates through the wrap. **Tokenizer fix** (`src/tokenizer/tokenizer.cpp`): three main-mode emit sites (id-start global-hit, word fallback, generic op/punct) now pass `meaning.flagsApplied` so `EmptySpace` declared on lexeme meanings (`//`, `--`, `/*`) reaches `tok.flags` and the parser's `isSkippableTrivia` check. **Loader** (`src/core/types/grammar_schema_json.cpp`): parses `endsAt`/`bodyRule` per operator group; new `validateOperatorBodyRules` post-shapes validator confirms `bodyRule` references a declared shape (walker wrapper rules `binaryExpr`/`unaryExpr`/`postfixExpr` from `well_known_names.hpp` skip the check — they're synthesized by the Pratt walker without a shape). **Existing hand-driven `TsqlSubset` tests** flipped from `TreeBuilder` to `Parser::parse()` for the 5 cases broken by the schema reshape (closes the PA4 acceptance criterion's "flip hand-driven tests" line for the affected subset). Ships on `feature/parser-phase` with 5-agent review + fix-everything round addressing 2 confirmed regressions (`a[i+j*k]` flattening, `f()` rejected), 4 important findings (skip-list now sources from `well_known_names`, drain moved after watchdog snap with forward-progress assert, missing-closer pushes Error leaf, comment overreach trimmed), and follow-up items deferred per the "PA4 follow-ups" subsection below. 36 ctest-suites-at-shipping (now 56 post-08.55), 100% pass. |
| PA5a-prep | ✅ **done** — c-subset PA5a-readiness shape additions | Three v2-gap-catalog rows landed ahead of PA5a so the LSP scenarios produce richer c-subset trees: row 16 (compound assignment `+= -= *= /= %= &= \|= ^= <<= >>=` at precedence-15 right-assoc); row 29 (`extern` keyword + `externDecl` + `externTail` shapes; `topLevel` restructured to `alt` with the original anon-sequence preserved as the first arm); row 12 declarator (`arrayDeclSuffix` shape attached to `varDeclTail`/`varDeclHead`). 9 new smoke tests + 1 broken-path recovery test + 5 new corpus lines in `mini_calc.c` (including empty-bracket `int g_pending[];`). Shipped with 5-agent review + fix-everything round (compound-assign family coverage via `<<=` + right-assoc pin, externTail broken-path test, smoke tests upgraded to full `prettyPrintSubtree`, catalog over-claim trimmed, two comment wording fixes). Also flipped v2-gap-catalog rows 18 (longest-match) and the substrate half of row 32 (DiagnosticReporter::format) to closed — both shipped earlier without catalog reflection. 36 ctest-suites-at-shipping (now 56 post-08.55), 100% pass. |
| PA5a | ✅ **done** — LSP server skeleton + diagnostics    | Standalone language-server binary delivering editor diagnostics from the parser pipeline. **Phase 1 shipped** as `61660b0` (protocol types + JSON-RPC framing + UTF-8→UTF-16 column conversion). **Phase 2-6 + 5-agent review + fix-everything pass shipped** as this commit on `feature/parser-phase`. **Architecture**: Clean DI — `LspServer` ctor injects `unique_ptr<LspTransport>` (StdioTransport prod / InMemoryTransport tests) + `unique_ptr<IExecutor>` (ThreadPool prod / SynchronousExecutor tests) + `SchemaCache&`. **Components**: `src/lsp/transport.{hpp,cpp}` (pimpl stdio + close-aware writeMessage), `src/lsp/method_dispatcher.{hpp,cpp}` (LSP §3.1 fallbacks: unknown request → -32601, unknown notification → silent drop), `src/lsp/schema_cache.{hpp,cpp}` (two-mode resolution: shipped name vs `--schema-dir=PATH`; double-checked locking; lazy extension index), `src/lsp/diagnostic_translator.{hpp,cpp}` (dss `ParseDiagnostic` UTF-8 byte spans → LSP `Diagnostic` UTF-16 code units), `src/lsp/thread_pool.{hpp,cpp}` (`IExecutor` + `ThreadPool` + `SynchronousExecutor`), `src/lsp/document_store.{hpp,cpp}` (per-URI state + monotonic `parseGeneration` cancellation token; stale workers silently dropped via `setDiagnostics` returning false), `src/lsp/lsp_server.{hpp,cpp}` (composition root; advertises `positionEncoding: "utf-16"` + `textDocumentSync: 1`). **CLI**: `src/program/program.cpp` gains `--lsp` + `--schema-dir=<path>` flags; worker count = `clamp(hardware_concurrency, 1, 8)`. **Tests**: 10 test executables (38 cases) — utf16_column (8), json_rpc (16), protocol (4), method_dispatcher (6), document_store (8), thread_pool (4), schema_cache (5), diagnostic_translator (5), lsp_server_e2e (5 incl. parse-error + didChange version monotonicity), lsp_replay (2 golden-file sessions). Two `tests/lsp/sessions/*.in.jsonl`/`*.out.jsonl` golden pairs (`initialize_basic`, `toy_didopen_close`); `DSS_REFRESH_GOLDENS=1` to refresh. **Feature-tier 5-agent review + fix-everything** addressed: shutdown→exit race (handleExit_ drains executor BEFORE closing transport; `StdioTransport::writeMessage` short-circuits on observed close), unenforced `-32600` doc claim struck, ThreadPool exception logging to stderr (was silently swallowed), `lineByteRangeFor` unsigned underflow guard, e2e test exact-count strengthening + parse-error + didChange tests added, project-rule comment violations cleaned (no PA5a-task references, no inaccurate file paths, no narrate-the-obvious blocks). **Deferred** (file as PA5a-followups when triggered): `MessagePayload` strong-type wrapper (propagation change); LspServer lifecycle phase enum vs two atomics; factory-only construction for `Request`/`Range`/`Diagnostic`; DocumentStore concurrency stress test; deferred-executor stale-gen e2e (unit-covered today). **Acceptance:** 46 ctest suites, 100% pass. **Future-work anchor**: schema-map / source-to-target-language translation lives in [`00-compiler-implementation-plan - tbd.md`](./00-compiler-implementation-plan%20-%20tbd.md) §9 "Long-running design notes"; PA5a's two-mode schema resolver is the load-bearing scaffold for that future work. |
| PA-Walker-LeftRec | ✅ **done** — Walker postfix-chain redesign     | New substrate primitive `TreeBuilder::wrapLastChildInFrame(RuleId)` pops the current frame's most-recent pending child, opens a new frame, and re-attaches the popped subtree as the new frame's first child — the left-recursive build operation the rollback-replay strategy couldn't express. Walker `parseExpressionAt`'s postfix branch rewritten to use `wrapLastChildExprFrame` (no rollback, no recursive `parseExpressionAt`); the pre-primary `snap` is INTENTIONALLY NOT advanced across postfix iters so a later same-level infix iter (`f(a) + g(b)`) can still roll back through the chain and rebuild it inside the binaryExpr. Closes the `DISABLED_PostfixChainNestsLeftToRight` tombstone (now enabled with the full nested-`prettyPrint` pin) + adds chain tests for `a[i][j][k]`, `f()(g)`, `*p[i]++`, `(f(a)[i])`, broken-chain `f(a)[i }`, and `f(a) + g(b)` postfix-then-infix. **Substrate-tier** 5-agent review + fix-everything addressed: exception-safety try/catch around the pop/open window (strong-EH for `std::bad_alloc`), 4 new unit tests for `wrapLastChildInFrame` in `test_tree_builder.cpp` (positive + 2 invariant-violation + span semantics), upgraded chain tests from weak `postfixCount` predicates to full `prettyPrintSubtree` pins, factored `Parser::Impl::pushFrameBookkeeping_` shared between `openExprFrame` and `wrapLastChildExprFrame`, "top frame only" invariant added to the builder API doc, PR-tracking references trimmed from test comments. Shipped on `feature/parser-phase` as `8eee6b0` + `869fb8e`. 36 ctest-suites-at-shipping (now 56 post-08.55) green. |
| PA5b | ✅ **done** — LSP semantic-stub method handlers   | Six `textDocument/{hover,completion,definition,references,rename,signatureHelp}` request handlers added via a data-driven `kStubs` table in `LspServer::registerHandlers_`; each returns its LSP-spec default (`null` for all except `references` → `[]`) until later semantic-analysis work swaps individual entries to real handlers. `Method` enum + `parseMethod`/`methodName` extended with the 6 wire strings. `initialize.capabilities` advertises `{hover,definition,references,rename}Provider:true` and `{completion,signatureHelp}Provider:{}` (LSP §10.18/§10.20 mandate the options-object form for these two — comment in code documents the asymmetry). **Tests** (added on top of PA5a's 10 executables): 6 stub-result e2e cases + 1 capabilities-advertised case in `test_lsp_server_e2e.cpp` (all using parsed-JSON equality via `nlohmann::json::parse` per CLAUDE skill §7); 6 golden-file replay sessions (`sessions/{hover,completion,definition,references,rename,signature_help}_stub.in.jsonl` + `.out.jsonl`); `test_protocol.cpp` round-trip extended. **Feature-tier review + fix-everything** + a **cross-cutting 5-agent review** on the integrated LSP stack (PA5a + PA5b) landed in a single follow-up: (a) bug fixes — `run()` exit code now distinguishes shutdown-then-exit (0) from bare exit (1) per LSP §3.6; `MethodDispatcher::register*` fatal-aborts on duplicate (was silent overwrite, masked future real handlers behind stubs); destructor + handleExit_ both drain executor BEFORE closing transport; `StdioTransport::writeMessage` short-circuits on observed close; CLI rejects unknown long-flags loudly (was silent fall-through to demo-gui/hello path); unused `DocumentVersion`/`RequestId` strong IDs deleted from `core/types/strong_ids.hpp` (project rule: no dead code). (b) new tests — `LspTestHarness` factory in `lsp_test_helpers.hpp` collapses ~80 lines of e2e boilerplate; `test_stdio_transport.cpp` (3 cases) covers close-aware write + idempotent close + post-close EOF; `SchemaCache.ConcurrentResolveYieldsSingleSharedPointer` (16-thread storm); `DocumentStore.ConcurrentUpdatesAndStaleWritebackPreserveLatest` (worker stale-writeback race); `LspServerE2E.ExitWithoutShutdownReturnsExitCode1`; `LspServerE2E.DidOpenWithoutExtensionPublishesEmptyDiagnostics` (pins the unknown-extension behavior); `MethodDispatcherDeathTest.Duplicate{Request,Notification}RegistrationAborts`. (c) simplifications — `tryParseParams` helper collapses 3× parse-or-bail prelude in didOpen/didChange/didClose; `SchemaResult` type alias for the 4× `expected<shared_ptr<GrammarSchema const>, SchemaResolveError>`; `makeLoadError` collapses both load-failure detail-builders. (d) substring `find()` probes converted to parsed-JSON sub-object equality across `test_lsp_server_e2e.cpp` + `test_method_dispatcher.cpp`. The golden-file replay harness referenced in this row had already shipped in PA5a Phase 6. **47 ctest-suites-at-shipping (now 56 post-08.55), 100% pass** (was 46; +1 = test_stdio_transport). **Acceptance criterion met**: every method advertised in `initialize.capabilities` has both an e2e wire-protocol test and a golden-file session-replay test. |

### 2.3.1 PA4 follow-ups

All 8 items landed as a single follow-up commit. Tracking status:

| ID | Item | Status / Notes |
|----|------|----------------|
| PA4-F1 | **`OperatorTable::Entry` type refactor** to `std::optional<GroupedPostfix>` | ✅ done. `Entry` no longer admits the invalid "bodyRule without endsAt" combination. Loader DTO (`grammar_schema_json.cpp`), walker grouped-postfix dispatch (`parser.cpp`), and `validateOperatorBodyRules` all consume the new shape. Trivially-copyable preserved — the existing `static_assert` is the tripwire. |
| PA4-F2 | **`bodyDefaultTokenKinds` dedup** via shared `GrammarSchema` accessor | ✅ done. New `GrammarSchema::bodyDefaultTokenKinds()` returns the schema-owned set; `Parser::Impl` and `TreeBuilder` both hold a pointer to it instead of building independent copies. Loader populates once at schema build. |
| PA4-F3 | **Postfix-chain nesting investigation** | ✅ investigation done; **walker bug confirmed**, fix deferred. `f(a)[i]` produces a single `postfixExpr` (just `f(a)`) plus a diagnostic storm. Root cause: `parseExpressionAt`'s rollback-replay assumes right-recursive structure. On iteration 2 it rolls back to before-primary (S0), then re-parses at `prec+1` which EXCLUDES the same-prec `(` postfix already consumed in iter 1. Tokens are now at `(`; the iter-2 outer wrap pushes `(` as its operator instead of `[`. Captured as a `DISABLED_PostfixChainNestsLeftToRight` tombstone test. Fix requires walker redesign (preserve prior wrap as new primary rather than re-parse from scratch) — promoted to its own workitem, see "Open after PA4 follow-ups" below. |
| PA4-F4 | **Loader-validation unit tests for `endsAt`/`bodyRule`** | ✅ done. 10 new tests in `tests/core/test_operator_table.cpp::OperatorTableEndsAtBodyRule` covering all 8 loader error paths + the walker-wrapper-name skip + grouped-postfix `Entry` invariant pin on c-subset. |
| PA4-F5 | **Dedicated AltChoice→RuleLeaf expr-rule bypass test** | ✅ done. `PrattWalker.AltChoiceScanRoutesExprRuleThroughWalker` in `test_pratt_walker.cpp` uses a synthetic grammar with `body: { alt: ["expression"] }` to isolate the AltChoice scan path, then asserts a `binaryExpr` was built (proving the walker ran). |
| PA4-F6 | **Body-token drain × recovery / happy-path tests** | ✅ done (recovery half). `ParserRecovery.BodyCodepointsDoNotMultiplyDiagnostics` (broken-path: drain absorbs body chars during sync scan without per-codepoint diagnostics) and `ParserRecovery.BodyCodepointsAbsorbedCleanlyOnHappyPath` (clean parse with a string literal). Speculation × drain interaction not added — speculation requires a custom grammar with body modes; deferred until a shipped speculative grammar exercises body content (see "Open after PA4 follow-ups"). |
| PA4-F7 | **Numeric-literal `flagsApplied` channel** (structural fix) | ✅ done. New `GrammarSchema::flagsForKind(SchemaTokenId)` accessor (returns `NodeFlags::None` today); tokenizer numeric-literal emit site (`tokenizer.cpp:678-679`) now calls it so the channel is consistent with other emit sites. No new schema field invented — when a future schema needs per-kind flags, populating the accessor's data source is a one-line change. G-118 in production-readiness remains as a "design the schema field" follow-up. |
| PA4-F8 | **Smoke prettyPrint pins + corpus golden harness** | ✅ done. All 6 c-subset gap-closure smoke tests now use full `prettyPrint` equality against an exact expected literal. Corpus tests use a new `checkGoldenTree(t, "<file>.tree")` helper that byte-compares against a sibling golden file; refresh via `DSS_REFRESH_GOLDENS=1 ctest`. Three goldens shipped (`mini_calc.c.tree`, `schema_and_dml.sql.tree`, `demo.toy.tree`); missing golden = test failure with a refresh hint. |

#### Open after PA4 follow-ups

| Item | Where |
|------|-------|
| **Walker postfix-chain redesign** | ✅ shipped as `PA-Walker-LeftRec` (§2.3 row above) in parallel with PA5a. |
| **Schema field for per-kind flags** (PA4-F7's design half — G-118). Populate `flagsForKind` from a `literalFlags: { IntLiteral: [...] }` schema block. Wait until a real use case lands. | `07-production-readiness-plan - tbd.md` G-118. |
| **Speculation × body-token drain interaction test** (PA4-F6's deferred half). Requires constructing a speculative-alt schema with body modes. Land when speculative grammars exercise body content. | `tests/analysis/syntactic/test_parser_speculation.cpp` (new test). |

### 2.4 Speculation strategy

The schema marks `alt` shapes with `speculative: true` + `lookahead: N` (PR4 shipped the field; nothing consumes it yet). The parser:

1. At an `AltChoice` cursor, scan the next `lookahead` tokens.
2. For each branch in order:
   - Take a `TreeBuilder::Checkpoint`.
   - Attempt to drive the branch.
   - If we consume `lookahead` tokens AND no `P_*` error fired AND the cursor remains valid → commit, this branch wins.
   - Otherwise → rollback, try the next branch.
3. If every branch fails → emit `P_BacktrackFailed`, take the recovery path.

Non-speculative alts use direct FIRST-set lookup with no checkpoint — fastest path.

### 2.5 Forward-progress watchdog (PA1)

Every iteration of PA1's main dispatch loop captures `(walker.cursor(), tokens.position())`. If neither changes across one iteration, the parser fatal-aborts with a message naming the slot kind, the head token, and the current rule context. Per the project's fail-loud discipline (matching PA0's `enterRule(InvalidRule)` / underflow / cross-walker `restore` and the throwing-callback contract), this is a contract violation — better to halt the test process loudly than hang CI. PA2 and PA3 inherit the invariant; an infinite-loop regression introduced by Pratt-walker or recovery code surfaces as a death-test failure immediately, not as a CI timeout.

### 2.6 What's NOT in this phase

| Out of scope                                        | Why                                                                                            |
|-----------------------------------------------------|------------------------------------------------------------------------------------------------|
| Symbol tables / type inference                      | Phase #8 (semantic).                                                                           |
| IR generation                                       | Phase #9.                                                                                      |
| Multi-file compilation / imports / modules          | **Phase 7.5** ([`08-compilation-unit-plan - tbd.md`](./08-compilation-unit-plan%20-%20tbd.md)) — sits between parser and semantic. Parser still produces one Tree per file; CU bundles them. |
| Incremental parsing                                 | Post-v1. The current PA5 LSP impl re-parses on every `didChange`. Build caching + incremental landed when memory budget G-721 forces it. |
| Macro expansion / preprocessor                      | C-subset deliberately omits the preprocessor. If full C99 is ever a target, that's a new phase. |
| Semantic-powered LSP capabilities (hover, completion, goto-def, references, rename) | PA5 stubs them as empty-result responders. They light up post-phase #8 in a dedicated LSP follow-up plan. |

---

## 3. Acceptance criteria

- [ ] Every test currently in `tests/core/test_*_subset.cpp` that hand-drives `TreeBuilder` flips to driving through `Parser::parse()`. The tree shape is identical (or, for `ExpressionWithMixedOpsIsLeftFolded`, **changes to the precedence-correct shape** — the visible signal that v2-gap-catalog row 1 is closed end-to-end).
- [ ] Toy / c-subset / tsql-subset parse the new `tests/corpus/` programs without unexpected diagnostics.
- [ ] Diagnostic UX renders source context (line + column + caret), not bare byte offsets.
- [ ] Speculation cap is enforced — pathological input doesn't run unbounded checkpoints (`P_MaxSpeculationDepth` from `BuilderConfig` is already wired; PA1 just has to plumb the parser's own per-alt depth budget).
- [ ] Death-test coverage: parser aborts cleanly on null tokens / null schema / bogus checkpoint state, matching the discipline of TreeBuilder's existing death tests.
- [x] **Forward-progress watchdog** in code (PA1) + termination test for the canonical "every speculative branch matches FIRST but none commits" scenario (`ParserSpeculation.NoCommittableBranchTerminatesCleanly`). The watchdog itself is a safety net for PA2/PA3 regressions: under PA1's invariants, every dispatch path either advances cursor, advances token, or changes frame depth (P_BacktrackFailed + consume on speculation failure, EOF guard mid-rule, etc.), so a natural stall isn't constructible. A true `EXPECT_DEATH` for the watchdog would require fault injection — deferred to PA2/PA3 where their changes might actually risk regression.
- [ ] LSP server (PA5) starts via `dss-code-prime --lsp`, completes the initialize handshake, and publishes diagnostics on `didOpen`/`didChange` for every shipped language. Golden-file session replay green.
- [ ] Per-PR review cadence (5-agent review + "Fix everything" round) applies to every PA[0,1,2,3,4,5a,5b] commit. PA0 + future substrate touch gets full substrate-tier review; PA1–PA4 + PA5a/PA5b are feature-tier.

---

## 4. Open questions

| # | Question | Notes |
|---|----------|-------|
| 1 | When does the parser detect "ran off the end of the token stream mid-rule"? `TreeBuilder::finish()` already synthesizes premature-close diagnostics; does the parser layer add anything? | Default: parser stops at EOF and lets `finish()` synthesize the closes. The downside is that the parser can't emit a more precise "expected X here" diagnostic since it loses control of the cursor at EOF. Decide in PA3. |
| 2 | How does the parser interact with `artifactProfile`-specific shapes (e.g. a `cli` profile requires a `main` rule at the root)? | Per `06-artifact-profile-plan - tbd.md` §6 Q1: NOT a v1 concern. Profile-specific shape requirements are codegen-phase work. |
| 3 | What's the right strategy for c-subset's currently-omitted features (function calls, array indexing, pointer ops — rows 6/7/8/12)? Land them in c-subset during PA4, or defer to a `c-subset-v2.lang.json` follow-on? | Default: land in PA4 since real-world stress demands them. They're "postfix arity" work — operator-table extensions, not new shape kinds. v2-gap-catalog already flags them as PR1-shape resolutions. |
| 4 | Recovery quality benchmark — what's the bar? `clang -Weverything` quality? `tsc` quality? | TBD with the user. Default for v1: "every error produces an actionable message; recovery continues to EOF without cascading more than 3× the original error count." |
| 5 | ✅ **Closed.** PA1 carry-over: refactor `Impl::trySpeculativeBranch`'s `goto fail` + manual 4-machine restore into a `SpeculationProbe` RAII guard (mirrors `TreeBuilder::Checkpoint`). Type-design review flagged this as 9/10 confidence "would make the invariant local + structurally enforced". | Landed pre-PA2: `SpeculationProbe` nested in `Parser::Impl` owns token bookmark + `std::optional<TreeBuilder::Checkpoint>` + `std::optional<SchemaWalker::Snapshot>` + targetDepth + diagsBefore + probeStartPos + desyncedBefore + budget. Dtor pops frames → restores walker → rolls back builder → restores token bookmark → resets diag counter → decrements speculation depth, in that order. `commit()` re-baselines the watchdog tuple. `trySpeculativeBranch` collapses from `goto fail` + manual restore to `SpeculationProbe probe{*this}; … return false / probe.commit()`. 668 tests green, zero behavior change. |

---

## 5. Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| The schema doesn't carry enough info for a fully-generic driver — some grammar quirk needs per-language code | Medium | High (forces a re-think of the schema-driven bet) | Spike PA1 on toy first; only proceed to c-subset once toy round-trips cleanly. |
| Pratt walker integration is harder than expected because `expr` shape bodies don't compose cleanly with the position graph | Medium | Medium | The PR1 review hardened `expr` body validation. PA2's first task is a focused integration test against a synthetic 3-operator schema before touching c-subset. |
| Recovery quality is "good enough for tests, bad for users" | High | Medium | Treat PA3 as gated by real-world corpus stress (PA4 prereq). A test that says "no diagnostics" isn't proof — a test that takes a deliberately-broken real program and pins the diagnostic set is. |
| Speculation explodes on adversarial input (deeply nested ambiguous alts) | Low | Medium | `BuilderConfig::maxSpeculationDepth` already caps it; parser-side `ParserConfig::maxSpeculationDepth` is the per-alt budget. Death-test both. |

---

## 6. Next steps after this phase

**Phase 7.5** (`compilation-unit-model`, [`08-compilation-unit-plan - tbd.md`](./08-compilation-unit-plan%20-%20tbd.md)) starts here. Parser produces one `Tree` per file; phase 7.5 bundles them into a `CompilationUnit` + resolves cross-file imports declaratively per language. **Phase #8** (`analysis-semantic`) consumes the resulting `CompilationUnit` (not raw trees) so symbol tables / type checking / scope resolution work across files from day one. Contract chain: parser → Tree-per-file → CU bundle → semantic read-only over CU → IR.

**LSP spin-off — planned post-PA5b.** PA5a/PA5b live in this plan because the diagnostic-rendering pipeline is shared substrate with PA3, and shipping that pipeline through a real editor is the strongest forcing function for diagnostic quality (G-115). The scope will grow beyond what `05-parser-plan` can hold the moment semantic-powered methods light up: incremental rebuild, cross-file goto-def, workspace symbols, code actions, semantic-token highlighting, formatting. **At that point, spin off `09-lsp-plan - tbd.md`** owning everything in `src/lsp/`. The split point is "ship PA5a + PA5b, then create the LSP sub-plan and migrate the existing implementation under it." Don't pre-create the sub-plan — let the parser-side work establish the patterns first.

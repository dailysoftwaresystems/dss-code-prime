# C-Subset → Full C (ISO C23) — Sub-Plan (stepper **V2-4.X**)

> Upgrade the shipped **`c-subset`** language to **full C (ISO/IEC 9899:2024 / C23)** — across the WHOLE pipeline: grammar, tokenizer, syntactic + semantic analysis, the type system, HIR/MIR/LIR IRs, the ABI/calling-convention layer, the assembler/linker, the corpus runners, AND a complete C preprocessor. This is a **multi-month arc of many `/dss-cycle` cycles**, decomposed below so **every phase ships a green, runnable increment** (never a long-lived red branch).
>
> **Target = "C23 as mainstream compilers implement it."** The latest standard, in full — but a handful of genuinely-exotic C23 corners (`<threads.h>`, `<stdatomic.h>`, `<complex.h>`, `#embed`, `long double` format divergence) were **named, pinned deferrals** — but **ALL are now PROMOTED into scope as FC17.9** (§0.1; user 2026-07-14: 100% C23 BEFORE FC18, the DSS Axis substrate mandate), joining `_BitInt(N)` ✅ + VLAs [active arc, C1a–C4c ✅] promoted earlier (user 2026-07-11). Nothing C23 is silently dropped; FC17.9 makes DSS a COMPLETE C23 implementation before the FC18 conformance sweep.
>
> **Non-negotiable bar (unchanged):** source/target/linker AGNOSTIC (config-driven vocabulary, never `if (lang/arch/format/cc == …)` in shared substrate — `src/{opt,mir,hir,lir,core}` + the parser/semantic engines); best-long-term / no workarounds; the hard part of each phase lands that phase; fail-loud; strict + **positioned-diagnostic** tests (leveraging the V2-4 `D-DIAG` harness) + a runnable corpus per phase with §A.5 **cross-target runtime closure** (the binary EXECUTES on each target's native CI leg — byte-pins are NOT runtime proof).
>
> **THE AGNOSTICISM CRUX (read before any backend phase).** Three phase-clusters carry per-target/per-format work and MUST honour the sanctioned-tier rule: per-CC/per-format logic lives ONLY in the realization tier (`src/asm` encoder, `src/link/format/*.cpp`, `src/lir/lir_callconv.cpp` / `mir_to_lir::lowerCall`), is **PARAMETERIZED by declared config** (`.target.json` / the ABI catalog / `.format.json`), and NEVER becomes an identity branch in shared substrate. "Config-driven" here means **parameters-in-config + a bounded per-CC/per-format ALGORITHM in the realization tier** — NOT a fiction that the whole algorithm is data walked by one generic engine (SysV eightbyte classification + AAPCS64 HFA detection do not reduce to a flat table). Every backend FC (FC6, FC9–FC12c) opens with this distinction pinned, and FC9 + FC13 carry an explicit plan-lock **§B** on "what is data vs what is per-CC/per-format code."

## 0. Status (snapshot)

| | |
|---|---|
| Status        | 🟡 **IN PROGRESS — FC1–FC15 ✅ SHIPPED (2026-06-10 → 06-25); FC16 🟡 (2026-07-07: anon-member `D-CSUBSET-ANON-MEMBER-PROMOTION` + `_Static_assert` `D-CSUBSET-STATIC-ASSERT` + `_Generic` `D-CSUBSET-GENERIC-SELECTION` ✅ — both battery gaps S0017 + P0009 CLOSED; `_Alignof`/`alignas` ✅ + wide/UTF literals arc A–D ✅ + `packed` attr-semantics ✅ (2026-07-08, `D-CSUBSET-PACKED` — suffix-position packed → no-padding/align-1, all spellings honored-or-fail-loud, 4 registered deferrals, 603/603 + sqlite green); `noreturn` semantics ✅ (2026-07-08, `D-CSUBSET-NORETURN` — all 4 spellings → `isNoreturn`, callee-based `Block{call, Unreachable}` synthesis mirroring `wrapIfProvablyInfinite`, ZERO verifier/lowering change, config-driven `linkageSpecifierIgnoredNames` replaces the audit-rejected linkage no-op, 604/604 + sqlite green, 2 gated deferrals); `volatile`-honoring codegen ✅ (ALREADY-SHIPPED c21/c27/opt-10b — stale-plan correction; design pass traced all 5 cases clean, hardened 2026-07-08 with 3 coverage-by-construction pins + an ordering doc-contract)); FC17 ✅ + FC17.5 ✅ (2026-07-14, all residuals incl. the VLA arc C1a–C5); FC17.9 + FC18 ⏳. ★ sqlite-green all 3 legs (pe64 + x86_64 + arm64) + release-optimizer perf arc (>30min→2m00) + both T2 silent-miscompiles CLOSED. Original header (HISTORY): FC1 ✅ + FC2 ✅ + FC3 ✅ (c1+c2, 2026-06-10)** (cycle 1: binary `%` end-to-end x86_64+arm64 + role contract + capability-driven div/mod; cycle 2: C23 hex-floats + decimal `1.`/`.5` edge forms + decode de-hardcoding — zero FC1 deferrals; cycle 3: FULL C-style casts incl. typedef-name resolution via the binder sketch + include-oracle reparse, x86 SSE backend (`mandatoryPrefix` + `registerClassOps`), F64 rodata materialization, and the FIRST float runtime witnesses — float_cast exit 16 on Windows PE + linux ELF; FC3 cycle 1: width/signedness type system + the `dataModel` axis + the UAC engine — 230/230). FC3 c2 (the 32-bit ALU width axis) ✅ SHIPPED 2026-06-10, 236/236 — see the §0.1 FC3 row. **FC3.5 (the user-inserted FC1–FC3 deferral-closure sweep) ✅ COMPLETE 2026-06-11**: c1 (shifts end-to-end + the `&&`/`||`-condition fix + for-clause routing + ZExt-32→64, 240/240) + c2 (float completeness — F32 codegen + FCmp lowering + NaN truthiness, commit `8c71a66`, 244/244) + c3 (msub fusion + riprel fold + the 3 cast residuals + wide immediates, commit `c2f28fa`, 246/246; the c3 stamp landed retroactively via audit-residue sweep c2 2026-06-11 — c3 updated plan-00/plan-14/registry but not this header, `D-AUDIT-PLAN-STALENESS` ✅). **FC4 ✅ COMPLETE 2026-06-12 (c0 ✅ + c1 ✅ + c2 ✅ — the C declarator substrate + indirect-call codegen end-to-end; 263/263; `D-CSUBSET-FNPTR-INDIRECT-CALL` + `D-ML7-2.4` closed — see the §0.1 FC4 row). FC5 ✅ COMPLETE 2026-06-15 (289/289) — `goto`/labels + truly-empty statement + comma operator + the `entryFunctionNames` de-conflation [`D-LK10-ENTRY-MAIN-IMPLICIT-RETURN` ✅]; unstructured CF with ZERO MIR-substrate change; see the §0.1 FC5 row. **FC6 ✅ COMPLETE 2026-06-15 (292/292) — the aggregate-layout substrate (`type_layout` engine + per-`.target.json` `aggregateLayout` params + `ArenaAttribute<TypeInterner, StructLayout>` side-table) + full expression-context `sizeof` (const-folds via the engine) + the FAM front-end (`incompleteArray` type + struct-field array declarators + config-driven `allowFlexibleArray`); `D-FF3-1` layout-half ✅; named exclusion `D-FC6-SIZEOF-CONST-CONTEXT` (sizeof in array dims / global inits — fail-loud); see the §0.1 FC6 row. char VALUE codegen ✅ 2026-06-16 (branch p8): D-CSUBSET-CHAR-INT-WIDENING + D-CSUBSET-CHAR-STRING-VALUE-CODEGEN ✅ — a sub-native BYTE width axis (kLirInstFlagWidth8: byte load/store/sext on x86_64 movzx/mov-r8/movsx + arm64 LDURB/STURB/SXTB) + bidirectional char↔int cast (charConvertsToArith); width-8 confined to {load,store,sext}, plumbing promotes to 32, compute fails loud; 300/300; runtime-witnessed x86 native PE + arm64-ELF qemu (exit 132/131). **FC7 ✅ COMPLETE 2026-06-17 (315/315 + 133/133 c-subset examples) — member-access codegen (Phase 1) + nested struct fields (A) + aggregate byte-wise copy (B) + struct BY-VALUE args/returns ALL 4 ABIs (C1 SysV / C2 Win64 / C3 AAPCS64+Apple x8-sret + HFA); `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` ✅ + a found+fixed HIGH pre-existing multi-piece-`Return`-clone silent miscompile + the inlining-of-struct-returners hazard (refused at the legality gate, post-review); see the §0.1 FC7 rows + the registry. Remaining FC7-area deferrals are all trigger-gated/OPT7 (not autonomously openable). Next: array-storage cluster ✅ + FC8 SUBSTRATE COMPLETE 2026-06-19 (all 6 cycles + the 3 §B bit-field deferrals + aggregate ternary/comma + writable .data/.bss) — NEXT = FC9 (see §0.1 + the FC8 row); **FC13 cycle 1 (C preprocessor — Cluster D PARALLEL/INTERLEAVABLE track, user-directed pick in worktree p13) ✅ 2026-06-19 — object macros + textual quote-`#include`, 330/330; function-like macros = FC13 cycle 2; see the §0.1 FC13 row.** Plan rev. after a strict 3-reviewer plan-lock (agnosticism, slicing, accuracy). Stepper row **V2-4.X**, between V2-4 (program-api CLI ✅) and V2-5 (RISC-V). |
| Scope         | **Full C23 language + full preprocessor + aggregate ABI on ALL current targets** (x86_64 + arm64 × PE/ELF/Mach-O). Decomposed into 5 clusters / ~20 cycle-phases (FC1–FC18, incl. the NEW **FC17.9** full-C23-completeness tier, §0.1) so each ships green+runnable. Closes ~12 OPEN deferrals (§3.1). The §3.2 exotic long-tail is **PROMOTED into FC17.9** (100% C23 before FC18 — the DSS Axis substrate mandate, user 2026-07-14). |
| Predecessors  | V2-4 (`D-DIAG-CLI-POSITION-RENDER-AND-ASSERT` ✅) — the positioned-diagnostic + `expectDiagnostics` corpus harness is REUSED to assert the many new C error classes. The rich existing `c-subset` grammar (struct/union/enum/member-access/designated-init/for/switch all already have rules) is the FRONT-END base. **`D-FF3-1`** (plan-11 FF3 — the per-target ABI *layout* TargetSchema extension: struct padding + integer/pointer sizes + va_arg, trigger "first non-LP64 target") is the upstream substrate that FC6 + FC12 realize. |
| Successors    | V2-5 (RISC-V) reuses the aggregate ABI substrate (FC9–FC12c) when adding a 4th calling convention. The full-C corpus becomes the agnosticism acid test for every later target/format. |
| Key finding   | The **front-end grammar is mostly complete** (struct/union/enum/member-access/designated-init/typedef/const/multi-level-pointers/variadic/full-control-flow all already have rules) — **but NOT function pointers** (the type lattice has the `FnPtr` *kind*, but no `(*fp)(args)` declarator grammar exists; that is a front-end GAP, see FC4). The type lattice already has `Struct`/`Union`/`Enum`/`Ptr`/`FnPtr`/`FnSig`/`Array` kinds; MIR is aggregate-capable (`Alloca`/`Gep`/`ExtractValue`/`InsertValue` opcodes exist); asm/link are structurally feature-agnostic (opaque bytes + opaque relocation tags). **The real work is the BACKEND: a struct-LAYOUT substrate (offsets/align/padding, per-ABI, the `D-FF3-1` realization), aggregate member-access codegen, and the aggregate+variadic ABI per target — plus the missing front-end pieces (`%`, casts, the usual-arithmetic-conversions engine, width types, function-pointer declarators, `goto`, multi-declarator) and the entire preprocessor.** |

---

## 0.1 Phase breakdown — the FC* cycles by cluster

Each FC phase is one or more `/dss-cycle` cycles ending at a green push with a runnable corpus + strict/positioned tests. Every FC row carries the implicit **per-FC agnosticism acceptance gate** (§5): no new `if (lang/arch/format/cc.name == …)` in shared substrate; new per-CC/per-format logic lives only in the sanctioned realization tier, config-parameterized. Status: ⏳ planned · 🟡 in progress · ✅ done.

### Cluster A — Front-end completeness *(cheap config-driven wins first: de-risk, close quick deferrals, unblock an authentic corpus)*
| Phase | What | Closes |
|---|---|---|
| **FC1** ✅ | **Cycle 1 ✅ 2026-06-10 (the first full-C cycle):** binary `%` end-to-end on x86_64 (PE+ELF) **and arm64 (ELF+Mach-O)** — c-subset `%` token/operator/binaryOps (config-only; `%=` newly live); the **role-tagged projection contract** (`inputRoles`/`outputRoles` maps on `implicitRegisters` — BOTH sides per the plan-lock; loader-validated registered vocabulary; the lowering projects BY ROLE, positional indexing gone; red-on-disable levers via the schema-mutation substrate); the **capability-driven div/mod realization** (`lowerDivLike` rules: native verb opcode → implicit-register pair → generic rem = n−(n/d)·d expansion → fail-loud; arm64 gains `sdiv`/`udiv` + `neg` (surfaced by the corpus' negative literals) as pure JSON; all four SDiv/UDiv/SMod/UMod wired, UMod MIR-tier-tested); ConstFold SMod truncated semantics + the **INT64_MIN/-1 Overflow guard on BOTH Div+Rem** (fixed the pre-existing latent host-UB SDiv fold bug); the C23 `'` digit separator (config flip `_`→`'` + the universal **flanked-by-digits scanner rule**); 0b literal pins; `examples/c-subset/modulo` (4 target arms, truncated-vs-floored runtime discriminator, exit 42 — PE local + WSL x86_64 + qemu arm64 witnessed; Mach-O arm64 = macos-latest leg) + `float_mod_error` (positioned `%`-on-float fail-loud) + the division corpus gains 3 cross-target arms. 217/217 ctest. **Cycle 2 ✅ 2026-06-10 — FC1 COMPLETE (zero deferrals left):** C23 **hex-float literals** (`0x1.8p3`) via a per-prefix `float` continuation on `NumberPrefix` (`exponent` letters/sign + its OWN `exponentDigits` class — hex mantissa, DECIMAL exponent; exponent REQUIRED, committed-incomplete → ONE malformed token, never a silent split; entry gate admits `.` after a floating prefix so `0x.8p3` lexes) + **C23 decimal fractional-constant edge forms** closed IN-CYCLE per the user's no-deferrals directive (`trailingFraction`/`leadingFraction` opt-ins: `1.` and `.5` are floats in c-subset; default-false keeps range-operator languages split) + **decode de-hardcoding**: `decodeFloat` hoisted to `number_decode.hpp` and fixed (the old strip-every-'f' corrupted hex mantissas — `0x1.fp3` 15.5→8.0, red-on-disable demonstrated); `decodeInteger` prefix-radix now config-driven (the 0x/0b/0o/0 hardcode silently returned 0 for `$ff`), trailing-suffix-first strip (high-radix digit/suffix collision), a-z digit map (radix>16 was silently wrong). Loader: shared exponent parser (one source, unknown-key rejects on BOTH exponent objects), float-block validation incl. the exponent-letter-inside-digit-class dead-config reject. Engine-genericity pins on the synthetic schema (`$`+`^`+binary exponent digits; `%`+`@`+sign-not-optional+octal). NO runtime corpus — float VALUES are runtime-unobservable today (returns reject semantically; casts = FC2; FCmp has no LIR lowering — empirically probed); the value proof lives at the exact-double decode tier per the §A.5 carve-out, and **FC2 owns the first float runtime corpus** (see FC2 row). 218/218 ctest. | Cycle 1 CLOSED: `D-CSUBSET-MOD-OP-CODEGEN` ✅, `D-CSUBSET-MOD-OP-CODEGEN-OUTPUT-INDEX-CONTRACT` ✅, `D-MIR-TO-LIR-DIV-SEQUENCE-AGNOSTIC` ✅ (by capability-realization; the one-op/pre-coloring variant re-homed under `D-ML7-2.5`); NEW `D-LIR-MOD-MSUB-FUSION` (normal, peephole). Cycle 2: no anchors opened or closed — the would-be decimal-edge deferral was closed in-cycle. *(NOTE: `D-CSUBSET-DIVISION-OP-CODEGEN` was ALREADY CLOSED 2026-06-04 — cycle 1 added modulo, not division.)* |
| **FC2** ✅ **CLOSED 2026-06-10** (V2-4.X cycle 3; §B-resolved by the user: "most complete, best agnostic, long-term, no workaround"; plan-lock GO-WITH-FIXES — 3 MUST-FIX folded: oracle seat = `UnitBuilder::finish()` pre-freeze, FPR move/store breadth = ALL 5 class-blind sites via `registerClassOps`, corpus fold-resistance = function bodies never global inits; review SHIP-IT — 3 LOW folded; 225/225) | C-style casts `(type)expr` — FULL, including typedef-name casts — SHIPPED. **Runtime-witnessed: the FIRST float values ever observable at runtime** — `float_cast` exit 16 RAN on Windows PE (on-host ctest + CLI, after the corpus surfaced the ms_x64 callee-saved-XMM prologue store gap → closed in-cycle: `movsd_store` F2 0F 11 + the fpr `store` row) AND linux ELF (WSL, incl. full release pipeline); `typedef_cast` (value-DIVERGENT witness: the SAME `(myint)-X` form is a cast in `main` but a subtraction in `helper(int myint)` via param shadowing — mis-parse exits 52, correct exits 42) + `include_typedef_cast` (oracle witness) exit 42 on PE + ELF + qemu-arm64 + Apple Silicon (the macos leg ran them GREEN, run 27302630912). **As-built deltas from the lock:** NO loader extension needed (per-rule `minPrecedence` already loadable — castOperand = second expr rule, engine untouched); the binder sketch consumes the EXISTING `semantics.declarations`/`scopes` (one declaration, two consumers) via a `commitRequiresTypeName` shape field + a 4-rule triage whose ambiguity test derives from the operator table (never a token list; ternary included); sketch+candidates snapshot/restored in BOTH `SpeculationProbe` AND the Pratt `WalkerSnapshot` (a candidate survives its own probe via record-after-destruct) *(the WalkerSnapshot half was REMOVED 2026-06-12 by the associativity wrap-in-place fix — `D-PARSE-INFIX-ASSOCIATIVITY-STRUCTURAL`, registry; the sketch now restores only in SpeculationProbe)*; the oracle reparse REBUILDS crossRefs (fresh TreeIds; first-pass driver diags stay authoritative); semantic = `semantics.casts` + `S_InvalidCast` + `isExplicitCastable`; HIR = `hirLowering.castRule` → explicit `makeCast`. Backend as locked: `mandatoryPrefix` (fixed32-rejected, before REX, disasm round-trip), ADDSD/CVTTSD2SI/MOVAPS/movsd_load(mem+riprel)/movsd_store, F64→anonymous-rodata at HIR→MIR (FPR LiteralIndex retired fail-loud), `registerClassOps` consulted at every copy/spill site (row-less non-GPR → fail-loud, never silent GPR fallback), `requireF64Width` guards. Red-on-disable ×7 demonstrated (sketch-off, oracle-off, prefix-after-REX, LiteralIndex-revert, GPR-fallback, store-row-strip, corpus exit-flip). Docs: spec §11.7 (+§12.5 Model-3 staleness reconciled). **Residuals pinned:** `D-CSUBSET-CAST-ARRAY-DECAY`, `D-CSUBSET-CAST-VOID-DISCARD`, `D-CSUBSET-COMPOUND-LITERAL-TYPEDEF`, `D-LIR-GLOBALADDR-LOAD-RIPREL-FOLD`, `D-ARM64-FLOAT-SUBSTRATE` (✅ CLOSED cycle 4 next-day — declaration-only arm64 FPR parity, float_cast arm64-ELF exit 16 under qemu; the Mach-O arm runtime-pending the macos leg); oracle single-round by design (emergent candidates diagnose at semantic); FC3 owns implicit char↔int widening (probed: width casts can't reach an int return yet) + the missing type keywords. **The locked architecture (the §B decision record):** (1) `castExpr` as a speculative `operand` alternative (order: compoundLiteralExpr → castExpr → parenExpr) over the existing `typeRefAllowingStruct`; the cast OPERAND re-enters the Pratt walker at PREFIX precedence (90) via the existing `isExprRule`/`exprMinPrecedence` rule-leaf re-entry (a second expr-entry rule with `minPrecedence` — tiny loader extension if not already expressible). Keyword/tag/star/const-bearing type forms are structurally UNAMBIGUOUS (`(int)x`, `(char*)p`, `(struct S*)q`, `(a*)x` — `a*` is not an expression). (2) **The lone-`(Identifier)` ambiguity** (`(a)-b`/`(a)*b`/`(a)&b`/`(a)(b)` — cast iff `a` names a type) is resolved by a **schema-declared SCOPED SYNTACTIC BINDER SKETCH** in the parser (the industry "lexer hack" done agnostically): the grammar declares which rules INTRODUCE type names vs value names (`"nameBinders": [{rule: typedefDecl, binds: "type", namePosition: …}, {rule: varDeclHead/param/fnDecl, binds: "value", …}]`); the engine maintains a scope-stacked name set (scope opens/closes are already token-visible) and the castExpr probe's Identifier-typeBase arm consults it — declare-before-use + shadowing both correct, zero semantic-phase feedback, fully config-driven (any language can declare binder rules). (3) **Cross-FILE typedefs** (the `#include` tree-merge model means included typedefs are NOT in the includer's token stream): a post-merge **typedef-oracle pre-pass** harvests type-binder names from the merged CU trees; any file whose parse flagged an `AmbiguousCastCandidate` site that the oracle resolves as a type is **REPARSED once** with the oracle seeding the binder sketch's global scope (whole-file reparse, no tree surgery, no walker changes); FC13's textual preprocessor later makes the oracle path vacuous (tokens spliced before parse). Fail-loud: an unresolved lone-identifier cast-candidate parses as parenExpr and diagnoses at semantic (`S_`-not-a-value). (4) Semantic/HIR: castExpr types via the resolved `typeRef` + `HirBuilder::makeCast` (explicit, non-Synthetic); the complete `mapCast` matrix (FPToSI/SIToFP/Trunc/SExt/ZExt/Bitcast/Ptr↔Int) already exists. **Owns the FIRST float runtime corpus** — requires completing the x86 SSE assembler tier: `mandatoryPrefix` field on `TargetEncodingTemplate` (F2/F3/66 emitted BEFORE auto-REX at the designated "legacy prefixes" extension point), `addsd` (F2 0F 58 /r) / `cvttsd2si` (F2 REX.W 0F 2C /r) / `movsd` (F2 0F 10/11 incl. a RIP-relative load variant + an FPR-class register-move form — the GPR `mov` 8B must not encode XMM ordinals silently), and **float-constant materialization via the string-literal precedent**: F64 literals promote to anonymous `.rodata` globals at HIR→MIR (the existing `lowerMirGlobalsToDataItems` F64 branch already emits the bytes) + RIP-relative `movsd` load — the LIR `LiteralIndex` dead-end is retired for FPR constants. Corpus: `(int)(0x1.8p3 + 0.25)`-style exit-code witnesses on x86_64 PE+ELF (arm64 has no float substrate yet — FC3+ scope, pre-existing). | NEW: `D-ARM64-FLOAT-SUBSTRATE`, `D-CSUBSET-CAST-ARRAY-DECAY`, `D-CSUBSET-CAST-VOID-DISCARD`, `D-CSUBSET-COMPOUND-LITERAL-TYPEDEF`, `D-LIR-GLOBALADDR-LOAD-RIPREL-FOLD` (all normal/trigger-gated) |
| **FC3** ✅ **CLOSED 2026-06-10 — c1 + c2 BOTH SHIPPED** (the §B split executed back-to-back per "don't stop between cycles". **c2 (236/236, review SHIP-IT 2 LOW folded)**: the WIDTH AXIS — LirInst.flags bit-0 carrier [survives every flags-copying rewrite pass by construction] + `guard.width`∈{32,64} on encoding variants [loader/validate rejects: out-of-range, duplicate-(kinds,width), ambiguous keyed/absent mix] + ONE `variantMatchesInst` shared by the x86-variable AND fixed32 walkers + `widthFlagsForType` as the single TypeKind→bits point at COMPUTE ops [plumbing stays 64]; x86 no-REX.W 32-bit variants (mov rr+EXACT-imm32/add/sub/imul/cmp/neg/CDQ/idiv/div — SDM-re-derived by review) + trunc's FIRST encoding (mov r32,r32 = mod-2³²); arm64 W-forms + NEW sext=SXTW + trunc=W-mov (ARM-ARM-re-derived); gate narrowed (U32 out, I32 now TRUE 32-bit, conversion gates added: Trunc-non-32/SExt-non-I32/ZExt-non-Bool→`D-CSUBSET-ZEXT-32-TO-64` [pre-c2 the ZExt path was a LATENT SILENT MISCOMPILE — movzx read one byte — now walled]); lowerConst inlines full 32-bit range via exact imm32. **WITNESSES: u32_wraparound (0xFFFFFFFFu+1u→0) + uac_mixed_sign_u32 (-1>0u) + unsigned_division_u32 (0x80001003u/3u — the anchor's original literal) exit 42 on PE+WSL+qemu; datamodel_runtime_width: BYTE-IDENTICAL (sha256-equal) source exits 42 on Windows [LLP64 U32 wrap] and 23 on linux/arm64 [LP64 U64] — the RUNTIME dataModel divergence, native on each CI leg.** Red-on-disable: strip-variant = a PERMANENT in-suite pin; width-forced-64 demo'd 42→7 + restored. Shifts honestly OUT (no encodings exist on either target at ANY width — pre-existing assembler wall, pinned in the anchor's residue). c1 ✅ 2026-06-10 [+1 suite test_fc3_width_semantics 37 tests, +6 MIR pins, +5 LIR guard pins, +4 examples]; CLOSED `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH` + `D-CSUBSET-UDIV-RUNTIME-HIGH-BIT-PIN`[U64]; NEW `D-CSUBSET-32BIT-ALU-FORMS`[high, c2-next]; witnesses: unsigned_division + uac_mixed_sign + datamodel_long_width exit 42 on PE/WSL/qemu + the LLP64 expectDiagnostics differential on PE; as-built deltas: analyze() defaulted-LP64-identity param [driver always passes format.dataModel()], dataModel rides SemanticModel to HIR [tier divergence structurally impossible], ONE shared ladder header called from both tiers, corpus uses ll/ull so all 4 arms run under every model; **c2 IN FLIGHT immediately**) | Width/signedness type system + UAC + dataModel. **The locked architecture (do not re-litigate):** **c1 (semantics)** = (A) new keywords unsigned/signed/short/bool/_Bool/true/false/float/double + a `typeSpecifiers` SemanticConfig table mapping CANONICAL TOKEN MULTISETS → core types (C 6.7.2 order-free combos: `unsigned long long int` ≡ `long long unsigned`; invalid combos reject by absence, S_-loud); (B) **`dataModel` on the FORMAT schema** (plan-lock-corrected from "target" — x86_64 serves Windows-LLP64 AND Linux-LP64; the OS lives in the format: pe→LLP64, elf/macho→LP64; ILP32 declared, fail-loud-if-selected; loader fail-loud on a missing dataModel) resolved at `buildCuMir` and THREADED into `analyze()`+`lowerToHir` (signature extension — semantic already re-runs per (CU×target), verified program.cpp:200/:492/:329 + compile_pipeline.cpp:139/:147); `builtinTypes`/`typeSpecifiers` rows gain `coreByDataModel` overrides (LLP64 long→I32); the 6 LP64-merged descriptor symbols (fseek/ftell/atol/strtol/strtoul/labs) gain the same per-dataModel signature override (Model-3 library-map precedent); (C) the **UAC engine** in type_rules.hpp driven by a SemanticConfig `arithmeticConversions` discipline block (closed-verb `mixedSignedness:"rank-prefer-unsigned"` enum — never a free string; `integerPromotion.minRank` — Bool/Char/I8/I16/U8/U16 promote to I32 so 8/16-bit ALU never exists, C's own rule) materialized as explicit HIR SExt/ZExt casts at combineBinary; toy/tsql undeclared → byte-identical typing, pinned; (D) the **literal ladder** (C 6.4.4.1) — a 2-TIER RESTRUCTURE (plan-lock C-2: typing is token-kind-keyed BEFORE decode today and decodeInteger destroys the suffix; the decoded magnitude + re-derived suffix-class must reach the typing site in BOTH semantic_analyzer AND cst_to_hir): `integerLiteralTyping` config = ordered candidate TYPE-NAME lists per (suffix-class × radix-class), names resolved through the dataModel-aware table (LLP64 long=I32 falls out free); (E-guards) U32-and-narrower ALU ops fail-loud behind `D-CSUBSET-32BIT-ALU-FORMS` (positioned, red-on-disable-pinned — I32 SIGNED arithmetic stays 64-wide: conforming, signed overflow is UB; U32 wraparound is DEFINED hence guarded until c2); float→F32 SEMANTIC split lands, F32 codegen fail-louds behind `D-CSUBSET-F32-CODEGEN` (the requireF64Width precedent); c1 corpus = unsigned-LONG division high-bit (closes `D-CSUBSET-UDIV-RUNTIME-HIGH-BIT-PIN` at 64-bit width), `-1 > 0ul` UAC witness, the dataModel DIAGNOSTIC differential (narrowing long-literal: compiles LP64 / S_-rejects LLP64 — expectDiagnostics arm on the windows leg; a DEFINED-behavior runtime-divergent LP64/LLP64 witness is impossible-by-construction until 32-bit ALU exists [signed overflow=UB; unsigned-32=guarded] — the runtime witness is c2's), ladder pins. **c2 (32-bit ALU)** = a WIDTH AXIS on the encoder's variant guard (the established operandKinds-guard mechanism, config-parameterized — never if(width) in shared MIR/HIR); x86 32-bit no-REX.W variants (add/sub/imul/cmp/mov/div-family/shifts — x86 32-bit ops auto-zero-extend = free U32 zext); **arm64 W-forms + NEW sext/trunc opcodes (plan-lock C-3: arm64 has NEITHER today, all-X-form)**; un-gate the U32 guard; U32 corpus (0xFFFFFFFFu+1u→0 wraparound, -1>0u, 0x80001003u/3 unsigned-int division) + the RUNTIME LP64/LLP64 divergent witness (unsigned-long wrap on LLP64=U32). *(FC1c2 note: c-subset `floatSuffixes` lacks `l`/`L` — long-double suffixes land here with the `float`-vs-`double` split; `1.5L` today accidentally rides the integer-suffix fallback.)* | `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH` (c1), `D-CSUBSET-UDIV-RUNTIME-HIGH-BIT-PIN` (c1, at unsigned-long width), NEW c1: `D-CSUBSET-32BIT-ALU-FORMS` (closed by c2), `D-CSUBSET-F32-CODEGEN` (trigger-gated) |
| **FC3.5** ✅ **COMPLETE 2026-06-11 — c1 ✅ + c2 ✅ + c3 ✅ (c3: commit `c2f28fa`, 246/246 — msub fusion + riprel fold + the 3 cast residuals + wide immediates, "THE SWEEP COMPLETE"; this completion stamp landed retroactively via audit-residue sweep c2, `D-AUDIT-PLAN-STALENESS` ✅ — c3 updated plan-00/plan-14/registry but not this row). USER-INSERTED 2026-06-11; sweep c1 ✅ + sweep c2 ✅ SHIPPED 2026-06-11 (c2: 244/244 — float completeness: F32 codegen + FCmp lowering + NaN adjudication; see the c2 sweep note in the What column) (240/240; review FIX-FIRST → the CRITICAL shift-result-RCX latent miscompile was caught by the reviewer's OWN pressure probe pre-commit, fixed via the GENERIC regalloc result-def implicit-exclusion rule [requires2Address+implicitRegisters ops exclude their declared ordinals from the RESULT vreg — the legalize pre-copy conduit hazard, class-closed; pressured pin + binary-tier 162-vs-42 witness], then audit VERIFIED-CLEAN)** | The FC1–FC3 deferral-closure sweep (one or more cycles, grouped by coherence). **Sweep c1 — expression/statement completeness** ✅ (shifts end-to-end [x86 D3-CL/C1-imm8 both widths via the FC1 `count` inputRole contract + NEW Imm8 slot in lockstep; arm64 native LSLV/LSRV/ASRV X+W — ONE capability-driven lowerShift, zero arch identity], the `&&`/`||`-condition I_StructCfMismatch fix [rhs block marked IfThen — the lowering was the bug, the verifier untouched], the for-clause routing fix [VarDecl/AssignStmt clauses → lowerStmt, BOTH update and init; all 10 compound-assigns + postfix ++/-- covered via the shared AssignStmt desugar], ZExt-32→64 [source-width threading; Bool keeps movzx, U32 takes mov r32,r32; `D-CSUBSET-ZEXT-32-TO-64` ✅ CLOSED]; in-sweep find: arm64 cmp [reg,imm12] X+W gap closed; corpus logical_condition + shift_ops [incl. the pressured arm] + zext_u32_to_u64 ALL witnessed exit 42 on PE/WSL/qemu): (a) SHIFTS end-to-end (`<<`/`>>` are writable c-subset source [tokens at prec 60, HIR Shl/AShr/LShr wired] but NO target has ANY shift encoding → assembler wall; close = x86 SHL/SAR/SHR with the CL-implicit-register contract [the FC1 idiv inputRoles/outputRoles precedent] both widths + arm64 LSLV/ASRV/LSRV W+X forms; closes the `D-CSUBSET-32BIT-ALU-FORMS` shifts residue); (b) the `&&`/`||`-as-condition `I_StructCfMismatch` bug (chip task_bd58aa3d — pre-existing MIR LogicalAnd/Or marker pairing in condition position, reproduces with Bool operands); (c) the for-update clause bug (chip task_20b1224d — `for (...; ...; i = i - 1)` fails "AssignStmt not yet supported": hir_to_mir routes the update through lowerExpr, init correctly uses lowerStmt); (d) `D-CSUBSET-ZEXT-32-TO-64` (the realization exists — mov r32,r32 / W-mov; thread the source width, admit U32 in the gate, corpus). **Sweep c2 — float completeness ✅ SHIPPED 2026-06-11 (244/244)**: (1) `D-CSUBSET-F32-CODEGEN` CLOSED — the F3-prefix SSE scalar-single family (ADDSS/DIVSS/CVTTSS2SI/CVTSS2SD/MOVSS load+store+riprel) + arm64 S-forms (FADD-S/FDIV-S/FCVTZS-from-S/FCVT both directions/LDUR-S/STUR-S) ride the FC3-c2 WIDTH AXIS (`widthFlagsForType` F32→32; existing F64 variants width-keyed 64 — the mnemonic-vs-variant adjudication: ONE mnemonic, width-discriminated variants, per the established axis pattern; conversions key on the SOURCE width via the ZExt-style widthOverride — the result-width default would pick the WRONG fpcvt direction and mis-key fp_to_si); F32 literal materialization extends the F64 anonymous-rodata path (4-byte items, movss/LDUR-S width-exact loads — an 8-byte read of a 4-byte item could cross a section edge; FPR copies stay width-BLIND by design, movaps/FMOV-D precedent); `floatLiteralTyping` (C 6.4.4.2 suffix map, integer-ladder-shaped: loader-validated coverage/unsuffixed/float-kind cross-checks, ONE shared `typeFloatLiteral` called from BOTH typing tiers) flips `1.5f`→F32 (the c1 interim F64 pin re-pinned to the asymmetric narrowing probe); `requireF64Width`→`requireEncodedFloatWidth` admits F32, walls F16/F128; fdiv gains its FIRST encodings (DIVSD/DIVSS + arm64 FDIV — the NaN constructor). (2) FCmp LIR lowering — `floatCmpPlan` (mir_to_lir): swap-canonicalize Olt/Ole→fcmp(b,a)+Fogt/Foge (IEEE-exact incl. unordered); per-target SINGLE conditions declared as OPTIONAL float `condCodeEncoding` arms (NEW TargetCondCode entries fogt/foge/foeq/fone/fune/fuo/ford — separate from the integer codes because arm64 float `>` is GT while integer-ugt HI is TRUE-on-unordered, a NaN miscompile); ABSENCE of a single = the capability signal for the UNIVERSAL composition (Foeq=Eq∧Ford, Fone=Ne∧Ford, Une=Ne∨Fuo — x86: foeq/fune composed via the NEW and/or encodings [REX.W 21/09 + 32-bit siblings], fone=setne single since UCOMI sets ZF on unordered; arm64: foeq/fune singles, fone composed — the mirror asymmetry); CondBr fuses single-cc predicates (fcmp+jcc), composed predicates branch on the materialized Bool (jcc-pair fusion = a deliberate optimizer-peephole deferral); Ueq/Ult/Ule/Ugt/Uge stay fail-loud (no C producer). (3) `D-COND-FLOAT-NAN-TRUTHINESS-FCMP` CLOSED — mapBinaryOp float Ne flipped FCmpOne→**FCmpUne** (C 6.5.9: NaN!=x TRUE; one source serves `!=` AND the truthiness Ne(d,0.0) → `if(NaN)` true); Eq stays Oeq. Tests: `tests/lir/test_fcmp_lowering.cpp` (the EXHAUSTIVE 7-predicate × 4-outcome × 2-target truth table vs an INDEPENDENT SDM/ARM-ARM flag simulator + shape pins + strip-foeq→composition-fallback + strip-fogt→fail-loud + loader float-arm rules) + FloatWidthAxis byte-pins for EVERY new encoding both targets (incl. parity setcc 0F 9A/9B, CSET cond-inversion, the permanent strip-ADDSS lever) + floatLiteralTyping loader rejects. Corpus (4 arms each, exit 42 RUN on PE native + WSL x86_64 + qemu arm64; Mach-O compiles, macos leg = witness): `float_compare` (6-predicate branch matrix), `f32_arithmetic` (ADDSS+FPExt+FPTrunc+CVTTSS2SI chain), `float_nan` (0.0/0.0 → nan!=nan true, ordered relationals false, if(nan) true). Red-on-disable: Ne→One flip (MIR pin + float_nan RED) + Ford-dropped-from-Oeq (truth-table + float_nan RED) demonstrated+restored; strip-ADDSS permanent. Honest residue: fenv signaling unmodeled (quiet UCOMI/FCMP serve all predicates — Annex F's COMISD/FCMPE distinction needs an FP-environment model); Bool-ALU `(a==b)+(a!=b)` still S_ReturnTypeMismatch (the pre-existing semantic-tier Bool-arithmetic gap — materialized-FCmp coverage lives at the LIR pin tier); FSub/FMul still un-encoded (pre-existing wall). **Sweep c3 — peepholes + cast residuals ✅ SHIPPED 2026-06-11 (commit `c2f28fa`, 246/246; ALL SIX anchors closed — see their ✅ registry rows; stamp retro-applied by audit-residue sweep c2)**: `D-LIR-MOD-MSUB-FUSION` (fixed32 Ra slot + arm64 msub + the rule-3 preference), `D-LIR-GLOBALADDR-LOAD-RIPREL-FOLD` (lea+load → riprel-movsd single-use fold), `D-CSUBSET-CAST-ARRAY-DECAY` ((char*)"str" — apply the implicit decay to the cast operand), `D-CSUBSET-CAST-VOID-DISCARD` ((void)x evaluate-and-discard), `D-CSUBSET-COMPOUND-LITERAL-TYPEDEF` ((MyT){...} — reuse castTypeRef + the binder triage; honest tier parse+semantic+HIR, residue (c) promoted → `D-CSUBSET-TYPEDEF-DECLARATION-POSITION`), `D-LK10-ENTRY-ARM64-WIDE-IMMEDIATE` (MOVZ/MOVK ladder; the wide_immediate corpus — chunks 2/3 runtime traffic added by audit-residue sweep c2's constfold-arm negCheck witness) — ~~if reachable~~ all six reached. Residue that stays honestly gated EVEN in this sweep: 8/16/128-bit ALU forms + Bool-ALU (C's UAC makes them source-UNREACHABLE — no consumer can exist until packed struct fields/atomics; not user-demand-firable). | sweep c1 closes: the 32BIT-ALU shifts residue, `D-CSUBSET-ZEXT-32-TO-64`, chips task_bd58aa3d + task_20b1224d; c2 ✅ closes: `D-CSUBSET-F32-CODEGEN`, `D-COND-FLOAT-NAN-TRUTHINESS-FCMP`; c3 ✅ closes: `D-LIR-MOD-MSUB-FUSION`, `D-LIR-GLOBALADDR-LOAD-RIPREL-FOLD`, the 3 FC2 cast residuals, `D-LK10-ENTRY-ARM64-WIDE-IMMEDIATE` (all ✅ 2026-06-11, commit `c2f28fa`) |
| **FC4** ✅ **COMPLETE 2026-06-12 — c0 ✅ + c1 ✅ (2026-06-11) + c2 ✅ (2026-06-12, indirect-call codegen end-to-end, 263/263)** (the §B D14 c0→c1→c2 split, plan-00 §0.2). **c0** (commit `4ba1884`, 249/249): declaration-ORDERED speculative probing (was interner-alphabetical accident — the engine fix the statement sub-alt depends on; red-on-revert pinned) + the prefix-strip shared helper `core/types/decl_prefix_strip.hpp` (3 consumers routed, `lowerVarLike` latent fixed, `D-DECL-PREFIX-STRIP-SHARED-HELPER` ✅). **c1** (261/261, plan-lock GO-WITH-FIXES 8 MUST-FIX folded, review SHIP-IT): the **C11 specifier/declarator split** — recursive declarator grammar (pointerLayer*/directDeclarator/parenDeclarator/fnSuffix/arrayDeclSuffix/initDeclaratorList) + the agnostic **`semantics.declarators` config vocabulary** consumed by THREE generic engines (BinderSketch multi-name declarator binding; the semantic **inversion fold** — stars innermost, suffixes right-to-left, group recursion: `(*fp)(int)`→Ptr<FnSig>, `*fp(int)`→fn-returning-ptr, `(*arr[2])(int)`→Array2<Ptr<FnSig>>; HIR per-initDeclarator lowering); statement-position typedef names via a CONTAINED speculative `[identVarDecl, exprStmt]` sub-alt + the FC2 triage (`MyP * p;` declares, `a * b;` with value-a multiplies, unknown→oracle); **guard-POLARITY mode** `requireKnownType` resolves the C11 6.7.6.3p11 param `( Identifier )` ambiguity; typedef struct/union BODY forms (tagged + anonymous); multi-declarators (star-binds-per-declarator pinned); `(void)` normalization (lattice-kind + `parameters.soleVoidMeansEmpty` opt-in, S_InvalidVoidParam); unnamed params (ABI-slot-preserving, runtime-pinned `unnamed_param_slot`); register/auto accepted, `restrict` grammar-bound to pointer layers, **volatile LOUD** (`gatedMarkers`→S_VolatileNotSupported, red-on-disable demo'd); `visibility("hidden")` composite-key linkageFrom + the DCE lever test (`D-CSUBSET-LINKAGE-VISIBILITY-SYNTAX` ✅); C23 `[[…]]` parsed+IGNORED per 6.7.12.1 (`linkageSpecifierIgnoredRules`) while unknown `__attribute__` keeps the Error; **`fp(3)` walls LOUD** S_IndirectCallNotSupported (both callee arms; the LIR L_IndirectCallUnsupported wall backstops every other shape) behind `D-CSUBSET-FNPTR-INDIRECT-CALL` (HIGH, c2); 3 deep parser fixes the suite surfaced (speculative expr-handoff, all-failed REPLAY for diagnostic quality, watchdog soundness under probes); `&function`→GlobalAddr typed Ptr<FnSig> (hir_to_mir lvalue arm). 10 corpus examples (7 runtime ALL exit-42 on PE+WSL+qemu — incl. the fold-resistant fn-ptr-through-call-ABI `fnptr_declare` + `unnamed_param_slot`; 2 expectDiagnostics positioned; typedef_struct_body) ×4-target arms (Mach-O = macos leg). Honest residue pinned: `D-CSUBSET-FN-PROTOTYPE`, `D-CSUBSET-TYPEDEF-FIELD-EXTERN-POSITION` (fields/externs keep legacy heads + block-scope typedef statements), `D-CSUBSET-LOCAL-STATIC`, `D-CSUBSET-STRUCT-BODY-VARDECL-POSITION` (✅ CLOSED 2026-06-17, struct-head cycle), `D-CSUBSET-VOLATILE-SEMANTICS` (FC16), `D-CSUBSET-C23-AUTO-INFERENCE`, `D-CSUBSET-DECL-GRAMMAR-LOW-RESIDUES`. **c2 ✅ SHIPPED 2026-06-12 (263/263): indirect calls end-to-end** — semantic callee PEEL (paren-group descent + declared-Deref-token peel with C 6.5.3.2p4 decay algebra; `checkCallAgainstSig` shared tail; `(helper)(...)` UPGRADED to arity-checked; `S_IndirectCallNotSupported` 0xE015 RETIRED) + HIR deref-of-fn decay fold + `calleeSigOf` param-coercion in BOTH call arms + LIR Reg-callee materialization sharing all arg planning (`L_IndirectCallUnsupported` ALIVE re-messaged for residual kinds) + **the 3-layer cc-config-driven callee-reg exclusion** (R2 regalloc argGprs∪argFprs∪variadic-countReg exclusion [red-on-disable demo'd] + B2 spill-reload scratch filter + B1 `L_IndirectCalleeClobberedByArgSetup` 0xB00B backstop — arg pre-copies are POST-regalloc, a consumed-at-call callee doesn't "cross", so without the rule an arg move silently clobbers the callee = jump through an argument) + additive same-mnemonic `call` ["reg"] variants (x86 `FF /2` no-REX.W, arm64 `BLR`; byte-pins FF D0 / 41 FF D2 / 0xD63F0120). Corpus: `fnptr_call` (replaces the deleted `fnptr_call_error`) + `fnptr_callback` (self-recursive apply → SCC-refusal unit-pinned, can never devirtualize; `(*f)` decay witnessed; its Inlining-bearing arm is GATED on `D-OPT-RELEASE-PIPELINE-CONTROL-FLOW-COMPOSITION` — the pre-existing loud release-pipeline×recursion bug the c2 audit surfaced) + `fnptr_regalloc_pressure` — ALL exit-42 on PE local + WSL x86_64 + qemu arm64 (Mach-O compile-arm; macos leg = runtime witness). | `D-CSUBSET-LINKAGE-VISIBILITY-SYNTAX` ✅, `D-DECL-PREFIX-STRIP-SHARED-HELPER` ✅ (c0), `D-CSUBSET-TYPEDEF-DECLARATION-POSITION` ✅ (declaration statements + params + typedef heads; the field/extern remainder = `D-CSUBSET-TYPEDEF-FIELD-EXTERN-POSITION`, normal); c2 ✅: `D-CSUBSET-FNPTR-INDIRECT-CALL` + `D-ML7-2.4` (plan-12, with the double sketch-correction recorded there); residue rows above stay open |
| **FC5** ✅ **COMPLETE 2026-06-15** (289/289 ctest; plan-lock GO-WITH-FIXES — 5 MUST-FIX folded incl. the real-rule comma-gate sites + the dead-code-after-goto label carve-out + the infinite-loop×goto interaction; independent review SHIP-IT, no CRIT/HIGH/MED, GCC-13-built + corpus re-run debug+release) | `goto`/labels + truly-empty statement `;` + the comma operator + the `entryFunctionNames` de-conflation, ALL end-to-end. **`goto`/labels** (the hard part — UNSTRUCTURED control flow): new language-NEUTRAL HIR kinds `GotoStmt`/`LabelStmt`; function-scoped + forward-referenceable + own-namespace labels resolved at the CST→HIR per-function pre-scan (`labelOrdinals_`, saved/restored across all 3 function-lowering paths) → fail-loud positioned `S_DuplicateLabel` (C 6.8.1) / `S_UndefinedLabel` (C 6.8.6.1); MIR lowering = lazy `getOrCreateLabelBlock` (forward+backward uniform) + `goto`→`Br`, **ZERO MIR-substrate change** (the CFG is already a general graph, markers re-derive from it, the mandatory unreachable-prune already owns the goto orphan-block class); `pathTerminates` treats `goto`→terminator + `LabelStmt`→transparent; the dead-code scan carves out a goto's target label (no spurious `H_UnreachableCode`). goto-over-initialization + the infinite-loop-escape (`while(1){…goto out…}`, harmless via the prune) both work in-cycle. **Comma operator** → the existing `SeqExpr` substrate (non-Synthetic); the SEPARATOR-vs-OPERATOR ambiguity resolved by the C two-level grammar: a prec-10 comma + an `assignmentExpr` rule (`minPrecedence: 15`) routed through EVERY separator site (`initValue`→initDeclarator/structField/braceInit, `enumerator`, `argList` ×2) so `int a=b,c;` stays 2 declarators, while `expression` (minPrec 0) keeps the comma operator at exprStmt/for-clauses/return/parens/index/conditions. **Empty statement** → empty `Block` no-op. **`entryFunctionNames` split** closes `D-LK10-ENTRY-MAIN-IMPLICIT-RETURN` (de-conflates program-entry from the C return-0 set; both `["main"]` for c-subset; consumer fallback keeps toy/tsql unchanged). Corpus (4-target arms, runtime-proven on host PE + `optimizedPipelines` baseline==optimized over the unstructured CFG; arm64/macos = CI legs): `goto_cleanup`, `goto_loop` (mul via backward goto), `comma_operator` (all 3 roles + the `int a=b,c;` lever), `empty_statement`, `goto_infinite_escape` (MUST-FIX 3 witness) + `goto_undefined_error`/`duplicate_label_error` (positioned `expectDiagnostics`). Strict pins: 3 HIR red-on-disable (goto/label terminate-via-label, the dead-code carve-out, comma→SeqExpr+separator) + 2 grammar (entryFunctionNames independence via a synthetic divergent config + dup-check) + the parser-smoke/golden updates (argList→assignmentExpr). AGNOSTIC (all config + generic verb dispatch; agnosticism scan clean). **§A.5:** corpus runs on each target's CI leg. **NAMED EXCLUSIONS (NOT deferred FC5 work, fail-loud):** `D-CSUBSET-COMPUTED-GOTO` (GNU `goto *expr`, outside C23-mainstream) + `D-CSUBSET-LABEL-BEFORE-CASE` (`foo: case N:`, P0001, clean fix = case-labels-as-statements refactor). | `D-LK10-ENTRY-MAIN-IMPLICIT-RETURN` ✅; NEW named exclusions `D-CSUBSET-COMPUTED-GOTO`, `D-CSUBSET-LABEL-BEFORE-CASE` |

### Cluster B — Aggregate backend *(the gating engine work — realizes `D-FF3-1`)*
| Phase | What | Closes |
|---|---|---|
| **FC6** ✅ *(2026-06-15)* | The struct/union/array **layout substrate**: field offsets + alignment + padding + total size + flexible-array-member handling. **AGNOSTICISM LOCUS (the C2 fix):** the per-ABI layout PARAMETERS (alignment rules, `long`/pointer width, padding/packing rules, bitfield allocation-unit rules) are **declared in `.target.json`** (the `D-FF3-1` cross-tier TargetSchema extension); a generic `type_layout` engine READS them and computes layout into an `ArenaAttribute<TypeId, StructLayout>` **side-table** (NOT in the interned `TypeRecord` — layout is target/ABI-dependent, so it must not bloat a language-level interned type; this is the codebase's established side-table pattern, the best-long-term choice). Fail-loud on a target that doesn't declare its layout params. **DONE:** `aggregate_layout.hpp` (`AggregateLayoutParams{scalarAlignment, maxAlignment}`, the locked natural-alignment param block) + `type_layout.{hpp,cpp}` (the generic engine: scalar/pointer/enum/array-stride/struct-padding/union/FAM, fail-loud nullopt on out-of-scope kinds & non-last FAM) + `TypeLayoutTable = ArenaAttribute<TypeInterner, StructLayout>` side-table + per-target `aggregateLayout` block in both `*.target.json`. **`sizeof` (full, expression context)** wired end-to-end: grammar `sizeofExpr` (speculative `sizeofType`/`sizeofValue`) → semantic stamp (operand + `size_t`/U64 result) → HIR `makeSizeOf` → MIR const-fold via `computeLayout`. **FAM front-end:** `incompleteArray` lattice type + struct/union-field array declarators (optional `arrayDeclSuffix`) + config-driven `allowFlexibleArray` `DeclarationRule` flag (struct field only — standalone `int x[]` still fails loud, no regression). 2 runnable corpus (`sizeof_flexible_array`, `sizeof_struct_layout`, exit 42, layout-sensitive) + 12 `test_type_layout` engine pins + 2 diagnostic-corpus fail-loud pins. 292/292. Excludes: `sizeof` in a **const-expression** context (array dims / global inits → named exclusion `D-FC6-SIZEOF-CONST-CONTEXT`, fail-loud), bitfields (→ FC8). | `D-FF3-1` ✅ (the layout half; va_arg half by FC12) |
| **FC7** ✅ **Phase 1 ✅ (2026-06-16, branch p9)** | **Phase 1 DONE** — member-access codegen (`s.x`/`p->x` read+write + `&s.x` → a 2-op byte-offset `Gep` → the base+disp `lea` both targets ship) + by-value aggregate LOCALS (Alloca payload = layout size; `lir_callconv` per-alloca frame slots via the shared `allocaSlotCount`) + element-wise aggregate/designated initializers + field-wise struct COPY (`a=b` / `T a=b` / `*pa=*pb`); 304/304; **D-FC7-MEMBER-ACCESS ✅**. **Phase 2 (the user-requested add): struct by-value ARGS/RETURNS** — per-ABI SysV/AAPCS64/MS-x64 classification; fail-loud meanwhile (`D-FC7-STRUCT-BY-VALUE-ARG-RETURN`, high). Front-end deferrals: **`D-FC7-NESTED-STRUCT-FIELD` ✅ CLOSED (closeout Cycle A, 2026-06-16 — the `typeBaseAllowingStruct` bare-`Identifier` alt makes struct-typed fields parse + `lowerAggregateInitIntoSlot` nested-init recursion; nested `a.b.c` + `{{…},…}` work, x86 PE + arm64 qemu exit 42)**; **`D-FC7-AGGREGATE-COPY-MEMCPY` ✅ CLOSED (closeout Cycle B, 2026-06-16 — `lowerByteWiseCopy` I64→I32→Char chunks; UNION + struct-with-aggregate-field copy + aggregate-field-init-from-value; x86+arm64 exit 42)**. **FC7 CLOSEOUT ✅ COMPLETE 2026-06-17** (user: close ALL FC7 anchors before FC8): Cycle A ✅ (nested fields), Cycle B ✅ (aggregate copy), Cycle C+ (Phase 2 by-value ABI, §B). Surfaced 2 pre-existing NON-FC7 array-storage gaps (`D-MIR-STORAGE-ARRAY-INDEX-GEP`, `D-MIR-ARRAY-FIELD-AGGREGATE-INIT`) — fail-loud, pinned, flagged for the user. **Phase 2 (by-value ABI) §B-RESOLVED (user): HIR→MIR boundary + Option A multi-register Arg; PHASED C1a→C1b→C2→C3.** **C1a ✅ (2026-06-16, commit b0a140c): x86_64 SysV substrate + the `aggregate_abi` eightbyte classifier (13 engine pins) + SINGLE-register (≤8B) + BY-REFERENCE (>16B) struct ARGS — runtime-witnessed x86_64-ELF exit 42.** **C1b ✅ (2026-06-16, commit b7f547d): the §B Option-A multi-register Arg extension — a 2-eightbyte (9–16B) struct now passes/receives IN REGISTERS** (HIR→MIR per-class `ArgOrdinalCounter` + `mir_verifier` bound relaxed from FnSig-paramCount to the physical-`Arg`-count [sound, no classifier-threading needed]; `D-FC7-SYSV-STRUCT-ARG-MULTIREG` ✅). **Also CLOSED the latent `D-ML7-2.10`** (mixed int/float scalar args now placed per-class — the double in xmm0, witnessed `mixed_class_args` exit 42). **C1c ✅ (2026-06-16, commit 1016d47): SysV struct RETURNS by value — reg-pair (rax:rdx) via the §B-Option-A new `ReturnPiece` opcode (caller-side mirror of `Arg`) + multi-operand `Return` + sret hidden-pointer (>16B) + mixed-class {double;long} (xmm0:rax); MF-1 `emitParallelRegMoves` cycle-break (the arg path only REJECTS, D-ML7-2.3) + fixed a latent dangling-`fnParams`-span crash (interner realloc on `interner.pointer`); `D-FC7-SYSV-STRUCT-RETURN-IN-REGS` ✅; runtime-witnessed x86_64-ELF exit 42 (reg-pair+sret+mixed, debug+release).** | Phase 1 ✅; closeout A+B ✅; **Phase 2 C1(a/b/c)+C2 ✅ (full SysV struct ARGS + RETURNS: 1-reg/multi-reg/by-ref args + reg-pair/sret/mixed returns via `ReturnPiece` + D-ML7-2.10 + D-FC7-SYSV-STRUCT-RETURN-IN-REGS ✅; **C2 ✅ Win64 ms_x64 by-size — pow2≤8→1 GPR else by-ref/sret, native-PE exit 42**); C2 ✅ Win64 (a72d249); **C3 ✅ AAPCS64+Apple x8-sret + HFA (d25696d + 03ba02d) — closed the parent `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` (all 4 ABIs) + a found+fixed HIGH pre-existing multi-piece-`Return`-clone silent miscompile + the inlining-of-struct-returners hazard (gate-refused, post-review). FC7 ✅ COMPLETE — 315/315 + 133/133 c-subset examples (x86-native + arm64-qemu). Next: NON-FC7 array-storage cluster, then FC8.** |
| **FC8** ✅ *(enums ✅ · bit-fields ✅ · array-decay ✅ · bit-field init ✅ · enum-bitfields ✅ · typedef-enum-tc ✅ — ALL IN-SCOPE COMPLETE 2026-06-18; WIDE-UNIT ✅ · RVALUE-RUNTIME ✅ · ABI-EXACT ✅ · AGGREGATE-VALUED-CONTROL-EXPR ✅ 2026-06-18 — all 3 §B deferrals + the aggregate ternary/comma sibling closed; FC8 SUBSTRATE COMPLETE)* | Enums end-to-end (values + enum-typed exprs); **bitfields** (their packing rules are part of the FC6 per-ABI parameter set — MS vs SysV bitfield allocation differs, declared in `.target.json`, never hardcoded); non-string `const` aggregate globals in rodata. **§A.5:** exit-code/data-read corpus on x86_64+arm64 ELF + arm64 Mach-O (the rodata-aggregate-on-arm64 high-VA-reloc territory of `D-LK6-AARCH64-ADDABSLO12-HIGH-VA`). **EARLY-LANDED via the struct-head cycle (2026-06-17, closing the FC4 residue `D-CSUBSET-STRUCT-BODY-VARDECL-POSITION` ✅):** the **non-string `const` aggregate-global → rodata PRODUCER is DONE** (`D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL` ✅ — `encodeAggregateValue` byte-exact struct/array/union globals, corpus `struct_body_top_level` exit 42 on x86_64-PE + arm64-ELF/qemu) — so FC8 inherits that piece. The **enum-value-as-int** half is now **✅ COMPLETE (FC8 cycle 1, 2026-06-17, branch p10 — `D-CSUBSET-ENUM-INT-CONVERSION` ✅):** an enum behaves AS its underlying int (C 6.7.2.2) across assign/UAC/cast/`coerce` + the `isEnumerator`-gated enumerator Ref→Const fold + `++/--` computed at the underlying + the REVIEW-CAUGHT LIR `reprKind` packed-enum-field memory-width fix (an over-wide enum field/element store clobbered its neighbour — `D-LIR-INT-MEMORY-WIDTH-EXACT` for enums, masked by scalar-local slots); corpus `enum_value` exit 7 incl. the packed-field width witness (red-on-regression), x86-PE/ELF + arm64-qemu; 7 unit pins + 1 LIR width pin, all red-on-disable. **Bit-fields ✅ COMPLETE (FC8 cycle 2, 2026-06-18, branch p10 — `D-CSUBSET-BITFIELD` ✅):** named/anonymous/zero-width, signed+unsigned, read/write/packing — bit-width on the interned Struct/Union scalars (empty when none) + a config-driven `BitFieldStrategy` (`.target.json` `aggregateLayout.bitFieldStrategy = gnu_packed`) GNU/SysV LSB-first packing in `computeLayout` + `hir_to_mir` extract (LShr+And / Shl+AShr sign-extend) & read-modify-write insert reusing existing MIR ops; corpus `bitfields` exit 42 (red-on-disable → 11) x86-PE/ELF + arm64-qemu; 7 layout pins + 4 diag fail-loud pins + 1 structural codegen pin (all red-on-disable). The **§B-decided ABI-exact** per-platform layout is trigger-gated (`D-CSUBSET-BITFIELD-ABI-EXACT`); enum-typed bit-fields were deferred (`D-CSUBSET-ENUM-BITFIELD` — closed in cycle 5, below). **Non-string array→ptr DECAY ✅ COMPLETE (FC8 cycle 3, 2026-06-18, branch p10 — `D-LK4-RODATA-PRODUCER-LOCAL-ARRAY-DECAY` + `-NONSTRING-GLOBAL-ARRAY-DECAY` ✅):** a non-string array used as a pointer decays to `&arr[0]` via `lowerLvalueAddress`+`Gep(0)` (local alloca / global GlobalAddr / member / indexed sub-array), reusing the existing lvalue-address + aggregate-global-rodata infra; corpus `array_decay` exit 42 incl. write-through, x86-PE/ELF + arm64-qemu, red-on-disable. (The stale `D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY` was reconciled — non-string array GLOBALS already work via the aggregate-global producer.) **Bit-field aggregate INIT ✅ COMPLETE (FC8 cycle 4, 2026-06-18, branch p10 — `D-CSUBSET-BITFIELD-INIT` ✅):** aggregate INITIALIZATION of a bit-field struct/union packs per allocation unit at all 3 sites (local slot via `lowerBitfieldAggregateInitIntoSlot`, a two-pass zero-then-write; global via the `asm.cpp` byte encoder; const-fold rvalue) + the C 6.7.9 anonymous-bit-field positional-skip in `lowerBraceInit`; REVIEW-CAUGHT F1 — an ordinary field sharing a bit-field's allocation unit was clobbered by lazy per-unit zeroing (a global/local divergence) — folded the same cycle via the two-pass; a non-foldable runtime by-value rvalue → `D-CSUBSET-BITFIELD-RVALUE-RUNTIME`; corpus `bitfield_init` exit 42 (x86-PE/ELF + arm64-qemu), red-on-disable. **Enum-typed bit-fields ✅ COMPLETE (FC8 cycle 5, 2026-06-18, branch p10 — `D-CSUBSET-ENUM-BITFIELD` ✅):** an enum bit-field (`enum E e : W;`) validates its width against the underlying (`resolveBitfieldSuffix` via `enumUnderlyingOrSelf`) and lowers its unit access at the underlying (`enumReprType` at emitBitfieldExtract/Insert/constIntOfType); a signed-underlying enum bit-field SIGN-extends. Independent review caught + re-pinned the codegen red-on-disable honestly (negative-enumerator corpus `enum_bitfield` exit 42-not-74 + a MIR op-representation pin). **Typedef-enum trailing comma ✅ COMPLETE (FC8 cycle 6, 2026-06-18 — `D-CSUBSET-TYPEDEF-ENUM-TRAILING-COMMA` ✅, RECONCILIATION):** already closed by the global `recomputeAltExpectedSets` fixpoint (verified via a toggle that reddens BOTH trailing-comma pins; the original inline-vs-named root-cause was inaccurate — the fixpoint, not the rule shape, is the determinant); no grammar change, pinned `D5_5_TypedefEnumTrailingCommaParses`. **ALL IN-SCOPE FC8 NOW COMPLETE** — the remaining FC8 work is the user-authorized bit-field deferral builds (§B resolved 2026-06-18): `D-CSUBSET-BITFIELD-WIDE-UNIT` ✅ CLOSED 2026-06-18 (64-bit-base bit-fields end-to-end — the "64-bit ALU" premise was refuted; the real gap was wide-constant materialization, done via `mov r64,imm64` [x86] / MOVK ladder [arm64], capability-probed; + a latent copyLiteralPool fix; 324/324); `D-CSUBSET-BITFIELD-RVALUE-RUNTIME` ✅ CLOSED 2026-06-18 (the by-value aggregate-rvalue carrier — design B synthesized-slot in `lowerLvalueAddress`, general for ALL aggregate rvalues incl. non-bit-field + the discard position; +232-line dead-SSA-path deletion; opened sibling `D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR` for the aggregate ternary/comma carrier); `D-CSUBSET-BITFIELD-ABI-EXACT` ✅ CLOSED 2026-06-18 (byte-ABI-exact bit-field layout: `msvc_straddle` [PE, cl.exe-verified byte-exact] / `gnu_packed` [ELF+Mach-O, Apple==gnu_packed for bit-fields]; strategy resolved at the FORMAT level; a conformance witness shells to the native compiler [cl.exe/gcc/clang] and asserts dss's layout matches). **ALL 3 §B bit-field deferrals (WIDE-UNIT + RVALUE-RUNTIME + ABI-EXACT) are now CLOSED; and the sibling `D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR` ✅ CLOSED 2026-06-18** (the aggregate-typed ternary/comma rvalue carrier — `lowerLvalueAddress` gains Ternary+SeqExpr arms that materialize EACH control-flow arm into ONE common slot under a CFG diamond, with NO Phi-of-aggregate [memory-based model]; the gate's independent review was SHIP-CLEAN but surfaced — and this cycle SEALED — a PRE-EXISTING discard-position silent miscompile [a discarded aggregate `Ternary`/`SeqExpr` with named-lvalue arms produced a forbidden Phi-of-aggregate] via a GENERAL `ExprStmt` aggregate chokepoint + anti-resurrection guards on `lowerExpr` Ternary/SeqExpr; corpus `aggregate_ternary_rvalue` PE/ELF/qemu=42, 5 MIR pins, red-on-disable demonstrated). **FC8 SUBSTRATE COMPLETE — FC9 is the next block.** | `D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY`; `D-CSUBSET-ENUM-INT-CONVERSION` ✅ (enum half, FC8 c1 2026-06-17); `D-CSUBSET-BITFIELD` ✅ (bit-fields, FC8 c2 2026-06-18); `D-LK4-RODATA-PRODUCER-LOCAL-ARRAY-DECAY` + `-NONSTRING-GLOBAL-ARRAY-DECAY` ✅ (array decay, FC8 c3 2026-06-18); `D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL` ✅ (rodata producer, done early) |

### Cluster C — Aggregate ABI *(the heaviest; "all current targets now" — user §B. Each FC = the COMPLETE ABI for ONE calling convention, runtime-closed on its native CI leg.)*
> **FC9's plan-lock §B is ✅ RESOLVED + SHIPPED (2026-06-19 reconciliation):** the cluster-C parameters-vs-algorithm boundary was locked and built under FC7 Phase 2's §B (HIR→MIR boundary + Option-A multi-register Arg, user-resolved) — the config fields landed as `aggregateClassification` (a closed `AggregateClassKind` strategy enum: `sysv_eightbyte`/`win64_by_size`/`aapcs64_hfa`) + `aggregateMaxRegBytes` + `indirectResultRegister`/`aggregateSretViaHiddenArg` on the target ABI config, and the bounded per-CC classification ALGORITHM lives in the `classifyAggregate` realization tier (`aggregate_abi.cpp`), keyed on the strategy enum — NEVER on cc.name/target identity (`lir_callconv.cpp` has zero identity branches; the lowering routes on `AbiPieceClass`). FC12 (variadic) inherits this settled boundary — no new cluster-C §B. **ORIGINAL (the §B as opened):** **FC9 opens with a plan-lock §B** (parallel to FC13's) locking the parameters-vs-algorithm boundary for ALL of cluster C: the new config fields on `TargetCallingConvention`/the ABI catalog (e.g. `aggregateMemoryThresholdBytes`, `maxAggregateRegs`, `hfaMaxMembers`, `structReturnByAddress`, `indirectResultRegister`) vs the bounded per-CC classification ALGORITHM in `lir_callconv.cpp`/`mir_to_lir::lowerCall`. **Cluster C assumes FC3's width/signedness types are shipped** (struct field widths feed layout/ABI) — do NOT reorder C ahead of A.

| Phase | What | Closes |
|---|---|---|
| **FC9** ✅ *(reconciled 2026-06-19 — DELIVERED EARLY by FC7 Phase 2 C1a/b/c)* | x86_64 **SysV** aggregate ABI: struct-by-value args (the eightbyte INTEGER/SSE/MEMORY classification — bounded per-CC algorithm, config-parameterized), struct return (small in regs / large via hidden pointer). **✅ DONE via FC7 Phase 2** (the `sysv_eightbyte` strategy in the `classifyAggregate` realization tier `aggregate_abi.cpp:149-184`, keyed on the `AggregateClassKind` enum — NOT cc.name/target identity; config `aggregateClassification`/`aggregateMaxRegBytes=16` in `x86_64.target.json:1490-1491`; 13 SysV classifier pins `test_aggregate_abi.cpp:46-211` + multireg-Arg/ReturnPiece/sret/mixed-class MIR pins `test_mir_lowering_c_subset.cpp:2816-3016`; corpora `struct_byval_sysv`/`struct_return_sysv`/`mixed_class_args` runtime-closed exit 42 on ELF-x86_64 + the linux CI legs). The **§A.5 honesty** residual stands: the **Mach-O-x86_64 arm is byte-pinned-only + RUNTIME-PENDING** (no macOS-x86_64 CI leg — `macos-latest` is Apple Silicon; pinned, §0.2). | `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` ✅ (FC7 Phase 2) |
| **FC10** ✅ *(reconciled 2026-06-19 — DELIVERED EARLY by FC7 Phase 2 C2)* | x86_64 **Win64** aggregate ABI: aggregates >8B by address, hidden-pointer return + shadow space; PE runnable corpus. **✅ DONE via FC7 Phase 2** (the `win64_by_size` strategy `aggregate_abi.cpp:80-97`, `aggregateMaxRegBytes=8` `x86_64.target.json:1507-1508`; 5 Win64 classifier pins `test_aggregate_abi.cpp:213-268` + shadow-space/by-ref callconv pins; corpus `struct_byval_win64` runtime-closed exit 42 on **PE**). | `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` ✅ (FC7 Phase 2 C2) |
| **FC11** ✅ *(reconciled 2026-06-19 — DELIVERED EARLY by FC7 Phase 2 C3)* | arm64 **AAPCS64** + **Apple** aggregate ABI: HFA/HVA homogeneity in SIMD regs, by-address, indirect-result-location; ELF + Mach-O arm64 corpus (qemu + macos-latest — both runtime-close). **✅ DONE via FC7 Phase 2** (the `aapcs64_hfa` strategy `aggregate_abi.cpp:99-147` + `indirectResultRegister:x8`/`aggregateMaxRegBytes=16` `arm64.target.json:1374-1404`, Apple==AAPCS64 for aggregates; 7 AAPCS64 classifier pins `test_aggregate_abi.cpp:279-360` incl. the 4-double-HFA-in-4-FPRs-not-by-ref case + HFA/x8-sret callconv+MIR pins; corpus `struct_byval_aapcs64` runtime-closed exit 42 on **arm64-ELF qemu** + the Apple arm on the **macos-latest** leg). | `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` ✅ (FC7 Phase 2 C3); `D-FF3-APPLE-ARM64-ABI-DIVERGENCE` (aggregate half ✅; variadic half → FC12c) |
| **FC12a** 🟡 *(CORE ✅ + STRUCT ✅ 2026-06-19 + >16B-MEMORY-class ✅ + fixed-overflow ✅ 2026-06-20 — `D-FC12A-VARIADIC-CALLEE` ✅; only the al-gated-spill size-opt [gated, §B] + FFI-descriptor half open)* | **SysV** variadic ABI: the `__va_list_tag` register-save-area (gp_offset/fp_offset/overflow_arg_area) + `va_start`/`va_arg`/`va_end` lowering; real `printf`-family FFI; ELF corpus. **✅ CORE DONE:** SCALAR `va_start`/`va_arg`/`va_end` + the register-save-area, config-driven (a new `vaListLayout` block on the CC) + source/target/linker-AGNOSTIC (va_arg = a dedicated `SizeOf`-style HIR node; va_arg classifies via the existing `classifyAggregate`; zero identity branches). Run-witnessed ELF-x86_64: `varargs_sum` 100 (GPR), `varargs_double` 50 (SSE), `varargs_overflow` 55 (the overflow_arg_area read path). Also closed a pre-existing gap (variadic DEFINITIONS now build a variadic FnSig via a declarator-level marker). **✅ STRUCT DONE (FC12a-struct):** struct-BY-VALUE across the variadic boundary for the **InRegisters (≤16B eightbyte)** class at all three sites — (1) caller passes a struct vararg (the `appendByValueArg` piece-expansion + a running-per-class-count **atomic-fit** check that fails loud on a register/stack split), (2) `va_arg(ap, struct)` gathers the eightbytes from the reg-save-area into a temp via an atomic reg-vs-overflow diamond and returns the struct's ADDRESS (new `lowerLvalueAddress` `VaArg` arm; bare-rvalue `lowerExpr` arm fail-loud anti-resurrection), (3) a fixed InRegisters struct param in a variadic fn (`receiveByValueParam` advances `argCtr` per-class → `va_start` cursors reflect the pieces); ALSO fixed the `call_payload` `fixedArgCount`→`fixedOperandCount` operand-unit boundary (a struct expands to N operands — the AL/vector-count was param-unit-wrong for variadic struct-returning calls). Reuses `classifyAggregate` (closed-enum, agnostic — Win64 fails loud, no `vaListLayout`). Run-witnessed ELF-x86_64: `varargs_struct` 195 (mixed `{long;double}` gather + same-class `{long,long}`/`{double,double}` cursor-threading + a fixed struct param). **DEFERRED (all fail-loud, no silent miscompile):** >16B **MEMORY-class** structs at the variadic boundary AND the register-exhaustion split (`D-FC12A-VARIADIC-MEMORY-CLASS-STRUCT` ✅ CLOSED 2026-06-20 — the Option-C `ByValueStackArg` by-value-overflow carrier); the al-gated SSE-spill size optimization (`D-FC12A-VARIADIC-AL-GATED-SPILL`); fixed-params-overflow-to-stack (`D-FC12A-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`). | `D-FC12A-VARIADIC-CALLEE` ✅ (core + struct); `D-FC12A-VARIADIC-MEMORY-CLASS-STRUCT` ✅ (>16B/split, 2026-06-20); `D-FFI-DESCRIPTOR-VARIADIC-SIGNATURE` (descriptor half, open) |
| **FC12b** ✅ *(SCALAR+FP ✅ 2026-06-19 + STRUCT-VARARG ✅ 2026-06-20 — `D-FC12B-WIN64-VARIADIC-CALLEE` + `D-FC12B-WIN64-STRUCT-VARARG` both closed; Win64 variadic COMPLETE)* | **Win64** variadic ABI (homogeneous `char*` `va_list`, contiguous home+overflow); PE corpus. **✅ DONE (scalar + FP):** the per-CC va_list TYPE + lowering STRATEGY is a closed enum `VaListStrategy` (`SysVRegisterSave`/`HomogeneousPointer`/`Aapcs64DualCursor`, in `aggregate_layout.hpp` beside the other strategy enums) — every va seam dispatches on it with a fail-loud default, zero identity branches. Win64 `va_list` is a `char*` (8B, vs SysV 24B — threaded into `analyze()` via a new `optional<VaListStrategy>` param so sizeof is right per-ABI: **BLOCKER-2**); va_arg is a LINEAR pointer bump (no diamond) across the CONTIGUOUS home(rcx/rdx/r8/r9)+overflow area whose base is `totalFrameSize + callPushBytes` with **NO shadowSpaceBytes** (the home IS the shadow space: **BLOCKER-1**, via a new `VaHomeArgAreaAddr` MIR leaf + `va_home_arg_area` LIR op carrying the named-arg slot count; the prologue spills the named int regs into the same base — spill-target == va_arg-read-target, congruence-pinned). FP varargs are passed in both xmm AND the matching home GPR, so the CALLER duplicates each register-resident FP vararg into argGprs[slot] via a new `movq_xmm_to_gpr` opcode (`66 REX.W 0F 7E /r`, byte-pinned). Run-witnessed PE-x86_64 on the Windows host: `varargs_win64_sum` (int + double varargs, anti-folded). **✅ STRUCT-VARARG DONE 2026-06-20:** Win64 struct-by-value varargs (`D-FC12B-WIN64-STRUCT-VARARG` ✅ CLOSED — reused `appendByValueArg` + the existing Win64 slot placement [≤8B pow2 by-value in 1 slot / else by-ref copy pointer in 1 slot] + the HomogeneousPointer `va_arg` arm [≤8B → the slot addr; >8B → deref the slot pointer]; NO force-to-stack carrier needed; run-witnessed PE `varargs_win64_struct` exit 45). | `D-FC12B-WIN64-VARIADIC-CALLEE` ✅ (scalar+FP); `D-FC12B-WIN64-STRUCT-VARARG` (struct, pinned) |
| **FC12c** ✅ *(SCALAR+FP ✅ 2026-06-19 + STRUCT-VARARG ✅ 2026-06-20 + fixed-stack-overflow ✅ 2026-06-21 [`D-FC12C-AAPCS64-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`, qemu exit 210] — AAPCS64 qemu-witnessed; Apple Mach-O runtime witnessed green on the macos-15-arm64 leg [run 27891928277])* | **AAPCS64 + Apple** variadic ABI (Apple = always-stack variadic — the `D-FF3-APPLE-ARM64-ABI-DIVERGENCE` variadic half); arm64 corpus. **✅ DONE (scalar + FP), §B-FORCED (no fork — reuses the FC12b `VaListStrategy` substrate; the architect+design-audit confirmed every item forced by the AAPCS64 §B.6 + Apple specs).** **AAPCS64 (Linux arm64) = the realized `Aapcs64DualCursor` strategy:** `va_list` = a 5-field `__va_list` struct (32B); dual-cursor `va_arg` (NEGATIVE `__gr_offs`/`__vr_offs` from `__gr_top`/`__vr_top`, else `__stack`); a callee register-save-area spilling x0-x7 (8B) + v0-v7 (**16B via a NEW `fstur_q` STUR-Qt opcode `0x3C800000`** — design-audit BLOCKER-1: the architect's `0x3C000000` was STURB/8-bit); the negative i32 cursor is **sign-extended (SXTW) before the address Gep** (BLOCKER-2: a non-extended −40 → +4GB wild addr → segfault). Run-witnessed under qemu-aarch64: `varargs_aapcs64_sum` 100 / `_double` 50 / `_double_overflow` 60 (the FP `__stack` overflow arm) / `_overflow` 55 (the GPR `__stack` arm) / `_sret` 126 (a variadic fn RETURNING a >16B struct — the review caught a silent miscompile where the prologue scratch-base clobbered the x8 sret pointer; fixed by excluding `cc.indirectResultRegister` from the scratch pick). **Apple arm64 = `HomogeneousPointer` (char* `va_list`, reused from FC12b) + two config bits:** `variadicUsesOverflowBase` (va_start base = `VaOverflowArgAreaAddr`, congruent at `totalFrameSize` since shadow=callPush=0) + `variadicArgsAlwaysStack` (the CALLER forces ALL varargs to the stack); no home spill. `varargs_apple_sum` compiles to a valid Mach-O arm64 PIE — **runtime WITNESSED GREEN on the macos-15-arm64 CI leg** (run 27891928277, 2026-06-21 — `varargs_apple_sum`/`_struct` executed natively with their asserted exits; the AAPCS64 qemu witness covers the shared substrate). ALSO fixed a latent SHARED regalloc bug (`pickScratchRegs` uint64-bitmask→per-class-set; arm64 d31 at global ordinal 64 overflowed the mask → fail-loud crash on FPR-heavy fns). Every va seam dispatches on the closed `VaListStrategy` (zero identity branches). **DEFERRED (fail-loud, pinned):** arm64 struct-by-value va_arg / struct-vararg (`D-FC12C-AAPCS64-HFA-STRUCT-VARARG` ✅ CLOSED 2026-06-20 — the HFA dual-cursor VR-gather + ≤16B-GPR + >16B-by-ref callee va_arg + caller per-class atomic-fit/carrier + the Apple Step-2.0 size-aware HomogeneousPointer bump; run-witnessed ELF `varargs_aapcs64_struct` + Mach-O `varargs_apple_struct` [macos-15-arm64 leg, run 27891928277]); ✅ CLOSED 2026-06-21: AAPCS64 fixed-params-overflow-to-stack (`D-FC12C-AAPCS64-VARIADIC-OVERFLOW-FIXED-STACK-ARGS` — qemu exit 210, after `D-ASM-AARCH64-LARGE-FRAME-IMM12` closed). | `D-FF3-APPLE-ARM64-ABI-DIVERGENCE` (variadic half ✅, Apple runtime witnessed — macos leg run 27891928277); `D-FC12C-AAPCS64-VARIADIC-CALLEE` ✅ + `D-FC12C-APPLE-ARM64-VARIADIC-CALLEE` ✅ (scalar+fp); `D-FC12C-AAPCS64-HFA-STRUCT-VARARG` ✅ (2026-06-20) + `D-FC12C-AAPCS64-VARIADIC-OVERFLOW-FIXED-STACK-ARGS` ✅ (2026-06-21) |

### Cluster D — Preprocessor *(PARALLEL/INTERLEAVABLE track — user §B; a NEW token-stream transform UPSTREAM of the parser, architecturally DISTINCT from the existing post-parse tree-merge `#include` resolver)*
> **⚠ §B DESIGN FORK at FC13-open.** The preprocessor MUST stay agnostic — a config-SELECTED/DECLARED pass (the `.lang.json` opts in + declares directive + macro syntax; the engine is a generic token-stream rewriter, building on the EXISTING config-driven `popAtNewline` directive-lexing substrate). It is NOT an `if (lang=="c")` in shared substrate, and NOT the current tree-merge `#include` (which loads a header as a separate `Tree` + `CrossTreeRef` AFTER parsing — a real PP splices TOKENS BEFORE parsing). The §B owns: (1) the config-declared-vocabulary vs generic-engine model; (2) the fate of the existing include-following resolver (co-exist vs supersede — `import_resolver`); (3) the multi-language opt-in pin (toy/tsql must get NO macro expansion — a strict test proves it). Disambiguate from plan-11 **FF9** (a separate post-v1 header-parser concept, NOT this pass).

| Phase | What | Closes |
|---|---|---|
| **FC13** ✅ | Preprocessing substrate (C translation phases 3–4): object + function-like macro definition/expansion; `#include` TEXTUAL token expansion. (Resolves the FC13 §B above.) **Cycle 1 ✅ 2026-06-19 (worktree p13, branch feature/0-0-2-p13):** the config-SELECTED pre-parse preprocessor substrate — object-like `#define`/`#undef` + macro expansion (rescan + blue-paint self-reference guard) + textual quote-`#include` (recursive text-concat into ONE synthesized buffer + a LINE-MAP for diagnostic origin attribution); a function-like macro definition FAILS LOUD; angle-`#include <h>` is LEFT in the stream and co-exists with the post-parse FF11 resolver (the resolver's quote arm is gated off under `preprocess().enabled`); a `directive`/`header-context` lexer-mode split fixes the catastrophic `<`-in-`#define` mis-lex. §B (user) = TEXT-CONCAT + LINE-MAP representation. 330/330; independent review SHIP-WITH-FIXES + dss-audit GREEN-BUT-RULE-BREAKING → 4 must-fixes folded (agnostic `functionLikeOpenToken` config field replacing a hardcoded `"ParenOpen"`; full origin-buffer diagnostic attribution via `CompilationUnit::auxiliaryBuffers()` registered with the driver `BufferRegistry`, CLOSING `D-PP-INCLUDE-SPLICE-POSITION-ATTRIBUTION`; the quote-include double-diagnostic resolver gate; a `<<`-in-`#define` regression pin) → fold re-review SHIP. **Cycle 2 ✅ 2026-06-19 (function-like macros — `D-PP-FUNCTION-LIKE-MACRO` CLOSED):** def/parameter-list parse + invocation expansion over the common direct-invocation subset of C §6.10.3 — paren-balanced argument collection (config-driven `(`/`)`/`,` token kinds), full argument PRE-expansion, parameter substitution, rescan + blue-paint self-reference freeze; arity / unterminated / duplicate-param / malformed-list / variadic all fail loud (`P_PreprocessorMacroArgument`/`P_PreprocessorDirective`/`P_PreprocessorUnsupported`). 331/331; review SHIP + dss-audit GREEN-BUT-RULE-BREAKING → 4 folds (config-drive the variadic marker by token-KIND not the `...` lexeme; a `)`/`,` agnosticism RED-on-disable pin; a fail-loud diagnostic at the depth-256 expansion backstop; pin `D-PP-MACRO-HIDESET-PRECISE` for the cross-stream-boundary hide-set gap — residual cases fail loud, NOT silent) → fold re-gate 331/331 + spot-check clean. **Cycle 3 ✅ 2026-06-19 (variadic macros — `D-PP-VARIADIC-MACRO` CLOSED):** a trailing `...` (config `variadicMarkerToken` KIND) makes a macro variadic; an invocation binds the named params then gathers the rest into a config-driven `__VA_ARGS__` (preserving the source separator commas), pre-expanded + substituted + rescanned; PLAIN `__VA_ARGS__` only (NO `#`/`##` / GNU comma-elision — that stays FC15); C23 empty-variadic allowed; fail-loud on `...`-not-last / too-few-args / `__VA_ARGS__`-in-non-variadic / `__VA_ARGS__`-as-parameter-name (C 6.10.3p6). 332/332; review SHIP-WITH-FIXES + dss-audit VERIFIED-CLEAN (30+ adversarial CLI probes, zero silent miscompiles) → 2 folds (the `__VA_ARGS__`-as-param-name guard + 2 edge-case pins). **Remaining FC13 deferrals (user §B 2026-06-19 — build the hide-set, keep the oracle): `D-PP-MACRO-HIDESET-PRECISE` (precise per-token hide-set — NEXT = cycle 4), `D-PP-FC2-ORACLE-RETIRE` (oracle KEPT — correct + needed for the in-buffer forward-ref case; cleanup-only, deferred).** **Cycle 4 ✅ 2026-06-19 (precise per-token hide-set — `D-PP-MACRO-HIDESET-PRECISE` CLOSED):** the recursion-scoped blue-paint was replaced by PROSSER's per-token hide set (the standard §6.10.3.4 realization — an `ExpToken{Token, HideSet=shared_ptr<set<string> const>}` working wrapper since `Token` is 16B; a SPLICE-RESCAN cursor; object → `hide ∪ {M}`, function-like → `(hide(name) ∩ hide(close)) ∪ {M}`), so the cross-stream-boundary witnesses now EXPAND (`A(F)(3)` → `((3)+100)`, `NAME(4)` → `((4)*(4))`) while self-reference/mutual freeze is preserved. 333/333; review SHIP + dss-audit VERIFIED-CLEAN (DIFFERENTIAL oracle vs gcc 13.2.0 `-E` on ~30 probes incl. Prosser's `f(2)(9)` → `2*9*g` and the intersection-freeze `G K(3)` → `H (3)` — ZERO divergence from gcc). **FC13 ✅ 100% COMPLETE (cycles 1–4: object + function-like + variadic macros + textual quote-`#include` + the precise hide-set; `D-PP-INCLUDE-SPLICE-POSITION-ATTRIBUTION` + `D-PP-FUNCTION-LIKE-MACRO` + `D-PP-VARIADIC-MACRO` + `D-PP-MACRO-HIDESET-PRECISE` ALL CLOSED). Remaining: `D-PP-FC2-ORACLE-RETIRE` (oracle KEPT — user §B chose defer; correct + needed for the in-buffer forward-ref); `#`/`##`/stringize/predefined = FC15 (out of FC13 scope). Cycle-4 audit also flagged a PRE-EXISTING, macro-INDEPENDENT parser recursion-depth overflow on pathologically deep expressions (a plain `((((0))))` reproduces it; the PP itself fires its backstop) → future-work, out of FC13 scope.** | — (new substrate) |
| **FC14** ✅ **CLOSED 2026-06-22 (`D-PP-CONDITIONAL-COMPILATION` ✅)** | Conditional compilation: `#if`/`#ifdef`/`#ifndef`/`#elif`/`#else`/`#endif` + `defined()`. **Shared substrate (honored):** its `#if` integer-constant-expression (ICE) evaluator is the SAME const-expr evaluator FC3 (enum/array-size) + FC8 (`enum { A = 1<<3 }`) need — built ONCE, agnostically (not two). **DELIVERED** (registry `D-PP-CONDITIONAL-COMPILATION`, 356 ctest): a single-pass condition stack in `MacroExpander` (the 6 conditional directives dispatch UNCONDITIONALLY to balance-track nesting; `#define`/`#undef`/`#include` + body emission gated on `stackActive()` so a dead `#if 0` group is elided per C 6.10.1) + a NEW lightweight preprocess-time token-Pratt ICE evaluator (`src/analysis/preprocess/pp_if_eval.{hpp,cpp}`) that REUSES the main const-eval with ZERO duplication (the §B — `opFromName`/`opEntryFor`/`const_eval_arith`/`decodeInteger`/`operatorTable()` precedence all shared via `src/hir/const_eval_operators.hpp`); `defined(X)`/`defined X` resolved BEFORE expansion; short-circuit `&&`/`||`/ternary fold only the live arm; div0/shift/overflow refuse via `EvalOptions`. Runnable `examples/c-subset/preprocessor_conditionals` (exit 42, 4 targets + release). | `D-PP-CONDITIONAL-COMPILATION` ✅ |
| **FC15** ✅ | Token-paste `##`, stringize `#`, predefined macros (`__FILE__`/`__LINE__`/`__STDC__`/`__STDC_VERSION__`…), `#pragma` (recognized), `__has_include`/`__has_c_attribute`. **Cycle a ✅ 2026-06-24 (branch feature/0-0-2-p15, 395 ctest):** `#`/`##` in FUNCTION-LIKE macros — `#` stringize (C 6.10.3.2: whitespace-collapse + `"`/`\` escaping, RAW un-pre-expanded operand) + `##` paste (C 6.10.3.3: LEFT-TO-RIGHT, raw operands, **re-tokenized → exactly ONE token else fail-loud**). Agnostic config-declared token KINDs (`stringizeToken="HashOp"` reuse + `pasteToken="HashHashOp"`; opt-out pinned for non-PP langs). **§B (user) = A2** synthetic-token buffer: each product's spelling is APPENDED to the synth text BEFORE the single parse buffer freezes → product tokens span the appended tail (ONE buffer, no side-vector; `ppLexemes` stays span-sliced = parser-faithful by construction). Plan-lock folded F1 (re-tokenize→1-token) / F2 (real escaping) / F3 (HashOp non-contamination) / F4 (no-order-claim chain pin). New `P_PreprocessorStringize`/`P_PreprocessorPaste`; 15 red-on-disable pins + runnable `preprocessor_stringize_paste` exit 42 (release arm — paste→`add3` resolves in the real parser). Residuals (fail-loud, NOT silent): `D-PP-PASTE-OBJECT-LIKE` (`##` in object-like macros), `D-PP-PASTE-PLACEMARKER` (empty-operand C 6.10.3.3p2), `D-PP-VARIADIC-GNU-COMMA-ELISION` (`,##__VA_ARGS__`). **Cycle b ✅ 2026-06-24 (predefined macros — branch feature/0-0-2-p15, 396 ctest):** `__FILE__`/`__LINE__`/`__STDC__`/`__STDC_VERSION__`/`__STDC_HOSTED__`/`__DATE__`/`__TIME__` (C 6.10.8). Config kind-table {line,file,constant,date,time} — AGNOSTIC (the engine dispatches on `kind`, NEVER the macro NAME; zero `__LINE__`/`__FILE__` literals in src; opt-out pinned). `__LINE__`/`__FILE__` are OFFSET-DERIVED via the line-map against the INVOCATION offset (`ExpToken.invOffset` threaded — REPLACEMENT tokens INHERIT the invoking offset, ARGUMENT tokens KEEP their own — so `#define WARN __LINE__` resolves to the INVOCATION line per C 6.10.8.1, NOT the define-site line; **the plan-lock caught the architect's define-site silent-miscompile; make-or-break red-on-disable pin**). A2 reuse for value tokens; `#if __STDC_VERSION__ >= …` works (`pp_if_eval` combined prefix+product buffer); `#define`/`#undef` of a predefined → fail-loud `P_PreprocessorPredefinedMacro`. 13 red-on-disable pins + runnable `preprocessor_predefined_macros` exit 42 (release arm; x86 PE + arm64-ELF qemu). Opens `D-PP-LINE-DIRECTIVE` (`#line` — `__LINE__`/`__FILE__` report the PHYSICAL line/name until it lands; fail-soft). **Cycle c ✅ 2026-06-24 (`#pragma` + `__has_include` + `__has_c_attribute` — 398 ctest):** `#pragma` consume-and-drop (C 6.10.6p2 "unrecognized pragmas are ignored"); `__has_include(<h>)`/`("h")` + `__has_c_attribute(attr)` as `#if`-operand operators (config-declared, rewritten in `pp_if_eval` like `defined`, operands NOT macro-expanded). AGNOSTIC: angle delimiters matched by config token KINDs (`hasIncludeAngleOpenToken`/`...CloseToken` = LtOp/GtOp), NOT a literal `<`/`>` byte-scan (plan-lock Finding 1 — input-classification by hardcoded byte is the trap PreprocessConfig forbids). **★ plan-lock Finding 3 (silent-miscompile caught + fixed): `__has_include(<h>)` MUST agree with what `#include <h>` does — both now funnel through ONE shared `resolveSystemDescriptor` (`stem + ".json"` + `findInDirs(systemDirs)`, since DSS ships `stdio.json` descriptors, NO `.h` on the system path); a naive literal-name search returned 0 while `#include <stdio.h>` succeeds.** `systemDirs` threaded (Option A; `compilation_unit.cpp`). New `P_PreprocessorHasInclude` (0x001C). 16 red-on-disable pins + 2 runnable corpora (`pp_pragma_consume` + `pp_has_include_and_attr`, exit 42, release arm, arm64-ELF qemu). Opens `D-PP-PRAGMA-OPERATOR-FORM` (`_Pragma("...")`) + `D-PP-PRAGMA-RECOGNIZED-SEMANTICS`. **Cycle d ✅ 2026-06-24 (FC15 paste residuals — 399 ctest Release, branch feature/0-0-2-p15): the 3 residuals close FC15 FULLY, no deferrals.** Object-like `##` (`#define HW a##b`→`ab`) now routes the object-like replacement through the SHARED `collapsePastes` chokepoint the function-like arm uses (no duplicated paste logic). Empty `##` operands become PLACEMARKERS (`bool placemarker` on `ExpToken`, marked in Phase A by `stampArgOrPM`; `collapsePastes` resolves `pm##X`→X / `X##pm`→X / `pm##pm`→pm + drops survivors at its RETURN so a placemarker never re-enters `expand`; `J(x,)`→`x`). GNU `,##__VA_ARGS__` comma-elision is CONFIG-GATED (`preprocess.variadicCommaElision`; separator matched by config arg-separator KIND, `__VA_ARGS__` by `variadicArgsName` — never a hardcoded `,`/name; empty→drop separator+`##`, non-empty→keep separator + pre-expanded args, no paste). A genuine dangling `##` still fails loud (boundary check UNCHANGED + fires first). plan-lock GO-WITH-FIXES (MUST-FIX-1: the vaArgs paste-operand fall-through uses `stampArgOrPM` so an empty `## __VA_ARGS__` is a placemarker not nothing; MUST-FIX-2: drop placemarkers at `collapsePastes` return, `run()` backstop). 12 red-on-disable pins + runnable `preprocessor_paste_residuals` (object-like ## + empty-arg placemarker + comma-elision → exit 42, release arm). **FC15 ✅ COMPLETE.** | ✅ D-PP-PASTE-OBJECT-LIKE / -PLACEMARKER / -VARIADIC-GNU-COMMA-ELISION; opens D-PP-VA-OPT (C23 `__VA_OPT__` — distinct mechanism) |

### Cluster E — C completeness + C23 modern + conformance *(caps the arc)*
| Phase | What | Closes |
|---|---|---|
| **FC16** ✅ **COMPLETE 2026-07-08 — the parse-without-semantics stub class is closed** | **Standard-C completeness the front-end parsed but doesn't yet MEAN** (the no-stub closure). ✅ **`_Generic`** generic selection (`D-CSUBSET-GENERIC-SELECTION`, `9353b74` — semantic-tier match+selection; clears the LAST P0009 gap). ✅ **`_Static_assert`/`static_assert`** (`D-CSUBSET-STATIC-ASSERT`, `16d27ac` — const-eval at Pass2Post so `sizeof`/enum fold; 1-arg + spelling; unsuppressable). ✅ **anonymous struct/union members** (`D-CSUBSET-ANON-MEMBER-PROMOTION`, `ea40d45` — promotion + HIR chain; clears S0017). ✅ **`_Alignof`/`alignof`** (6.5.3.4 — additive `HirKind::AlignOf` mirroring SizeOf, reads type alignment; const-evaluable) + **`_Alignas`/`alignas`** (6.7.5, 2026-07-07 — the FC6-layout integration: `D-CSUBSET-MEMBER-ALIGNAS` carrier [per-field `fieldAligns` interner span] + `D-CSUBSET-ALIGNAS` frontend [5 unsuppressable constraint codes] + `D-CSUBSET-ALIGNAS-VARIABLE-CODEGEN` [global `AssembledData.alignment` + local per-alloca frame `alignUp`; over-aligned STACK locals >stackAlignment fail-loud-gated `D-CSUBSET-ALIGNAS-OVERALIGNED-STACK-LOCAL`]; independent design+code audit caught+fixed 2 silent-miscompile seams [bitfield+alignas member, arm64 2nd over-aligned local]; 3-leg gate 579/579 + sqlite re-probe green). ✅ **wide/UTF STRING literals** (`L"…"`/`u"…"`/`U"…"`/`u8"…"`, 2026-07-08 — Cycle A of the user-approved 4-cycle split: config-driven `LiteralPrefixEntry` table [format-keyed wchar_t via `elementCoreByFormat`, NO engine branch] + tokenizer longest-match multi-char openers [zero engine change] + UTF-8-decode/BMP-encode + astral-cp fail-loud `H_WideCharSurrogateUnsupported`; independent design-audit caught 2 blocking gaps + orchestrator caught 1 agnosticism violation; 3-leg 587/587 + sqlite green). ✅ **wide/UTF CHAR literals** (`L'x'`/`u'x'`/`U'x'`/`u8'x'`, 2026-07-08 — Cycle B: the NEW C23 6.4.4.4 per-prefix char-typing rule [char TYPE depends on prefix; narrow `'x'`=int kept via wide-only override] + shared decode/validate at BOTH semantic+lowering [closes the `sizeof(u8'β')` silent-size gap] + core-keyed range checks [U8≤0x7F / U16≤0xFFFF / U32,I32≤0x10FFFF; `H_Utf8CharLiteralOutOfRange` + `H_WideCharValueUnrepresentable`]; independent design-audit caught 4 blocking fail-loud gaps; format-keyed witness `L'😀'` pe-fail/elf-pass; code-audit no-blockers; 3-leg 592/592 + sqlite green). ✅ **`\u`/`\U` universal-character-names + UTF-16 surrogate pairs** (6.4.3, 2026-07-08 — Cycle C: `\uXXXX`/`\UXXXXXXXX` decode with in-decoder surrogate/>0x10FFFF reject [`H_InvalidUniversalCharacterName`] + astral surrogate-pair encode for u16 strings [`u"\U0001F600"`→`3D D8 00 DE`; the char `u'😀'` stays fail-loud — one code unit]; ALSO eliminated a pre-existing silent miscompile — `\x`/octal in a wide literal now fail-loud `H_WideByteEscapeUnsupported` [was silently collapsing code units], full escape-value-as-code-unit deferred trigger-gated `D-CSUBSET-WIDE-HEX-OCTAL-ESCAPE-VALUE`; design-audit caught the silent miscompile + code-audit caught 2 witness-coverage gaps, all folded; per-format witness `L"\U0001F600"` pe-pair/elf-u32; 3-leg 599/599 + sqlite green). ✅ **adjacent-concat prefix-mixing** (6.4.5p5, 2026-07-08 — Cycle D, the arc FINALE: the run's element core = the effective prefix [any non-narrow prefix wins, narrow segments widen, position-independent] via a shared `effectiveStringConcatPrefix` chokepoint both tiers call; fixed 2 pre-existing SILENT defects [`"a" L"b"` mistyped narrow → now widens; `u"a" U"b"` silently U16 → now fail-loud `H_ConflictingStringLiteralPrefixes`] + closed the FF3-mixed hole [`"a" L"\xC3"` now fails loud]; the conflict compares opener TOKEN KINDS not resolved cores [format-invariant — `u"a" L"b"` rejected identically pe/elf]; design-audit caught the MF1 plain-statement miscompile + the MF2 format-divergence, code-audit VERIFIED-CLEAN; 3-leg 601/601 + sqlite green). **★ the wide/UTF literals arc is COMPLETE (A strings + B chars + C `\u`/`\U`/surrogate + D concat-mixing).** ✅ **struct/union `packed` attribute SEMANTICS** (`D-CSUBSET-PACKED`, 2026-07-08 — `__attribute__((packed))`/`[[gnu::packed]]`/`[[packed]]`/`__packed__` in the SUFFIX position [incl. the `typedef struct {…} __attribute__((packed)) N;` form] remove all inter-field padding + set the aggregate alignment to 1; a member `alignas` still raises per-field [alignas wins]; a per-composite `CompositeFields.packed` bool through the interner [mirrors the `fieldAligns` alignas channel] + a NON-DEFAULTED `completeComposite` param + a `type_reintern.cpp` carry [a packed type can't silently reintern unpacked across a CU boundary] + `packed` folded into `contentDeclSiteKey` guarded-on-true [a packed `S` is a distinct TypeId from a padded `S`]; codegen needed ZERO change — arm64 unaligned-tolerant LDUR/STUR + x86 mov, audit-confirmed + witnessed by an arm64-qemu runtime example reading/writing a misaligned field → 42; new `S_UnknownTypeAttribute`(0xE031) + `S_PackedBitfieldUnsupported`(0xE032, unsuppressable); every packed spelling×position honored-or-fail-loud [leading position → H_UnknownLinkageSpecifier; after-keyword → loud semantic rejection]; independent design-audit + code-audit both cleared; 3-leg ctest 603/603 + sqlite 3.54.0 + CRUD 42 on all 3 legs; registered deferrals `D-CSUBSET-PACKED-AFTER-KEYWORD-POSITION` + `-BITFIELD-INTERACTION` + `-PER-MEMBER-PACKED` + `-ATOMIC-MEMBER` [all fail-loud + gated]). ✅ **`noreturn` function-attribute SEMANTICS** (`D-CSUBSET-NORETURN`, 2026-07-08 — all 4 spellings [`_Noreturn` keyword / `[[noreturn]]` / `__attribute__((noreturn))` / `__attribute__((__noreturn__))`] set a config-driven `SymbolRecord.isNoreturn`, OR-merged across proto/def; shipped `abort`/`exit` marked noreturn; at HIR lowering a direct call to a noreturn callee is wrapped `Block{ ExprStmt(call), Unreachable }` — a structural mirror of `wrapIfProvablyInfinite` — so the path terminates, with ZERO hir_verifier/hir_to_mir/prune change [the missing-return relaxation + post-call dead-code fall out for free]; F1 miscompile-guard `isDirectNoreturnCall` inspects the callee `Ref` child [NOT `firstNameToken`] so an address-takeable `(cond?die:other)(1)` stays conservative; the audit-planned `{binding:global}` linkage no-op was REJECTED as a silent miscompile [last-wins `linkageFrom` would clobber a co-present `static`'s internal linkage] → config-driven `linkageSpecifierIgnoredNames` skips `noreturn` with NO linkage effect, fail-loud default kept [pinned by a `static noreturn`→Local regression]; independent design-audit + code-audit both cleared; 3-leg ctest 604/604 + sqlite 3.54.0 + CRUD 42 on all 3 legs; 2 gated deferrals `D-CSUBSET-NORETURN-VALUE-POSITION` [a noreturn call in value/for-clause position isn't wrapped → spurious-but-fail-loud H_VerifierFailure, NOT a miscompile] + `D-CSUBSET-NORETURN-NON-FUNCTION-OBJECT` [inert, isFnSig-gated]). ✅ **`volatile`-honoring codegen** (ALREADY-SHIPPED c21/c27/opt-10b — stale-plan correction; design pass traced all 5 cases clean, hardened 2026-07-08 with 3 coverage-by-construction pins + an ordering doc-contract). Both live battery gaps (S0017 + P0009) now CLOSED. **★ FC16 CORE COMPLETE → FC17/FC18 next (the parse-without-semantics stub class is closed).** | — |
| **FC17** ✅ **COMPLETE 2026-07-09 — nullptr · enum-underlying · typeof · constexpr · [[attributes]] all landed** | C23-specific modern: ✅ **`[[attributes]]` semantics** (`D-CSUBSET-ATTRIBUTE-SEMANTICS`+`D-CSUBSET-ATTRIBUTE-STATEMENT`, 2026-07-09 — the FC17 FINALE; config-table closed-verb engine [suppressUnused/warnOnUse/warnOnDiscard/none]: maybe_unused suppresses S000A, deprecated/nodiscard warn per-use/per-discard [0xE03D/0xE03E, proto/def OR-merged, the audit-caught TWO-hop discard check, `(void)f()` suppression pinned], `[[fallthrough]];` statement parses [declOrAttrStmt wrapper, hot-path zero-speculation], unknown `[[attr]]` = suppressible 0xE03C [C23-conformant], GNU spellings via linkageSpecifierIgnoredNames; 3-leg 611/611 + sqlite green; 6 gated deferrals), ✅ **`constexpr`** (`D-CSUBSET-CONSTEXPR`, 2026-07-09 — C23 §6.7.1 object storage-class; ZERO codegen; the delta vs `const` = eager declaration-time initializer validation [S_ConstexprNonConstantInitializer 0xE037 + 4 siblings 0xE038–0xE03B]; scalars incl. floats/enum + null-only pointers; C23 6.2.2p3 file-scope internal linkage via one config linkage entry; rides the shared CST const-eval [buildConstEvalEnv extraction, 3 invariants preserved] + F2 FIXED the pre-existing char/bool leaf-fold gaps [`_Static_assert('a'==97)`/`(true)` flipped fail→pass]; 3-leg 610/610 + sqlite green; deferrals: aggregates + (T*)0-cast-null), ✅ **`typeof`/`typeof_unqual`** (`D-CSUBSET-TYPEOF`, 2026-07-09 — C23 §6.7.2.5; a PURE semantic-tier type resolver [ZERO codegen; resolves to a TypeId in `resolveTypeNodeImpl`]; both spellings + expr & type-name operands + 7 type-specifier positions; `requireKnownType` polarity; design-audit caught+fixed a CRITICAL qualifier-scan leak [`opaqueRules` stops the volatile/const decl-head scans at typeof nodes so `typeof_unqual` actually strips]; `S_TypeofBitfieldOperand` 0xE036; 3-leg 609/609 + sqlite green), ✅ **`nullptr`/`nullptr_t`** (`D-CSUBSET-NULLPTR`, 2026-07-09 — NEW semantic-tier-only `TypeKind::NullptrT`; the keystone lowers `nullptr`→integer-0 null const at HIR [Fix 1(a)] so NullptrT NEVER reaches MIR; one-way `Ptr←NullptrT` conversion; explicit `S_NullptrInvalidOperand` fail-loud operator gate; `I_NullptrTypeInMir` tripwire; 4 gated deferrals incl. nullptr→bool), ✅ **enums with explicit underlying type** (`D-CSUBSET-ENUM-UNDERLYING-TYPE` + `D-PARSE-SPECULATIVE-OPTIONAL`, 2026-07-09 — `enum E : T` via a NEW GENERIC speculative-`{"optional"}` PARSER-ENGINE capability [user §B chose Option B → ZERO regression on the anon `enum:4` bit-field, over config-only]; the enum TYPE substrate was ALREADY generic [`scalars[0]`=underlying] so only the grammar + one `enumType()` call site; fail-loud `S_InvalidEnumUnderlyingType`0xE034 / `S_EnumeratorValueOutOfRange`0xE035; design-audit caught+fixed a body-probe bounded-cost defect [Fork B]; a stale-design codegen gap fixed [`mir_to_lir requireNativeIntWidth`→`reprKind`]; 3-leg 608/608 + sqlite green) | `nullptr` → `D-CSUBSET-NULLPTR`; enum → `D-CSUBSET-ENUM-UNDERLYING-TYPE` + `D-PARSE-SPECULATIVE-OPTIONAL` |
| **FC17.5** ✅ **COMPLETE 2026-07-14 — all FC17.5 residuals landed incl. the full VLA arc C1a–C5; (conformance sweep — dss-state battery-surfaced 2026-07-08; user-directed 2026-07-10: close FC17.5 BEFORE FC18)** | Residual C23/C11/C99 semantics the battery flags RED (`modern` cat now 24/27): ✅ **`auto` type-inference (`D-CSUBSET-AUTO-TYPE-INFERENCE`, 2026-07-10, FC17.5 cycle 2 — the audit-REVISED design implemented: headless `autoInferredVarDecl`/`autoInferredForDecl` grammar + the GENERIC `DeclarationRule.inferTypeFromInitializer` + `requiredSpecifierToken` engine capabilities [loader waives the declarator-mode head for the flag; head∧infer → C_ConflictingField]; ★C1 presence gate keeps `static x=5;`-class C89 implicit-int LOUD [S_AutoInferenceInvalid]; ★C2 inference-FIRST branch order + the all-fail committed REPLAY keeps the >4096-token static-init compiling [EMPIRICALLY red under varDecl-first]; ★C3 Pass-1.5 arm `resolveAutoInferredDeclaration` — braced-single unwrap → literal PRE-STAMP via the EXTRACTED shared `typeLiteralIfAny` chokepoint [at 1.5 the Pass-2 stamps don't exist — the design's subtreeType-types-literals premise was FALSE for literal leaves; the audit's p1 typeof-literal probe passed only via the Pass-2 backfill, probe-confirmed] → subtreeType → Array/FnSig DECAY → Void/NullptrT/self-ref REJECT LOUD → stripVolatile → the ORDINARY declarator fold writes it; 0xE041–0xE044 ALL unsuppressable [the backfill seam]; battery `c23_auto_type` GREEN; 16 unit pins [3 groups verified red-on-arm-disable] + 5 goldens + the 4-target `auto_type_inference` example exit-42 debug+release; deferrals `D-CSUBSET-AUTO-QUALIFIED`/`-DERIVED-DECLARATOR`/`-FILE-SCOPE` + the nullptr_t fold into `D-CSUBSET-NULLPTR-T-DECLARABLE`)** · `_BitInt(N)` (major, registry `D-CSUBSET-BITINT`) · ✅ **empty initializer `{}` + scalar brace-init** (`D-CSUBSET-EMPTY-INITIALIZER`, 2026-07-10 — the ONLY gap was lowerBraceInit's non-aggregate reject; a closed-allowlist scalar branch [0 elems → the existing synthZeroOrError incl. Ptr-null; 1 non-brace elem → the normal coerce; excess/designated/`{{42}}` → `S_InvalidScalarInitializer` 0xE03F; `(void){}` stays loud]; the constexpr empty-brace arm; the anticipated `ScalarCompoundLiteral*` pin flipped) · ✅ **`thread_local` — the TLS ARC (registry `D-CSUBSET-THREAD-LOCAL`, ✅ CLOSED 2026-07-11 — all 4 legs runtime-witnessed: design + independent design-audit ✅ GO-WITH-FIXES + user §B split-approval ✅ all 2026-07-10; the USER-APPROVED 4-cycle split: **C1 ELF x86_64 local-exec ✅ LANDED 2026-07-10** (all 8 audit folds + the 2-thread pthread witness [both workers 105 from the .tdata template, main kept 6 → exit 42, debug+release] + structural pins [PT_TLS exact fields, Variant-II tpoffs −8/−4, _Alignas(32) −64/−32/−28, no-TLS 5-phdr byte-identity] + the code-audit VERIFIED-CLEAN [tpoff physics confirmed vs gcc + live glibc; LOW-1 → the MirThreadStorage strong-enum addGlobal param; LOW-2 → 6 registry rows; LOW-3 → 3 driver-tier E2E pins]; battery note: `c11_thread_local` flips green on a LINUX host — the Windows driver compiles host-native pe64 where 0x8015-red stays CORRECT until C3; gates: 3-leg ctest 618/618 + sqlite 3.54.0/CRUD-42 × 3 [TLS dormant]; + the user mid-cycle `thread_local_pthread_header` real-`#include <pthread.h>` example + `D-CSUBSET-C11-THREADS-HEADER` gated) → **C2 arm64 ELF ✅ LANDED 2026-07-10** (MRS+ADD/ADD hi12/lo12 over the reused 2-word patch machinery [zero encoder diff]; the ONE new kernel formula Aarch64TprelAddHi12 with the 16MiB fail-loud cap; capability-probed tlsbase shape; Variant-1 physics qemu-witnessed incl. hi12≠0 + _Alignas(32); all 3 examples ×2 arms exit 42 under qemu; code-audit clean, 3 folds) → **C3 PE ✅ LANDED 2026-07-11** (native pe64 thread_local via gs:[0x58]+_tls_index+IMAGE_TLS_DIRECTORY64; single `.tls`+embedded-zero-tbss [DEV-1]; kind-4 reuse + IMAGE_REL_AMD64_SECREL format row; pe.cpp CRIT-1 backstop replicated; a CreateThread 2-thread witness discriminates → 42; code-audit CLEAN + 1 fail-loud fold [over-align gate]; runs in LOCAL Windows ctest) → **C4 Mach-O TLV ✅ CODE-LANDED 2026-07-11** (the dynamic descriptor-call model; arm64 Darwin; a THIRD TLS model that opts out of the tlsSymbols/XOR backstop; code-audit CLEAN incl. the x29-FP seam; 3 arm64 binaries produced + parsed) — ★★ ALL 4 LEGS' CODE LANDED; D-CSUBSET-THREAD-LOCAL ✅ RUNTIME-WITNESSED GREEN 2026-07-11 (C4 macho arm64 → 42 NATIVELY on the user's Apple-Silicon Mac via the MH_HAS_TLV_DESCRIPTORS fix; macos-latest CI re-confirms post-push) [runtime witness FIRED 2026-07-11: C4 executed → 42 NATIVELY on the user's Apple-Silicon Mac; local structural pins + macos-latest CI re-confirm post-push]; forks locked arm64-ADD/ADD · local-exec-only · dead-static-arm-unhardened; full spec = session memory `project_thread_local_arc_design_2026_07_10`)** · ✅ **`__func__` + `__FUNCTION__`** (`D-CSUBSET-FUNC-PREDEFINED-IDENTIFIER`, 2026-07-10 — config-spelling Pass-1 synthetic const `Array<core,N+1>` bound BEFORE params; folds to a string-literal-shaped HirLiteralValue at the ONE identifier-operand path; ★ the design-audit REFUTED the fold's identity story — C REQUIRES `__func__==__func__` (a declared static array) → **byte-content MEMOIZATION in materializeStringLiteralGlobal** (legal for ALL strings, C 6.4.5p7; red-on-disable ×2; sqlite-wide blast radius gated); `isConst` → S_ConstViolation on assignment; `++__func__` → `S_PredefinedIdentifierNotAddressable` 0xE040; `&__func__` LEGAL and works) · ✅ **VLA DECIDED** (2026-07-10 — NO VLA support, CONFORMANTLY declared: `__STDC_NO_VLA__=1` added to `predefinedMacros` [C11/C23 require a VLA-less implementation to define it; DSS claims 202311L]; `int a[n]` stays fail-loud S000B; registry `D-CSUBSET-VLA` decision row; real VLAs = **SCHEDULED (user 2026-07-11): a full VLA arc AFTER `_BitInt`, BEFORE FC18 — this no-VLA decision is SUPERSEDED; `__STDC_NO_VLA__` REMOVED when VLA lands**) · `#elifdef`/`#elifndef` ✅ **CLOSED 2026-07-09 (`D-PP-ELIFDEF-ELIFNDEF`)** (C23 PP §6.10.1 — was ⚠ SILENT MISCOMPILE; now implemented via `sbHandleElif` + `SbIfKind::Ifdef`/`Ifndef`, silent miscompile fixed, negative pins 42→2/42→3, 3-leg 606/606 + sqlite green → stepper T2.3 RESOLVED) · `[[fallthrough]]` in STATEMENT position (declaration-position attributes already pass) · `u8'x'` UTF-8 char constant (overlaps FC16 wide/UTF literals). Already scoped elsewhere (NOT re-listed): `typeof`/`typeof_unqual`/`constexpr`/`nullptr`/enum-explicit-type → FC17. Passing already (no work): `bool`/`true`/`false` keyword, digit separators, `alignof`/`alignas`, `[[nodiscard]]`, `__has_include`, compound literals. | `#elifdef` → `D-PP-ELIFDEF-ELIFNDEF` / stepper T2.3 |
| **FC17.9** ⏳ **(FULL-C23 COMPLETENESS — the §3.2 named exclusions PROMOTED into scope; user 2026-07-14: 100% C23 BEFORE FC18, the DSS Axis substrate mandate)** | The last named-exclusion tier — DSS becomes a COMPLETE C23 implementation before the FC18 conformance sweep, because C (and C++) are the substrate DSS Axis stands on, so the exotic corners must be REAL, not fail-loud. Each sub-feature is a normal /dss-cycle (best-long-term, agnostic, fail-loud, strict red-on-disable + a runnable release-arm corpus where runtime-observable) and is ALREADY tracked as a loud-RED `c23_advanced` probe in the dss-state battery (`.claude/skills/dss-state/probes.mjs`, committed `a50a334`) that flips GREEN the cycle it lands. **Order (leverage/dependency):** **(a) `<threads.h>` ✅ COMPLETE 2026-07-14 (Cycle 1+2)** — Cycle 2 landed the OS-callback trampolines: `thrd_create` (DIRECT-pass, no closure — x64 C11 `int(*)(void*)` ≡ Win32 `DWORD(*)(void*)`), `call_once` (`__dss_once_tramp` via InitOnceExecuteOnce), `thrd_join` (the FIRST multi-block synth recipe, canonical markers via `rederiveStructCfMarkers`); `__STDC_NO_THREADS__` now macho-ONLY (threads work on elf/pe64); a REAL-thread create/join witness → 42 on 3 legs; design-audit deleted the malloc-closure (direct-pass) + code-audit SHIP; closes `-TRAMPOLINES` + the pre-FC18 threads config gate. Original Cycle-1: (mutex/cnd/tss + thrd_join/yield/exit/detach/current on elf+pe64+arm64; pe64 = a synthesized Win32 shim via the NEW `synthesize`-recipe descriptor mechanism [the tagged symbol skips its kernel32 import but seeds `functionSymbols`, and `synth_threads_shim` supplies the MIR body]; single- AND multi-CU; battery `c11_threads_mutex` GREEN; ★ trampolines `thrd_create`/`call_once` + `__STDC_NO_THREADS__` removal = Cycle 2)** — — C11 `thrd_*`/`mtx_*`/`cnd_*`/`tss_*`, ALREADY PROMOTED + lands FIRST (registry `D-CSUBSET-C11-THREADS-HEADER`; a `shippedLibs/threads.json` over the landed TLS substrate + the pe64 posture §B) → **(b) `<stdbit.h>`** — C23 `stdc_count_ones`/leading-zeros/bit-width, pure library intrinsics over the existing popcount codegen → **(c) `setjmp`/`longjmp`** — `<setjmp.h>` non-local control flow, a per-target `jmp_buf` save/restore intrinsic → **(d) `_Atomic` / `<stdatomic.h>`** — the atomics-with-memory-ordering arc: the `_Atomic` type qualifier + `atomic_*` + `memory_order_*` fences → per-target atomic encodings (multi-cycle) → **(e) `long double`** — the width/format-divergence primitive (80-bit x87 SysV-x86_64 vs 64-bit Win64/ARM64 vs IEEE-128): a per-format `longDouble` axis, honored-or-fail-loud, never silently mis-bound (the `long` lesson) → **(f) `_Complex` / `<complex.h>`** — the C99 complex type-system + `creal`/`cimag`/`I` + complex-arithmetic lowering → **(g) `<tgmath.h>`** — C99 type-generic math, a `_Generic`-shaped dispatch selecting `sqrtf`/`sqrt`/`sqrtl` by argument type → **(h) `#embed`** — C23 binary-resource inclusion, a preprocessor expansion of a byte file into an initializer list over the FC13–FC15 PP substrate → **(i) inline assembly** — the `__asm__`/`asm` statement (per-target template + operand constraints), the one common EXTENSION the substrate needs. Each registers its own `D-FULLC-*` / `D-CSUBSET-*` anchor as it lands. **★ The PRE-FC18 CONFIG GATE closes here:** remove BOTH `c-subset.lang.json` predefinedMacros — `__STDC_NO_VLA__` (already removed at VLA C1b) + `__STDC_NO_THREADS__` (at `<threads.h>`). | promotes §3.2; `D-CSUBSET-C11-THREADS-HEADER` + a `D-FULLC-*` per sub-feature (registered as each lands); pre-FC18 config gate |
| **FC18** ⏳ | A broad runnable C23 corpus (real-ish programs across all targets, §A.5) + positioned-diagnostic assertions for the new error classes (REUSES the V2-4 Part C `expectDiagnostics` harness); grows the golden diagnostic corpus | `D-DIAG-CORPUS-EVERY-CODE` (grows toward closure) |

### Cluster F — SQLite-readiness (FC16-pre) *(USER-PRIORITIZED 2026-06-24)*
> **Goal:** clear the cheap, pervasive gaps blocking the first REAL-WORLD program (SQLite / `sqlite3.c`). Each is a single narrow gap from the live `dss-state` 104-probe battery failing-list; NOT phase-gated. **User priority order (2026-06-24, set during the autonomous /loop):** (1) **FC15 fully delivered — NO deferrals** (close the FC15a paste residuals: object-like `##`, placemarker, GNU comma-elision), THEN (2) **this Cluster F**, THEN (3) the rest of C23 (FC16→FC18). Each item is a normal /dss-cycle (best-long-term, agnostic, fail-loud, strict red-on-disable + runnable corpus where observable).

| Item | What (the gap + the agnostic fix) | Battery probe(s) |
|---|---|---|
| **F1 — `++`/`--`** ✅ **DONE 2026-06-24 (cycle 5, 400/400 ctest Release)** | pre/post increment + decrement, integer AND pointer. **Postfix-int already worked; this cycle adds PREFIX `++x`/`--x` (was unparsed — prefix op-group lacked them) + FIXES a pointer SILENT MISCOMPILE** (`p++` emitted a raw `Add`, stepping 1 BYTE not `sizeof(*p)`). The pointer step now routes `AddressOf(Index(lvRead(p), ±1 I64, T))` → the EXISTING `scaleIndexToBytes`→`Gep` element-scaling (steps `sizeof(*p)`); ONE shared `incDecNewValue` helper (pointer-vs-integer branch) at ALL 3 lowering sites (pre-value / post-value / stmt). Pre yields NEW, post yields OLD (single-evaluation via the lvalue temp). New `S_IncDecNeedsModifiableLvalue` (0xE024) + an lvalue-SHAPE guard fail loud on a non-lvalue (`5++`, `(a+b)++`). AGNOSTIC: dispatch on config target names (PreInc/PostInc) + `TypeKind::Ptr` + element size from the layout axis — no hardcoded `++` spelling/width. plan-lock GO-WITH-FIXES (5 MF: the 3-site shared helper, the I64 step for width-coherent scaling, the semantic-typer PreInc/PreDec arm, the unaryExprRule stmt arm, the diagnostic in both files). 6 red-on-disable pins (HIR+MIR, incl. STMT-position pointer) + corpus `core_incdec` (pre/post distinct + sizeof-scaled pointer step + single-evaluation, exit 0, release arm). Opens D-CSUBSET-INCDEC-CONST-LVALUE (const lvalue unchecked — pre-existing, shared with assignment). | `core_incdec` ✅ |
| **F2 — bitwise encodings** ✅ **DONE 2026-06-24 (cycle 6, 401/401 ctest Release)** | PROBED the actual gap (not assumed): grammar+HIR+MIR already handle `& \| ^ ~ << >>`; only the ENCODER rows were missing — x86 `xor` (`^`) + `not` (`~`) opcode rows existed but lacked an `encoding` block (`A_NoEncodingDeclared`); arm64 had NO `not` opcode at all (`L_RequiredLirOpcodeMissing`). `&`/`\|`/`<<`/`>>` (signed `sar`/`asr` + unsigned `shr`/`lsr`) already worked. Added: x86 XOR (`0x31 /r`, mirror `and`) + NOT (`0xF7 /2`, mirror `neg`) encoding blocks; arm64 `not` opcode = MVN = `ORN Xd,XZR,Xm` (`0xAA2003E0` / `0x2A2003E0` w32, mirror `neg`=SUB-via-XZR). PURE `.target.json` config — NO engine change (the `xor`/`not` LIR mnemonics already exist). AGNOSTIC (config encoding rows; the encoder reads them). Verified by UNFORGEABLE RUNTIME witness: a full `& \| ^ ~ << >>` program exits 69 on BOTH x86 native AND arm64 qemu. 3 byte pins (x86 `48 31 C8` XOR + `48 F7 D0` NOT, arm64 `E0 03 21 AA` MVN) + corpus `core_bitwise` (every operator + signed-vs-unsigned `>>` distinction, exit 42, release arm, cross-target). FAIL-LOUD preserved. | `core_bitwise` ✅ |
| **F3 — float mul/sub** ✅ **DONE 2026-06-24 (cycle 7, 402/402 ctest Release)** | PROBED: front-end already lowers float `*`/`-` to MIR FMul/FSub; `float_add` (ADDSD/FADD) worked — only the ENCODER rows were missing. x86 `fmul`/`fsub` rows existed but lacked `encoding` blocks (`A_NoEncodingDeclared`); arm64 had NO `fmul`/`fsub` opcodes (`L_RequiredLirOpcodeMissing`). Added (mirror `fadd`): x86 MULSD/MULSS `0F 59` + SUBSD/SUBSS `0F 5C` (F2/F3 mandatory-prefix, width-keyed F64/F32); arm64 FMUL `0x1E600800` + FSUB `0x1E603800` (FP-dataproc, opcode field [15:10]={opcode[3:0],10}). PURE `.target.json` config, NO engine change. ★ SUBSD/FSUB are NON-COMMUTATIVE — operand order pinned (corpus `(a-b)-(b-a)=84` + the arm64 `…NonCommutative` byte pin; a swapped Rn/Rm wire flips the sign). Runtime witness exit 30 (probe) + corpus `float_mul_sub` exit 42 on x86 native + arm64 qemu; 4 byte pins (x86 MULSD `F2 0F 59 C1` + SUBSD `F2 0F 5C C1`, arm64 FMUL `20 08 62 1E` + FSUB `20 38 62 1E`). ★ The corpus probe surfaced a SIBLING gap: negative float LITERALS (`-42.0`) need `fneg` (un-encoded, out of F3 scope) → corpus rewritten to avoid it; `D-CSUBSET-FLOAT-NEG-ENCODING` anchored. | `float_mul_sub` ✅ |
| **F4 — integer narrowing** ✅ **DONE 2026-06-25 (cycle 8, 403/403 ctest Release)** | ★ PROBE OVERTURNED THE SCOPE: integer PROMOTION (`short`/`char`→`int` in arithmetic) was ALREADY done by FC3 UAC; the real SQLite blocker is implicit same-signedness integer NARROWING (`short s = anInt;`, C 6.3.1.3) — needing BOTH a semantic gate AND backend sub-32-bit conversion codegen. User chose "full backend narrowing now". **SEMANTIC** (`D-CSUBSET-INT-SAME-SIGN-NARROW`): the 4th C-conformance gate `intSameSignednessNarrows` (after bool/char/enum/cross-sign) — isAssignable's rank arms admit `rank(rhs)>rank(lhs)` only when on (widening unconditional); 5 semantic call sites; default false → toy/tsql byte-identical. **BACKEND** (`D-CSUBSET-SUBNATIVE-ALU-FORMS` conversion forms): the `char` lazy-consumer model extended to I8/I16/U8/U16 — `widthFlagsForType` maps I8/U8→8, I16/U16→16; `registerOpWidthFlags` collapses 8/16→32 (partial-reg hazard); the Trunc/SExt/ZExt gates admit the new widths; encodings x86 movsx r/m16 (`0F BF`) + movzx r/m8/r/m16 (`0F B6`/`B7`), arm64 SXTH (`0x93403C00`) + zext split to width-keyed (UXTH `0x53003C00`, UXTB `0x53001C00`). A narrowing Trunc routes through the promoted width-32 `mov` (low bits kept); narrowing realizes at the byte/half-exact consumer. WITNESS: `int_narrowing` corpus (4 assignment positions + truncation + promotion) exit 42 x86 native + arm64 qemu; LIR width pins flipped to assert realization (red-on-disable). Reconciled 3 tests/corpus that pinned old "narrowing rejected" (`datamodel_long_width_llp64` → runtime exit-7 truncation witness; `MixedWidthBinaryArgTypedByUacNotLeaf` → float operand). pr-review VERIFIED-CLEAN (rebuilt + 2 native witnesses + ISA-decoded every byte + all 21 widthFlagsForType callers traced). Opened `D-CSUBSET-INT-FLOAT-IMPLICIT-CONVERT`. | `int_narrowing` ✅ (the real gap was narrowing, not promotion) |
| **F5 — string index + global** ✅ **DONE 2026-06-25** | string-literal indexing (`"abc"[i]`, exit 215) + symbol-address globals (`int* p=&x` / `char* g="..."`, exit 143) emitting an abs64 relocation resolved cross-format: PE DIR64 · ELF ET_EXEC in-place · **Mach-O in-place + dyld REBASE stream (PIE slide)**. A symbol-address pointer is forced to `.data` (load-writable) on every format. Folds: multi-CU merge remaps the embedded SymbolId; DCE propagates liveness through symbol-address inits. `D-CSUBSET-SYMBOL-ADDRESS-GLOBAL` CLOSED. | `agg_string_index`, `decl_string_global` |
| **F6 — struct/union member declarator** ✅ **DONE 2026-06-25 (cycle 10, 415/415 ctest Release)** | **The #1 SQLite blocker: function-pointer struct members (`int (*f)(int);`).** A USER-APPROVED, plan-locked full refactor switching c-subset `structField`/`unionField` from a bespoke `{optional Identifier}{arrayDeclSuffix\|bitfieldDeclSuffix}` shape to the SHARED `declarator` (the `param` row's proven `head:0, declarator:1` model) + an OUTER `bitfieldDeclSuffix` sibling. A member now parses + type-derives identically to every other declarator position, so fn-ptr members, `int (*xCmp)(void*,…)` vtables, and arrays-inside-the-declarator (`int arr[3];`) all work for free. THREE behaviours preserved by porting them onto the declarator path: (1) **FAM** — `bool allowFlexibleArray` threaded through `declaratorDeclaredType`/`applyDeclaratorSuffix` (the absent-length array → `incompleteArray`; NOT propagated into a grouped fn-ptr-param inner declarator); (2) **named bit-fields** — `resolveBitfieldSuffix` ported into the declarator-mode Pass-1.5 loop, searched from the structField NODE (the suffix is the declarator's SIBLING, not inside it); (3) **anonymous fields** — `int :3;` / `int ;` bind a synthetic `<anon:rule:node.v>` symbol after the (empty) declarator loop in Pass-1 + Pass-1.5, with the `S_DeclarationDeclaresNothing` fail-loud for a non-bit-field anonymous member preserved. 100% config-driven (the `declarator` rule + `head`/`declarator` roles); FRONTEND-only, ZERO downstream change. WITNESS: 6 positive probes compile clean (the goal `int (*f)(int)`, an all-features struct incl. anon bitfield, FAM, union fn-ptr, a SQLite-shaped vtable typedef, a bitfield global-init) + `int ;` STILL fails loud `S_DeclarationDeclaresNothing 1:12`; the full 415-test suite green (the bitfield/FAM diagnostic corpus + `enum_bitfield` example unbroken). `D-CSUBSET-STRUCT-MEMBER-DECLARATOR` CLOSED. | bitfield/FAM diagnostic corpus + `enum_bitfield` |

---

## 0.2 Scope & design decisions (the user §B answers + the plan-lock locks, 2026-06-10)

Captured here so they are not re-litigated each cycle:

1. **C standard = C23, "as mainstream compilers implement it"** (user). The full language; the exotic long-tail (§3.2) is pinned-deferred, not blocking.
2. **Full preprocessor IS in scope** (Cluster D) — a complete macro + conditional-compilation + include-expansion pass; a NEW token-stream transform upstream of the parser (NOT the existing tree-merge resolver).
3. **Aggregate ABI on ALL current targets** (x86_64 + arm64 × PE/ELF/Mach-O) within V2-4.X (Cluster C). The runtime-closure honesty exception: Mach-O-x86_64 (FC9) is byte-pinned-only (no macOS-x86_64 CI leg).
4. **Recorded as this dedicated `plan-23` + a `V2-4.X` row in plan-00 §0.1.**
5. **Preprocessor ordering = PARALLEL/INTERLEAVABLE track** (Cluster D is upstream of parsing, independent of the type/ABI backend; pick per-cycle whether D or B/C advances).

**Plan-lock locks (folded from the 3-reviewer review, 2026-06-10):**
- **Width agnosticism (FC3):** widths resolve per the active target's `dataModel` axis, NOT a language-config constant — the registry-prescribed mechanism for `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH`.
- **Aggregate-ABI agnosticism (Cluster C):** parameters-in-config + bounded per-CC algorithm in the realization tier; an explicit FC9-open §B.
- **Layout agnosticism (FC6):** per-ABI layout params declared in `.target.json` (the `D-FF3-1` extension), read by a generic `type_layout` engine into a side-table.
- **Preprocessor agnosticism (FC13):** config-selected/declared generic pass + the multi-language opt-in pin; an explicit FC13-open §B.
- **Shared const-expr evaluator:** FC3 / FC8 / FC14 use ONE evaluator.
- **No over-claimed closures:** the already-shipped predecessors (§3.1) — 2 closed anchors (division, extern-call-dispatch) + the `main` implicit-return feature — are struck, not re-claimed; `D-LK10-ENTRY-MAIN-IMPLICIT-RETURN` stays OPEN (non-main successor).

---

## 1. Motivation & current state

`c-subset` today already RUNS real programs (factorial, fibonacci, `puts("hello")`, cross-CU linkage) on 3 OSes × 2 arches. Its grammar is rich — but it is a *subset*: a program using `%`, `unsigned`, a C-style cast, a function-pointer declarator, `goto`, a struct passed by value, a variadic call, or a `#define` either fails to parse or fails to codegen. "Full C" means a developer takes an ordinary C23 translation unit and DSS compiles + links + runs it.

The exploration (2026-06-10, three code-explorers) + a strict 3-reviewer plan-lock established the shape: the front-end is mostly there (minus function pointers + the UAC engine + the missing operators), the **backend (aggregate layout + member-offset codegen + aggregate/variadic ABI) is the bulk**, the **preprocessor is wholly absent**, and the ABI/width/layout work is the agnosticism-critical part (it must stay parameters-in-config + bounded realization code, never an identity branch). Hence the ordering: cheap front-end wins first (A), then the gating layout substrate (B), then the large per-target ABI (C), with the preprocessor (D) as a parallel track and C completeness + C23 polish + conformance (E) last.

---

## 2. The clusters (detail)

### 2.A — Front-end completeness (FC1–FC5)
Mostly config-driven (grammar + semantic + HIR-lowering config), with three real engine pieces: `%`/div-mod codegen (FC1), the **UAC engine + the `dataModel` width axis** (FC3 — the genuine hard part, a classic miscompile farm: requires a negative test like `-1 > 0u`), and **function-pointer declarator-stacking** (FC4 — a real grammar/parser gap, not config). Each phase: a runnable corpus arm + positioned-diagnostic tests for the new error cases. **FC5 goto/labels — orphan-block note (2026-06-13):** goto creates unreachable blocks (code after an unconditional `goto`; dead predecessors of an un-targeted label). That orphan-block class is ALREADY owned centrally by the mandatory MIR reachability-prune normalize (`D-MIR-UNREACHABLE-PRUNE-NORMALIZE`, wired at the `optimizeModule` chokepoint) — FC5 adds only the goto SYNTAX + HIR/MIR lowering; the prune drops any resulting unreachable blocks by construction, so FC5 needs NO per-construct dead-block handling.

### 2.B — Aggregate backend (FC6–FC8)
**FC6 (layout substrate) is the gating piece + realizes `D-FF3-1`.** The agnosticism design: per-ABI layout PARAMETERS in `.target.json` (the `D-FF3-1` cross-tier extension), a generic `type_layout` engine reading them into an `ArenaAttribute<TypeId, StructLayout>` side-table. **Why the side-table, not `TypeRecord`:** `TypeRecord` is a fixed trivially-copyable arena record for a *language-level interned type*; layout is *target/ABI-dependent*, so baking offsets into it would make an interned type target-specific (the WRONG long-term move). The side-table is the established codebase pattern and the clean choice. Bitfield packing (FC8) is part of the same per-ABI parameter set (MS vs SysV differ). FC7 threads layout into `hir_to_mir` (MIR already has `Alloca`/`Gep`/`ExtractValue`/`InsertValue`).

### 2.C — Aggregate ABI (FC9–FC12c) — the heaviest
The per-calling-convention struct-by-value + variadic lowering in the **sanctioned realization tier** (`lir_callconv.cpp` / `mir_to_lir::lowerCall`), **parameterized** by new `TargetCallingConvention`/ABI-catalog config fields (the FC9 §B locks the data-vs-code boundary). Each FC ships a runnable corpus with §A.5 cross-target runtime closure on its native CI leg — EXCEPT FC9's Mach-O-x86_64 arm (byte-pinned-only; no macOS-x86_64 leg). The classification ALGORITHMS (SysV eightbyte merge lattice; AAPCS64 HFA recursive homogeneity test) are bounded per-CC code — NOT a data-walked engine, NOT an identity branch in shared MIR/HIR. Variadic is split per-CC (FC12a/b/c) mirroring the aggregate split (one shippable target per phase; SysV's register-save-area `va_arg` alone rivals a whole FC9).

### 2.D — Preprocessor (FC13–FC15) — parallel track
A NEW token-stream pass between tokenization and parsing, building on the existing config-driven `popAtNewline` directive-lexing substrate — architecturally DISTINCT from the post-parse tree-merge `#include` resolver. **Agnosticism is the crux** (FC13 §B): config-selected/declared mechanism + a multi-language opt-in pin (toy/tsql get NO macro expansion). The `#if` ICE evaluator is the shared const-expr evaluator (FC3/FC8/FC14 — one engine).

### 2.E — C completeness + C23 modern + conformance (FC16–FC18)
FC16 gives MEANING to the standard-C features the front-end parses but doesn't yet implement (`_Generic`, real `static_assert`/`alignas`/`alignof`, anonymous members, attribute *semantics* incl. layout-affecting `packed`, wide/UTF literals, `volatile`-honoring codegen) — closing the parse-without-semantics stub class. FC17 adds the C23-specific surface. **FC17.9 (NEW, user 2026-07-14) closes the §3.2 named exclusions — `<threads.h>` · `<stdbit.h>` · `setjmp`/`longjmp` · `_Atomic`/`<stdatomic.h>` · `long double` · `_Complex`/`<complex.h>` · `<tgmath.h>` · `#embed` · inline asm — so DSS is a COMPLETE C23 implementation before the sweep (C is the DSS Axis substrate).** FC18 is the broad C23 corpus + the positioned-diagnostic conformance sweep that grows `D-DIAG-CORPUS-EVERY-CODE`.

---

## 3. Deferrals

### 3.1 — Deferrals this arc CLOSES (~12 OPEN anchors)
`D-CSUBSET-MOD-OP-CODEGEN`, `D-CSUBSET-MOD-OP-CODEGEN-OUTPUT-INDEX-CONTRACT`, `D-MIR-TO-LIR-DIV-SEQUENCE-AGNOSTIC` (FC1); `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH`, `D-CSUBSET-UDIV-RUNTIME-HIGH-BIT-PIN` (FC3); `D-CSUBSET-LINKAGE-VISIBILITY-SYNTAX`, `D-DECL-PREFIX-STRIP-SHARED-HELPER` (FC4); **`D-FF3-1`** (layout half ✅ CLOSED 2026-06-15 by FC6 — the `type_layout` engine + `.target.json` `aggregateLayout` params + side-table; va_arg half still OPEN for FC12a); `D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY` (FC8); `D-FF3-APPLE-ARM64-ABI-DIVERGENCE` (FC11 aggregate + FC12c variadic); `D-FFI-DESCRIPTOR-VARIADIC-SIGNATURE` (FC12a); `D-DIAG-CORPUS-EVERY-CODE` grows toward closure (FC18). Several ARM64/multi-format codegen anchors (`D-LK10-ENTRY-ARM64-*`) have triggers that FIRE as the full-C corpus exercises them — closed opportunistically inside the relevant FC.

> **ALREADY SHIPPED — do NOT re-claim as new FC work** (caught by the plan-lock accuracy review). Two genuinely-CLOSED **anchors**: `D-CSUBSET-DIVISION-OP-CODEGEN` (✅ 2026-06-04, signed division shipped — FC1 only adds modulo); `D-FFI-EXTERN-CALL-DISPATCH` (✅ 2026-06-08 — FC12 does variadic dispatch, a different concern). Plus one closed **feature** with no anchor row: the `main` implicit-return-0 (✅ 2026-06-02, live in `c-subset.lang.json`, config-only) — note the anchor `D-LK10-ENTRY-MAIN-IMPLICIT-RETURN` itself is the still-OPEN *non-main* successor (registry §, line ~109), which FC5 adds optionally; it is NOT a closed anchor.

### 3.2 — Named exclusions (pinned, NON-blocking, NOT in the V2-4.X critical path)
Honest scope-bounding for "C23 as mainstream-implemented," each its own future trigger-gated work (pinned as `D-FULLC-*` registry rows as each cluster lands, per §F). **★ PROMOTED 2026-07-14 (user): the remaining exclusions below are NO LONGER deferred — they are SCHEDULED as **FC17.9** (§0.1), the full-C23 completeness tier, to land BEFORE FC18. Rationale: C (and C++) are the substrate for DSS Axis, so 100% C23 is a foundation, not an optional long-tail. The bullets are kept for provenance; each now carries its FC17.9 pointer.**
- **VLAs** (`int a[n]`) — **✅ PROMOTED + ACTIVE ARC** (`D-CSUBSET-VLA`, user 2026-07-11): the full VLA arc C1–C5 ✅ COMPLETE 2026-07-14 (block-scope teardown landed — the LAST VLA sub-arc; arc CLOSED); `__STDC_NO_VLA__` REMOVED. This "deferred" text is SUPERSEDED, kept for provenance.
- ~~**`sizeof` in a const-expression context** — `D-FC6-SIZEOF-CONST-CONTEXT`~~ ✅ **CLOSED 2026-06-15 (FC6-deferrals cycle):** `sizeof` now const-folds in global initializers (`int g = sizeof(T)`, both forms) AND array-dimension TYPE form (`int a[sizeof(T)]`), via a symmetric `resolveTypeSize`/`resolveSizeof` hook on both const-eval engines sharing the `computeLayout` kernel. The "target-dependent type" fear was disproven (semantic re-runs per-(CU×target) into a private lattice). **RESIDUAL** (named): the array-dim VALUE form (`int a[sizeof expr]`) stays fail-loud (`S_NonConstantArrayLength`) — a NARROWER blocker (PASS ORDERING: array dims resolve Pass 1.5, expression operand types Pass 2), tracked as `D-FC6-SIZEOF-ARRAY-DIM-VALUE-FORM` (normal). Also CLOSED this cycle: `D-FC6-FAM-SEMANTIC-CONSTRAINTS` (positioned `S_FlexibleArray{NotLast,SoleMember,InAggregate}` diagnostics at struct composition).
- **`_Complex` / `<complex.h>`**, **`_Atomic` / `<stdatomic.h>`** — large library + type-system features. **→ BOTH PROMOTED into FC17.9 (§0.1), user 2026-07-14 (atomics-with-ordering + the complex type-system).** (**`<threads.h>` → PROMOTED into scope, SCHEDULED before FC18** — user 2026-07-11; registry `D-CSUBSET-C11-THREADS-HEADER`; removes `__STDC_NO_THREADS__` from `c-subset.lang.json` predefinedMacros when it lands.)
- **`setjmp`/`longjmp`** (non-local control flow), **inline assembly**, **`<tgmath.h>`** (type-generic math), **`<stdbit.h>`** niche bits. **→ ALL PROMOTED into FC17.9 (§0.1), user 2026-07-14.**
- **C23 `#embed`** **→ PROMOTED into FC17.9 (§0.1), user 2026-07-14.** (~~arbitrary-width `_BitInt(N)`~~ **→ PROMOTED into scope, IN-FLIGHT: the full arbitrary-width `_BitInt` arc — C1/C2/C3 ✅ + C4a ✅ (`_BitInt` bit-fields) 2026-07-12; C4b ✅ 2026-07-13 (wb/uwb arbitrary-magnitude literals + wrap-aware bignum const-fold) CLOSED it → the whole `_BitInt` arc is COMPLETE**, registry `D-CSUBSET-BITINT` ✅.)
- **`long double` width/format divergence** (80-bit x87 on SysV-x86_64 vs 64-bit on Win64/ARM64 vs IEEE-128) — another platform-divergent primitive FC3 surfaces; better named-deferred than silently mis-bound (the `long` lesson). **→ PROMOTED into FC17.9 (§0.1), user 2026-07-14 — a per-format `longDouble` axis, honored-or-fail-loud.**

---

## 4. Cross-plan relationships

- **Grammar/schema** ([02](./02-schema-expressiveness-v2-plan%20-%20ok.md), `c-subset.lang.json`): Cluster A (incl. the function-pointer declarator gap) + FC17 C23 syntax.
- **Semantic** ([08.6](./08.6-semantic-plan%20-%20ok.md)): the UAC engine + `dataModel` (FC3), member lookup (FC7), the shared const-expr evaluator (FC3/FC8/FC14).
- **HIR** ([09](./09-hir-plan%20-%20ok.md)) + **MIR/LIR** ([12](./12-mir-lir-plan%20-%20ok.md)): the `type_layout` side-table (FC6), member-access lowering (FC7), the aggregate ABI in `lir_callconv` (FC9–FC12c).
- **FFI** ([11](./11-ffi-plan%20-%20tbd.md)): **`D-FF3-1`** (the per-target ABI layout TargetSchema extension — realized by FC6/FC12) + variadic descriptors (FC12a). Disambiguate FC13 from plan-11 **FF9** (a separate post-v1 header-parser concept).
- **Assembler** ([13](./13-assembler-plan%20-%20tbd.md)) + **Linker** ([14](./14-linker-plan%20-%20tbd.md)): div/mod encoding (FC1), rodata aggregates (FC8).
- **Preprocessor** — a NEW concern (token-stream transform upstream of the parser); closest existing home is the tokenizer/source-translation plans ([04](./04-tokenizer-plan%20-%20ok.md), [10](./10-source-translation-plan%20-%20tbd.md)). FC13's §B decides its plan home + the existing-resolver interaction.
- **Diagnostics** — the V2-4 `D-DIAG-CLI-POSITION-RENDER-AND-ASSERT` harness (`tests/analysis/test_diagnostic_corpus.cpp` + the `expectDiagnostics` corpus field) is REUSED throughout for the new error classes.

---

## 5. Acceptance

V2-4.X is "done" when an ordinary C23 translation unit (structs-by-value, variadics, macros, conditional compilation, the full type system + conversions) **compiles → links → runs** on all current targets (x86_64 + arm64 × PE/ELF/Mach-O), proven by a broad runnable corpus (§A.5 cross-target runtime closure on every native CI leg — except the named Mach-O-x86_64 byte-pin gap) + strict positioned-diagnostic assertions for the error classes + the named-exclusion deferrals pinned.

**Per-FC acceptance gate (every cycle, alongside the strict-test + corpus lines):** *agnosticism — no new `if (lang/arch/format/cc.name == …)` in `src/{opt,mir,hir,lir,core}` or the parser/semantic engines; new per-CC/per-format/per-data-model logic lives ONLY in the sanctioned realization tier and is config-parameterized; no parse-without-semantics stub (a feature that parses must MEAN something or be a pinned exclusion).* Each FC phase is independently green-and-shippable; the arc never sits on a long red branch.

# DSS Code Prime — Universal Compiler Implementation Plan

> **Universal-compiler thesis (rev 2 — 2026-05-23).** Three architectural decisions now anchor every downstream plan:
>
> 1. **Hermetic.** We own every byte from source to signed binary. No `ld` / `link.exe` / `ld64` / `lld` / `wasm-ld` / `xcrun` / `clang` / `gcc` / `MSVC` / `dxc` / `glslc` / `dsymutil` / `mspdb` / `codesign` / `signtool` invocations. OS-supplied runtime libs (libc.so / libSystem.dylib / msvcrt.dll / ntdll.dll), browser/wasmtime, GPU drivers, and Apple developer certs are FFI targets and credentials — never tools.
> 2. **Three IR layers: HIR → MIR → LIR.** [HIR](./09-hir-plan%20-%20ok.md) (language-neutral, structured, typed — the pivot for transpilation, native lowering, and shader/WASM lowering) → [MIR](./12-mir-lir-plan%20-%20ok.md) (SSA over CFG with structured-CF markers preserved for WASM) → [LIR](./12-mir-lir-plan%20-%20ok.md) (per-target ISA). A single SSA IR cannot serve both binary codegen *and* source-to-source transpilation cleanly; HIR is the layer where the seven goals converge.
> 3. **Core type lattice + per-language extensions.** Universal core (primitives, ptr/ref/fnptr, struct/union/tuple, array/slice, vector/matrix, function signatures with calling-convention attribute) lives in [`08.5-substrate-prep-plan`](./08.5-substrate-prep-plan%20-%20ok.md) §2.2; each `.lang.json` registers extension type-kinds (C++ MemberPtr, C# Boxed/Delegate/GcRef, TSQL Varchar<N>/RowType, VHDL Std_Logic, shader Sampler/Texture). Transpilation maps via the core; extensions need explicit map entries.
> 4. **Config-driven all the way down — the engine has NO per-language / per-target / per-format C++.** The thesis "the source language is configurable" applies to *every* phase, not just lex/parse. Semantic analysis ([`08.6-semantic-plan`](./08.6-semantic-plan%20-%20ok.md)) reads a `semantics` block from `.lang.json` (schema v4) via one language-agnostic engine; CST→HIR lowering ([`09-hir-plan`](./09-hir-plan%20-%20ok.md)) is likewise schema-described. ML5 cycle 2a (2026-05-29) extended the same discipline to backend targets: a compile target is a `.target.json` file, ARM64 is "drop a JSON file." **No phase may branch on `schema.name()` / `target.name()` / `format.name()` or hardcode a language/target/format's rules in C++.**
>
>    **What "config-driven" admits — the three-bucket rule (clarification added 2026-05-29 to close back-half drift):**
>
>    | Bucket | What it is | Where it lives | Config-driven? |
>    |---|---|---|---|
>    | **1. Declarative layout** | Fixed-width fields, opcode tables, section/symbol tables, reloc formulas, encoding rows (e.g. ModR/M slots, ARM bit-field templates), DWARF tag tables. | JSON | ✅ schema = bytes |
>    | **2. Universal algorithm over declared vocabulary** | Isel (patterns + costs), instruction encoding (prefix/ModR/M assembly), procedural streams (Mach-O bind/lazy/rebase opcode trie, DWARF line program, CFI), regalloc, GOT/PLT synthesis. | One engine algorithm; JSON declares the vocabulary it walks | ✅ — this is "the hard part in the source code" |
>    | **3. Identity-branching C++** | `if (schema.name() == "c-subset")`, `if (arch == "arm64")`, per-arch / per-format / per-language `.cpp` directories. | Nowhere — forbidden | ❌ Decision #4 violation |
>
>    **The operative test, stated once:** config-driven is defined by **the absence of an identity branch, not the absence of an algorithm**. A 2,000-line universal bind-trie emitter that branches on nothing is fully thesis-compliant. A 10-line `if (format == "macho")` is not. The hard algorithms living in the engine ARE the design — not a compromise, not an escape hatch.
>
>    **Bucket 2 is not "one giant universal function." It is shape-keyed dispatch over a closed vocabulary, with one specialized walker per shape.** In-tree precedent: `ChildLower` in `src/core/types/hir_lowering_config.hpp` declares a closed set of lowering verbs (`Expr` / `FlatExpr` / `Ext` / `Ref` / `VarDecl`); the CST→HIR engine has one specialized handler per verb; the JSON config declares which verb to use per child slot; the engine dispatches on the verb (a closed-vocabulary enum), never on language identity. The handlers are bucket-2 walkers — each is shape-keyed (e.g. "handle a flat-expr sequence"), not identity-keyed ("handle c-subset"). Adding a new language uses the existing verbs; adding a new verb is one new bucket-2 walker + one new config-string. Same pattern applies to the back half: assembler-encoders dispatch on `encoding.format` (x86-variable / fixed32 / wasm-leb / spirv-word); linker stream emitters dispatch on `format-section.shape` (linear-bytes / opcode-trie / chained-fixups); debug-info section emitters dispatch on `dwarf.section.shape` (string-table / opcode-stream / state-machine). The closed-vocabulary enum is bucket-1 declaration; each handler is bucket-2 algorithm; no walker branches on target/format/arch identity.
>
>    **Arch-shape / format-shape differences live in the JSON vocabulary, not in the engine.** x86 ModR/M, ARM bit-field encoding, RISC-V immediate splits, Mach-O bind opcodes vs ELF rela formulas — all different *vocabulary instances*. Bucket 1 must be general enough to express each one uniformly; the bucket-2 algorithm walks the declared rows and never branches on which target/format produced them.
>
>    **Existence proof:** the ML5 cycle 2a pivot. Pre-pivot, the LIR substrate had per-arch C++ (`Lir<TargetTraits>`, `targets/x86_64.hpp`). Post-pivot, the LIR substrate is target-blind: `runtime TargetSchemaId + JSON config + universal engine`. The MIR→LIR isel is a ~2,000-line bucket-2 algorithm parameterized by the JSON vocabulary; adding ARM64 became "drop a `.target.json`," zero engine work. This is the template for the back half — assembler ([`13-assembler-plan`](./13-assembler-plan%20-%20tbd.md)), linker ([`14-linker-plan`](./14-linker-plan%20-%20tbd.md)), debug-info ([`15-debug-info-plan`](./15-debug-info-plan%20-%20tbd.md)), shader ([`17-shader-gpu-plan`](./17-shader-gpu-plan%20-%20tbd.md)), WASM ([`18-wasm-plan`](./18-wasm-plan%20-%20tbd.md)) all follow the same bucket-1+2 shape.
>
>    **The one carve-out:** any genuinely unavoidable identity-specific code is a named, delimited real-blocker (per the standing "no deferring without a real blocker" rule) — never silent, never permanent. The expectation is that no such carve-out is needed; if one becomes necessary, it's a thesis falsification event worth its own review pass.
>
> **v1 production target.** End-to-end binaries for every language in `src/source-config/languages/` (`toy`, `c-subset`, `tsql-subset`) on **{Windows, Linux, macOS} × {x86_64, ARM64}**, gated by the [`artifactProfile`](./06-artifact-profile-plan%20-%20tbd.md) compatibility mechanism — produced via the **hermetic in-tree toolchain** (assembler + linker + debug-info writer + codesigner all owned). **See [`07-production-readiness-plan - tbd.md`](./07-production-readiness-plan - tbd.md) for the master gap catalog**.
>
> **v1.x targets** (reserved namespaces; designs land now to keep substrate honest): WASM ([`18-wasm-plan`](./18-wasm-plan%20-%20tbd.md)), shader/GPU ([`17-shader-gpu-plan`](./17-shader-gpu-plan%20-%20tbd.md)), source-to-source transpilation ([`10-source-translation-plan`](./10-source-translation-plan%20-%20tbd.md)), FFI ingestion of precompiled libraries ([`11-ffi-plan`](./11-ffi-plan%20-%20tbd.md)).
>
> **Reserved (no work until trigger):** VHDL/Verilog ([`19-hir-hw-reserved-plan`](./19-hir-hw-reserved-plan%20-%20tbd.md)), user's custom language ([`20-custom-language-reserved-plan`](./20-custom-language-reserved-plan%20-%20tbd.md)), language runtime ([`21-runtime-reserved-plan`](./21-runtime-reserved-plan%20-%20tbd.md)).
>
> **Status & sub-plans.** This document is the high-level master plan.
> Detailed designs for individual modules now live in dedicated sub-plans:
>
> - **[`01-tree-node-model-plan - ok.md`](./01-tree-node-model-plan - ok.md)** — the tree/node data structure, `GrammarSchema` config loader, `TreeBuilder`, diagnostics. **Supersedes** the relevant pieces of §4.2.2 (`ast.hpp`), §4.3 (config JSON shape), §4.4 (`source-factory/` internals), §4.5 (tokenizer trivia handling), and §4.6.2 (parser/AST construction) of this document. Where this document and the sub-plan disagree, **the sub-plan wins**; in-line `> AMENDED:` notes in the sections below redirect readers.
> - **[`02-schema-expressiveness-v2-plan - ok.md`](./02-schema-expressiveness-v2-plan - ok.md)** — additive extensions to the v1 schema for operator precedence, contextual keywords, scope-stack patterns, speculative `alt`, string interpolation, and custom string-literal variants. **✅ done.** PR0 (c-subset + gap catalog), PR1 (operator precedence), PR2a (real `SchemaCursor` walker), PR2b (contextual keywords + `reservedWordPolicy`), PR3 (`scopeRequire`), PR4 (`TreeBuilder::Checkpoint` + speculative-alt loader plumbing), PR5 (`lexerModes` + `LexerModeStack` + `modeOp`), PR6 (`stringStyle` + `SchemaId`/`StringStyleId` strong ids), PR7 (tsql-subset stress test proving v2 is empirically sufficient), PR8 (cross-plan close-out) all shipped + review-fixed. Unlocks the eventual `languages-onboarding-plan.md`.
> - **[`03-substrate-hardening-plan - ok.md`](./03-substrate-hardening-plan - ok.md)** — originally three small de-risking PRs (SH1–SH3) that land **between v2 and the tokenizer phase**; extended with SH4 (v2 follow-ups: CI hygiene + shape-based `switch`/`case`/`default` adoption + multi-level AltChoice nesting stress test). **✅ done.**
> - **[`04-tokenizer-plan - ok.md`](./04-tokenizer-plan - ok.md)** — opens parent phase #5: `Tokenizer` + `TokenStream` + `SourceReader`, mode-aware schema resolution, hand-coded numeric lexing, `LexerModeStack` integration, `stringStyle` body handling, comment modes. TZ1 (bare tokenizer + toy E2E flip), TZ2 (modes + strings + comments + c-subset comment mode), TZ3 (c-subset / tsql-subset E2E flips + 3 semantic gaps closed) all shipped; TZ3 review-fix round 1 (13 items) shipped on top. Master plan §8 row 6 (`analysis-lexical`) is **subsumed** into this plan + the v2 schema-aware builder. **✅ done.**
> - **[`05-parser-plan - ok.md`](./05-parser-plan - ok.md)** — opens parent phase #7 (`analysis-syntactic`): substrate refactor + schema-driven RD driver + Pratt walker + recovery + corpus + LSP scaffolding. **Seven PRs** (PA0 `SchemaWalker` extraction ✅, PA1 RD driver ✅, PA2 Pratt walker ✅, PA3 recovery + diagnostic UX ✅, PA4 corpus stress ✅, PA5a LSP server + diagnostics ✅, PA5b LSP semantic-stub method handlers ✅). Plus shipped sub-workitems: PA-Walker-LeftRec (postfix-chain walker redesign), PA5a-prep (c-subset readiness shape additions), PA4-followups (8 items), and an LSP cross-cutting review fix-everything pass on the integrated stack. LSP spins off to `09-lsp-plan` once scope outgrows parser-plan. Closes `v2-gap-catalog - tbd.md` row 1 and production-readiness G-115 (diagnostic UX) end-to-end. **🟢 all 7 PRs ✅ done — parser phase closed.**
> - **[`06-artifact-profile-plan - tbd.md`](./06-artifact-profile-plan - tbd.md)** — new cross-cutting concept: `artifactProfile` field on language configs (declares supported outputs: `cli`/`gui`/`lib`/`staticlib`/`script`/`sproc`) + project configs (picks one). Driver enforces compatibility at load time. Codegen reads it to pick entry-point names, subsystem flags, output extensions. Four PRs (AP1 schema + loader, AP2 project config + driver, AP3 profile↔format compatibility gate [§B-RE-SCOPED 2026-06-09 from "`CompilationContext` plumbing" — codegen-threading would be a dead knob until a non-format-redundant consumer like gui exists], AP4 onboard shipped languages). **🟢 AP1 ✅ + AP2 ✅ + AP3 ✅ + AP4 ✅ (2026-06-09); only profile→codegen *shape* deferred (trigger-gated, `D-AP2-COMPILATION-CONTEXT`).** Gates §11 (`gen-link`).
> - **[`07-production-readiness-plan - tbd.md`](./07-production-readiness-plan - tbd.md)** — **master gap catalog for v1 production.** 127 numbered gaps (G-001..G-762) covering everything between current state and shipping v1 binaries on 3 OS × 2 arch. Cross-references every sub-plan above. Owns the acceptance criteria. **🔵 opened.**
> - **[`08-compilation-unit-plan - tbd.md`](./08-compilation-unit-plan - tbd.md)** — opens **phase #7.5** (`compilation-unit-model`): bridges parser (per-file `Tree`) and semantic (cross-file symbol table) via a `CompilationUnit` type + driver-level aggregator + per-language import resolver. Now five PRs (CU1 type, CU2 multi-file driver, CU3 cross-tree symbol shape, CU4 import resolver, **CU5 multi-language CUs** — added rev 2). Closes production-readiness G-110 + G-111. **🟢 CU1–CU4 done — phase #7.5 complete (G-110 ✅ + G-111 ✅); CU5/CU6 v1.x.** Preceded phase #8 (`analysis-semantic`).
> - **[`08.5-substrate-prep-plan - ok.md`](./08.5-substrate-prep-plan%20-%20ok.md)** — **new (rev 2).** Three-PR refactor between CU plan and semantic phase: SP1 generalize the arena + attribute substrate so HIR/MIR/LIR can reuse `Tree`'s engineering primitives; SP2 ship the core type lattice + per-language extension registry; SP3 type-aware designator resolution (`compositeScopeFor(TypeId)` + Pass-2 type-position stamping + InitSlot tree's `nested` substrate). Substrate-tier review. **✅ done (SP1 + SP2 + SP3)** — `src/core/substrate/` + `src/core/types/type_lattice/`; `typeExtensions[]` schema v3.
> - **[`08.55-pre-se1-cleanup-plan - ok.md`](./08.55-pre-se1-cleanup-plan%20-%20ok.md)** — **new.** A focused cleanup PR between 08.5 and 08.6, forced by a 5-agent audit (2026-05-25): retires the remaining engine-side source-language hardcoding under the user's standing "NO CAVEATS, NO WORKAROUNDS" rule (decision #4). Schema-drives the Pratt wrapper-rule names (`expr.wrapperRules`), schema-drives numeric lexing (`numberStyle` block), prunes pre-interned token kinds to universal categories, retires `well_known_names`/`tree_views`/`symbol_population`, makes the LSP shipped-language list a directory scan with loud-fail `ShippedDirNotFound`/`ShippedDirEmpty` error kinds. **✅ shipped (commit `1a433c2`).** Unblocked SE1.
> - **[`08.6-semantic-plan - ok.md`](./08.6-semantic-plan%20-%20ok.md)** — Opens phase #8 (`analysis-semantic`): symbol table + scope resolution + type checking. Load-bearing decision: semantics are **config-driven** (a `semantics` block in `.lang.json`, schema **v4**) read by one language-agnostic `analyze()` engine — **no per-language C++**, honoring the universal-compiler thesis (decision #4 below). Seven PRs (SE1 toy → SE2 c-subset → SE3 tsql → SE4 const → SE5 typedef → SE6 functions/calls/overloads → SE7 LSP). **✅ SE1–SE7 all shipped** (two 5-agent review rounds; 64/64 ctest; engine stays language-agnostic via the `semantics`/`kindByChild`/`callRules`/`builtinFunctions` config facets, proven generic by the Synth2/Synth3 schemas). Closes G-201..G-212.
> - **[`09-hir-plan - ok.md`](./09-hir-plan%20-%20ok.md)** — **new (rev 2).** The pivot layer — language-neutral, structured, typed HIR with shader/FFI/transpile attribute side-tables. CST→HIR lowering is **config-driven** (per thesis decision #4: the lowering is schema-described, NOT per-language C++) so a new language lowers to HIR via config, not engine edits. Eleven PRs (HR1–HR11). **✅ COMPLETE (HR1–HR11 all landed 2026-05-26..28 on `feature/hir-1`): HR1 ✅ (commit `406d5c7` on `feature/hir-1`, 2026-05-26 — arena + node shapes + walker + ids + extension registry, 69/69 ctest) + HR2 ✅ (2026-05-26 — typed expressions: operator open-core+registry, typed builder helpers, `HirVerifier` expression-typing rule, 72/72 ctest) + HR3 ✅ (2026-05-26 — structured CF: statement builders + typed read-accessor layer + break/continue & per-kind-arity verifier rules, 73/73 ctest) + HR4 ✅ (2026-05-26 — declarations + extern surface: 7 declaration builders/accessors, FfiMetadata side-table, checkDeclarationShape verifier rule, 74/74 ctest) + HR5 ✅ (2026-05-26 — attribute system + side-tables: 4 value-struct headers + `hir_attrs.hpp` catalog, verifier emits real diagnostic spans via optional `HirSourceMap` (closes the HR2 span-stash IOU), dedup key folds in `actual`, 75/75 ctest) + HR6 ✅ (2026-05-27 — full verifier: block dead-code + return completeness + Call-arg-vs-FnSig + intrinsic-registered + shader-restriction subverifier, new `HirIntrinsicRegistry`, optional `TypeInterner` injection, `H_UnknownIntrinsic`/`H_ShaderViolation`, 76/76 ctest) + HR7 ✅ (2026-05-27 — round-trippable `.dsshir` text format: `emitHir`/`parseHir` + non-owning `HirTextContext` + heap-stable `HirParseResult`, inline structural types, positional `%N` symbol handles, all 5 side-tables inline, verify-on-load, in-memory + golden-corpus tests, `H_TextMalformed`/`H_TextVersionMismatch`/`H_TextUnknownName`, 77/77 ctest) + HR8 ✅ (2026-05-27 — config-driven CST→HIR lowering engine + `hirLowering` schema facet, proven end-to-end on c-subset (reordered ahead of toy); per-expression type inference + a `HirLiteralPool` of decoded literal values; verify-on-load; deferred constructs fail loud via `H_UnsupportedLoweringForKind`; closed an HR7 return-value-attr parse bug; 78/78 ctest) + HR9 ✅ (2026-05-27 — `toy` enriched in place into a real typed language + a generic lowering test proving the one engine handles a second grammar; **arrays un-deferred end-to-end** via a config-driven `DeclarationRule.arraySuffix` declarator-suffix descriptor + semantic-time constant-length eval (`S_NonConstantArrayLength`/`S_ArrayLengthOutOfRange`, fail-loud, no pointer decay); shared `decodeInteger` (`number_decode.hpp`); compound-assign/`++`/externs/genericity earlier; 5-agent review + fix pass caught a silent negative-length wrap; 80/80 ctest) **+ gap-closure pass 2026-05-28** completing the frontend→HIR layer: char/string literal VALUES (coalesce-body lexer — the former TZ4 blocker is resolved), HIR `SeqExpr` (value-yielding `++`/assignment-as-expression/complex-lvalue compound-assign), pointers, ternary `?:`, `#include` skip, `.dsshir` inline literal values (81/81) + HR10 ✅ (2026-05-27..28 — CST→HIR lowering for **tsql-subset** through the SAME language-agnostic engine: role-explicit SQL extension nodes via a generic `childGathering` config vocabulary + `ChildLower` enum, flat-expr lowering, coalesced/doubled-delimiter string VALUES → `Array<Char,N>`, `NULL`→`TSQL::Null`, relational names→`refExtensionKind` leaf (not core `Ref`), `ReferenceRule.hardParents` positional table-vs-column resolution, SQL calls reuse `callRules`; 5-perspective review + fix pass clean; 82/82 ctest). **HR11 ✅ done 2026-05-28 (multi-language CU lowering). Plan 09 (HIR) COMPLETE — HR1–HR11 all ✅.**
> - **[`10-source-translation-plan - tbd.md`](./10-source-translation-plan%20-%20tbd.md)** — **new (rev 2).** Promotes the formerly-long-running source-translation note (§9) into a real phase. Language-pair `.map.json` + HIR→HIR walker + target-CST builder + pretty-printer. Six PRs (ST1–ST6). v1.x. **⏳ planned.**
> - **[`11-ffi-plan - tbd.md`](./11-ffi-plan%20-%20tbd.md)** — **new (rev 2).** Hermetic FFI: in-tree ELF/PE/Mach-O/ar readers, C header mode parser, ABI catalog, name mangling, HIR extern-decl ingestion. Six v1 PRs (FF1–FF6). C++ mangling, full preprocessor reserved post-v1. **✅ MOSTLY DONE 2026-06-02 — FF1–FF6 closed; shipped-lib ingestion (FF11) live.**
> - **[`12-mir-lir-plan - ok.md`](./12-mir-lir-plan%20-%20ok.md)** — **new (rev 2).** MIR (SSA over CFG + structured-CF markers preserved) + LIR (per-target JSON-configured ISA, virtual+physical regs, calling-conv lowered, frame materialized). Eight PRs (ML1–ML8). **✅ done 2026-05-27..29 — ML1–ML8 closed end-to-end** including ML8 cycle 3 substrate cleanup (TargetTerminatorKind enum + schema-driven dispatch; `isTerminator` field deleted, derived from `terminatorKind`). ML7 cycle 2 ARM64-stackPointer + ABI goldens still anchored as legitimate next-cycle work.
> - **[`13-assembler-plan - tbd.md`](./13-assembler-plan%20-%20tbd.md)** — **new (rev 2).** In-tree machine-code encoder for x86_64 + ARM64. Hand-written encoding tables per Intel SDM / ARM ARM. Per-(arch×format) relocation taxonomy. Six PRs (AS1–AS6). **✅ done 2026-05-29 — AS1–AS6 closed end-to-end** (`feature/as-1`) including shape-keyed byte-encoder walkers (`x86-variable` + `fixed32`), round-trip oracle disassembler, relocation taxonomy unifier, SourceMapEntry stamping at dispatch level, end-to-end c-subset corpus pipeline.
> - **[`14-linker-plan - tbd.md`](./14-linker-plan%20-%20tbd.md)** — **new (rev 2; config-driven shift rev 3 2026-05-29).** In-tree linker — JSON-configured object formats (`src/dss-config/object-formats/*.format.json` for ELF / PE / Mach-O / WASM-skeleton / SPIR-V-skeleton) + ONE format-blind engine + symbol resolution + relocation application + per-platform metadata + file emission + driver pipeline wiring. Ten PRs (LK1–LK10). Mirrors the `GrammarSchema` (frontend) + `TargetSchema` (backend ISA) config-driven pattern — new object format = drop a JSON file, no engine edits. **Largest single chunk of backend work.** **✅ LK1–LK10 CLOSED end-to-end 2026-05-30** on `feature/lk-111`: LK4 substrate + LK1/LK2/LK3 writers (.o + executable images ET_EXEC/.exe/MH_EXECUTE) + LK6 cycle 1 reloc-apply + LK6 cycle 2a–d dynamic linking (PE IAT / ELF GOT+PLT / Mach-O LC_DYLD_INFO_ONLY / HIR→AS extern thread-through) + LK7 codesign placeholders + LK8/LK9 WASM/SPIR-V skeletons + LK10 file emission (`dss::linker::writeImage`) + driver pipeline wiring (`Program::compileFiles` / `compileDirectory`). LK5 (TLS) deferred to first TLS-bearing corpus; LK11 (cross-CU) deferred to v1.x.
> - **[`15-debug-info-plan - tbd.md`](./15-debug-info-plan%20-%20tbd.md)** — **new (rev 2).** DWARF 5 writer (ELF + Mach-O) + PDB writer (PE) + CFI / SEH / compact-unwind tables + source-position chain. Twelve PRs (DB1–DB12). **⏳ planned.**
> - **[`16-codesign-publish-plan - tbd.md`](./16-codesign-publish-plan%20-%20tbd.md)** — **new (rev 2).** Hermetic codesign + publish: Apple Mach-O codesign + notarization stapling, iOS provisioning, Windows Authenticode + RFC 3161 TSA, Android APK v3 (skeleton). In-tree crypto substrate (BearSSL vendored). Nine PRs (CS1–CS9). Apple-host-free local dev. **⏳ planned.**
> - **[`17-shader-gpu-plan - tbd.md`](./17-shader-gpu-plan%20-%20tbd.md)** — **new (rev 2).** SPIR-V codegen + shader-shape HIR extensions + binding-resource model + same-source CPU+GPU function dual-lowering. Ten PRs (SG1–SG10). v1.x — lit up for the custom language. **⏳ planned (reserved scope).**
> - **[`18-wasm-plan - tbd.md`](./18-wasm-plan%20-%20tbd.md)** — **new (rev 2).** WASM backend — HIR/MIR → WASM bytecode using structured-CF markers (no Relooper needed). WASM as "LIR-equivalent" target. Ten PRs (WA1–WA10). v1.x. **⏳ planned.**
> - **[`19-hir-hw-reserved-plan - tbd.md`](./19-hir-hw-reserved-plan%20-%20tbd.md)** — **new (rev 2).** Reserved sibling-IR for VHDL / Verilog / SystemVerilog (concurrent + signal-typed + clock-bound). Software HIR and HIR-HW do not unify cleanly. 🔒 reserved.
> - **[`20-custom-language-reserved-plan - tbd.md`](./20-custom-language-reserved-plan%20-%20tbd.md)** — **new (rev 2).** Reserved for the user's eventual custom language. The engine already supports it via the schema mechanism; this plan owns vocabulary + stdlib + runtime decisions. 🔒 reserved. **⤳ Superseded by [`24-dss-axis-language-reserved-plan`](./24-dss-axis-language-reserved-plan%20-%20tbd.md) — "DSS Axis," the rich named reservation of this same language** (2026-06-15).
> - **[`21-runtime-reserved-plan - tbd.md`](./21-runtime-reserved-plan%20-%20tbd.md)** — **new (rev 2).** Reserved for GC / exception unwinder / coroutines / threading primitives. Distinct from OS-supplied runtime libs (those are FFI targets). 🔒 reserved.
>
> See §8 (Implementation Phases) for the current cross-plan dependency view.

## 0. Current Status (snapshot)

> **Version: v0.0.2 (in development) · v0.0.1 SHIPPED.** **v0.0.1** = the config-driven c-subset→native toolchain, end-to-end: tokenizer/parser/LSP + CU + semantic + HIR→MIR→LIR + x86_64/ARM64 hand-encoders + ELF/PE/Mach-O in-tree linker + cross-CU LTO merge + FFI shipped-lib ingestion (FF11) + the full optimizer **OPT1–OPT7** (ConstFold/DCE/Mem2Reg/CopyProp/CSE/SimplifyCFG/LICM/block-merge/**Inlining** — shipped in release, recursion-safe, cross-CU, intrinsic-aware). Runnable binaries; **Windows-PE + Linux-ELF (x86_64) execution-verified**. **v0.0.2 roadmap** (the recommended-interleave build-out, §0.1): **v0.0.2** = ARM64 runnable+verified ∥ language-agnostic shipped-lib ingestion → artifactProfile + `dss-code-prime build` CLI → RISC-V → WASM → OPT7+ (V2-1…V2-7). **v0.0.3** = the original v0.0.1 backlog v0.0.2 doesn't cover (debug-info → codesign → Mach-O-verify → CU6 → CU5 → SPIR-V → source-translation → infra → reserved). §0.1 is now SPLIT three ways — the **v0.0.2 ROADMAP** then the **v0.0.3 ROADMAP** (next-up, the lists `/dss-cycle` walks) above the **v0.0.1 as-built ledger** (done).

> **v1 production target locked.** End-to-end binaries for `toy` / `c-subset` / `tsql-subset` on **{Windows, Linux, macOS} × {x86_64, ARM64}**. Frontend (tokenizer + parser + LSP) ✅; CU + semantic + HIR + MIR + LIR + assembler + linker + FF6 runtime print ✅; OPT1–OPT6 v1 + v1.x mandatory subset ✅ (OPT5+ c3 closed 2026-06-03; v1 ConstFold + DCE done; v1.x Mem2Reg + CopyProp + CSE + SimplifyCFG + LICM all live). **OPT7-prep 10a-10r ✅** 2026-06-04 — alias analysis substrate + char-exception config + alias-arc strict capstone (corpus + MIR-tier effectiveness) + LICM chained-invariants closure + `long`→I64 in c-subset + sext encoding + schema-mutation test substrate + diagnostic code per-kind split + **Arc A integer-division codegen** (idiv/div, 4-opcode REX-correct split `50a07ab`; closes D-CSUBSET-DIVISION-OP-CODEGEN, unblocks D-OPT6-LICM-TRAP-SAFE-HOIST). **Pre-OPT7 chain locked** 2026-06-04 (full cross-CU Weak-pin scope: front-end linkage specifiers → HIR→MIR linkage mapping → CU6 → LK11 → OPT7; see plan 22 §0.1) — OPT7 stays the HARD-STOP / user-supervised boundary (`G-406`). Driver (`compileProject`) + `artifactProfile` mechanism + debug info + cross-host ARM64 / Linux / macOS Stage 2 still ⏳ — see [`07-production-readiness-plan - tbd.md`](./07-production-readiness-plan - tbd.md) for the 127-item gap catalog.

| Area | State |
|---|---|
| Build system (CMake 4.0 floor, C++23, FetchContent of nlohmann/json 3.12.0 + GoogleTest 1.17.0) | ✅ working |
| Core types — full sub-plan T0–T12 (tree/node/diagnostics/schema + `TreeBuilder` + `TreeCursor` + `tree_visitor` + `NodeAttribute<T>` + typed views + E2E + CMake wireup + onboarding docs) | ✅ **complete** |
| Schema expressiveness v2 (sub-plan PR0–PR8) — c-subset + operator precedence (`OperatorTable`) + real `SchemaCursor` walker + contextual keywords + `scopeRequire` + `TreeBuilder::Checkpoint` + `lexerModes`/`LexerModeStack`/`modeOp` + `stringStyle` descriptor + tsql-subset empirical stress + cross-plan close-out | ✅ **done** — all 10 PRs (PR0, PR1, PR2a, PR2b, PR3–PR8) shipped + review-fixed |
| **Total ctest cases** | **628/628 ctest (current 2026-07-13 through the FC17.5 `_BitInt` arc — C1–C4b complete, the arc CLOSED; the FC16-era detail below is historical: `_Alignof`/`alignas` + wide/UTF literals A–D + `packed` attr-semantics `D-CSUBSET-PACKED` + `noreturn` semantics `D-CSUBSET-NORETURN`, both 2026-07-08, + `volatile`-honoring codegen already-shipped (c21/c27/opt-10b, confirmed 2026-07-08 — stale-plan correction) → **FC16 CORE COMPLETE; FC17/FC18 next**; earlier post-sqlite arc: mem2reg conditional-init-undef +2 [`cond_init_release`/`cond_init_float_release`]; c116 +3 [the x64 SEH funclet + `__C_specific_handler` scope-table FINALE (`D-WIN64-SEH-FUNCLETS` + `D-WIN64-XMM-UNWIND-RESTORE` CLOSED) — sqlite compiles+runs with SEH ON (no defines), `SQLITE_OMIT_SEH` DROPPED; 3 runnable Windows witnesses `seh_catch_{av,parent_local,multiblock}` (AV caught->42, H1 parent-frame recovery->42 with a segfault negative-control, loop-in-try->42) + the new generic `RecoverParentFrameSlot` op + byte-exact pe scope-table/xmm-guard pins + 4 `SynthSehFunclets.*` MIR pins riding existing binaries; design-audit GO-WITH-FIXES + code-audit GO]; c115 +0 [the `__try`/`__except` SEH FRONTEND (parse+sema+HIR+MIR; mir_to_lir fail-loud, D-WIN64-SEH-FUNCLETS c116) — sqlite compiles WITHOUT `SQLITE_OMIT_SEH` (87 parse errors -> 35 funclet-wall diagnostics); 14 new red-on-disable pins (7 HIR context/fail-loud + 4 MIR skeleton/release-pipeline-green/mem2reg-skip/nesting-marker + mir_to_lir anchor fail-loud + CSE clobber + EXCEPTION_RECORD layout) all RIDE existing binaries; no runnable corpus [fail-loud boundary = no runtime, the carve-out — c116 ships the AV witness]]; c114 +1 [pe_unwind_selftest corpus — the pe64 .pdata/.xdata unwind-table RUN witness (`D-WIN64-PDATA-XDATA-UNWIND`): `RtlLookupFunctionEntry` finds the emitted RUNTIME_FUNCTION -> exit 42; sqlite3.exe compiles SEH-only (no SQLITE_DISABLE_INTRINSIC) + RUNS on all 3 legs (win pe64 + linux x86_64 + arm64-qemu, --version 3.54.0 + SELECT sum 42); the 2 new test_pe_writer pins `FunctionUnwindInfoEmitsPdataXdataAndExceptionDataDir` + `FunctionUnwindInfoStackProbePrologueUsesFixedAllocLen` (the audit-caught fixed-probe-length fold) ride the existing binary]; c113 +1 [intrin_barrier corpus — pe64 `<intrin.h>` + the `_ReadWriteBarrier` compiler-barrier builtin (`D-CSUBSET-INTRINSIC-BARRIER`): sqlite compiles+runs WITHOUT SQLITE_DISABLE_INTRINSIC; the audit-F1 clobber-walk hardening pins `Cse.LoadNotCsedAcrossCompilerBarrier` + `MirAlias.RegionWalkTreatsSideEffectingNonStoreAsClobber` + `RealIntrinHeaderIsPeOnlyAndCarriesNoEagerSymbols` ride existing binaries]; c112 +1 [addr_import corpus — the PE import-thunk address-taken-import RUN witness (`D-FFI-PE-IMPORT-THUNK`): sqlite3.exe RUNS on Windows, all 3 legs run real SQL; the 2 new PE walker pins `ShippedPeExecIsDirectPlt` + `AddressTakenImportResolvesToTextThunkNotIdataSlot` ride the existing test_pe_writer binary]; c76-c89 +39, c90 +1 assign_value_coerce example + 2 audit HIR pins, c91 +1 array_decay_pointer_identity example + 2 decay pins [HIR arms + MIR no-Array-Load invariant]; c92 +0 [arm64 FCVTZU legs added to the existing double_to_unsigned example — count unchanged, qemu-run]; c93 +0 [index_negative regained its release/full-release-like arms — same ctest entry, arm64-release negative-disp lea now runs under qemu]; c94 +0 [arm64 native-SUB lea + the const-disp fold-skip: 5 byte-pins ride existing test_asm_arm64/x86_variable; + the sqlite multi-target test harness build-and-test.sh]; c95 +1 [windows_predefine_macro corpus; the _WIN32 per-format-predefine mechanism + quote->angle include fallback + windows.json skeleton -> sqlite os_win.c PARSES for pe64; test_preprocessor opt-out 7->13 + a load-error pin ride the existing binary]; c96 +0 [4 Win32 typedefs LPHANDLE/LPDWORD/LPBOOL/ULONG64 added to windows.json + the windows_predefine_macro corpus extended to round-trip them — same ctest entry]; c97 +0 [compile-time perf ~102x (15m29s->9.09s): O(log K) verified sibling search in the include resolver + per-phase --time timers + O(1) parser dense-table/bitset; the PhaseTimers RED-pin + 2 sibling-fallback pins ride existing binaries]; c98 +1 [3 Windows CRT header descriptors process/io/direct.json (msvcrt, pe-gated) clearing the 4 F001A + the shipped_windows_crt_batch corpus] = 551; c99 +2 [fam_struct_in_union_member (a FAM-bearing struct as a DIRECT union member — C99 6.7.2.1p18 / gcc-clang parity — the sqlite SrcList stack-slab) + cast_deref_through_typedef_pointer (the non-reproducing cast-deref regression pin), both carrying release/shippedPipeline arms; the 4 semantic pins + a TypeLayout companion pin + the updated fam_in_union diagnostic corpus ride existing binaries] = 553; then a config-discovery hardening +1 [core/test_config_path_walk — the DSS_CONFIG_ROOT env override so OUT-OF-TREE/CI ctest resolves shipped config regardless of cwd, red-on-disable] = 554; c100/c101/c102 +0 [each added a Real* pe-descriptor SIGNATURE/LAYOUT pin - RealTimeStructTmPerFormatLayout / RealWindowsSyncStructLayout / RealWindowsKernel32FileHeapTimeSignatures - to the EXISTING test_shipped_lib_descriptor executable; a gtest TEST inside an already-registered ctest binary adds no ctest ENTRY, so the count holds. c102 shipped 47 kernel32 file/heap/time fns + 15 constants -> sqlite pe64 50->6 undefined, residual = the 2 c103 intrinsics] = 554; c103 +1 [examples/c-subset/umulh_intrinsic -- the FIRST DSS intrinsic-lowering seam + __umulh (high-64 of the u64*u64 product): a config `lowering` builtin -> HirKind::BuiltinCall -> MirOpcode::UMulH -> x86 umul_op / arm64 umulh; the umul_op + umulh encoder byte-pins ride existing test_asm binaries] = 555; then the CI-FIX +0 [D-CE-HOST-SIGNED-OVERFLOW-UB: const-eval Neg/Add/Sub/Mul now WRAP via uint64 (host-UB-free; PR#34 UBSan leg) — 2 boundary-pin TESTs ride the existing test_const_eval binary] = 555; c104 +1 [examples/c-subset/atomic_cas_intrinsic -- InterlockedCompareExchange as the _InterlockedCompareExchange DSS builtin (the c103 seam + a full type-text signature axis): MirOpcode::AtomicCas -> x86 lock_cmpxchg (LOCK 0F B1, implicit-RAX comparand/old) / arm64 LDAXR-STLXR retry loop as REAL CFG blocks; the lock_cmpxchg prefix-order + ldaxr/stlxr byte-pins ride existing test_asm binaries] = 556; c105 +0 [the MSVC-profile flip (USER SS-B: _MSC_VER=1943 pe -- sqlite dirent needs its MSVC windirent shim; MinGW opendir is unimportable) + --define NAME[=VALUE] (D-PP-USER-DEFINE: the <command-line> prologue, ordinary-macro semantics) + function-like predefines (D-PP-FUNCTION-LIKE-PREDEFINE: __declspec(x) erase via the <built-in> prologue) + __int64/__forceinline pe maps + malloc.json (_msize) + the harness OMIT_SEH/DISABLE_INTRINSIC knobs (c106 removes the latter); 8 pp pins ride the existing test_preprocessor binary] = 556; c106 +0 [the shell.c pe descriptor batch: 10 shippedLibs descriptors (wchar_t per-format variants, errno/fcntl/sys-types/sys-stat pe axes w/ loader-law symbol gates, windows.json find/console surface, stdio __iob_func stream macros, msvcrt TIME32 find/stat families, strtoll->_strtoi64 split) -> pe64 2-TU 853->2 (the ULARGE_INTEGER.QuadPart pair = c107 D-FFI-DESCRIPTOR-UNION-OVERLAY); 5 Real* per-format pins ride test_shipped_lib_descriptor] = 556; c107 +0 [the ULARGE_INTEGER overlap = the descriptor explicit-`offset` field (D-FFI-DESCRIPTOR-UNION-OVERLAY): offsets in a parallel interner channel + the content identity + computeLayout + type-text `@off` + 2 fail-loud guards; 4 unit pins [TypeLayout ExplicitOffsets*, RealWindowsUlargeOverlayLayout, ParseTypeFromText/HirText ExplicitFieldOffsets*] ride existing binaries] = 556; c108 +0 [the gcc-CI FlatChainParseWorkIsLinear flake fix (D-PARSE-FLAT-CHAIN-WORK-LINEAR): a DETERMINISTIC token-access work counter (TokenStream accessCount → ParseResult.tokenAccessCount) replaces the flaky wall-clock ratio; the existing test now pins the access-count first-difference constant] = 556; c109 +0 [FILE sized to the 48B msvcrt _iobuf (opaque arr<u64,6>) in stdio.json×35+io.json×1 so pe `__iob_func()[N]` strides → sqlite3.c+shell.c pe64 COMPILE SURFACE = 0 errors, reaches encode+link (residual = the entry-trampoline PE link wall); config-only, run-green preserved (elf+arm64 sqlite SELECT→42, witnessed by the multi-target harness, not a new ctest entry)] = 556; c110 +0 [the PE entry + loader-imports: c-subset.lang.json entryFunctionNames += wmain (shell.c's MSVC entry; main is #define'd to utf8_main) + time.json localtime_s/gmtime_s → the real msvcrt _localtime64_s/_gmtime64_s exports + pe macros (D-FFI-MSVCRT-LOCALTIME-S-EXPORT, the c105-M3 landmine) → sqlite3.c+shell.c LINK + LOAD to a Windows sqlite3.exe (0 compile errors, no 0xC0000139); the wmain wide-args runtime = c111] ; c111 +1 [wmain_argc pe64 WIDE-argv runtime witness + main_argc_argv pe leg flipped runOn=windows; the synthesized pre-main CRT-args init `synthesizePeStartup` -> D-RUNTIME-PE-MAIN-ARGS CLOSED; the 4 SynthPeStartup pins + descriptor pins ride existing binaries] = 557), 100% pass — Release AND Debug** (Win MSVC + MinGW; the formerly-pre-existing MSVC-**Debug** deep-recursion segfault #90 is now FIXED — PR #32's `prescanLabels` heap-work-stack flattening (`D-PARSE-DEEP-NEST-RECURSION-MEMORY` missed site) merged to main and rebased into p15 2026-06-24, orthogonal to this cycle) — the 399 total now INCLUDES the 215/215 c-subset examples (x86-native + arm64-qemu, baseline+optimized) [count current as of 2026-06-24, branch feature/0-0-2-p15, FC15 ✅ COMPLETE + Cluster F F1 `++`/`--` ✅ + F2 bitwise ✅ + F3 float mul/sub ✅ (cycle 7) — Cluster F F5 string-index + symbol-address-globals DONE 2026-06-25 (cross-format: PE+ELF exit 143, Mach-O dyld-rebase + multi-CU merge fold) -- F4 landed integer NARROWING (the real SQLite blocker; promotion was already done), closing the integer-conversion matrix; **p18 SQLite-readiness Cluster G** (branch feature/0-0-2-p18-sqlite-green): cycle 1 = shipped `<stdint.h>`/`<stddef.h>`/`<stdarg.h>` neutral JSON descriptors (FIRST real consumer of the FC14 typedef-injection path; corpus `shipped_int_headers` exit 42 baseline+release) + the `--time` compiler CLI flag + the `real-examples/c/sqlite/build-and-test.sh` harness; ★ DISCOVERED + anchored HIGH `D-OPT-VARIADIC-RELEASE-MISCOMPILE` (a variadic fn exits 42 baseline / 0 release — blocks SQLite-green RELEASE; FC12 shipped variadic baseline-only so the optimizer×va_list compose was never witnessed). c2 ✅ FIXED the variadic-release INLINING miscompile (`D-OPT-VARIADIC-RELEASE-MISCOMPILE` CLOSED — `inlineLegalityGate` refuses any callee with a va_start leaf [VaRegSaveAreaAddr/VaOverflowArgAreaAddr/VaHomeArgAreaAddr], the universal "variadic fn not inlinable" rule; MIR pin + `varargs_inline_release` release witness, both red-on-disable). ★ Closing it surfaced 2 MORE variadic-release optimizer bugs the FC12 baseline-only corpus masked (a CLUSTER): `D-OPT-VARIADIC-RELEASE-MEM2REG-PHI` (Mem2Reg phi fatal on varargs_win64_sum) + `D-OPT-VARIADIC-RELEASE-ARGINDEX` (Arg-index-out-of-range on varargs_overflow_fixed_stack) — both fail-LOUD, anchored. c3 ✅ FIXED the Mem2Reg one — ★ the PROBE OVERTURNED its scope: NOT variadic-specific but a GENERAL Mem2Reg crash on ANY loop-body-local (`while(){ int x=...; }`), HIGH-severity, masked because no release-arm example had a loop-body-local; fixed via FULLY-PRUNED SSA (gate phi placement on LIVE-IN; preserves the genuine-uninit fatal) — `D-OPT-MEM2REG-DEAD-PHI-PRUNE` (subsumes `D-OPT-MEM2REG-LOOP-BODY-LOCAL-DEAD-PHI`) + `D-OPT-VARIADIC-RELEASE-MEM2REG-PHI` CLOSED. ★ the c3 pr-review's RUNTIME battery caught that the initial semi-pruned commit (ecf40c3) only fixed the loop-body-local half — NESTED loops still crashed (a name dead at one IDF block but live elsewhere) → upgraded to fully-pruned (live-in) the same cycle; witnesses `loop_body_local` + `nested_loop_release` + `varargs_win64_sum` release arms. c4 ✅ FIXED the last cluster member `D-OPT-VARIADIC-RELEASE-ARGINDEX` (root cause = DCE elided unused param `Arg`s → a stranded high-ordinal `Arg` tripped the verifier's count-based bound; GENERAL not variadic; fix = `Arg` is an ABI-parameter ROOT in DCE) — **the whole variadic-release CLUSTER (inlining + Mem2Reg dead-phi + argindex) is now CLOSED**. c5 ✅ descriptor `macros` surface (preprocessor injects function-like/object-like/variadic `#define`s when an angle-`#include` resolves to a descriptor; FIRST consumer `<assert.h>` → `assert(e)` = `((void)0)` NDEBUG no-op; corpus `shipped_assert_macro` exit 42 baseline+release on all 4 ABIs; `D-PP-DESCRIPTOR-MACRO-INJECT` CLOSED). ★ SQLite-green STRATEGY (user 2026-06-25): FULL UNIX build (real os_unix VFS + DYNAMIC libc -> run-green in WSL; proven runnable by shipped_include_puts), NOT SQLITE_OS_OTHER. c6 ✅ probed sqlite3.c (exact POSIX surface) + fixed SUBDIRECTORY descriptor resolution (resolveSystemDescriptor preserves the subdir: `<sys/types.h>` -> sys/types.json, DISTINCT from `<time.h>` -> time.json) + shipped sys/types.json (POSIX typedefs, LP64-linux) + a subdir-collision red-on-disable pin. c7 ✅ shipped the STRUCT-BODY descriptor mechanism (a `structs` surface + the semantic tag/field-scope/compositeScopeByType injection built by hand, byte-identical to a user struct; named-field structs like struct timeval resolve by-name + auto-layout offsets; FRONTEND ZERO-CHANGE; corpus shipped_struct_timeval exit 42). next = sys/stat(struct stat padding/field-order) / fcntl(flock+O_/F_) / errno / unistd / sys/mman / pthread / ... -> compile+run sqlite3.c in WSL] [latest 2026-06-19: **Mutable-global store crash fixed — writable `.data`/`.bss` sections ✅** (`D-LK4-DATA-PRODUCER-MUTABLE-GLOBAL` — a pre-existing SIGSEGV: `asm.cpp` put ALL initialized globals in read-only `.rodata`, so a store to a mutable global faulted; now an initialized MUTABLE global → writable `.data`, a TENTATIVE zero-init → `.bss` [closing `D-LK4-RODATA-PRODUCER-BSS-EMIT`], a CONST → `.rodata`, keyed on `MirGlobal.isConst` threaded source→codegen via a `MutabilityAttr` HIR side-table; all 3 exec writers [ELF W^X 2nd R+W PT_LOAD / PE / Mach-O `__DATA`] emit writable `.data` + NOBITS `.bss` from config-driven `.format.json` rows — zero identity branches; corpus `global_write` exit 42 [was SIGSEGV 139] on PE/ELF/qemu, macho on the macos-latest leg; const-preservation pinned red-on-disable across all 4 global-clone sites [verbatim/carve-out/DCE/merge]; runtime-init globals stay fail-loud [`D-LK4-RODATA-PRODUCER-RUNTIME-INIT`]; user §B-decision = build `.data`+`.bss` now, runtime-init deferred); prior **FC8 SUBSTRATE — aggregate-valued ternary/comma rvalue carrier ✅** (`D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR` CLOSED — an aggregate-typed `Ternary`/comma-`SeqExpr` used BY VALUE materializes each control-flow arm into ONE COMMON slot under a CFG diamond, with NO Phi-of-aggregate [the memory-based model]; `lowerLvalueAddress` gains Ternary+SeqExpr arms + `materializeAggregateArmIntoSlot` [a compound-literal arm inits in place; a named-lvalue/call arm copies]; the gate's independent review was SHIP-CLEAN but surfaced — and this cycle SEALED — a PRE-EXISTING discard-position silent miscompile [a discarded aggregate `Ternary`/`SeqExpr` with named-lvalue arms produced a forbidden Phi-of-aggregate + aggregate-width Loads] via a shared `lowerDiscardedExpr` chokepoint covering BOTH discard positions [the `ExprStmt` site AND the bare for-init/for-update clause — a follow-up seal review caught the for-clause as a 2nd discard position the ExprStmt-only fix missed] + anti-resurrection fail-loud guards on `lowerExpr` Ternary/SeqExpr; corpus `aggregate_ternary_rvalue` [both arms witnessed + named-arm copy-path + signed bit-field ternary, anti-fold via a global load] PE/ELF/qemu=42 [macho on macos-latest]; 6 MIR pins [diamond+common-slot+no-Phi, bit-field per-unit, SeqExpr, named-arm copy-path, ExprStmt discard-seal, for-clause discard-seal], red-on-disable demonstrated [the Phi-of-aggregate is caught]); this + all 3 FC8 §B deferrals (`-WIDE-UNIT`/`-RVALUE-RUNTIME`/`-ABI-EXACT`, macos+arm64 CI green) complete the FC8 SUBSTRATE — **FC9 is the next block**; prior **FC8 cycle 6 — typedef-enum trailing comma ✅** (`D-CSUBSET-TYPEDEF-ENUM-TRAILING-COMMA` CLOSED — RECONCILIATION: the gap was ALREADY closed by the global `recomputeAltExpectedSets` fixpoint, verified via a toggle that reddens both trailing-comma pins; no grammar change, now pinned `D5_5_TypedefEnumTrailingCommaParses`); this completes ALL IN-SCOPE FC8 — the only remaining FC8 work is trigger-gated/blocked (§B-PAUSE); prior **FC8 cycle 5 — enum-typed bit-fields ✅** (`D-CSUBSET-ENUM-BITFIELD` CLOSED — an enum bit-field validates + lowers at its underlying integer via `enumReprType`; a signed-underlying enum bit-field sign-extends; independent review caught a codegen red-on-disable over-claim, re-pinned via a negative-enumerator corpus + a MIR op-representation pin); prior **FC8 cycle 4 — bit-field aggregate init ✅** (`D-CSUBSET-BITFIELD-INIT` CLOSED — packs per allocation unit at all 3 init sites; review-caught F1 ordinary-field-in-shared-unit clobber fixed via a two-pass zero; opened `D-CSUBSET-BITFIELD-RVALUE-RUNTIME`); prior **FC8 cycle 3 — non-string array→ptr decay ✅** (`D-LK4-RODATA-PRODUCER-LOCAL-ARRAY-DECAY` + `-NONSTRING-GLOBAL-ARRAY-DECAY` CLOSED — a non-string array used as a pointer decays to `&arr[0]` via `lowerLvalueAddress`+`Gep(0)` [local alloca / global GlobalAddr / member / indexed], reusing existing infra; corpus `array_decay` exit 42 incl. write-through, x86-PE/ELF + arm64-qemu, red-on-disable; the stale `D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY` reconciled — array globals already work); prior **FC8 cycle 2 — C bit-fields ✅** (`D-CSUBSET-BITFIELD` CLOSED: named/anonymous/zero-width, signed+unsigned, read/write/packing — bit-width on the interned Struct/Union scalars (empty when none → no TypeId churn) + a config-driven `BitFieldStrategy` (`gnu_packed`, `.target.json`) GNU/SysV LSB-first packing in `computeLayout` + `hir_to_mir` extract (LShr+And / sign-extend) & read-modify-write insert reusing existing MIR ops; corpus `bitfields` exit 42 [red-on-disable → 11] x86-PE/ELF + arm64-qemu; 7 layout pins + 4 diag fail-loud pins + 1 structural codegen pin, all red-on-disable; ABI-exact layout trigger-gated `D-CSUBSET-BITFIELD-ABI-EXACT`, enum-bitfields `D-CSUBSET-ENUM-BITFIELD`); prior **FC8 cycle 1 — enums end-to-end ✅** (`D-CSUBSET-ENUM-INT-CONVERSION` CLOSED: an enum behaves as its underlying int (C 6.7.2.2) across assign/UAC/cast/`coerce` + the `isEnumerator`-gated enumerator Ref→Const fold + inc/dec computed at the underlying + the REVIEW-CAUGHT LIR `reprKind` packed-enum-field memory-width fix [an over-wide enum field/element store clobbered its neighbour — `D-LIR-INT-MEMORY-WIDTH-EXACT` for enums, masked by scalar-local slots]; corpus `enum_value` exit 7 incl. the packed-field width witness [red-on-regression → -93], x86-PE/ELF + arm64-qemu; 7 unit pins + 1 LIR width pin, all red-on-disable); prior **top-level struct/union/enum head grammar + aggregate-global rodata codegen ✅** (commits 1252fe5/fc5deb8/8daeb57: `D-CSUBSET-STRUCT-BODY-VARDECL-POSITION` + the schema-FOLLOW root-fix `D-PARSE-SCHEMA-NESTED-NULLABLE-FOLLOW` + `D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL` ✅ — initialized struct/union/array globals → byte-exact rodata, corpus `struct_body_top_level` + 5 `AsmAggregateGlobal.*` red-on-disable pins; `int ;` now fails loud; NEW open fail-loud anchors `D-CSUBSET-ENUM-INT-CONVERSION` + `D-CSUBSET-TYPEDEF-ENUM-TRAILING-COMMA`); prior **FC7 ✅ COMPLETE** — C3 AAPCS64/Apple x8-sret + HFA closes `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` (all 4 ABIs) + a found+fixed HIGH pre-existing multi-piece-`Return`-clone silent miscompile + the inlining-of-struct-returners hazard (gate-refused, post-review); FC7 C2 Win64 ms_x64 struct by-value (by-size: pow2<=8 -> 1 GPR else by-ref/sret), native-PE exit 42; C1c SysV struct-RETURNS — ReturnPiece+sret+mixed, D-FC7-SYSV-STRUCT-RETURN-IN-REGS ✅, branch p9] — post **2026-06-15 sizeof char/string cycle** (branch p8: fixed a compiler CRASH on `(int)sizeof('c')` [`TypeInterner::get out of range` — unguarded `interner.kind()` on a failed-sizeof InvalidType operand in lowerCast] + typed char literals `int` [`sizeof('c')`==4, C 6.4.4.4] and string literals `char[N+1]` [`sizeof("abcd")`==5, C 6.4.5] so sizeof folds; config-driven via a diverted `stringArray` literalTypes row + a unidirectional int→char `isAssignable` arm; +2 corpus), itself post the **2026-06-15 pre-FC7 residuals cycle** (R1: member access folds `int a[sizeof(s.y)]` at Pass 1.5 via shared `resolveMemberAccess`; R2: closed `D-SEMANTIC-NULL-CONSTANT-FOLDING` — folded-zero `1-1`/`-0` null pointer constant admits + lowers to NULL, multi-tier marker), itself post the **2026-06-15 semantic-expr-typer cycle** (the project's FIRST complete semantic-tier expression typer — `subtreeType` types every wrapper by its operator VERB, mirroring `cst_to_hir` via shared `type_rules.hpp` primitives, closing `D-SEMANTIC-SUBTREETYPE-TRANSPARENT-WRAPPERS` and auto-closing `D-FC6-SIZEOF-ARRAY-DIM-VALUE-FORM` [sizeof-of-expression in an array dim folds: count idiom + cast re-types]; FIXES a latent mixed-width-binary narrowing unsoundness; companion `isAssignable` Bool->arith gated so the verifier stays strict; +standalone array-of-FAM reject; +3 runnable corpus + death-test pins; 3-agent design panel + review SHIP-IT after fixing the C1 cast-at-Pass-1.5 silent miscompile pre-commit; see plan-14 anchor row). Prior **293/293** — post **2026-06-15 FC6-deferrals cycle** (closed BOTH FC6 named exclusions: **`sizeof` const-folds in const-expression contexts** — global initializers [`int g = sizeof(T)`, both forms] + array-dimension TYPE form [`int a[sizeof(T)]`], via a symmetric `resolveTypeSize`/`resolveSizeof` hook on BOTH const-eval engines sharing the `computeLayout` kernel [`D-FC6-SIZEOF-CONST-CONTEXT` ✅]; the "target-dependent type" fear was DISPROVEN — semantic re-runs per-(CU×target) into a private lattice; residual: array-dim VALUE form stays fail-loud → `D-FC6-SIZEOF-ARRAY-DIM-VALUE-FORM` [pass-ordering] — AND **positioned FAM-misuse diagnostics** [`S_FlexibleArray{NotLast,SoleMember,InAggregate}` 0xE01B-D at struct composition; `D-FC6-FAM-SEMANTIC-CONSTRAINTS` ✅]; +1 runnable corpus `sizeof_const_context` [global-init + array-dim + expr sizeof, exit 42] + HIR const-eval red-on-disable pin + 4 diagnostic goldens; plan-lock GO-WITH-FIXES [5, the M1 wrapper-peel hazard] + review SHIP-IT; see plan-23 §3.2). Prior **292/292** — post **2026-06-15 plan-23 FC6** (the struct/union/array **aggregate-layout substrate**: a generic `type_layout` engine reading per-`.target.json` `aggregateLayout` params into an `ArenaAttribute<TypeInterner, StructLayout>` side-table [`D-FF3-1` layout-half ✅] + **FULL expression-context `sizeof`** const-folding through the engine + the **FAM front-end** [`incompleteArray` lattice type + struct-field array declarators + config-driven `allowFlexibleArray` — standalone `int x[]` still fails loud]; 2 layout-sensitive runnable corpus [`sizeof_flexible_array`/`sizeof_struct_layout`] + 12 `test_type_layout` engine pins + 2 fail-loud diagnostic pins; named exclusion `D-FC6-SIZEOF-CONST-CONTEXT` [sizeof in array dims / global inits — fail-loud]; review SHIP-IT; see plan-23 §0.1 FC6). Prior **289/289** — post **2026-06-15 plan-23 FC5** (`goto`/labels [unstructured CF, ZERO MIR-substrate change] + truly-empty statement + the comma operator [two-level `assignmentExpr` gate] + the `entryFunctionNames` de-conflation [`D-LK10-ENTRY-MAIN-IMPLICIT-RETURN` ✅]; fail-loud `S_DuplicateLabel`/`S_UndefinedLabel`; +7 corpus examples [5 runnable optimized==baseline over the goto CFG + 2 positioned-diagnostic]; NEW named exclusions `D-CSUBSET-COMPUTED-GOTO` + `D-CSUBSET-LABEL-BEFORE-CASE`; plan-lock GO-WITH-FIXES [5 MUST-FIX] + review SHIP-IT [GCC-13-verified]; see plan-23 §0.1 FC5). Prior **282/282** post the **2026-06-13 OPT7 callee-Phi inline-splice** (`D-OPT7-MULTIBLOCK-SPLICE-PHI` ✅ — the OPT7 hard-stop opened [user-authorized] + closed via a supervised §B cycle: the multi-block inliner now CLONES a callee `Phi` via a DEFERRED-incoming-flush [placeholder `addPhi` in the RPO clone loop; flush after — VALUE via the shared `local` map, PRED via `calleeBlockMap`, fail-loud `abort` on a pred-not-in-map], the `inlineLegalityGate` callee-Phi refusal DROPPED with NO shape gate, fully source/target/linker-agnostic; the deferral is load-bearing for a LOOP-phi back-edge; 6 RED-on-disable MIR-tier pins [join+return-merge coexist, loop back-edge, arg-keyed corpus mirror, the verifier-CANNOT-catch transposition, pred-mis-remap-IS-caught, AND a verifier-blind value↔pred SOLE-guard test that runs THROUGH the real `runInlining`] + the `examples/c-subset/phi_inline` runtime witness exit 9; +1 corpus example; independent review CLEARS-THE-BAR; 282). Prior **281/281** (Win MSVC) — post the **2026-06-13 pre-FC5 deferral mini-arc** (`D-HIR-LOOP-BODY-ONLY-RETURN-DOUBLE-ATTACH` ✅ — fixed the implicit-return-0 double-attach crash on any non-terminating `main`; +2 corpus examples), the **2026-06-13 MIR unreachable-prune normalize** (mandatory `optimizeModule`-chokepoint reachability prune — `D-MIR-UNREACHABLE-PRUNE-NORMALIZE`; closes `D-HIR-SEALED-JOIN-UNREACHABLE-BLOCK`; +2 corpus examples; dss-state cf_if_else_chain/cf_switch FAIL->PASS), the **2026-06-13 D15 cycle-C release-pipeline composition closure** (RC-A SimplifyCfg post-fold reachability + RC-B MultiBlockInliner topological layout by construction + the new `I_LayoutUseBeforeDef` 0xA010 verifier rule; +2 corpus examples) and the **2026-06-12 D15 cycle-B marker re-derivation substrate** (`D-OPT4-1-NON-LINEAR-MARKER-MERGE` ✅ + D-OPT4-1 FULLY closed: NEW post-dominators [`computeMirPostDomTree`, shared slot-parameterized CHK core, virtual exit, 3-valued ipdom] + the canonical `deriveStructCfMarkers` 6-rule priority derivation [`src/mir/mir_struct_markers.{hpp,cpp}`; ExitBlock/LoopLatch/SwitchHead now dormant vocabulary] + EVERY producer re-derives post-finish [hir_to_mir, SimplifyCfg, inliner, mir_merge] + the VERIFIER independently recomputes and checks stored==derived per reachable block [count-parity + ExitBlock-terminator + LoopHeader-back-edge rules replaced; the old verifier comment's own "full structural nesting validity needs the dom tree" deferral delivered] + SimplifyCfg's incremental marker repair DELETED [absorb-chain walk, chainNonLinearMarker_, the merge gate's marker condition — both-non-Linear merges ADMITTED, closing pin `BothNonLinearMarkersNowMerge` red-on-disable] + BONUS: degenerate loops (`while(1){break;}`) flip from production REJECTION to compile+run-42 [corpus `degenerate_loop_break` ×4 targets; dss-state `cf_break` probe green, battery 58.7%] + a latent branch-fold stale-phi-incoming bug fixed via edge-level `acceptPhiIncoming` [pinned]; NEW anchor `D-HIR-SEALED-JOIN-UNREACHABLE-BLOCK` [✅ CLOSED 2026-06-13 by the MIR unreachable-prune normalize; FC5 unblocked]; 281; see §0.2 D15 — A shipped, B shipped, C shipped (D15 COMPLETE 2026-06-13)). Prior: **265/265** post the **2026-06-12 associativity SILENT-MISCOMPILE fix** (`D-PARSE-INFIX-ASSOCIATIVITY-STRUCTURAL` ✅ created+closed: same-precedence infix chains parsed RIGHT-associative in every Pratt grammar — `10-3+1`→6, `a/b/c`→wrong, the dss-state `ptr_swap_through` probe →34 — because the walker built right-recursive trees while its promised "downstream associativity consumer" never existed; FIX = wrap-in-place infix+ternary [the postfix arm's own `wrapLastChildExprFrame` mechanism] + RHS minPrec from the DECLARED table assoc [Right→prec, Left/None→prec+1] + trivia hold-then-place [which ALSO fixed the pre-existing spaced-postfix `f (40)`→H0001 bug] + the WalkerSnapshot rollback-replay machinery DELETED [caller-less; left chains now ITERATIVE — a 2000-op chain parses clean]; exact-shape pins c-subset+synthetic with red-on-disable demonstrated + semantic const-eval + MIR ConstFold pins; +2 corpus examples `ptr_swap_through`+`assoc_chains` exit-42 PE/WSL/qemu; dss-state MISCOMPILES EMPTY, `--strict` exit 0; tsql immune [flat path already left-folded]; see §0.2 D15 for the cycle sequencing this fix led). Prior: **263/263** post **FC4 ✅ COMPLETE (c0+c1+c2), the C declarator substrate + indirect calls** (c2 2026-06-12: indirect-call codegen end-to-end — semantic callee peel + HIR deref-of-fn decay fold + LIR Reg-callee materialization + the 3-layer cc-config-driven callee-reg exclusion [R2 regalloc exclusion red-on-disable + B2 spill-scratch filter + B1 `L_IndirectCalleeClobberedByArgSetup` 0xB00B backstop] + same-mnemonic x86 `FF /2`/arm64 `BLR` call variants + 3 runtime corpus examples [`fnptr_call` replacing the deleted `fnptr_call_error`, the un-devirtualizable `fnptr_callback`, `fnptr_regalloc_pressure` — all exit-42 PE/WSL/qemu], 263; closes `D-CSUBSET-FNPTR-INDIRECT-CALL` + plan-12 `D-ML7-2.4` [with its sketch double-corrected]; `S_IndirectCallNotSupported` 0xE015 retired; c0 `4ba1884` 2026-06-11 declared-order probing + prefix-strip helper, 249; c1 the C11 specifier/declarator split + 10 corpus examples, 261 — see plan-23 FC4 row + §0.2 D14; the audit-residue sweep before it: 248). Prior: 246/246 post the **regalloc-anchor closure cycle** (2026-06-11, user-directed pre-FC4: `D-LIR-REGALLOC-PRESSURED-IMPLICIT-CLOBBER-PIN` ✅ [the pressured DIV pin — nLive 10..16 sweep, every covering vreg avoids rax/rdx; red-on-disable at EVERY sweep point; the reviewer proved the covered-position and result-def arms independent: div pin red while the c1 pin stays green] + `D-OPT-REGALLOC-EXCLUSION-BUFFER` ✅ [excludedStorage fixed-8 array → a hoisted growable scratch, allocation-free after high-water; all 3 regallocFatal overflow arms removed SOUNDLY — push_back can never truncate set-membership; >8-union pins via 14-reg-clobber schema mutations, both arms]; division/ gains the pressured() corpus arm [disabled-mode witness = a deterministic 0xC0000005 null-deref: the alloca-ADDRESS lands on RDX and CDQ zeroes it]; ctest count unchanged — 3 new gtests ride lir/test_lir_regalloc 24→27). Prior headline — post **V2-4.X FC3.5 sweep c3 → THE SWEEP COMPLETE** (2026-06-11: `D-LIR-MOD-MSUB-FUSION` ✅ [Ra slot full-lockstep + arm64 msub X+W + rule-3 mnemonic-presence preference, mul+sub fallback pinned], `D-LIR-GLOBALADDR-LOAD-RIPREL-FOLD` ✅ [mir_to_lir local fold; Phi-aware per-function use census; capability-driven — fires only for declared SymbolRef variants, FC2's byte-pinned riprel movsd FINALLY pipeline-live; multi-use keeps the pair, pinned], `D-CSUBSET-CAST-ARRAY-DECAY` ✅ [C 6.3.2.1p3 re-kind + the synthetic decay Cast], `D-CSUBSET-CAST-VOID-DISCARD` ✅ [void targets admitted, no Cast node, side-effects preserved], `D-CSUBSET-COMPOUND-LITERAL-TYPEDEF` ✅ [castTypeRef + binder triage; NEW semantics.compoundLiterals stamping — even (int){42} was H0009 before; struct-to-LIR stays gated honestly], `D-LK10-ENTRY-ARM64-WIDE-IMMEDIATE` int32-arm ✅ [movk_lsl16/32/48 + the magnitude ladder; 48/64-bit stays at the LiteralPool dead end both targets, row honest]; +2 examples cast_decay_void_discard [puts("hi")+42!] + wide_immediate, witnessed PE/WSL/qemu; review SHIP-IT zero findings — the msub W-form pin VERIFIED against the ARM ARM incl. the o0 bit). Prior 244 post **V2-4.X FC3.5 sweep c2** (2026-06-11: FLOAT completeness — F32 codegen on the width axis [`D-CSUBSET-F32-CODEGEN` ✅: ADDSS/CVTTSS2SI/MOVSS/CVTSS2SD+CVTSD2SS + fdiv's FIRST encodings DIVSD/DIVSS + arm64 S-forms/FCVT/FDIV; floatLiteralTyping 1.5f→F32 both tiers; F32 4-byte rodata; conversions key on SOURCE width]; FCmp LIR lowering [NEW `fcmp` UCOMISD/UCOMISS/quiet-FCMP; floatCmpPlan Olt/Ole swap-canonicalize; 7 OPTIONAL float condCodes with absence-triggered universal setcc-composition — x86 Oeq=sete∧setnp/One=setne-alone/Une=setne∨setp, arm64 Oeq=EQ/Une=NE/One-composed, the mirror asymmetry; 56-cell exhaustive truth-table pin vs an independent flag simulator; NEW x86 and/or encodings]; the NaN adjudication [`D-COND-FLOAT-NAN-TRUTHINESS-FCMP` ✅: float Ne→FCmpUne per C 6.5.9 — `if(NaN)` TRUE]; +test_fcmp_lowering +3 examples float_compare/f32_arithmetic/float_nan witnessed PE/WSL/qemu ×9). Prior 240 post **V2-4.X FC3.5 sweep c1** (2026-06-11: shifts END-TO-END [x86 D3-CL/C1-imm8 both widths via the FC1 `count` inputRole contract + NEW Imm8 slot; arm64 native LSLV/LSRV/ASRV X+W — ONE capability-driven lowerShift] + the `&&`/`||`-condition I_StructCfMismatch fix [rhs block IfThen — the lowering was the bug] + the for-clause AssignStmt routing fix + ZExt-32→64 [`D-CSUBSET-ZEXT-32-TO-64` ✅] + **the GENERIC regalloc result-def implicit-exclusion rule — the reviewer's own pressure probe demonstrated a CRITICAL latent RCX-count-clobber miscompile pre-commit [legalize pre-copy conduit]; class-closed via the declared implicitRegisters, pressured pin + binary-tier 162-vs-42 witness** + the Mach-O witness flips [run 27341390786: macos leg GREEN on 966c977 — float_cast + FC3's nine + bare_int_condition ALL executed on Apple Silicon]; +3 examples logical_condition/shift_ops/zext_u32_to_u64 witnessed PE/WSL/qemu). Prior 237 post the **C-truthiness fix** (2026-06-11, `966c977`: bare conditions lower to `Ne(cond, typed-zero)`, never Cast-to-Bool/Trunc; +`bare_int_condition` example). Prior 236 post **V2-4.X FC3 c2** (2026-06-10, cycle 6: the 32-bit ALU width axis — LirInst width carrier + guard.width variant discrimination [both walkers, one matcher] + x86 no-REX.W forms + trunc's first encoding + arm64 W-forms/SXTW/W-mov; gate narrowed [U32 out, I32 true-32-bit]; +test_asm_width_axis +5 examples incl. **datamodel_runtime_width: byte-identical source exits 42 on Windows/LLP64 vs 23 on linux/LP64 — the runtime dataModel divergence**; `D-CSUBSET-32BIT-ALU-FORMS` U32/I32 arc closed, residue narrowed [8/16/128-bit + shifts + `D-CSUBSET-ZEXT-32-TO-64`]). Prior 230 post **V2-4.X FC3 c1** (2026-06-10, cycle 5: the complete width/signedness front-end — typeSpecifiers multisets, the dataModel FORMAT-schema axis [LP64/LLP64, closes `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH`], the C 6.4.4.1 literal ladder, the UAC engine, signatureByDataModel descriptors, the `D-CSUBSET-32BIT-ALU-FORMS` width guard; +test_fc3_width_semantics +4 examples incl. unsigned_division closing `D-CSUBSET-UDIV-RUNTIME-HIGH-BIT-PIN` at U64). Prior 225 post **V2-4.X FC2** (2026-06-10, cycle 3: FULL C-style casts `(type)expr` incl. typedef-name resolution [binder sketch + include-oracle reparse — the §B "most complete" decision] + the x86 SSE backend [`mandatoryPrefix`, ADDSD/CVTTSD2SI/MOVSD, `registerClassOps` per-class table, F64 rodata materialization] + the **FIRST float runtime witnesses** [`float_cast` exit 16 RAN on Windows PE + linux ELF] — +4 new test binaries [parser_cast_expr, type_name_oracle, asm_x86_sse, asm_cast_integration] +3 examples [float_cast, typedef_cast, include_typedef_cast]; the corpus surfaced + in-cycle-closed the ms_x64 callee-saved-XMM `movsd_store` gap; 5 anchors pinned [`D-ARM64-FLOAT-SUBSTRATE` + 3 cast residuals + the riprel-fold peephole]). Prior 218 post **V2-4.X FC1 cycle 2** (2026-06-10: C23 hex-floats + decimal `1.`/`.5` edge forms + decode de-hardcoding, +1 `test_number_decode`; FC1 COMPLETE). Prior 217 post the **V2-4.X FC1 cycle 1** (2026-06-10: binary `%` modulo end-to-end x86_64+arm64, +2 examples `modulo`/`float_mod_error`; prior 215 after V2-4 Part C's `undeclared_var_error` + the recovery corpus). Prior headline — post the **v0.0.2 artifactProfile AP3** cycle (2026-06-09 — the profile↔FORMAT compatibility gate: each object format config-declares `artifactProfiles[]` [the 4 runnable exec formats → `["cli"]`, all else empty/fail-closed], `compileProject` enforces `profile ∈ format.served` after the language gate → new `D_ArtifactProfileFormatMismatch=0xD011` [a `cli` project on a dll format, or `lib` on an exec format, rejected at config-load]; the shared registered-vocab extracted to `core/types/artifact_profile.hpp` → closes `D-AP2-PROFILE-REGISTRY-SPLIT`; codegen-threading stays trigger-gated [`D-AP2-COMPILATION-CONTEXT` — no shipped profile drives a non-format-redundant codegen difference]. §B-decided (AP3-as-codegen-plumbing would be a dead knob). **ctest STAYS 213** — the new gtest cases ride the EXISTING `link/test_object_format_schema` + `program/test_project_config` binaries; both RED-on-disable levers [vocab + gate] proven; code-review SHIP-IT + silent-failure VERIFIED-FAIL-LOUD; agnosticism clean). Prior the **v0.0.2 artifactProfile AP2** cycle (2026-06-09; +1 = `program/test_project_config` — the `.dss-project.json` loader + the driver profile-enforcement gate `D_ArtifactProfileNotSupported` [fail-CLOSED empty-set] + the shared `routesToMultiUnit` source-count routing; the `compileProject` `D_PlanNotLanded` stub is replaced by a real load→grammar→enforce→delegate path; plan-lock resolved 2 forks against the architect on the bar [empty-set REJECT not accept; NO dead `CompileOptions::artifactProfile` knob — deferred to AP3]). Prior the **v0.0.2 `#include puts("hello")` 3-OS STRING-FFI CAPSTONE** cycle (2026-06-09): a `#include <stdio.h>` + `puts("hello")` c-subset program now PRINTS `"hello\n"` to stdout + exits 42, RUNNABLE on **linux-x86_64 + linux-arm64 + macos-arm64** — string-FFI-to-stdout PARITY with the Windows `hello_puts`/`shipped_include_puts`. **THREE coupled changes, gated by TWO user §B decisions (recorded in §0.2 D12 + D13 so they are not re-litigated):** (1) **Model 3 shipped-lib FFI** (§B: chose "neutral descriptor + per-format library map" over Option-A-one-line-hack + Model-2-per-platform-dir-resolution) — the 15 per-platform `shippedLibs/<os-arch>/<h>.json` descriptors COLLAPSED into 5 NEUTRAL `shippedLibs/<h>.json`, each with platform-neutral signatures + a per-format `library` MAP `{pe,elf,macho}` carried target-AGNOSTICALLY through the front-end (descriptor→`ShippedExternSymbol`→`HirExternRecord`) + resolved to ONE string per-target at `compile_pipeline.cpp` (via `objectFormatKindName`; `ingest.cpp` single-string; missing key → `externLibraryByFormat` fallback; unknown key → fail loud); `shippedLibDirs` → the neutral root `["shippedLibs"]` → **DISSOLVES `D-FFI-SHIPPED-LIB-PLATFORM-SELECT` ✅**. (2) **ELF dynamic-FFI `.rodata`** (`elf.cpp` `encodeElfExecDynamic` now emits `.rodata` via the shared `buildExecRodata`/`addDataSymbolVas` — was static-`encodeElfExec`-only, so a string-literal pointer arg reaches a DYNAMIC extern; byte-identical when no rodata) → **closes the dynamic arm of `D-LK1-ELF-EXEC-DATA-SECTIONS`**. (3) **Exit spine → libc `exit(3)`** (§B: chose "always exit via libc `exit(3)` for hosted formats" over surgical-iff-libc + fflush-before-rawexit) — ELF `processExit` flipped raw `exit_group` syscall → by-name-import `exit`/`libc.so.6` (libc flushes stdio at exit; the raw-syscall arm is config-retained for a future freestanding target); Mach-O kept `_exit` (CORRECTION: on Darwin `_exit` IS the flushing C `exit(3)`; the comment-only fix — it was flushing all along); PE kept `ExitProcess` (flushes via msvcrt). Trampoline + `linker.cpp` byte-UNCHANGED (config-only; AGNOSTIC). **§A.5 RUNTIME HONESTY**: `shipped_include_puts` → stdout `"hello\n"` + exit 42 **PROVEN LOCALLY on linux-x86_64 (WSL native) AND linux-arm64 (qemu-aarch64** — `strace` shows `write(1,…)` THEN `exit_group(42)` = the flush precedes exit); `global_int` (now dynamic via libc exit) → exit 42 both arches; the native ubuntu CI legs re-confirm post-push. **macos-arm64 is ✅ RUNTIME-PROVEN 2026-06-09 (run 27212153591, `macos-clang-release` ctest #206 `shipped_include_puts` Passed → "hello\n"+exit 42 on real Apple Silicon)** — the FIRST macOS execution of `puts("hello")`→stdout (the Mach-O `_exit`=flushing-C-`exit(3)` + Model-3 libSystem resolution confirmed at runtime; the previously RUNTIME-PENDING witness is now executed). **ctest STAYS 212/212** (the new pins ride EXISTING test binaries — no new ctest entry); build clean; anchor-guard OK (194); agnosticism clean; independent review = ship-it (no CRIT/HIGH/MED). **DSS now PRINTS to stdout via shipped-lib FFI + exits cleanly on 3 OSes × 2 arches** (Win-PE/Linux-ELF/macOS-Mach-O × x86_64/arm64 — linux + macos-arm64 added this cycle; macos-arm64 runtime ✅ CONFIRMED on Apple Silicon, run 27212153591 #206). **This achieves FULL 3-OS × 2-arch stdout-FFI parity — every target runs `#include puts("hello")` → "hello\n"+exit.** Prior tail — post the **v0.0.2 macOS Mach-O `__TEXT,__const` DATA SECTION** cycle (`D-LK1-MACHO-EXEC-DATA-SECTIONS` / `D-LK3-RODATA` exec arm, 2026-06-09 — the parallel of the just-landed ELF `.rodata` cycle for macOS): the Mach-O exec writer (`src/link/format/macho.cpp` `encodeExecDynamic`) now emits a loadable read-only **`__TEXT,__const`** section from `module.dataItems`, so a c-subset global (`int answer=42;`) REACHES a runnable arm64-darwin binary. **§A.5 RUNTIME-PROVEN (run 27192415923 `macos-clang-release` #184 `global_int` → exit 42 on Apple Silicon)**: the produced Mach-O's `__TEXT,__const` holds `2a 00 00 00` (byte-pinned by the `MachOArm64Exit.MachoArm64Const*` cases in `tests/link/test_macho_arm64_exit.cpp`) AND the `global_int` `arm64:macho64-arm64-darwin-exec` corpus arm (`runOn: ["darwin"]`) RAN on real Apple Silicon → ctest **#184 Passed** → exit 42, which means the data global (answer=42) was READ at runtime AND the high-VA ADD_ABS_LO12_NC reloc resolved (else exit ≠ 42). This **closes `D-LK1-MACHO-EXEC-DATA-SECTIONS` + `D-LK6-AARCH64-ADDABSLO12-HIGH-VA` ✅** (CI run **27192415923** FULLY GREEN on all 5 legs — the 3 linux legs + windows-msvc confirm the reloc relaxation + the elf.cpp refactor are behavior-preserving no-ops for ELF/PE). **DSS now READS a data global at runtime on 3 OSes × 2 arches** (Win-PE x86_64, Linux-ELF x86_64+arm64, macOS-Mach-O arm64). **Three sub-pieces**: (1) a shared header-only helper `src/link/format/exec_data_section.hpp` (`buildExecRodata` + `addDataSymbolVas` — carrying the H1 max-item-align + M1 anonymous-skip + non-Rodata/data-item-reloc fail-loud) was hoisted; `elf.cpp` was refactored to USE it (behavior-preserving — ELF byte-pins stay green) and `macho.cpp` consumes it (new) — this FIRES + acts on `D-LK4-RODATA-WALKER-RELOC-EXTENSION-HOIST` (elf + macho = 2 of 3 walkers converted; **PE deferred** as the residual 3rd consumer → `D-LK-RODATA-HELPER-PE-PARITY`, since PE is the Windows-critical-path runtime whose latent H1/M1 are masked by page-aligned `.rdata`). (2) `macho.cpp` emits `__const` inside `__TEXT` after `__stubs`, gated on `hasConst` (no-dataItems output byte-identical to before), citing the ELF precedent `D-LK1-ELF-EXEC-DATA-SECTIONS` as the mirror. (3) the PREREQUISITE that made the runnable arm64-darwin data path possible — the shared reloc kernel (`exec_reloc_apply.hpp`) `Aarch64AddAbsLo12` case DROPPED its ELF-centric `S+A ≤ UINT32_MAX` upper bound (kept `S+A ≥ 0`) because `ADD_ABS_LO12_NC` is a magnitude-INDEPENDENT low-12 extraction + the paired ADRP range-checks the magnitude; this was REQUIRED to let `global_int` compile for arm64-darwin (Mach-O PIE VAs sit above the 4 GiB `__PAGEZERO`), is a NO-OP for ELF/PE (their VAs < UINT32_MAX), and is tracked as **`D-LK6-AARCH64-ADDABSLO12-HIGH-VA`** (byte-pinned `Aarch64AddAbsLo12.ResolvesSplusAAboveUint32Max` + `MachOArm64Exit.MachoArm64ConstAdrpAddLeaHighVaResolvesLowTwelve`, RED-on-disable demonstrated). **AGNOSTIC**: the walker code is format-blind (driven by the JSON `__const` row + `supportedDataSections` on `macho64-arm64-darwin-exec.format.json`; the `linker.cpp` gate is CONSULTED, never branched); `linker.cpp` untouched. **`macho64-x86_64-darwin-exec` was deliberately NOT given a data section** (no macOS-x86_64 CI leg → would ship an untested §A.5 form; `D-LK1-MACHO-X8664-DARWIN-DATA-SECTION`). **ctest STAYS 212/212** (the new pins live inside the EXISTING `test_macho_arm64_exit` + `test_aarch64_reloc_formulas` binaries — no new ctest entry). Independent code-review: ready to land (guard relaxation independently re-verified correct + red-on-disable reproduced); plan-lock GO-WITH-FIXES, all MUST-FIX folded (corpus `runOn` is `"darwin"` not `"macos"`; the layout-shift chain complete; the helper supersedes the single-function anchor shape). **⚠️ CI INFRA HEADS-UP — `D-CI-WINDOWS-MSVC-BUILD-TIME-CEILING` (registry, OPEN, NOT a code failure):** the `windows-msvc-release` CI **Build** step is at the **45-min timeout ceiling** — the prior cycle's Build was 42m42s, and run 27192415923's FIRST `windows-msvc-release` attempt TIMED OUT the 45-min Build by a thin margin, then PASSED on a re-run at **45m31s**. A codebase-growth issue (compile time scaling with the tree), NOT a regression (local Win-MSVC 212/212; the other 4 legs green). Recommended fixes flagged for a user/infra decision: **raise the Build timeout 45 → 60 min** OR **add Windows PCH / `ccache`/`sccache` build caching**; the `/dss-cycle` loop will surface it. Prior tail — post the **v0.0.2 ELF EXEC DATA SECTIONS (rodata)** cycle (`D-LK1-ELF-EXEC-DATA-SECTIONS`, 2026-06-09): the ELF ET_EXEC writer now emits a loadable **`.rodata`** section from `module.dataItems`, so a c-subset global (`int answer=42;`) REACHES the binary + is READ at runtime — the FIRST data-section-bearing runnable ELF. **§A.5 RUNTIME-PROVEN LOCALLY**: the `global_int` corpus → **exit 42 on BOTH x86_64-ELF (WSL native) AND arm64-ELF (qemu-aarch64)** (strace confirms `exit_group(42)`; the global's 42 is read from `.rodata` via `lea [rip+disp]`→load); the Linux + native-`ubuntu-24.04-arm` CI legs re-confirm post-push. **Plan-lock-blessed (rodata-only scope — it corrected 5 premise errors in my design**: the producer `lowerMirGlobalsToDataItems` emits ONLY `Rodata` [so `.data`/3-PT_LOAD/flags-grouping were dropped as untested gold-plating], the shared reloc kernel is functions-only, the format-JSON + linker-marker change was mandatory, section R/W/X must come from config). **AGNOSTIC**: all in `elf.cpp` (the per-format realization tier) + the 2 `elf64-*-linux-exec.format.json` (`.rodata` SHF_ALLOC row + `supportedDataSections:["rodata"]` — the `linker.cpp` gate is CONSULTED, never branched) — x86_64-ELF + arm64-ELF share ONE code path (arch via config), zero `if(arch)`; `.rodata` folds into the single R+X `.text` PT_LOAD via a config-derived `shFlagsToPFlags` (closing the pre-existing hardcoded `p_flags=5`), guarded by a file/VA-congruence FAIL-LOUD. Data-symbol VAs join the static `symbolVa` so the code→global reloc resolves through the EXISTING `applyExecRelocations` (kernel unchanged). Fail-loud: non-Rodata / `.o` / dataItem-relocs deferred (`D-LK1-ELF-RODATA-DATAITEM-RELOC`). This ALSO runtime-closes `D-LK4-RODATA-PRODUCER-ARM64-GLOBALADDR` (arm64 `GlobalAddr→lea` proven) + makes `D-AS4-3`'s arm64 `lea` runtime-observable. 7 host-independent byte-pins (`ElfExecWriter.Rodata*` — section bytes/VA/PT_LOAD-extent + PT_LOAD-extension RED-on-disable + a no-dataItems byte-identity control) + the marker/linker-gate test flips. Delegated impl + independent code-review; 212/212 ctest (new pins inside the existing `test_elf_exec_writer` binary). Prior tail — post the **v0.0.2 macOS-ARM64 EBADMACHO FIX** (`D-LK10-ENTRY-MACHO-EXIT` runtime-RED repair, 2026-06-09): the prior cycle's arm64-darwin exec byte-pinned green but the `macos-latest` leg's `macho_arm64_exit` corpus FAILED `posix_spawn rc=88` = **EBADMACHO** — the §A.5 catch WORKING (byte-pins + independent review + audit all clean; only the real macOS LOAD exposed it). Root cause = `macho.cpp` hardcoded `kPageSize=0x1000` (4 KiB) for LC_SEGMENT_64 alignment, but **Apple Silicon REQUIRES 16 KiB-aligned segments**. Fixed AGNOSTICALLY: a config-driven **`image.segmentPageSize`** (16384 arm64-darwin / 4096 default → every pre-arm64 Mach-O byte-identical) read at BOTH writer sites + `__text.virtualAddress` 0x100001000→0x100004000 (16K mmap congruence). The independent plan-lock flagged a HIGH-confidence SECOND blocker — the writer emitted NO platform load command — so this cycle ALSO emits **`LC_BUILD_VERSION`** (config `image.buildVersion {macos, minOs 11.0}` + a shared `appendBuildVersionCommand` 24-byte emit on the dynamic path), since dyld on macOS 11+/Apple Silicon rejects a platform-less main executable AND no DSS Mach-O had EVER loaded on macOS (zero prior evidence a platform-less binary loads → land maximally-correct, don't let CI be the first observation). Also DECOUPLED `codeSignature.pageSize` 16384→4096 (the conventional code-hash page Apple uses on both arches — distinct from the VM segment page; the prior cycle had conflated the two). validate() gained **power-of-two** + **text-VA-congruence** (rejects the exact 0x100001000-under-16K bug at schema-load) + .o-block-rejection guards; the static exec path fails loud on `buildVersion` (anchored `D-LK10-ENTRY-MACHO-STATIC-BUILD-VERSION`). **RED-on-disable DEMONSTRATED** (`Arm64SegmentsAre16KAligned` → revert segmentPageSize to 4096 → __DATA_CONST.vmaddr 0x100005000 / __LINKEDIT misaligned, fails) + `Arm64ExecEmitsBuildVersionMacOs` byte-pin (cmd 0x32, cmdsize 24, PLATFORM_MACOS, minos 0x000B0000) + 2 validate fail-loud pins + `X86DarwinExecDefaultsTo4KSegmentPageSize` knob-that-lies guard. **Independent plan-lock GO-WITH-FIXES (LC_BUILD_VERSION = MUST-FIX #1, all folded) + code-review NO CRITICAL/HIGH/MEDIUM.** AGNOSTIC (page size + platform read as DATA, zero `if(arch)`); 212/212 ctest (new pins are gtest cases in the existing binary), anchor-guard OK (187). **`D-LK10-ENTRY-MACHO-EXIT` ✅ CLOSED 2026-06-09 — RUNTIME-CONFIRMED on Apple Silicon** (CI run 27182845048, the `macos-latest` arm64 leg GREEN: the `macho_arm64_exit` corpus ctest #194 — compile→spawn→assert exit 42 — Passed, where the prior cycle FAILED with rc=88/EBADMACHO → the binary now genuinely loads + runs on real Apple Silicon). **This completes the macOS-ARM64 runnable-exec arc → DSS now produces RUNNING binaries on 3 OSes × 2 arches (Win-PE/Linux-ELF/macOS-Mach-O × x86_64/arm64), runtime-verified on every native CI leg.** (windows-msvc leg cancelled on an infra Build-timeout — redundant with local Win-MSVC 212/212; the other 4 test legs incl. both arm64 + ASan/UBSan green.) Prior tail — post the **v0.0.2 macOS-ARM64 RUNNABLE-EXEC backend** (§B-2-directed full libSystem `_exit` mechanism): the `macho64-arm64-darwin-exec` target now PRODUCES a signed arm64-darwin executable. Landed (plan-lock GO + the agnosticism crux resolved by the ELF `emitPltStub` precedent — a localized per-cputype switch in the format writer is the sanctioned §D realization tier): the arm64 Mach-O stub substrate `machoStubSizeFor`/`emitMachoStub`/`emitArm64MachoStub` (ADRP x16→LDR→BR, mirroring `emitArm64PltStub`; x86 stub BYTE-IDENTICAL) + a `macho64-arm64-darwin-exec.format.json` (CPU_TYPE_ARM64 + processExit=ByNameImport `_exit` + the new `apple_arm64`/`CcApple` cc [**closes `D-FF3-5`**, trigger fired] + `direct-plt` + ad-hoc codeSignature) + the `macho_arm64_exit` corpus + 6 host-independent byte-pins (RED-on-disable DEMONSTRATED: BR x16→x0 fails the stub pin). **It also CAUGHT + FIXED a confirmed LATENT MISCOMPILE**: Mach-O was mislabeled `externCallDispatch: indirect-slot` while physically built `direct-plt` (`symbolVa→stub`), so a real FFI/exit call emitted `FF 15` → deref the stub CODE → SIGSEGV (the Mach-O analogue of the ELF `D-FFI-EXTERN-CALL-DISPATCH` bug, masked by zero macOS runtime). Fixed: flipped BOTH darwin formats to `direct-plt` + a dispatch↔symbolVa coherence FAIL-LOUD guard + the entry trampoline's exit-call dispatch made FORMAT-DRIVEN via the SINGLE shared `externCallUsesIndirectShape` rule (reused by `mir_to_lir` + the trampoline — no second copy, no `if(arch)`). **AGNOSTIC**: zero new identity branch in shared substrate (`linker.cpp` untouched); the per-cputype stub switch is the sanctioned format-writer tier. **HONEST CLOSURE — `D-LK10-ENTRY-MACHO-EXIT` is 🟢 IMPL DONE, RUNTIME-PENDING the `macos-latest` leg: the corpus RUNS only on the Apple-Silicon CI leg post-push (RUN + AMFI-accept the signature + exit 42); confirm green at next Step 0, THEN mark CLOSED — NO "runs on Apple Silicon" claim this cycle.** Plan-lock GO + independent review (built under GCC-13.3, no CRITICAL/HIGH) + independent self-audit VERIFIED-CLEAN (no over-claim; the latent-bug flip regression-proven by `ShippedX86DarwinExecIsDirectPlt`) + folded the audit NITs (the x86-flip format pin; the precise `D-FF3-APPLE-ARM64-ABI-DIVERGENCE` anchor replacing a mis-cite). +2 ctest. 2026-06-08. Prior tail — post the **v0.0.2 ad-hoc Mach-O CODE-SIGNING cycle** (the macOS-ARM64 prerequisite; §B-directed "add Apple Silicon"): Apple Silicon's kernel SIGKILLs unsigned arm64 binaries, so a runnable macOS-ARM64 exec needs a code signature FIRST. Landed the ad-hoc-signature BUILDER, 2 increments: (1) a self-contained FIPS-180-4 **SHA-256** in `src/core/crypto` (`dss::crypto`, FIPS-vector-pinned with literal digests — the independent oracle; commit 056b21e, CI-green all 5 legs incl. clang-ASan/UBSan); (2) the **fill** — a `codeSignature` config block on `MachOImage` (closed-enum kind/hashAlgorithm + pageSize∈[4096,65536] + non-empty identifier, all fail-loud) consumed by a `src/link/format/macho_codesign.{hpp,cpp}` CD/SuperBlob builder (ad-hoc `CS_CodeDirectory` v0x20400 + `CS_SuperBlob`, all big-endian, SHA-256 page hashes) wired into the `macho.cpp` per-format walker (replaces the LK7 zero-fill placeholder). **AGNOSTIC** — all in the macho.cpp walker + macho_codesign.cpp, driven by the JSON block; ZERO `if(format==macho)` in shared substrate (`linker.cpp` untouched). **Verification** (pure-substrate per §A.5 — the signature BYTES are the deliverable): byte-pins of the CD/SuperBlob against Apple `cs_blobs.h` literals + page hashes recomputed via the FIPS-proven `sha256` + byte-matched (non-circular) + Condition-2 `blob.size()==reservation==datasize` + non-default-pageSize-through-the-wire; 2 RED-on-disable levers MANUALLY demonstrated (BE→LE magic; zeroed hash). **HONEST CLOSURE — `D-LK7-ADHOC-CODESIGN-MACHO` is 🟡 BUILDER BYTE-PROVEN, NOT runtime-closed: whether macOS/AMFI ACCEPTS + runs a signed binary is the NEXT cycle (no `macho64-arm64-darwin-exec`, no run, `codesign --verify` deferred) — NO "macOS-verified"/"runs on Apple Silicon" claim.** Plan-lock GO (3 conditions bound: SHA-256-home anchor `D-CRYPTO-SHA256-HOME-CONVERGENCE`, derived size, FIPS-oracle red-on-disable) + independent self-audit VERIFIED-CLEAN (confirmed pure-substrate, not the FFI-ISEL over-claim trap) + folded 2 NITs (the pageSize floor closing a u32-overflow, `D-LK7-CODESIGN-PAGESIZE-OVERFLOW-HARDENING`; the non-default-pageSize wire test). +2 ctest (test_sha256 + test_macho_codesign binaries). 2026-06-08. Prior tail — post the **v0.0.2 ARM64 CONTROL-FLOW cycle** (the ARM64-FFI arc's cycle 5): a c-subset function with `if`/`while`/loops now compiles + RUNS on ARM64. Landed (all agnostic, config-driven via `arm64.target.json` + the generic fixed32 encoder): `cmp` (SUBS XZR — flags), `jcc` (B.cond imm19 + B imm26, a target-blind 2-WORD MACRO via `fixedWords`/`wordIndex`), `jmp` (B imm26), `setcc` (CSET), `zext` (ORR), `alloca`→frame-relative `lea` (ADD Xd,sp,#imm12); the now-real **`D-AS3-BLOCK-REL-IMM19/26`** block-relative branch resolver (`src/asm/asm.cpp`: `disp=(target−patch)>>2`, NO +4 bias, bit-field RMW, alignment + signed-range fail-loud); the **`D-AS3-COND-CODE-ARM64`** condCodeEncoding table + the GENERIC `condBitPos`/`condInvert` template knobs (x86 defaults → byte-identical). **Closures**: `D-LK10-ENTRY-ARM64-CONTROL-FLOW` + `D-AS3-BLOCK-REL-IMM19/26` + `D-AS3-COND-CODE-ARM64`. **Proof**: `examples/c-subset/arm64_control_flow/` (`while (n<42) n=n+1; return n;` → exit 42, x86_64+arm64 cross-target differential, +1 ctest) PROVEN LOCALLY under qemu-aarch64 (exit 42 ×3, while-loop + fused-if), + host-independent byte-pins (`Arm64ControlFlowSkeletonEncodesCmpBcondBWithResolvedOffsets` 6-word [incl. the negative back-edge] + `CsetEncodesInvertedCondAtBits12` + `ZextEncodesOrrW` + `FrameRelativeLeaEncodesAddImm12` + `FrameLeaOffsetWiderThanImm12FailsLoud`); RED-on-disable DEMONSTRATED for the CSET cond-invert, the branch `>>2` scaling, AND the Imm12 range guard. §A.5 cross-target runtime closure SATISFIED: the native `ubuntu-24.04-arm` CI leg (real arm64 hardware) CONFIRMED green — run 27170873148, all 8 legs success, 2026-06-08. Built on the **v0.0.2 ARM64 non-leaf LINK-REGISTER (x30) frame fix** (the ARM64-FFI arc's cycle 4; user "we must dig this until find the root cause … we NEED arm64 to really work"): the RUNTIME root cause of the arm64-FFI SIGSEGV that the PRIOR cycle's `D-LK10-ENTRY-ARM64-FFI-ISEL` closure OVER-CLAIMED away — it asserted the corpus "→ exit 42" but the binary actually **SIGSEGV'd (CI exit 139)**; that cycle's own self-audit had flagged this exact runtime risk. A non-leaf ARM64 function did NOT spill the link register x30 (AAPCS64 carries the return address in x30, not pushed on the stack like x86_64's `call`), so `main`'s epilogue `ret` returned to the x30 clobbered by `bl abs` → an infinite self-loop whose repeated epilogue `add sp` overran the stack into a guard page → SIGSEGV. **The FFI dispatch itself was always correct** (`abs(7-49)`→42 in x0; the PLT stub + GLOB_DAT reloc worked). Fix (`lir_callconv.cpp::materializeOneFunc`, agnostic/config-driven): a non-leaf function whose CC declares a `linkRegister` folds it into `savedRegs` → spilled in the prologue + reloaded before `ret`; ZERO `if(arch==…)` — x86_64 (no `linkRegister`) takes the no-op path; the schema's long-declared-but-unconsumed `linkRegister` field is finally read. `extern_call_elf` arm64 NOW genuinely runs to exit 42 (verified under qemu + the native arm64 CI leg). Pins: `LirCallconv.NonLeafAarch64FunctionSpillsLinkRegister` (non-leaf saves x30 / leaf does NOT; RED-on-disable DEMONSTRATED — disable the fold → the pin fails on `savedRegsContain` false) + `LirCallconv.NonLeafX8664FunctionHasNoLinkRegisterToSpill` (agnosticism control) + the corpus arm (red-on-disable by construction — was SIGSEGV → CI red) **+ a same-day HOST-INDEPENDENT byte-pin `NonLeafAarch64FramePrologueEpilogueByteExact`** (drives the real production tail legalize→callconv→assemble; byte-exact non-leaf prologue+epilogue incl. `stur x30,[sp,#16]`/`ldur x30,[sp,#16]` vs hand-verified ARM-ARM literals + a leaf-control; RED-on-disable demonstrated — proves the spill ENCODES right on EVERY leg incl. Windows MSVC, not just by execution on the one native-arm64 leg; the 3rd complementary layer, none replaces another). NEW `D-LK10-ENTRY-ARM64-NONLEAF-LINK-REGISTER` ✅ + `D-OUTPUT-EXEC-BIT` (deferred — the `--output` writer emits mode 0644; the test runner `chmod +x`es before spawn so non-blocking; surfaced because the missing exec bit MASKED this SIGSEGV under a manual `qemu` invocation). ctest unchanged (new pins are gtest cases in an existing binary); 2026-06-08. Prior tail — post the **v0.0.2 runnable ARM64-ELF FFI "proof"** (the ARM64-FFI arc's cycle 3; user "please continue on the cycles") **[⚠️ its "RUNS … → exit 42" RUNTIME claim was the OVER-CLAIM corrected above; the COMPILE half — closing `D-LK10-ENTRY-ARM64-FFI-ISEL` — was real]**: closes **`D-LK10-ENTRY-ARM64-FFI-ISEL`** — the END-TO-END ARM64-ELF FFI call-WITH-RESULT path (compile path). **EMPIRICALLY re-diagnosed the gap**: it was the `lea`/GlobalAddr lowering (NOT the "reg-move-class op" the format-dispatch cycle first guessed) — the raw `lowerToLir` lowers a `Call`'s callee `GlobalAddr` via `lowerGlobalAddr`→`lea`, so the D-AS4-3 ARM64 `lea` (ADRP+ADD, cycle 2) was the missing piece. RED-on-disable DEMONSTRATED: with the arm64 `lea` opcode disabled, BOTH the MIR→LIR fixture AND the `extern_call_elf` arm64 compile fail with the exact `L_RequiredLirOpcodeMissing: target 'arm64' declares no 'lea' opcode (required for lowering MIR GlobalAddr)`. Proof: (1) `MirToLir.Arm64ExternCallWithResultLowersUnderDirectPlt` (the dropped pin reconstructed — lowers clean, 2 direct calls / 0 indirect, AAPCS64), (2) `examples/c-subset/extern_call_elf` gained an `arm64:elf64-aarch64-linux-exec` arm (compiles host-independently to a valid AArch64 ELF — **but at RUNTIME it SIGSEGV'd until the cycle-4 x30 fix above; the "→ exit 42" claimed here was the over-claim**). The full chain was already in place — `direct-plt` dispatch (D-FFI-EXTERN-CALL-DISPATCH ✅), the ARM64 PLT stub (`emitArm64PltStub`, D-LK6-8 ✅), the `lea` (D-AS4-3 ✅) — this cycle PROVES + closes it. NO src code changed (test pin + corpus arm only). ctest unchanged (the pin + the corpus arm are within existing test entries); 2026-06-08. Prior tail — post the **v0.0.2 ARM64 `lea` (ADRP+ADD) + the generic multi-instruction-macro / multi-relocation fixed32 encoder** (the ARM64-FFI arc's cycle 2; user "Build the full arm64 lea now"): generalized the fixed32 encoder from one-word/≤1-reloc to a GENERIC multi-word substrate — `TargetEncodingTemplate.fixedWords` (N-word base) + `TargetEncodingWire.wordIndex` + `PendingRelocSlot.wordIndex` + a per-word `words` vector / per-(word,slot) `wroteSlot` / `orInto(slot,value,wordIndex)` / a `pendingRelocs` VECTOR whose emit loop stamps each reloc at its word's START (cursor-derived) — plus a new generic `EncodingSlotKind::SymbolPatchMarker` (width-0; the linker owns the field; ONE marker serves both the ADRP page-reloc + the ADD lo12-reloc, distinguished by the wire's wordIndex+relocationKind) + `TargetEncodingVariant.extraResultSlots` (the result reg threaded into word0.Rd + word1.Rd + word1.Rn). validate() made word-aware (per-(wordIndex,slot) uniqueness across resultSlot∪extraResultSlots∪wires + wordIndex<wordCount + fixedWords-only-on-fixed32 + both-set reject + extraResultSlots-requires-resultSlot). The arm64 `lea` opcode (2-word [0x90000000 ADRP, 0x91000000 ADD]) consumes the already-shipped `adr_prel_pg_hi21`/`add_abs_lo12_nc` relocations; `lowerGlobalAddr` is UNCHANGED (target-blind — mirrors x86's 1-op RIP-relative lea). ZERO `if(arch/format)`; single-word path BYTE-IDENTICAL (x86 + all ~30 existing fixed32 opcodes unchanged). **D-AS4-3 ✅ CLOSED** (its named trigger — "ARM64 ADRP imm21 + ADD imm12 pair" — fired). Proven by `Arm64Encoder.{LeaEmitsAdrpAddWordPair, LeaStampsTwoRelocsAtWordOffsets, LeaProducesOneSrcMapEntrySpanning8Bytes}` (RED-on-disable DEMONSTRATED: word-blind `wroteSlot` → all 3 fail) + 4 `EncodingValidate.MultiWord*` synthetic pins; linker side already proven (`test_aarch64_reloc_formulas.cpp`). The multi-word ROUND-TRIP disasm is fail-loud-deferred (`D-AS5-MULTIWORD-DISASM`); the RUNNABLE end-to-end corpus is deferred on `D-LK1-ELF-EXEC-DATA-SECTIONS` (the ELF ET_EXEC writer emits no `.data`/`.rodata`, so a data global can't reach the binary — the lea ENCODER, the user-directed deliverable, is complete + strongly pinned; only runtime-observability is gated). ctest count unchanged (new pins are gtest cases in existing binaries); 2026-06-08. Prior tail — post the **v0.0.2 format-driven extern-call dispatch** (the ARM64-FFI arc's cycle 1; user "both, staged, using /dss-cycle"): a per-OBJECT-FORMAT `externCallDispatch` enum (`indirect-slot` = PE IAT / Mach-O __got → `call_indirect_via_extern` FF 15; `direct-plt` = ELF → plain `call`/BL to the linker PLT stub) read as DATA by MIR→LIR — **fixes a CONFIRMED latent x86_64-ELF FFI miscompile** (the ELF linker pointed an extern's symbolVa at its PLT STUB [code] but the call site emitted FF 15 = `call [ptr]`, dereferencing stub bytes as a fn-pointer → SIGSEGV; the ELF dynamic-FFI path had NEVER run end-to-end, only Windows-PE). ELF needs NO new codegen opcode (reuses `call`→PLT-stub); ZERO `if(arch/format)`. Proven by `examples/c-subset/extern_call_elf` (FIRST E2E ELF FFI run, x86_64-ELF `abs`→exit 42, RED-on-disable demonstrated in WSL) + 4 LIR pins + a 3-arm fail-loud guard. `D-FFI-EXTERN-CALL-DISPATCH` ✅; the ARM64 RUNNABLE corpus is deferred (`D-LK10-ENTRY-ARM64-FFI-ISEL` — an orthogonal arm64 call-with-result isel gap) to the arc's next cycle (with `linux-arm64` descriptors + `D-FFI-SHIPPED-LIB-PLATFORM-SELECT`); 2026-06-08. Prior tail — post the **v0.0.2 shippedLibs build-out** (the FULL Windows/Linux/macOS descriptor surface — stdio/stdlib/string/ctype/math per platform, LP64-vs-LLP64 ABI-delta correct — + the REQUIRED `header` / optional `standard` **provenance** fields on the descriptor schema + `AllShippedDescriptorsDecode` + `MissingHeaderFailsLoud` pins + the `shipped_include_abs` non-stdio end-to-end corpus [+1 example] + `src/dss-config/shippedLibs/README.md`; 2026-06-08) **+ the V2-1 CI-red repair** (ported the cross-ARCH run-gate from `examples_runner` to `integrated_tests/runner.cpp` — the subprocess corpus harness gated only on host OS, so it spawned the AArch64 `arm64_exit` ELF on an x86_64 CI host → ENOEXEC; now it SKIPs/emulates by arch exactly like the in-process runner). Prior tail post v0.0.2 V2-2 (language-neutral shipped-lib FFI descriptor — neutral JSON `shippedLibs/<platform>/*.json` + universal reader + the single `parseTypeFromText` type-text codec [extracted from `hir_text.cpp`] + semantic-phase symbol injection; `#include <stdio.h>` now runs with NO inline `extern` via `stdio.json` [which REPLACED the c-subset-source `stdio.h`]; goal-2 user-decl-wins; fail-loud `F_ShippedLibDescriptorMalformed` [0x501B] + `F_ShippedLibUnsupportedType` [0x501C], both unsuppressable; new `tests/ffi/test_shipped_lib_descriptor` entry +1; closes `D-FFI-SHIPPED-LIB-DESCRIPTOR-AGNOSTIC`, 2026-06-06) — prior tail: post cycle 29 (IntrinsicCall inlining RELAXATION — the MIR inliner's `inlineLegalityGate` now ADMITS a callee containing an `IntrinsicCall` [single-block + multi-block]; SSA-correct via the splice's generic arm [payload-carried intrinsic id copied verbatim]; the frame-sensitive-intrinsic hazard is deferred trigger-gated [D-OPT7-INLINE-FRAME-SENSITIVE-INTRINSIC] behind a fail-loud registry-empty tripwire, since no shipped frontend emits any intrinsic; flipped IntrinsicCalleeIsInlined [value-threaded] + Multi-block + Void pins, RED-on-disable; ctest unchanged [new pins are gtest cases in existing binaries], 2026-06-05; **with c6 the user-directed OPT7 sequence c3–c6 is COMPLETE**; cycle 28 = inline COST MODEL + ship inlining in release — `OptPipeline.inlineThreshold` [config-driven via the pipeline JSON, default 50] + the gate refuses inlining a callee whose inst-count > threshold; **`Inlining` now SHIPS in `release.pipeline.json`** — a real -O optimization [previously opt-in only]; cost-model pins [small-inlined / large-refused / at-vs-over boundary / threshold-0 fail-safe, RED-on-disable] + loader reject/default tests + `ShippedReleaseContainsInlining`; 2026-06-05; cycle 27 = general non-leaf inlining + SCC recursion safety — lifted the `Call`-leaf refusal [a callee with a regular `Call` is now inlined; splice unchanged — it already clones inner `Call`s] + a new `call_graph_scc` iterative-Tarjan substrate; the gate REFUSES inlining within a recursive call-graph SCC [generalizes self→mutual recursion]; non-leaf is correctness-preserving + OPT-IN [not in release.pipeline.json]; `examples/c-subset/non_leaf_inline` exit 41, flipped `NonLeafCalleeIsInlined` + `MutualRecursiveCallIsNotInlined` + `test_call_graph_scc` pins, 2026-06-05; cycle 26 = cross-CU function INLINING — `D-OPT7-1` ✅: the optimizer now runs on the whole-program MERGED module [`optimizeModule` chokepoint + the merged-optimize wiring in the N>1 driver] → cross-CU calls inlined; proven by `CrossCuCallIsInlinedOnMergedModule` [Call gone, RED-on-disable] + the `cross_cu_call` Inlining arm exit 42 + the `weak_inline_crosscu` cross-CU Weak negative pin [exit 42, RED at 7]; 2026-06-05; cycle 25 = the whole-program MIR merge — `reinternType` topological type re-intern walker + `mergeCuMirs` cross-module merge [clone + TypeId/SymbolId remap + cross-CU call→DIRECT rewire + drop-shadowed-weak + FFI-import dedup] + driver wiring; cross-CU calls now intra-module DIRECT, BEHAVIOR-PRESERVING [`cross_cu_call` exit 42 via direct call, single-CU byte-identical]; `D-OPT7-CROSSCU-MIR-MERGE` ✅; cycle 24 = the prereq substrate: per-CU pipeline split into build-MIR / lower-MIR halves [`buildCuMir` + `lowerCuMirToAssembly` + `CuMirModule` retaining the SemanticModel] + a two-loop driver [build-all-MIRs-then-lower-all] + the extracted shared `resolveCrossCuDefs` weak-vs-strong resolver; BEHAVIOR-PRESERVING, the cycle-17 analog; cycle 23 = OPT7 cycle 2 multi-block-LEAF inline splice; cycle 22 = OPT7 cycle 1 minimal single-block inliner + §2.9 legality gate + the Weak-inline negative pin) — through LICM trap-safe-hoist + Arc A division-codegen 10a–10r + cross-CU LK11 cycles 15–19 + tokenizer context-sensitive lexing (cycle 20) + FF11 (cycle 21) + OPT7 inliner cycle 1 (cycle 22) + OPT7 multi-block-leaf splice cycle 2 (cycle 23) + cross-CU inlining prereq substrate cycle 24 (pipeline build-MIR/lower-MIR split + shared `resolveCrossCuDefs`) + whole-program MIR merge cycle 25 (reinternType + mergeCuMirs + cross-CU calls now direct) + cross-CU function inlining cycle 26 (optimizer runs on the merged module; D-OPT7-1 ✅ — the cross-CU inlining loop closed) + general non-leaf inlining + SCC recursion safety cycle 27 (call_graph_scc Tarjan; non-leaf admitted, recursion refused by SCC) + IntrinsicCall inlining relaxation cycle 29 (OPT7 c6 — last of the user-directed sequence). Growth trace: 56 suites at SE1-pre baseline → HIR (HR1–HR11) → MIR (ML1–ML4) → LIR (ML5–ML8) → AS1–AS6 (assembler) → LK4 substrate → LK1–LK3 + cycle 2 executable images + LK6 cycle 1–2d (dyn link) + LK7 (codesign) + LK8/LK9 (WASM/SPIR-V skeletons) + LK10 cycle 1 (file emission) + LK10 cycle 2 (driver pipeline + 24 new program tests) → 13.1–13.4 substrate (ML7-2.2/2.6 + void* typing + null-ptr-const + extern-library-syntax + hello_writefile + D-LANG-VARIADIC + hello_printf/printf_int) → 13.5 cycle 1+2 (while-loop substrate + 14 corpus examples) → 13.6 c1 OPT1 substrate + 5 negative pins → 13.7 c1 OPT2 ConstFold + JSON pipeline loader + differential-verify → c2 OPT3 DCE + linkage assertion → c3 OPT4 c1 refactors + c4 OPT4 c2 Mem2Reg + c5 OPT4 c3 CopyProp + c6 OPT5 c1 CSE + c7 OPT5+ c2 SimplifyCFG + pipeline fixed-point + c8 OPT6 c1 LICM + c9 OPT5+ c3 block-merge + marker re-derivation + effectiveness substrate (passMutationCount) → **OPT7-prep 10a-10g** (alias-analysis substrate end-to-end: mirMayAlias 7-rule + region walk + per-Mir aliasingMode + char-exception config-driven + DSS_EXPORT discipline + IdentityPolicy round-trip pin) → **10h-10o** (deref smoke + `long`→I64 + sext I32→I64 encoding + alias-arc strict capstone fixture + MIR-tier effectiveness pin tightened to mutationCount[Cse]==1u + Add-operand-identity pin + LICM chained-invariants per-loop fixed-point + structural-derived iter cap + schema-mutation test substrate + LICM Info diag bridge + diag code per-kind split + CI portability fix) → **10p-10r** (Arc A integer division: implicit-register-constraint substrate `cb3ac20` + SDiv/UDiv codegen `bf7c7f8` + 4-opcode REX-correct split `50a07ab`; closes D-CSUBSET-DIVISION-OP-CODEGEN + D-CSUBSET-DIVISION-CORPUS-MATERIALIZE-BUG; unblocks D-OPT6-LICM-TRAP-SAFE-HOIST) → **cross-CU LK11 (cycles 15–19)**: D-LK4-3 compound `(cuId,SymbolId)` linker index → LK11a definition + reference resolution (`resolvedGlobalDefs` / `resolvedCrossCuRefs`) → LK11b merged-image emission (pre-merge + existing-walker reuse) → the multi-CU driver (`Program::compileUnits` + CLI gcc-semantics dispatch) + cross-CU CALL via the rodata thunk slot + PE `.reloc` ASLR base relocations (`examples/c-subset/cross_cu_call` exit 42; cross-platform `.reloc` byte-pin `PeExecWriter.Abs64RodataSlotEmitsDir64BaseRelocation`) + inline cost model + ship-in-release cycle 28 (OptPipeline.inlineThreshold; Inlining now in release.pipeline.json). |
| Substrate hardening (sub-plan SH1–SH4) — landing-log generator + Linux CI matrix + cross-tree `NodeId` guard + v2 follow-ups | ✅ **done** — all four PRs shipped. |
| Tokenizer (sub-plan TZ1–TZ3 + review-fix round 1) — `SourceReader`, `Tokenizer`, `TokenStream`, `LexerModeStack`, `stringStyle` body handling, comment modes, `C_BodyDefaultKindInShape` loader guard, synthesis-allowlist assertion, fail-loud `E2EHarness`. **Post-TZ3: numeric lexing is now fully config-driven** (08.55 retired the hand-coded C-style `scanNumber()`; each language declares its `numberStyle` block, schema v4). | ✅ **done** — phase #5 closed; phase #6 (`analysis-lexical`) ✅ subsumed |
| **Parser** (sub-plan PA0–PA5b + PA5a-prep + PA-Walker-LeftRec) — substrate `SchemaWalker` ✅; iterative RD driver ✅; Pratt walker ✅; recovery + diagnostic UX ✅; corpus stress ✅; c-subset readiness shapes ✅; postfix-chain walker redesign ✅; LSP server + LSP semantic-stub handlers ✅ | ✅ **all 7 PRs done — parser phase closed.** PA0–PA3 as before; PA4 onboarded real corpus programs (c-subset / tsql-subset / toy) and closed v2-gap-catalog rows 6/7/8/12 (postfix call/index/inc/dec, prefix deref). PA4 follow-ups (PA4-F1..F8) landed substrate refactors (`OperatorTable::Entry → optional<GroupedPostfix>`, `bodyDefaultTokenKinds` dedup), additional test coverage, and corpus golden harness. PA5a-prep closed v2-gap-catalog rows 12-declarator, 16 (compound assignment), 29 (`extern` declaration). PA-Walker-LeftRec added the `TreeBuilder::wrapLastChildInFrame` substrate primitive + rewrote the Pratt walker's postfix branch. PA5a shipped the LSP server skeleton + diagnostics (stdio JSON-RPC, UTF-16 position encoding, document store with monotonic parseGeneration stale-suppression, two-mode schema resolution, thread-pool executor); PA5b added the 6 semantic-stub method handlers (`textDocument/{hover,completion,definition,references,rename,signatureHelp}`) returning LSP-spec defaults. A cross-cutting 5-agent review on the integrated LSP stack closed 22 follow-up items in a single commit. Semantic-powered LSP methods land post-phase #8 in a spun-off `09-lsp-plan`. **Post-PA5: the Pratt walker now reads its wrapper-rule RuleIds from `schema.exprWrapperRules(exprRule)` (08.55), not from any hardcoded constants — thesis decision #4 end-to-end.** **56 ctest suites green** (post 08.55). |
| **Compilation unit** (sub-plan CU1–CU6) — multi-file `CompilationUnit` + per-language `ImportResolver` | 🟢 **CU1 + CU2 + CU3 + CU4 done.** CU1 type/builder; CU2 multi-file `addFile`/`addInMemory` + `D_*` codes + lexer+parser diagnostics unified in the Tree (cross-plan amendments to 01/05); CU3 CU-scoped `SymbolId` + `UnitAttribute<T>` (the CU-scoped `NodeId`→`T` side-table phase #8 consumes) + membership-based cross-CU `NodeId` guard + sanctioned minimal `populateDeclarationSymbols` walk (§2.7 C3-X1); CU4 per-language `ImportResolver` populating `crossRefs` — c-subset `#include` following (grammar amended, §2.8 C4-X1) + tsql cross-statement table-name matching + toy identity + `D_UnresolvedImport`/`D_UnresolvedReference`. **G-110 ✅ + G-111 ✅ resolved.** CU5/CU6 v1.x. Phase #7.5 complete. See [`08-compilation-unit-plan - tbd.md`](./08-compilation-unit-plan - tbd.md). Bridges parser (per-file `Tree`) and semantic (cross-file symbol table). |
| **Semantic** — symbol table, scope resolution, type checking | ✅ **done (SE1–SE7)** (phase #8; sub-plan [`08.6-semantic-plan`](./08.6-semantic-plan%20-%20ok.md)). One language-agnostic `analyze()` engine driven by a `semantics` schema-v4 block produces a `SemanticModel` (symbol table + scope tree + node→SymbolId / node→TypeId side-tables + `S_*` diagnostics). Toy/c-subset/tsql onboarded purely by config; cross-file visibility is `crossRefs[]`-import-driven. SE4 const-correctness, SE5 typedefs, SE6 functions/calls/overloads (`kindByChild`/`callRules`/`builtinFunctions` facets), SE7 LSP wiring all shipped. **G-201..G-212 closed.** Two 5-agent review rounds; 64/64. |
| **IR** — HIR + MIR + LIR (rev 2; replaces single-IR plan) | ✅ done — **HIR HR1–HR11 ✅ (plan 09 COMPLETE, 2026-05-26..28, `feature/hir-1`)**. **MIR ✅ ML1–ML4 done 2026-05-27..28** (`feature/mir-lir`): ML1 skeleton, ML2 HIR→MIR (6 cycles incl. Cast emission), ML3 MirVerifier (7 rule families + `I_*` 0xA00x), ML4 `.dssir` text + round-trip. **LIR ✅ ML5–ML8 done 2026-05-29 + ML7 cycle 2 done 2026-05-30**: ML5 JSON-configured targets + register-file + calling conventions in JSON + MIR→LIR isel cycles 1–3e for arithmetic/cast/CFG/memory/wide-literal/float/bitwise/Calls/Aggregates with `LirVerifier`; ML6 liveness (`bc596ae`) + linear-scan + variant migration + R_* family + rewrite pass + `verifyLirPostRegalloc`; ML7 cycle 1 callconv + stack frame (`5362766`); ML7 cycle 2 ✅ (`b9b5304`) `arg` + `call` virtual-op materialization + GlobalAddr→SymbolRef peephole + move-cycle detector + 4 new L_* codes; ML8 cycle 1 `.dsslir` EMITTER (`040d496`) + cycle 2 PARSER + verify-on-load + round-trip + schema version (`9622b38`); **D-LK10-ENTRY-ML7-FRAME-BIAS-UNIFY ✅ 2026-06-02** (Win64 shadow-space + post-CALL alignment-bias for non-leaf functions via new `cc.callPushBytes` field + `FrameLayout.hasCalls` + `functionHasCalls` scan); **MIR→LIR extern-call opcode selection ✅ 2026-06-02** (new `MnemonicSlot::CallIndirectViaExtern` + `externSymbols` set — extern callees now lower as FF 15 indirect-via-IAT, internal as E8 direct). New diag codes: `L_*` 0xB00x family. **Remaining**: D-ML7-2.2 ✅ **CLOSED 2026-06-02** (stack-passed args); D-ML7-2.4 ✅ **CLOSED 2026-06-12 (FC4 c2)** indirect function-pointer calls (same-mnemonic ["reg"] call variants + the 3-layer callee-reg exclusion — see the plan-12 row's double sketch-correction); D-ML7-2.5 regalloc pre-coloring for arg vregs (perf optimization); cross-CU type-import for HR Cast (downstream-of-HR real blocker). 225/225 ctest (latest, post v0.0.2 V2-4.X FC2 2026-06-10; was 213/213 post V2-3 AP3 2026-06-09). Production-readiness §3 (G-301a/b/c + G-302..G-310). Owned by [`09-hir-plan`](./09-hir-plan%20-%20ok.md) (HIR pivot; **HR1–HR11 ✅**) + [`12-mir-lir-plan`](./12-mir-lir-plan%20-%20ok.md) (MIR + LIR; **ML1–ML8 ✅**). |
| **Optimizer** — multi-tier, config-driven (source/target/linker-agnostic): HIR-transpile + MIR + LIR + per-target | 🟩 **v1 + v1.x mandatory subset DONE 2026-06-03 + OPT7-prep cycles 10a-10r ✅ 2026-06-04 (incl. Arc A integer-division codegen)** — sub-plan [`22-optimizer-plan`](./22-optimizer-plan%20-%20tbd.md). **OPT1 ✅** (substrate: PassId / pipelines / X_UnknownPassId, commits `5d29532` + `7d5932e`) + **OPT2 c1 ✅** (ConstFold + JSON pipeline loader + differential-verify runner, `8bae225` + `1a2bed5`) + **OPT3 c1 ✅** (DCE + CompileConfig threading + linkage assertion, `52c1380`) + **OPT4 c1 ✅** (substrate refactors: `mir_dom.hpp` + `mir_rebuild_helper.{hpp,cpp}`, `256b970`) + **OPT4 c2 ✅** (Mem2Reg via Cytron-Ferrante, `2e89b83`) + **OPT4 c3 ✅** (SSA CopyProp Phi-collapse, `f496aea`) + **OPT5 c1 ✅** (CSE / GVN dom-tree value numbering + `path_compress.hpp` helper, `ec7220b`) + **OPT5+ c2 ✅** (SimplifyCFG branch-fold + jump-thread + pipeline-level fixed-point loop, `96bb941`) + **OPT6 c1 ✅** (LICM + onBlockBeforeTerminator hook, `164a1ca` + `93362f1`) + **OPT5+ c3 ✅** (general block-merge + StructCfMarker re-derivation single-non-Linear case, `738413d`) + **effectiveness substrate ✅** (`OptResult.passMutationCount: std::array<size_t, kPassIdCount>` + ConstFold-rerun-post-Mem2Reg effectiveness pin, `86044f4`). **OPT7-prep 10a-10o ✅ 2026-06-04**: alias-arc capstone end-to-end (corpus + MIR-tier effectiveness pin with `mutationCount[Cse] == 1u` + Add-operand-identity substitution-direction pin); LICM chained-invariants via per-loop fixed-point with structural-derived iter cap + silent-miscompile negative regression test; c-subset gains `long`→I64 + I32→I64 sext encoding `movsxd r64,r/m32`; corpus +4 fixtures (deref_load/deref_store/long_primitive_smoke/strict_alias_cse_capstone); test-support `mutateShippedTargetSchemaJson` schema-mutation substrate closes D-CSUBSET-LOCAL-INT-CODEGEN-NEGATIVE-PIN; D-CONFIG-DIAGNOSTIC-CODE-PER-KIND split into C_InvalidLanguageName/C_InvalidTargetName/C_InvalidFormatName; LICM ambiguous-preheader silent-skip upgraded to Info diagnostic. **Release pipeline shipped**: `[Identity, ConstFold, Mem2Reg, CopyProp, Cse, Licm, SimplifyCfg, Dce]` with `maxIterations: 4`. Heuristics-as-data from PR1 → OPT10 autotuner. **Remaining v1.x → v2**: `D-CSUBSET-DIVISION-OP-CODEGEN` ✅ CLOSED (Arc A, 10p-10r `50a07ab`) → D-OPT6-LICM-TRAP-SAFE-HOIST now UNBLOCKED (idiv `#DE` observable; pin-able — full closure needs value-range, OPT8+); D-OPT6-LICM-PREHEADER-INSERTION (CFG modification + Phi-merge substrate); D-OPT-COPYPROP-BLOCK-CURSOR (general copy-prop beyond Phi-collapse); D-OPT4-1-NON-LINEAR-MARKER-MERGE (WASM/SPIR-V backend trigger); **OPT7 inlining ✅ OPENED 2026-06-05 (the `G-406` user-supervised HARD STOP, now unlocked) + c1–c6 arc DONE (cycles 22–29; see §0.1 row 23 + plan 22 §3.1); pre-OPT7 chain (cross-CU Weak-pin): **P1 linkage-specifier binding ✅ + P2 HIR→MIR mapping ✅ (cycle 13 — c-subset `static`→Local + `__attribute__((weak))`→Weak flow source→HIR→MIR→DCE, regression-proven; D-DECL-SPECIFIER-PREFIX-SUBSTRATE cycle 12 ✅)** → **P3 CU6** — the OPT7-prereq cross-CU slice (multi-CU driver + cross-CU MIR-merge) landed (cycles 19/24–26); the broader plan-08 CU6 item (incremental rebuild) stays v1.x. (P4 LK11 ✅ cycles 15–19 — D-LK4-3 compound `(cuId, SymbolId)` index → LK11a definition+reference resolution → LK11b merged-image emission → the multi-CU driver + runnable cross-CU CALL `cross_cu_call` exit 42; `resolvedGlobalDefs` IS the OPT7 Weak-pin substrate); see plan 22 §0/§3.1)**; OPT8 LIR peephole + regalloc coalescing; OPT9 vectorization; OPT10 autotuner. Production-readiness §4 (G-401..G-410). |
| **Codegen + in-tree linker + assembler** — ELF + PE + Mach-O × x86_64 + ARM64 + per-platform ABI + **hermetic linker** (rev 2; replaces system-linker integration) | ✅ **CLOSED + RUNNABLE end-to-end 2026-06-02** for x86_64 Windows. Owners: [`13-assembler-plan`](./13-assembler-plan%20-%20tbd.md) + [`14-linker-plan`](./14-linker-plan%20-%20tbd.md). **Assembler ✅ COMPLETE (AS1–AS6 landed 2026-05-29 on `feature/as-1`)** — x86_64 + ARM64 byte encoding via shape-keyed walkers, round-trip oracle disassembler, relocation taxonomy unifier, source-map stamping. **Linker ✅ COMPLETE (LK1–LK10 landed 2026-05-30 on `feature/lk-111`) + RUNNABLE (D-LK10-ENTRY Stage 1 + D-LK10-ENTRY-ML7-FRAME-BIAS-UNIFY landed 2026-06-02)** — `ObjectFormatSchema` + format-blind engine + 11 `K_*` codes at 0x8xxx + 5 `D_*` driver-tier codes at 0xD007–0xD00B + cross-side reloc unifier; per-format walkers for ELF / PE / Mach-O / WASM (skeleton) / SPIR-V (skeleton); relocatable .o/.obj/.o AND executable images ET_EXEC/.exe/MH_EXECUTE; dynamic linking (PE IAT runtime-proven via 2-DLL hello_puts / ELF GOT+PLT / Mach-O LC_DYLD_INFO_ONLY); codesign placeholders (LK7); file emission (`dss::linker::writeImage`); driver pipeline wired (`Program::compileFiles` / `compileDirectory`). ✅ **First byte-correct c-subset e2e ACHIEVED 2026-06-02**: `int42.c` (Stage 1 exit-42) + `hello_puts/main.c` (Stage 2 Windows half — prints "hello\r\n" + exits 42 via msvcrt+kernel32 IAT). Remaining v1 deferrals: LK5 (TLS — first TLS-bearing corpus trigger); LK11 (cross-CU — v1.x); cross-host Stage 2 (Linux/macOS/ARM64) ✅ since LANDED 2026-06-09 (D-LK10-ENTRY-ARM64 ✅ + D-LK10-ENTRY-MACHO-EXIT ✅ + per-host CI runners; `shipped_include_puts` runs on all 3 OSes × 2 arches); variadic `printf` gated on ML7 stack-args + variadic ABI substrate. Production-readiness §5 (G-501..G-571). |
| **Driver / project config** — `dss-code-prime build my-project.dss-project.json` | 🟢 **partial — programmatic API + project-config loader landed.** `Program::compileFiles` / `compileDirectory` wired through the full pipeline at LK10 cycle 2 (2026-05-30); **`compileProject` IMPLEMENTED 2026-06-09 (AP2+AP3)** — loads `.dss-project.json` → enforces `artifactProfile` (language gate `D_ArtifactProfileNotSupported` + format gate `D_ArtifactProfileFormatMismatch`) → routes by source count → delegates to `compileFiles`/`compileUnits` (the `D_PlanNotLanded` stub now backs only `transpile`, plan 10). CLI argument routing (`--compile` / `--target` / `--output`) is LK10 cycle 3; the `dss-code-prime build` CLI surface + positioned source-context diagnostic rendering (DSS's own; NO clang/LLVM) is V2-4 (Part A landed 2026-06-09). Production-readiness §6 (G-601..G-609) + [`06-artifact-profile-plan - tbd.md`](./06-artifact-profile-plan - tbd.md) AP2/AP3. |
| **`artifactProfile` mechanism** — language config declares supported profiles; project config picks one; codegen reads it | 🟢 **AP1 ✅ + AP2 ✅ + AP3 ✅ + AP4 ✅ (2026-06-09) — substantially complete; only profile→codegen *shape* deferred (trigger-gated).** Language config declares its profiles (AP1); project config picks one; the driver enforces `profile ∈ language-set` (AP2, `D_ArtifactProfileNotSupported=0xD010`) + `profile ∈ format-served-set` (AP3, `D_ArtifactProfileFormatMismatch=0xD011`, the format declares `artifactProfiles[]`). **AP4** = the per-language onboarding matrix (both gates exercised through `compileProject` across toy/c-subset/tsql-subset × declared/undeclared/served/unserved, strict three-sided cells + red-on-disable) + one real end-to-end cli emit + the `docs/project-config-spec.md` spec. Codegen-READING the profile (entry-symbol/subsystem/extension = artifact shape) is deferred trigger-gated on a non-format-redundant consumer (`D-AP2-COMPILATION-CONTEXT`); §7 #2/#4 remain 🟡. See [`06-artifact-profile-plan - tbd.md`](./06-artifact-profile-plan - tbd.md). Gates `gen-link` acceptance. |
| **Debug info** — DWARF (ELF + Mach-O) + PDB (PE) | ⏳ pending. Production-readiness §5.4 (G-530..G-533). |
| CI/CD pipelines | 🟦 Linux x86_64 + Windows x86_64 ✅; **Linux ARM64 (native `ubuntu-24.04-arm`) + macOS ARM64 (`macos-latest` Apple Silicon) ✅ — both build AND run the corpus natively in CI** (runtime-verified across the v0.0.2 cycles). **Windows ARM64 still pending** (G-704..G-706); macOS x86_64 has no CI leg (the `macho64-x86_64-darwin` path stays byte-pinned-only). linux-clang ASan/UBSan leg ✅; broader sanitizer/fuzzing coverage partial. |
| **Real-world corpus** — `tests/corpus/{c-subset,tsql-subset,toy}/` programs that exercise the full grammar | ✅ parser-side done (PA4 — `mini_calc.c`, `schema_and_dml.sql`, `demo.toy` driven through `Parser::parse()` with golden-tree byte-comparison via `DSS_REFRESH_GOLDENS`). ✅ **End-to-end "runs a compiled binary" ACHIEVED 2026-06-02** (Windows host) — `examples/c-subset/{int42,hello_puts,...}/` compile → link → spawn → assert OS exit code + captured stdout byte-for-byte. **Cross-host ✅ ACHIEVED 2026-06-09** — the corpus runs end-to-end on **Linux-ELF (x86_64 + arm64) + macOS-Mach-O (arm64)** in CI (`D-LK10-ENTRY-ARM64` + `D-LK10-ENTRY-MACHO-EXIT` ✅ closed; `#include puts("hello")` + `global_int` execute on every native leg). **Remaining**: Windows-ARM64 (no CI leg) + macOS-x86_64 (byte-pin-only). Production-readiness §7.3 (G-730..G-734). |
| **Diagnostic UX** — positioned source-context (`file:line:col` + caret) + actionable messages (DSS's own renderer, NO clang/LLVM) | 🟢 **partial — CLI positioned rendering wired 2026-06-09 (V2-4 Part A).** The driver `drainDiagnosticsToStderr` now routes buffer-bearing (parser/semantic) diagnostics through DSS's own `DiagnosticReporter::format()` (`--> file:line:col` + source line + `^` caret) via a `BufferRegistry` built from the CUs; buffer-less driver `D_*` stay code-only. **Part B ✅ 2026-06-09** — the golden-DIAGNOSTIC harness (`tests/analysis/test_diagnostic_corpus.cpp`) pins every `P_*`/`S_*` diagnostic's code + line:col (+ related positions) by construction against `.diag` goldens, so a wrong-position regression is RED. **Remaining (V2-4 C):** driver `examples_runner` `expectError`(+position) field. Production-readiness G-115 + G-608. |
| `source-factory/` thin facade | ⏳ pending (low priority; loader-level work already done in `GrammarSchema::loadShipped`). |
| Docker / cross-compile toolchains | ⏳ pending. |
| **Process / discipline** — per-PR review tiering (substrate / feature / doc), onboarding doc, second-engineer ramp | ⏳ pending. Production-readiness §7.6 (G-760..G-762). |

Drill into the [sub-plan §0 status table](./01-tree-node-model-plan - ok.md#0-current-status-snapshot) for tree/node phase detail.

## 0.1. Stepper — next-up by block

> **Definition of "next" used here:** a step is eligible to start the moment **all its dependencies are ✅ shipped** — nothing pending blocks it. Critical-path steps are numbered first; sidewise-but-unblocked work is grouped at the bottom. This is a condensed view of [§8](#8-implementation-phases-todos) — see §8 for full phase metadata, and each sub-plan for PR-level scope.
>
> Read top-to-bottom: step **1** is what to start now; the **Blocked by** column tells you why every other step has to wait.
>
> **Reorganized 2026-06-06 into a version split.** The **v0.0.2 ROADMAP** below is the authoritative forward list `/dss-cycle` walks (recommended-interleave order); the **v0.0.1 as-built ledger** beneath it is the shipped history. Where a v0.0.1 ledger row is still ⏳/🟦, the v0.0.2 roadmap **supersedes** it (its forward work was carried up — do not double-pick).

### v0.0.2 ROADMAP — next-up (recommended interleave)

> Walk top-to-bottom. Each arc is one or more `/dss-cycle` cycles (a cycle = one priority, fully, to push). New-target / new-backend arcs are **substrate cycles** → `/dss-cycle` Step 3.5 pauses for a §B design sign-off before building. **v0.0.2 = these 7 arcs (V2-1…V2-7)**: ARM64-verified + language-agnostic shipped-lib + artifactProfile + `dss-code-prime build` CLI + RISC-V + WASM + OPT7+. **The original v0.0.1 backlog v0.0.2 does NOT cover lives in the v0.0.3 ROADMAP below** (drawn as arcs complete; the loop walks rows top-to-bottom regardless of the version labels).
>
> **Pre-FC5 deferral mini-arc (2026-06-13, user-directed "deferrals before FC5"):** clear the eligible HIR-correctness deferrals BEFORE plan-23 FC5 — (1) `D-HIR-LOOP-BODY-ONLY-RETURN-DOUBLE-ATTACH` ✅ CLOSED 2026-06-13 (the implicit-return-0 double-attach crash on any non-terminating `main`); (2) `D-HIR-INFINITE-LOOP-NOT-TERMINATING` ✅ CLOSED 2026-06-13 (wrap a provably-infinite loop as `Block{loop,Unreachable}` so `pathTerminates` recognizes termination; also incidentally fixed the `<both-arms-return-if> <sibling>` latent crash); (3) `D-HIR-DEAD-CODE-AFTER-RETURN-REJECTED` ✅ CLOSED 2026-06-13 (relaxed to an `H_UnreachableCode` WARNING; the dead code reaches MIR and is dropped by the prune). **The HIR-correctness mini-arc is COMPLETE.** (4) `D-OPT7-MULTIBLOCK-SPLICE-PHI` ✅ CLOSED 2026-06-13 (§B design brief signed off; the inliner now clones callee Phis via a deferred-incoming-flush — VALUE via the `local` map, PRED via `calleeBlockMap`, fail-loud on a pred-not-in-map; NO shape gate, fully agnostic; 6 RED-on-disable MIR-tier pins incl. a verifier-blind value↔pred SOLE-guard test that runs THROUGH the real inliner + the `examples/c-subset/phi_inline` runtime witness, exit 9). **The pre-FC5 mini-arc is COMPLETE (4/4) and the OPT7 callee-Phi gap is CLOSED.** **plan-23 FC5 ✅ COMPLETE 2026-06-15** (goto/labels + empty statement + comma operator + the `entryFunctionNames` de-conflation; 289/289). **plan-23 FC6 ✅ COMPLETE 2026-06-15** (cluster B — the aggregate-layout substrate: `type_layout` engine + per-`.target.json` `aggregateLayout` params + `ArenaAttribute<TypeInterner, StructLayout>` side-table [`D-FF3-1` layout-half ✅] + full expression-context `sizeof` const-folding + the FAM front-end; 292/292). **FC6-deferrals ✅ CLOSED 2026-06-15** (user-directed "address the FC6 named exclusions before FC7"): `D-FC6-SIZEOF-CONST-CONTEXT` ✅ (sizeof const-folds in global inits + array-dim type form; residual array-dim VALUE form → `D-FC6-SIZEOF-ARRAY-DIM-VALUE-FORM`, pass-ordering) + `D-FC6-FAM-SEMANTIC-CONSTRAINTS` ✅ (positioned FAM-misuse diagnostics); 293/293. **FC7 ✅ COMPLETE 2026-06-17** (member-access + nested struct fields + byte-wise aggregate copy + struct BY-VALUE args/returns ALL 4 ABIs; `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` ✅; 316/316). **Array-storage cluster + width-exact integer memory ✅ COMPLETE 2026-06-17** (Option A agnostic MIR element scaling closed `D-MIR-STORAGE-ARRAY-INDEX-GEP` + `D-MIR-ARRAY-FIELD-AGGREGATE-INIT`; the self-audit then exposed + fixed a PRE-EXISTING width-8-integer-memory silent miscompile [`D-LIR-INT-MEMORY-WIDTH-EXACT` — width-32/16 memory encodings on all 4 targets] that crashed packed array/struct-field storage under the REAL release pipeline; 316/316). **Top-level struct/union/enum head grammar + aggregate-global codegen ✅ COMPLETE 2026-06-17** (`D-CSUBSET-STRUCT-BODY-VARDECL-POSITION` ✅ + the schema FOLLOW root-fix `D-PARSE-SCHEMA-NESTED-NULLABLE-FOLLOW` ✅ + aggregate-global rodata `D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL` ✅; `int ;` fails loud; dead grammar rules deleted; 318/318). **FC8 cycle 1 — enums end-to-end ✅ 2026-06-17** (`D-CSUBSET-ENUM-INT-CONVERSION` closed — enum as underlying int across assign/UAC/cast/coerce + enumerator Ref→Const fold + inc/dec-at-underlying + LIR `reprKind` packed-enum-field width fix; 319/319). **FC8 cycle 2 — C bit-fields ✅ 2026-06-18** (`D-CSUBSET-BITFIELD` closed — named/anon/zero-width packing + extract/RMW codegen, config `BitFieldStrategy`; 320/320). **FC8 cycle 3 — non-string array→ptr decay ✅ 2026-06-18** (`D-LK4-RODATA-PRODUCER-LOCAL-ARRAY-DECAY` + `-NONSTRING-GLOBAL-ARRAY-DECAY` closed; 321/321). **FC8 cycle 4 — bit-field aggregate init ✅ 2026-06-18** (`D-CSUBSET-BITFIELD-INIT` closed — packs per allocation unit at all 3 init sites; review-caught F1 ordinary-field-in-shared-unit clobber fixed via a two-pass zero; opened `D-CSUBSET-BITFIELD-RVALUE-RUNTIME`; 322/322). **FC8 cycle 5 — enum-typed bit-fields ✅ 2026-06-18** (`D-CSUBSET-ENUM-BITFIELD` closed — validates/lowers at the underlying; signed sign-extends; review re-pinned the codegen red-on-disable; 323/323). **FC8 cycle 6 — typedef-enum trailing comma ✅ 2026-06-18** (`D-CSUBSET-TYPEDEF-ENUM-TRAILING-COMMA` closed — RECONCILIATION: gap already closed by the global fixpoint, no grammar change, pinned). **ALL IN-SCOPE FC8 COMPLETE.** Remaining FC8 = trigger-gated/blocked only (`D-CSUBSET-BITFIELD-ABI-EXACT` trigger=FFI/foreign-`.o`; `D-CSUBSET-BITFIELD-WIDE-UNIT` blocked=64-bit codegen; `D-CSUBSET-BITFIELD-RVALUE-RUNTIME` blocked=by-value-aggregate-rvalue carrier) → **§B RESOLVED 2026-06-18 (user decision):** build all 3 via dedicated cycles (best-long-term / config-driven / no-workarounds / no-follow-ups) BEFORE FC9 — ✅ ALL 3 DONE 2026-06-18 (+ sibling `D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR` ✅ CLOSED); FC8 SUBSTRATE COMPLETE. NEXT = FC9.**

| Order | Arc · cycles | What | Plan / sub-plan | Blocked by | Status |
|---|---|---|---|---|---|
| **T1.0** ✅ | perf profile · ½ | **(2026-07-07 perf sweep, user-confirmed order compile-speed→miscompiles→C23 — ACTIVE HEAD now moved to FC16/T3.1; each row = one `/dss-cycle`; detail [[project_v0_0_2_post_sqlite_c23_perf_roadmap_2026_07_07]]).** Profile the current 2-TU sqlite compile, re-rank the perf levers by present phase-cost + record the baseline (the c97 numbers are stale) — opens the compile-speed tier. **DONE 2026-07-07: ranking INVERTED — optimize 74s/50% (3 runs = OPT7 double-optimize) + `[other]` 44.5s/30% UNATTRIBUTED dominate; preprocess 10.2s/6.8% (NOT the stale 68%). Surfaced that `--config=release` sqlite ABORTED at mem2reg → release-sqlite never end-to-end green (harness compiles sqlite in debug). User reorder: mem2reg → instrument-`[other]` → OPT7.** | `D-PERF-1..4` (measure) | — | ✅ DONE |
| **T-M2R** | correctness · 1 | **✅ DONE 2026-07-07 — `D-OPT-MEM2REG-CONDITIONAL-INIT-UNDEF`:** release Mem2Reg no longer aborts on benign conditional-init (`int x; if(c) x=1; use(x)`) — undef-as-zero (GPR entry-block `Const 0`) + de-promote (FPR/aggregate → memory). 3-leg gate 565/565; independent design + code audits; repro→2; `cond_init_release`/`cond_init_float_release` release-arm corpus. sqlite `--config=release` now grinds PAST mem2reg (full release-green is downstream, perf-gated). | `D-OPT-MEM2REG-CONDITIONAL-INIT-UNDEF` | T1.0 | ✅ DONE |
| **T-PERF-ARC** · PARKED §B | LLVM-style optimizer · multi | **The >30min release-optimize → the LLVM-style analysis-caching arc** (memory `project_v0_0_2_llvm_style_optimizer_arc_2026_07_07`). ROOT: every pass rebuilds the whole module + recomputes dom/liveness from scratch (no caching). ✅ verify-once (`93bdcfd`, ~10-20min verify gone) · ✅ Cse hoist (`D-OPT-CSE-ANALYSIS-HOIST`, 118→56s, byte-identical) · ✅ **MemorySSA (`D-OPT-MEMORYSSA-CLOBBER-WALK` CLOSED — the `MirMemoryClobbers` clobber index + memoized reachability; Licm 107→60.5s −43%, Cse worst-case class removed; LICM absorbed the preds hoist; MemoryPhi walk-past REJECTED w/ a constructed-miscompile pin → successor `D-OPT-MEMSSA-WALK-PAST-PRECISION` gated; ctest 565→566)** · ✅ **SimplifyCfg preds-hoist (`141f0b1`..next; the 3rd `D-OPT-CSE-ANALYSIS-HOIST` application — the per-function whole-module `mirBuildPredecessors` was ALLOCATION-bound; 120→64s −47%)**. · ✅ **marker-maintenance posture (the verify-once split applied to `rederiveStructCfMarkers`: markers feed ONLY the verifier → release posture skips the 8 in-loop whole-module re-derivations, ONE final re-stamp before the final verify; Inlining 62→7.3s −88% + SimplifyCfg 64→14.4s −78%; the `ReleasePostureRederivesMarkersOnceAfterPipeline` canonicality pin)**. · ✅ **dom-tree scratch (`D-OPT-DOMTREE-SCRATCH-REUSE` — THE biggest single win: `computeMirDomTree` re-copied the whole-module preds map + 5 module-sized arrays PER FUNCTION; now a reusable scratch w/ audited touched-slot reset. Cse dom 14.9s→26ms, Licm dom 14.3s→8ms, Mem2Reg 27.8→16.4s free via the fresh-path predSlots kill, rederive 12→2.1s; compile 4m24→2m00, optimize 1m35)**. **ARC TOTAL: >30min → 2m00s (15×+).** ⚠ REMAINING floor ≈1m35 optimize: per-pass REBUILD (~2s × pass × iter — Phase 4 in-place territory) + Mem2Reg iter-0 16s (own SSA construction) + verify-once 12s + rederive 6s. **The original Phase 2/3 justification (analysis recompute ×4 iters) has largely EVAPORATED — dom is now ~ms; §B decision pending: continue into Phase 2/3/4 (diminishing returns, architectural risk) vs accept ~2min and advance to T2 miscompiles / T3 FC16-18.** | `D-OPT-CSE-ANALYSIS-HOIST` (×3) + `D-OPT-MEMORYSSA-CLOBBER-WALK` ✅ + `D-OPT-DOMTREE-SCRATCH-REUSE` ✅ + `D-OPT1-VERIFY-FREQUENCY-CONFIG` (markers) + gated: `D-OPT-POSTDOM-SCRATCH-REUSE` / `D-OPT-MEMSSA-WALK-PAST-PRECISION` | T1.0 | 🟡 PARKED §B |
| **T1.1** | perf harness · 1 | `D-PERF-3-HARNESS-MULTI-TARGET` — one invocation, N `--target`; ~3× the multi-target build step (harness-only, no compiler change) | registry `D-PERF-3` | — | ⏳ |
| **T1.2** | perf parser · 1–2 | `D-PERF-2-TYPEDEF-SEED-DISAMBIGUATION` — seed the parse-time binder w/ descriptor+sibling typedefs; kills the ~0.75s oracle reparse | registry `D-PERF-2` | T1.0 | ⏳ §B (diag-ordering fork) |
| **T1.3** | perf preproc · 2–3 | `D-PERF-1-PREPROCESSOR` — the **~68%** (6.21s) wall; profile-localized O(n²) fix (descriptor-macro splice / `__has_include` re-eval / 269k-line expansion) | registry `D-PERF-1` | T1.0 | ⏳ |
| **T1.4** | perf CU-par · 1 | `D-PERF-4-CU-PARALLELISM` — per-CU thread pool; halves the multi-TU front half (deterministic reporter merge) | registry `D-PERF-4` | — | ⏳ |
| **T1.5** | perf LTO · 1 | `D-OPT7-CROSSCU-LTO-SINGLE-OPTIMIZE` — skip per-CU optimize for N>1; optimize the merged module ONCE | registry `D-OPT7-1` | — | ⏳ |
| **T1.6** | perf verify · §B | verify-once vs verify-after-EVERY-pass (the biggest Release back-end lever, ~23.5s optimize over sqlite's VDBE fn) — safety-vs-speed | new §B | — | ⏳ §B |
| ~~**T2.1**~~ | correctness · 1 | `D-OPT-RELEASE-FP-CONSTANT-UNLOADED-XMM` — ✅ **CLOSED 2026-07-07 (SAME ROOT as T2.2).** The "un-loaded xmm" symptom WAS the mixed-class inline arg-drop (FP params mapped to int actuals). Fixed by T2.2; `mixed_class_stack_overflow` re-armed with its release arm → 73 on all legs. | registry ✅ | T2.2 | ✅ CLOSED |
| ~~**T2.2**~~ | correctness · 1 | `D-OPT-RELEASE-SYSV-MIXED-CLASS-REG-ARG-DROP` — ✅ **CLOSED 2026-07-07.** ROOT: the MIR `Arg` carried only a per-class register ordinal (SysV GPR/FPR pools collide at ordinal 0), so the inliner spliced the WRONG actuals for a mixed int+FP callee (`g(1,47.0)`→0). FIX: Arg now packs `(position<<16)\|ordinal` (`arg_payload.hpp`); the inliner maps by the flat call-operand POSITION. Design-audit F1-F8 folded (esp. F1: the 3 addArg re-encode sites thread position; F5 duplicate-position verifier). Closed BOTH miscompiles (one root). 3-leg 568/568; corpus `inline_mixed_class_args` + `_crosscu` (2-TU merge) + re-armed `mixed_class_stack_overflow`; MIR-tier operand-identity + rebuild-round-trip pins. Opened F8 gated `D-OPT-LIR-ARG-REGISTER-CLASS-MISMATCH-FAILLOUD`. | registry ✅ | T1 | ✅ CLOSED |
| **T2.3** | correctness · 1 | `#elifdef`/`#elifndef` NOT recognized (C23 PP §6.10.1) → when the `#if`/`#ifdef` arm is false, the true `#elifdef` branch is SKIPPED and control falls through to `#else` = **silent miscompile** (dss-state `c23_elifdef`: rc=0, exit **2** not 42, 2026-07-08). Fix = implement the directive (also tracked as a FC17.5 preprocessor gap); registry anchor `D-PP-ELIFDEF-ELIFNDEF` (2026-07-08, per user). | `D-PP-ELIFDEF-ELIFNDEF` + plan-23 FC17.5 | — | ✅ RESOLVED 2026-07-09 (`D-PP-ELIFDEF-ELIFNDEF` closed — C23 §6.10.1 `#elifdef`/`#elifndef` implemented, silent miscompile fixed, negative pins 42→2/42→3, 3-leg 606/606 + sqlite 3.54.0/CRUD green) |
| **T3.1** | C23 FC16 · 3–4 | **★ ACTIVE HEAD (2026-07-08).** FC16 standard-C completeness (2 features/cycle, user 2026-07-07; ⚠ sqlite re-probe every cycle gate). ✅ **anon struct/union member semantics** (`D-CSUBSET-ANON-MEMBER-PROMOTION`, clears S0017). ✅ **`_Static_assert`/`static_assert`** (`D-CSUBSET-STATIC-ASSERT`). ✅ **`_Generic`** (`D-CSUBSET-GENERIC-SELECTION` — semantic-tier selection + stamped-node lowering; clears the LAST P0009 battery gap; both live battery gaps now closed). ✅ **`_Alignof`/`alignas`** (6.5.3.4/6.7.5). ✅ **wide/UTF literals arc** (A strings + B chars + C `\u`/`\U`/surrogate + D concat-mixing, 2026-07-08). ✅ **`packed` attribute SEMANTICS** (`D-CSUBSET-PACKED`, 2026-07-08 — suffix-position packed → no-padding/align-1, all spellings honored-or-fail-loud, codegen ZERO change, 4 registered deferrals; 3-leg 603/603 + sqlite green). ✅ **`noreturn` SEMANTICS** (`D-CSUBSET-NORETURN`, 2026-07-08 — all 4 spellings → config-driven `isNoreturn` (OR-merged proto/def); direct noreturn-callee call wrapped `Block{ExprStmt(call), Unreachable}` mirroring `wrapIfProvablyInfinite`, ZERO verifier/lowering/prune change; F1 guard `isDirectNoreturnCall` inspects the callee `Ref` child; audit-rejected `{binding:global}` no-op replaced by config-driven `linkageSpecifierIgnoredNames` (fail-loud kept); 3-leg 604/604 + sqlite green; 2 gated deferrals). **`volatile`-honoring codegen ✅ (ALREADY-SHIPPED c21/c27/opt-10b — a stale-plan correction; a design pass traced all 5 cases clean, this cycle added 3 coverage-by-construction pins + an ordering doc-contract, 2026-07-08)**. **★ FC16 CORE COMPLETE — the parse-without-semantics stub class is closed; next frontier = FC17/FC18 (T3.2/T3.3).** | plan-23 FC16 + `D-CSUBSET-VOLATILE-SEMANTICS` | — | 🟡 IN PROGRESS |
| **T3.2** | C23 FC17 · 2–3 | **★ ACTIVE HEAD (2026-07-09).** FC17 C23-modern features (~2/cycle; sqlite re-probe every cycle). ✅ **`nullptr`/`nullptr_t`** (`D-CSUBSET-NULLPTR`, 2026-07-09 — NEW semantic-tier-only `TypeKind::NullptrT`; the KEYSTONE lowers `nullptr`→integer-0 null const at HIR [Fix 1(a)] so NullptrT NEVER reaches MIR; one-way `Ptr←NullptrT` conversion; explicit `S_NullptrInvalidOperand` fail-loud operator gate [WITHOUT it `nullptr+1` would silently compile as `0+1`]; `combineTernary` arm; `I_NullptrTypeInMir` tripwire; 4 gated deferrals incl. nullptr→bool [the c-subset has no scalar→bool]; runnable `nullptr_basic`→15 + 2 diag goldens + red-on-disable; independent design-audit + code-audit [VERIFIED-CLEAN, 2 MEDIUM folded: FnSig/Array peer-decay + flag co-requirement loader guard]; 3-leg ctest **607/607** [Win Debug + WSL x86_64 + arm64 qemu] + sqlite 3.54.0/CRUD 42 on all 3 legs). ✅ **enum explicit underlying-type** (`D-CSUBSET-ENUM-UNDERLYING-TYPE` + `D-PARSE-SPECULATIVE-OPTIONAL`, 2026-07-09 — `enum E : T`; a NEW GENERIC parser-engine capability [speculative `{"optional"}` grammar clauses] so `enum E:T` doesn't regress the anon `enum:4` bit-field [**user §B chose Option B** over config-only]; the enum TYPE substrate was ALREADY generic [`scalars[0]`=underlying] so only the grammar + one `enumType()` call site changed; range-check fail-loud `S_InvalidEnumUnderlyingType`0xE034 / `S_EnumeratorValueOutOfRange`0xE035; a stale-design codegen gap fixed [`mir_to_lir requireNativeIntWidth` now reads `reprKind` → narrow-underlying enum conversions work]; design-audit caught+fixed a body-probe bounded-cost defect [adopt Fork B: exclude the optional's skip branch from candidate enumeration]; code-audit VERIFIED-CLEAN [confirmed `reprKind` is identity for every non-enum kind + the parser capability is inert for all existing grammars — the enum optional is the SOLE speculative+nullable position in the config; 1 LOW → opened `D-CSUBSET-ENUM-UNDERLYING-CHAR-BOOL`]; 3-leg ctest **608/608** + sqlite 3.54.0/CRUD 42 on all 3 legs; 3 gated deferrals). ✅ **`typeof`/`typeof_unqual`** (`D-CSUBSET-TYPEOF`, 2026-07-09 — C23 §6.7.2.5; a PURE semantic-tier type resolver [ZERO codegen — resolves to a TypeId in `resolveTypeNodeImpl`, reuses the sizeof/_Alignof machinery]; both keyword spellings + expression & type-name operands + all 7 type-specifier positions; `requireKnownType` commit polarity [like alignas — `typeof(ENUM_CONST)` resolves as a value]; ★ design-audit caught+fixed a CRITICAL qualifier-scan leak [the volatile/const decl-head scans descended INTO the typeof operand and re-qualified it AFTER the arm stripped → `typeof_unqual(volatile T)` silently inert; fix = a config-driven `opaqueRules` set stopping `subtreeContainsToken`/`declaratorObjectIsConst` at typeof nodes]; `S_TypeofBitfieldOperand` 0xE036; code-audit VERIFIED-CLEAN [confirmed the `opaqueRules` scan change is provably INERT for every non-typeof declaration on the shared decl-head path; 3 LOW, incl. a `typeof(const int)` const-flag gap → the deferred const-model class]; 3-leg ctest **609/609** + sqlite 3.54.0/CRUD 42 on all 3 legs; 1 gated deferral). ✅ **`constexpr`** (`D-CSUBSET-CONSTEXPR`, 2026-07-09 — C23 §6.7.1 object storage-class; ZERO codegen; the DELTA vs `const`: the initializer is validated AT THE DECLARATION [`constexpr int x=argc;` → `S_ConstexprNonConstantInitializer` while `const int x=argc;` stays legal-lazy]; scalar arithmetic incl. floats + enum + null-only pointers; file-scope internal linkage per C23 6.2.2p3 via ONE config `linkageSpecifiers` entry [composes with static, no clobber]; rides the shared CST const-eval via an extracted `buildConstEvalEnv` [3 audit-named invariants preserved] + a float-capable `constExprValue`; ★ F2 also FIXED the pre-existing evaluator gaps — `'a'` char-literals + `true`/`false` fixed-value keywords now FOLD [`_Static_assert('a'==97)`/`_Static_assert(true)` flipped fail→pass], config-driven leaf arms, NullptrT filtered so `_Static_assert(nullptr)` stays loud; 5 diags 0xE037–0xE03B unsuppressable; design-audit GO-WITH-FIXES [F1 hook-hoist above the bare-declarator gate + F2] + code-audit VERIFIED-CLEAN [both crux claims CONFIRMED by reading: the env extraction byte-preserving incl. the dynamic/static scope-capture asymmetry, the evaluator additions leak-free behind 3 walls; 3 MEDIUM folded/pinned: the 6.7.1p10 exact-representability boundary → gated `D-CSUBSET-CONSTEXPR-EXACT-REPRESENTABILITY` + accepting-pin, the `'\xFF'` high-byte ICE-fold signedness question → gated `D-CSUBSET-CHAR-HIGHBYTE-ICE-SIGNEDNESS` (pre-existing cycle-B class, probe pending), the constexpr-assignment S_ConstViolation pin added]; 3-leg ctest **610/610** + sqlite 3.54.0/CRUD 42 on all 3 legs; 4 gated deferrals [aggregates, (T*)0-cast-null, exact-representability, char-highbyte-ICE]). ✅ **`[[attributes]]` SEMANTICS** (`D-CSUBSET-ATTRIBUTE-SEMANTICS` + `D-CSUBSET-ATTRIBUTE-STATEMENT`, 2026-07-09 — the LAST FC17 item; ZERO codegen; a config TABLE with a CLOSED verb set [suppressUnused/warnOnUse/warnOnDiscard/none]: `[[maybe_unused]]` SUPPRESSES S_UnusedVariable [was silently ignored]; `[[deprecated]]`+msg WARNS per use `S_DeprecatedSymbolUsed` 0xE03D [proto/def OR-merged]; `[[nodiscard]]`+msg WARNS on a discarded call `S_NodiscardResultDiscarded` 0xE03E [★ the design-audit caught the one-hop check would NEVER fire — the landed check is TWO-hop parent(call)==expression && parent²==exprStmt, config-driven; `(void)f()` suppression pinned]; `[[fallthrough]];` STATEMENT position now parses [attributeDeclaration + a `declOrAttrStmt` speculative wrapper replacing varDecl in the statement alt — the wrapper MATERIALIZES a CST node, consumer-transparent via the HIR soleMeaningfulChild peel, hot path stays zero-speculation unique-survivor direct descent, 1 corpus .tree regenerated]; unknown `[[attr]]` → SUPPRESSIBLE Warning `S_UnknownAttribute` 0xE03C [stdAttr-form only — C23 forbids fatal unknown std attrs; GNU file-scope keeps H000C]; GNU spellings honored via the linkageSpecifierIgnoredNames extension; design-audit GO-WITH-FIXES [F1 two-hop + F2 wrapper-ownership + F3/F4/F5] + code-audit VERIFIED-CLEAN [wrapper transparency + two-hop no-false-positive + suppressible-warnings ALL CONFIRMED by mechanism; the F6 budget cliff verified LOUD; 2 MEDIUM registry-text folds: the declarator-less unknown-attr miss + the for-clause discard]; 3-leg ctest **611/611** + sqlite 3.54.0/CRUD 42 on all 3 legs; 6 gated deferrals). **★ FC17 COMPLETE (all 5 features: nullptr · enum:T+parser-engine · typeof · constexpr · attributes) → USER-DIRECTED (2026-07-10): FC17.5 residuals BEFORE FC18. FC17.5 cycle 1 ✅ 2026-07-10: `{}` empty/scalar brace-init (`D-CSUBSET-EMPTY-INITIALIZER` 0xE03F) + `__func__`/`__FUNCTION__` (`D-CSUBSET-FUNC-PREDEFINED-IDENTIFIER` 0xE040 + the string-literal byte-content MEMOIZATION — C requires `__func__==__func__`) + the VLA DECISION (`__STDC_NO_VLA__=1`, conformantly-declared no-VLA; `D-CSUBSET-VLA`); battery modern 23/27; 613/613 × 3 legs + sqlite × 3. FC17.5 cycle 2 ✅ 2026-07-10: `auto` type-inference (`D-CSUBSET-AUTO-TYPE-INFERENCE` — a generic `inferTypeFromInitializer`+`requiredSpecifierToken` row capability; the design-audit's 3 CRITICALs folded [the auto-presence gate vs implicit-int; the branch order avoiding the 4096-token budget-cliff on existing static inits — sqlite's largest is 3918/4096; the full decay/Void/NullptrT/self-ref normalization vs the Pass-2 backfill]; 0xE041–0xE044 unsuppressable; battery modern 24/27, c23_auto_type GREEN; 614/614 × 3 + sqlite × 3). REMAINING FC17.5 majors: ★ **USER §B DECISION (2026-07-10): the `thread_local` TLS arc FIRST** (over FC18-now and _BitInt-first; rationale: complete the C11/C23 storage-class core before the conformance sweep; the consumer-less caveat [no shipped thread-creation API yet] accepted — the TLS machinery is real per-format substrate the sweep can then pin). The full-arc design + INDEPENDENT design-audit ✅ DONE (2026-07-10, GO-WITH-FIXES — the 3 load-bearing discoveries CONFIRMED to the byte level [always-dynamic ELF → PT_TLS loader-processed for free (empirically: even a zero-extern probe is PT_INTERP+DT_NEEDED); the symbolVa signed-tpoff reuse → ZERO reloc-kernel change on x86_64; fail-loud-for-free gates]; the audit CAUGHT 3 CRITICAL silent-miscompile seams, ALL folded into C1's mandatory scope [`&tls` in a static initializer would write a bit-cast negative tpoff as an abs64 pointer → C11 6.6p9 reject `S_ThreadLocalAddressNotConstant` 0xE048 + a walker backstop on TLS-targeting data relocs; the F5/c67 section overrides (asm.cpp:1055/:1127) would silently DEMOTE `thread_local char *msg="hi"` Tdata→Data = process-shared; the isThreadLocal flag would DROP at 3 positional addGlobal clone sites (mir_merge + the optimizer rebuild ×2) → non-defaulted param + propagation pins] + the REAL tpoff formulas [gcc-witnessed with _Alignas(32): memsz-based + p_align-rounded; arm64 = alignUp(tcbHeaderBytes,p_align)+offset] + the PT_TLS/PT_LOAD coverage spec [.tdata = a file-backed member of the data LOAD; conditional kNumPhdrs 5/6 so no-TLS images stay BYTE-IDENTICAL — the sqlite-dormant guarantee, pinned] + Tbss isZeroFill at the 3 Bss-equality chokepoints + the riprel-fold exclusion + `constexpr/typedef × thread_local` rejects; PIE REFUTED on ELF [DSS emits fixed-base ET_EXEC → link-time abs64 in the TLS template is sound]; full spec = memory `project_thread_local_arc_design_2026_07_10`). ★ **USER §B APPROVED (2026-07-10) — the 4-CYCLE split (user EXTENDED the arc with Mach-O as a real cycle):** **C1** ELF x86_64 local-exec (grammar+semantics+HIR/MIR flag+the fs: TlsBase encoder slot+`tls-tpoff32`+the PT_TLS walker+ALL 8 audit folds+the 2-THREAD pthread_create witness [glibc≥2.34 pthread-in-libc — the true per-thread-storage runtime discriminator] + structural pins) → **C2** arm64 ELF (MRS TPIDR_EL0 + ADD/ADD hi12/lo12 Variant I; qemu witness) → **C3** PE (.tls section + IMAGE_DIRECTORY_ENTRY_TLS + a writable `_tls_index` + the gs:[0x58]-indexed sequence + DIR64 .reloc sites for template pointers [PE IS rebased]) → **C4 Mach-O** (the TLV descriptor machinery: `__thread_vars` 3-word descriptors + `_tlv_bootstrap` + S_THREAD_LOCAL_* section types — the audit found the __DATA writer ALREADY EXISTS [the old gated-deferral rationale was stale]; ★ the RUNTIME witness = the CI `macos-latest` Apple-Silicon ctest leg at PR-END [macOS has NO off-Mac emulator — the cross-target closure discipline], locally C4 ships host-independent STRUCTURAL pins red-on-disable). Forks LOCKED per audit: A = arm64 ADD/ADD (over MOVZ/MOVK) · B = local-exec ONLY (initial-exec gated `D-CSUBSET-THREAD-LOCAL-INITIAL-EXEC` on a shared-lib profile) · C = the dead static-ELF arm stays unhardened (+ the cheap 0x8015 guard). ★ **C1 ✅ LANDED (2026-07-10, this branch — user proceeded in-session): thread_local RUNS on the WSL x86_64 leg** — the full audited scope incl. all 8 folds; 2 runnable witnesses (`thread_local_basic` + `thread_local_pthread` — the per-thread discriminator: workers both observe 105 from the template while main keeps 6; exit 42 debug+release); independent code-audit VERIFIED-CLEAN (tpoff physics re-confirmed vs gcc + a live glibc loader; 3 LOW folded: the MirThreadStorage strong-enum param, 6 registry rows incl. `D-CSUBSET-THREAD-LOCAL-CROSS-TU-MISMATCH` [cross-TU 6.7.1p3 mismatch = missed diagnostic, codegen CORRECT] + `D-CSUBSET-DECL-QUALIFIER-BEFORE-STORAGE-CLASS` [pre-existing `const static` class], 3 driver-tier E2E pins); arm64/pe64/macho + the static-ELF arm all fail loud (0x8015/gates); + the USER mid-cycle add `thread_local_pthread_header` (the REAL `#include <pthread.h>` via the shipped pthread.json — mutex-summed 210 → 42 ×2 arms) + `D-CSUBSET-C11-THREADS-HEADER` registered. 3-leg ctest **618/618** (Win Debug + WSL x86_64 + arm64 qemu) + sqlite 3.54.0/CRUD-42 × 3 legs (TLS dormant); battery modern 24/27 host-correct. **TLS C2 (arm64 ELF) ✅ LANDED (2026-07-10): thread_local RUNS under qemu-aarch64** — MRS TPIDR_EL0 fixed word + the ADD/ADD hi12/lo12 pair over the REUSED SymbolPatchMarker 2-word machinery (ZERO encoder diff, config-only — audit-verified); ONE new kernel formula `Aarch64TprelAddHi12` ((S+A)>>12 → imm12[21:10], FAIL-LOUD outside [0,0xFFFFFF] = the 16MiB cap [audit LOW-d], boundary-pinned both sides); the lo12 rides the EXISTING add-abs-lo12 FORMULA under a DISTINCT tls-flagged KIND (native ids 549/551 TLSLE _NC — gcc local-exec pairing) so the CRIT-1 XOR backstop covers arm64; the tlsbase LIR-shape asymmetry resolved by a capability PROBE on the target row's declared operandKinds (0-operand MRS vs x86's [MemOffset]+payload; x86 character-identical; neither-shape fail-loud red-pinned — the lowerDiv slot-probe precedent, zero arch identity); arm64 target tls{variant1, tcbHeaderBytes 16} + elf64-aarch64 format opt-in; Variant-1 physics qemu-WITNESSED (2-var tpoffs 0x10/0x14; _Alignas(32) → tpoff=alignUp(16,32)=32; the hi12≠0 composition: 5000-byte block → add #1,lsl12 + add #0x398 → 42); all 3 thread_local examples gained the arm64 arm — 6/6 witnesses exit 42 debug+release; independent code-audit CLEAN by mechanism (binutils-decoded every pinned word; re-derived the kernel arithmetic; independently witnessed hi12≠0 live), 3 folds landed (the 0x8015-before-opcode ORDERING red-pin restored + the tlsbase/lea $comment de-dup + the hi12≠0 structural E2E belt). **TLS C3 (PE/COFF x86_64) ✅ LANDED (2026-07-11): thread_local RUNS as a native pe64 exe in LOCAL Windows ctest** — the PeIndexed 4-instr access (mov rax,gs:[0x58] TEB ThreadLocalStoragePointer → 32-bit `mov ecx,[__dss_tls_index]` [CRIT-3 width] → scale-8 SIB module block base → lea +secrel32); ONE `.tls` section = the Tdata template with Tbss embedded as raw zeros (DEV-1: SizeOfZeroFill=0, the loader-doesn't-zero-early-main-thread case, MSVC-consistent, audit-verified sound); IMAGE_TLS_DIRECTORY64 + DataDirectory[9] + the writer-minted LINK-tier singleton `__dss_tls_index` (reserved SymbolId 0xFFFFFF01, fail-loud on multi-CU); reloc = SHARED target kind 4 (Linear) with the PE↔ELF split at the FORMAT tier (IMAGE_REL_AMD64_SECREL nativeId row — R3/CRIT-1, x86_64.target is shared) + a positive-secrel walker helper (NOT the variant-keyed negative one); the CRIT-1 backstop REPLICATED in pe.cpp (directory VAs as section-span, never tls-relocs → no false-reject); 3 DIR64 relocs (Start/End/Index). ★ A kernel32 CreateThread 2-thread witness DISCRIMINATES (workers 105/105 from template, main 6 → 42) — PE's per-thread proof. Independent code-audit VERIFIED-CLEAN (both deviations byte-level sound; all 3 CRITICALs decoded live) + 1 fail-loud fold (over-aligned PE TLS → loud K_ + D-CSUBSET-THREAD-LOCAL-PE-OVERALIGN, since IMAGE_TLS_DIRECTORY64 has no block-align field unlike ELF PT_TLS). Opened -PE-OVERALIGN + -PE-LARGE-ZEROFILL. **★★ TLS C4 (Mach-O TLV, arm64 Darwin) ✅ CODE-LANDED (2026-07-11) — ALL 4 LEGS OF THE thread_local ARC ARE BUILT.** The dynamic descriptor-call model (macOS has NO local-exec TLS): one 3-word descriptor per thread_local in __DATA,__thread_vars (word0 = a legacy bind to __tlv_bootstrap, word1=0, word2 = the 0-based block offset), init in __thread_data (S_THREAD_LOCAL_REGULAR), zero in __thread_bss (S_THREAD_LOCAL_ZEROFILL); access `adrp/add x0,desc; ldr x1,[x0]; blr x1` → x0=&x (all slots pre-existing incl. BLR; ZERO new encoder). ★ Mach-O is a THIRD TLS model — stores a REAL descriptorVA in symbolVa (ordinary reloc), so OPTS OUT of the tlsSymbols/XOR backstop [CRIT-1: only the decoupled arm-(a) data-item belt runs; `&t`→S0048+belt, plain thread_local links clean]; word2 = 0-based, no TCB bias [CRIT-2]; one __tlv_bootstrap bind per descriptor at word0 [CRIT-3]; over-align >16 → fail-loud 0x8016 [M-3]. USER §B DECISION (2026-07-11): build C4 now (the loop's Windows host cannot execute Mach-O), the USER runs the produced arm64 binaries on an Apple-Silicon Mac + the CI macos-latest leg = the runtime witness. Independent code-audit VERIFIED-CLEAN (the x29-FP-clobber-across-blr seam re-derived SAFE; all 8 cruxes re-derived from the produced binary; HIGH confidence it runs on a Mac) + 2 hardening folds (a fail-loud seg-index invariant guard + the FP-chain by-design note). ★ **`D-CSUBSET-THREAD-LOCAL` stays OPEN (runtime-PENDING) until the macos-latest CI leg [blocked by the PR#38 required-label — a MAINTAINER action] or the user's Mac witnesses C4 → 42, per the cross-target-runtime-closure rule.** THEN `_BitInt` stays gated (`D-CSUBSET-BITINT`) → FC18 (T3.3). (.tls + IMAGE_DIRECTORY_ENTRY_TLS(9) VA-based IMAGE_TLS_DIRECTORY64 + writable `_tls_index` + the gs:[0x58]-indexed sequence [mov rax,gs:[0x58] 64-bit — the C1 TlsBase primitive with config {0x65,0x58}; mov ecx,[_tls_index] riprel; the scaled-index quartet; the C1 MemRelocDisp32 lea] + DIR64 .reloc sites for template abs64s [PE IS rebased] + AddressOfCallBacks=0 [[D-CSUBSET-THREAD-LOCAL-PE-CALLBACKS]]) → THEN C4 Mach-O (TLV; CI macos-latest witness at PR-end). `_BitInt` arc (C1/C2/C3 ✅ + C4a ✅ 2026-07-12 [`_BitInt` bit-fields, `D-CSUBSET-BITINT-BITFIELD`]; C4b ✅ 2026-07-13 [wb/uwb arbitrary-magnitude literals + wrap-aware bignum const-fold; a from-scratch host bignum + 2 forks resolved at §B; design+code-audit, I1/I2 folds re-verified; 628/628 ×3 legs + sqlite ×3] CLOSED the whole `_BitInt` arc) → **★ full VLA — ARC OPENED 2026-07-13 (ACTIVE HEAD): the substrate probe CORRECTED the premise (`int a[n]` is a SEMANTIC reject `S_NonConstantArrayLength`, ZERO grammar work); §B forks DECIDED (user 2026-07-13) [A = `TypeKind::Array`+`kVlaLength=-2` sentinel+an out-of-band size side-table · B = TRUE dynamic stack `sub rsp,reg`+frame-pointer+SP save/restore, best-long-term + unlocks the `alignas` frame-model]; split C1a[type+semantic+HIR/MIR]/C1b[LIR/asm dynamic-alloca ×3 targets]→C2 sizeof→C3 multi-dim→C4 ptr-to-VLA+typedef+`[*]`/`[static]`→C5 block teardown; ★ **C1a ✅ LANDED 2026-07-13** (front-end+IR: `kVlaLength=-2` sentinel + `vlaArray`; Pass-2 `validateVlaDeclarator` [file-scope/non-integer `S_VlaSizeNotInteger` 0xE053/static/multi-dim rejects]; MIR runtime-operand `Alloca` → fail-loud C1b boundary `L_VlaDynamicAllocaUnsupported` 0xB00D; design-audit GO-WITH-FIXES + code-audit caught+fixed a CRITICAL non-integer-length silent-truncation; 628/628 ×3 + sqlite ×3; `__STDC_NO_VLA__` stays), **C1b ✅ LANDED 2026-07-13** (`int a[n]` RUNS on all 3 legs, LEAF scope: the dynamic-stack frame model — a VLA-only conditional frame pointer + `sub_sp_reg` [arm64 EXTENDED-register, reg31=SP] + SP restore, agnostic; `__STDC_NO_VLA__` REMOVED + `c99_vla` battery GREEN; design-audit folded 4 CRITICAL silent-miscompiles pre-code + the spilling test caught an FP-leak + code-audit folded 1 missed base-switch; `c99_vla`/`_spill`/`_structparam` → 42 on x86_64/arm64-qemu/pe64, 631/631 ×3 + sqlite ×3; new anchors `-NONLEAF-CALL-FRAME`/`-WIN64-UNWIND`/`-WIN64-STACK-PROBE`), **C2 ✅ LANDED 2026-07-13** (`sizeof <vla-object>` is a RUNTIME `size_t` — a Load of a hidden 8-byte size slot frozen ONCE at the decl [§6.7.6.2p2, `count*stride`]; the operand match peels transparent wrappers to the bare identifier so a COMPOSITE operand `sizeof(b,a)`/`sizeof(0,a)` is REJECTED → fail loud [C decays it to a pointer — never a silent wrong size]; ★ THE INVARIANT: const-eval keeps DECLINING a VLA sizeof [the SizeOf node's `vlaArray` TypeRef unchanged] → `_Static_assert(sizeof a==K)`→`S0029`, never a wrong constant; object-form ONLY, type-form `sizeof(int[n])` isn't parseable → deferred `-SIZEOF-TYPEFORM`; design-audit GO [invariant safe by construction] + code-audit caught+fixed a CRITICAL composite-operand mis-key; `c99_vla_sizeof`→42 on x86_64-elf/arm64-qemu/pe64 [release arm], 632/632 ×3 legs + sqlite 3.54.0/CRUD-42 ×3), **C3 ✅ LANDED 2026-07-13** (multi-dim VLAs RUN with a RUNTIME inner stride: `int a[n][m]`/`a[n][m][k]`/`a[n][5]`/`a[5][n]` — ALL block-scope shapes, LEAF; §B DECIDED [user] storage=Option-1 store-cumulative-strides + `a[5][n]` INCLUDED via a transitive `interner.typeContainsVla`; single-tier [semantic+cst_to_hir+hir_to_mir+interner+verifier, NO LIR/asm — a runtime stride is just more MIR Mul/Load]; nested `vlaArray(vlaArray(int))` type composes for free; ★ CRITICAL-1 convention [design-audit caught a self-contradictory level-map → `a[1][0]`→`a[0][1]`]: scale/size by the runtime `sizeof` of the SUBSCRIPT-RESULT type, slots keyed by `(sym, elemTy TypeId)` so store/load keys match by construction + index/row-sizeof/whole-sizeof UNIFY; `typeContainsVla` threads 8 `isVlaArray` gates, doesn't over-fire on `int[5][5]`; evaluate-once §6.7.6.2p2; design-audit GO-WITH-FIXES [3 CRITICAL folded pre-code] + code-audit SHIP [stride arithmetic hand-verified per-shape, keys match, 3 MINOR — reworded the misleading C4-deferred fail-loud message]; orchestrator-verified the off-diagonal `a[1][0]`==77; `c99_vla_multidim`→42 x86_64-elf/arm64-qemu[debug+release]/pe64, 633/633 ×3 legs + sqlite ×3), **C4 ptr-to-VLA/typedef/`[*]`/`[static n]` — §B DECIDED (user): cover all 4 across cycles; C4a-LOCAL ✅ LANDED 2026-07-13** (a LOCAL `int(*p)[n]` RUNS: `p[i][j]` scales by the runtime pointee size via a per-pointer stride slot frozen at the DECL SITE [design-audit CRITICAL-2: not the hoisted alloca] reusing C3's `scaleIndexToBytes`/`vlaStrideSlot` verbatim; the `captureVlaSize` gate widened for ptr-to-VLA; shared `computeVlaByteSize`/`storePtrToVlaStride` helpers refactored from `vlaAllocaForLocal` [behavior-preserving]; design-audit GO-WITH-FIXES [2 CRITICAL folded: decl-site placement + NOT swapping the init-derivation wholesale = brace-init mass-regression] + code-audit SHIP; witness = the assignment form `int(*p)[n]; p=b; p[i][j]`, off-diagonal `p[1][0]`==77 on all 3 legs, `c99_vla_ptr`→42, 635/635 ×3 + sqlite ×3; DEFERRED fail-loud: the `=b` init form [`D-CSUBSET-VLA-PTR-INIT-FORM-TYPING` — init node pre-stamped decayed] + `p+j` pointer-arith). **C4a-PARAM ✅ LANDED 2026-07-13** (a PARAMETER `int(*p)[n]`+adjusted `int a[][n]` RUNS: the callee `p[i][j]` scales by the runtime pointee size frozen in the callee prologue from param `n`; a ptr-to-VLA param emits only a FIXED 8-byte slot [no `sub_sp_reg`] → NOT leaf-restricted [CALLS freely], and a genuine VLA arg `int b[K][n]` decays+type-checks; GAP 1b = Option B [a DISTINCT `paramDecay` bool threaded through the 3 declarator-build signatures, checked FIRST at the nullopt site → present-length PARAM `[n]`=`vlaArray(-2)`, while the struct-FAM path keeps `allowFlexibleArray` → BYTE-IDENTICAL `StructFieldVlaSoleMemberStillFailsLoud`]; design-audit GO-WITH-FIXES + code-audit SHIP VERIFIED-CLEAN; `c99_vla_ptr_param`→42 x86_64-elf/arm64-qemu[release]/pe64, 636/636 ×3 + sqlite 3.54.0/CRUD-42 ×3; new anchor `-FIXED-ARRAY-ARG-COMPAT` [fixed `int b[2][2]` arg → S0003 fail-loud, stricter-than-C]). **C4b ✅ LANDED 2026-07-14** (VLA typedef `typedef int R[n]; R a;` RUNS + `R a;R b;` shared freeze + multi-dim `R[n][m]`/`R[5][n]`/`R[n][5]` + multi-declarator + const; C99 §6.7.7p2 freeze-at-typedef: `n` evaluated ONCE at the typedef, FROZEN — a probe mutating `n=100` after the typedef keeps `sizeof b==12`; reuses C1b/C2/C3/C4a substrate: a NEW `SymbolRecord::vlaTypedefOrigin` a→R [semantic I1 gate `declTy==headTy`, excludes `R a[m]`/`R *p`] + `captureVlaSize(R)` at `lowerTypeDecl` + the `TypeDecl` MIR no-op→`computeVlaByteSize(R,freeze)` + `vlaAllocaFromTypedef` copies R's frozen slots into a's own keys [I3 enumerate-frozen-only]; ★ the #1 hoist hazard does NOT apply [entry-alloca hoist skips VLA locals, design-audit verified]; design-audit GO-WITH-FIXES [I1 crux: token-keyed capture wouldn't fire→resolveDeclTypesPost] + code-audit SHIP-WITH-FIXES [no CRITICAL/silent seam; folded the CHAINED `typedef R S;`→clean fail-loud `D-CSUBSET-VLA-TYPEDEF-CHAINED`]; `c99_vla_typedef`→42 x86_64-elf/arm64-qemu[release]/pe64, 637/637 ×3 + sqlite ×3; deferred fail-loud: stacked-suffix + ptr-to-VLA typedef + chained). **C4c ✅ LANDED 2026-07-14** (array-param `static`/cv-qualifiers/`[*]` — `int f(int a[static 3])`/`[const n]`/`int a[*]` all decay to a pointer; parse-permissive + a paramDecay-gated `S_ArrayParamQualifierNonParameter` 0xE054 fail-loud for non-param uses; the `[*]` form needed 2 user §B decisions → a GENERALIZABLE parser-engine capability [schema compiler honors `speculative` on inline repeat-alts] + `arrayStarSuffix` + a guard-less `fnSuffixTail`; deferred lenient/fail-loud: const/volatile-enforcement, outermost-static, unnamed abstract `int f(int[*])`; design-audit GO-WITH-FIXES + code-audit SHIP; all 3 langs ctest byte-identical for the engine change; `c99_array_param_static`→42 ×3 legs + sqlite ×3) — **C5 (block-scope teardown) NEXT — the LAST VLA sub-arc**, then C11 `<threads.h>`; design [[project_vla_arc_design_2026_07_13]]** (`D-CSUBSET-VLA`) + **C11 `<threads.h>`** (`D-CSUBSET-C11-THREADS-HEADER`) [both user 2026-07-11 — remove the `__STDC_NO_VLA__`/`__STDC_NO_THREADS__` predefinedMacros when each lands] → FC18 (T3.3).** | plan-23 FC17 | T3.1 | 🟡 → ✅ pending gate |
| **T3.3** | C23 FC18 · 2–3 | **Preceded by (FC17.5 tail, user 2026-07-11): the `_BitInt` arc (C1 ✅ → C2/C3/C4) THEN a full VLA arc (`D-CSUBSET-VLA`) + C11 `<threads.h>` (`D-CSUBSET-C11-THREADS-HEADER`). ★ PRE-FC18 CONFIG GATE: remove BOTH `predefinedMacros` rows in `c-subset.lang.json` — `__STDC_NO_VLA__` (:61, when VLA lands) + `__STDC_NO_THREADS__` (:62, when `<threads.h>` lands).** FC18 broad C23 corpus + positioned-diagnostic conformance sweep | plan-23 FC18 + `D-DIAG-CORPUS-EVERY-CODE` | T3.1, T3.2, `_BitInt`, VLA, `<threads.h>` | ⏳ |
| **T4** | cleanup · rolling | registry-hygiene (flip stale-open sqlite-arc rows, e.g. `D-FFI-WINDOWS-KERNEL32-FUNCTIONS`) + Tier-4 deferrals: `D-CSUBSET-LOCAL-STATIC`, `D-PP-VA-OPT`, `_Pragma`, `#pragma once`, `D-PP-IF-UNSIGNED-INTMAX`, `D-CSUBSET-GLOBAL-AGGREGATE-RELOC-MEMBER` | various | — | ⏳ backlog |
| **V2-1** ∥ | ARM64 runnable + verified (~2–4) | Build **`D-LK10-ENTRY-ARM64`** (ARM64 `svc #0` syscall + GOT/PLT macro-op `ADRP`+`LDR`+`BLR` rows in `arm64.target.json` + per-format `processExit` for `elf64-aarch64-linux-exec`) so ARM64 binaries actually exit/run; THEN add `qemu-aarch64` to the ubuntu CI runner + flip `examples_runner`'s cross-host SKIP to RUN aarch64 under QEMU → execution-verify the whole ARM64 backend. Builds the QEMU verify harness V2-5 reuses. | [14](./14-linker-plan%20-%20tbd.md) 13X.1 · §8 testing | — (ARM64 emits; needs exit + QEMU) | 🟢 **IMPL DONE 2026-06-06 + PUSHED (PR #18) + CI-VERIFIED 2026-06-08.** CI: the native `ubuntu-24.04-arm` leg + Windows-MSVC + macOS are GREEN (real arm64, NOT QEMU — user's call); the two x86_64-Linux legs initially false-failed ONLY because the `integrated_tests` subprocess harness lacked the cross-ARCH run-gate (it spawned the AArch64 `arm64_exit` ELF on x86_64 → ENOEXEC) — the in-process `examples_runner` was already gated; FIXED 2026-06-08 by porting the arch+emulator gate to `integrated_tests/runner.cpp` (x86_64-without-qemu → SKIP; native arm64 → run). The plan-lock DISPROVED the "bucket-2 zero-code / JSON-only" premise: a runnable ARM64 binary through the REAL pipeline needed a genuine **fixed32 encoder substrate** — `Imm16` MOVZ (syscall-number load) + `Imm9`/`MemBaseNoScale` unscaled LDUR/STUR (the callconv requires `load`/`store` upfront) + `Imm12` ADD/SUB-immediate (prologue/epilogue frame adjust) — plus the `bl`→`call` generic-verb rename, the `syscall` (SVC #0) opcode, `frame_load`/`frame_store` pseudo-ops, and a `qemu-<arch>` run-capability in the harness (`run_binary` launcher-prefix + `examples_runner` cross-arch gate). §B (user): "expand V2-1 to include load/store now" → genuinely runnable. Byte-pinned (exact 20-byte `_start` trampoline + MOVZ/LDUR/STUR/ADD-imm) + `arm64_exit` corpus (exit 42, qemu-gated; compiles clean on every host). GOT/PLT dynamic-import macro-op (`ADRP`+`LDR`+`BLR`) NOT needed (exit = syscall, no extern) → stays deferred. ARM64 control-flow (cmp/branch/jmp/setcc/zext + the block-relative resolver) + a runtime load/store corpus are ✅ **DONE 2026-06-08** (the ARM64-FFI arc's cycle 5 — `examples/c-subset/arm64_control_flow` while-loop → exit 42 under qemu; closes `D-LK10-ENTRY-ARM64-CONTROL-FLOW` + `D-AS3-BLOCK-REL-IMM19/26` + `D-AS3-COND-CODE-ARM64`); large-frame scaled LDR/STR + shifted ADD-imm stays fail-loud-deferred (`D-LK10-ENTRY-ARM64-WIDE-IMMEDIATE`); **non-leaf x30-save is ✅ DONE 2026-06-08** (`D-LK10-ENTRY-ARM64-NONLEAF-LINK-REGISTER` — the runtime cause of the arm64-FFI SIGSEGV; `extern_call_elf` arm64 now genuinely runs to exit 42 under qemu + native arm64). 206→207→208 ctest. **Data-global path (3-OS parallel, tracked in §0 + the registry — NOT a separate stepper arc):** ELF `.rodata` ✅ runtime-proven on x86_64+arm64 (`D-LK1-ELF-EXEC-DATA-SECTIONS`, 2026-06-09); macOS Mach-O `__TEXT,__const` ✅ **RUNTIME-PROVEN on Apple Silicon** (`D-LK1-MACHO-EXEC-DATA-SECTIONS` / `D-LK3-RODATA` exec arm + the `D-LK6-AARCH64-ADDABSLO12-HIGH-VA` reloc-guard relaxation, 2026-06-09 — run **27192415923** `macos-clang-release` #184 ran `global_int` macos-arm64 → exit 42 on real Apple Silicon: a c-subset `int answer=42;` reaches an arm64-darwin binary's `__const` AND is READ at runtime [which also proves the high-VA ADD_ABS_LO12_NC reloc resolved]); PE `.rdata` shipped since LK4. **DSS now READS a data global at runtime on 3 OSes × 2 arches** (Win-PE x86_64, Linux-ELF x86_64+arm64, macOS-Mach-O arm64). **String-FFI-to-stdout parallel (also tracked here + the registry — NOT a separate stepper arc): the `#include <stdio.h>` + `puts("hello")` capstone (2026-06-09) now PRINTS `"hello\n"` + exits 42** — ✅ RUNTIME-PROVEN LOCALLY on linux-x86_64 (WSL) + linux-arm64 (qemu; flush-precedes-exit confirmed via strace), ✅ macos-arm64 RUNTIME-PROVEN (run 27212153591 #206 `shipped_include_puts` Passed → "hello\n"+exit 42 on Apple Silicon). Three coupled changes (Model 3 neutral shipped-lib descriptor + per-format `library` map [dissolves `D-FFI-SHIPPED-LIB-PLATFORM-SELECT` ✅]; ELF `encodeElfExecDynamic` `.rodata` [closes the dynamic arm of `D-LK1-ELF-EXEC-DATA-SECTIONS`]; hosted-format exit via libc `exit(3)` [§0.2 D13]); two user §B decisions (§0.2 D12 + D13). **FULL 3-OS × 2-arch stdout-FFI parity with the Windows `hello_puts`** (Win-PE x86_64, Linux-ELF x86_64+arm64, macOS-Mach-O arm64 — ALL run `#include puts("hello")` → "hello\n"+exit; linux + macos-arm64 added this cycle, macos-arm64 ✅ RUNTIME-PROVEN run 27212153591 #206). 212/212 ctest (the macho pins live in existing test binaries; the puts-capstone pins likewise ride existing binaries). (✅ infra RESOLVED 2026-06-10: `windows-msvc-release` Build was at the 45-min timeout ceiling — `D-CI-WINDOWS-MSVC-BUILD-TIME-CEILING` **CLOSED**; **MSVC PCH landed (§B option b: per-lib `PRIVATE` src PCH + shared `REUSE_FROM` test PCH + Debug canary)**, 214/214 green on BOTH the PCH Release + PCH-free Debug-canary paths; the PCH push `1284aea` ran all 5 CI legs GREEN (run 27245366191) with the windows Build comfortably under the ceiling — the prior `ddac0de` push timed out.) |
| **V2-2** ∥ | Language-agnostic shipped-lib ingestion ✅ (1 cycle) | ✅ **DONE 2026-06-06** — the language-NEUTRAL JSON shipped-lib descriptor (**`D-FFI-SHIPPED-LIB-DESCRIPTOR-AGNOSTIC` ✅ CLOSED**): shipped libs described ONCE in a neutral JSON form under `src/dss-config/shippedLibs/<platform>/*.json` (signatures encoded as hir-text type strings, e.g. `"fn(ptr<char>) -> i32"`), decoded by the SINGLE shared `parseTypeFromText` codec (extracted from `hir_text.cpp`) + a universal reader `src/ffi/shipped_lib_descriptor.{hpp,cpp}`; the resolver maps angle `<stdio.h>`→`<stem>.json` + the SEMANTIC phase injects the symbols into CU-root scope (the `builtinFunctions` seam) so the call resolves → externDecls → the UNCHANGED FF5 `synthesizeFfiFromSourceDecls` → linker import. `#include <stdio.h>` now runs with NO inline `extern` via `stdio.json` (which REPLACED the c-subset-source `stdio.h`); goal-2 = user-decl-wins; fail-loud `F_ShippedLibDescriptorMalformed`(0x501B) + `F_ShippedLibUnsupportedType`(0x501C). Per-language `shippedLibDirs` search-path UNCHANGED. **USER-PRIORITY — delivered.** | [11](./11-ffi-plan%20-%20tbd.md) FF11+ | — (FF11 core ✅ c21) | ✅ **DONE (2026-06-06)** |
| **V2-3** | artifactProfile AP2–AP4 (~3) | Driver/project-config enforcement + codegen-reads-profile + CLI diag rendering (AP1 schema field already closed — §0.2 D4). Registered-set vocab (cli/gui/lib/staticlib/script/sproc/transpile/shader/hdl). Gates steps 13+16. | [06](./06-artifact-profile-plan%20-%20tbd.md) | — | ✅ **DONE 2026-06-09 (AP2+AP3+AP4).** **AP2 ✅** — `.dss-project.json` loader (`src/program/project_config.{hpp,cpp}`, JSON via nlohmann, fail-loud `C_MalformedJson`/`C_MissingField` + unknown-key reject) + driver profile-enforcement gate (`Program::compileProject` rewritten: load→`GrammarSchema::loadShipped`→enforce `artifactProfile ∈ grammar.artifactProfiles()`→route by `routesToMultiUnit`→delegate; new `D_ArtifactProfileNotSupported=0xD010`, fail-CLOSED empty-set). Independent plan-lock resolved 2 design forks ON THE BAR (empty-set REJECT not accept = fail-loud; NO `CompileOptions::artifactProfile` dead knob → deferred to AP3) + caught the source-count routing parity (compileUnits vs compileFiles). 6 `D-AP2-*` deferrals pinned. RED-on-disable demonstrated. **AP3 ✅ 2026-06-09** — the profile↔FORMAT compatibility gate (§B-decided: AP3-as-codegen-plumbing would be a DEAD KNOB since no shipped profile's codegen differs from what its `(target:format)` already encodes; so AP3 = the profile's first REAL effect). Each object format now config-declares `artifactProfiles[]` (which profiles it SERVES — the format-side twin of AP1's language set; 4 runnable exec formats → `["cli"]`, all else empty/fail-closed); `compileProject` enforces `profile ∈ format.artifactProfiles()` after the language gate → new `D_ArtifactProfileFormatMismatch=0xD011` (e.g. a `cli` project on a dll format, or `lib` on an exec format, rejected at config-load). Shared registered-vocab extracted to `core/types/artifact_profile.hpp` (closes `D-AP2-PROFILE-REGISTRY-SPLIT` — 2nd consumer fired). Agnostic (generic set-membership, no profile-name branch); 2 RED-on-disable levers proven (vocab + gate); code-review SHIP-IT + silent-failure VERIFIED-FAIL-LOUD. Codegen-threading (profile→entry-symbol/subsystem) now trigger-gated on a non-format-redundant consumer (gui) via `D-AP2-COMPILATION-CONTEXT`. **AP4 ✅ 2026-06-09** — the per-language onboarding matrix: both gates exercised through `compileProject` across toy/c-subset/tsql-subset × declared/undeclared/served/unserved (8 strict three-sided gate-outcome cells in `ArtifactProfileMatrix` — exact gate-code + other-gate-code-absent + rc, so gate distinctness is load-bearing; red-on-disable demonstrated) + ONE real end-to-end emit (`RealCliProjectEmitsElfExecutable`: a real cli c-subset `main()` → ELF executable, `\x7fELF`-pinned) + docs (`docs/project-config-spec.md` + `language-config-spec.md` §11.6 reconcile). Profile→codegen *shape* stays deferred (`D-AP2-COMPILATION-CONTEXT`); §7 #2/#4 🟡, #1/#3/#5 ✅. Independent plan-lock (5 MUST-FIX folded) + 2 reviews SHIP-IT. **V2-3 (artifactProfile AP2–AP4) COMPLETE — next-up is V2-4 (program-api CLI).** |
| **V2-4** | program-api CLI (~2–3) | `dss-code-prime build my-project.dss-project.json`: project loader + driver + **positioned source-context diagnostic renderer** (DSS's OWN — `file:line:col` + caret; NO clang/LLVM). Makes the compiler a usable tool. **MUST include (user decision 2026-06-09; strict tests are an acceptance bar, not nice-to-have) — `D-DIAG-CLI-POSITION-RENDER-AND-ASSERT`:** **(A) ✅ DONE 2026-06-09 (commit `686095a`):** wired the CLI stderr drain to DSS's OWN positioned renderer `DiagnosticReporter::format()` (`--> file:line:col` + source line + `^` caret + related-location notes — hand-written DSS over SourceBuffer/SourceSpan, **NO clang/LLVM**). A `BufferRegistry` built in `runCusToTargets` from the CUs' trees resolves each buffer-bearing (parser/semantic) diagnostic's `BufferId`; buffer-less driver `D_*` keep the code-only line (per-diagnostic routing on `BufferId::valid()`). The renderer existed (`diagnostic_reporter.cpp`) but had NO production caller. `Tree::sourceShared()` added; e2e `CaptureStderr` test (`bad.c:2:12`+caret) with RED-on-disable demonstrated; **(B)** EXPLICIT position assertions (`.span` / `lineCol`, not code-only) on the `P_*`/`S_*` parser+semantic error tests (today they assert the code + sometimes `.actual`, almost never the position → a wrong-position regression passes silently); **(C)** corpus expected-compile-error + miscompile identification — extend `examples_runner` + `expected.json` with an `expectError`/`expectDiagnostic` (+ position) field so a malformed source asserts a SPECIFIC diagnostic at a SPECIFIC location, alongside the existing optimized==baseline negative-miscompile pins. "Very good error detection" (compile correctly + identify miscompiles + precise positioned diagnostics) is the V2-4 acceptance bar. | §8 program-api (step 16) | V2-3 (artifactProfile) | ✅ **DONE 2026-06-10 — Part A ✅ + Part B ✅ + Part C ✅ → `D-DIAG-CLI-POSITION-RENDER-AND-ASSERT` arc CLOSED.** (A 2026-06-09: positioned CLI diagnostics wired; B 2026-06-09: golden-DIAGNOSTIC harness `tests/analysis/test_diagnostic_corpus.cpp` pins every `P_*`/`S_*` diagnostic's code + line:col by construction — §B re-scope from per-test assertions, user-chosen.) **Part C ✅ 2026-06-10:** an `expectDiagnostics` manifest field (`[{code,line,col}]`) in BOTH corpus harnesses — the in-process `examples_runner` (`runErrorTarget`) asserts the EXACT positioned diagnostic multiset over the full `Program::compileFiles` path; the CLI-subprocess `integrated_tests` (`runErrorExampleViaCli`) asserts the CLI REJECTS (rc!=0) + emits each diagnostic's positioned `:line:col` (the Part-A renderer). New `examples/c-subset/undeclared_var_error` (`return missing_var;` → `S_UndeclaredIdentifier` at 2:12) + RED-on-disable; `.gitattributes examples/** text eol=lf` pins position-sensitive bytes; agnostic (JSON-declared, generic compare); independent review SHIP-IT. The miscompile-identification (optimized==baseline) discipline already lives in the runner (`optimizedPipelines`). The CLI `--project`/build surface already exists (AP2). 215/215 ctest. **Parser recovery hardening (2026-06-10):** `int x = ;` (a missing initializer) previously tripped a `P_BuilderInvariant` "scope stack non-empty" + a Missing-node EOF cascade (the `task_5647346f` deficiency, deliberately kept OUT of the Part-B corpus); FIXED via grammar-AGNOSTIC missing-rule synthesis at a stop-point (`synthesizeMissingRule` in `parser.cpp`; `isStopPoint` = schema `syncTokens`+FOLLOW, no language identity) → ONE positioned diagnostic at the `;`, the block scope balances, recovery resumes; `missing_init.c` corpus + RED-on-disable count pins (1/0/0 vs pre-fix 3/1/3) + a suppress-reset pin; toy/tsql goldens shed their spurious EOF cascade (the multi-level frame-unwind generalization = `D-PARSE-RECOVERY-MULTI-LEVEL-UNWIND`, low). Independent plan-lock (4 MUST-FIX folded) + code-review SHIP-IT (empirically verified the suppress). |
| **V2-4.X** | c-subset → **full C (ISO C23)** (~MANY cycles) | Upgrade the shipped `c-subset` to FULL C23 across the WHOLE pipeline (grammar/semantic/types/IRs/ABI/asm/linker/runner) + a complete preprocessor + aggregate ABI on ALL current targets. A big multi-cycle arc decomposed into 5 clusters / ~17 phases (FC1–FC17) so each ships green+runnable: **A** front-end completeness (`%`/casts/width-types/`goto`/multi-declarator — cheap wins; closes the div/mod + primitive-width anchors), **B** aggregate backend (the `type_layout` offset/align substrate → member-access codegen → enums/bitfields/rodata-aggregates), **C** aggregate ABI (struct-by-value + variadic per calling convention: SysV→Win64→AAPCS64+Apple→`<stdarg.h>` — all targets now), **D** preprocessor (macros + conditional compilation + include-expansion; PARALLEL/interleavable track; agnosticism = a §B at FC13), **E** C23 modern (`[[attrs]]`/`typeof`/`constexpr`/`nullptr`) + a broad conformance corpus reusing the V2-4 `expectDiagnostics` harness. Closes ~12 OPEN deferrals (3 cited successors — modulo / non-main-return / variadic — supersede already-shipped predecessors: 2 closed anchors + the `main` implicit-return feature); exotic long-tail (VLA/_Complex/_Atomic/threads/#embed/_BitInt) = named NON-blocking exclusions. 4 user §B scope decisions captured (C23-as-mainstream, full-PP, all-target-ABI, parallel-PP track). | [23](./23-full-c-plan%20-%20tbd.md) | V2-4 (D-DIAG harness reused) | 🟡 **IN PROGRESS — FC1 cycle 1 ✅ 2026-06-10** (the first full-C cycle, on the post-squash `feature/0-0-2-p3`): binary `%` modulo END-TO-END on x86_64 (PE+ELF) + arm64 (ELF+Mach-O) — config-only c-subset `%` (the `%=` compound newly live); the role-tagged `inputRoles`/`outputRoles` projection contract (positional indexing GONE from the div/mod projection; loader-validated registered vocabulary; red-on-disable levers via the schema-mutation substrate, RED demonstrated by flipping quotient↔remainder); the capability-driven `lowerDivLike` realization (native verb opcode [arm64 sdiv/udiv — pure JSON, + `neg` surfaced by the corpus' negative literals] → implicit-register pair by-role [x86] → generic rem = n−(n/d)·d expansion [arm64 mod] → fail-loud; zero identity branches — the plan-lock DISPROVED the old anchor's "regalloc pre-coloring exists" premise, that variant re-homed under D-ML7-2.5); ConstFold SMod truncated semantics + INT64_MIN/-1 Overflow guards on BOTH Div+Rem (fixed a pre-existing latent host-UB SDiv fold bug the UBSan leg would trip); C23 `'` digit separator (flip `_`→`'` + the universal flanked-by-digits scanner rule); `examples/c-subset/modulo` (4 arms, truncated-vs-floored runtime discriminator → exit 42 WITNESSED: PE local ctest + WSL x86_64 + qemu-aarch64; Mach-O arm64 runtime-pending the macos-latest leg) + `float_mod_error` (positioned fail-loud) + division corpus now cross-target (exit 14 qemu-witnessed on arm64). CLOSED: D-CSUBSET-MOD-OP-CODEGEN ✅ + D-CSUBSET-MOD-OP-CODEGEN-OUTPUT-INDEX-CONTRACT ✅ + D-MIR-TO-LIR-DIV-SEQUENCE-AGNOSTIC ✅; NEW D-LIR-MOD-MSUB-FUSION (peephole). Plan-lock GO-WITH-FIXES (5 MUST-FIX folded, incl. role-tag-the-input-side + the separator flanked-by-digits rule) + code-review SHIP-IT (no CRIT/HIGH/MED; encodings re-derived bit-exact). 217/217 ctest (+2 examples). **FC1 cycle 2 ✅ 2026-06-10 → FC1 COMPLETE (zero deferrals left, per the user's no-deferrals directive):** C23 hex-floats (`0x1.8p3`) via a per-prefix `float` continuation on NumberPrefix (own DECIMAL `exponentDigits` class vs the hex mantissa; exponent REQUIRED; committed-incomplete → ONE malformed token; entry gate admits `.` after a floating prefix so `0x.8p3` lexes); C23 decimal edge forms CLOSED IN-CYCLE rather than anchored (`trailingFraction`/`leadingFraction` opt-ins — `1.`/`.5` are floats in c-subset; default-false preserves every other language); decode de-hardcoding (decodeFloat hoisted to number_decode.hpp — the old strip-every-'f' CORRUPTED hex mantissas, `0x1.fp3` 15.5→8.0, red-on-disable demonstrated; decodeInteger prefix-radix config-driven [the 0x/0b/0o/0 hardcode returned 0 for `$ff`], trailing-suffix-first, a-z digit map); loader shared exponent parser + float-block validation (unknown-key rejects, exponent-letter-inside-digit-class dead-config reject); engine genericity pinned on the synthetic schema (`$`/`^`/binary-exp-digits + `%`/`@`/sign-not-optional/octal). NO runtime corpus — float values are runtime-unobservable until FC2 casts (empirically probed: FCmp has no LIR lowering; returns reject semantically); the value proof is exact-double decode pins per the §A.5 carve-out, FC2 row owns the first float runtime corpus. Code-review CLEAN (no CRIT/HIGH/MED; 1 hygiene + 1 NIT folded). **Mach-O-arm64 modulo/division witness FLIPPED: the macos-clang-release leg ran GREEN on the c1 push (run 27276320850) — exit 42/14 executed on Apple Silicon.** 218/218 ctest (+test_number_decode). **FC2 ✅ 2026-06-10 (cycle 3) — FULL C-style casts + the FIRST float runtime witnesses** (§B-resolved by the user: "most complete, best agnostic, long-term, no workaround" — full typedef-name casts, NOT a keyword subset): `castExpr` speculative operand alt + `castOperand` second expr-entry rule at prefix precedence 90 (NO loader extension needed — per-rule `minPrecedence` was already loadable); the lone-`(Identifier)` cast-vs-expression ambiguity resolved by the **binder sketch** (parser-side scoped name→type/value map consuming the EXISTING `semantics.declarations`/`scopes` vocabulary via the new `commitRequiresTypeName` shape field; 4-rule triage with the ambiguity test DERIVED from the operator table; sketch state captured/restored in BOTH SpeculationProbe and the Pratt WalkerSnapshot — the WalkerSnapshot half REMOVED 2026-06-12 by the associativity wrap-in-place fix, `D-PARSE-INFIX-ASSOCIATIVITY-STRUCTURAL`) + the **include-oracle one-shot reparse** at `UnitBuilder::finish()` (post-resolver pre-freeze; crossRefs rebuilt on fresh TreeIds) for #include-delivered typedefs; semantic `semantics.casts` + `S_InvalidCast` + `isExplicitCastable`; HIR `castRule`→explicit makeCast (mapCast already complete). Backend: `mandatoryPrefix` on TargetEncodingTemplate (before REX, fixed32-rejected, disasm round-trip) + ADDSD/CVTTSD2SI/MOVAPS/movsd_load/movsd_store SSE encodings + F64 literals→anonymous-rodata at HIR→MIR (FPR LiteralIndex retired fail-loud) + the `registerClassOps` per-class move/load/store table at EVERY copy/spill site (row-less non-GPR → fail-loud) + `requireF64Width` guards. **Corpus: float_cast exit 16 RAN on Windows PE (the corpus SURFACED the ms_x64 callee-saved-XMM store gap — closed in-cycle: movsd_store + the fpr store row) + linux ELF (WSL incl. release pipeline); typedef_cast (value-DIVERGENT shadowing witness — same `(myint)-X` form is cast-in-main/subtraction-in-helper, mis-parse exits 52) + include_typedef_cast (oracle witness) exit 42 on PE+ELF+qemu-arm64 + **Apple Silicon (run 27302630912 — ALL 5 legs GREEN on 836ba71, the Mach-O arms EXECUTED)**.** Red-on-disable ×7. Plan-lock GO-WITH-FIXES (3 MF folded: oracle seat, FPR-move breadth ×5 sites, fold-resistance = function-bodies-never-global-inits [HIR const-eval folds float globals]) + review SHIP-IT (3 LOW folded incl. spec §11.7 + the §12.5 Model-3 staleness). NEW anchors: D-ARM64-FLOAT-SUBSTRATE (x86 = the exact template), D-CSUBSET-CAST-ARRAY-DECAY, D-CSUBSET-CAST-VOID-DISCARD, D-CSUBSET-COMPOUND-LITERAL-TYPEDEF, D-LIR-GLOBALADDR-LOAD-RIPREL-FOLD (peephole). 225/225 ctest (+4 binaries, +3 examples). **arm64-FLOAT parity ✅ 2026-06-10 (cycle 4, user-fired trigger "fully work in arm64 as it did in x86") — `D-ARM64-FLOAT-SUBSTRATE` CLOSED, DECLARATION-ONLY (zero engine changes, exactly as the anchor predicted)**: arm64.target.json d0-d31 fpr registers + FADD/FCVTZS/FMOV/LDUR-D/STUR-D fixed32 encodings (every word re-derived from the ARM ARM + cross-assembled against GNU aarch64-as by the independent review — zero findings) + `registerClassOps` fpr row + aapcs64/apple_arm64 argFprs d0-d7/returnFprs d0/calleeSaved d8-d15/callerSaved d0-d7∪d16-d31 (the callerSaved completeness is load-bearing — regalloc treats non-callerSaved allocatables as call-safe; omitting it = latent cross-call clobber); fadd is NATIVE 3-op (no requires2Address — the per-opcode flag discriminates x86's 2-addr ADDSD from arm64's FADD with zero engine logic). 6 byte-pins + 2 full-pipeline tests (d-reg arg path, d8-d15 fstur/fldur spills, rodata-F64 ADRP/ADD 2-reloc). **§A.5: float_cast arm64-ELF → exit 16 EXECUTED under qemu (local); the Mach-O arm runtime-pending the macos leg on this push.** Red-on-disable ×2. 225/225 (pins ride the existing arm64 binary). **FC3 c1 ✅ 2026-06-10 (cycle 5, on `feature/0-0-2-p4` post-#20-squash) — the COMPLETE width/signedness front-end semantics** (§B: user chose "Split: c1 semantics + c2 32-bit ALU, don't stop between cycles"; plan-lock GO-WITH-FIXES 7 MF folded incl. the 3 premise corrections [analyze() signature, 2-tier ladder restructure, arm64-lacks-sext/trunc]): 9 new keywords + `typeSpecifierSeq` multiset grammar + the `semantics.typeSpecifiers` table (31 C 6.7.2 multiset rows, canonical sorted-token resolution, S_InvalidTypeSpecifierCombination on undeclared combos, `long double` rejects by absence); **`dataModel` on the FORMAT schema** (REQUIRED closed enum LP64/LLP64/ILP32 — pe→LLP64, elf/macho→LP64, wasm/spirv→ILP32-declared-only [S_UnsupportedDataModel if selected]; loader+validate reject missing/unknown; plan-lock-corrected from "target": x86_64 serves BOTH OSes) threaded `format.dataModel()`→`analyze()` (defaulted-LP64-identity param) →`SemanticModel`→HIR; `coreByDataModel` overrides (long→I64/LLP64→I32); the **C 6.4.4.1 literal ladder** as ONE shared header (`integer_literal_ladder.hpp`) called from BOTH tiers (decode-first + suffix re-derived vs numberStyle; type-NAME candidates resolved dataModel-aware; S_IntegerLiteralTooLarge); the **UAC engine** (`arithmeticConversions` closed-verb block: minRankType+alsoPromote+`rank-prefer-unsigned`+promoteComparisons → `usualArithmeticCommonType` in type_rules.hpp consumed at 3 HIR combine sites + the C 6.5.7 shift left-operand rule; block-less toy/tsql byte-identical, dataModel-invariance PINNED); `signatureByDataModel` descriptor overrides (fseek/ftell/atol/strtol/strtoul/labs LLP64 i32/u32); `requireNativeIntWidth` guard (U8-U32/I8/I16/I128/U128/Char/Byte/Bool ALU fail-loud citing `D-CSUBSET-32BIT-ALU-FORMS`; I32 exempt — signed overflow UB makes 64-wide conforming). CLOSED: **`D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH` ✅** (the dataModel axis + both surfaces: keyword + descriptors) + **`D-CSUBSET-UDIV-RUNTIME-HIGH-BIT-PIN` ✅** (at U64 width — high-bit 65535⁴ dividend, udiv=42/sdiv=7 discriminator). Corpus: unsigned_division + uac_mixed_sign (-1ll>0ull) + datamodel_long_width all WITNESSED exit 42 (PE native + WSL + qemu; Mach-O arms = macos leg) + datamodel_long_width_llp64 (the SAME long vocabulary expectDiagnostics-REJECTS S_TypeMismatch@21:14 on the PE arm — the dataModel red-on-disable; a DEFINED-behavior runtime-divergent LP64/LLP64 witness is impossible until c2's 32-bit ALU). Red-on-disable ×3 (pe64-model-flip, guard-off, UAC-verb-flip). Review SHIP-IT (zero findings ≥80 — UAC/ladder/multiset all re-derived vs the standard). 230/230 ctest (+test_fc3_width_semantics 37 + 6 MIR + 5 LIR pins + 4 examples). **FC3 c2 ✅ 2026-06-10 (cycle 6, same session) — the 32-bit ALU width axis: `D-CSUBSET-32BIT-ALU-FORMS` U32/I32 arc CLOSED** (LirInst.flags width carrier + `guard.width` variant discrimination via ONE matcher shared by both walkers + `widthFlagsForType` single threading point; x86 no-REX.W variants [SDM-re-derived] + trunc's first encoding [mov r32,r32]; arm64 W-forms + NEW SXTW sext + W-mov trunc [ARM-ARM-re-derived]; conversion gates added — Trunc-non-32/SExt-non-I32/ZExt-non-Bool, the last walling a pre-c2 LATENT movzx-one-byte miscompile → `D-CSUBSET-ZEXT-32-TO-64`; lowerConst full-32-bit-range exact imm32). **Corpus: u32_wraparound + uac_mixed_sign_u32 + unsigned_division_u32 exit 42 ×3 legs; datamodel_runtime_width = BYTE-IDENTICAL source → 42 on Windows (U32 wrap) / 23 on linux+arm64 (U64) — the RUNTIME LP64/LLP64 divergence, native per leg; Mach-O arms = macos leg.** Shifts honestly out (no encodings either target ANY width — assembler wall, anchor residue). Review SHIP-IT (every byte re-derived; 2 LOW folded). 236/236. **FC3 COMPLETE → next: FC4 (function-pointer declarators) per plan-23 cluster A; ~~Mach-O witnesses of c1+c2 + the c4 float_cast macho arm flip at next Step 0~~ — the flip EXECUTED (macos leg GREEN, run 27341390786 on `966c977`; recorded at §0; this stale tail struck by audit-residue sweep c2 2026-06-11, `D-AUDIT-PLAN-STALENESS` ✅). FC3.5 (the deferral-closure sweep, c1+c2+c3) ✅ COMPLETE 2026-06-11 — see plan-23 §0/§0.1. **FC4 ✅ COMPLETE 2026-06-12 — c0 ✅ (`4ba1884` — declared-order speculative probing + the prefix-strip shared helper) + c1 ✅ 2026-06-11 (the C11 specifier/declarator substrate: fn-ptr declarators, typedef-decl-position, multi-declarators, storage/qualifiers, attr syntax, visibility — plan-23 FC4 row + §0.2 D14; 261/261; PR #22) + c2 ✅ 2026-06-12 (indirect-call codegen end-to-end: the semantic callee peel + `checkCallAgainstSig` shared tail [0xE015 retired, `(helper)(...)` upgraded to arity-checked] + HIR deref-of-fn decay fold + `calleeSigOf` coercion in both call arms + LIR Reg-callee materialization + the 3-layer cc-config-driven callee-reg exclusion [R2 red-on-disable demo'd + B2 + B1 0xB00B] + same-mnemonic x86 `FF /2`/arm64 `BLR` variants + corpus `fnptr_call`/`fnptr_callback` [self-recursive, un-devirtualizable]/`fnptr_regalloc_pressure` ALL exit-42 PE/WSL/qemu, Mach-O = macos leg; closes `D-CSUBSET-FNPTR-INDIRECT-CALL` + plan-12 `D-ML7-2.4` [sketch double-corrected]; 263/263). **FC5 ✅ COMPLETE 2026-06-15 (289/289)** — `goto`/labels (unstructured CF, ZERO MIR-substrate change) + truly-empty statement + the comma operator (two-level `assignmentExpr` gate) + the `entryFunctionNames` de-conflation (`D-LK10-ENTRY-MAIN-IMPLICIT-RETURN` ✅); fail-loud `S_DuplicateLabel`/`S_UndefinedLabel`; 7 runnable/diagnostic corpus examples (optimized==baseline over the goto CFG); plan-lock GO-WITH-FIXES + review SHIP-IT (GCC-13-verified). NEW named exclusions `D-CSUBSET-COMPUTED-GOTO` + `D-CSUBSET-LABEL-BEFORE-CASE` (fail-loud). **FC6 ✅ COMPLETE 2026-06-15 (292/292) — cluster B aggregate-layout substrate, realizes `D-FF3-1` (layout half)**: a generic `type_layout` engine (`src/core/types/type_lattice/type_layout.{hpp,cpp}` + `aggregate_layout.hpp`'s `AggregateLayoutParams{scalarAlignment, maxAlignment}`) computes struct/union/array field offsets + alignment + padding + total size + FAM handling from per-`.target.json` `aggregateLayout` params, into an `ArenaAttribute<TypeInterner, StructLayout>` side-table (NOT the interned TypeRecord — layout is target/ABI-dependent); fail-loud nullopt on out-of-scope kinds + non-last FAM. **FULL expression-context `sizeof`** wired end-to-end (grammar `sizeofExpr` speculative `sizeofType`/`sizeofValue` → semantic stamp [operand + `size_t`/U64] → HIR `makeSizeOf` → MIR const-fold via `computeLayout`). **FAM front-end**: `incompleteArray` lattice type + struct/union-field array declarators (optional `arrayDeclSuffix`) + a config-driven `allowFlexibleArray` `DeclarationRule` flag (struct field only — a standalone `int x[]` still fails loud, no regression). 2 layout-sensitive runnable corpus (`sizeof_flexible_array` exit 42, `sizeof_struct_layout` exit 42) + 12 `test_type_layout` engine pins (red-on-disable: maxAlignment cap divergence, non-last FAM) + 2 fail-loud diagnostic pins (`incomplete_array_local`, `sizeof_in_array_dim`). Independent review SHIP-IT (no CRIT/HIGH/MED). NEW named exclusions `D-FC6-SIZEOF-CONST-CONTEXT` (sizeof in array dims / global inits — fail-loud, blocked by the target-dependent-type architectural question) + `D-FC6-FAM-SEMANTIC-CONSTRAINTS` (positioned FAM-misuse diagnostics; the dangerous mislayout case already fails loud). **FC7 ✅ COMPLETE 2026-06-17 (all anchors + struct by-value all 4 ABIs; 316/316). Array-storage cluster + width-exact integer memory ✅ 2026-06-17 (`D-MIR-STORAGE-ARRAY-INDEX-GEP` + `D-MIR-ARRAY-FIELD-AGGREGATE-INIT` via Option A agnostic MIR element scaling; + the found+fixed pre-existing width-8-int-memory silent miscompile `D-LIR-INT-MEMORY-WIDTH-EXACT` [width-32/16 mem encodings, all 4 targets]; 316/316). **Top-level struct/union/enum head grammar + aggregate-global codegen ✅ COMPLETE 2026-06-17** (`D-CSUBSET-STRUCT-BODY-VARDECL-POSITION` ✅ — body + bare tag-REF heads parse/compile/RUN incl. nested/recursive fields; the schema-compiler FOLLOW root-fix `D-PARSE-SCHEMA-NESTED-NULLABLE-FOLLOW` ✅; aggregate-global rodata codegen `D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL` ✅ — initialized struct/array globals byte-exact, corpus `struct_body_top_level` exit 42 on x86_64-PE + arm64-ELF/qemu; `int ;` now fails loud `S_DeclarationDeclaresNothing`; dead structDecl/unionDecl/enumDecl rules deleted; NEW pre-existing fail-loud anchors pinned: `D-CSUBSET-ENUM-INT-CONVERSION` + `D-CSUBSET-TYPEDEF-ENUM-TRAILING-COMMA`; 318/318; review SHIP-IT). **FC8 cycle 1 — enums end-to-end ✅ 2026-06-17** (`D-CSUBSET-ENUM-INT-CONVERSION` closed; 319/319). **FC8 cycle 2 — C bit-fields ✅ 2026-06-18** (`D-CSUBSET-BITFIELD` closed; 320/320). **FC8 cycle 3 — non-string array→ptr decay ✅ 2026-06-18** (`D-LK4-RODATA-PRODUCER-LOCAL-ARRAY-DECAY` + `-NONSTRING-GLOBAL-ARRAY-DECAY` closed; 321/321). **FC8 cycle 4 — bit-field aggregate init ✅ 2026-06-18** (`D-CSUBSET-BITFIELD-INIT` closed; 322/322). **FC8 cycle 5 — enum-typed bit-fields ✅ 2026-06-18** (`D-CSUBSET-ENUM-BITFIELD` closed; 323/323). **FC8 cycle 6 — typedef-enum trailing comma ✅ 2026-06-18** (closed, reconciliation). **FC8 in-scope COMPLETE.** **§B RESOLVED 2026-06-18 (user decision):** build ALL 3 remaining FC8 bit-field deferrals via dedicated cycles — best-long-term / config-driven / no-workarounds / no-follow-ups — BEFORE FC9; the triggers are FIRED by user authorization (a supervised §D opening), they are now ALL ✅ DONE (FC8 SUBSTRATE COMPLETE — plus the sibling `D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR` ✅ CLOSED 2026-06-18; **FC9 is the next block**): `D-CSUBSET-BITFIELD-WIDE-UNIT` ✅ DONE 2026-06-18 (closed — refuted "64-bit ALU" premise; real gap was wide-const materialization: `mov r64,imm64` [x86] / MOVK ladder [arm64], capability-probed; +latent copyLiteralPool fix; 324/324), `D-CSUBSET-BITFIELD-RVALUE-RUNTIME` ✅ DONE 2026-06-18 (closed via design B — a synthesized-slot carrier in `lowerLvalueAddress`, NOT the design-A InsertValue→memory which contradicts the established memory-based aggregate model; GENERAL for all aggregate rvalues incl. non-bit-field + the discard position; +232-line dead-SSA-path deletion; opened sibling `D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR` for aggregate ternary/comma; 325/325), `D-CSUBSET-BITFIELD-ABI-EXACT` ✅ DONE 2026-06-18 (closed — `msvc_straddle` for PE [cl.exe byte-exact] / `gnu_packed` for ELF+Mach-O [Apple==gnu_packed for bit-fields]; strategy resolved at the FORMAT level [format-wins, target-fallback]; conformance witness shells to the native compiler; 326/326) — **ALL 3 §B BIT-FIELD DEFERRALS CLOSED** (and the sibling `D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR` — aggregate ternary/comma rvalue — ✅ CLOSED 2026-06-18, commits 98c52e3+1a4d562). **FC13 cycle 1 (Cluster D preprocessor — the PARALLEL/INTERLEAVABLE track, user-directed pick) ✅ 2026-06-19 (worktree p13 / branch feature/0-0-2-p13): config-SELECTED pre-parse C preprocessor — object-like `#define`/`#undef` + expansion (rescan/blue-paint self-ref) + textual quote-`#include` (text-concat → ONE synthesized buffer + line-map for diagnostic attribution); function-like macro def fails loud (→ FC13 cycle 2 ✅ 2026-06-19, `D-PP-FUNCTION-LIKE-MACRO` CLOSED); a `directive`/`header-context` lexer-mode split fixes the catastrophic `<`-in-`#define` mis-lex; §B (user) = text-concat+line-map; 330/330; review SHIP + dss-audit-folded (agnostic `functionLikeOpenToken` + full origin-buffer diagnostic attribution [closes `D-PP-INCLUDE-SPLICE-POSITION-ATTRIBUTION`] + quote-include double-diag gate). See the plan-23 FC13 row. **FC13 ✅ 100% COMPLETE 2026-06-19 (cycles 1–4 — object + function-like + variadic macros + textual quote-#include + the precise per-token hide-set; `D-PP-FUNCTION-LIKE-MACRO`/`D-PP-VARIADIC-MACRO`/`D-PP-MACRO-HIDESET-PRECISE`/`D-PP-INCLUDE-SPLICE-POSITION-ATTRIBUTION` all CLOSED; oracle kept per user §B; `#`/`##` = FC15; PR #29).** **FC13-hardening cycle ✅ 2026-06-20 (PR #29; 333 ctest):** the parser deep-expression-nesting GUARD (positioned `P_ExpressionTooDeep` + graceful recovery at the single `parseExpressionAt` chokepoint, default cap **16**) — `D-PARSE-EXPR-DEPTH-GUARD` guard delivered — plus the FC13 PP-fatal parse-gate (`PreprocessResult::fatal` → EOF-only parse that still carries the PP diagnostics, halting the truncated-stream cascade) — `D-PP-FATAL-HALTS-PARSE` ✅ CLOSED. A deep-safe-pipeline + high-cap (256) attempt was EXPLORED and REVERTED: the ~25-level overflow is DOWNSTREAM of the parser (semantic/HIR/MIR recurse on the main ~1 MB stack) and a pre-existing c-subset speculative-operand O(N²) makes deep input HANG before the guard — both promoted to a dedicated follow-up cycle (`D-PARSE-DEEP-FRONTEND-STACK` + `D-PARSE-SPECULATION-OPERAND-QUADRATIC`), the two named blockers to raising the cap. **Speculative O(N²) FIXED 2026-06-20 (PR #29):** `D-PARSE-SPECULATION-OPERAND-QUADRATIC` ✅ CLOSED via a config-driven LL(k) PREDICTIVE PRUNE — `computePredictivePrefixes` precomputes a per-rule FIRST_k prefix at schema-load (sound over-approximation, stops before a nullable element) + the speculative AltChoice prunes candidates whose prefix can't match the next tokens + unique-production direct descent; the cast-vs-paren `((` no longer probes castExpr ⇒ O(N) (n=300 154s→0.17s, ~1100×); review-folded (nullable-stop soundness guard + `D-PARSE-PREDICTIVE-PRUNE-CONTEXTUAL-KEYWORD` for the tsql contextual case + synthetic red-on-disable tests); agnostic + 333 ctest. **Deep-frontend-stack CLOSED 2026-06-20 (FC13 cycle 2):** `D-PARSE-DEEP-FRONTEND-STACK` ✅ CLOSED — new `core/substrate/large_stack_call` facility (`runOnLargeStack`/`callOnLargeStack`, 64 MiB reserved worker stack, synchronous-join + exception-rethrow, fail-loud on every thread-API/caller-contract violation, agnostic host utility); BOTH the CU build (parse/preprocess/oracle in `Program::compileFiles`/`compileUnits`) AND semantic `analyze` now run on it (the prior "only analyze" finding was insufficient — the raised cap exposed a parse-side overflow the low cap had masked; HIR/MIR lowering ALSO wrapped via buildCuMir (audit-caught: they recurse per-level on the main thread); only the flat-SSA LOWER half (MIR->LIR->codegen) needs no wrap). **Cap RAISED 16→256** — now a real SEMANTIC limit (parse O(N) + analyze/parse on the big stack); deep-but-legal C compiles end-to-end (n=100/200/250 → exit 0, ~0.2s) and only pathological nesting past 256 trips the positioned `P_ExpressionTooDeep` (n=300 fails loud fast). **BOTH cap-raise blockers now CLOSED** (`D-PARSE-SPECULATION-OPERAND-QUADRATIC` + `D-PARSE-DEEP-FRONTEND-STACK`). Re-added the runnable witness `examples/c-subset/deeply_nested_expression` (200 parens → exit 7, PE+ELF); 335 ctest. **Residual deep-nest super-linearity CHARACTERIZED 2026-06-22 (commit 954ad97):** the wall-clock super-linearity remaining after the O(N²) prune fix is a constant-factor memory cost of the N-deep HOST recursion, NOT algorithmic (the parse WORK is O(N) — controlled flat-vs-nested experiment + `ParserSpeculation.FlatChainParseWorkIsLinear` pin); bounded/moot under the 256 cap (downstream frontend is recursion-bound to the same cap), anchored trigger-gated as `D-PARSE-DEEP-NEST-RECURSION-MEMORY` (then no fix — 335 ctest). **[SUBSEQUENTLY BUILT 2026-06-24 — plan 24 iterative-traversal arc CLOSED:** the parser's 4/5 deepening expr arms + ALL of cst_to_hir/hir_to_mir/semantic/const-eval flattened onto explicit heap work-stacks (host recursion → O(N)); `maxExpressionDepth` made config-driven and raised 256→1024; the paren/postfix parser arm (Stage 5b) is a documented §B high-bounded-cap residual. 388 ctest. See plan 24.]** FC8 SUBSTRATE COMPLETE — FC9–FC11 ✅ (cluster-C aggregate ABI delivered early by FC7 Phase 2; reconciled 2026-06-19); FC12a ✅ + FC12b ✅ + FC12c ✅ (SysV scalar+struct-InRegisters + Win64 scalar+FP + AAPCS64 dual-cursor / Apple homogeneous scalar+FP — all variadic-callee, qemu+PE-witnessed [Apple Mach-O macos-leg-pending]; struct-vararg + fixed-overflow edges pinned per CC); FC12 (variadic ABI) COMPLETE. **FC12-deferral-closure loop (user-directed) IN PROGRESS: ① `D-FC12A-VARIADIC-MEMORY-CLASS-STRUCT` ✅ 2026-06-20 (SysV by-value struct varargs on the overflow stack — the Option-C `ByValueStackArg` carrier, reused by ②③); ② Win64 `D-FC12B-WIN64-STRUCT-VARARG` ✅ 2026-06-20 (reused appendByValueArg + the existing Win64 slot placement — no carrier needed); ③ AAPCS64/Apple `D-FC12C-AAPCS64-HFA-STRUCT-VARARG` ✅ 2026-06-20 (HFA dual-cursor VR-gather + ≤16B-GPR + >16B-by-ref callee va_arg + caller per-class atomic-fit/carrier; Step-2.0 size-aware HomogeneousPointer bump fixed the Apple 16B-HFA under-bump); **§B 2026-06-20 (user → "commit ③ now, fix next"): the loop's Step-8.5 self-audit + CI verification found `varargs_win64_struct` (FC12b ②, 41a64d7) crashes the COMPILER on ALL 4 non-Windows CI legs (linux-asan/macos/linux-gcc/linux-arm64) under its optimized-pipeline arm — the Windows-only local gate masked it; leading hypothesis = Mem2Reg promoting the ABI-aliased `va_list` alloca to SSA (`D-OPT-VALIST-ALLOCA-MEM2REG`). ③ is clean+independent (its corpora have no opt arm) → committed now; the crash fix is the IMMEDIATE NEXT cycle.** `D-OPT-VALIST-ALLOCA-MEM2REG` ✅ FIX SHIPPED 2026-06-20 (CI-pending — the Mem2Reg/va_list hypothesis was REFUTED; the real bug was a pre-existing use-after-`pop_back` in `TreeBuilder::closeFrame_`, fixed by copy-before-pop in 7f321fd — which UNMASKED a SECOND host-dependent-UB bug on the same corpus: a dangling-span-into-`operandPool_` heap-use-after-free in `lowerAggregateCopy` (the twin of the FC7-C1c hazard at hir_to_mir.cpp:4660), also fixed via copy-to-owning-vector; BOTH 345/345 + MSVC-ASan red-on-disable; both fixes verified complete incl. a CI-matching Ubuntu container — but CI STAYED red on a STALE PR #28 merge ref (refs/pull/28/merge pinned at e36dea16 = "Merge 7f321fd into base", PRE-061fa23; GitHub never regenerated it after 061fa23 → every PR CI run built pre-fix source — NOT a 3rd code bug); remedy = a fresh push regenerated the ref (e0234d8); ✅ CLOSED 2026-06-20 — e0234d8 CI green on all 5 legs) → NEXT (after CI green) → ④ `D-FC12A`(SysV) ✅ run-witnessed (exit 108) + `D-FC12C-AAPCS64`(logic) ✅ structurally-pinned every leg, 2026-06-20 (gate 346; AAPCS64 RUNTIME witness deferred behind the discovered `D-ASM-AARCH64-LARGE-FRAME-IMM12` assembler imm9-range gap [orthogonal/pre-existing, review-confirmed]; multi-slot-aggregate straddle → new fail-loud `D-FC12-VARIADIC-OVERFLOW-FIXED-AGGREGATE-STACK-ARGS`) → §B-RESOLVED 2026-06-20 (user): ⑤ `D-FC12A-VARIADIC-AL-GATED-SPILL` → **LEAVE GATED** (pure size-opt, trigger unfired — recorded, not re-litigated); the `D-ASM-AARCH64-LARGE-FRAME-IMM12` imm12-scaled assembler fix → **DO NEXT**. ✅ `D-ASM-AARCH64-LARGE-FRAME-IMM12` CLOSED 2026-06-21 (imm12-scaled LDR/STR shipped, byte-pinned + qemu-witnessed; ④'s AAPCS64 fixed-overflow RUNTIME now witnessed via `varargs_aapcs64_overflow_fixed_stack` exit 210 → `D-FC12C-AAPCS64-VARIADIC-OVERFLOW-FIXED-STACK-ARGS` FULLY CLOSED). **FC12-DEFERRAL LOOP COMPLETE — ①②③④ all closed (incl. the AAPCS64 runtime §A.5 witness); ⑤ §B-resolved (LEAVE GATED). Remaining FC12-adjacent items are normal out-of-loop deferrals: `D-FC12-VARIADIC-OVERFLOW-FIXED-AGGREGATE-STACK-ARGS` (multi-slot-aggregate straddle) + `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12` (the >32760/negative offset residual) **[UPDATE 2026-06-24: `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12` now FULLY CLOSED — the add/sub/lea-immediate half (2026-06-22) + the scaled-imm12 frame load/store displacement half (`da65205`, 2026-06-24); the narrower genuinely-unencodable register-offset residual is re-anchored `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-SCALED-IMM12` (OPEN, trigger-gated)]**. **Item 1 (the angle-`#include <h>` "next-up") ✅ DONE 2026-06-22 — REDESIGNED on agnosticism (§B): the first plan (textual splice of a C `.h` from `shippedLibDirs`) was REJECTED — a C-syntax `.h` in `src/dss-config/` couples the language-NEUTRAL shipped config to C; chose DESCRIPTOR-ONLY (angle-textual-splice reverted). The neutral `*.json` shipped-lib descriptor now carries `constants` (named integer constants, e.g. `CHAR_BIT`) + `typedefs`, INJECTED by the semantic phase; constants fold in VALUE and CONSTANT-EXPRESSION position (`int a[CHAR_BIT]`) via a new `CstEvalEnvironment::resolveSymbolValue` const-eval direct-value arm that ALSO closes the latent enum-enumerator-in-array-dim gap. `D-FFI-SHIPPED-DESCRIPTOR-CONSTANTS-TYPEDEFS` ✅; neutral `limits.json` + `examples/c-subset/shipped_include_limits` (exit 42, 4 targets + release arm); independent review CLEAN; 355/355. **FC14 (Item 2) ✅ DONE 2026-06-22 — C conditional compilation** (`#if`/`#ifdef`/`#ifndef`/`#elif`/`#else`/`#endif` + `defined`): a single-pass CONDITION STACK in `MacroExpander` (`CondFrame{enclosingActive,anyBranchTaken,thisBranchActive,seenElse}`; conditionals dispatched unconditionally to balance-track nesting, `#define`/`#undef`/`#include`/diagnostics gated on `stackActive()`; dead-branch operand NOT evaluated; `seenElse` + unterminated-EOF fail-loud) + a NEW lightweight preprocess-time token-Pratt ICE evaluator (`pp_if_eval.{hpp,cpp}`) that REUSES the main const-eval with ZERO duplication (§B-resolved: `opFromName`/`opEntryFor` extracted to a shared `src/hir/const_eval_operators.hpp` [CST evaluator byte-identical] + the `const_eval_arith` core + `schema->operatorTable()` precedence — config-driven, no hardcoded operator/token names); `defined` config-paren resolved pre-expansion; surviving identifiers→0 (incl. `sizeof`, C-faithful no-keyword-special-case); short-circuit/ternary fold only the live arm; div0/shift/overflow fail loud. `D-PP-CONDITIONAL-COMPILATION` ✅ + 2 named exclusions (`D-PP-CONDITIONAL-INCLUDE-ORDERING`, `D-PP-IF-UNSIGNED-INTMAX`). Plan-lock GO-WITH-FIXES (5 MF) + independent review (no CRIT/HIGH; MED `SizeofKeyword`-hardcode removed + 2 LOW folded); 20 pins + `examples/c-subset/preprocessor_conditionals` exit 42 (4 targets + release arm); 356 ctest. **Item 3 = the 2 FC12 squash deferrals. ① `D-FC12-VARIADIC-OVERFLOW-FIXED-AGGREGATE-STACK-ARGS` ✅ DONE 2026-06-22 (p14) — the COMPLETE all-or-nothing fix (the plan-lock found the deferral was BIGGER than framed: a straddling by-value aggregate was SILENTLY SPLIT by caller+callee, non-conformant, affecting variadic AND non-variadic; the old fail-loud guard masked it). As built: caller routes a straddling InRegisters aggregate WHOLE to the `ByValueStackArg` carrier (the exhaustion check hoisted out of `if(calleeVariadic)` → runs for EVERY call); callee receives it via a NEW symmetric `RecvByValueStackParam` MIR op (byte-copy from the incoming overflow, ZERO registers); va_start's displacement = an exact incoming-stack-byte cursor replacing the old slot-count. ★ MF-B REFINEMENT (modern-compiler comparison): SysV BACKFILLS the leftover registers vs AAPCS64 EXHAUSTS the overflowed class (gcc function_arg_advance_64 vs aarch64_layout_arg) — resolved AGNOSTICALLY via a NEW `aggregateStackExhaustsRegisters` CC flag (x86_64=false/arm64=true), threaded into the caller (HIR→MIR + lir_callconv pre-scan pad + placement clamp via the carrier's exhaust-class), callee, and va_start. Inlining gate refuses a `RecvByValueStackParam` callee (frame-sensitive). 2 fail-loud pins INVERTED to `...Succeeds` + a caller placement pin; runtime corpus `varargs_struct_straddle_fixed` (SysV exit 131) + `..._aapcs64` (qemu exit 161), release pipeline. 2 named residuals (rare multi-overflow shapes, fail-loud). 358/358 Win + 173/173 WSL examples; independent review + cross-plan. ② `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12` ✅ DONE 2026-06-22 (p14) — the AArch64 large-frame add/sub/lea-immediate blocker. A >4095-byte frame (`int big[9000]`) failed to encode at the prologue/epilogue `sub/add sp` + the high-element address `lea` (all an imm12-slot immediate >4095). FIXED SCRATCH-FREE at the ENCODER (ZERO lir_callconv/mir_to_lir change — a plan-lock REFINEMENT over the registry's MOVZ/MOVK+scratch suggestion): a new `EncodingSlotKind::Imm12HiLo24` slot + a 2-word `op Xd,Xn,#lo` / `op Xd,Xd,#hi,LSL #12` macro variant per add/sub/lea, magnitude-routed by a new `immMin`/`immMax` variant guard (1-word ≤4095, 2-word 4096..16 MiB). AGNOSTIC (x86_64 has no imm12 slot → never reached). Byte-pinned + boundary pins + run-witnessed `examples/c-subset/large_frame_arm64` exit 42 on qemu-aarch64 + x86_64-linux (leaf; non-leaf + register-pressure-spill large frames also runtime-confirmed correct). Independent plan-lock + pr-review both CLEAN. Residuals: `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-16MIB` (>16 MiB, unreachable) + the direct frame load/store >32760 tail (unreachable — addresses route through the fixed lea). Surfaced + anchored an ORTHOGONAL x86_64-Windows silent-crash: `D-WIN64-LARGE-FRAME-STACK-PROBE` (no `__chkstk` for >1-page PE frames). **★ Item 3 COMPLETE → the user's 3-item priority loop (Item 1 shipped-constants c522c48, Item 2 FC14 6eb9696, Item 3① variadic-aggregate-straddle 4e2868d, Item 3② large-frame) is COMPLETE.** |
| **V2-5** | RISC-V (riscv64) 3rd CPU target (~3–5) | New `riscv64.target.json` + hand-encoder shape-walker + per-(arch×format) relocation rows + syscall/exit block + ELF-riscv64 + corpus + `qemu-riscv64` verify (reuses the V2-1 harness). The 3rd ISA = the agnosticism acid test. | [13](./13-assembler-plan%20-%20tbd.md) + new target.json + [14](./14-linker-plan%20-%20tbd.md) | V2-1 (QEMU harness) | ⏳ |
| **V2-6** | WASM backend (~4–6) | LIR→WASM via the structured-CF markers (no Relooper) + module writer (`wasm32-v1.format.json` exists) + corpus + wasmtime/node CI verify. New format class (web reach). | [18](./18-wasm-plan%20-%20tbd.md) WA1–WA10 | steps 8/10/13 ✅ | ⏳ |
| **V2-7** | OPT7+ optimizer continuation | OPT7-gate relaxations (callee-Phi inline [frontend-reachable], sophisticated cost model, depth-bounded recursion, escape) + `scalar_eval` consolidation + cost/machine/peephole-as-JSON + OPT8+/OPT10 autotuner + LICM enhancements (`D-OPT6-LICM-PREHEADER-INSERTION`, aggressive trap-safe hoist) + `D-OPT-COPYPROP-BLOCK-CURSOR`. (`D-OPT4-1-NON-LINEAR-MARKER-MERGE` is trigger-gated on WASM/SPIR-V — may activate once V2-6 lands.) | [22](./22-optimizer-plan%20-%20tbd.md) | — | ⏳ normal |

### v0.0.3 ROADMAP — original v0.0.1 backlog NOT in v0.0.2 (dependency + value ordered)

> Exactly the items from the initial v0.0.1 stepper that v0.0.2 (V2-1…V2-7) does not cover — nothing new invented. Ordered by dependency then value: ship-ready binaries first (debug-info → codesign, dependency-correct → Mach-O 3rd-OS verify), then scale (CU6 incremental → CU5 multi-language), then specialized (SPIR-V), then niche (source-translation), then cross-cutting infra + trigger-gated reserved. Same `/dss-cycle` discipline; substrate arcs pause for §B sign-off.

| Order | Arc · cycles | What | Plan / sub-plan | Blocked by | Status |
|---|---|---|---|---|---|
| **V3-1** | Debug info (DWARF/PDB) | DB1–DB12: DWARF 5 (ELF+Mach-O) + PDB (PE) + CFI / SEH / compact-unwind. Debuggable binaries; **prereq for codesign**. (orig v0.0.1 step 14) | [15](./15-debug-info-plan%20-%20tbd.md) (step 14) | steps 10/13 ✅ | ⏳ |
| **V3-2** | Codesign / publish | CS1–CS9: Apple codesign + notarization + Authenticode + RFC 3161 + APK v3 (vendored BearSSL). Signed, distributable artifacts — the v1-production ship gate. (orig step 15) | [16](./16-codesign-publish-plan%20-%20tbd.md) (step 15) | **V3-1** (debug-info / step 14) | ⏳ |
| **V3-3** → **promoted into the ARM64 arc** | Mach-O exec-verify (Apple Silicon) | ✅ **DONE 2026-06-09 (was ACTIVE 2026-06-08, user-directed) — the macOS-runner trigger FIRED + the work LANDED + runtime-confirmed**: `macos-latest` CI = Apple Silicon (arm64), so a `macho64-arm64-darwin-exec` corpus runs natively there. Folded into the ARM64 arc (see 13X.2) as the macOS-Mach-O platform alongside Linux-ELF; completes 3-OS execution verification. **§B 2026-06-08 — BLOCKED on code-signing → ad-hoc codesign goes FIRST.** Apple Silicon's kernel SIGKILLs unsigned arm64 binaries + DSS only RESERVES `LC_CODE_SIGNATURE` placeholder bytes (real fill = Plan 16 / V3-2, unbuilt), so a macho64-arm64-darwin-exec could be byte-pinned but NOT RUN (the §A.5 trap). User chose: build minimal **ad-hoc Mach-O codesign** (`D-LK7-ADHOC-CODESIGN-MACHO` — SHA-256 in `src/core/crypto` + a config-driven `codeSignature` block filled by the `macho.cpp` walker; plan LOCKED in the registry) as the active prerequisite cycle, THEN this macOS-ARM64 exec + a runnable corpus. | 13X.2 + `D-LK7-ADHOC-CODESIGN-MACHO` | — (both prerequisites landed) | ✅ **DONE 2026-06-09** — ad-hoc codesign shipped + `D-LK10-ENTRY-MACHO-EXIT` ✅ runtime-confirmed on Apple Silicon (macho_arm64_exit + global_int + shipped_include_puts all RUN on `macos-latest`); 3-OS Mach-O exec verification achieved. The §B codesign-first sequencing below is now history. |
| **V3-4** | CU6 cross-CU / incremental | Incremental rebuild + shared libs/DLLs (LK11 cross-CU largely landed cycles 15–19; CU6 incremental remains). (orig step 21, CU6 part) | [08](./08-compilation-unit-plan%20-%20tbd.md) CU6 + [14](./14-linker-plan%20-%20tbd.md) LK11 (step 21) | — | ⏳ v1.x |
| **V3-5** | CU5 multi-language CUs | One binary, mixed source languages, HIR convergence. (orig step 20) | [08](./08-compilation-unit-plan%20-%20tbd.md) CU5 (step 20) | steps 4/8 ✅ | ⏳ v1.1 |
| **V3-6** | SPIR-V / shader-GPU | SG1–SG10: SPIR-V codegen + shader HIR + GPU bindings + dual CPU+GPU lowering (`spirv-1.6.format.json` exists). (orig step 18) | [17](./17-shader-gpu-plan%20-%20tbd.md) (step 18) | steps 8/10 ✅ | ⏳ |
| **V3-7** | Source-to-source translation | ST1–ST6: HIR-pivot transpilation (`.map.json` + HIR→HIR walker + target-CST + pretty-printer). (orig step 19) | [10](./10-source-translation-plan%20-%20tbd.md) (step 19) | step 8 ✅ | ⏳ |
| **V3-8** | P2 docker · P3 CI/fuzzing | Dockerfile + cross-compile toolchains; nightly fuzzing + extended sanitizers (rolls continuously). (orig P2 + P3 residue) | §8 | — | 🟦 partial |
| **V3-9** | Reserved (gated) | R1 HW HIR (VHDL/Verilog/SystemVerilog), R2 custom language, R3 runtime (GC/exceptions/coroutines/threading). (orig R1/R2/R3) | [19](./19-hir-hw-reserved-plan%20-%20tbd.md) / [20](./20-custom-language-reserved-plan%20-%20tbd.md) / [21](./21-runtime-reserved-plan%20-%20tbd.md) | trigger | 🔒 |

### v0.0.1 — as-built ledger (SHIPPED ✅; forward work carried into the v0.0.2 roadmap above)

| Step | Plan / PR | What | Blocked by | Status |
|---|---|---|---|---|
| ~~1~~ | [08](./08-compilation-unit-plan%20-%20tbd.md) **CU1** | `CompilationUnit` + `UnitBuilder` + `CrossTreeRef` + `CompilationUnitId` (single-tree/empty CU; move-only; `addTree` only) | — | ✅ **done** (13 tests, 48/48 suite green, 5-agent review) |
| ~~2~~ | [08](./08-compilation-unit-plan%20-%20tbd.md) CU2 | Multi-file `UnitBuilder` (`addFile`/`addInMemory`); `D_*` codes; lexer+parser diagnostics unified in the Tree (cross-plan 01/05) | step 1 ✅ | ✅ **done** (48/48 suite, 5-agent review) |
| ~~3~~ | [08](./08-compilation-unit-plan%20-%20tbd.md) CU3 | CU-scoped `SymbolId` + `UnitAttribute<T>` (CU-scoped `NodeId`→`T` side-table) + membership-based cross-CU `NodeId` guard + sanctioned minimal `populateDeclarationSymbols` walk (§2.7) | step 2 ✅ | ✅ **done** (21 + 3 tests, 50/50 suite green, substrate-tier 5-agent review + fix pass) |
| ~~4~~ | [08](./08-compilation-unit-plan%20-%20tbd.md) CU4 | Per-language `ImportResolver` populating `crossRefs`: c-subset `#include` following (grammar amended), tsql cross-statement table matching, toy identity, `D_Unresolved*` diagnostics | step 3 ✅ | ✅ **done** (16 tests, 51/51 suite green, substrate-tier 5-agent review + fix pass) |
| ~~5~~ | [08.5](./08.5-substrate-prep-plan%20-%20ok.md) SP1 | Generalize arena + `NodeAttribute<T>` for HIR/MIR/LIR reuse (`ArenaContainer`/`ArenaBuilder`/`ArenaAttribute`; Tree + TreeBuilder rebuilt on it) | step 4 ✅ | ✅ **done** (54/54 suite green, full extraction, substrate-tier 5-agent review + fix pass) |
| ~~6~~ | [08.5](./08.5-substrate-prep-plan%20-%20ok.md) SP2 | Core type lattice + per-language extension registry (`TypeKind`/`TypeRecord`/`TypeInterner`/`TypeRegistry`/`TypeLattice`; `typeExtensions[]` schema v3) | step 5 ✅ | ✅ **done** (57/57 suite green, bound-but-separate, substrate-tier 5-agent review + fix pass) |
| ~~7a~~ | [08.55](./08.55-pre-se1-cleanup-plan%20-%20ok.md) | **Pre-SE1 genericity cleanup**: schema-drive Pratt wrappers, schema-drive numeric lexing, prune pre-interned kinds, retire toy-shaped headers + CU3 placeholder, LSP list config-driven. | step 6 ✅ | ✅ **done** (commit `1a433c2`) |
| ~~7~~ | [08.6](./08.6-semantic-plan%20-%20ok.md) SE1–SE7 | Symbol table + scope resolution + type checking — **config-driven** (`semantics` block, schema v4; one language-agnostic engine). Engine + toy/c-subset/tsql configs; per-tree-root scopes + `crossRefs[]` import injection; const-correctness; typedefs; functions/calls/overloads (`kindByChild` discriminator); LSP wiring (6 handlers). 64/64. | step 7a ✅ | ✅ **SE1–SE7 done** |
| 8 | [09](./09-hir-plan%20-%20ok.md) HR1–HR11 | HIR — language-neutral structured typed pivot; CST→HIR lowering per shipped language | step 7 | ✅ **plan 09 (HIR) COMPLETE — HR1–HR11 ✅** (2026-05-26..28, `feature/hir-1`; HR8 = config-driven lowering engine on c-subset; HR9 = toy enriched into a typed language + arrays un-deferred + gap-closure (char/string VALUES, SeqExpr, pointers, ternary); HR10 = tsql-subset lowering — role-explicit SQL extension nodes via generic `childGathering` config; HR11 = multi-language CU lowering, all language-agnostic). **Post-closure substrate fix** 2026-05-28 (`feature/mir-lir` commit `3e6b523`): `lowerTopLevel` was skipping the `coerce(initE, type)` call for module-global initializers; uncovered while writing plan-12.5's CE5 MIR e2e test; one-line fix to a missed call site of the same language-blind helper. **D5.3 (designated initializers) — READY TO RESUME** 2026-05-28: architecture + full C99 §6.7.8 scope frozen (2 cycles, recursive aggregate variant arm, all 4 context sites, dot-chained designators); both parser-substrate prerequisites ✅ done — sub-cycle A `6a8f146` (token-leaf branches in speculative alts) + sub-cycle B `14d789b` + `19c0b8b` (walker recognizes auto-interned Pratt wrappers, suppresses false-positive desync latch via O(1) `wrapDepth_ > 0` check; 3-agent review-fix pass). |
| 9 | [11](./11-ffi-plan%20-%20tbd.md) FF1–FF6 | FFI — hermetic ELF/PE/Mach-O/ar readers + C-header mode parser + ABI catalog + name mangling | step 8 | ✅ **MOSTLY DONE 2026-06-01..02**: FF1-ELF + FF2 + FF3 + FF4 + FF5 + FF6 (Windows hello_puts: msvcrt.puts + 2-DLL IAT + runtime print asserted byte-for-byte) all closed. **Remaining**: FF1-PE / FF1-MachO binary readers (anchored — triggered by first PE/macOS corpus needing extern resolution via shipped binaries rather than headers); cross-host FF6 equivalents (Linux/macOS/ARM64) ✅ DONE 2026-06-09 (`shipped_include_puts` runs on all 3 OSes × 2 arches; D-LK10-ENTRY-ARM64 ✅ + D-LK10-ENTRY-MACHO-EXIT ✅); variadic `printf` gated on ML7 stack-args closure. |
| 10 | [12](./12-mir-lir-plan%20-%20ok.md) ML1–ML8 | MIR (SSA over CFG + structured-CF markers) + LIR (per-target ISA) | step 8 | ✅ **ML1–ML8 closed end-to-end 2026-05-27..29 + ML7 cycle 2 closed 2026-05-30 + frame-bias-unify 2026-06-02** (`feature/mir-lir` / `feature/lk-111`). **MIR ML1–ML4 ✅**: skeleton + HIR→MIR (6 cycles incl. Cast emission) + MirVerifier (7 rule families, `I_*` 0xA00x) + `.dssir` text round-trip. **D5 sub-arc ✅** (structs/unions/enums + designated init + brace-init + ConstructAggregate). **Plan 12.5 ✅** (CE1–CE5 + D6/D7). **LIR ML5–ML8 ✅**: ML5 JSON-configured targets + register-file + calling conventions in JSON + MIR→LIR isel through cycle 3e; ML6 liveness + linear-scan + rewrite pass + `verifyLirPostRegalloc`; ML7 cycle 1 callconv + stack frame (`5362766`); ML7 cycle 2 ✅ (`b9b5304`) `arg` + `call` virtual-op materialization + GlobalAddr→SymbolRef peephole + move-cycle detector + 4 new L_* codes; ML8 cycle 1 `.dsslir` EMITTER (`040d496`) + cycle 2 PARSER + verify-on-load + round-trip + schema version (`9622b38`); **D-LK10-ENTRY-ML7-FRAME-BIAS-UNIFY ✅ 2026-06-02** Win64 shadow-space + post-CALL alignment-bias for non-leaf functions; **MIR→LIR extern-call opcode selection ✅ 2026-06-02** new MnemonicSlot::CallIndirectViaExtern + externSymbols set on Lowerer. **Remaining**: D-ML7-2.2 stack-passed args (✅ closed 2026-06-02); D-ML7-2.4 indirect function-pointer calls (✅ closed 2026-06-12, FC4 c2); D-ML7-2.5 regalloc pre-coloring perf optimization. 149/149 ctest. ML6–ML8 ✅ parallel-with step 9. |
| 10.5 | [12.5](./12.5-const-eval-plan%20-%20ok.md) CE1–CE5 | **Shared constants-evaluation engine** in `src/hir/const_eval.{hpp,cpp}` (corrected location from initial draft — engine reads HIR so it cannot live in the lattice library below HIR). Lifts ML2's inline `tryConstFold` into one language-blind engine consumed by MIR-globals init, HIR verifier (array-length, future enum-bounds), D5.5 enum-value computation, and the optimizer's const-fold pass. Closes the float-arithmetic fold gap delimited by ML2 globals + the `Cast`-fold gap delimited by HR Cast emission. | step 10 | ✅ **CE1–CE5 done 2026-05-28** (engine + integer fold + Ref callback + target-aware Cast + commonType-tagged BinaryOp + short-circuit LogicalAnd/Or/Ternary + `allowFloat` IEEE 754 policy via host `<cmath>` with F32 narrowing, NaN-unordered comparisons, float→int unconditional refuse-on-out-of-int64; F16/F128 + lossy-conversion knob + schema-driven `allowFloat` + env/policy split + result cache delimited as plan-12.5 §0.2 deferred-by-design; 71 unit tests + 4 MIR end-to-end; 89/89). **Plan 12.5 CLOSED.** |
| 12 | [13](./13-assembler-plan%20-%20tbd.md) AS1–AS6 | x86_64 + ARM64 hand-encoder + per-(arch×format) relocation taxonomy | step 10 | ✅ **AS1–AS6 closed end-to-end 2026-05-29** (`feature/as-1`). x86_64 + ARM64 byte encoding via shape-keyed walkers (`x86-variable` + `fixed32`); round-trip oracle disassembler; relocation taxonomy unifier (target-side); SourceMapEntry stamping at dispatch level; end-to-end c-subset corpus pipeline test. `A_*` diag family at 0x1xxx. |
| 13 | [14](./14-linker-plan%20-%20tbd.md) LK1–LK10 | In-tree linker — JSON-configured object formats (`*.format.json` for ELF/PE/Mach-O/WASM/SPIR-V) + format-blind engine + symbol resolution + relocs + dynamic linking + file emission + driver wiring | steps 9, 12, **P1** | ✅ **LK1–LK10 CLOSED + RUNNABLE end-to-end 2026-06-02** (`feature/lk-111`). LK4 substrate + LK1/LK2/LK3 per-format writers + LK6 cycle 1 intra-module reloc-apply + LK6 cycle 2a–d dynamic linking + LK7 codesign placeholders + LK8/LK9 skeletons + LK10 file emission + LK10 cycle 2 driver pipeline wiring + **★ D-LK10-ENTRY Stage 1 ✅ + frame-bias-unify ✅** (first runnable DSS binary on Windows; hello_puts prints "hello\r\n" + exits 42 asserted byte-for-byte). LK5 (TLS) + LK11 (cross-CU) deferred. |
| **`★ STAGE 2 — STRICT SEQUENCE: c-subset host-runnable corpus expansion (2026-06-02+)`** | | | | |
| ~~13.1~~ | [12](./12-mir-lir-plan%20-%20ok.md) **D-ML7-2.2** stack-passed args | ✅ **CLOSED 2026-06-02 — co-closed with D-ML7-2.6 (Win64 slot-aligned).** FrameLayout extended with `outgoingArgAreaSize` + `outgoingSlotSize`. `computeMaxOutgoingStackArgs` pre-scans isCall insts. New `cc.slotAligned` field drives Win64 (ms_x64 = true) vs SysV/AAPCS64 (false) overflow semantics. Materialize call arm emits stack stores BEFORE register moves (hazard avoidance); arg arm reads stack-resident args via frame_load. Saved-reg prologue/epilogue offsets corrected (CRITICAL audit fold C1 — was missing `savedRegAreaOffset()` bias). `L_StackPassedArgUnsupported = 0xB007` stays as defensive guard. NEW anchors: D-ML7-2.10 (latent independent-counter payload mixed-class miscompile), D-ML7-2.6-MIXED-CLASS-FIXTURE (Win64 mixed int/float pin awaits c-subset float-param support). 149/149 ctest. | — | — |
| ~~13.2~~ | [01](./01-tree-node-model-plan%20-%20ok.md) c-subset `void*` typing | ✅ **CLOSED 2026-06-02 — D-LANG-POINTER-VOID-CONVERT + 8-agent audit fold.** `void*` opaque-pointer support landed via shared substrate: `SemanticConfig::PointerConversionRules { implicitToVoidPtr, implicitFromVoidPtr }` (directional split — C admits both, C++ self-host will admit only T*→void*). `isAssignable` extended with Ptr<Void> ↔ Ptr<T> arms gated on per-language rules (default-false = strict-reject). HIR `coerce()` emits matching `Cast(Ptr<T>↔Ptr<Void>)` nodes. c-subset.lang.json declares both directions true (C-standard §6.3.2.3). **THREE silent-failure surfaces closed in same commit** (audit-grade fixes, not workarounds): the void-pointer negative-pin test (`DistinctTypedPointersRemainMismatch`) failed because `checkCall`'s arg-assignability used `s.typeAt(argNodes[i])` which returns InvalidType for bare identifier-reference expression wrappers — silently suppressing the mismatch diagnostic. The 8-agent fold surfaced TWO parallel sites with the same wrapper-vs-leaf gap: `checkMemberAccess`'s `lhsType` lookup (suppressed S_NotAPointer / S_NotAComposite / field-type write-back for `p->x` on bare-identifier `p`); declaration-initializer's `initTy` lookup (suppressed BOTH type-inference AND mismatch diagnostic on `T* x = bareRef;` patterns). All three switched to `subtreeType()` (the existing DFS-descent helper). **JSON loader hardened** with typo-defense — unknown sub-keys under `pointerConversions` now fail-loud (`C_InvalidSemantics`); pre-fix a typo like `implictToVoidPtr` would silently fall back to default-false strict. **HIR coerce() comment corrected** (false-hazard claim about a "default-false SemanticConfig used by the lowerer below" — no such fallback path exists). **3 new anchors registered** (all audit-fold): D-LANG-VOIDPTR-ARITH-REJECT (default-reject; sizeof(void) undefined; GCC extension under config-flag); D-LANG-VOIDPTR-FN-CONVERT (default-reject; UB in standard C; universal compiler permit); D-HIR-VERIFIER-POINTER-CONVERT-CONTRACT (pin the verifier's post-coerce invariant — explicit Cast nodes required at HIR-tier); D-LANG-VOIDPTR-PREDICATE-GATE (type-design D2 — future per-element-type predicates); D-TYPERULES-PTRRULES-PASS-BY-VALUE (type-design D4 — marginal pass-by-value vs const-ref). **Test discipline**: `countCode` strict-pin form replaces any-bool sawTypeMismatch (audit-fold G1 criticality 8); added 3 return-direction + init-direction tests (G2/G3 coverage of the 2 other isAssignable call sites). 7 new tests total; 149/149 ctest. **Agnosticism re-verified by all 8 agents**: zero hardcoded source/target/format names in shared substrate (type_rules.hpp / semantic_analyzer.cpp / cst_to_hir.cpp / semantic_config.hpp); per-language values live only in c-subset.lang.json. **REJECTED across agents** (anchored): predicate-gate (type-design D2 anchor only — YAGNI for v1, additive when needed), pass-by-value (type-design D4 anchor only — marginal, address-on-3rd-rules-block). | step 13.1 ✅ | ✅ **CLOSED 2026-06-02** |
| ~~13.3a~~ | semantic substrate | ✅ **CLOSED 2026-06-02 — hello_writefile prereq substrate.** Three substrate cycles surfaced + closed when writing the hello_writefile example: (a) **D-LANG-NULL-POINTER-CONSTANT** — `nullPointerConstantFromIntegerZero: bool` field on `SemanticConfig::PointerConversionRules`; value-aware helper `isLiteralIntegerZero` admits the literal `0` to any `Ptr<*>` at the 3 isAssignable sites (call-arg, return, init); HIR `coerce()` materializes as `Cast(IntLit(0), Ptr<*>)` (MIR `IntToPtr` lowering routes to a pointer-width zero in the dest register). C-standard §6.3.2.3.3 covered for the common bare-`0` case; future const-eval-aware admission (`0+0`, `(int)0`) anchored as **D-LANG-NULL-PTR-CONST-EVAL**. (b) **D-CSUBSET-EXTERN-LIBRARY-SYNTAX** — trailing `stringLiteralExpr` inside `externFuncTail` (between `)` and `;`) carries a per-symbol import-library override. Example: `extern void* GetStdHandle(int n) "kernel32.dll";`. New fields: `HirExternRecord.libraryOverride` + `ExternDeclRef.libraryOverride`. `synthesizeFfiFromSourceDecls` prefers the per-extern override over the format-level default. Source-agnostic — any grammar producing a `stringLiteralExpr` child in extern tails gets per-symbol routing for free. (c) **D-SEMANTIC-SUBTREETYPE-TRANSPARENT-WRAPPERS** partial closure — `subtreeType` now STOPS descent at any internal node whose visible children include an operator-token (any arity, via `OperatorTable.lookup`). This fixes a 2nd-order regression introduced by 13.2's audit fold: unary `&x` / `*p` (type-changing wrappers) would have returned the inner leaf's type instead of the wrapper's evaluated type — silently mis-admitting `int*` ↔ `int` at call-arg / init / return sites. Source-agnostic via schema's OperatorTable. Full closure (pass-2 expression typing for unary/binary/ternary wrappers) anchored. 6 new semantic-tier tests + 1 HIR Cast-emission test; 149/149 ctest. | step 13.2 ✅ | ✅ **CLOSED 2026-06-02** |
| ~~13.3b~~ | LIR/asm: local-int codegen | ✅ **CLOSED 2026-06-02 — D-CSUBSET-LOCAL-INT-CODEGEN substrate.** Three coupled pieces landed: (a) `FrameLayout` extended with `localAreaSize` + `numLocalAllocas` + `localAreaOffset()` accessor — locals sit ABOVE the spill area (positive RSP offset post-prologue, x86_64 disp8 encoding density + .pdata adjacency); (b) `materializeOneFunc` gained an `alloca` arm immediately after the `arg` arm rewriting each `alloca` LIR op → `lea result, [sp + localAreaOffset() + i * slotSize]` using the existing 3-op no-index `lea` form (scan-order slot assignment via shared `functionLocalAllocaCount` pre-scan + `localAllocaIndex` counter); (c) `neg` encoding added to `x86_64.target.json` (`REX.W F7 /3`, 3 bytes, modrmRegExt=3, requires2Address=true). `h.alloca_` + `h.lea` are optional `OpcodeHandles` — shader/WASM targets without body-local allocas legitimately omit both. 7-agent audit fold: **F1 HIGH** (3-agent convergence — alloca-arm docblock severed the call-arm comment block; moved to land after the `arg` arm where it structurally belongs); **F2 HIGH silent-failure** (`functionLocalAllocaCount` re-scans every function unconditionally; anchored D-CSUBSET-ALLOCA-COUNT-CACHE for MIR→LIR-time caching, trigger=profiler evidence); **F3 HIGH test-coverage** (added 2 substrate-tier tests: `AllocaMaterializesToLeaInScanOrder` pinning 2+ allocas → 2 leas with distinct monotonic offsets; `AllocaWithVirtualResultFailsLoud` pinning post-regalloc invariant + diagnostic-code specificity; third negative pin anchored D-CSUBSET-LOCAL-INT-CODEGEN-NEGATIVE-PIN, trigger=custom-schema-mutation test infrastructure). ANCHOR-LATER: D-FRAMELAYOUT-COUNT-FIELDS-AUDIT (A1, redundant byte+count fields), D-OPCODE-HANDLES-NAMING-SWEEP (A2, `alloca_` trailing-underscore), D-LIR-ALLOCA-LOWERING-EXTRACTION (A3, separate pass when dynamic stack growth lands). Agnosticism re-verified: zero `if (target == ...)` branches; mnemonic-based dispatch; ARM64 lands by JSON-only addition. 150/150 ctest (+ examples/c-subset/hello_writefile passing). | step 13.3a ✅ | ✅ **CLOSED 2026-06-02** |
| ~~13.3~~ | examples + plan 14 | ✅ **CLOSED 2026-06-02 — ★ THE FIRST DSS-EMITTED WINDOWS BINARY THAT PRINTS WITH ZERO MSVCRT DEPENDENCY.** `examples/c-subset/hello_writefile/main.c` calls `kernel32.GetStdHandle(-11)` → `kernel32.WriteFile(h, "hello\r\n", 7, &written, 0)` → returns 42. Execution-verified by `examples_runner` on Windows host: both `exitCode==42` AND `capturedStdout=="hello\r\n"` asserted byte-for-byte. Closes D-LK10-KERNEL32-WRITE-PATH. **Substrate exercised end-to-end**: D-ML7-2.2 stack-args (lpOverlapped on stack); D-LANG-POINTER-VOID-CONVERT (void* HANDLE/lpBuffer/lpOverlapped); D-LANG-NULL-POINTER-CONSTANT (literal `0` as null pointer constant for lpOverlapped); D-CSUBSET-EXTERN-LIBRARY-SYNTAX (`"kernel32.dll"` per-symbol library override); D-CSUBSET-LOCAL-INT-CODEGEN (`int written;` alloca→lea + `-11` neg encoding); D-LK10-ENTRY Stage 1 (trampoline ExitProcess injection). Compared to hello_puts (step 13.1's payoff, leaned on msvcrt DllMain self-init), this is more honest — the binary depends on NOTHING but the loader resolving kernel32's IAT entries. 150/150 ctest. | steps 13.1, 13.2 ✅, 13.3a ✅, 13.3b ✅ | ✅ **CLOSED 2026-06-02** |
| ~~13.4~~ | [12](./12-mir-lir-plan%20-%20ok.md) variadic ABI + c-subset `...` grammar | ✅ **CLOSED 2026-06-02 — D-LANG-VARIADIC substrate + first DSS-emitted printf binary.** Five-tier substrate landed end-to-end: (a) **Type system**: `TypeInterner::fnSig(...)` 4-arg overload adds `isVariadic` parameter; scalars[1]=1 encoding (preserving non-variadic scalars=[cc] default for every pre-13.4 call site, scalars=[cc, 1] for variadic); `fnIsVariadic()` accessor. (b) **Grammar**: c-subset `EllipsisOp` token + `paramOrEllipsis` alt-rule wrapping `param | ellipsisParam`; `paramList` repeats `Comma paramOrEllipsis`. (c) **Semantic**: `DeclarationRule::variadicMarker: optional<SchemaTokenId>` (source-agnostic — language declares its own marker; c-subset declares `EllipsisOp`); `paramsSubtreeHasVariadicMarker()` walks the params subtree for the marker token; semantic_analyzer's Function-decl path passes the result to the 4-arg `fnSig` builder. Call-arity check admits `argNodes.size() >= fixedCount` for variadic FnSigs (`tooFewForVariadic || wrongCountForFixed`). (d) **MIR/LIR Call payload**: new `core/types/call_payload.hpp` encodes `(isVariadic, fixedArgCount)` into the call inst's u32 payload (bit 31 = isVariadic; bits 0..30 = fixedArgCount; non-variadic always payload=0); HIR→MIR Call lowering stamps the payload from the callee's FnSig; MIR→LIR Call lowering propagates it; HIR Verifier admits args beyond fixed params for variadic FnSigs (assignability loop bounded at `params.size()` not `args.size()`). (e) **ABI emission**: `TargetCallingConvention::variadicVectorCountReg: optional<NamedRegisterRef>` (SysV declares `rax`/AL per §3.5.7; Win64/AAPCS64 leave empty — `D-TARGET-SUBREGISTER-ALIASES` anchors future `al` byte-alias support so the JSON can name the ABI-spec register exactly); ML7 materialize call arm emits `mov <countReg>, <fpr-count-in-vararg-region>` before the call instruction when `isVariadic(payload) && cc.variadicVectorCountReg.has_value()`. Count is FPR-class args in `[fixedArgCount, ops.size())` — c-subset has no float type today so count is always 0, but the loop is type-driven (future float varargs light up zero-substrate-change). **Examples**: `hello_printf` (1-arg call: `printf("hello\n")`, exit 42 + stdout "hello\r\n"); `printf_int` (2-arg variadic: `printf("answer=%d\n", 42)`, exit 0 + stdout "answer=42\r\n"). Both pin the full path semantic → HIR → MIR → LIR → assembled binary → OS run. **5-agent audit fold**: (1) **Simplifier HIGH** — `paramsSubtreeHasVariadicMarker` was a tree-walker duplicate of the existing `subtreeContainsToken` (SE4 const-marker scan); collapsed both consumers to the single walker (-22 LOC). (2) **Silent-failure HIGH-1** — `fnIsVariadic` was empty-scalar-tolerant; added `latticeFatal` on 0-slot FnSig (pins the cc-scalar invariant). (3) **Silent-failure HIGH-3** — HIR→MIR call-payload encoder silently truncated `params.size() > kFixedArgMask` (2^31); added fail-loud `unsupported()` at encode site. (4) **Silent-failure HIGH-5** — same site silently degraded a Ptr-with-empty-operands to non-variadic; fail-loud `unsupported()`. (5) **Silent-failure MEDIUM-1** — semantic checkCall's S_ArgCountMismatch now carries the "fixed " word for variadic-too-few (mirrors HIR verifier diagnostic shape). (6) **Comment-analyzer HIGH** — hello_printf docblock wrongly claimed `printf("%d\n", 42)` (actual is `printf("hello\n")`); fixed + cross-ref to printf_int. (7) **Comment-analyzer HIGH** — lir_callconv count-mov rationale (rax-aliasing-returnGprs[0]) was incorrect; rax isn't a SysV arg-passing GPR — corrected to "future indirect-call shape OR parallel-copy serializer could use it as scratch." (8) **Test-analyzer rating-9** — 3 new HIR verifier tests pin the variadic-arity 3-way matrix (admit args>params, reject args<fixed with "fixed " word, non-variadic still rejects args!=params). (9) **Silent-failure MEDIUM-2** — added Win64 negative test (`MsX64CcDoesNotDeclareVariadicVectorCountReg`) — a regression copy-pasting SysV's `variadicVectorCountReg` into ms_x64 surfaces fail-loud. **Anchored future cycles**: `D-ML7-VARIADIC-WIN64-DOUBLE-SPILL` (Win64 vararg-double-spill — every float vararg duplicates in matching GPR slot — surfaces when c-subset gets float types); `D-TARGET-SUBREGISTER-ALIASES` (register-table aliases for `al`/`ax`/`eax` so cc fields name ABI-spec subregisters exactly); `D-LANG-VA-LIST` (`va_list`/`va_arg`/`va_start`/`va_end` callee-side vararg consumption — not needed for caller-side printf, blocks user-defined variadic functions); `D-CONFIG-LOADER-UNKNOWN-KEYS-FAIL-LOUD` (sweep cycle — a typo in `"variadicMarker"` / `"variadicVectorCountReg"` JSON keys silently disables the feature with no diagnostic; project-wide sweep to add unknown-keys validation following the void* JSON loader precedent); `D-LANG-VARIADIC-MARKER-DESCENT-BOUNDARY` ✅ **CLOSED in same cycle (post-fold)** — `subtreeContainsToken` now takes an optional `declByRule` map and stops descent at any nested decl-rule node when supplied. Both consumers (SE4 const-marker + D-LANG-VARIADIC variadic-marker) pass it. Generalizes to any future "marker token within a decl subtree" scan; `D-ML7-VARIADIC-COUNT-MODE` (code-architect Q1) — `variadicVectorCountReg` + FPR-count scan generalizes to `variadicCountMode: enum {FPRCount, GPRCount, TotalArgCount}` sibling field when the first non-FPR-count ABI arrives (MIPS O32 total-vararg-arg-count etc.); `D-CALL-PAYLOAD-WIDENING-GATE` (code-architect Q2) — `call_payload.hpp` owns the u32 bit layout; every new call-shape bit (tail-call marker, musttail, indirect-call type tag) declared there; widen both `MirInst::payload` + `LirInst::payload` to u64 when a 32nd bit is needed (not pointer-to-struct — payloads stored inline in arenas); `D-ML7-CC-PRECALL-IMM-LOADS` (code-architect Q3) — when a 2nd pre-call register-load pattern lands (fixed syscall-number register etc.), generalize single `variadicVectorCountReg` field to `preCallImmediateLoads: [{reg, value}]` array on CC; `D-LANG-VARARG-PROMOTION-CHECK` (code-architect Q4) — per-language default-argument-promotion rules for vararg positions via optional `varargPromotionRules` block in SemanticConfig, checked in semantic_analyzer's checkCall AFTER fixed-param loop (HIR verifier's vararg-region skip stays correct permanently at that tier — this is a source-language check, not a type-tier check); `D-CALLPAYLOAD-STRONG-WRAPPER` (type-design Q1) — convert `call_payload` namespace to `struct CallPayload { uint32_t bits; }` with explicit conversions when a 2nd opcode reuses u32 payload differently (trivial mechanical when triggered). Grammar: mini_calc.c.tree refreshed (intentional shape change — non-variadic params now wrap in `paramOrEllipsis > param`). 152/152 ctest including 3 new HIR verifier tests + 2 new lir_callconv schema-tier tests + 2 new TypeInterner variadic tests. | step 13.1 ✅ | ✅ **CLOSED 2026-06-02** |
| ~~13.5 cycle 1+2~~ | examples + corpus expansion | ✅ **CLOSED 2026-06-03 — D-CSUBSET-WHILE-LOOP-SUBSTRATE + D-CSUBSET-MULTI-FN-WIN64-CC + D-LIR-SETCC-WIDTH-CONTRACT + D-ASM-ENCODE-FAILURE-FUNCTION-ROLLBACK + 14 runnable corpus examples + cross-platform CI fix.** First non-trivial host-runnable c-subset programs with INTRA-FUNCTION CONTROL FLOW. The sum-loop example (`while (i<=10) { sum += i; i++; }` → exit 55) surfaced 4 declared-but-unencoded x86_64 opcodes (jmp / cmp / jcc / setcc) AND the maxof3 example (sequential if/else) surfaced an ICmp+CondBr garbage-upper-bits bug. **Substrate landed** (single cycle, target-agnostic): (a) per-target `condCodeEncoding[10]` table mapping abstract TargetCondCode → ISA-numeric encoding (x86: 4-bit nibble in opcode-byte low; ARM64 will declare its own table for B.cc bits 0..3); (b) `EncodingSlotKind::CondCodeNibble` + `template.condCodeFromPayload: bool` driving cond-from-payload OR-into-opcode-byte at encode time; (c) `EncodingSlotKind::BlockRel32` + per-function block-offset table + per-function patch list in asm.cpp resolving intra-function PC-rel branches at assemble time (NOT linker relocs); (d) `Wire.prefixOpcodeBytes` admitting compound encodings (jcc emits `0F 8x rel32; E9 rel32` — 11 bytes — via wire[1].prefixOpcodeBytes=[0xE9] bridging the cond-branch's rel32 to the trailing uncond jmp's rel32); (e) `OperandKindFilter::BlockRef` + walker_util `BlockRelPatch` + `filterToLirKind` arm; (f) MIR→LIR `lowerCondBr` passes both successors as BlockRef operands AND fuses adjacent ICmp+CondBr into `cmp lhs, rhs; jcc-cond` (the naive `cmp result_b8, 0` would compare against setcc's garbage upper 56 bits and trip the branch the wrong way — the maxof3 bug). Validate's "single-writer slot" rule extended with BlockRel32 admitted-as-multi-writer (jcc has 2 BlockRel32 wires for taken+fallthrough). 4 new encoding rows in x86_64.target.json. Examples: `sum_loop/` (exit 55), `max_of_3/` (exit 13), `factorial/` (exit 120 — multiplicative loop), `fibonacci/` (exit 55 — 2-var rotation; phi cross-iteration stress), `cmp_all_signed_conds/` (exit 14 = bit-packed witness of 6 signed cond-codes — per test-analyzer rec). **7-agent post-fold** (cycle 2): (1) silent-failure CRITICAL #2 + HIGH #3 — `(void)encodeInst` laundered per-inst failures, partial bytes leaked into subsequent block-offset captures corrupting intra-function branch patches → closed via D-ASM-ENCODE-FAILURE-FUNCTION-ROLLBACK (track funcEncodeOk, truncate partial bytes per-inst, drop entire function on any failure + emit new `A_FunctionEncodeAborted` summary). (2) code-reviewer C1 — `lowerSwitch` passed empty operands to jcc which now requires 2 BlockRef operands → closed (every switch statement would have failed `A_NoMatchingEncodingVariant`; no corpus example exercised switch yet, so silent gap). (3) code-reviewer C2 — setcc-width contract bug: setcc writes ONLY the low byte; `cmp r64, 0` would read garbage upper 56 bits + setcc r/m8 without REX prefix references ah/ch/dh/bh aliases (not spl/bpl/sil/dil) for r4..r7 destinations. Closed via D-LIR-SETCC-WIDTH-CONTRACT: new `template.forceRexPrefix` flag (setcc declares it true for proper low-byte access on all 16 GPRs); new `zext` encoding (`REX.W 0F B6 /r` — movzx r64, r/m8); lowerICmp now emits `cmp → setcc b8 → zext result` (proper SSA, no multi-def). Closes the silent miscompile for any future MIR pattern that stores/returns a bool. (4) architect FOLD-NOW + type-design — rel32 formula was hardcoded in shared `assemble()` (agnosticism break per standing rules) → closed via `BlockRelPatchKind: enum { X86Rel32, Arm64Imm19, Arm64Imm26 }` on `BlockRelPatch`; resolver dispatches via kind; ARM64 arms fail-loud `A_NoEncodingShapeWalker` until D-AS3-BLOCK-REL-IMM19/26 closes them. (5) simplifier F1 — new `asm_byte_emit::writeU32LEAt` helper replaces hand-coded 4-byte LE store in the patch loop. New diagnostic code `A_FunctionEncodeAborted = 0x1006`. **Corpus shape note** (user emphasis 2026-06-03): each runnable example is a future OPT1 (13.6) differential-verification gate. Cycle-2 expansion (factorial / fibonacci / cmp_all_signed_conds) anchors distinct CFG shapes (multiplicative loop / 2-var phi rotation / sequential if/else stack). Anchored future cycles: D-OPT-JCC-FALLTHROUGH (elide trailing uncond jmp when ifFalse is next-laid-out block; saves 5 bytes per jcc); D-LIR-SETCC-DEAD-AFTER-FUSION (setcc's result is dead after ICmp+CondBr fusion — DCE removes wasted bytes); D-LIR-SETCC-WIDTH-CONTRACT (model byte/qword in LirReg so zero-extend isn't ad-hoc); D-AS3-COND-CODE-ARM64 (ARM64's cond-code table); D-AS3-BLOCK-REL-IMM19/26 (ARM64 branch displacement encoding); D-CSUBSET-LONG-BRANCH (function bodies > 2GB need rel32-overflow thunks). D-LIR-TRUNC-ENCODING (`trunc` mnemonic has no encoding; unblocks bool-stored / bool-returned MIR patterns); D-CSUBSET-UNSIGNED-CMP-CORPUS (unsigned cond-codes corpus once c-subset gains `unsigned`); D-LIR-FUSION-USE-COUNT-GATE (single-use guard on ICmp+CondBr fusion when a 2nd consumer of the setcc result arrives); D-SCHEMA-JSON-DUPLICATE-KEY-DETECT (silent-failure HIGH #1 — JSON object duplicate keys silently keep last value; project-wide sweep); D-CSUBSET-BOOL-ZEXT-CONTRACT (architect — document where bool zero-extension is guaranteed for the non-fused fallback arm); ~~D-CSUBSET-MULTI-FN-WIN64-CC~~ ✅ **CLOSED in cycle 2 post-fold 2026-06-03** — the original anchor mis-diagnosed (suspected wrong CC); actual bug was the trampoline injector's `resolveUserEntrySymbol` falling back to `functions[0]` when format.entryPoint was empty, silently picking the first-declared function (helper, not main). Closed via new `AssembledModule.userEntrySymbol: optional<SymbolId>` populated by compile_pipeline from grammar.semantics().declarations[].implicitReturnZeroForFunctionNames (source-agnostic — c-subset declares "main", future languages name their own). 7-agent fold (3 agents, strong convergence): (a) silent-failure HIGH #1 + code-reviewer #1 — silent first-match ambiguity → ALL matches now collected, fail-loud `K_SymbolUndefined` if >1 (never silently pick); (b) silent-failure HIGH #3 + code-architect Q5 — `functions[0]` fallback was the original bug class; now ONLY triggers for single-function modules (multi-function modules with no explicit userEntrySymbol nor format.entryPoint emit NotFound rather than silently invoke wrong fn); (c) code-architect Q3 — inline type-discriminator comments on `imageEntryOverride` (INDEX) and `userEntrySymbol` (SymbolId). New runnable example `examples/c-subset/multi_function/` (max-of-3 via helper, exit 7) pins the end-to-end fix. Newly anchored: D-CSUBSET-ENTRY-NAME-SPLIT (split `implicitReturnZeroForFunctionNames` from entry-fn-name list when a language gets implicit-return-0 on a non-entry function); D-CSUBSET-MULTI-FN-ENTRY-LK1-1-RETIRE (D-LK1-1 real-symbol-name preservation will let format.entryPoint match by name, retiring AssembledModule.userEntrySymbol); D-LK10-ENTRY-CASCADE-DEDUP (silent-failure MEDIUM #4 — encode-failure cascade dedup when assemble dropped the entry function); D-LK10-ENTRY-RESOLVE-INDEX (silent-failure LOW #5 — anchor O(1) entry-fn lookup when >1k functions land). **Cycle 2 corpus expansion added more runnables**: nested_loop/ (3*4=12; CFG nested back-edges + counter live-across-loops); early_return/ (cumulative sum exits at 21 via `return` inside loop body's `if`); countdown_sum/ (10+9+...+1=55 via `n != 0` cond — exercises Ne cond-code for loop-exit, distinct from sum_loop's `<=`); multi_function/ (max-of-3 via helper, exit 7); recursive_factorial/ (fact(5)=120 self-recursion); two_helpers/ (doubled→square chain, exit 100); helper_before_main/ (sub(50,8)=42 regression pin for D-CSUBSET-MULTI-FN-WIN64-CC); loop_invariant/ (D-OPT1-LICM-CORPUS — 5 iter × a*b=12 = 60; pins LICM-safe rewrites); cse_candidate/ (D-OPT1-CSE-CORPUS — (5+3)*2 + (5+3)*3 = 40; pins CSE-safe value-numbering). Total: **14 runnable corpus examples** exercising distinct CFG / arithmetic shapes — each is a future OPT1 (13.6) differential-verification gate. 166/166 ctest (latest). **Cross-platform CI fix 2026-06-03** (commit `a6bc3d2`): explicit `<algorithm>` + `<format>` includes + `std::ranges::any_of` → `std::any_of` in compile_pipeline.cpp (Linux gcc-13 + macOS Apple-clang both lacked transitive includes that MSVC provided). | step 13.4 | ✅ **CLOSED 2026-06-03** |
| 13.6 cycle 1 | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT1 | ✅ **CLOSED 2026-06-03 — OPT1 cycle 1 substrate + corpus negative pins.** **Cycle 1 deliverable**: (a) **5 OPT1-correctness negative pins** per test-analyzer rec (dce_negative_pin → exit 100, const_fold_inside_expr → exit 42, copy_prop_across_join → exit 10, licm_conditional_mutation → exit 68, cse_noncommutative → exit 58). Each pins a SPECIFIC bug class a future OPT1/OPT2 pass could introduce, with exit-code distance making the bug bisectable. (b) `src/opt/optimizer.{hpp,cpp}` with `optimize(Mir, TargetSchema, OptPipeline, reporter) → Mir` + closed `PassId` enum (cycle 1: only `Identity` no-op). (c) compile_pipeline step 3.5 between MIR build + MIR→LIR, wired with default `[Identity]` pipeline so cycle 1 exercises the engine's loop + dispatch end-to-end without behavioral change. (d) New `X_UnknownPassId = 0x2001` diagnostic (the X_* family opener for the optimizer tier). **3-agent post-fold** (strong convergence): code-reviewer C1 missing `<vector>` include → fixed; C2 + silent-failure F1 silent enum-drift fallback → X_UnknownPassId now emits before return-false; I1 `OptPipeline::name` string_view dangle risk → switched to owned `std::string` before OPT2 JSON pipelines land; silent-failure F2 empty-pipeline never exercises Identity → default is now `{Identity}` so the dispatch loop ALWAYS runs; silent-failure F3 false-return-without-diagnostic contract → entryErrorCount snapshot + assertion belt-and-suspenders in optimize(); silent-failure F4 cse_noncommutative comment arithmetic corrected (70 buggy-CSE, not 76). **Newly anchored future cycles**: D-OPT1-PASS-ID-STABILITY (ordinal-append-only); D-OPT1-VERIFY-AFTER-EVERY-PASS (slot for OPT2's first real pass to land the verifier hook); D-OPT1-PIPELINE-FROM-CONFIG (resolve pipeline name → PassId list from JSON when OPT2 lands); D-OPT1-RETURN-FALSE-DIAGNOSTIC-CONTRACT (every false-return path emits a diagnostic); D-OPT1-PASS-RUN-MAX-ITER (`vector<PassId>` → `vector<PassRun>` when first fixed-point pass lands); D-OPT1-PIPELINE-BUDGET (OPT10 autotuner concern); D-OPT1-PIPELINE-MAX-ITERATIONS (whole-pipeline-fixed-point if/when needed). Total corpus: **19 runnable c-subset programs** (14 positive shape + 5 OPT1-negative-pin). 172/172 ctest. **NEXT cycle 2 = OPT2 const-fold** (the highest-leverage pass; reuses CE1-CE5's const-eval engine; differential-verified against all 19 corpus pins). Per code-architect Q3: jump straight to const-fold — dominators (mir_dominators) NOT needed yet; lands later with copy-prop. | step 13.5 ✅ | ✅ **cycle 1 CLOSED 2026-06-03** |
| 13.7 cycle 1 | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT2 const-fold | ✅ **CLOSED 2026-06-03 — MIR-tier ConstFold pass + JSON pipeline loader + verifier hook + differential-verify runner + OptResult shape + FFI dead-code removal.** PassId::ConstFold landed alongside the engine substrate: `runConstFold(Mir, TypeInterner, reporter) → ConstFoldResult` rebuilds the MIR functionally via `MirBuilder`, walks each function's blocks/insts in scan order with a `MirInstId.v → MirInstId` rewrite map, folds any pure-functional integer/bool/bitwise/ICmp opcode whose operands all resolve to `Const` via the `hir/const_eval_arith.hpp` helpers (`applyBinaryInt` / `applyUnaryInt` / `wrapToIntTarget`). Wrap semantics (no per-inst `nsw`/`nuw`); div-by-zero + shift-out-of-range defer to runtime via verbatim copy. **JSON pipeline loader** (D-OPT1-PIPELINE-FROM-CONFIG): `*.pipeline.json` 7-step loader mirroring `TargetSchema::loadFromText`; ships `debug.pipeline.json` (Identity) + `release.pipeline.json` (Identity+ConstFold) under `src/dss-config/pipelines/`. Unknown-keys + empty-passes-array reject loud (`X_PipelineMalformed`); unknown pass names → `X_UnknownPassName` (config-load analog of runtime `X_UnknownPassId`); 4 new diagnostic codes 0x2002-0x2005. **PassId single-source table** (`kPassNameTable`): one row per pass; `optPassIdFromName` + `optPassIdName` + ordinal-stability static_assert all derive from it. Closes the 4-site drift the 7-agent review surfaced. **D-OPT1-VERIFY-AFTER-EVERY-PASS** live: `MirVerifier::verify` runs after every successful pass under all build modes; verifier-detected SSA violations are build breaks. `optimize()` signature widened with `TypeInterner const&` so the verifier's interner-gated rule set (CondBr-is-Bool, Return-matches-FnSig, Arg-in-range, no-Extension-types) is active. **OptResult shape** (D-OPT1-OPT-RESULT-SHAPE): bool→`OptResult { ok, passesRun, passesMutated, fixedPointReached }`. **examples_runner differential-verify** (D-OPT1-DIFFERENTIAL-VERIFY-RUNNER): manifests declare `optimizedPipelines: [{label, passes}]`; runner compiles + spawns each arm with `Program::setOptimizerPipelineOverride`, asserts baseline-equal exit + stdout. `ArmResult` is a tri-state enum (Ran / SkippedCrossHost / Poisoned) — closes the silent compile-fail-vs-cross-host conflation. First arm pinned on `const_fold_inside_expr` (exit 42, constfold-only); other 4 negative pins arm up when their target pass lands. **MirBuilder rewrite-map fail-loud** (D-OPT2-REWRITE-MAP-COMPLETENESS): rewriteOperand aborts on missing entry; pre-fold the invalid `MirInstId{}` was admitted by three downstream safety nets (cross-arena check + 2 verifier `if (!op.valid()) continue` arms). **3 unit-test files** under `tests/opt/`: test_optimizer (UnknownPassId 1-count + OptResult shape + optPassIdFromName all-enumerators); test_const_fold (Add/Sub fold + Arg-not-foldable + div-zero defer + ICmp→Bool + **G1 i32-wrap-on-overflow** + **G3 nested constCache pickup** + **G4 multi-block CondBr CFG preservation**); test_pipeline_loader (shipped debug/release + 6 reject paths inc. empty-array + malformed-JSON + non-string-entry). **FFI dead-code removal**: `src/dss-config/ffi-headers/` tree + `findShippedFfiHeader` + `readCHeaderShipped` + `HeaderReadErrorKind::InvalidShippedPath` + 8 corresponding tests removed — sole consumers were the tests of the headers themselves; no production caller. Diagnostic codes `C_InvalidShippedFfiHeaderPath` (0xC034) + `F_HeaderInvalidShippedPath` (0x500F) retired (numbers reserved, marked "RETIRED 2026-06-03"). FF1/FF2/FF5 substrate (readCHeader/readCHeaderFromText/binary_readers/ingest()) stays as latent capability — unused features, not dead code. **7-agent review fold** consolidated (Critical: rewriteOperand fail-loud + runtime-init-globals ok=true skip + fs::file_size no-throw; High: PassId table consolidation + isICmp removal + closed-key helper + ArmResult enum + OptPipeline empty-passes reject + comment cleanup). **6 anchors registered** for follow-up: D-OPT-COMPILE-OPTIONS-STRUCT (8th parameter trigger), D-OPT-FIXED-POINT-LOOP (sub-pipeline design), D-OPT-INTERNER-MUTABLE (first type-creating pass), D-OPT-PASS-METRICS (sink-pattern), D-OPT-PRE-PIPELINE-VERIFY (debug-mode), D-OPT-RUNNER-SHARD (corpus×pipeline scaling). 175/175 ctest. Differential-verify runner caught zero issues — both arms identical exit 42 on const_fold_inside_expr, confirming ConstFold neither breaks nor mis-optimizes the live-edge pattern. | step 13.6 ✅ | ✅ **cycle 1 CLOSED 2026-06-03** |
| 13.7 cycle 2 | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT3 DCE | ✅ **CLOSED 2026-06-03 — first DESTRUCTIVE MIR pass + CompileConfig threading + linkage assertion.** `PassId::Dce` lands with 3 reachability layers (inter-procedural live-symbol BFS, per-function CFG RPO, intra-block live-inst worklist). `mir_cfg.hpp` extracted (verifier ↔ optimizer share `mirReversePostOrder`). `CompileOptions` struct + `resolvePipelineName` constexpr table close D-OPT-COMPILE-OPTIONS-STRUCT + D-OPT1-PIPELINE-CONFIG-FROM-COMPILECONFIG: CLI `--config=release` now actually loads `release.pipeline.json` (which gains `Dce`). **D-OPT2-DCE-LINKAGE-SYMTAB-ASSERTION CLOSED** via `tests/opt/test_dce_linkage.cpp` — hand-built MIR with Global/Default + Local-binding functions, asserts post-DCE survival via `mir.funcBinding/Visibility` (format-blind; doesn't need FF1-PE since PE `.exe` has zero `IMAGE_SYMBOL` by design). 6 DCE tests + 3 linkage tests + 4 CompileOptions/resolvePipelineName tests + Phi-preservation pin in test_const_fold + dce_negative_pin gains `optimizedPipelines: [{label:"dce-only", passes:["Dce"]}]`. **7-agent fold critical items**: DCE runtime-init carve-out emits `X_OptPassSkipped` Info (parity with ConstFold); `LiveSymbolScanner` dedupes per-function liveInsts so rebuild reuses them (no double-scan); Phi unreachable-predecessor crash fixed; `resolvePipelineName` returns `optional<string_view>` with caller-side fail-loud on out-of-range CompileConfig; `kPipelineNameTable` `kPipelineNameTableInOrder()` swap-drift guard mirroring `kPassNameTable`; TRAP docblock corrected; dead `fn_` + `symToGlobal_` fields removed. **6 anchors registered** for follow-on cycles: D-OPT-MIR-REBUILDER-EXTRACT (OPT4 — 3rd-consumer trigger), D-OPT-DOMTREE-EXTRACTION (OPT4 — first non-verifier consumer), D-OPT-PIPELINE-OVERRIDE-VALUE-TYPE (async/shared_ptr trigger), D-OPT-COMPILE-OPTIONS-VARIANT (3rd overlapping field), D-OPT-RUNPASS-EXHAUSTIVE-WARN (next PassId enumerator), D-OPT-RESULT-CHANGED-FIELD (LICM trigger — mutate-without-counter). **Plan 22 §0.1 cycle stepper added** per user request: single navigation surface for cycle-by-cycle progress with commits + closure status. 178/178 ctest. | step 13.7 cycle 1 ✅ | ✅ **CLOSED 2026-06-03** |
| 13.7 cycle 3 | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT4 c1 pre-cond refactors | ✅ **CLOSED 2026-06-03** — pure-refactor pre-conditions for Mem2Reg/CopyProp. `mir_dom.hpp` extracted from `mir_verifier.cpp` (Cooper-Harvey-Kennedy idom + tri-state dominates + NEW `mirDominanceFrontier`). `mir_rebuild_helper.{hpp,cpp}` extracted from ConstFold+DCE: `MirRebuildPolicy` virtual base + `MirFunctionRebuilder` concrete with 7-hook policy interface; ConstFold + DCE refactored to `ConstFoldPolicy` + `DcePolicy`. 3 new dom-tree tests (diamond + DF + trivial). **7-agent fold**: restored I_VerifierFailure for out-of-range successor edges (CRITICAL — extraction docblock claimed `checkStructuralInvariants` caught them; it didn't); `mirDominanceFrontier` skips `gaveUp` blocks + aborts loud on step-cap overflow; `MirDomTree::gaveUp` switched `vector<bool>` → `vector<uint8_t>`; `onZeroPhiIncomings` signature gains `(oldPhi, oldBlock, oldFn)` for debuggable diagnostics; `instructionsSkipped` counter moved from rebuilder to policy (layering fix); dead `<algorithm>` include + dead `MirOpcode op` param dropped; provenance/WHAT-comment cleanup. **8 anchors registered** for OPT4 c2/c3+ (D-OPT-MIR-REBUILDER-ONBLOCKBEGIN-HOOK / D-OPT-MIR-DOM-HPP-CPP-SPLIT / D-OPT-COPYPROP-BLOCK-CURSOR / D-OPT-MIR-DOM-STEPCAP-INCONSISTENCY / D-OPT-MEM2REG-PROMOTABLE-ALLOCA-SCAN / D-OPT-MIR-DOMTREE-CLASS-INVARIANT / D-OPT-REBUILDER-IDENTITY-POLICY-TEST / D-OPT-DOMFRONTIER-LOOP-TEST). 179/179 ctest. | step 13.7 cycle 2 ✅ | ✅ **CLOSED 2026-06-03** |
| 13.7 cycle 4 | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT4 c2 Mem2Reg | ✅ **CLOSED 2026-06-03 — Cytron-Ferrante SSA construction** — `src/opt/passes/mem2reg.{hpp,cpp}` + `PassId::Mem2Reg = 3` + release pipeline `[Identity, ConstFold, Mem2Reg, Dce]`. Substrate: `MirRebuildPolicy::onBlockBegin` hook (closes `D-OPT-MIR-REBUILDER-ONBLOCKBEGIN-HOOK`); `mir_dom.hpp` gains `mirDomTreeChildren` + `mirIteratedDominanceFrontier`. Promotability gate (closes `D-OPT-MEM2REG-PROMOTABLE-ALLOCA-SCAN`): walks reachable blocks; only `Load[op0]`/`Store[op1]` accepted; scalar form only; Volatile + Phi-value escape + gaveUp-block-touch all disqualify. Rename walk iterative (Visit/Leave frame stack — no stack overflow on deep CFGs). Phi-incoming completeness invariant enforced (every PendingPhi has exactly reachable-pred-count incomings). **The 4 user-mandated risk pins**: (1) promotability gate → `AddressTakenAllocaNotPromoted` + `VolatileAccessAllocaNotPromoted` + `AllocaReturnedAsPointerNotPromoted`; (2) Phi at DF → `DiamondConditionalStoreInsertsPhi` + `LoopInductionVariablePromoted`; (3) differential-execution → `examples/c-subset/copy_prop_across_join` `mem2reg-only` arm spawns OS process under Mem2Reg + diff-asserts byte-equal vs baseline; (4) CFG safety → `StructCfMarkersUnchanged` byte-compares pre/post block markers. **7-agent fold**: Volatile gate (silent-failure CRITICAL); gaveUp gate (silent-failure CRITICAL); RPO-reachable analyze (code-reviewer CRITICAL); iterative rename (HIGH-5); phi-incoming completeness (HIGH-6); dead `phiNewBlock_` + `snapshotMarkers` removed; temporal-provenance comment-header rewrites; AGNOSTICISM AUDIT CLEAN. `LoopCfgDominanceFrontier` test pin closes `D-OPT-DOMFRONTIER-LOOP-TEST`. 180/180 ctest. | step 13.7 cycle 3 ✅ | ✅ **CLOSED 2026-06-03** |
| 13.7 cycle 5 | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT4 c3 SSA copy-prop | ✅ **CLOSED 2026-06-03 — SSA Phi-collapse** — `src/opt/passes/copy_prop.{hpp,cpp}` + `PassId::CopyProp = 4` + release pipeline `[Identity, ConstFold, Mem2Reg, CopyProp, Dce]`. Substrate: new `MirRebuildPolicy::substituteOldOperand` hook (pre-rewrite, OLD-id space; 6 wrap sites in `mir_rebuild_helper.cpp`); shared `cloneGlobalsOrCarveOut` helper extracted (eliminates 4× duplication of runtime-init globals carve-out + global-clone prelude). Algorithm: worklist + path-compression on OLD ids; post-compression invariant assertion fail-loud (silent-miscompile vector closed). `unordered_map<MirInstId, MirInstId>` strong-id keys (kills manual `.v`/arenaTag round-trip latent bug surface). Dead Phis stay for DCE sweep. **8 unit tests** + corpus `phi_collapse_same_value/` (exit 42; 2 differential arms) + `copy_prop_across_join` gets `copyprop-after-mem2reg` arm (idempotency on non-collapsible). **7-agent fold**: post-compression assertion (silent-failure CRITICAL); cycle-case comment for `distinct.size() != 1` invariant (silent-failure HIGH); strong-id map keys (type-design HIGH); shared carve-out helper (~90 LOC saved); multi-function + runtime-init parity tests; test rename + comment cleanup; AGNOSTICISM CLEAN. Closes `D-OPT-COPYPROP-PHI-COLLAPSE-CORPUS` + activates `D-OPT1-COPY-PROP-JOIN-PIN` runtime arm. Defers `D-OPT-COPYPROP-BLOCK-CURSOR` + `D-OPT-FIXED-POINT-LOOP` + `D-OPT1-PASS-RUN-MAX-ITER` to OPT5+ (general operand-substitution variant + pipeline-level fixed-point; this pass is internally fixed-point + idempotent). 182/182 ctest. | step 13.7 cycle 4 ✅ | ✅ **CLOSED 2026-06-03** |
| 13.7 cycle 6 | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT5 c1 CSE/GVN | ✅ **CLOSED 2026-06-03 — dom-tree-scoped value numbering** — `src/opt/passes/cse.{hpp,cpp}` + `PassId::Cse = 5` + release pipeline `[Identity, ConstFold, Mem2Reg, CopyProp, Cse, Dce]`. Algorithm: iterative dom-tree DFS with Visit/Leave frame stack + scoped hash table (rolled back at Leave); operand canonicalization for commutative 2-operand ops; transitive resolution through `cseMap_`; path-compression with fail-loud post-condition. Same `substituteOldOperand` substitution shape as CopyProp — dead duplicates DCE-swept. Substrate additions: new `MirOpcode::isCommutative` constexpr predicate; new `path_compress.hpp` shared `resolveTransitive` + `pathCompressAndVerify` template helpers (extracted from CopyProp + CSE — single source of truth for the chain-walk + cycle-fail-loud + post-compression invariant assertion). Excluded opcodes: side-effecting + terminator + Phi + Load (alias-unsafe defer) + Volatile + Alloca (defensive). **D-OPT1-CSE-NONCOMMUTATIVE-PIN runtime arm activated** via `cse_noncommutative`'s `[Mem2Reg, Cse]` arm → exit 58 (NOT the buggy 70 a permissive predicate would produce). `cse_candidate` gains 3 differential arms. **11 unit tests** including 2 fold-now additions: ResultTypeDiscriminatesKey + TransitiveChainResolves. **7-agent fold**: defensive Alloca exclusion (silent-failure HIGH); boost-style hashCombine replacing XOR-of-shifts (type-design FOLD-NOW); LogEntry collapsed to vector<CseKey> (type-design FOLD-NOW); shared path_compress.hpp helper (simplifier FOLD-NOW — ~50 LOC dedup); 2 new tests + comment cleanup. AGNOSTICISM CLEAN. 183/183 ctest. | step 13.7 cycle 5 ✅ | ✅ **CLOSED 2026-06-03** |
| 13.7 cycle 7 | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT5+ c2 SimplifyCFG + fixed-point | ✅ **CLOSED 2026-06-03 — branch-folding + empty-block jump-threading + pipeline-level fixed-point loop** — `src/opt/passes/simplify_cfg.{hpp,cpp}` + `PassId::SimplifyCfg = 6` + release pipeline `[Identity, ConstFold, Mem2Reg, CopyProp, Cse, SimplifyCfg, Dce]` with `maxIterations: 4`. Branch-fold: `CondBr(Const(true\|false), T, F)` → `Br(T\|F)`; folded-out arm goes unreachable → next-iter DCE sweeps. Empty-block jump-thread: trampoline `B` with only `Br(S)` terminator + S has no Phi nodes (conservative) → elide B; preds redirect to S via path-compressed `jumpThreadMap_`. 2 new `MirRebuildPolicy` hooks (`redirectBlockTarget` + `tryRewriteTerminator`); fail-loud `mapSucc` replacing `blockMap_.at`. Pipeline fixed-point: `OptPipeline.maxIterations: uint8_t` (range [1, 32]; loader rejects out-of-range); engine reruns passes until convergence OR cap. `OptResult.fixedPointReached` default `true → false` (pessimistic). **Closes `D-OPT-FIXED-POINT-LOOP` + `D-OPT1-PASS-RUN-MAX-ITER`**. **Out-of-scope** (anchored): general block-merge (Phi-incoming fan-out), StructCfMarker re-derivation (D-OPT4-1 — branch-fold + empty-elide PRESERVE markers since they don't change roles), Switch-on-Const folding. **7-agent fold**: fail-loud `mapSucc` (silent-failure CRITICAL — `at` throws std::out_of_range with no context); `recordTerminatorInRewrite override return false` (silent-failure HIGH — branch-folded Br id was being mis-recorded under CondBr's id); `OptResult.fixedPointReached` default `true → false` (HIGH); `elided_` set dropped (simplifier — single source of truth in `jumpThreadMap_`); `maxIterations` uint16→uint8 (type-design); 5 new engine/loader tests; AGNOSTICISM CLEAN. 184/184 ctest. | step 13.7 cycle 6 ✅ | ✅ **CLOSED 2026-06-03** |
| 13.7 cycle 8 | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT6 LICM | ✅ **CLOSED 2026-06-03 — natural-loop detection + invariant hoist + new onBlockBeforeTerminator hook** — `src/opt/passes/licm.{hpp,cpp}` + `PassId::Licm = 7` + release pipeline `[Identity, ConstFold, Mem2Reg, CopyProp, Cse, Licm, SimplifyCfg, Dce]` with `maxIterations: 4`. Algorithm: natural-loop forest from back-edges (`mirNaturalLoops` in `mir_dom.hpp`); hoist pure invariants whose ALL operands are defined OUTSIDE the loop body, to the unique non-back-edge predecessor of the header (the preheader). Out-of-scope (anchored): preheader insertion for multi-pred headers (D-OPT6-LICM-PREHEADER-INSERTION); chained-invariant resolution (D-OPT6-LICM-CHAINED-INVARIANTS); trap-eligible hoist via value-range analysis (D-OPT6-LICM-TRAP-SAFE-HOIST); Load hoist (D-OPT-LOAD-ALIAS-ANALYSIS — shared with CSE). New `MirRebuildPolicy::onBlockBeforeTerminator` hook closes `D-OPT-MIR-REBUILDER-ONBLOCKBEFORETERMINATOR-HOOK`. **7-agent fold**: determinism — body + loops sorted (silent-failure HIGH); `recordHoist` single-writer chokepoint (type-design); body invariant assertion in `mirNaturalLoops`; `!opBlock.valid()` defensive guard; 3 new tests including `InvariantHoistLandsInPreheaderNotLoopBody` (verifies placement, not just counter). **Strengthens 96bb941**: 2 new marker-preservation tests in test_simplify_cfg making D-OPT4-1 REAL. AGNOSTICISM CLEAN. 185/185 ctest. | step 13.7 cycle 7 ✅ | ✅ **CLOSED 2026-06-03** |
| 13.7 cycle 9 | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT5+ c3 block-merge + marker re-derivation | ✅ **CLOSED 2026-06-03 — general block-merge + StructCfMarker re-derivation (single-non-Linear-per-chain)** — `src/opt/passes/simplify_cfg.{hpp,cpp}` extended. For `(P, B)` where `P.term == Br(B)`, `preds[B] == {P}`, B has no Phi, B.instCount > 1, ≤1 of {P.marker, B.marker} non-Linear, and chain doesn't already have non-Linear: B's non-terminator insts inline into P; B's terminator becomes merged's. New `MirRebuildPolicy::absorbSuccessor` hook + `mapOperand` helper. `chainNonLinearMarker_` per-head tracking enforces ≤1 non-Linear per chain at admit; `onBlockBegin` overrides via setBlockMarker. **Closes `D-OPT5-BLOCK-MERGE` + partial `D-OPT4-1`** (single-non-Linear case). New deferred anchor `D-OPT4-1-NON-LINEAR-MARKER-MERGE` (both-sides-non-Linear via CFG-shape-driven re-derivation). **7-agent fold**: 2 cap-exhaustion silent returns → fail-loud (silent-failure CRITICAL); jumpThreadMap_/absorbedToHead_ disjointness fail-loud (HIGH); phase-3 blockMap-miss fail-loud (HIGH); chain-non-Linear invariant tracking (HIGH); absorbedBlocks_ dropped; mapOperand extracted; tighter Phi-pred pin; new ChainNonLinearMarkerRejectsSecondAdmission test. 8 new block-merge tests. AGNOSTICISM CLEAN. 185/185 ctest. | step 13.7 cycle 8 ✅ | ✅ **CLOSED 2026-06-03** |
| ~~13.7 cycle 10a-10g~~ | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT7-prep — alias substrate | ✅ **CLOSED 2026-06-03..04** — alias-analysis substrate end-to-end: `mirMayAlias` 7-rule TBAA predicate + `StrictTbaa` enum + region walker (`mirRegionBetween` + `mirAnyMayAliasingStoreInRegion`) + CSE/LICM Load admission gate + `MirAliasingMode` per-Mir flag + `MirBuilder::setAliasingMode` propagation through every pass via `cloneGlobalsOrCarveOut` + `SemanticConfig::PointerAliasingRules { strictAliasingOnDistinctTypes, charTypesAliasAll }` JSON struct + loader (unknown-key fail-loud) + C99 §6.5 ¶7 character-type exception config-DECLARED (not hardcoded) + `mir_dom.hpp` split (hpp + cpp) + `IdentityPolicy` round-trip test (`DSS_EXPORT` discipline on `MirRebuildPolicy` + `MirFunctionRebuilder` + `cloneGlobalsOrCarveOut`). Closes D-OPT-LOAD-ALIAS-ANALYSIS + D-OPT-LOAD-ALIAS-ANALYSIS-STRICT-TBAA-WIRING + D-OPT-MIR-ALIAS-CHAR-EXCEPTION-OVERRIDE + D-OPT-LOAD-ALIAS-ANALYSIS-PIPELINE-PROPAGATE + D-OPT-MIR-DOM-HPP-CPP-SPLIT + D-OPT-REBUILDER-IDENTITY-POLICY-TEST. | step 13.7 cycle 9 ✅ | ✅ **CLOSED 2026-06-03..04** |
| ~~13.7 cycle 10h-10o~~ | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT7-prep — capstone + LICM hardening + c-subset substrate | ✅ **CLOSED 2026-06-04** (autonomous-run series; branch tip `ec15e0c0`). **10h** `73ed927`: c-subset deref smoke fixtures (`deref_load` + `deref_store`) + `long`→I64 added to c-subset.lang.json + I32→I64 sext encoding `movsxd r64,r/m32 = REX.W 0x63 /r` in x86_64.target.json + smoke fixture `long_primitive_smoke` with `0 - 1` discriminating sentinel + GCC CI fix for cycle-10g's `ASSERT_EQ`-in-non-void-helper. **10i** `d222f20`: alias-arc strict capstone — corpus fixture `examples/c-subset/strict_alias_cse_capstone/` (`compute(int* pI, long* pL){int a=*pI; *pL=7; return a+*pI;}` two-armed) + MIR-tier `Optimizer.EffectivenessAliasArcStrictTBAACapstone` (3 ARMs: Identity / `[Cse, Dce]` / shipped release with explicit "release.pipeline.json contains Cse" pin). **10j** `1f5140f` + `35cc798`: hard-stopped trap-safe-hoist per autonomous-loop guardrail (negative miscompile pin requires SDiv codegen — anchored `D-CSUBSET-DIVISION-OP-CODEGEN`); CLOSED D-OPT6-LICM-CHAINED-INVARIANTS via per-loop fixed-point iteration + silent-miscompile negative regression test. **10k** `a28567c`: D-CSUBSET-LOCAL-INT-CODEGEN-NEGATIVE-PIN closed — new `tests/test_support/mutate_target_schema.hpp` helper `mutateShippedTargetSchemaJson` (JSON-mutation beats parallel-broken-file). **10l** `7629f5f`: LICM ambiguous-preheader silent skip upgraded to Info `X_OptPassSkipped` citing D-OPT6-LICM-PREHEADER-INSERTION. **10m** `a9590b8`: D-CONFIG-DIAGNOSTIC-CODE-PER-KIND — added `C_InvalidTargetName=0xC035` + `C_InvalidFormatName=0xC036`. **10n** `e6c6453`: doc cleanup. **10o** `14ff9e1`: 3-audit FOLD-NOW — LICM iter cap structural-derived; capstone tightened to `mutationCount[Cse] == 1u` + Add-operand-identity pin (proves CSE substitution direction). **CI fix** `abb9a4c`: missing `#include <format>` in licm.cpp + defensive `<span>` in target_schema.cpp (unblocked macOS clang + Linux GCC). **Plan 22 alignment** `ec15e0c0`. AGNOSTICISM CLEAN throughout; standing rules attested every cycle. 191/191 ctest. | step 13.7 cycle 10a-10g ✅ | ✅ **CLOSED 2026-06-04** |
| ~~13.7 cycle 10p~~ | [`12`](./12-mir-lir-plan%20-%20ok.md) Arc A foundation — implicit-register-constraint substrate | ✅ **CLOSED 2026-06-04** (`cb3ac20`) — config-driven `ImplicitRegisterConstraint` per-opcode block on `TargetOpcodeInfo`. Loader-tier name→ordinal resolution (post-register-load) + 6 rejection arms (empty-block typo discriminator, unknown register name, within-array duplicate, unknown sub-key per D-CONFIG-LOADER-UNKNOWN-KEYS-FAIL-LOUD, non-object block, non-array sub-field, non-string entry). 11 new tests. Substrate ships **unconsumed** in 10p — pre-cycle-10q invariant pin asserts no shipped opcode declares it yet. Closes **D-TARGET-IMPLICIT-REGISTER-CONSTRAINT**. Unlocks cycle 10q's idiv/cdq/cqo + future shift-by-CL + ARM64 fixed-operand ops + SPIR-V fixed-arg ops by JSON-only declaration — zero `if (target == x86_64)` branches. Foundation of Arc A (division codegen). | step 13.7 cycle 10h-10o ✅ | ✅ **CLOSED 2026-06-04** |
| ~~13.7 cycle 10q~~ | [`13`](./13-assembler-plan%20-%20tbd.md) Arc A continuation — compound div opcodes + MIR→LIR Div arm + regalloc consumer | ✅ **CLOSED 2026-06-04** — compound div opcodes (`sdiv_compound` = REX.W CQO + REX.W IDIV /7; `udiv_compound` = XOR EDX,EDX + REX.W DIV /6) declared in x86_64.target.json with `implicitRegisters` declarations consuming the 10p substrate. MIR→LIR `lowerDiv` reads ordinals + classes config-driven (post-7-agent-fold F1: register class from schema, not hardcoded GPR); emits 3-LIR-op sequence (mov-to-rax + compound + mov-from-rax). Regalloc consumes `implicitRegisters.clobbered` via new `collectImplicitClobberPositions` + `implicitClobbersCrossedBy`. 4 new tests pinning: byte-level sdiv/udiv discrimination (FLAG 1 — different first bytes prove SDiv/UDiv route to different opcodes); MIR→LIR routing (UDiv→udiv_compound NOT sdiv_compound). 2 new anchors: `D-MIR-TO-LIR-DIV-SEQUENCE-AGNOSTIC` (3-LIR-op shape is x86-structural; ARM64's single-inst SDIV needs config-driven sequence-expansion when 2nd target lands) + `D-CSUBSET-MOD-OP-CODEGEN-OUTPUT-INDEX-CONTRACT` (role-tagged outputs for future SMod/UMod) + `D-CSUBSET-UDIV-RUNTIME-HIGH-BIT-PIN` (behavioral test when c-subset unsigned types arrive) + `D-OPT-REGALLOC-EXCLUSION-BUFFER` (growable exclusion buffer when union exceeds 8). | step 13.7 cycle 10p ✅ | ✅ **CLOSED 2026-06-04** |
| ~~13.7 cycle 10r~~ | [`14`](./14-linker-plan%20-%20tbd.md) Arc A capstone — c-subset division corpus + REX-overlap bug fix | ✅ **CLOSED 2026-06-04** (`50a07ab`). Initial 10r-WIP (`6b4881b`) added regalloc-exclusion semantic improvement (covers-semantics + forbidden=inputs∪clobbered) but the new corpus surfaced a deeper bug: cycle-10q's compound encoding silently lost REX.B for high-reg (R8-R15) divisors → STATUS_INTEGER_DIVIDE_BY_ZERO at runtime. **10r BUG FIX**: split the compound opcodes (`sdiv_compound`/`udiv_compound`) into 4 single-instruction opcodes (`cqo`, `idiv_op`, `xor_rdx_zero`, `div_op`) so each gets its own encoder-computed auto-REX. Introduced `DivSlotPair` named-aggregate (prevents pre/core transposition at the type level). New `outputs ⊆ clobbered` schema-loader invariant (catches JSON-typo silent-miscompile class). Corpus `examples/c-subset/division/` returns 14 (=100/7) end-to-end on Windows. Closes `D-CSUBSET-DIVISION-OP-CODEGEN` + `D-CSUBSET-DIVISION-CORPUS-MATERIALIZE-BUG`. Unblocks `D-OPT6-LICM-TRAP-SAFE-HOIST`. 192/192 ctest green. **7-agent fold applied 10 FOLD-NOW items**: DivSlotPair (type-design 9/10) + preInfo validation (silent-failure F2 8/10) + ASSERT_EQ tightening (F3+TypeC4) + outputs⊆clobbered invariant (F1 9/10) + R8/R15 byte pins (pr-test #1 9/10) + NoLegacyCompoundDivideOpcodes anti-regression (pr-test #2 8/10) + UDiv survives-pipeline (pr-test #3 7/10) + unordered identity (F4 7/10) + unified byte-pin helper (simplifier #2) + OVERRIDE→superseded wording fix (comment-analyzer #3). | step 13.7 cycle 10q ✅ | ✅ **CLOSED 2026-06-04** |
| 13.7 cycle 11+ | [`22`](./22-optimizer-plan%20-%20tbd.md) OPT7+ inlining | Tier-neutral `scalar_eval` consolidation → cost/machine/peephole/pipeline as JSON → OPT10 autotuner. Remaining LICM enhancements (D-OPT6-LICM-PREHEADER-INSERTION + D-OPT6-LICM-TRAP-SAFE-HOIST unblocks after 10r); D-OPT-COPYPROP-BLOCK-CURSOR; D-OPT4-1-NON-LINEAR-MARKER-MERGE (WASM/SPIR-V trigger). | step 13.7 cycle 10h-10o ✅ | 🟢 **INLINING ✅ DONE** (OPT7 c1–c6, cycles 22–29 — shipped in `release.pipeline.json`, recursion-safe/cross-CU/intrinsic-aware) + **LICM trap-safe-hoist safe-default ✅** (cycle 11, runtime-pinned). REMAINDER → v0.0.2 **V2-13**: `scalar_eval` consolidation, machine/peephole/cost-as-JSON, OPT10 autotuner, `D-OPT6-LICM-PREHEADER-INSERTION`, `D-OPT-COPYPROP-BLOCK-CURSOR`, aggressive trap-safe hoist; `D-OPT4-1-NON-LINEAR-MARKER-MERGE` **trigger-gated** (WASM/SPIR-V). |
| 13.8 | [`11`](./11-ffi-plan%20-%20tbd.md) **FF11** | **Language-agnostic shipped-lib ingestion** — call shipped-library externs (libc/kernel32/libSystem stdlib) WITHOUT inline `extern` re-declaration. Shared platform-keyed neutral store (`src/dss-config/shippedLibs/<platform>/*.json`, described once across languages) + per-language `shippedLibDirs` ordered search-path (references, doesn't own) + neutral JSON `ImportSurface` format (one universal reader; `.h` = one dialect, not the format) + lazy resolution feeding the EXISTING `ingest()` matcher. Closes D-FFI-SHIPPED-LIB-DESCRIPTOR-AGNOSTIC + D-FFI-HEADER-VALIDATION-OPTIONAL. **Scheduled right after the optimizer arc** — off the optimizer's critical path; NOT dependency-blocked by it (reuses the proven `ImportSurface` / `ingest()` / `FfiMetadata` back half — a discovery front-door + neutral JSON reader, not a new pipeline). | step 13.7 (scheduled-after, not dependency-blocked) | ✅ **DONE (cycle 21)** — `#include <stdio.h>` calls shipped `puts` with NO inline extern, runs end-to-end (`shipped_include_puts` corpus); closed FF11 + `D-FFI-HEADER-VALIDATION-OPTIONAL`. FF11 cycle 21 shipped via C-subset-source headers (`shippedLibs/<platform>/*.h`); **v0.0.2 V2-2 (2026-06-06) then landed the language-NEUTRAL JSON descriptor** (`shippedLibs/<platform>/*.json` + universal reader + the single `parseTypeFromText` codec + semantic-phase symbol injection; the `.h` for shipped libs was RETIRED in favor of `stdio.json`) — so `D-FFI-SHIPPED-LIB-DESCRIPTOR-AGNOSTIC` is now ✅ **CLOSED** (see §0.1 V2-2). |
| **`★ STAGE 2 — CROSS-HOST (gated on CI runners or hardware; do NOT pursue without execution-verification)`** | | | | |
| 13X.1 | [14](./14-linker-plan%20-%20tbd.md) **D-LK10-ENTRY-ARM64** | 🟢 **IMPL DONE 2026-06-06 (V2-1) — push HELD for CI-green-under-QEMU.** The trampoline EMITTER stayed bucket-2 zero-change, but the "JSON-only" premise was DISPROVEN by the plan-lock: a runnable ARM64 binary through the real pipeline needs a genuine fixed32 ENCODER substrate (no immediate or memory-addressing path existed). Landed: `EncodingSlotKind` Imm16/Imm9/Imm12/MemBaseNoScale + the encoder arms (range-checked, fail-loud `A_ImmediateOperandOutOfRange`), MOVZ + SVC #0 (`syscall`) + LDUR/STUR (`load`/`store`) + ADD/SUB-imm + `frame_load`/`frame_store` pseudo-ops in `arm64.target.json`, the `bl`→`call` generic-verb rename, `processExit`/`entryCallingConvention` on `elf64-aarch64-linux-exec.format.json`, and the `qemu-<arch>` run-capability (`run_binary` launcher-prefix + `examples_runner` cross-arch gate). Verified locally: 206 ctest, exact-byte trampoline + MOVZ/LDUR/STUR/ADD-imm pins, `arm64_exit` corpus compiles to AArch64 ELF. The GOT/PLT macro-op (ADRP+LDR+BLR) is NOT needed for the syscall exit path → stays deferred. **Execution verification (qemu-aarch64 on CI) is the user-owned DSS.DevOps half — push held for the green flag.** | (D-FF6-RUNTIME-PRINT ✅) + ARM64 runner | ✅ **PUSHED + CI-VERIFIED 2026-06-08** (PR #18; the native `ubuntu-24.04-arm` leg runs the arm64 corpora GREEN — real arm64 hardware, NOT QEMU, per the user's DevOps call; the held-for-QEMU note is superseded). See §0.1 V2-1 row. |
| 13X.2 | [14](./14-linker-plan%20-%20tbd.md) **D-LK10-ENTRY-MACHO-EXIT** + **D-LK6-14** (Apple Silicon) | 🟢 **PROMOTED TO ACTIVE 2026-06-08 (user: "add Apple Silicon to the dss cycles") — the runner trigger has FIRED.** The CI `macos-clang-release` leg runs on `macos-latest` = **macOS 14 / Apple Silicon (arm64)** (the green leg = the compiler already builds + runs NATIVELY on arm64-darwin), so a `macho64-arm64-darwin-exec` corpus (runOn `["darwin"]`) RUNS natively there — macOS-ARM64 is now CI-execution-verifiable. Scope (part of the ARM64 arc, dual-platform with Linux-ELF): (1) NEW `macho64-arm64-darwin-exec.format.json` (CPU_TYPE_ARM64=0x0100000C + `useChainedFixups:true` + 0x4000 page + sections + `processExit` + `entryCallingConvention:"aapcs64"`); (2) CPU-type dispatch in `macho.cpp::encodeExecDynamic` (CPU_TYPE_ARM64 → LC_DYLD_CHAINED_FIXUPS [the `macho_chained_fixups` encoder is READY] vs x86_64 → LC_DYLD_INFO_ONLY) [D-LK6-14 code-side]; (3) the **macOS exit mechanism** [D-LK10-ENTRY-MACHO-EXIT] — RECOMMEND **libSystem `_exit` by-name import** (consistent with the dynamic-linking/chained-fixups path the binary is already on; Apple-blessed; mirrors PE+ExitProcess; avoids Apple's "unstable BSD syscall numbers" warning) over the raw BSD `exit` syscall; (4) the SHARED ARM64 call-with-result isel — `D-LK10-ENTRY-ARM64-FFI-ISEL` ✅ CLOSED 2026-06-08 (the gap was the `lea`/GlobalAddr lowering, supplied by D-AS4-3; proven on Linux-ARM64 via `extern_call_elf`), so this is NO LONGER a macOS blocker — the isel lowers identically for macho64-arm64 (the cc differs [apple_arm64] but the GlobalAddr→lea path is arch-shared); (5) de-risk-first: a minimal `macho64-arm64-darwin-exec` exit-42 corpus that RUNS on the macos CI leg (self-confirms the runner is arm64 — RUN not SKIP) before the FFI corpus. macos-arm64 shipped descriptors already exist. | (D-FF6-RUNTIME-PRINT ✅) + macos-latest=arm64 (FIRED) | ✅ **CLOSED 2026-06-09 — RUNTIME-VERIFIED on Apple Silicon** (D-LK10-ENTRY-MACHO-EXIT; CI run 27182845048 macos-latest GREEN, corpus #194 `macho_arm64_exit` → exit 42). AS-BUILT note (diverges from this row's pre-impl scope): shipped the **legacy LC_DYLD_INFO_ONLY** bind (not `useChainedFixups`), `direct-plt` extern dispatch, libSystem `_exit` import, **16 KiB `segmentPageSize`** + `LC_BUILD_VERSION` (the EBADMACHO fix, e2cabd0). See plan-14 D-LK10-ENTRY-MACHO-EXIT + §0 cell. |
| 13X.3 | [11](./11-ffi-plan%20-%20tbd.md) FF1-PE + FF1-MachO | PE/COFF export-table reader (`.edata` + `.idata`) + Mach-O `LC_SYMTAB` + `LC_DYSYMTAB` + `LC_DYLD_INFO_ONLY` bind-opcode reader. **Not a blocker for by-name imports** (the runtime loader resolves by name; hello_puts proves this works without binary readers). Value is signature validation against shipped DLLs/dylibs — nice-to-have. | trigger: first PE/macOS corpus that needs binary-reader-validated externs | ⏳ low priority |
| 13X.4 | [14](./14-linker-plan%20-%20tbd.md) **D-LK10-CRT-INIT-INVOKE** | msvcrt CRT init shim (`_initterm` + `__getmainargs` + `_iob_func`) from trampoline prologue before `call main`. **NOT the blocker** for `puts` (msvcrt's DllMain self-inits stdio; proven 2026-06-02). Required for: `printf` with `%f` (locale-dependent), `fopen` (locale-aware paths), `atexit`, static-init / C++ ctors. Anchored as future enabler. | trigger: first c-subset example crashing on uninitialized CRT state (printf-with-float / fopen / locale) | ⏳ low priority |
| 14 | [15](./15-debug-info-plan%20-%20tbd.md) DB1–DB12 | DWARF 5 (ELF + Mach-O) + PDB (PE) + CFI / SEH / compact-unwind | steps 10, 13 | ⏳ |
| 15 | [16](./16-codesign-publish-plan%20-%20tbd.md) CS1–CS9 | Apple codesign + notarization + Authenticode + RFC 3161 + APK v3 (in-tree crypto via vendored BearSSL) | steps 13, 14 | ⏳ |
| 16 | §8 `program-api` | `dss-code-prime build my-project.dss-project.json` CLI + project loader + positioned source-context diag renderer (DSS's own; NO clang/LLVM) | steps 13, **P1** | 🟡 CLI/project surface ✅ (AP2); positioned rendering Part A ✅ 2026-06-09; B+C remain |
| **v1.x — unblock when their deps land** | | | | |
| 17 | [18](./18-wasm-plan%20-%20tbd.md) WA1–WA10 | WASM backend via structured-CF markers (no Relooper) | steps 8, 10, 13 | ⏳ |
| 18 | [17](./17-shader-gpu-plan%20-%20tbd.md) SG1–SG10 | SPIR-V codegen + shader HIR + GPU bindings + same-source CPU+GPU dual-lowering | steps 8, 10 | ⏳ |
| 19 | [10](./10-source-translation-plan%20-%20tbd.md) ST1–ST6 | Source-to-source translation via HIR pivot | step 8 | ⏳ |
| 20 | [08](./08-compilation-unit-plan%20-%20tbd.md) CU5 | Multi-language CUs (one binary, mixed source languages, HIR convergence) | step 4 + step 8 | ⏳ v1.1 |
| 21 | [08](./08-compilation-unit-plan%20-%20tbd.md) CU6 + [14](./14-linker-plan%20-%20tbd.md) **LK11** | Cross-CU references — lifts the single-CU-per-binary assumption; shared libraries / DLLs / incremental compilation (paired CU6 ↔ LK11; substrate hooks already in CU1+CU3). **Now also the OPT7 critical path** (= plan 22 pre-OPT7 P3+P4, decision 2026-06-04): the cross-CU Weak-inline negative pin + `D-OPT7-1` cross-CU inlining both require it. Itself preceded by front-end linkage specifiers + HIR→MIR linkage mapping (plan 22 §0.1 pre-OPT7 P1/P2). **Symbol-namespace foundation D-LK4-3 ✅ landed 2026-06-04** (compound `(cuId, SymbolId)` linker index + `K_CrossCuMergeUnsupported` N>1 fail-loud; LK11 now = the cross-CU symbol-table MERGE + weak-vs-strong on top of it). **LK11a ✅ 2026-06-04: the cross-CU DEFINITION merge + weak-vs-strong landed (`resolvedGlobalDefs` — the OPT7 Weak-pin substrate); reference resolution (extern → sibling def) ALSO landed; **merged-image bytes ALSO landed (cycle 18 — pre-merge into one combined module + existing-walker reuse, byte-pin regression-proven)**; **cross-CU CALL + multi-CU driver ALSO landed (cycle 19 — `Program::compileUnits` + CLI gcc-semantics dispatch + the rodata thunk slot + PE `.reloc` ASLR base relocations; the FIRST runnable cross-CU-call binary `cross_cu_call` exit 42)**; remaining = ELF/Mach-O exec thunk emission (D-LK11-ELF-MACHO-CROSSCU-THUNK-EMISSION, format-triggered) + extern-import dedup. **The P4 LK11 substrate the OPT7 Weak-pin needs (`resolvedGlobalDefs`) is COMPLETE; P3 CU6 is the last pre-OPT7 link.** | step 4 + step 13 + trigger (artifact profile needing multiple CUs **OR** the pre-OPT7 chain) | 🟢 **LK11a+b ✅ (cycles 15–19); CU6 remaining** |
| 22 | tokenizer engine ✅ + [11](./11-ffi-plan%20-%20tbd.md) FF11 | **Context-sensitive per-mode lexing ✅ LANDED (cycle 20, 2026-06-05)** (`D-TOKENIZER-CONTEXT-SENSITIVE-LEXING`) — `GrammarSchema::lookupLexemeInMode` wired into the scanner's 3 main-scan branches with global fallback + per-mode `tokens` override parsing (the meaning-parser extracted to a shared `parseMeaningArray` so global + per-mode never drift). Agnostic engine capability (any grammar declares a context-sensitive mode); ZERO cross-language regression (default-mode path byte-identical; 194/194); synthetic-grammar RED-on-disable test (`<` lexes as a header-opener in-mode, `LtOp` out); ships UNCONSUMED (no shipped grammar declares an override — FF11 is the first consumer). **THEN FF11 (NOW NEXT-UP)** — C-faithful shipped headers: `#include <h>` angle form on a config `shippedLibDirs` system path → merged via existing include-following → existing FFI synthesis; inline-extern KEPT. User §B 2026-06-05 chose Option A (tokenizer investment) → 2-cycle path; the tokenizer half is now done. Spec in plan-11 FF11 entry. | cross-CU ✅ (cycles 15–19) | ✅ **tokenizer (cycle 20) + FF11 (cycle 21) DONE** — `#include <stdio.h>` runs end-to-end (`shipped_include_puts`, exit 42, no inline extern); inline-extern + quote-include KEPT. Next = LK11b residue → OPT7 HARD STOP. |
| 23 | [22](./22-optimizer-plan%20-%20tbd.md) **OPT7** | 🔓→✅ **OPENED 2026-06-05 (user §B, SUPERVISED — `G-406` HARD STOP); the user-directed c1–c6 inlining arc is COMPLETE 2026-06-05 (cycles 22–29 — see the outcome cell + plan 22 §3.1)** — the pre-OPT7 chain P1–P4 is COMPLETE (linkage specifiers + HIR→MIR mapping + multi-CU driver + cross-CU weak-vs-strong resolution + the corpus harness all landed). First inter-procedural pass = **inlining**. First cycle (user direction): CORRECTNESS-FIRST — the §2.9 inline-legality framework + the end-to-end `D-OPT7-WEAK-INLINE-NEGATIVE-PIN` (a 2-CU weak/strong corpus; OPT7 must REFUSE the weak inline; linker resolves strong-over-weak; runtime proves it) on a minimal call-graph + inline, before the full machinery. **Each OPT7 cycle user-supervised** (present + confirm). Plan-22 OPT7 row + §3.1. | pre-OPT7 P1–P4 ✅ | 🟩 **OPT7 inlining c1–c6 arc COMPLETE (cycles 22–29) — see narrative below + plan 22 §3.1; cycle 2 = general multi-block-LEAF inline splice: call-site block split + RPO CFG clone + return-merge Phi + StructCfMarker re-derived to Linear + caller-Phi redirect (`examples/c-subset/multiblock_inline` `pick(1)+pick(0)`=16; RED-on-disable via verify-after-every-pass — a mis-merged Phi → MirVerifier rejects). D-OPT7-MULTIBLOCK-SPLICE ✅ + robustness gate (a non-returning-path leaf callee is refused, else the splice leaves a predecessor-less continuation → build break) + D-OPT7-MULTIBLOCK-SPLICE-PHI anchored (callee-Phi leaf conservatively refused; frontend-reachable via ternary/`&&`/`||`). CYCLE 1 (2026-06-05): minimal single-block inliner + §2.9 gate + Weak-pin (`weak_inline_crosscu` exit 42); D-OPT7-WEAK-INLINE-NEGATIVE-PIN ✅. **USER-DIRECTED OPT7 SEQUENCE (§B 2026-06-05 "do all in a multi /dss-cycle — cross-CU first, then safety, then the rest"): CYCLE 3 = cross-CU inlining (`D-OPT7-1`, Design A whole-program MIR merge per the §B 2026-06-05 "keep linker merge + share resolver" decision; itself a multi-cycle arc — **dss-cycle 24 ✅ DONE 2026-06-05 (prereq): the prereq substrate [per-CU pipeline split into `buildCuMir`/`lowerCuMirToAssembly` + `CuMirModule` retaining the SemanticModel + a two-loop build-all-then-lower-all driver + the extracted shared `resolveCrossCuDefs` weak-vs-strong resolver; behavior-preserving, 199/199, the cycle-17 analog]** → **dss-cycle 25 ✅ DONE 2026-06-05 (the MIR merge): the whole-program MIR merge landed [`D-OPT7-CROSSCU-MIR-MERGE` ✅: `reinternType` topological type re-intern + `mergeCuMirs` interner unification + arena-tag/SymbolId remap + const/symbol/global/extern merge + drop shadowed-weak + rewire cross-CU calls to DIRECT; cross-CU calls now intra-module direct, behavior-preserving — `cross_cu_call` exit 42 via direct call, single-CU byte-identical; 201/201]** → **dss-cycle 26 ✅ DONE — cross-CU function INLINING (`D-OPT7-1` ✅): the optimizer runs on the merged module → cross-CU calls inlined. The cross-CU inlining loop is CLOSED (c24 prereq ✅ + c25 merge ✅ + c26 inline ✅).**) → **dss-cycle 27 ✅ DONE — general NON-LEAF function inlining + SCC recursion safety** (lifted the `Call`-leaf refusal; new `call_graph_scc` Tarjan substrate; the gate refuses inlining within a recursive SCC — generalizes self→mutual recursion). The planned "c4 recursion/SCC safety" was found premature (leaf-only ⇒ recursion already refused) so the user chose general non-leaf inlining now (the recursion policy got a real consumer). → **dss-cycle 28 ✅ DONE — inline COST MODEL + ship inlining in release** (`OptPipeline.inlineThreshold` config-driven [default 50] + the gate refuses a callee whose inst-count exceeds it; `Inlining` now SHIPS in `release.pipeline.json` — a real -O optimization, no longer opt-in-only). → **dss-cycle 29 ✅ DONE — IntrinsicCall inlining relaxation** (the gate now admits an IntrinsicCall-bearing callee [single-block + multi-block]; frame-sensitive-intrinsic gating is trigger-gated `D-OPT7-INLINE-FRAME-SENSITIVE-INTRINSIC` behind a registry-empty fail-loud tripwire — no shipped frontend emits any intrinsic today). **The user-directed OPT7 sequence c3–c6 is now COMPLETE.** The OPT7 hard-stop arc is done; remaining OPT7-gate relaxations (the sophisticated cost model, depth-bounded recursion, escape tightening, callee-Phi) stay deferred (normal / trigger-gated). **Next-up = the next eligible §0.1 stepper item or an eligible deferred-anchor promotion** (the next cycle's Step 1 picks it — not pre-decided here).** Each OPT7 cycle remained supervised (scope pre-approved; in-cycle §B gates still live for genuine forks). (D-OPT7-INLINE-LEGALITY-GATE stays OPEN — only the recursion + regular-`Call`-non-leaf + the SIZE/cost-bound + the IntrinsicCall arms closed; depth-bounded recursive inlining + the sophisticated cost model + escape relaxation remain.)** |
| **Sidewise — unblocked NOW, can run in parallel** | | | | |
| P1 | [06](./06-artifact-profile-plan%20-%20tbd.md) AP1–AP4 | `artifactProfile` mechanism (registered-set vocab) — **gates steps 13 + 16 acceptance** | — | ⏳ unblocked; parallel-eligible with steps 1–12 |
| P2 | §8 `docker-setup` (residual) | Dockerfile + cross-compile toolchains | — | ⏳ unblocked; cross-cutting |
| P3 | §8 `testing` ARM64 + fuzzing | ARM64 CI legs (G-704..G-706) + nightly fuzzing (G-710) + extended sanitizer coverage (G-709) | — | 🟦 partial; rolls continuously |
| **Reserved — no work until trigger** | | | | |
| R1 | [19](./19-hir-hw-reserved-plan%20-%20tbd.md) | Hardware HIR sibling (VHDL / Verilog / SystemVerilog) | step 8 + trigger | 🔒 |
| R2 | [20](./20-custom-language-reserved-plan%20-%20tbd.md) | User's eventual custom language (vocab + stdlib + runtime decisions) | all v1 + step 17 + step 18 + trigger | 🔒 |
| R3 | [21](./21-runtime-reserved-plan%20-%20tbd.md) | Language runtime (GC / exceptions / coroutines / threading) | step 8 + step 10 + trigger | 🔒 |

> **v1 paths (two distinct arcs from step 13 — pursue Stage 2 first, signed binary second):**
>
> - **Stage 2 — c-subset corpus expansion (strict sequence, 2026-06-02+):** 13 → **13.1** (ML7-2.2 stack args) → **13.2** (c-subset `void*`) → **13.3** (hello_writefile — CRT-free hermetic print) → **13.4** (variadic ABI + c-subset `...` grammar) → **13.5** (non-trivial host-runnable corpus expansion) → **13.6** (OPT1 const-fold + DCE + copy-prop) → **13.7** (OPT2–OPT10 multi-tier optimizer) → **13.8** (FF11 — language-agnostic shipped-lib ingestion: shared platform-keyed neutral JSON store + per-language `shippedLibDirs` search-path + lazy resolution onto the existing `ingest()` matcher). Steps 13.1–13.5 build the corpus the optimizer differentially verifies against; sequencing optimizer after corpus is deliberate ("runs everywhere" before "runs fast" — but "runs USEFULLY" before either). 13.8 is scheduled after the optimizer (the user's call) but is NOT dependency-blocked by it — it reuses the proven FFI back half, so it could move earlier if priorities shift.
> - **First signed binary:** 13 → 14 (debug-info) → 15 (codesign) → 16 (build CLI). Independent of Stage 2 file-wise (no shared editing surface). Can run in parallel structurally but the audit-fold discipline (7-agent + cross-plan sweep per cycle) favors serial focus — pick one cycle's worth of work per landing.
> - **Cross-host (steps 13X.*) — BLOCKED on CI runners or hardware.** ARM64 + Mach-O codegen would land byte-correct but unverified-at-runtime, regressing the execution-verification methodology that has caught every silent bug on this branch (the SEGV; the regalloc 42+0=84; the wrong-call-opcode IAT trap). Do NOT pursue these without a host that can spawn the produced binary. FF1-PE/MachO binary readers similarly are nice-to-have (signature validation), not blocker (by-name imports work without them).
>
> **P1 (artifact-profile)** must land before step 13 and step 16 reach acceptance — schedule it anywhere in parallel from step 1 onward.

## 0.2. Deferred & Known-Open Items (registry)

> **Single authoritative list of everything consciously left open as of the pre-HIR gap-closure (2026-05-26).** Each item names WHY it's deferred, the plan/phase that OWNS its eventual closure, and the TRIGGER. Nothing here is a silent gap — it's all tracked. `cheap-config` = closeable any time as additive `.lang.json` config with no engine work (do when a corpus/language needs it); `blocked` = genuinely needs a downstream layer; `feature` = language-surface expansion (v1.x). When an item closes, strike it through with the closing commit.
>
> This registry is the index; the detail lives in the owning plan (linked). Do not duplicate detail here — it drifts.
>
> **Closure pass 2026-05-26:** a "what can be done now" triage closed the items genuinely reachable with the current (pre-HIR) implementation — **D2** (full), **D3** (type-resolution; only the `VARCHAR(N)` *parameter* remains), **D4-AP1** (the `artifactProfiles` schema field + loader), and **D8** (the never-referenced unused-variable warning — the registry had mis-classified this as optimizer-blocked, but SE7's `usesBySymbol` reverse index makes it a pure semantic-layer check). Closed/narrowed rows are struck through below; the surviving rows carry a refined reason + an explicit *when*.

| # | Deferred item | Why deferred (not a silent gap) | Class | Owner / closure | Trigger |
|---|---|---|---|---|---|
| ~~D1~~ | **Pointer / array *type-level* modelling** + `&` addr-of (c-subset) | ✅ **fully closed.** Array half in HR9 (2026-05-27): `int a[10]`→`Array<I32,10>` via `DeclarationRule.arraySuffix` + semantic const-length eval. Pointer half in gap-closure G5 (2026-05-28): `int *p`/`int **p`→`Ptr` via a config-driven `semantics.pointerToken` declarator-DEPTH count in `resolveTypeNode`; `&` added as a prefix operator (AddressOf→Ptr, Deref→pointee result types were already in lowering). **Successor (pinned → D11):** FULL C declarators — function pointers `int (*f)()`, arrays-of-pointers, multi-declarator `int *a, b` — are NOT covered (single-declarator pointer depth only). | feature/config | — (closed; full declarators → D11) | — |
| ~~D2~~ | ~~**Compound-assignment const-correctness** (c-subset)~~ | ✅ **closed 2026-05-26.** Added the 10 compound-assign operator tokens (`+=`/`-=`/`*=`/`/=`/`%=`/`&=`/`\|=`/`^=`/`<<=`/`>>=`) as gated `assignments` entries; `const int x=1; x+=2;` now flags `S_ConstViolation`. The engine `assignByRule` index became multi-entry (one rule → many gated entries) with a load-time guard that an ungated entry must be a rule's sole entry. Pure config + generic engine change. | ~~cheap-config~~ | — | — |
| D3 | ~~**tsql column-type coverage**~~ → **`VARCHAR(N)` parameter only** | ✅ **type-resolution closed 2026-05-26.** A `columnDecl` declaration + `createTableStmt`-as-scope mint columns as typed symbols; `INT`→`I32`, `BIT`→`Bool` via `builtinTypes`, and `VARCHAR`→the `TSQL::Varchar` extension via a NEW generic `builtinTypes.extension` facet (any language maps a type name → a registered `typeExtensions` entry). **Remaining:** only the `VARCHAR(N)` *length parameter* — the tsql grammar's `typeRef` is `alt[IntKw,VarcharKw,BitKw]` and never parses `VARCHAR(50)`, so the extension is built un-parameterized. That is a grammar-expressiveness gap, not a type-system one. (`DECIMAL`/`MONEY`/`DATE`/`NVARCHAR` are not yet in the grammar at all → see D6 language-surface.) | feature (grammar) | a tsql grammar round that parses parameterized type refs, then thread the parsed `N` into `interner().extension(...args)` | tsql needs precise `VARCHAR(N)` typing, or HIR lowering of SQL types |
| D4 | **Driver / project-config / `artifactProfile` (AP2–AP4) / CLI diagnostic rendering** | ✅ **AP1 closed 2026-05-26:** the optional top-level `artifactProfiles: [...]` schema field + loader validation against the registered profile vocabulary (`cli`/`gui`/`lib`/`staticlib`/`script`/`sproc`/`transpile`/`shader`/`hdl`) + a `GrammarSchema::artifactProfiles()` accessor; the three shipped configs declare their profiles. **AP2 ✅ + AP3 ✅ (2026-06-09):** the `.dss-project.json` loader + the driver that enforces `artifactProfile` (language gate `D_ArtifactProfileNotSupported`=0xD010 + format gate `D_ArtifactProfileFormatMismatch`=0xD011, the format declares `artifactProfiles[]`) now CONSUME the declared profiles. **AP4 ✅ (2026-06-09)** (per-language onboarding matrix) + the **CLI** `--project`/build surface ✅ (AP2) + the positioned `DiagnosticReporter::format()` renderer wired into the CLI AND strict-asserted at every tier (V2-4 A+B+C, `D-DIAG-CLI-POSITION-RENDER-AND-ASSERT` ✅ **CLOSED 2026-06-10**). Codegen-READING the profile (entry-symbol/subsystem) stays deferred trigger-gated (`D-AP2-COMPILATION-CONTEXT`). | blocked (own phase) | phase #12 `program-api` + [06](./06-artifact-profile-plan%20-%20tbd.md) AP4; [07](./07-production-readiness-plan%20-%20tbd.md) G-601..G-609 | step 16 (`program-api`) |
| D5 | **c-subset structs / unions / enums** | 🟡 **PULLED FORWARD 2026-05-28** (before ML2) to make ML2's HIR→MIR lowering honor "no real-blocker deferrals" on `MemberAccess`/`ConstructAggregate`/`TypeDecl`. **Kitchen-sink scope**: core structs + typedef'd/anon + designated initializers + unions + enums + first-class MIR aggregates (ExtractValue/InsertValue). **6-PR sub-arc**: **D5.1 core structs ✅** → D5.2 typedef'd/anon → **D5.3 designated init ✅ (cycle 1a + 1b + SP3)** → **D5.4 unions ✅** → **D5.5 enums ✅** → **D5.6 MIR aggregate opcodes ✅** → then ML2. **D5.6 landed 2026-05-28** (commit `6007fde`+ review fix-up `65cbd53`): `MirOpcode::ExtractValue` + `InsertValue` first-class aggregate read/write opcodes with Gep-shaped operand layout `[agg, (value,) idx0, idx1, ...]` (indices are Const-i32 operands). `MirBuilder::addExtractValue` / `addInsertValue` helpers + shared `appendIndexPathOperands_` private helper. 7-agent review fix-up: docstring contradictions corrected (was `scalars[0..N)`, now `operands[1..N)`); InsertValue result-type-must-match-aggregate-type fatal added; missing tests landed (path-value-correctness, InsertValue empty-path death, InsertValue nested, result-type mismatch death). 9 D5.6 tests + 89/89 ctest. **D5 sub-arc CLOSED** — ML2 can now wire `MemberAccess` → ExtractValue and `ConstructAggregate` → chained InsertValue. **D5.6-FU1 anchored** (scalars-encoded path) — substrate-tier rework opportunity flagged by simplifier; type-design reviewer recommends staying with Gep convention for use-list/RAUW uniformity. Decision deferred to ML2/ML3 verifier cycle when first concrete consumer measures the cost. **D5.5 landed 2026-05-28** (commit `f0ddfa8`): `TypeKind::Enum` + `TypeInterner::enumType(name, underlying=I32)`; `CompositeKind::Enum` as third Pass 1.5 dispatch; Pass 1.5 enum branch sets each enumerator's type to the enum TypeId AND lifts enumerator bindings to the enclosing scope so C-classic `enum E { A } ... A` works; same-name collision in the enclosing scope emits S_RedeclaredSymbol; `requireFieldTypes` gate conditional on composite kind; c-subset grammar adds `enumDecl`/`enumerator`/`enumTypeRef` (mirrors D5.3's braceInitList trailing-comma pattern to avoid FIRST-token alt-ambiguity); HIR text format adds `enum "Name"` emit/parse (round-trip currently lossy for non-I32 underlying — v1 schemas don't expose underlying selection). 4 D5.5 tests; 36/36 D5 lowering tests total (20 D5.3 + 12 D5.4 + 4 D5.5); 89/89 ctest. **§D5-addressable-followups CLOSURE PASS 2026-05-28** (commit `fe87994`): the bundleable items landed in one focused cycle to avoid passing through the same code twice ahead of ML2 cycle 6. **CLOSED**: D5.4-FU1 (`HirVerifier::checkConstructAggregate` rule — Struct=N, Union=1, Array=length, with per-child type-match; subsumes FU4 positional type-mismatch); D5.4-FU3 simplifier helper consolidation (`peelToDesignatorLeaf` + `firstIdentifierToken` + unified `synthZeroOrError` Struct/Union arm); D5.5-FU2 `liftToEnclosingScope` config opt-out (c-subset opts in explicitly; future Rust-style enum schemas can omit); D5.5-FU3 `synthZeroOrError` Enum arm (zero literal tagged AS the enum TypeId, preserving nominal identity); D5.5-FU5 HIR text underlying-type round-trip (`enum "E" : <kindOrdinal>` when underlying ≠ I32); D5.5-FU4 test coverage (added `D5_5_EnumHirTextRoundTrip` pinning the full declare → lower → emit → re-parse → verify path). **STILL QUEUED** (tier 2 — own substrate cycles): D5.4-FU2 explicit variant-tag attribute (deferred to ML2 union-lowering cycle when codegen needs the explicit tag — implicit-tag-by-child-type is now type-checked by the new verifier rule); D5.6-FU1 scalars-encoded path (measurement-gated, stay with Gep); ~~Plan 12.5 §0.2 D6 CST-side const-eval~~ ✅ **closed 2026-05-28** (commits `723bea4`+`dcbfe2b`: shared CST engine + 3-site wiring + 14 tests + 7-agent review fix-up — Ref support for `isConst`-bound symbols via `CstSymbolInitResolver` closure; D5.3/D5.5 non-literal cases now fold); Plan 12.5 §0.2 D1b F128; nominal-collision intern-key disambiguator (struct/union/enum). **§D5-addressable-followups queued 2026-05-28** (NOT deferrals — addressable inline; cycle order is non-blocking; surfaced by the D5.3/D5.4 7-agent reviews + the latest audit): (a) **D5.4-FU1 `ConstructAggregate` arity-by-TypeKind verifier rule** — Struct=fieldCount / Union=1 / Array=length. Promotes the helper-only invariant in `lowerBraceInit`/`lowerUnionBraceInit`/`synthZeroOrError` to a checked HirVerifier rule. ~30 LOC + a negative test. Cost-low; lands any time the verifier surface is open. (b) **D5.4-FU2 Explicit variant-tag attribute on union `ConstructAggregate`** — `HirAttribute<std::uint32_t> activeVariant` populated by `lowerUnionBraceInit` from the resolved variant index. Closes the implicit-tag gap MIR/codegen would otherwise inherit (member-access on a union currently obliges MIR to walk back to the producing aggregate to recover which variant is active). Additive side-table; no opcode change. (c) **D5.4-FU3 simplifier-helper consolidation** — extract `peelToDesignatorLeaf(NodeId) → {core,rule}` + `firstIdentifierToken(NodeId)`; fold `synthZeroOrError` Struct/Union arms into one composite arm parameterized by `(unionKind ? 1 : N)`. Pure refactor; replaces 3 hand-rolled copies. (d) **D5.4-FU4 positional-type-mismatch diagnostic** — bug-reviewer flagged silent path: positional union init with a value whose type doesn't match the first variant currently lowers without a diagnostic (the verifier doesn't type-check ConstructAggregate children). The arity verifier rule from (a) is the natural place to add per-child type-match for both struct and union. (e) **Plan 12.5 §0.2 D6 CST-side const-eval** — anchored already in plan 12.5; the only D5.3 substrate-blocker still locked-in via `D5_3_NonLiteralIndexDesignatorEmitsDiag`. Fresh-audit candidate: the current engine "consumes HIR" but might grow a CST sibling with modest work; revisit before ML2 resumes. (g) **D5.5-FU1 explicit enumerator value computation** — `enum E { A = 5, B }` parses cleanly but the explicit value is NOT yet computed; Pass 1.5 only sets the type. C99 semantics: explicit value goes through const-eval; subsequent enumerators auto-increment (`A=5, B=6`). Needs an EnumValue side-table on the SemanticModel (TypeId+fieldIndex → int64) populated by Pass 1.5 via const-eval on the explicit-value expression. (h) **D5.5-FU2 `liftToEnclosingScope` config opt-out** — currently the "lift enumerator bindings to enclosing scope" behavior is hardcoded to `CompositeKind::Enum`. A future enum-bearing language wanting Rust-style `E::A`-only would have no opt-out. Add `FieldChildrenDescriptor::liftToEnclosingScope: bool` (default false, c-subset opts in explicitly). (i) **D5.5-FU3 `synthZeroOrError` Enum arm** — currently falls through to the scalar default, which produces an I32 zero literal tagged as the underlying I32 type, not the enum TypeId. Should emit a zero literal tagged with the enum's TypeId. (j) **D5.5-FU4 test-coverage gaps from review** — assert enumerator's actual type (not just "no errors"); negative test for the redeclaration-on-lift diagnostic; HIR text round-trip for an enum-typed program. (k) **D5.5-FU5 HIR text underlying-type round-trip** — `enum "Name"` emits without the underlying; parser re-interns with default I32. Round-trip is currently lossy for non-I32 underlying (no v1 consumer needs this; revisit when a schema declares `enum E : u8`). (f) **Plan 12.5 §0.2 D1b F128 Cast target** — `HirLiteralValue::value` variant lacks a `long double` / `__float128` arm. Listed in plan 12.5 §0.2 as deferred-by-design; "no v1 consumer" may not satisfy the no-deferrals rule. Fresh-audit candidate; would need a substrate variant extension + soft-float helper paired (similar to D1's `narrowToHalf`). Items (a)–(d) intentionally sit AFTER D5.5/D5.6 in the cycle order because they touch the verifier/HIR substrate which D5.5 enums + D5.6 aggregate opcodes will also touch — batching them avoids two passes over the same code. **D5.4 landed 2026-05-28** (commit `968f301` on `feature/mir-lir`): c-subset adds `UnionKeyword`/`unionDecl`/`unionField`/`unionTypeRef`; new `FieldChildrenDescriptor::compositeKind: CompositeKind::{Struct,Union}` additive config field; Pass 1.5 dispatches struct vs union interning by the flag; new `lowerUnionBraceInit` implements C99 §6.7.8 union semantics (positional → first variant, designator → named variant, empty `{}` → first-variant zero-fill, multi-element / unknown-variant / index-designator all emit specific diagnostics); `synthZeroOrError`'s Union arm now produces a 1-child aggregate (was N-child like struct — wrong). 7 D5.4 tests; 27/27 D5 lowering tests total (20 D5.3 + 7 D5.4); 89/89 ctest. **D5.4 landed 2026-05-28** (commit `968f301` on `feature/mir-lir`): c-subset adds `UnionKeyword`/`unionDecl`/`unionField`/`unionTypeRef`; new `FieldChildrenDescriptor::compositeKind: CompositeKind::{Struct,Union}` additive config field; Pass 1.5 dispatches struct vs union interning by the flag; new `lowerUnionBraceInit` implements C99 §6.7.8 union semantics (positional → first variant, designator → named variant, empty `{}` → first-variant zero-fill, multi-element / unknown-variant / index-designator all emit specific diagnostics); `synthZeroOrError`'s Union arm now produces a 1-child aggregate (was N-child like struct — wrong). 7 D5.4 tests; 27/27 D5 lowering tests total (20 D5.3 + 7 D5.4); 89/89 ctest. **D5.1 landed in 5 additive checkpoints** (`feature/mir-lir`): `251eca0` (config vocab + `SymbolRecord::fieldIndex`/`structScope`), `9079916` (Pass 1/1.5/2 engine consumption + `S_NotAPointer`/`S_NotAComposite` codes + cross-field loader validation), `09916d5` (`followerRule` operator-table primitive + `MemberAccessRule.operatorToken` discriminator), Cycle 3 (struct schema wired end-to-end through parse+semantic: `struct Foo { ... };` definitions + member access in PARAM and LOCAL VAR positions; struct types compose via `structType(name, fields)`; field uses resolve via Pass 2), and Cycle 4 (HIR MemberAccess lowering: `s.x` → `MemberAccess(Ref, idx)`, `p->x` → `MemberAccess(Deref(p), idx)`; new `HirVerifier::checkMemberAccess` rule for fieldIndex-bounds + base-must-be-composite; defensive lowering check that resolved symbol is actually a field; dot-form + arrow-form + verifier-negative tests). **Scope cut documented**: top-level `struct Foo x;` globals + `struct Foo make()` struct-returning functions deferred to D5.2 — converting `topLevelDecl`/`externDecl` to `typeRefAllowingStruct` would re-introduce the StructKeyword first-token collision with `structDecl` in the `topLevel` alt; D5.2 lands the shared-prefix factoring + typedef'd structs (`typedef struct Foo Foo;`) that lifts the limitation. **D5.2 cycle 1 ✅** added `Identifier` to `typeBase` (typedef-alias at top level + bare-struct-tag usage in type position — known C divergence: single namespace + SE5 alias path means every struct tag is implicitly usable as a bare type name; pinned with 3 tests including the same-name redeclaration negative). **D5.2 cycle 2 attempted + reverted (diagnosis refined cycle 3)**: marking `statement` alt speculative + adding `Identifier` to `typeBaseAllowingStruct` caused widespread parser-test regressions. **Cycle 3 investigation corrected the root cause**: TreeBuilder::Checkpoint DOES already snapshot+rollback the DiagnosticReporter (line 1231 of tree_builder.cpp), so diagnostic rollback is NOT the issue. The real architectural blocker is the **speculative-alt branch-success criterion**: `trySpeculativeBranch` commits the first branch whose frame closes structurally, even if the branch needed internal recovery to get there. For `f(a, b);` inside a block: speculative `statement` tries `varDecl` first (Identifier in FIRST after the typeBase widening); `varDecl` parses `f` as `typeBase`, expects another Identifier, sees `(`, recovers internally (emits diag, advances, frame closes structurally) → probe sees frame closed → commits → rest of input mis-parsed. Fix requires either: (a) tightening the speculative-success criterion (`probe.emittedDiag() || probe.hasInnerErrors()` already exist but aren't all wired), (b) k>1 lookahead disambiguation, or (c) restructuring alts so FIRST sets are unambiguous (defeats the purpose). **(a) attempted cycle 4 + reverted**: adding the missing `probe.emittedDiag()` pre-commit check improved the situation (7→5 failures, no aborts) but left `f(a,b);`-style calls in blocks failing both branches with `P_BacktrackFailed` — deeper interaction with SchemaWalker desync-latch and/or per-probe diagsBefore_ baseline still needs untangling in a focused substrate-tier sub-PR. **Decision**: pivot to ML2 (D5.1 + D5.2 cycle 1 are sufficient for `MemberAccess` + `TypeDecl` end-to-end; `ConstructAggregate` stays a real-blocker defer until D5.3). **ML2 cycle 1 ✅ landed 2026-05-28**: `src/mir/lowering/` OBJECT lib + straight-line vertical slice through c-subset (Function + Block + Return + Literal + Ref + 16 BinaryOps + implicit-void-return synthesis + fail-loud unsupported handling). 88/88 ctest. See plan 12 ML2 row. **All remaining D5 sub-features** (block-scope alias, anonymous structs `struct { … } var;`, top-level `struct Foo x;` via shared-prefix factoring, designated initializers in init context) **share the same speculative-alt or multi-token-disambiguation engine need**. Recommended path: either focused substrate-tier sub-PR on speculative-alt diagnostic rollback (unlocks every remaining D5 feature), OR pivot to ML2 with D5.1 + D5.2 cycle 1 substrate (sufficient for `MemberAccess` + `TypeDecl` lowering; `ConstructAggregate` stays a documented fail-loud-defer in ML2 until D5.3). **87/87 ctest** throughout. | feature (multi-PR) | the 6-PR D5 sub-arc (D5.1 ✅; D5.2 in progress) | maximally honest c-subset for ML2 |
| D6 | **tsql JOINs / subqueries / CTEs / stored-procs / transactions** | Language-surface expansion; current tsql is single-table query/DML + CREATE TABLE. | feature (v1.x) | a future `tsql-subset` expansion round | same |
| ~~D7~~ | **Ternary `? :` (mixfix)** | ✅ **closed 2026-05-28 (gap-closure G6).** Added `OperatorArity::Ternary` (4th arity, fits the 2-bit key-pack) + `OperatorTable::Entry.ternaryMiddle` + an optional `expr.wrapperRules.ternary`; a `DefaultPrattWalker` mixfix branch (condition at prec+1, middle at minPrec 0, else at prec → right-assoc) + `lowerTernary` → the existing HIR `Ternary` node. Config-driven, no `?:` special-casing. | (closed) | — | — |
| D8 | ~~**Unused-variable**~~ → **write-only / dead-store only** | ✅ **never-referenced closed 2026-05-26** (registry had this mis-classified as optimizer-blocked). A config-driven `warnIfUnused` flag on `DeclarationRule` + a post-pass-2 sweep of SE7's `usesBySymbol` reverse index emits `S_UnusedVariable` (a WARNING) for an opted-in symbol with an empty use-set — no CFG needed. Per-decl opt-in (c-subset locals, not params/globals/columns); proven generic via Synth5. **Remaining:** write-only / dead-store ("assigned but never read") and dead-code — those DO need dataflow/liveness, so they stay with the optimizer. | blocked (optimizer) | [`22`](./22-optimizer-plan%20-%20tbd.md) optimizer (dead-code → OPT2 DCE; write-only/dead-store → OPT3 DSE); [07](./07-production-readiness-plan%20-%20tbd.md) G-210 | optimizer phase (CFG/liveness exists) |
| D9 | **Use-before-init / unreachable-after-return** | Needs a control-flow graph; the semantic layer is pre-CFG. HIR/MIR will have the CFG to do this strength of analysis. | blocked (HIR/MIR) | phase #9 HIR + [`22`](./22-optimizer-plan%20-%20tbd.md) optimizer (CFG/dominators in OPT1) | CFG exists |
| D10 | **CU6 cross-CU references / incremental rebuild** | v1 is single-CU-per-binary; cross-CU + incremental are paired CU6 ↔ LK11 substrate hooks already reserved. | blocked (v1.x) | [08](./08-compilation-unit-plan%20-%20tbd.md) CU6 + [14](./14-linker-plan%20-%20tbd.md) LK11 | artifact profile needing multiple CUs |
| D11 | ✅ **CLOSED 2026-06-11 (FC4 c1 — the declarator-grammar round this row reserved; see D14 + the plan-23 FC4 row: full C11 specifier/declarator split, fn-ptrs `int (*fp)(int)` + arrays-of-fn-ptrs + multi-declarators with per-declarator star binding, 261/261).** ~~**Full C declarators (c-subset)**~~ — function pointers `int (*f)()`, arrays-of-pointers `int *a[10]`, multi-declarator `int *a, b` | Successor to the now-closed D1. Gap-closure G5 (2026-05-28) gave c-subset single-declarator pointer DEPTH (`int *p`/`int **p` → `Ptr` via `semantics.pointerToken`) and HR9 gave array suffixes (`int a[10]`) — but the FULL C declarator grammar (parenthesized declarators binding a name through pointer+function+array layers, and comma-separated multi-declarators where `*` binds per-declarator) is a distinct grammar+semantic round. Not failing loud today: these forms simply don't parse (a parse error), they don't miscompile. Config + grammar work, no downstream phase needed. | feature (grammar) | a future c-subset declarator-grammar round; relates to D5 (structs) language-surface expansion | a corpus program needs function pointers / arrays-of-pointers / multi-declarators |
| **D12** (§B decision record) | **Shipped-lib FFI = Model 3 (neutral descriptor + per-format `library` map)** — chosen **2026-06-09** (the `#include puts("hello")` 3-OS string-FFI capstone). | **NOT a deferral — a §B architectural decision, recorded so it is not re-litigated.** The 15 per-platform `shippedLibs/<os-arch>/<h>.json` descriptors were COLLAPSED into 5 NEUTRAL `shippedLibs/<h>.json`, each carrying platform-neutral signatures + a per-format **`library` MAP** `{pe,elf,macho}`; the map is threaded target-AGNOSTICALLY through the front-end (descriptor→`ShippedExternSymbol`→`HirExternRecord`) and resolved to ONE library string per-target LATE at `compile_pipeline.cpp` (via `objectFormatKindName(format.kind())`; `ingest.cpp` stays single-string; missing key → `externLibraryByFormat` fallback; unknown key → fail loud); `shippedLibDirs` → the single neutral root `["shippedLibs"]`. **Chosen over** (a) Option-A — a one-line `libraryOverride` hack (REJECTED: latently wrong for the ABI-divergent signatures + would leave dead descriptor `library` fields) and (b) Model-2 — per-platform-dir target-aware resolution (REJECTED: keeps the per-platform duplication + needs an invasive per-target front-end). **DISSOLVES** `D-FFI-SHIPPED-LIB-PLATFORM-SELECT` (✅ — no directory to select by target anymore). | decision (resolved) | — (decided; the latent LP64-vs-LLP64 width residue for 6 symbols is tracked by `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH`, still OPEN) | — |
| **D13** (§B decision record) | **Hosted-format process-exit = libc `exit(3)`** — chosen **2026-06-09** (same capstone). | **NOT a deferral — a §B architectural decision.** ELF `processExit` was flipped from the raw `exit_group` syscall → a **by-name-import** of libc `exit` (`libc.so.6`), because the C-standard hosted exit flushes buffered stdio + runs `atexit` (a raw `exit_group` would drop a `puts("hello")` that hadn't flushed). Mach-O kept `_exit` (on Darwin `_exit` IS the flushing C `exit(3)`; the POSIX non-flushing `_exit(2)` is `__exit` — so Mach-O was flushing all along; the capstone only fixed the comment). PE kept `ExitProcess` (already flushes via msvcrt `DLL_PROCESS_DETACH`). **Chosen over** (a) surgical-iff-libc (only flip when the program already links libc) and (b) fflush-before-rawexit — REJECTED in favour of the C-standard, no-workaround default that matches crt0 + Windows; a pure-exit program now gains a libc dependency like every real C program. The raw-syscall arm is **config-retained** (`processExit.mechanism:"syscall"`) for a future freestanding/no-libc target (the `Syscall` emitter arm is still tested via synthetic formats). Trampoline + `linker.cpp` byte-UNCHANGED (config-only; agnostic). | decision (resolved) | plan 14 §LK10 (the per-platform exit table + Slice B); a future freestanding target re-activates the config-retained syscall arm | freestanding / no-libc target wants the raw-exit arm |
| **D14** (§B decision record) | **FC4 declarator-substrate scope + design** — chosen **2026-06-11** (plan-lock GO-WITH-FIXES, 8 MUST-FIX folded; user: "implement it in many cycles as you recommend… don't stop until full FC4 delivered"). | **NOT a deferral — the FC4 §B decision set, recorded so it is not re-litigated.** **(D1=a)** FC4 ships as **c0 (engine pre-steps: declaration-ordered speculative probing + the prefix-strip shared helper) → c1 (the C11 specifier/declarator substrate per the plan-23 FC4 row; `fp(3)` walls LOUD behind `D-CSUBSET-FNPTR-INDIRECT-CALL` high) → c2 (indirect-call codegen closing that anchor — LIR call-reg + x86 `FF /2` + arm64 `BLR` + the callee-reg regalloc hazard)**, chosen over one mega-cycle (review quality) and over calls-pinned-normal (leaves executable fn-ptrs unowned — no FC row owns indirect calls, an explorer-found plan gap). **(D2=a)** The abstract-declarator `( Identifier )` param ambiguity (C11 6.7.6.3p11) is resolved by a **config-declared guard-POLARITY mode** on `commitRequiresTypeName` (commit-only-if-sketch-Type; unknown → rollback + oracle candidate) — engine-generic, reusing probe+sketch+oracle; chosen over loud-rejecting `( Identifier`-led abstract forms (would partial-close FC4's own headline). **(D3)** Derived calls confirmed: `volatile` parses but fail-louds (`S_` + anchor → FC16; DSS ships LICM/CSE that would legally break volatile — silent-accept is a miscompile class); local `static` stays a parse error (pinned residue); unknown `[[…]]` IGNORED per C23 6.7.12.1 while unknown `__attribute__` keeps the decided Error (each standard's own semantics); struct-body heads stay OUT of `topLevelDecl` in c1 (pinned residue); `forStmt` init gets the same identifier-led speculative sub-alt. The canonical fn-ptr type is **`Ptr<FnSig>`** (the `FnPtr` lattice kind stays builder-less). | decision (resolved) | plan 23 FC4 row (the owning surface); `D-CSUBSET-FNPTR-INDIRECT-CALL` (c2); D11 above (FC4 c1 IS the "future c-subset declarator-grammar round" D11 reserved — flip D11 when c1 lands) | — |
| **D15** (§B decision record) | **Release-pipeline closure sequencing + the marker-rederivation substrate** — chosen **2026-06-12** (the user's §B answer during the optimizer-composition plan-lock, which had flagged the SimplifyCfg marker-repair identification as an architectural fork). | **NOT a deferral — the §B decision set, recorded so it is not re-litigated.** **(1) Sequencing**: cycle A = the associativity silent-miscompile fix FIRST (correctness outranks the fail-loud pipeline bugs; shipped 2026-06-12, `D-PARSE-INFIX-ASSOCIATIVITY-STRUCTURAL` ✅); **cycle B = a FOCUSED SUBSTRATE CYCLE** for structured-CF markers: build dom/post-dom (post-dom does not exist yet), a **`rederiveMarkers(cfg)`** that recomputes the loop family from BACK-EDGES and the if family from **SESE/post-dominator regions**, point the **MirVerifier** at it, and **DELETE the incremental marker-repair special cases** (SimplifyCfg's absorb-chain non-Linear-wins walk, the inliner's per-clone Linear-ization) as subsumed — **closes `D-OPT4-1-NON-LINEAR-MARKER-MERGE` (plan-22) and retires the branch-fold marker question in the same stroke**; **cycle C = the optimizer-composition closure (✅ SHIPPED 2026-06-13)** (`D-OPT2-REWRITE-MAP-COMPLETENESS` + `D-OPT-RELEASE-PIPELINE-CONTROL-FLOW-COMPOSITION`) per the 2026-06-12 plan-lock GO-WITH-FIXES: MultiBlockInliner topological-layout-by-construction (interleaved pre-creation), SimplifyCfg post-fold reachability with **EDGE-based** `acceptPhiIncoming` (accept iff pred reachable AND (pred's terminator unfolded OR its fold target == the phi's own block)), the new MIR-verifier LAYOUT rule (`I_LayoutUseBeforeDef` = 0xA010, reachable-only, dominance-gated, Phi-incomings exempt), `shippedPipeline` manifest-arm references resolving via `loadShippedPipeline` (zero drift), corpus arms incl. `fnptr_callback` `["Inlining"]` + release arms + the recursion×Inlining unit pin, and the D-OPT2 closure scoped honestly (the layout rule closes the CROSS-PASS class; the rebuilder abort stays as the in-pass backstop). **(2) Why B before C**: C's SimplifyCfg dead-arm pruning partially dissolves marked constructs; with `rederiveMarkers` as substrate the repair is a single post-rebuild re-derivation — no pairing substrate, no shape-walks, no family-Linear-ize compromise. | decision (resolved; **A shipped `8832948`, B shipped 2026-06-12, C shipped 2026-06-13 — D15 COMPLETE**) | `D-PARSE-INFIX-ASSOCIATIVITY-STRUCTURAL` (registry, A ✅); `D-OPT4-1-NON-LINEAR-MARKER-MERGE` (plan-22, **B ✅ 2026-06-12** — `deriveStructCfMarkers` + post-dom + verifier equality + repair-machinery deletions; bonus: degenerate-loop unblock + the branch-fold phi-edge fix); `D-OPT2-REWRITE-MAP-COMPLETENESS` + `D-OPT-RELEASE-PIPELINE-CONTROL-FLOW-COMPOSITION` (C's targets — sequencing notes current in both rows) | — |

> **Pre-HIR readiness:** none of the remaining items blocks opening **HR1** (HIR). After the 2026-05-26 closure pass the open set is: **D1** (pointer/array type-level — feature/grammar round, *when* a corpus needs typed pointers or HIR lowers them), **D3-residual** (`VARCHAR(N)` parameter — feature/grammar round, *when* tsql needs precise length typing), **D4-residual** (AP4 + CLI — AP2/AP3 ✅ landed 2026-06-09; owned by phase #12 `program-api`), **D5/D6** (c-subset and tsql language-surface expansion — v1.x feature rounds, *when* the credibility-as-real-language goal or a corpus needs them), **D7** (ternary/mixfix — its own schema-v3 round, *when* a shipped language needs it), **D9** (use-before-init / unreachable — *when* HIR's CFG exists), and **D10** (cross-CU / incremental — *when* an artifact profile needs multiple CUs). D9 is the only one HIR itself helps unblock.

## 0.3 Diagnostic-code nibble registry (cross-plan authority)

**Single authoritative table for the diagnostic-code high-nibble allocation.** The shipped implementation lives in `src/core/types/parse_diagnostic.cpp`'s `diagnosticCodePrefix()` switch (the runtime that maps the numeric code to its rendered prefix letter). This table mirrors that; **the cpp file is the source of truth, this is the planning view** so cross-plan PRs don't accidentally re-use a slot. Update both when a new family lands.

| Nibble | Rendered prefix | Family | Owner plan | Status |
|---|---|---|---|---|
| `0x0xxx` | `P_` | Parse | [`05`](./05-parser-plan%20-%20ok.md) | ✅ shipped |
| `0x1xxx` | `A_` | Assembler (byte-encoding pass) | [`13`](./13-assembler-plan%20-%20tbd.md) | ✅ shipped (AS1 cycle 1 — 2026-05-29) |
| `0x2xxx` | `X_` | Optimizer (pass pipeline / cost-model / verify-after-pass / linkage-liveness) | [`22`](./22-optimizer-plan%20-%20tbd.md) | ⏳ reserved (claimed by plan 22 rev 1; lands with OPT1) |
| `0x3xxx` | — | (reserved — free) | — | available |
| `0x4xxx` | `R_` | Register allocator | [`12`](./12-mir-lir-plan%20-%20ok.md) | ✅ shipped (ML6 cycle 3a) |
| `0x5xxx` | `O_` / `F_` | Object format / linker / FFI | [`14`](./14-linker-plan%20-%20tbd.md), [`11`](./11-ffi-plan%20-%20tbd.md) | ✅ live (FF1–FF5 codes 0x5000–0x5019 shipped — F_* prefix used for the FFI half; linker O_* codes still reserved within the band per the original allocation) |
| `0x6xxx` | `W_` | WAT/WASM verifier + emit-side | [`18`](./18-wasm-plan%20-%20tbd.md) §2.9b | ⏳ reserved (post-rev-3 allocation) |
| `0x7xxx` | `V_` | SPIR-V verifier + emit-side | [`17`](./17-shader-gpu-plan%20-%20tbd.md) §2.9 | ⏳ reserved (post-rev-3 allocation) |
| `0x8xxx` | `K_` | Linker (object format engine + reloc apply + file emission) | [`14`](./14-linker-plan%20-%20tbd.md) | ✅ shipped (LK1–LK10 + post-fold additions through 2026-06-02; 14 K_* codes 0x8001–0x800B + 0x800D–0x800F — `K_EntryPointResolvesToExtern`, `K_DuplicateDataSymbol`, `K_BssDataHasBytes`) |
| `0x9xxx` | `P_` | Parse (internal-invariant range, distinct from 0x0xxx user-facing) | [`05`](./05-parser-plan%20-%20ok.md) | ✅ shipped |
| `0xAxxx` | `I_` | MIR (IR-gen mid-level + verifier) | [`12`](./12-mir-lir-plan%20-%20ok.md) | ✅ shipped (ML3) |
| `0xBxxx` | `L_` | LIR lowering + verifier | [`12`](./12-mir-lir-plan%20-%20ok.md) | ✅ shipped (ML5–ML8) |
| `0xCxxx` | `C_` | Config-loader (`.lang.json` / `.target.json` / future `.format.json`) | [`02`](./02-schema-expressiveness-v2-plan%20-%20ok.md) + cross-plan | ✅ shipped (0xC001..0xC033 in production) |
| `0xDxxx` | `D_` | Driver / compilation-unit / import resolver | [`08`](./08-compilation-unit-plan%20-%20tbd.md) | ✅ shipped (CU2+ ImportResolver) |
| `0xExxx` | `S_` | Semantic analysis | [`08.6`](./08.6-semantic-plan%20-%20ok.md) | ✅ shipped (SE1–SE7) |
| `0xFxxx` | `H_` | HIR (verifier + text + lowering) | [`09`](./09-hir-plan%20-%20ok.md) | ✅ shipped (HR1–HR11) |

**Allocation discipline:**
- Each family owns its full nibble (4096 codes); cycles within a plan allocate codes incrementally without coordinating with other plans.
- Before adding a new family, **claim the slot here AND in `parse_diagnostic.cpp`'s `diagnosticCodePrefix()` switch** in the same PR. Reading either in isolation is the failure mode.
- Free slots: `0x3xxx` (`0x2xxx` now claimed by plan 22 — optimizer `X_*`). Post-v1 candidates (JVM IL, .NET IL, future shader-stage validators, future debug-info-emit) draw from `0x3xxx`.
- **DO NOT subdivide existing nibbles across families.** Pre-rev-3, plans 18 + 17 had each claimed sub-bands `0xC1xx` / `0xC2xx` within the `C_*` config nibble — silent collision with the shipped `C_001..C_033` range. Corrected to top-level nibble ownership.

## 1. Vision & Overview

**DSS Code Prime** is a universal, configurable compiler written in C++. Its core design principle is that **both the source language and the target platform are configurable**, making it a single compiler engine capable of compiling _any_ defined language to _any_ supported target.

### Key Design Goals

| Goal | Description |
|---|---|
| **Language-Agnostic Input** | Any programming language can be defined via a JSON configuration file that describes its full syntax, grammar, type system, and semantics. |
| **Multi-Target Output** | Compile to Windows, Linux, macOS, iOS, Android, and Web (WASM) — each with their accessible processor architectures (x86_64, ARM64, RISC-V, WASM, etc.). |
| **Three-Phase Analysis** | Classical compiler pipeline: Lexical → Syntactic → Semantic analysis. |
| **Portable Build** | Runs natively on Windows, Linux, and macOS. Docker image provided for reproducible cross-compilation environments. |
| **Extensible Architecture** | New languages are added via config files; new targets via pluggable backend modules. |

---

## 2. High-Level Architecture

```
┌──────────────────────────────────────────────────────────────────────────┐
│                          DSS Code Prime                                  │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                        program (Public API)                        │  │
│  │  Receives: project file | file list | directory path               │  │
│  │  Receives: source language name                                    │  │
│  │  Receives: target platform(s) — one or many                        │  │
│  │  Resolves source files by extension, dispatches compilation        │  │
│  └───────────────────────────┬────────────────────────────────────────┘  │
│                              │                                           │
│                              ▼                                           │
│  ┌──────────────┐   ┌───────────────────────┐   ┌───────────────┐       │
│  │  dss-config   │──▶│   source-factory      │──▶│  core/types   │       │
│  │  (JSON files) │   │ (parser + validator)  │   │  (in-memory   │       │
│  └──────────────┘   └───────────────────────┘   │   lang model) │       │
│                                                  └──────┬────────┘       │
│                                                         │                │
│  ┌──────────────────────────────────────────────────────┘                │
│  │                                                                       │
│  ▼                                                                       │
│  ┌────────────┐   ┌──────────────────────────────────┐                   │
│  │ tokenizer  │──▶│           analysis                │                   │
│  │ (char→tok) │   │  ┌─────────┬──────────┬────────┐ │                   │
│  └────────────┘   │  │ lexical │syntactic │semantic│ │                   │
│                    │  │ (rules  │(parser + │(types, │ │                   │
│                    │  │  check) │ AST)     │ scope) │ │                   │
│                    │  └─────────┴──────────┴────────┘ │                   │
│                    └──────────────┬───────────────────┘                   │
│                                  │                                       │
│                                  ▼                                       │
│                    ┌─────────────────────────────────────────────────┐   │
│                    │                    gen                           │   │
│                    │  ┌──────────────┬────────────┬───────────────┐  │   │
│                    │  │ intermediate │ optimizer  │     link      │  │   │
│                    │  │ (AST → IR)   │ (IR passes │ (target emit  │  │   │
│                    │  │              │  & xforms) │  + linking)   │  │   │
│                    │  └──────────────┴────────────┴───────────────┘  │   │
│                    └─────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────┘
```

### Data Flow Summary

```
User Input (via program API)
    │  ─ project file (.dsp), file list, or directory path
    │  ─ source language name (e.g. "ExampleLang")
    │  ─ target platform(s) (e.g. ["linux-x86_64", "web-wasm"])
    │
    ▼
[program] ── resolves input → discovers source files by extension
    │         loads language config via source-factory
    │         iterates targets, dispatches per-file compilation
    │
    ▼ (for each source file × each target)
[source-factory] ── loads language definition from JSON
    │
    ▼
[tokenizer] ── raw characters → token stream
    │
    ▼
[analysis/lexical] ── validates tokens against language rules
    │
    ▼
[analysis/syntactic] ── token stream → Abstract Syntax Tree (AST)
    │
    ▼
[analysis/semantic] ── type checking, scope resolution, semantic validation
    │
    ▼
[gen/intermediate] ── AST → Intermediate Representation (IR)
    │
    ▼
[gen/optimizer] ── IR optimization passes (constant folding, DCE, CSE, etc.)
    │
    ▼
[gen/link] ── optimized IR → target-specific machine code, linking, output binary
    │
    ▼
Executable / Library / WASM module (per target)
```

---

## 3. Project File Structure

```
dss-code-prime/
│
├── .plans/
│   └── 00-compiler-implementation-plan - tbd.md      # This document
│
├── CMakeLists.txt                           # Root CMake build (cross-platform)
├── README.md
├── .gitignore
│
├── docker/
│   ├── Dockerfile                           # Multi-stage build image
│   ├── docker-compose.yml                   # Dev/CI compose file
│   └── toolchains/                          # CMake toolchain files for cross-compile
│       ├── linux-x86_64.cmake
│       ├── linux-arm64.cmake
│       ├── windows-x86_64.cmake
│       ├── macos-x86_64.cmake
│       ├── macos-arm64.cmake
│       ├── ios-arm64.cmake
│       ├── android-arm64.cmake
│       └── web-wasm.cmake
│
├── docs/
│   ├── architecture.md                      # Detailed architecture documentation
│   ├── language-config-spec.md              # JSON config specification
│   └── target-config-spec.md                # Target platform specification
│
├── libs/                                    # Third-party dependencies
│   └── README.md                            # Dependency documentation
│
├── src/
│   ├── CMakeLists.txt                       # Src-level CMake
│   ├── main.cpp                             # CLI entry point (thin — delegates to program)
│   │
│   ├── program/                             # ── Public API / Driver ──
│   │   ├── CMakeLists.txt
│   │   ├── program.hpp                      # Public API: the open interface for compilation
│   │   ├── program.cpp                      # Orchestrates input resolution + multi-target compilation
│   │   ├── input_resolver.hpp               # Resolves project / file list / directory → source files
│   │   ├── input_resolver.cpp
│   │   ├── project_file.hpp                 # Parses .dsp project file (project definition)
│   │   ├── project_file.cpp
│   │   ├── compilation_request.hpp          # Request model: source lang, files, targets
│   │   └── compilation_result.hpp           # Result model: per-target output paths + diagnostics
│   │
│   ├── core/                                # ── Shared types & utilities ──
│   │   ├── CMakeLists.txt
│   │   ├── compiler.hpp                     # Top-level compiler orchestrator interface
│   │   ├── compiler.cpp                     # Orchestrates the full pipeline
│   │   ├── types/
│   │   │   ├── token.hpp                    # Token type (kind, value, location)
│   │   │   ├── ast.hpp                      # AST node definitions (base + variants)
│   │   │   ├── ir.hpp                       # Intermediate Representation node types
│   │   │   ├── symbol.hpp                   # Symbol table entry type
│   │   │   ├── source_location.hpp          # File/line/col position tracking
│   │   │   └── target_info.hpp              # Target OS + processor descriptor
│   │   ├── error/
│   │   │   ├── error.hpp                    # Error/Warning/Info diagnostic type
│   │   │   ├── error.cpp
│   │   │   ├── error_reporter.hpp           # Collects and formats diagnostics
│   │   │   └── error_reporter.cpp
│   │   └── utils/
│   │       ├── file_io.hpp                  # Cross-platform file reading utilities
│   │       ├── file_io.cpp
│   │       ├── string_utils.hpp             # String manipulation helpers
│   │       └── string_utils.cpp
│   │
│   ├── dss-config/                          # ── DSS Configuration Files ──
│   │   ├── README.md                        # How to write a language / target config
│   │   ├── schemas/
│   │   │   └── language-schema.json         # JSON Schema for validation
│   │   ├── sources/                         # per-language grammars (was `languages/`)
│   │   │   └── example.lang.json            # Example language definition
│   │   └── targets/                         # per-target ISA configs (ML5+)
│   │       └── x86_64.target.json           # Example target definition
│   │
│   ├── source-factory/                # ── Config Parser & Loader ──
│   │   ├── CMakeLists.txt
│   │   ├── config_reader.hpp                # Public API: load(path) → LanguageConfig
│   │   ├── config_reader.cpp                # JSON parsing + hydration into models
│   │   ├── models/
│   │   │   ├── language_config.hpp          # Root model: holds full language definition
│   │   │   ├── token_definition.hpp         # Defines a token kind (regex, keyword list, etc.)
│   │   │   ├── grammar_rule.hpp             # BNF/PEG-style production rule model
│   │   │   ├── type_system_config.hpp       # Primitive types, type rules, coercion
│   │   │   ├── operator_definition.hpp      # Operators: symbol, precedence, associativity
│   │   │   └── semantic_rule.hpp            # Semantic constraints (e.g. "variables must be declared")
│   │   └── validators/
│   │       ├── config_validator.hpp          # Validates loaded config for completeness/consistency
│   │       └── config_validator.cpp
│   │
│   ├── tokenizer/                           # ── Tokenization (char stream → tokens) ──
│   │   ├── CMakeLists.txt
│   │   ├── tokenizer.hpp                    # Public API: tokenize(source, lang_config) → TokenStream
│   │   ├── tokenizer.cpp                    # Core tokenization engine (driven by token definitions)
│   │   ├── token_stream.hpp                 # Iterable token container with peek/advance
│   │   ├── token_stream.cpp
│   │   ├── source_reader.hpp                # Buffered character reader with location tracking
│   │   └── source_reader.cpp
│   │
│   ├── analysis/                            # ── Three-Phase Analysis ──
│   │   ├── CMakeLists.txt
│   │   │
│   │   ├── lexical/                         # Phase 1: Lexical Analysis
│   │   │   ├── lexer.hpp                    # Public API: validates & classifies token stream
│   │   │   ├── lexer.cpp                    # Applies lexical rules from language config
│   │   │   ├── lexical_rules.hpp            # Rule engine: keyword matching, literal validation
│   │   │   └── lexical_rules.cpp
│   │   │
│   │   ├── syntactic/                       # Phase 2: Syntactic Analysis (Parsing)
│   │   │   ├── parser.hpp                   # Public API: parse(tokens, grammar) → AST
│   │   │   ├── parser.cpp                   # Recursive descent / table-driven parser
│   │   │   ├── grammar.hpp                  # Runtime grammar representation (from config)
│   │   │   ├── grammar.cpp                  # Grammar loading and first/follow set computation
│   │   │   ├── ast_builder.hpp              # Constructs AST nodes during parsing
│   │   │   └── ast_builder.cpp
│   │   │
│   │   └── semantic/                        # Phase 3: Semantic Analysis
│   │       ├── semantic_analyzer.hpp        # Public API: analyze(AST) → annotated AST
│   │       ├── semantic_analyzer.cpp        # Orchestrates all semantic passes
│   │       ├── symbol_table.hpp             # Scoped symbol table (variables, functions, types)
│   │       ├── symbol_table.cpp
│   │       ├── type_checker.hpp             # Type inference and checking engine
│   │       ├── type_checker.cpp
│   │       ├── scope_resolver.hpp           # Scope entry/exit, name resolution
│   │       └── scope_resolver.cpp
│   │
│   └── gen/                                 # ── Code Generation ──
│       ├── CMakeLists.txt
│       │
│       ├── intermediate/                    # IR Generation
│       │   ├── ir_generator.hpp             # Public API: generate(AST) → IR
│       │   ├── ir_generator.cpp             # Walks annotated AST, emits IR nodes
│       │   └── ir_node.hpp                  # IR instruction set (three-address code style)
│       │
│       ├── optimizer/                       # IR Optimization (target-independent)
│       │   ├── optimizer.hpp                # Public API: optimize(IR) → optimized IR
│       │   ├── optimizer.cpp                # Runs the optimization pass pipeline
│       │   ├── pass.hpp                     # Abstract base class for optimization passes
│       │   ├── passes/                      # Individual optimization pass implementations
│       │   │   ├── constant_folding.hpp     # Evaluate compile-time constant expressions
│       │   │   ├── constant_folding.cpp
│       │   │   ├── constant_propagation.hpp # Replace variables with known constant values
│       │   │   ├── constant_propagation.cpp
│       │   │   ├── dead_code_elimination.hpp # Remove unreachable / unused instructions
│       │   │   ├── dead_code_elimination.cpp
│       │   │   ├── common_subexpr_elim.hpp  # Reuse already-computed expressions
│       │   │   ├── common_subexpr_elim.cpp
│       │   │   ├── copy_propagation.hpp     # Replace copies with original values
│       │   │   ├── copy_propagation.cpp
│       │   │   ├── strength_reduction.hpp   # Replace expensive ops with cheaper equivalents
│       │   │   ├── strength_reduction.cpp
│       │   │   ├── loop_invariant_motion.hpp # Hoist invariant computations out of loops
│       │   │   └── loop_invariant_motion.cpp
│       │   └── analysis/                    # IR analysis utilities used by passes
│       │       ├── cfg_builder.hpp          # Build control-flow graph from IR
│       │       ├── cfg_builder.cpp
│       │       ├── liveness.hpp             # Variable liveness analysis
│       │       ├── liveness.cpp
│       │       ├── reaching_defs.hpp        # Reaching definitions analysis
│       │       └── reaching_defs.cpp
│       │
│       └── link/                            # Target Code Emission & Linking
│           ├── linker.hpp                   # Public API: link(IR, target) → output binary
│           ├── linker.cpp                   # Resolves symbols, invokes target emitter
│           ├── target_config.hpp            # Target descriptor (OS, arch, ABI)
│           ├── target_config.cpp
│           └── targets/                     # Per-target code emitters
│               ├── target_base.hpp          # Abstract base class for all targets
│               ├── target_windows_x86_64.hpp
│               ├── target_windows_x86_64.cpp
│               ├── target_linux_x86_64.hpp
│               ├── target_linux_x86_64.cpp
│               ├── target_linux_arm64.hpp
│               ├── target_linux_arm64.cpp
│               ├── target_macos_x86_64.hpp
│               ├── target_macos_x86_64.cpp
│               ├── target_macos_arm64.hpp
│               ├── target_macos_arm64.cpp
│               ├── target_ios_arm64.hpp
│               ├── target_ios_arm64.cpp
│               ├── target_android_arm64.hpp
│               ├── target_android_arm64.cpp
│               ├── target_web_wasm.hpp
│               └── target_web_wasm.cpp
│
└── tests/                                   # ── Test Suite (mirrors src/) ──
    ├── CMakeLists.txt
    ├── program/
    │   ├── test_program.cpp
    │   ├── test_input_resolver.cpp
    │   └── test_project_file.cpp
    ├── core/
    │   ├── test_error_reporter.cpp
    │   └── test_types.cpp
    ├── source-factory/
    │   ├── test_config_reader.cpp
    │   └── test_config_validator.cpp
    ├── tokenizer/
    │   ├── test_tokenizer.cpp
    │   ├── test_token_stream.cpp
    │   └── test_source_reader.cpp
    ├── analysis/
    │   ├── lexical/
    │   │   └── test_lexer.cpp
    │   ├── syntactic/
    │   │   ├── test_parser.cpp
    │   │   └── test_grammar.cpp
    │   └── semantic/
    │       ├── test_semantic_analyzer.cpp
    │       ├── test_symbol_table.cpp
    │       └── test_type_checker.cpp
    └── gen/
        ├── intermediate/
        │   └── test_ir_generator.cpp
        ├── optimizer/
        │   ├── test_optimizer.cpp
        │   ├── test_constant_folding.cpp
        │   ├── test_dead_code_elimination.cpp
        │   └── test_cfg_builder.cpp
        └── link/
            └── test_linker.cpp
```

---

## 4. Module Detailed Documentation

---

### 4.1 `program/` — Public API & Driver

The **program** module is the open API layer — the entry point that external consumers (CLI, IDE integrations, CI pipelines) use to invoke compilation. It handles **what** to compile and **where** to output, then delegates the **how** to the compiler pipeline.

#### 4.1.1 Input Modes

The API accepts three input modes:

| Mode | Description | Example |
|---|---|---|
| **Project file** | A `.dsp` (DSS Project) JSON file that declares source language, source directories/files, targets, and output configuration. | `program.compile("myapp.dsp")` |
| **File list** | An explicit list of source file paths, plus language name and target(s). | `program.compile(files, "ExampleLang", targets)` |
| **Directory** | A directory path — the program scans it recursively for files matching the language's `fileExtensions` from the config. | `program.compile("./src/", "ExampleLang", targets)` |

#### 4.1.2 `program.hpp/.cpp` — Public API

**Status (2026-05-30): shipped at plan 14 LK10 cycle 2.** The v1 surface diverges from the historical TargetInfo-struct sketch — targets are passed as `"<targetName>:<formatName>"` strings (e.g. `"x86_64:elf64-x86_64-linux"`) so the C-ABI (`dss_compile_directory`) stays a flat `const char**` array. The richer `TargetInfo`-style struct will arrive when LK10 cycle 3 ships CLI flag routing (`--target`/`--format`) backed by `TargetSpec` parsing (see `src/program/target_spec.hpp`).

```cpp
class Program {
public:
    Program() = default;
    ~Program() = default;

    /// CLI entry point — currently dispatches `--lsp` mode only.
    int run(int argc, char* argv[]);

    /// Compile from a .dsp project file (self-contained — language, files, targets defined inside).
    /// Cycle 2 stub: fails loud with `D_PlanNotLanded` until plan 06 (`.dsp` parser) ships.
    int compileProject(const std::string& projectFilePath);

    /// Compile an explicit list of source files for the given language to the given target(s).
    /// Each target encodes "<targetName>:<formatName>" — both halves are `loadShipped` keys.
    int compileFiles(
        const std::vector<std::string>& sourceFiles,
        const std::string& languageName,
        const std::vector<std::string>& targets
    );

    /// Compile all matching source files found in a directory (recursive scan by language extension).
    int compileDirectory(
        const std::string& directoryPath,
        const std::string& languageName,
        const std::vector<std::string>& targets
    );
};
```

**Behavior (post-LK10 cycle 2):**
- `compileFiles` loads the language schema once, builds ONE multi-file `CompilationUnit`, drains CU + per-Tree + SemanticModel diagnostics into the run-wide reporter, then per-target compiles through the full HIR → MIR → LIR → ASM → link → writeImage pipeline (`src/program/compile_pipeline.{hpp,cpp}::compileSingleUnit`).
- Output path: `<cwd>/target/<formatName>/<stem><ext>`; extension derived from `ObjectFormatKind × objectType` via `TargetSpec::outputExtension`. Plan 06 will eventually own the artifact-profile-driven scheme (D-LK10-3).
- `compileDirectory` recursively walks the directory, filters by the language schema's declared `fileExtensions`, sorts deterministically, delegates to `compileFiles`. (Plan 00 §4.1.3's `InputResolver` class is anchored at D-LK10-1 for LK10 cycle 3.)
- Returns 0 on success; 1 on any tier failure with diagnostics drained to stderr.
- Cycle 2 acceptance is WIRING (output directory creation proves the pipeline routed through every tier); byte-on-disk e2e auto-tightens when plan 12 ML7 cycle 2 + plan 13 AS load/store/ret-variant cycles close (D-LK10-2).

#### 4.1.3 `input_resolver.hpp/.cpp` — Input Resolution

```cpp
class InputResolver {
public:
    /// Resolve a directory to a list of source files matching the given extensions.
    static std::vector<std::string> resolveDirectory(
        const std::string& directoryPath,
        const std::vector<std::string>& fileExtensions,
        bool recursive = true
    );

    /// Validate that all files in a list exist and are readable.
    static std::vector<std::string> validateFiles(const std::vector<std::string>& filePaths);

    /// Resolve a mixed input (could be a file, directory, or glob pattern).
    static std::vector<std::string> resolve(
        const std::string& input,
        const std::vector<std::string>& fileExtensions
    );
};
```

**Responsibilities:**
- Recursively scans directories for files matching configured extensions (e.g. `.exl`, `.example`)
- Validates file existence and read permissions
- Supports glob patterns (e.g. `src/**/*.exl`)
- Returns sorted, deduplicated absolute paths
- Cross-platform (Windows `\` vs POSIX `/`)

#### 4.1.4 `project_file.hpp/.cpp` — Project File Parser

A `.dsp` (DSS Project) file is a JSON document that bundles all compilation parameters:

```jsonc
{
  "project": {
    "name": "MyApplication",
    "version": "1.0.0"
  },
  "source": {
    "language": "ExampleLang",
    "include": [
      "src/",
      "lib/helpers.exl"
    ],
    "exclude": [
      "src/tests/",
      "src/deprecated/"
    ]
  },
  "targets": [
    { "os": "linux",   "arch": "x86_64" },
    { "os": "windows", "arch": "x86_64" },
    { "os": "web",     "arch": "wasm"   }
  ],
  "output": {
    "directory": "build/",
    "nameTemplate": "${project.name}-${target.os}-${target.arch}"
  }
}
```

```cpp
class ProjectFile {
public:
    /// Parse a .dsp project file.
    static ProjectFile load(const std::string& projectFilePath);

    std::string projectName;
    std::string projectVersion;
    std::string languageName;
    std::vector<std::string> includePaths;   // files and/or directories
    std::vector<std::string> excludePaths;   // exclusion patterns
    std::vector<TargetInfo> targets;
    std::string outputDirectory;
    std::string nameTemplate;
};
```

#### 4.1.5 `compilation_request.hpp` — Request Model

```cpp
struct CompilationRequest {
    std::vector<std::string> sourceFiles;    // Resolved absolute paths
    std::string languageName;                // Language config to load
    std::vector<TargetInfo> targets;         // One or more targets
    std::string outputDirectory;             // Where to write outputs
    std::string nameTemplate;                // Output naming pattern (optional)
};
```

#### 4.1.6 `compilation_result.hpp` — Result Model

```cpp
struct FileTargetResult {
    std::string sourceFile;
    TargetInfo target;
    bool success;
    std::string outputPath;                  // Path to generated binary (on success)
    std::vector<Error> diagnostics;          // Errors, warnings, info for this file+target
};

struct CompilationResult {
    bool overallSuccess;                     // True if all files × all targets succeeded
    std::vector<FileTargetResult> results;   // Per-file, per-target results
    std::vector<Error> globalDiagnostics;    // Diagnostics not tied to a specific file

    /// Convenience: get all results for a specific target.
    std::vector<FileTargetResult> resultsForTarget(const TargetInfo& target) const;

    /// Convenience: get all failed results.
    std::vector<FileTargetResult> failures() const;
};
```

---

### 4.2 `core/` — Shared Types & Utilities

The **core** module contains types and utilities shared across every stage of the compiler. Nothing in `core/` depends on any other `src/` module — it is the foundation layer.

#### 4.2.1 `core/compiler.hpp/.cpp` — Compiler Orchestrator

The top-level class that wires the entire pipeline together:

```cpp
class Compiler {
public:
    /// Construct with a language config path and target specification.
    Compiler(const std::string& langConfigPath, const TargetInfo& target);

    /// Compile a source file, returning diagnostics and (on success) the output path.
    CompileResult compile(const std::string& sourceFilePath);

private:
    std::unique_ptr<LanguageConfig> langConfig_;
    TargetInfo target_;
    ErrorReporter reporter_;
};
```

**Responsibilities:**
- Loads the language configuration via `source-factory`
- Orchestrates: Tokenize → Lex → Parse → Semantic → IR Gen → Optimize → Link
- Collects and returns diagnostics from all phases

#### 4.2.2 `core/types/` — Fundamental Data Types

> **AMENDED by [`01-tree-node-model-plan - ok.md`](./01-tree-node-model-plan - ok.md) §5.1–§5.11.** The `ast.hpp` line below is replaced by the full tree/node model (`Tree`, `Node`, `NodeFlags`, `TreeBuilder`, `TreeCursor`, `NodeAttribute<T>`, typed views). `token.hpp` is replaced by the sub-plan's `Token` + `CoreTokenKind` + `SchemaTokenId` split. `source_location.hpp` is replaced by `SourceBuffer` + `SourceSpan` (byte offsets; line/col derived). The list below is kept for historical reference only; new work follows the sub-plan.

| File | Purpose |
|---|---|
| `token.hpp` | `Token` struct: `{ TokenKind kind; std::string lexeme; SourceLocation loc; }` — the universal token representation used from tokenizer through parser. |
| `ast.hpp` | AST node hierarchy. Base `ASTNode` with variants: `LiteralNode`, `BinaryExprNode`, `UnaryExprNode`, `IdentifierNode`, `FunctionDeclNode`, `BlockNode`, `IfNode`, `WhileNode`, `ReturnNode`, etc. Uses a visitor pattern for traversal. |
| `ir.hpp` | IR instruction types: `IRInstruction` base with `IRBinaryOp`, `IRUnaryOp`, `IRLoad`, `IRStore`, `IRCall`, `IRBranch`, `IRLabel`, `IRReturn`, etc. Three-address code style. |
| `symbol.hpp` | `Symbol` struct: `{ std::string name; TypeInfo type; ScopeLevel scope; SourceLocation declLoc; bool isInitialized; }` |
| `source_location.hpp` | `SourceLocation` struct: `{ std::string filePath; size_t line; size_t column; }` — used in every diagnostic message. |
| `target_info.hpp` | `TargetInfo` struct: `{ TargetOS os; TargetArch arch; std::string abi; }` — describes the compilation target. |

#### 4.2.3 `core/error/` — Diagnostic System

> **AMENDED.** Replaced by `ParseDiagnostic` + `DiagnosticReporter` + `DiagnosticPolicy` in [01-tree-node-model-plan - ok.md §5.13–§5.14](./01-tree-node-model-plan - ok.md). The diagnostic system is now structured (`expected`/`actual`/`scopeStack`/`related`), uses a stable `DiagnosticCode` enum, supports per-code suppression/promotion, and lives at `src/core/types/`. Drop `src/core/error/` from the layout.

A centralized error reporting system used by every compiler phase:

- **`Error`**: Holds severity (Error/Warning/Info/Hint), message, source location, and an error code.
- **`ErrorReporter`**: Accumulates diagnostics, supports formatted output, can abort on first error or collect all.

#### 4.2.4 `core/utils/` — Cross-Platform Utilities

- **`file_io`**: Read file to string, check existence, resolve paths (abstracts Windows/POSIX differences).
- **`string_utils`**: UTF-8 aware helpers, escape sequences, string trimming.

---

### 4.3 `dss-config/` — DSS Configuration Files (JSON)

> **AMENDED by [01-tree-node-model-plan - ok.md §5.12](./01-tree-node-model-plan - ok.md).** The config schema below describes the *original* design (BNF rules, `lexical`/`syntactic`/`semantic` top-level sections). The sub-plan defines the *current* schema: a `tokens` map with multi-typed lexeme meanings (priority-tiebroken), `keywords`, `scopes` (validity rules + opens/closesScope), and `shapes` (the expected node tree with `sequence` / `alt` / `optional` / `repeat`). The schema is versioned via `dssSchemaVersion`. Configs are loaded via `GrammarSchema::loadFromFile` and produce a `Result<…>` with `C####` diagnostics on malformed input. The JSON sketch in this section is kept for historical context only.

This directory contains the **JSON configuration files** that define programming languages. Each file fully describes a language's lexical, syntactic, and semantic rules — the compiler engine reads these at startup.

#### 4.3.1 JSON Schema Structure (`schemas/language-schema.json`)

The schema defines the expected shape of every language config file. Top-level sections:

```jsonc
{
  "$schema": "language-schema.json",
  "language": {
    "name": "ExampleLang",
    "version": "1.0.0",
    "fileExtensions": [".exl", ".example"]
  },

  "lexical": {
    "comments": {
      "singleLine": "//",
      "multiLineStart": "/*",
      "multiLineEnd": "*/"
    },
    "whitespace": {
      "significant": false,
      "newlineSignificant": false
    },
    "literals": {
      "integer": { "pattern": "[0-9]+", "suffixes": ["u", "l", "ul"] },
      "float": { "pattern": "[0-9]+\\.[0-9]+", "suffixes": ["f", "d"] },
      "string": { "delimiters": ["\""], "escapeChar": "\\", "multiline": false },
      "char": { "delimiters": ["'"], "escapeChar": "\\" },
      "boolean": { "trueKeyword": "true", "falseKeyword": "false" },
      "null": { "keyword": "null" }
    },
    "keywords": [
      "if", "else", "while", "for", "return", "function",
      "var", "const", "class", "import", "export"
    ],
    "operators": [
      { "symbol": "+",  "precedence": 10, "associativity": "left",  "type": "binary" },
      { "symbol": "-",  "precedence": 10, "associativity": "left",  "type": "binary" },
      { "symbol": "*",  "precedence": 20, "associativity": "left",  "type": "binary" },
      { "symbol": "/",  "precedence": 20, "associativity": "left",  "type": "binary" },
      { "symbol": "=",  "precedence": 1,  "associativity": "right", "type": "binary" },
      { "symbol": "==", "precedence": 5,  "associativity": "left",  "type": "binary" },
      { "symbol": "!=", "precedence": 5,  "associativity": "left",  "type": "binary" },
      { "symbol": "!",  "precedence": 30, "associativity": "right", "type": "unary_prefix" },
      { "symbol": "++", "precedence": 30, "associativity": "right", "type": "unary_prefix" }
    ],
    "delimiters": {
      "statementTerminator": ";",
      "blockStart": "{",
      "blockEnd": "}",
      "parenStart": "(",
      "parenEnd": ")",
      "bracketStart": "[",
      "bracketEnd": "]",
      "separator": ","
    },
    "identifiers": {
      "startPattern": "[a-zA-Z_]",
      "continuePattern": "[a-zA-Z0-9_]",
      "caseSensitive": true
    }
  },

  "syntactic": {
    "grammar": {
      "format": "BNF",
      "rules": [
        { "name": "program",          "production": "statement*" },
        { "name": "statement",        "production": "variableDecl | functionDecl | expressionStmt | ifStmt | whileStmt | returnStmt | block" },
        { "name": "variableDecl",     "production": "('var' | 'const') IDENTIFIER (':' type)? ('=' expression)? ';'" },
        { "name": "functionDecl",     "production": "'function' IDENTIFIER '(' paramList? ')' (':' type)? block" },
        { "name": "paramList",        "production": "param (',' param)*" },
        { "name": "param",            "production": "IDENTIFIER ':' type" },
        { "name": "block",            "production": "'{' statement* '}'" },
        { "name": "ifStmt",           "production": "'if' '(' expression ')' block ('else' (ifStmt | block))?" },
        { "name": "whileStmt",        "production": "'while' '(' expression ')' block" },
        { "name": "returnStmt",       "production": "'return' expression? ';'" },
        { "name": "expressionStmt",   "production": "expression ';'" },
        { "name": "expression",       "production": "assignment" },
        { "name": "assignment",       "production": "equality ('=' assignment)?" },
        { "name": "equality",         "production": "comparison (('==' | '!=') comparison)*" },
        { "name": "comparison",       "production": "addition (('<' | '>' | '<=' | '>=') addition)*" },
        { "name": "addition",         "production": "multiplication (('+' | '-') multiplication)*" },
        { "name": "multiplication",   "production": "unary (('*' | '/') unary)*" },
        { "name": "unary",            "production": "('!' | '-' | '++') unary | primary" },
        { "name": "primary",          "production": "INTEGER | FLOAT | STRING | BOOLEAN | NULL | IDENTIFIER | '(' expression ')' | functionCall" },
        { "name": "functionCall",     "production": "IDENTIFIER '(' argList? ')'" },
        { "name": "argList",          "production": "expression (',' expression)*" },
        { "name": "type",             "production": "IDENTIFIER ('<' typeList '>')?" },
        { "name": "typeList",         "production": "type (',' type)*" }
      ]
    }
  },

  "semantic": {
    "typeSystem": {
      "primitiveTypes": ["int", "float", "double", "string", "bool", "void"],
      "typeInference": true,
      "implicitConversions": [
        { "from": "int", "to": "float" },
        { "from": "int", "to": "double" },
        { "from": "float", "to": "double" }
      ]
    },
    "scoping": {
      "model": "block",
      "allowShadowing": true,
      "hoisting": false
    },
    "rules": [
      { "id": "VAR_DECL_BEFORE_USE",     "description": "Variables must be declared before use", "severity": "error" },
      { "id": "TYPE_MISMATCH",            "description": "Assignment type must match or be implicitly convertible", "severity": "error" },
      { "id": "CONST_REASSIGNMENT",       "description": "Cannot reassign a const variable", "severity": "error" },
      { "id": "RETURN_TYPE_MATCH",        "description": "Return value must match function return type", "severity": "error" },
      { "id": "UNUSED_VARIABLE",          "description": "Variable declared but never used", "severity": "warning" },
      { "id": "UNREACHABLE_CODE",         "description": "Code after return statement", "severity": "warning" }
    ]
  }
}
```

#### 4.3.2 `languages/` — Shipped Language Definitions

Initially ships with `example.lang.json` as a reference implementation. Future language configs (e.g., a C-subset, a scripting language) can be added here.

---

### 4.4 `source-factory/` — Config Parser & Loader

> **AMENDED.** `LanguageConfig`, the `models/` directory, and `ConfigValidator` are **replaced** by `GrammarSchema` defined in [01-tree-node-model-plan - ok.md §5.12](./01-tree-node-model-plan - ok.md). `src/source-factory/models/` and `src/source-factory/validators/` are dropped from the layout. What remains of `source-factory/` is a thin facade: it resolves a language name (e.g. `"csharp"`) to a config-file path (shipped under `src/source-config/languages/`) and calls `GrammarSchema::loadFromFile`. JSON parsing happens inside `grammar_schema_json.cpp` in `core/types/`; nlohmann/json never leaks past that translation unit.

Reads a `.lang.json` file and hydrates it into a strongly-typed C++ object model.

#### 4.4.1 `config_reader.hpp/.cpp`

```cpp
class ConfigReader {
public:
    /// Load and parse a language config file. Throws on I/O or parse error.
    static std::unique_ptr<LanguageConfig> load(const std::string& configFilePath);

private:
    static void parseLexicalSection(const json& j, LanguageConfig& config);
    static void parseSyntacticSection(const json& j, LanguageConfig& config);
    static void parseSemanticSection(const json& j, LanguageConfig& config);
};
```

**JSON library:** [nlohmann/json](https://github.com/nlohmann/json) (header-only, MIT license).

#### 4.4.2 `models/` — In-Memory Language Model

| File | Class | Key Fields |
|---|---|---|
| `language_config.hpp` | `LanguageConfig` | `name`, `version`, `fileExtensions`, `lexical`, `syntactic`, `semantic` — root container. |
| `token_definition.hpp` | `TokenDefinition` | `kind` (keyword, operator, literal, etc.), `pattern` (regex), `value` (exact string). |
| `grammar_rule.hpp` | `GrammarRule` | `name`, `production` (string), `alternatives` (parsed list of symbol sequences). |
| `type_system_config.hpp` | `TypeSystemConfig` | `primitiveTypes`, `typeInference` flag, `implicitConversions` list. |
| `operator_definition.hpp` | `OperatorDefinition` | `symbol`, `precedence`, `associativity`, `type` (binary/unary_prefix/unary_postfix). |
| `semantic_rule.hpp` | `SemanticRule` | `id`, `description`, `severity` (error/warning). |

#### 4.4.3 `validators/` — Config Validation

- **`ConfigValidator`**: Checks that the loaded config is internally consistent:
  - All grammar non-terminals are defined
  - Operator precedences don't conflict
  - Keywords don't collide with identifier patterns
  - Semantic rules reference valid types
  - All required sections are present

---

### 4.5 `tokenizer/` — Character Stream to Token Stream

> **AMENDED.** The tokenizer **emits every token, including whitespace, newlines, and comments** — it does **not** call `skipWhitespaceAndComments`. Trivia tokens carry `CoreTokenKind::Whitespace` / `LineComment` / `BlockComment`; the schema-aware resolver inside `TreeBuilder::pushToken` (see [01-tree-node-model-plan - ok.md §5.7](./01-tree-node-model-plan - ok.md)) applies `NodeFlags::EmptySpace` per the language config. This preserves source fidelity for formatters/IDE tooling. The `Tokenizer` constructor takes a `GrammarSchema` (sub-plan §5.12), not the obsolete `LanguageConfig`.

The tokenizer is the **first stage** of the pipeline. It reads raw source characters and emits a stream of `Token` objects based on the loaded language config's lexical definitions.

#### 4.5.1 `tokenizer.hpp/.cpp`

```cpp
class Tokenizer {
public:
    /// Construct tokenizer for a given language config.
    explicit Tokenizer(const LanguageConfig& config);

    /// Tokenize an entire source file into a token stream.
    TokenStream tokenize(const std::string& sourceFilePath);

    /// Tokenize from a string (for testing / REPL).
    TokenStream tokenizeString(const std::string& source, const std::string& fileName = "<string>");

private:
    const LanguageConfig& config_;

    Token nextToken(SourceReader& reader);
    Token readStringLiteral(SourceReader& reader);
    Token readNumberLiteral(SourceReader& reader);
    Token readIdentifierOrKeyword(SourceReader& reader);
    Token readOperatorOrDelimiter(SourceReader& reader);
    void skipWhitespaceAndComments(SourceReader& reader);
};
```

**How it works:**
1. Uses `SourceReader` to consume characters one at a time
2. Matches characters against the language config's patterns (longest match wins)
3. Differentiates keywords from identifiers by checking the keyword list
4. Handles string escapes, multi-line strings, nested comments per config
5. Tracks source location for every token

#### 4.5.2 `token_stream.hpp/.cpp`

```cpp
class TokenStream {
public:
    const Token& peek() const;           // Look at current token
    const Token& peekAhead(size_t n);    // Look ahead n tokens
    Token advance();                      // Consume and return current token
    bool match(TokenKind kind);           // Consume if matches, return true/false
    Token expect(TokenKind kind);         // Consume if matches, else error
    bool isAtEnd() const;
    SourceLocation currentLocation() const;

private:
    std::vector<Token> tokens_;
    size_t position_ = 0;
};
```

#### 4.5.3 `source_reader.hpp/.cpp`

Buffered character reader with:
- `peek()` / `advance()` / `isAtEnd()` interface
- Automatic line/column tracking
- UTF-8 aware character consumption

---

### 4.6 `analysis/` — Three-Phase Analysis

---

#### 4.6.1 `analysis/lexical/` — Phase 1: Lexical Analysis

While the tokenizer splits characters into tokens, the **lexical analyzer** validates and enriches them using language-specific rules.

```cpp
class Lexer {
public:
    explicit Lexer(const LanguageConfig& config, ErrorReporter& reporter);

    /// Validate and enrich the token stream. Returns validated stream.
    TokenStream analyze(TokenStream&& rawTokens);

private:
    LexicalRules rules_;
    ErrorReporter& reporter_;
};
```

**Responsibilities:**
- Validates that all tokens match allowed patterns (rejects malformed literals, unknown symbols)
- Classifies ambiguous tokens (e.g., `-` as unary vs binary based on context)
- Validates string escape sequences
- Validates numeric literal suffixes and ranges
- Reports lexical errors with precise source locations

**`LexicalRules`** — Engine that compiles the language config's lexical section into efficient matchers (regex or DFA-based).

---

#### 4.6.2 `analysis/syntactic/` — Phase 2: Syntactic Analysis (Parsing)

> **AMENDED.** The parser produces a **CST** (`Tree`) via the schema-aware `TreeBuilder` from [01-tree-node-model-plan - ok.md §5.7](./01-tree-node-model-plan - ok.md), not an AST hierarchy. The "recursive descent" framing below describes *how* the parser drives `TreeBuilder` (call `open(ruleId)` / `pushToken` / let `OpenScope` close on RAII). Validation is performed by the builder against `GrammarSchema`; errors become `Error`/`Missing` nodes + structured `ParseDiagnostic`s, never exceptions. The AST is recovered as a *view* over the CST via the AST cursor mode (skips `NodeFlags::EmptySpace`).

Transforms the validated token stream into an **Abstract Syntax Tree (AST)**.

```cpp
class Parser {
public:
    Parser(const LanguageConfig& config, ErrorReporter& reporter);

    /// Parse token stream into an AST.
    std::unique_ptr<ASTNode> parse(TokenStream& tokens);

private:
    Grammar grammar_;
    ASTBuilder builder_;
    ErrorReporter& reporter_;

    // Recursive descent methods (generated from grammar rules)
    std::unique_ptr<ASTNode> parseRule(const std::string& ruleName, TokenStream& tokens);
    std::unique_ptr<ASTNode> parseExpression(TokenStream& tokens, int minPrecedence);
};
```

**How it works:**
- The `Grammar` class loads the BNF rules from the language config and computes FIRST/FOLLOW sets
- The parser uses **recursive descent** for statement-level constructs
- Uses **Pratt parsing** (precedence climbing) for expressions, driven by operator definitions from the config
- Error recovery via synchronization tokens (e.g., skip to next `;` or `}`)

**`ASTBuilder`** — Factory for creating AST nodes. Attaches source locations and parent pointers.

**`Grammar`** — Runtime grammar representation:
- Parses BNF production strings into structured alternatives
- Computes FIRST and FOLLOW sets for conflict detection
- Validates grammar is unambiguous (or flags ambiguities for Pratt resolution)

---

#### 4.6.3 `analysis/semantic/` — Phase 3: Semantic Analysis

Validates the AST for **meaning** — type correctness, scope rules, and language constraints.

```cpp
class SemanticAnalyzer {
public:
    SemanticAnalyzer(const LanguageConfig& config, ErrorReporter& reporter);

    /// Perform all semantic passes on the AST. Annotates AST in-place.
    void analyze(ASTNode& root);

private:
    SymbolTable symbolTable_;
    TypeChecker typeChecker_;
    ScopeResolver scopeResolver_;
    ErrorReporter& reporter_;

    void resolveNames(ASTNode& node);    // Pass 1: Name resolution
    void checkTypes(ASTNode& node);      // Pass 2: Type checking
    void checkSemanticRules(ASTNode& node); // Pass 3: Custom rules from config
};
```

**Sub-components:**

| Component | Purpose |
|---|---|
| **`SymbolTable`** | Scoped hash map of symbols. Supports nested scopes (push/pop). Stores variable types, function signatures, class definitions. |
| **`TypeChecker`** | Infers expression types bottom-up. Validates assignments, function calls, returns. Applies implicit conversions from config. |
| **`ScopeResolver`** | Manages scope stack. Resolves identifiers to their declarations. Enforces scoping model (block/function/global) per config. Detects use-before-declare. |

**Semantic passes (in order):**
1. **Name Resolution** — Walk AST, register declarations in symbol table, resolve references
2. **Type Checking** — Infer and check types for all expressions and statements
3. **Custom Rule Checking** — Apply semantic rules from the config (unused variables, unreachable code, const enforcement)

---

### 4.7 `gen/` — Code Generation

---

#### 4.7.1 `gen/intermediate/` — IR Generation

> **AMENDED.** Input is the `Tree` (CST) plus the `NodeAttribute<T>` side-tables populated by semantic analysis (`NodeAttribute<TypeInfo>`, `NodeAttribute<SymbolId>`, …) — see [01-tree-node-model-plan - ok.md §5.10](./01-tree-node-model-plan - ok.md). The generator walks the tree via `TreeCursor` in AST mode and **must not** mutate the tree (immutable post-`finish()`). It bails before emitting IR if `tree.diagnostics().hasErrors()` and strict mode is on; otherwise it generates best-effort IR for the error-free regions.

Transforms the semantically-validated AST into a **target-independent Intermediate Representation**.

```cpp
class IRGenerator {
public:
    explicit IRGenerator(ErrorReporter& reporter);

    /// Generate IR from a semantically-annotated AST.
    IRProgram generate(const ASTNode& root);

private:
    std::vector<IRInstruction> instructions_;
    size_t tempCounter_ = 0;
    size_t labelCounter_ = 0;

    IRValue generateExpression(const ASTNode& expr);
    void generateStatement(const ASTNode& stmt);
    std::string newTemp();     // Generate unique temporary variable
    std::string newLabel();    // Generate unique label
};
```

**IR Design** — Three-address code with the following instructions:
- `BINARY_OP dest, left, op, right` — Arithmetic/logic operations
- `UNARY_OP dest, op, operand` — Unary operations
- `LOAD dest, source` — Load variable value
- `STORE dest, source` — Store to variable
- `CALL dest, function, args...` — Function call
- `PARAM value` — Push function parameter
- `LABEL name` — Label for jumps
- `JUMP label` — Unconditional jump
- `JUMP_IF cond, label` — Conditional jump
- `JUMP_IF_NOT cond, label` — Conditional jump (negated)
- `RETURN value?` — Function return
- `FUNC_BEGIN name` / `FUNC_END` — Function boundaries
- `ALLOC dest, type` — Allocate stack space

---

#### 4.7.2 `gen/optimizer/` — IR Optimizer

The optimizer is a **dedicated module** that sits between IR generation and target emission. It operates **on the IR** (not the AST, not the machine code) because:

1. **IR is simpler than AST** — uniform three-address instructions are easier to analyze than a tree of heterogeneous nodes
2. **Target-independent** — every optimization here benefits all 8 target backends simultaneously
3. **IR is designed for it** — three-address code / SSA representations were specifically created to enable efficient dataflow analysis and transformation
4. **Separated concern** — keeping optimization as its own module makes passes independently testable, orderable, and toggleable

##### Pipeline Architecture

The optimizer runs a configurable **pipeline of passes** over the IR. Each pass implements a common interface and can be enabled/disabled or reordered:

```cpp
/// Abstract base for all optimization passes.
class OptimizationPass {
public:
    virtual ~OptimizationPass() = default;
    virtual std::string name() const = 0;
    virtual bool run(IRProgram& program) = 0;  // Returns true if IR was modified
};

/// Runs the full optimization pipeline.
class Optimizer {
public:
    explicit Optimizer(ErrorReporter& reporter);

    /// Register a pass to the pipeline.
    void addPass(std::unique_ptr<OptimizationPass> pass);

    /// Run all registered passes. Repeats until no pass modifies the IR (fixed-point).
    void optimize(IRProgram& program);

    /// Convenience: build the default optimization pipeline.
    static Optimizer createDefault(ErrorReporter& reporter);

private:
    std::vector<std::unique_ptr<OptimizationPass>> passes_;
    ErrorReporter& reporter_;
    size_t maxIterations_ = 10;  // Safety bound for fixed-point iteration
};
```

##### Optimization Passes

| Pass | What it does | Example |
|---|---|---|
| **Constant Folding** | Evaluates expressions with known constant operands at compile time. | `t1 = 3 + 4` → `t1 = 7` |
| **Constant Propagation** | When a variable is assigned a constant, replaces subsequent uses with that constant. | `x = 5; y = x + 1` → `x = 5; y = 5 + 1` |
| **Dead Code Elimination** | Removes instructions whose results are never used, and unreachable code after unconditional jumps/returns. | Unused `t2 = a * b` removed if `t2` is never read |
| **Common Subexpression Elimination** | When the same expression is computed multiple times (with unchanged operands), reuses the first result. | `t1 = a + b; ... t3 = a + b` → `t1 = a + b; ... t3 = t1` |
| **Copy Propagation** | When `x = y` and `y` doesn't change, replaces uses of `x` with `y`, enabling further dead code elimination. | `t1 = t0; use(t1)` → `use(t0)` |
| **Strength Reduction** | Replaces expensive operations with cheaper equivalents. | `x * 2` → `x + x`; `x * 8` → `x << 3` |
| **Loop-Invariant Code Motion** | Moves computations that produce the same result on every iteration to before the loop. | `for(...) { t = a + b; ... }` → `t = a + b; for(...) { ... }` |

##### IR Analysis Utilities

The passes rely on shared analysis infrastructure:

| Analysis | Purpose |
|---|---|
| **CFG Builder** | Constructs a Control-Flow Graph from the linear IR instruction list. Basic blocks and edges between them. Required by most passes. |
| **Liveness Analysis** | Determines which variables are "live" (will be read later) at each point. Powers dead code elimination and future register allocation. |
| **Reaching Definitions** | For each point in the program, determines which definitions (assignments) could have produced each variable's current value. Powers constant propagation and CSE. |

##### Default Pass Ordering

The optimizer runs passes in this order (repeating until stable):

```
1. Constant Folding          ─┐
2. Constant Propagation       │  These feed each other:
3. Copy Propagation           │  folding creates constants,
4. Common Subexpr Elimination │  propagation enables more folding
5. Dead Code Elimination     ─┘
6. Strength Reduction
7. Loop-Invariant Code Motion
```

---

#### 4.7.3 `gen/link/` — Target-Specific Emission & Linking

Transforms the optimized IR into **target-specific machine code** and produces the final output (executable, library, or WASM module).

```cpp
class Linker {
public:
    Linker(const TargetInfo& target, ErrorReporter& reporter);

    /// Emit target code and link into final output.
    LinkResult link(const IRProgram& program, const std::string& outputPath);

private:
    std::unique_ptr<TargetBase> target_;
    ErrorReporter& reporter_;
};
```

**`TargetBase`** — Abstract base class for all target emitters:

```cpp
class TargetBase {
public:
    virtual ~TargetBase() = default;

    /// Emit target-specific code from IR.
    virtual std::vector<uint8_t> emit(const IRProgram& program) = 0;

    /// Get the file extension for this target's output.
    virtual std::string outputExtension() const = 0;

    /// Get the target's pointer size in bytes.
    virtual size_t pointerSize() const = 0;

    /// Get the target's endianness.
    virtual Endianness endianness() const = 0;
};
```

**Supported Targets:**

| Target spec | OS | Architecture | Object format | Status |
|---|---|---|---|---|
| `x86_64:elf64-x86_64-linux` / `-exec` | Linux | x86_64 | ELF (.o / executable) | ✅ shipped (LK1 + LK1 cycle 2 + LK6) |
| `x86_64:pe64-x86_64-windows` / `-exec` | Windows | x86_64 | PE/COFF (.obj / .exe) | ✅ shipped (LK2 + LK2 cycle 2 + LK6 cycle 2a) |
| `x86_64:macho64-x86_64-darwin` / `-exec` | macOS | x86_64 | Mach-O (.o / executable) | ✅ shipped (LK3 + LK3 cycle 2 + LK6 cycle 2c) |
| `arm64:*` | Linux / macOS / iOS / Android | ARM64 | ELF / Mach-O | ⏳ first non-x86_64 cycle anchored at D-LK6-1 (assembler ✅ ARM64 already shipped at AS3) |
| `*:wasm32-v1` | Web | WASM | WebAssembly (.wasm) | ⏳ skeleton walker shipped (LK8); section emitters land in plan 18 |
| `*:spirv-1.6` | GPU shader | SPIR-V | SPIR-V module (.spv) | ⏳ skeleton walker shipped (LK9); instruction stream lands in plan 17 |

**Historical note (2026-05-30):** the v1 sketch listed `TargetWindowsX86_64` and sibling C++ classes; that hand-rolled `src/gen/` was retired at LK10 cycle 2 (config-driven `*.target.json` + `*.format.json` walkers superseded it). The `TargetConfig`/`TargetBase` factory abstraction never shipped — `TargetSchema::loadShipped(name)` + `ObjectFormatSchema::loadShipped(name)` are the substrate, called directly by the driver.

---

## 5. Docker Configuration

### 5.1 `docker/Dockerfile`

Multi-stage image providing:
- **Build stage**: GCC/Clang, CMake, Ninja, nlohmann-json headers
- **Cross-compile stage**: Target toolchains (MinGW for Windows, Android NDK, Emscripten for WASM, osxcross for macOS)
- **Runtime stage**: Minimal image with just the compiler binary

### 5.2 `docker/toolchains/`

CMake toolchain files for each cross-compilation target. Each file sets:
- `CMAKE_SYSTEM_NAME` / `CMAKE_SYSTEM_PROCESSOR`
- Compiler paths (`CMAKE_C_COMPILER`, `CMAKE_CXX_COMPILER`)
- Sysroot paths
- ABI flags

---

## 6. Build System

**CMake 4.0+** (current latest-stable on project inception, 4.3.2 at time of writing). Build system structure:

- Root `CMakeLists.txt` sets **C++23** standard, hidden-by-default visibility, `enable_testing()`, and pulls deps via `FetchContent` (no system packages required on any platform).
- Each `src/` subdirectory has its own `CMakeLists.txt` producing an object library; the shared `dss-code-prime-lib` is composed from object targets.
- Libraries link: `core` ← `source-factory` ← `tokenizer` ← `analysis` ← `gen` (intermediate + optimizer + link) ← `program`
- `main.cpp` links `program` (which transitively pulls everything) into the `dss-code-prime` executable
- `tests/CMakeLists.txt` uses **GoogleTest** (fetched via `FetchContent`); `ctest` runs the suite.

---

## 7. Third-Party Dependencies

Engine is **standard-library-first**. Most needs are met by C++23 stdlib:
- `std::expected<T, E>` — fallible results (replaces a Result<T> polyfill)
- `std::format` — string formatting (replaces fmt dependency)
- `std::filesystem` — cross-platform paths
- `std::span`, `std::optional`, `std::ranges` — non-owning views, nullable values, traversal

External dependencies are kept to two, both header-mostly and pulled via `FetchContent`:

| Library | Purpose | License |
|---|---|---|
| [nlohmann/json](https://github.com/nlohmann/json) | JSON parsing for language configs (used **only** by `grammar_schema_json.cpp`) | MIT |
| [GoogleTest](https://github.com/google/googletest) | Unit testing framework (only when `DSS_BUILD_TESTS=ON`) | BSD-3 |

Cross-platform compiler matrix: **MSVC 17.5+, GCC 13+, Clang 16+** (the LTS-grade releases that ship full C++23 support). Build verified via WSL or Docker on Linux/macOS in parallel with native Windows builds.

---

## 8. Implementation Phases (Todos)

> **⚠ HISTORICAL SNAPSHOT (frozen ~2026-05-30).** These phase rows are an as-built ledger and several now lag §0; the **§0.1 "Stepper" roadmap (above) is the authoritative current view** of what is done/next — consult it, not this table.

> **v1 acceptance criteria per remaining phase.** Each `⏳`/`🔵`/`🟦` row below now carries an explicit "v1 must deliver" line so we don't drift on scope. The 127-gap catalog in [`07-production-readiness-plan - tbd.md`](./07-production-readiness-plan - tbd.md) is the canonical detail; this table is the per-phase summary.

| # | Status | ID | Title | Dependencies | Description |
|---|---|---|---|---|---|
| 1  | ✅ mostly done (Docker still pending) | `scaffold-project` | Scaffold project & build system | — | Create CMakeLists.txt files, directory structure, Docker setup. Current: CMake 4.0 floor, C++23, hidden visibility, shared lib + exe wired, integrated test target alive. **Pending:** Dockerfile + toolchains. |
| 2  | ✅ done (12/12 of sub-plan complete) | `core-types` | Implement core types | scaffold-project | **See [01-tree-node-model-plan - ok.md](./01-tree-node-model-plan - ok.md) — checkpoint snapshot at top.** **Done:** T0 build deps, T1 source primitives + strong IDs + interners (transparent heterogeneous lookup), T2 Tree storage + Node + NodeFlags, T3 ParseDiagnostic + DiagnosticReporter + DiagnosticPolicy (FNV-1a64 dedup with ruleContext), T4 GrammarSchema + SchemaCursor + ScopeKind + JSON loader + `toy.lang.json`, T5 schema-aware `TreeBuilder` with RAII `OpenScope`, cascade-cookie tracking, release-mode invariant guards, recovery + EOF synthesis, T6 `TreeCursor` with CST/AST modes, opaque Bookmark with TreeId guard, cycle-capped depth/parent walks, convenience forwarders, T7 `tree_visitor.hpp` header-only `walkPreOrder`/`walkPostOrder` with `WalkAction` skip/stop control and subtree-bounded traversal, verified zero allocations on the 10K-node walk via a global `operator new` counter, T8 `NodeAttribute<T>` header-only side-table with sparse↔dense auto-promotion at 50% coverage / 16-node floor, Tree-bound with nodeCount bounds checks (cross-tree guard is bounds-based, not full membership — documented caveat in §5.10), mutable + const accessors, forward iterator over both backings with internal-gap skipping, custom move ops that leave the source observably empty, T9 typed views (`tree_views.hpp` + `well_known_names.hpp` — **retired in 08.55** per thesis decision #4; engine reads structure config-driven via `semantics`/`hirLowering` blocks): seven header-only views (IdentifierView, LiteralView with cached Kind enum, BinaryExprView, BlockView, FunctionDeclView, VarDeclView, ExprStmtView), each with unchecked ctor + `::from()` factory returning `std::optional`, EmptySpace-skipping structural accessors via internal `nthVisibleChild` helper, trivially-copyable POD layout; new `Tree::hasSchema()` / `Tree::hasDiagnostics()` probes let token-level `from()` return nullopt cleanly on schema-less trees, T10 end-to-end integration test (`test_tree_end_to_end.cpp`) ties the full stack together: shipped toy.lang.json loaded from disk → SourceBuffer → TreeBuilder driven by a sequential TokenSeq helper → Tree → walkPreOrder AST traversal → indented `rule:`/`tok:` pretty-printer → string-equality assertion + diagnostic-code assertion, with 3 happy paths (varDecl, exprStmt, multi-statement), 1 T9-views-resolve test against the real parse, and 5 broken-path recovery flavors (unknown token with Error-leaf walk, unclosed scopes at EOF, truncated after keyword, explicit `pushError`, scope-stack underflow via `}`), T11 CMake wireup audit (zero orphan files; DSS_EXPORT properly applied), T12 onboarding docs (`docs/tree-model.md` + `docs/language-config-spec.md` with cookbook-pin test) (**278 test cases, 100% pass**). **Sub-plan complete.** Next-tier work: parser layer (parent plan phase #7) and schema-expressiveness-v2 (precedence, contextual keywords, etc.). The obsolete `core/error/` directory is dropped (subsumed by `DiagnosticReporter`). |
| 3  | ✅ subsumed into #2 (T4) | `source-config-schema` | Design language config schema | scaffold-project | The JSON schema is defined inside `grammar_schema_json.cpp`; `toy.lang.json` is the reference. Shipped languages (`csharp.lang.json` etc.) are authored later in `languages-onboarding-plan.md`. |
| 4  | ⏳ pending — depends on #2 | `source-factory` | Implement source factory | core-types | **Now a thin facade** that resolves a language name → config-file path and calls `GrammarSchema::loadFromFile`. Note: `GrammarSchema::loadShipped` already does both for the engine; this phase is the public API thin wrapper. `models/` and `validators/` directories are dropped. |
| 4.5 | ✅ done — sub-plan [`03-substrate-hardening-plan - ok.md`](./03-substrate-hardening-plan - ok.md) | `substrate-hardening` | SH1 landing-log generator + SH2 Linux CI matrix + SH3 cross-tree `NodeId` guard + SH4 v2 follow-ups | core-types, schema-v2 | De-risked the substrate before the tokenizer/parser/semantic phases land on top of it. SH1 stops landing-log drift (recurring bug class through v2). SH2 verified the cross-platform claim via the existing `DSS.DevOps@v2` multi-OS matrix (Linux/GCC, Linux/Clang+ASan, Windows/MSVC, macOS/AppleClang). SH3 closed the cross-tree `NodeId` caveat (`NodeId.treeTag` + tag validation in `NodeAttribute<T>` / `Tree::node_`). SH4 bundled three v2 follow-ups (CI hygiene wiring of SH1's tool; shape-based `switch`/`case`/`default` adoption in c-subset; multi-level AltChoice nesting stress test). 7 commits + 1 merge (PR #3); 531 cases / 26 suites, 100% pass. **Unblocks** phase #5. |
| 5  | ✅ done — sub-plan [`04-tokenizer-plan - ok.md`](./04-tokenizer-plan - ok.md) | `tokenizer` | Implement tokenizer | core-types, source-factory, substrate-hardening | TZ1 (bare tokenizer + toy E2E flip) + TZ2 (lexer modes + strings + comments) + TZ3 (c-subset + tsql-subset E2E flip + body-mode pins) + TZ3 review-fix round 1 (13 items addressed; new loader pass `C_BodyDefaultKindInShape`, `makeSyntheticMeaning` helper + synthesis-allowlist assertion, `E2EHarness` fail-loud + clean-diags dtor assertion, 7 new unit pins). Live `Tokenizer`-driven `TokenStream` drives every E2E test. TZ3 surfaced and closed three real semantic gaps in the builder/tokenizer: built-in-kind trust when per-lexeme table has no entry, body-mode cursor-advance skip, and multi-char operator wins over id-runs (T-SQL `N'`-style). Closes `v2-gap-catalog - tbd.md` row 3 end-to-end. 625 cases / 29 suites, 100% pass. |
| 6  | ✅ subsumed into #5 (see `04-tokenizer-plan - ok.md` §2.7) | `analysis-lexical` | (subsumed) | tokenizer | The v2 schema-aware `TreeBuilder` already does the validation phase 6 was envisioned for (`scopeRequire` filtering, `expectedSet`-driven contextual demotion, `P_SchemaCursorDesync`). The remaining lexical concerns (escape-sequence integrity, numeric ranges, comment / string body handling) live inside `src/tokenizer/` per `04-tokenizer-plan - ok.md`. |
| 7  | ✅ **CLOSED — all 7 PRs done** — sub-plan [`05-parser-plan - ok.md`](./05-parser-plan - ok.md) | `analysis-syntactic` | Implement syntactic analysis | analysis-lexical | ✅ `SchemaWalker` substrate extraction (PA0); ✅ schema-driven RD driver with try-each-branch speculation (PA1); ✅ Pratt walker for operator precedence (PA2, closes v2-gap-catalog row 1); ✅ panic-mode recovery + `followSetOf` + clang-style diagnostic rendering (PA3, closes G-115 end-to-end); ✅ real-world corpus stress + grouped-postfix + off-grammar body drain (PA4); ✅ LSP server skeleton with diagnostics over stdio JSON-RPC + UTF-16 position encoding + monotonic-generation stale-suppression (PA5a); ✅ LSP semantic-stub method handlers for hover/completion/definition/references/rename/signatureHelp (PA5b). PA-Walker-LeftRec added `wrapLastChildInFrame` substrate primitive; PA5a-prep closed v2-gap-catalog rows 12-declarator/16/29. Hand-driven `tests/core/test_*_subset.cpp` flipped to `Parser::parse()`; `tests/corpus/{toy,c-subset,tsql-subset}/` programs parse cleanly with golden trees; LSP server publishes diagnostics in real editor sessions (47 ctest suites green at parser-phase close; now 56/56 post-08.55). |
| 7.5 | 🟢 **CU1 + CU2 done; CU3–CU4 pending** — sub-plan [`08-compilation-unit-plan - tbd.md`](./08-compilation-unit-plan - tbd.md) | `compilation-unit-model` | Multi-file translation units + import resolution | analysis-syntactic | Bridges parser (per-file `Tree`) and semantic (cross-file symbol table). Six PRs (CU1–CU6; CU5 multi-language added rev 2; CU6 cross-CU references paired with [`14-linker-plan`](./14-linker-plan%20-%20tbd.md) LK11 per its §2.12). **CU1 ✅** (bare type) + **CU2 ✅** (`addFile`/`addInMemory`, `D_*` codes, lexer+parser diagnostics unified in the Tree — cross-plan into 01/05); 48/48 suite. **G-110 ✅ resolved** (G-111 = CU4). **v1:** CU1–CU4. **v1.1:** CU5. **v1.x:** CU6 ↔ LK11; trigger = first artifact profile needing multiple CUs in one image. |
| 7.6 | ⏳ **new (rev 2)** — sub-plan [`08.5-substrate-prep-plan`](./08.5-substrate-prep-plan%20-%20ok.md) | `substrate-prep` | Generalize arena + ship type lattice | compilation-unit-model | SP1 generalize `Tree`'s arena + `NodeAttribute<T>` so HIR/MIR/LIR reuse the same substrate. SP2 ship core type lattice + per-language extension registry. Substrate-tier (5-agent review). **v1 must deliver:** zero behavior delta on existing Tree code; the lattice is in place before semantic phase opens. |
| 8  | ⏳ pending | `analysis-semantic` | Implement semantic analysis | substrate-prep | Symbol table + scope resolution + type checking + const-correctness + typedef resolution. **Consumes `CompilationUnit`** (from phase 7.5) — symbol table spans every Tree in the CU + cross-file refs are pre-resolved. Populates `NodeAttribute<TypeId>` / `NodeAttribute<SymbolId>` over each CST **against the lattice from 7.6**. New `S_*` diagnostic namespace. Production-readiness §2 (G-201..G-212). |
| 8.5 | ✅ **COMPLETE (rev 2 — HR1–HR11 done)** — sub-plan [`09-hir-plan`](./09-hir-plan%20-%20ok.md) | `hir` | High-level IR | analysis-semantic | The pivot layer. Language-neutral, structured, typed HIR with FFI / shader / transpile attribute side-tables; CST→HIR lowering per shipped language. Eleven PRs (HR1–HR11). HR1 ✅ 2026-05-26 (`feature/hir-1` commit `406d5c7` — arena + node shapes + walker + ids + extension registry) + HR2 ✅ 2026-05-26 (typed expressions + operator registry + verifier) + HR3 ✅ 2026-05-26 (structured CF + break/continue & per-kind-arity verifier rules) + HR4 ✅ 2026-05-26 (declarations + extern surface + FfiMetadata side-table) + HR5 ✅ 2026-05-26 (attribute system + side-tables + `hir_attrs.hpp` catalog; verifier emits real diagnostic spans via optional `HirSourceMap`) + HR6 ✅ 2026-05-27 (full verifier — block dead-code / return completeness / Call-arg-vs-FnSig / intrinsic-registered / shader-restriction subverifier; `HirIntrinsicRegistry`; optional `TypeInterner` injection) + HR7 ✅ 2026-05-27 (round-trippable `.dsshir` text format — `emitHir`/`parseHir`, inline structural types, verify-on-load) + HR8 ✅ 2026-05-27 (config-driven CST→HIR lowering engine + `hirLowering` schema facet, proven on c-subset; per-expression type inference + literal-value pool; verify-on-load) + HR9 ✅ 2026-05-27..28 (`toy` enriched into a typed language + generic lowering test; arrays un-deferred end-to-end via a config-driven declarator-suffix descriptor + semantic-time constant-length eval; + gap-closure: char/string VALUES, SeqExpr, pointers, ternary) + HR10 ✅ 2026-05-27..28 (tsql-subset lowering — role-explicit SQL extension nodes via a generic `childGathering` config vocabulary + `ChildLower` enum, flat-expr lowering, coalesced/doubled-delimiter strings, `NULL`/relational-name extensions, `ReferenceRule.hardParents`; same language-agnostic engine); **HR11 ✅ done 2026-05-28. Plan 09 (HIR) COMPLETE.** **Single largest substrate addition in v1.** Blocks every downstream sink (transpile / MIR / SPIR-V / WASM). |
| 9  | ✅ **COMPLETE (ML1–ML8 done 2026-05-27..29)** — sub-plan [`12-mir-lir-plan`](./12-mir-lir-plan%20-%20ok.md) | `mir-lir` | Mid-level + low-level IR | hir | MIR (SSA over CFG + structured-CF markers; replaces single-IR G-301) and LIR (per-target ISA). Eight PRs (ML1–ML8). **All eight closed end-to-end** on `feature/mir-lir`: ML1 MIR skeleton + ML2 HIR→MIR (6 cycles incl. Cast emission) + ML3 MirVerifier (7 rule families, `I_*` 0xA00x) + ML4 `.dssir` text round-trip; ML5 JSON-configured targets + register-file + calling conventions + MIR→LIR isel cycle 3 series + `LirVerifier`; ML6 liveness + linear-scan + variant migration + R_* family + rewrite pass + `verifyLirPostRegalloc`; ML7 cycle 1 callconv + stack frame (`5362766`); ML8 cycle 1 `.dsslir` EMITTER (`040d496`) + cycle 2 PARSER + verify-on-load + round-trip + schema version (`9622b38`). Remaining open: ML7 cycle 2 (ARM64 stackPointer + ABI goldens) — anchored as legitimate next-cycle work. 100/100 ctest. |
| 9.5 | ⏳ **new (rev 2)** — sub-plan [`11-ffi-plan`](./11-ffi-plan%20-%20tbd.md) | `ffi` | FFI binary readers + extern ingestion | hir | Hermetic ELF / PE / Mach-O / ar readers; C-header mode parser; ABI catalog; name mangling; HIR `ExternFunction`/`ExternGlobal` population. Six v1 PRs (FF1–FF6). **v1-blocking** — without it, no libc, no useful binary. |
| 10.5 | ✅ **AS1–AS6 closed 2026-05-29** — sub-plan [`13-assembler-plan`](./13-assembler-plan%20-%20tbd.md) | `assembler` | In-tree machine-code encoder | mir-lir | x86_64 + ARM64 encoding via shape-keyed walkers (`x86-variable` + `fixed32`); per-(arch×format) relocation taxonomy unifier (target-side at plan 13 §2.6 + format-side at plan 14 LK4); round-trip oracle disassembler (`disasm.{hpp,cpp}` + per-shape inverse walkers); SourceMapEntry stamping at dispatch level. Six PRs (AS1–AS6) all landed on `feature/as-1`. |
| 11 | ⏳ **in flight** — sub-plan [`14-linker-plan`](./14-linker-plan%20-%20tbd.md) | `linker` | In-tree linker + JSON-configured object formats | assembler, ffi, **artifactProfile** | **Hermetic** (replaces system-linker integration). **Config-driven object formats** (rev 3): `src/dss-config/object-formats/*.format.json` for ELF / PE / Mach-O / WASM / SPIR-V + ONE format-blind engine + TLS lowering + dynamic imports. **LK4 substrate + LK1–LK3 writers + LK1 ET_EXEC + LK6 c1 reloc-apply ✅ landed 2026-05-29..30** (`object_format_schema.*` + `linker.*` + per-format `elf`/`pe`/`macho` walkers; `.o`/`.obj` + executable images byte-validated in-memory; `K_*` diag at 0x8xxx). Ten PRs (LK1–LK10); remaining LK5 (TLS) / LK6 c2 (dynamic+extern) / LK7 (codesign) / **LK10 (hermetic on-disk file)** ⏳; LK8 WASM-skeleton + LK9 SPIR-V-skeleton for v1.x. |
| 11.4 | 🟩 **OPT1–OPT6 ✅ closed 2026-06-03 — sub-plan [`22-optimizer-plan`](./22-optimizer-plan%20-%20tbd.md)** | `gen-optimizer` | Multi-tier config-driven optimizer (HIR-transpile / MIR / LIR / per-target) — **source/target/linker-agnostic** | mir-lir + LK10 file-emission ✅ + step 13.5 corpus ✅ | **v1 ✅ + v1.x mandatory ✅** — OPT1 substrate + OPT2 ConstFold + OPT3 DCE + OPT4 Mem2Reg/CopyProp + OPT5 CSE + OPT5+ SimplifyCFG (branch-fold + jump-thread + block-merge + marker re-derivation single-non-Linear case) + OPT6 LICM + pipeline-level fixed-point loop + `passMutationCount` effectiveness substrate. Release pipeline `[Identity, ConstFold, Mem2Reg, CopyProp, Cse, Licm, SimplifyCfg, Dce]` with `maxIterations: 4` shipped. **Remaining v1.x → v2**: alias analysis + value-range substrate, OPT7 inlining, OPT8 LIR peephole/coalesce, OPT9 vectorization, OPT10 autotuner — all anchored in §3.1 with explicit triggers. `X_*` diag at 0x2xxx. Heuristics-as-data discipline kept throughout. Production-readiness §4. |
| 11.5 | ⏳ **new (rev 2)** — sub-plan [`15-debug-info-plan`](./15-debug-info-plan%20-%20tbd.md) | `debug-info` | DWARF + PDB + CFI / SEH | linker, mir-lir | DWARF 5 writer (ELF + Mach-O), PDB writer (PE), Win64 SEH, Mach-O compact-unwind, source-position chain through HIR/MIR/LIR/bytes. Twelve PRs (DB1–DB12). |
| 11.6 | ⏳ **new (rev 2)** — sub-plan [`16-codesign-publish-plan`](./16-codesign-publish-plan%20-%20tbd.md) | `codesign-publish` | Hermetic signing + notarization | linker, debug-info | Apple Mach-O codesign + notarization stapling, iOS provisioning, Authenticode + RFC 3161, APK v3 (skeleton). In-tree crypto via vendored BearSSL. Nine PRs (CS1–CS9). Apple-host-free. |
| 12 | 🟦 skeleton only | `program-api` | Implement program API & driver | core-types, linker, **artifactProfile** | **v1 must deliver:** `dss-code-prime build my-project.dss-project.json` CLI entry point; `.dss-project.json` loader; driver enforces `project.artifactProfile ∈ language.artifactProfiles` (`D_*` namespace); `CompilationContext` carries resolved profile through every phase; diagnostic-rendering layer produces positioned source-context output (`file:line:col` + caret — DSS's own renderer, NO clang/LLVM; Part A wired 2026-06-09); deterministic byte-identical output. Production-readiness §6 (G-601..G-609). |
| 12.5 | 🔵 sub-plan [`06-artifact-profile-plan`](./06-artifact-profile-plan%20-%20tbd.md) | `artifact-profile` | `artifactProfile` mechanism end-to-end (registered-set vocab — rev 2) | core-types | Language config declares supported profiles (now registered-set, not compile-time enum: `cli`/`gui`/`lib`/`staticlib`/`script`/`sproc`/`transpile`/`shader`/`hdl`); project config picks one; driver enforces; codegen reads. Four PRs (AP1–AP4). Gates phase #11 acceptance. |
| 13 | ⏳ pending — sub-plan [`18-wasm-plan`](./18-wasm-plan%20-%20tbd.md) | `wasm-backend` | WASM backend | hir, mir-lir, linker | HIR/MIR → WASM bytecode using structured-CF markers (no Relooper). WASM as "LIR-equivalent." Ten PRs (WA1–WA10). v1.x. Browser + wasmtime as runtime targets via FFI. |
| 13.5 | ⏳ **new (rev 2)** — sub-plan [`17-shader-gpu-plan`](./17-shader-gpu-plan%20-%20tbd.md) | `shader-gpu` | SPIR-V codegen + shader HIR + GPU bindings | hir, mir-lir | Same-source CPU+GPU functions via dual-lowering. Ten PRs (SG1–SG10). v1.x — lit up for the custom language. |
| 13.6 | ⏳ **new (rev 2)** — sub-plan [`10-source-translation-plan`](./10-source-translation-plan%20-%20tbd.md) | `transpile` | Source-to-source translation via HIR pivot | hir | Language-pair `.map.json` + HIR→HIR walker + target-CST builder + pretty-printer. Six PRs (ST1–ST6). v1.x. Promotes the former §9 long-running note. |
| 13.7 | 🔒 **reserved (rev 2)** — sub-plan [`19-hir-hw-reserved-plan`](./19-hir-hw-reserved-plan%20-%20tbd.md) | `hir-hw` | Hardware IR (VHDL / Verilog / SystemVerilog) | hir, substrate-prep | Sibling-of-HIR IR for concurrent + signal-typed + clock-bound hardware. **No work until trigger.** Plan exists to prevent software HIR from foreclosing hardware semantics. |
| 13.8 | 🔒 **reserved (rev 2)** — sub-plan [`20-custom-language-reserved-plan`](./20-custom-language-reserved-plan%20-%20tbd.md) | `custom-language` | User's eventual custom language | all v1 + shader + WASM | The engine already supports it via the schema mechanism; this plan owns language vocabulary + standard library + runtime decisions (the latter via 21). **No new core work needed** — validates substrate. |
| 13.9 | 🔒 **reserved (rev 2)** — sub-plan [`21-runtime-reserved-plan`](./21-runtime-reserved-plan%20-%20tbd.md) | `runtime` | Language runtime (GC / exceptions / coroutines / threading) | hir, mir-lir | Triggered module-by-module: GC when a GC'd language onboards; unwinder when a throwing language onboards; coroutines when async-shaped language onboards. Distinct from OS-supplied runtime libs (those are FFI targets). |
| 14 | 🟦 in progress (core test suite live) | `testing` | Comprehensive test suite | all above | **Current:** 29 ctest suites / 625 individual cases over `core/types/` + tokenizer + the three shipped `.lang.json` configs — all green on Linux/GCC + Linux/Clang+ASan + Windows/MSVC + macOS/AppleClang. **v1 must add:** real-world `tests/corpus/` programs per language (parser-plan PA4 prereq); "compile then run" CI tests on every runner; ARM64 CI legs (G-704..G-706); fuzzers (G-710). Production-readiness §7.3 (G-730..G-734). |
| 15 | 🟦 partial (CI/CD pipelines done — Docker not) | `docker-setup` | Docker & CI setup | scaffold-project | **Done:** `cpp-app-pr.yml` / `cpp-app-pkg.yml` / `cpp-app-deploy.yml` reusable workflows in [DSS.DevOps](../../DSS.DevOps/.github/workflows/) + consumer workflows in `dss-code-prime/.github/workflows/`. **v1 must add:** ARM64 runners for Linux + Windows + macOS (G-704..G-706); fuzzing nightly (G-710); extended sanitizer coverage (G-709). Cross-compile toolchains: out of v1. |

---

## 9. Open Questions & Notes

> **For the full v1 open-question list see [`07-production-readiness-plan - tbd.md`](./07-production-readiness-plan - tbd.md) §1–§7.** The entries below are the long-running design notes; the open *decisions* needed before each remaining phase ships are tracked in each sub-plan's §4 / open-questions section.

### Resolved

- ~~**Grammar format**: BNF chosen for readability; PEG is an alternative if ambiguity becomes an issue.~~ **Resolved** by [01-tree-node-model-plan - ok.md §5.12](./01-tree-node-model-plan - ok.md).
- ~~**Error recovery**: Parser will implement panic-mode recovery.~~ **Resolved** by [01-tree-node-model-plan - ok.md §5.15](./01-tree-node-model-plan - ok.md) + parser-plan PA3.
- ~~**Cross-platform scope.**~~ **Resolved 2026-05-21:** v1 = **{Windows, Linux, macOS} × {x86_64, ARM64}**. v1.x adds WASM + iOS + Android.
- ~~**v1 language scope.**~~ **Resolved 2026-05-21:** v1 = `toy` + `c-subset` + `tsql-subset`. Full C99 / C++ / C# / SQLite onboarding post-v1.
- ~~**Output target distinction.**~~ **Resolved 2026-05-21 + amended rev 2 2026-05-23:** `artifactProfile` mechanism with a **registered-set** vocabulary (no more compile-time enum). Adds `transpile`/`shader`/`hdl` to v1's `cli`/`gui`/`lib`/`staticlib`/`script`/`sproc`. See [`06-artifact-profile-plan`](./06-artifact-profile-plan%20-%20tbd.md) §3 rewrite.
- ~~**G-301 IR design** (was: "default SSA over CFG").~~ **Resolved rev 2 2026-05-23:** **three IR layers** — HIR ([`09-hir-plan`](./09-hir-plan%20-%20ok.md)) → MIR (SSA over CFG with structured-CF markers; [`12-mir-lir-plan`](./12-mir-lir-plan%20-%20ok.md)) → LIR (per-target ISA; same plan). Single IR cannot serve both transpilation and binary codegen cleanly.
- ~~**Linker strategy** (was: "system linker for v1, built-in post-v1").~~ **Resolved rev 2 2026-05-23:** **in-tree linker from day one** ([`14-linker-plan`](./14-linker-plan%20-%20tbd.md)). The hermetic-compiler invariant rules out shelling to `ld`/`link.exe`/`ld64`/`lld`/`wasm-ld`. Side-effect: Apple-host-free local dev.
- ~~**Type system** (was: "TypeKind enum + TypeId interner").~~ **Resolved rev 2 2026-05-23:** **core lattice + per-language extensions** ([`08.5-substrate-prep-plan`](./08.5-substrate-prep-plan%20-%20ok.md) §2.2). Each `.lang.json` registers extension type-kinds via `typeExtensions[]`.
- ~~**Source-to-target language translation.**~~ **Resolved rev 2 2026-05-23:** promoted out of "long-running" into [`10-source-translation-plan`](./10-source-translation-plan%20-%20tbd.md). HIR is the pivot; language-pair `.map.json` declares HIR-to-HIR mappings; target-CST construction reuses each target language's schema; pretty-printer applies the target's lexical conventions.
- ~~**Apple host requirement.**~~ **Resolved rev 2 2026-05-23:** **never required for local dev.** In-tree Mach-O writer + in-tree codesign + in-tree notarization HTTP client (with the Apple cert as a credential, not a tool). See [`14-linker-plan`](./14-linker-plan%20-%20tbd.md) LK3 + [`16-codesign-publish-plan`](./16-codesign-publish-plan%20-%20tbd.md).
- ~~**FFI / precompiled library reading.**~~ **Resolved rev 2 2026-05-23:** dedicated sub-plan [`11-ffi-plan`](./11-ffi-plan%20-%20tbd.md). In-tree binary readers + C-header mode parser + ABI catalog + name mangling. v1-blocking (without it, no libc → no useful binary).

### Still open — decide before the phase ships

- **G-201: Symbol table design.** Stack-of-hashmaps + `SymbolId` interner CU-scoped is the default. Decide before phase #8 PR1.
- **G-203: Type lattice extension surface.** Each `.lang.json` declares its `typeExtensions[]` (per `08.5-substrate-prep-plan` SP2). The lattice shape is decided; the per-shipped-language extension lists are not. Decide alongside phase #8.
- **G-114: Recovery quality benchmark.** Clang-level? tsc-level? Pin with stakeholders.
- **G-762: Milestone calendar.** Substantially expanded after rev 2. Substrate-prep + HIR + MIR/LIR + assembler + linker + debug-info + codesign + FFI all v1-blocking. Default (negotiable): parser closed Q2 ✅; CU + substrate-prep Q3; semantic Q3-Q4; HIR + FFI Q4; MIR/LIR + assembler + linker Q1 2027; debug-info + codesign + first end-to-end signed binary Q2 2027.
- **G-760: PR review tiering.** 5-agent review for substrate; 2-agent (code + tests) for feature; skip for docs.

### Long-running design notes

- **Standard library**: Owned by [`21-runtime-reserved-plan`](./21-runtime-reserved-plan%20-%20tbd.md) when triggered.
- **LLVM backend**: **Permanently out of scope** post rev 2. The hermetic-compiler invariant rules out LLVM as a runtime dependency. We own the entire pipeline.
- **Incremental compilation**: Not in v1 scope. Full recompilation per invocation. The arena + immutability design *permits* it later.
- **Register allocation**: Linear-scan for v1 (`12-mir-lir-plan` ML6); graph-coloring reserved post-v1.x.
- **Bootstrap**: The compiler is C++23 today. When the user's custom language (`20-custom-language-reserved-plan`) matures, a self-hosted rewrite is on the table — not committed.

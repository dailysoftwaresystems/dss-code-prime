# In-tree Linker — Sub-Plan (14)

> Owns the **object-format engine** (one language-blind C++ engine that reads JSON-configured format schemas) and the **linker engine** (symbol resolution, relocation application, section layout, per-platform metadata). Consumes (bytes, relocations, symbols) from the [in-tree assembler](./13-assembler-plan%20-%20tbd.md); produces final-form binaries.
>
> Per the user's mandate ("we will be the process from source to targets"), **no `ld` / `link.exe` / `ld64` / `lld` / `wasm-ld` invocation**. This is the single largest backend chunk of v1.
>
> **Universal-compiler thesis extension (rev 3 — 2026-05-29).** Object formats are **100% config-driven** — mirroring [`GrammarSchema`](./05-parser-plan%20-%20ok.md) on the frontend and [`TargetSchema`](./12-mir-lir-plan%20-%20ok.md) on the backend ISA tier. Each object format (ELF, PE/COFF, Mach-O, WASM, SPIR-V) is a JSON file in `src/dss-config/object-formats/<name>.format.json` declaring section taxonomy, symbol-table layout, relocation kinds, per-format metadata sections, and (where applicable) dynamic-linking constructs. ONE language-blind C++ engine reads the JSON to emit bytes — there is NO per-format C++ writer. Adding a new object format = drop a new JSON file, no engine edits. Honors thesis decision #4 ("config-driven everything") end-to-end.

## 0. Status (snapshot)

| | |
|---|---|
| Status        | ✅ **LK1–LK10 CLOSED + RUNNABLE 2026-06-02** on `feature/lk-111`. Substrate: `ObjectFormatSchema{Unknown=0,Elf,Pe,MachO,Wasm,Spirv}` + format-blind `dss::linker::link(AssembledModule, TargetSchema, ObjectFormatSchema, reporter) → LinkedImage` engine + cross-side reloc-taxonomy unifier + 11 `K_*` codes at 0x8xxx + 5 `D_*` driver-tier codes at 0xD007–0xD00B. Per-format walkers: ELF (.o + ET_EXEC + ET_DYN substrate with GOT/PLT + LC_DYLD_INFO_ONLY equivalents), PE32+ (.obj + .exe + .idata IAT — proven runtime with 2-DLL imports via hello_puts), Mach-O (MH_OBJECT + MH_EXECUTE + LC_DYLD_INFO_ONLY), WASM (skeleton preamble — plan 18 owns section emission), SPIR-V (skeleton header — plan 17 owns instruction stream). File emission via `dss::linker::writeImage(LinkedImage, fs::path, reporter)`. Driver pipeline wired via `Program::compileFiles` / `compileDirectory` (HIR→MIR→LIR→regalloc→callconv→ASM→link→write). ✅ **D-LK10-ENTRY Stage 1 CLOSED 2026-06-02** (entry trampoline + process exit + Windows exit-42 smoke + hello_puts runtime print). ✅ **D-LK10-ENTRY-ML7-FRAME-BIAS-UNIFY CLOSED 2026-06-02** (Win64 shadow-space + call-push alignment bias in non-leaf functions). **Remaining v1 deferrals**: cross-host runtime parity (Linux/macOS/ARM64) gated on D-LK10-ENTRY-ARM64 + FF1-PE/FF1-MachO binary-reader triggers + D-LK10-ENTRY-MACHO-EXIT (POSIX run-harness already landed); LK11 cross-CU linking (v1.x, paired CU6). **Plan rev 3 (2026-05-29): object formats are JSON-configured** — one engine, no per-format C++. |
| Predecessors  | ✅ [`13-assembler-plan`](./13-assembler-plan%20-%20tbd.md) (bytes + relocations) — AS1–AS6 closed 2026-05-29. ✅ (satisfied for the shipped path) [`11-ffi-plan`](./11-ffi-plan%20-%20tbd.md) — extern symbol declarations flow via inline externs + neutral shipped-lib descriptors (FF5/FF6/FF11 + Model 3) and LK6 consumes them (PE IAT / ELF GOT+PLT / Mach-O stubs runtime-proven); the FF1 precompiled-BINARY readers (PE/Mach-O) remain anchored/deferred. |
| Successors    | ⏳ [`16-codesign-publish-plan`](./16-codesign-publish-plan%20-%20tbd.md) (LC_CODE_SIGNATURE / PE security directory placeholders filled post-link). ⏳ [`15-debug-info-plan`](./15-debug-info-plan%20-%20tbd.md) (debug sections placed alongside code). |
| Scope         | **Bounded.** v1: LK1–LK10. v1 acceptance: link c-subset corpus → ELF / PE / Mach-O on every {OS × arch}. WASM (LK8) + SPIR-V (LK9) post-v1 skeletons. **v1.x: LK11** — cross-CU linking (couples with [`08-compilation-unit-plan`](./08-compilation-unit-plan%20-%20tbd.md) CU6). |

---

## 1. Motivation

Hermetic per [`00-master`](./00-compiler-implementation-plan%20-%20tbd.md) §1.1 — no `ld` / `link.exe` / `ld64` invocation. Four payoffs:

1. **Apple-host-free local dev.** Mach-O emission via the in-tree config-driven engine eliminates the `osxcross`/`xcrun` dependency.
2. **WASM + SPIR-V unification.** Same plumbing covers native, browser, GPU.
3. **Reproducibility.** Deterministic build-id; no embedded timestamps/paths/version-strings.
4. **Config-driven object formats.** Every supported format (ELF / PE / Mach-O / WASM / SPIR-V) is a JSON file, not C++. The engine is target-blind + format-blind; format-specific knowledge lives in `*.format.json` schemas validated at load time. Same thesis decision #4 discipline that makes the frontend onboard a new source language via one JSON file (`GrammarSchema`) and the backend ISA tier onboard a new processor via one JSON file (`TargetSchema`).

---

## 2. Design

### 2.0 Config-driven object formats (rev 3)

Mirroring `src/dss-config/sources/*.lang.json` (frontend) and `src/dss-config/targets/*.target.json` (backend ISA), each object format is one JSON file under `src/dss-config/object-formats/`:

- `elf.format.json` — ELF64 little-endian, section taxonomy (`.text`/`.data`/`.rodata`/`.bss`/`.debug_*`/…), program-header types (PT_LOAD/PT_DYNAMIC/PT_INTERP/PT_GNU_STACK/PT_GNU_RELRO), dynamic-section entries (DT_NEEDED/DT_HASH/DT_GNU_HASH/DT_RELA/…), relocation kinds (R_X86_64_64/R_AARCH64_ABS64/…), build-id placement (`.note.gnu.build-id`).
- `pe.format.json` — Section table (file/virtual alignment), import table (`.idata` + IAT), unwind info (`.pdata` + `.xdata`), exception/debug directory entries, signing window (`IMAGE_DIRECTORY_ENTRY_SECURITY` placeholder).
- `macho.format.json` — Segments (file/virtual alignment), load commands (LC_SEGMENT_64, LC_DYLD_INFO, LC_DYSYMTAB, LC_FUNCTION_STARTS, LC_UUID, LC_CODE_SIGNATURE placeholder), chained fixups (Apple Silicon), bind/lazy/rebase opcode tables.
- `wasm.format.json` — Module sections (type/import/function/table/memory/global/export/start/elem/code/data), name section convention, custom section taxonomy.
- `spirv.format.json` — Module header (magic/version/generator/bound/schema), section ordering convention (capability/extension/extinstimport/memorymodel/entrypoint/executionmode/...), debug info layout.

The schema vocabulary is universal: every format declares its `sections[]` (kind + flags + alignment), its `symbolTable` (entry layout, kinds, binding/visibility encodings), its `relocations[]` (kind name → operator + bit-width + signed-ness + applies-to-section-kind), its `metadataSections[]` (build-id / signing window / debug-id placement), and its `imports`/`exports` model (for formats that have one). The ENGINE is one set of headers/cpp files under `src/link/` reading `ObjectFormatSchema`.

**The bucket-1 vs bucket-2 split** (per [`00-master`](./00-compiler-implementation-plan%20-%20tbd.md) Decision #4's three-bucket rule, clarified 2026-05-29):

- **Bucket 1 — declarative layout (JSON):** section/symbol/reloc/field byte layouts; section kind taxonomy; relocation formulas (operator + bit-width + sign); load-command / program-header field tables; chained-fixups data-page descriptors; build-id placement; signing-window placement. The schema says "these bytes go in these slots."
- **Bucket 2 — universal algorithm over declared vocabulary (one C++ engine):** Mach-O bind / lazy-bind / rebase opcode tries (variable-length opcode streams keyed by per-segment data pages, encoded as JSON-declared opcode tables walked by ONE stream emitter); chained-fixups page-encoding walker; ELF GNU_HASH bucket computation; GOT / PLT slot synthesis; relocation application (mutator dispatch via `schema.relocationKind(name)` → bucket-1 formula); per-format metadata section emission. The engine walks the JSON vocabulary and emits the bytes; **no `if (format == "macho")` branch anywhere in the engine**.

Bucket-3 drift (per-format `.cpp` directories) is the failure mode this plan explicitly rejected — see §2.1 below for the deleted `objfmt/elf/`, `objfmt/pe/`, `objfmt/macho/` trees.

**Shared with plan 13 (assembler) — the relocation-taxonomy unifier.** Two-schema decomposition of the (arch × format) relocation matrix, joined by an opaque `uint32_t` tag:

| Schema | Owns | Consumed by |
|---|---|---|
| `*.target.json` `relocations[]` | The **formula+tag**: opaque `uint32_t tag → { isPCRelative, width, addendWidth, ... }`. **✅ target-side substrate landed 2026-05-29 (AS1 cycle 1)**: `TargetRelocationInfo { name; RelocationKind kind; formula }` (`RelocationKind` strong-id), `TargetSchema::relocations()` / `relocationInfo(RelocationKind)` / `relocationByName(name)` accessors, parallel `relocationKindIndex` for O(1) lookup, validate() rules (unique non-zero `kind`, unique non-empty `name`, type-strict `formula`). **✅ assembler-side emission landed 2026-05-29 (AS4 combined cycle)**: both walkers (`x86_variable` + `fixed32`) emit `Relocation` entries when a `SymbolRef` operand reaches a symbol-bearing slot (`Disp32` / `Imm26`); per-wire `relocationKind` declared in JSON and resolved at load time. x86_64 ships `rel32` + `abs64` + `abs32`; arm64 ships `call26` + `adr_prel_pg_hi21` + `abs64`. AssembledFunction.relocations now populates end-to-end through the c-subset corpus path. Decomposition into the richer `{isPCRelative, width, addendWidth, ...}` row is still owed by **LK6 / D-AS4-4** (current substrate stores `formula` as opaque text — sufficient for documentation cross-reference + plan 14's formula-string-to-formula-enum mapping decision). Wire-declared addend bias is anchored at plan 13 §3.1 D-AS4-4. | Assembler (plan 13) emits the tag; linker applies the formula via `relocation_apply.cpp` |
| `*.format.json` `relocations[]` (this plan) | The **format-name → tag** mapping: e.g. `"R_X86_64_PC32" → tag 1`, `"IMAGE_REL_AMD64_REL32" → tag 1`, `"X86_64_RELOC_BRANCH" → tag 1` (all three are PC-relative 32-bit signed — one formula, three names) | Linker (LK6 reloc-apply) uses the format name when writing the object file's reloc table |

Same opaque `uint32_t` tag joins both sides. **No per-(arch×format) C++ enum anywhere.** Cross-referenced from plan 13 §2.6 — AS4 (target-schema reloc rows) and LK6 (format-schema name→tag mapping) land in the same review window so the integer assignments don't drift. The cross-cycle "linker engine mismatch with assembler relocation kinds" risk (rev 1/2's §6 High/High) is closed structurally by both sides reading from the same opaque-tag namespace.

Loader pattern mirrors `TargetSchema::loadShipped`/`GrammarSchema::loadShipped`:

```cpp
auto fmt = ObjectFormatSchema::loadShipped("elf");
LinkResult bin = link(input, *fmt, /*architecture=*/"x86_64", reporter);
```

`O_*` diagnostic family at **0x5xxx** for object-format validation (per the central nibble registry in [`00-master`](./00-compiler-implementation-plan%20-%20tbd.md) §1.2 — the shipped `parse_diagnostic.cpp` has explicitly reserved 0x5xxx for `O_*` since the cross-plan diagnostic-band cleanup). Earlier rev claimed `0xC00x` which silently collided with the shipped `C_*` config family (0xC001..0xC033 — already in production). The corrected allocation mirrors `L_*` 0xBxxx for LIR, `I_*` 0xAxxx for MIR, `H_*` 0xFxxx for HIR.

**Cross-cutting consequence**: the v1 acceptance for "link c-subset to ELF/PE/Mach-O across {OS × arch}" is met by **3 JSON files + 1 engine**, not 3 separate C++ writers. Adding a new format (e.g., COFF for embedded; XCOFF for AIX; a-out for retro) = new JSON file, no engine edits.

### 2.1 Files

```
src/link/
├── linker.hpp / .cpp                   # Engine: resolve + relocate + lay out
├── object_format_schema.hpp / .cpp / _json.cpp
│                                       # ObjectFormatSchema + loadShipped (mirrors TargetSchema)
├── object_format_data.hpp              # detail::ObjectFormatData PODs (sections / symbols / relocs / metadata)
├── object_writer.hpp / .cpp            # ONE engine reading ObjectFormatSchema; emits bytes for any declared format
├── symbol_table.hpp / .cpp             # Defined / undefined / weak / local / hidden — format-blind
├── section.hpp                         # SectionFlags, SectionKind, layout primitives — format-blind
├── relocation_apply.hpp / .cpp         # Mutator dispatch via shared TargetSchema::relocationInfo(tag) — same opaque schema-tag the assembler emits per plan 13 §2.6 (NO per-(arch×format) C++ enum)
├── tls.hpp / .cpp                      # Per-platform TLS lowering — config-driven via schema's TLS model
└── build_id.hpp / .cpp                 # BLAKE3-based deterministic build-id; placement schema-driven

src/dss-config/object-formats/
├── elf.format.json                     # LK1 — ELF64 LE; section/symbol/reloc/dynamic schema
├── pe.format.json                      # LK2 — PE/COFF; section table + import/export + unwind schema
├── macho.format.json                   # LK3 — Mach-O 64; load-command + chained-fixups schema
├── wasm.format.json                    # LK8 (post-v1 skeleton)
└── spirv.format.json                   # LK9 (post-v1 skeleton)
```

The old `src/link/objfmt/elf/`, `objfmt/pe/`, `objfmt/macho/`, `objfmt/wasm/`, `objfmt/spirv/` per-format-writer C++ trees (rev 1/2 sketch) are DELETED — collapsed into `*.format.json` schemas + the single `object_writer.hpp` engine.

### 2.2 Engine

Input per CU:
- Section bytes (one buffer per logical section: `.text`, `.data`, `.rodata`, `.bss`, `.debug_*`, …)
- Relocation list (offset, target symbol, kind, addend)
- Symbol table (name, binding, visibility, section, offset, size)

Output per CU:
- One image file in the target format (ELF / PE / Mach-O / WASM / SPIR-V).

Engine flow:

1. **Merge symbol tables** across all input CUs. **v1 is single-CU per image** (per [`08-compilation-unit-plan`](./08-compilation-unit-plan%20-%20tbd.md) §2.4); the merge logic stubs to identity. Multi-CU input lands in **LK11** (v1.x), coupled with CU6 — see §2.12.
2. **Resolve symbols.** Each undefined ref resolves to either a defined symbol in the same image (intra-module) or to an FFI import (entry in the image's import table).
3. **Lay out sections.** Apply per-format conventions (ELF: page-aligned segments per PT_LOAD; PE: section table with file/virtual alignment; Mach-O: segments with file/virtual alignment).
4. **Apply relocations.** Per (arch × format) mutator from `13-assembler-plan` §2.5 taxonomy.
5. **Emit per-format metadata.** ELF dynamic, PE imports/exports, Mach-O LC_DYLD_INFO, etc.
6. **Compute build-id.** BLAKE3 over section contents → emitted to `.note.gnu.build-id` (ELF), `LC_UUID` (Mach-O), `IMAGE_DEBUG_DIRECTORY` (PE).
7. **Reserve signing window** for Mach-O / PE per [`16-codesign-publish-plan`](./16-codesign-publish-plan%20-%20tbd.md).

### 2.3 ELF (Linux, Android)

- ELF64 little-endian; class ELF64; OS/ABI = SYSV; e_machine = `EM_X86_64` / `EM_AARCH64`.
- Executables: program headers (PT_LOAD × N, PT_DYNAMIC, PT_INTERP, PT_GNU_STACK, PT_GNU_RELRO), DT_NEEDED for dynamic deps.
- Shared libs: same shape, no PT_INTERP.
- Sections: `.text`, `.rodata`, `.data`, `.bss`, `.symtab`, `.strtab`, `.dynsym`, `.dynstr`, `.rela.dyn` / `.rela.plt`, `.dynamic`, `.gnu.hash`, `.note.gnu.build-id`, `.eh_frame`, `.debug_*`.
- PLT/GOT synthesis for dynamic calls.
- GNU_HASH for dynamic symbol lookup (faster than SysV hash).

### 2.4 PE/COFF (Windows)

- DOS stub + PE header (`PE\0\0` + IMAGE_FILE_HEADER + IMAGE_OPTIONAL_HEADER64).
- Subsystem per `06-artifact-profile`: CLI → `IMAGE_SUBSYSTEM_WINDOWS_CUI`, GUI → `IMAGE_SUBSYSTEM_WINDOWS_GUI`, lib → DLL bit set in characteristics.
- Section table + raw section data: `.text`, `.rdata` (read-only data + import directory), `.data`, `.bss`, `.pdata` (function table for unwind), `.xdata` (unwind info), `.reloc` (base relocations).
- `.idata` for dynamic imports; IAT (Import Address Table) and ILT (Import Lookup Table) pairs per imported DLL.
- `.edata` for exports (lib only).
- Base relocations (R_X86_64_RELATIVE-equivalent: `IMAGE_REL_BASED_DIR64`).
- `IMAGE_DIRECTORY_ENTRY_SECURITY` placeholder reserved for `16-codesign-publish-plan` (Authenticode signature appended post-link).

### 2.5 Mach-O (macOS, iOS)

- mach_header_64; magic `MH_MAGIC_64`; CPU type `CPU_TYPE_X86_64` / `CPU_TYPE_ARM64`; filetype `MH_EXECUTE` / `MH_DYLIB`.
- Load commands: LC_SEGMENT_64 × N (`__TEXT`, `__DATA_CONST`, `__DATA`, `__LINKEDIT`), LC_SYMTAB, LC_DYSYMTAB, LC_LOAD_DYLIB × N (deps), LC_LOAD_DYLINKER, LC_UUID, LC_BUILD_VERSION (Apple Silicon), LC_FUNCTION_STARTS, LC_DATA_IN_CODE, LC_CODE_SIGNATURE (placeholder).
- LC_DYLD_INFO_ONLY for x86_64 Mach-O (legacy bind/lazy-bind/rebase/export opcode trie).
- **Chained fixups** (newer macOS 11+, all iOS): LC_DYLD_CHAINED_FIXUPS. Required for Apple Silicon binaries. v1 emits both modes; selects based on target.
- Mach-O fat-archive (`MH_UNIVERSAL`) — reserved post-v1; v1 ships one binary per arch.

### 2.6 WASM (web) — skeleton in v1, full in v1.x

See [`18-wasm-plan`](./18-wasm-plan%20-%20tbd.md). Linker duties = merge function/table indices, dedupe types, generate imports/exports.

### 2.7 SPIR-V (GPU) — skeleton in v1, full in v1.x

See [`17-shader-gpu-plan`](./17-shader-gpu-plan%20-%20tbd.md). Linker duties = emit module-level capability/extension/memory-model headers + per-entry-point `OpEntryPoint` decls + interface variables.

### 2.8 TLS lowering

| Platform | TLS access mechanism |
|---|---|
| Linux x86_64 | `%fs:` segment; `R_X86_64_TPOFF32` relocation; static / initial-exec / general-dynamic / local-dynamic models per ELF TLS ABI |
| Linux ARM64 | `tpidr_el0` register; `R_AARCH64_TLSDESC_*` relocations |
| Windows x86_64 | TEB at `%gs:0x58`; `IMAGE_REL_BASED_DIR64` + `_tls_index` lookup |
| Windows ARM64 | Same TEB model via TPIDR_EL0 + `_tls_index` |
| macOS x86_64 | `_thread_vars` indirection via `__thread_vars` Mach-O section |
| macOS ARM64 | Same via TPIDR_EL0 + `__thread_vars` |

v1: implement initial-exec model for static binaries; general-dynamic reserved post-v1.

### 2.9 Static vs dynamic linking

- **Static**: every symbol resolves within the image; no `.dynamic` / `.idata`; output is one self-contained binary. libc still typically dynamic on Linux/macOS; static glibc is reserved post-v1.
- **Dynamic**: symbols may resolve to imports; per-format mechanism (ELF DT_NEEDED + PLT/GOT; PE IAT; Mach-O LC_LOAD_DYLIB + bind opcodes). v1 default for `cli` artifactProfile.

### 2.10 Diagnostic namespace

`K_*` (K for "linK") — `K_SymbolUndefined`, `K_SymbolRedefined`, `K_RelocationOverflow`, `K_RelocationKindMismatch`, `K_SectionOverlap`, `K_ImportUnresolved`, `K_InvalidLoadCommand` (Mach-O), `K_InvalidPeHeader`, `K_InvalidElfHeader`, `K_TlsModelUnsupported`. The engine emits these directly; the schema's `ObjectFormatSchema::validate()` separately emits `O_*` diagnostics at 0x5xxx for malformed/incomplete format JSON.

### 2.12 Cross-CU linking (v1.x — coupled with `08-compilation-unit-plan` CU6)

v1 ships **one `CompilationUnit` per output image**. The engine's "merge symbol tables across all input CUs" step (§2.2 flow item 1) is therefore an identity operation in v1 — there is only one CU to merge.

**LK11** (v1.x) lifts that assumption to enable:

- **Shared libraries / DLLs consumed by a separate `cli`** — each lib is a CU; the executable's CU resolves symbols against the libs' CUs at link time (not at FFI ingestion time, which is LK6 territory).
- **Incremental compilation** — each translation unit becomes a separate CU; LK11 combines them.

Substrate already in place by the time LK11 ships:

- `CompilationUnitId` provenance ([CU1](./08-compilation-unit-plan%20-%20tbd.md) L2) — every symbol's defining CU is identifiable.
- Cross-CU `NodeId` guard ([CU3](./08-compilation-unit-plan%20-%20tbd.md) D3) — wrong-CU `NodeAttribute<SymbolId>` access is fatal at substrate level.
- CU's `crossRefs` table ([CU3/CU4](./08-compilation-unit-plan%20-%20tbd.md)) — pre-resolved inter-tree edges; LK11 extends the same data model to inter-CU edges.

**Trigger** (when LK11 unblocks): the first artifact profile that requires multiple CUs in one image — typically when [`06-artifact-profile-plan`](./06-artifact-profile-plan%20-%20tbd.md) `lib`/`staticlib` outputs need to be consumed by a separate `cli` project, OR when incremental rebuild enters scope. **UPDATE 2026-06: the trigger was overtaken — LK11a (cross-CU symbol merge) + LK11b (cross-CU call, 2-CU runnable image `cross_cu_call` exit 42) LANDED ahead of it, driven by the OPT7 cross-CU inlining arc (plus `Program::compileUnits` + the MIR-tier `mergeCuMirs` merge); the single-CU-per-image contract no longer holds. Remaining LK11b residuals: ELF/Mach-O exec thunk emission + extern-import dedup (see the LK11 row).**

**Out of scope for LK11**: shared symbol-table semantics across **different processes** (process-boundary IPC / DLL hot-reload). Those are runtime concerns owned by [`21-runtime-reserved-plan`](./21-runtime-reserved-plan%20-%20tbd.md).

### 2.11 Build-id

BLAKE3-256 hash of (section contents excluding the build-id field itself, in canonical order). Emitted to:
- ELF: `.note.gnu.build-id` (20 bytes for compat with GNU tooling; truncated SHA-1-shaped slot)
- Mach-O: `LC_UUID` (16 bytes)
- PE: `IMAGE_DEBUG_DIRECTORY` entry of type `IMAGE_DEBUG_TYPE_CODEVIEW` (16-byte GUID + monotonic age)

### 2.13 Program entry / startup trampoline (the runnable-binary spine) — ✅ **STAGE 1 CLOSED 2026-06-02**

> ✅ **Stage 1 CLOSED 2026-06-02.** D-LK10-ENTRY Stage 1 is live: the linker emits a synthetic `_start` trampoline that calls the user entry function and issues the platform process-exit. **The first DSS-produced binaries that the OS actually runs** — `int42.c` (exit 42 only) shipped end-of-Slice C; `examples/c-subset/hello_puts/main.c` (prints "hello\r\n" via msvcrt.dll's `puts` + exits 42) closed D-FF6-RUNTIME-PRINT and proved the multi-DLL IAT runtime path 2026-06-02. The §0 "LK1–LK10 CLOSED" status now means *byte-valid-in-memory + on-disk + executes*. Remaining: Stage 2 = cross-host equivalents (Linux/macOS/ARM64), variadic ABI for `printf` (gated by ML7 cycle 2 stack-args + variadic substrate), and the full hermetic-acceptance gate.

**The problem (closed — preserved for historical context).** Pre-Slice-C, the executable walkers set `e_entry` / `AddressOfEntryPoint` / `LC_MAIN.entryoff` to point **directly at the user's entry function** (e.g. `main`). But a process entry point is **not** an ordinary function:
- At process entry the stack is **not** a normal call frame — there is no return address on it (the kernel/loader transfers control via jump on POSIX, via CALL on Win64). So when the user function executes its epilogue `ret`, it pops a garbage "return address" and jumps into it → **SIGSEGV / access violation, not an exit code.** This is exactly the failure the runtime-smoke exploration surfaced ("`main` returns via `ret` → no return address at process entry → SEGV, not exit 42"). ✅ Fixed by Slice C's synthetic `_start` trampoline.
- Nothing made the **process-exit** request. A normal program exits because the C runtime's `_start` calls the user entry, takes its return value, and issues the exit syscall (`exit_group` on Linux / `ExitProcess` on Windows / `exit` on Mach-O). With no `_start`, even a function that *could* return cleanly had nowhere to return *to*. ✅ Fixed by Slice C's per-format `processExit` schema.

**Second-order Stage-1 gap closed 2026-06-02.** Slice C's trampoline made `int42.c` run, but the first user function that called an external (msvcrt's `puts`) AV'd at runtime. Diagnosis: (a) `computeFrameLayout` didn't allocate Win64 shadow-space + post-CALL alignment-bias for non-leaf user functions (the trampoline correctly allocated for ITS calls but the user function didn't for HERS); (b) MIR→LIR's `lowerCall` emitted `call` (E8 direct) for extern targets when it should emit `call_indirect_via_extern` (FF 15 indirect-via-IAT) — direct call would execute the IAT slot's bytes as code. Both closed via D-LK10-ENTRY-ML7-FRAME-BIAS-UNIFY: new `cc.callPushBytes` field + `FrameLayout.hasCalls` + `functionHasCalls` scan via `TargetOpcodeInfo::isCall` + new `MnemonicSlot::CallIndirectViaExtern` + `externSymbols` set on MIR→LIR Lowerer. Bucket-2 discipline preserved — no `if (target == ...)`, all CC fields drive the per-target encoding.

**The fix (chosen long-term solution — linker-emitted synthetic `_start` via the LIR substrate, no workarounds).** The linker synthesizes a tiny **entry trampoline** that becomes the real image entry point; it calls the user entry function, then makes the platform exit request with the returned value as the status code. Three candidate shapes were surveyed; the **byte-patch** option (rewrite the trailing `ret` post-assembly) is **rejected** as a fragile post-hoc workaround. A first-pass implementation on 2026-06-01 went after shape (a) — hand-coded byte sequences in the emitter with an `if (target == "x86_64")` arm — and was REJECTED MID-CYCLE for violating the no-`if (os/cpu == ...)`-in-the-emitter discipline. The accepted long-term shape is **(b) LIR-driven trampoline**: the emitter constructs a tiny standalone `Lir` function whose body is `call <user_entry>; mov <argreg>, <retreg>; <exit_tail>; unreachable` and runs it through the existing `assemble()` substrate so per-CPU encoding lives in the assembler (where it always has) and the trampoline becomes "just another function":

```
_start:                      ; becomes the image entry point (e_entry / AddressOfEntryPoint / LC_MAIN)
    call   <user_entry>      ; intra-image relocation to the user function
    mov    <status>, <ret>   ; move the user function's return value into the exit-status arg register
    <platform process-exit>  ; see per-platform table
```

**Per-platform exit mechanism (config-driven — the formula/import is data, the trampoline emitter is one universal walker arm).** This must obey the three-bucket rule: the trampoline-emit algorithm is bucket-2 (one universal arm), and the *which-mechanism* is bucket-1 vocabulary keyed off the format/target — **no `if (os == ...)` in the emitter**.

> **UPDATE 2026-06-09 — hosted formats now exit via libc `exit(3)`, not the raw syscall (the `#include puts` 3-OS string-FFI capstone; §B decision in plan-00 §0.2).** The original v1 design below picked the raw `exit_group` syscall for ELF specifically to avoid a libc dependency ("no libc, fully hermetic"). That is the WRONG default for a HOSTED program: a raw `exit_group` does NOT flush buffered stdio, so `puts("hello")` followed by exit could drop the output. ELF `processExit` was flipped to a **by-name-import** of libc `exit` (`libc.so.6`) — the C-standard exit that runs `atexit` handlers + flushes stdio, matching what crt0 does and what Windows/macOS already did. **Mach-O was flushing all along** (Darwin's `_exit` symbol IS the flushing C `exit(3)`; the POSIX non-flushing `_exit(2)` is `__exit` on Darwin), and **PE `ExitProcess` flushes** via `DLL_PROCESS_DETACH`→msvcrt. The raw-syscall arm is NOT deleted — it is **PRESERVED behind config** (`processExit.mechanism:"syscall"`) for a future freestanding / no-libc target, and the `Syscall` emitter arm is still exercised via synthetic formats. The table below shows the v1 ELF mechanism struck through and the current one beside it.

| Platform | Exit mechanism | How it's reached |
|---|---|---|
| **Linux (ELF)** | ~~`exit_group` syscall (`rax=231`, `rdi=status`, `syscall`)~~ → libc **`exit(3)`** (`exit` / `libc.so.6`, by-name import) **as of 2026-06-09** | **by-name import** through the dynamic-link machinery — libc's `exit(3)` flushes stdio + runs `atexit` (the C-standard hosted exit; matches crt0). Every ELF program now carries a `libc.so.6` `DT_NEEDED`. The v1 raw `exit_group` (no-libc, hermetic) is **SUPERSEDED for hosted formats but RETAINED behind config** (`mechanism:"syscall"`) for a future freestanding/no-libc target. |
| **Windows (PE)** | `kernel32!ExitProcess(status)` | a **by-name import** through the already-built IAT writer (LK6 2a). **No CRT needed** — `ExitProcess` is a kernel32 export, not a C-runtime symbol. This is why the first Windows runnable binary needs *no* `mainCRTStartup`. **Flushes stdio** via the msvcrt `DLL_PROCESS_DETACH` path. |
| **macOS (Mach-O)** | `_exit` via `libSystem` | by-name import through the existing bind machinery. **`_exit` on Darwin IS the flushing C `exit(3)`** (the POSIX non-flushing `_exit(2)` is `__exit`) — so the Mach-O path flushed stdio all along; the 2026-06-09 capstone only corrected the comment that had read this as the non-flushing form. (The arm64-darwin `processExit` was shipped 2026-06-08 / RUNTIME-confirmed on Apple Silicon — `D-LK10-ENTRY-MACHO-EXIT` ✅; `macho64-x86_64-darwin-exec` has **no `processExit`** — there is no macOS-x86_64 CI leg, so shipping one would be an untested §A.5 form; deferred, NOT a gap.) |
| **ARM64 (all OS)** | same mechanisms, AAPCS64 register usage (`x0`=status, `svc #0` for syscalls) | reuses the per-machine helper pattern from D-LK6-8. (Hosted ELF-arm64 now imports libc `exit`; the `svc #0` syscall arm is the freestanding-config path.) |

**Why it's genuinely cross-tier (and therefore a serial cycle, not a parallel-disjoint one).** The trampoline lives in the linker (it emits the bytes + the image entry point), but it needs: (a) the **address of the user entry function** (already known — the linker has the symbol table); (b) an **intra-image relocation** for the `call` (the reloc-apply kernel already exists); (c) the **exit-status register convention** (from the calling convention — already in `TargetSchema`); (d) the **syscall number / import name** as new bucket-1 data on the target/format. So it composes existing substrate but spans linker + target-schema + (for Windows) the import path — it cannot be carved into a file-disjoint parallel track, and should be scheduled as a **dedicated serial session**, accepting that it is not parallel-safe. (Selecting work by file-disjointness is what kept deferring this; the milestone justifies a non-parallel cycle.)

**LIR-driven cycle plan (3 substrate slices, each independently green):**

* **Slice A — new LIR opcodes (bucket-1 vocabulary growth).** Add two opcodes to the closed-enum vocabulary that every register-machine target with a runnable-binary spine needs. **Critical**: NEITHER is a terminator opcode (user feedback + silent-failure FN-1 converged on this — a terminator-`Unreachable` role hardcodes 0 operands per `target_schema.hpp:821`, which contradicts `call_indirect_via_extern`'s SymbolRef-operand requirement; and a general `syscall` opcode must remain reusable by FF6's `write` syscall which returns to the next instruction). The trampoline emitter marks the noreturn call site by appending a separate `unreachable` op (which encodes to `ud2` = `0F 0B` on x86_64, `BRK #0` / `D4 20 00 00` on ARM64) AFTER the syscall / indirect call — semantically a verifier hint, not part of the call.
  * `syscall` — raw kernel transition (x86_64 encoding `0F 05`; ARM64 `D4 00 00 01`). LIR-level semantics: no operands, no result, no successors, `hasSideEffects = true`, **terminator role `None`** (not `Unreachable` — see above). Encoding format: **`x86-variable` on x86_64 via a new "zero-operand fixed-bytes" variant within the existing shape (anchored D-LK10-ENTRY-X86-NO-MODRM-VARIANT — silent-failure + comment-analyzer convergence that the current variant set is ModR/M-centric); `fixed32` on ARM64** (matches `arm64.target.json`'s existing single-word-instruction shape — comment-analyzer fix: the plan previously invented `aarch64-variable` which does not exist).
  * `call_indirect_via_extern` — indirect call through an extern-import IAT slot. x86_64 encoding: opcode `FF` + ModR/M `/2` digit (= `FF 15`) + `disp32` paired with a `R_X86_64_PLT32`-shaped REL32 reloc (PE's `IMAGE_REL_AMD64_REL32` is byte-identical) — the reloc kind matches the existing pcRel-4-byte Linear formula. **ARM64**: this lowers to a **3-instruction macro-op** (ADRP page-pair + LDR x16 from GOT slot + BLR x16) — implemented as either three separate LIR opcodes in the trampoline or one `call_indirect_via_extern` opcode whose encoder produces 12 bytes via three back-to-back encoder calls (comment-analyzer correction: ARM64 indirect calls are NOT a single instruction). LIR-level semantics: 1 operand (the ExternImport `SymbolRef`), no result, no successors, **terminator role `None`** (general-purpose indirect call; trampoline appends a separate `unreachable`). Existing `LirOperandKind::SymbolRef = 5` (`lir_node.hpp:37`) covers this; no new operand kind needed (code-architect verified).

  Both opcodes land with full encoder coverage + tests pinning the byte sequences. **Slice A use-site discipline (silent-failure FN-2 fold)**: tests must construct a synthetic `Lir` via `LirBuilder` containing the new opcodes and round-trip through `assemble()` — NOT just unit-test the encoder table rows. A vocabulary addition with no caller violates the project's "no dead substrate" discipline (already-caught anti-pattern at `9945457` audit fold).

* **Slice B — ProcessExit substrate on ObjectFormatSchema (+ entryCallingConvention).** Add `ExitMechanism { None, Syscall, ByNameImport }` closed-enum + `ProcessExit` POD as schema vocabulary (in `core/types/target_schema.hpp` with the other vocabulary types — they're shared vocabulary, even though the field lives on the format schema), `processExit: optional<ProcessExit>` field on `ObjectFormatData` + JSON loader, plus `entryCallingConvention: std::string` field on `ObjectFormatData` + JSON loader (CRITICAL fold — names the cc the trampoline emitter resolves against; non-empty required on exec-flavored formats). Shipped:
    * `pe64-x86_64-windows-exec.format.json`: `mechanism = "by-name-import"` + `importLibraryPath = "kernel32.dll"` + `importMangledName = "ExitProcess"` + `entryCallingConvention = "ms_x64"`.
    * `elf64-x86_64-linux-exec.format.json`: ~~`mechanism = "syscall"` + `syscallNumber = 231` (exit_group) + `syscallNumGpr = "rax"` + `syscallOpcodeBytes = [15, 5]`~~ → **`mechanism = "by-name-import"` + `importMangledName = "exit"` + `importLibraryPath = "libc.so.6"`** (flipped 2026-06-09, the `#include puts` capstone — hosted ELF terminates via libc `exit(3)` so stdio flushes; see the per-platform table note above) + `entryCallingConvention = "sysv_amd64"`. The `syscall`/`syscallNumber`/`syscallOpcodeBytes` knobs + the `Syscall` emitter arm REMAIN in the substrate (config-retained for a future freestanding/no-libc target; still tested via synthetic formats) — they are no longer the ELF default. `elf64-aarch64-linux-exec` flipped identically.
    * Mach-O macOS path ✅ **CLOSED** — `D-LK10-ENTRY-MACHO-EXIT` (arm64-darwin: `mechanism = "by-name-import"` + `importMangledName = "_exit"` + `importLibraryPath = "libSystem.B.dylib"`; `_exit` on Darwin is the flushing C `exit(3)`). `macho64-x86_64-darwin-exec` deliberately ships NO `processExit` (no macOS-x86_64 CI leg → untested §A.5 form; deferred, not a gap).
    * **Note**: the `unreachable` LIR opcode this slice needs is already present in both `x86_64.target.json:170` and `arm64.target.json:77` — Slice A does NOT need to add it (code-architect Q2 verification).
    * **Note (silent-failure A2)**: when D-LK10-ENTRY-ARM64 lands, Slice A's encoding-row enumeration must explicitly cover (i) the new `syscall` opcode's ARM64 `fixed32` encoding `0xD4000001` (SVC #0), AND (ii) the `unreachable` opcode's ARM64 `fixed32` encoding `0xD4200000` (BRK #0) if it's not already declared in `arm64.target.json`. The plan implicitly assumed both target JSONs grow together; explicit per-CPU enumeration prevents landing only the x86 row.

* **Slice C — LIR-driven trampoline emitter + linker hook + run-harness + Windows exit(42) smoke.** New `src/link/entry_trampoline.{hpp,cpp}` constructs a one-function `Lir` module on the fly. **Bucket-2 universal arm**: zero `if (target == ...)` branches, zero hardcoded byte sequences. The emitter:
  * **Resolves user-entry SymbolId from the format's `entryPoint` string** by scanning `module.functions[].symbol`, comparing against the synthesized-name convention each walker uses today (`sym_<id>` for ELF/PE; `_sym_<id>` for Mach-O). Empty `entryPoint` defaults to `functions[0].symbol`. **Ordering invariant** (silent-failure H1 fold): this resolution MUST run BEFORE the trampoline is prepended; the `functions[0]` default is read against the pre-prepend module shape. Reading after prepend would self-reference the trampoline → infinite-recursion exit. **Fail-loud diagnostics** (silent-failure FN-4 fold): `K_SymbolUndefined` if the named symbol resolves to nothing; `K_SymbolUndefined` (with distinct message) if the name matches an `ExternImport` rather than a defined function (cannot use an extern as the user entry — anchored D-LK4-3 cross-CU symbol-table merge is the future closure). Real-name resolution (when D-LK1-1 closes the HIR→LIR symbol-name thread) joins this lookup; no new substrate needed at Slice C.
  * **Resolves the active calling convention from a NEW `entryCallingConvention: std::string` field on the format schema** (CRITICAL fold — code-architect Q5 + silent-failure C1 converged). Slice B adds this field to `ObjectFormatData`. Shipped: PE-Exec = `"ms_x64"`; ELF/Mach-O-Exec = `"sysv_amd64"`; ARM64-targets = `"aapcs64"`. The emitter looks up the cc via `target.callingConventionByName(format.entryCallingConvention())`. Without this field, `<active>` is undefined-by-construction (the trampoline emitter bypasses ML7 callconv lowering which normally selects cc per function attribute / driver flag) — the prior plan-text "`callingConventions[<active>]`" was a silent fallback. Fail-loud: `K_NoMatchingObjectFormat` if `entryCallingConvention` is empty on an exec-flavored format; `K_NoMatchingObjectFormat` if the name does not resolve to a declared cc.
  * Mints a fresh `SymbolId` for the trampoline (`_start`) by scanning `module.functions[].symbol` + `module.externImports[].symbol` for max-v and incrementing.
  * Builds the trampoline as a `LirBuilder`-constructed function using **physical register ids directly**. Construction shape (code-architect Q3 fold — no separate PhysReg type): `LirReg phys{*target.registerByName("rdi").value()}`; pass via `LirOperand::makeReg(phys)`. Physical and virtual regs share the same `LirReg` struct, distinguished by ordinal convention only — there is no separate type. **No virtual regs, no `frame_load`/`frame_store` pseudo-ops.** Per code-architect verification, `assemble()` walks whatever `LirReg` ids are present and calls `hwEncodingOf` — regalloc and ML7 callconv lowering are **skipped entirely**. Frame pseudo-ops would fire `A_NoEncodingDeclared` if accidentally included; the emitter must not emit them.
  * **`mov` LIR-instruction shape** (silent-failure H2 fold): when the plan writes `mov <argGprs[0]>, <returnGprs[0]>` in Intel syntax, the LirBuilder binding is **destination = inst's result `LirReg` slot; source = operand[0]** (NOT a 2-operand inst). `x86_64.target.json`'s `mov` declares `minOperands=1, maxOperands=1, result="value"`. Code: `auto dest = builder.declareReg(LirReg{argGprIdx}); builder.addBinaryOrUnary(movOpcode, dest, srcOperand)` — exact API per `lir.hpp`. Same shape for the syscall-num materialization: `mov <syscallNumGpr>, imm32` is one LIR inst with the syscall-num reg as result and `LirOperand::makeImmInt32(syscallNumber)` as operand[0].
  * **Syscall arm LIR sequence (5 ops)** — user feedback item 2 fold. **(As of 2026-06-09 this is the FREESTANDING / no-libc arm, not the Linux-ELF default — hosted ELF now takes the `ByNameImport` arm below with `exit`/`libc.so.6` so stdio flushes. The Syscall sequence is unchanged + still exercised via synthetic formats.)**
    1. `call <user_entry>` (intra-image `call` opcode; produces REL32 reloc).
    2. `mov <argGprs[0]>, <returnGprs[0]>` (e.g. `mov rdi, rax` on SysV AMD64 — status from user fn's return-value reg into the syscall-arg reg).
    3. `mov <processExit.syscallNumGpr>, immediate(<processExit.syscallNumber>)` (e.g. `mov rax, 231` — concrete immediate, NOT a payload; the syscall opcode itself takes no payload).
    4. `syscall` (new opcode).
    5. `unreachable` (existing terminator opcode — verifier hint that control does not fall through; encodes to `ud2` x86_64 / `BRK #0` ARM64).

    **Sign/zero extension semantics note (silent-failure A1)**: the user fn's i32 return reaches `rax` zero-extended by x86's architectural rule that any 32-bit write to `eax` clears the upper 32 bits of `rax`. The trampoline's `mov <argGprs[0]>, <returnGprs[0]>` is a REX.W reg-reg copy — bit-faithful, no extension involved. The kernel's `exit_group` masks the status to 8 bits at the syscall boundary. **No bug**; the analysis closes the loop the user surfaced.
  * **ByNameImport arm LIR sequence (4 ops + optional shadow space)** — also injects synthetic `ExternImport{name=importMangledName, lib=importLibraryPath}` into `module.externImports` first. **(As of 2026-06-09 this is the arm EVERY hosted format takes: Windows PE `ExitProcess`/`kernel32.dll`, Linux ELF `exit`/`libc.so.6`, macOS Mach-O `_exit`/`libSystem.B.dylib` — the C-standard flushing exit. The exact call shape per format follows the format's `externCallDispatch`: PE = `call_indirect_via_extern` through the IAT slot; ELF = a direct `call`/BL to the linker-synthesized PLT stub. The trampoline emitter is one universal arm — the dispatch is config DATA, no `if(format)`.)**:
    1. *(Optional)* stack-adjust for ABI shadow space (MS x64: 40 bytes = 32 shadow + 8 align). Sourced from `callingConventions[<active>].shadowSpaceBytes` (already in the substrate per `target_schema.hpp` cycle-2b coherence rules). When the active cc declares `shadowSpaceBytes > 0`, emit a `sub rsp, <bytes>` op; otherwise skip. ARM ABIs declare 0 here so this op disappears.
    2. `call <user_entry>` (REL32 reloc).
    3. `mov <argGprs[0]>, <returnGprs[0]>` (e.g. `mov rcx, rax` on MS x64).
    4. `call_indirect_via_extern <exit_import_symbol>` (new opcode; REL32-shaped reloc to the synthetic ExitProcess ExternImport's IAT slot).
    5. `unreachable`.
  * **lirToMir sentinel contract (code-architect FOLD-NOW)**: `assemble()` enforces `lirToMir.size() == lir.instCount()` at entry (`asm.cpp:167`). The emitter constructs a parallel `vector<MirInstId>` of sentinel `MirInstId{}` entries with `lir.instCount()` length and passes it as `lirToMir` — the synthetic Lir has no MIR provenance to attribute. Anchored `D-LK10-ENTRY-LIRTOMIRSENTINEL` for when a 2nd synthetic-Lir caller arrives (factor an `assembleHandBuilt(...)` wrapper that supplies the sentinel internally).
  * Wraps `assemble()`'s output as `AssembledFunction`, prepends to `module.functions`, increments `expectedFuncCount`, and sets `AssembledModule.imageEntryOverride = std::optional<std::size_t>{0}`. **The `optional` discriminant is load-bearing (user item 3 fold)**: index 0 is a valid override target (the trampoline genuinely sits at functions[0]); using 0 alone as "unset" would collide with the valid-0 case — same Unknown=0-vs-valid-0 trap LK4 cycle-1 review already caught. The walkers honor `imageEntryOverride.has_value()` before falling back to `format.entryPoint()` string resolution (3-line change per walker — bucket-2 universal).
  * Linker hook in `linker.cpp` invokes the emitter when `format.processExit().has_value()` and the module is exec-flavored.
  * `tests/test_support/run_binary.hpp` adds `CreateProcess` Windows arm (POSIX arm anchored D-LK10-ENTRY-POSIX-RUN-HARNESS).
  * ✅ **Stage-1 smoke CLOSED 2026-06-02 (Windows host)**: hand-built one-function module `mov eax, 42; ret` → linked as PE → written to disk → spawned via `runBinary()` → asserted `exit == 42`. **The first DSS-produced binary the OS actually runs.** Hello_puts (`puts("hello")` via msvcrt) followed 2026-06-02 — first DSS-produced binary that prints. The shipped `elf64-x86_64-linux-exec.format.json` `processExit` row is byte-level-tested via static format-walker assertions. **Runtime-exec on Linux is now PROVEN** — the `int42`/`global_int`/`shipped_include_puts` corpora RUN → correct exit on the native `linux-gcc-release` (x86_64) + `linux-arm64-gcc-release` (native arm64 hardware) CI legs (POSIX `runBinary` via `posix_spawn`, `D-LK10-ENTRY-POSIX-RUN-HARNESS` ✅). **macOS-arm64 runtime is PROVEN** too (`macho_arm64_exit` + `global_int` on the `macos-clang-release` Apple-Silicon leg, `D-LK10-ENTRY-MACHO-EXIT` ✅ 2026-06-09). As of the 2026-06-09 exit-spine flip these all terminate via the C-standard flushing `exit(3)` (libc `exit` on ELF, `_exit`=C-exit on Mach-O, `ExitProcess` on PE).

**Why this is genuinely bucket-pure.** The trampoline emitter has **zero `if (target == ...)` arms** and **zero hardcoded byte sequences**. Every CPU-specific byte (call opcode, mov-reg-reg encoding, stack-adjust, syscall opcode, indirect-call encoding) flows through the existing assembler's `*.target.json`-declared encoding tables — once D-LK10-ENTRY-X86-NO-MODRM-VARIANT closes the one-time variant-shape addition that the x86_64 `syscall` opcode triggers. Adding ARM64 to the runnable-binary spine = drop new opcode encoding rows into `arm64.target.json` + populate `processExit` in `elf64-aarch64-linux-exec.format.json`; the emitter requires no change. Adding macOS = pick syscall vs. libSystem-import in `macho64-*-darwin-exec.format.json`; the emitter requires no change.

**Staged delivery (de-risked — isolate "run at all" from "call a foreign function").**
- ✅ **Stage 1 CLOSED 2026-06-02 — `exit(N)` smoke, zero foreign calls.** Slices A+B+C landed. On the **Windows host** this is `ExitProcess(N)` via the IAT (no CRT, no `.rdata`, no stack args, no variadic). On Linux it was the raw `exit_group` syscall (no imports at all). Proved the **entire runnable-binary spine** with the minimum possible surface — the first DSS-produced binary that the OS actually runs. *(Forward note 2026-06-09: the ELF raw-`exit_group` default was later SUPERSEDED by libc `exit(3)` for hosted formats so stdio flushes — see the per-platform-table UPDATE above; the raw-syscall arm is config-retained for a future freestanding target. The Stage-1 `int42` exit-only corpus still runs correctly under the libc-exit path.)*
- ✅ **Stage 2 PARTIALLY DONE 2026-06-02 — `hello_puts` (plan 11 FF6 Windows half).** `examples/c-subset/hello_puts/main.c` calls msvcrt's `puts` + exits 42; both `exit==42` AND `capturedStdout=="hello\r\n"` asserted byte-for-byte. Closed alongside the string-literal `.rdata` substrate (D-LK4-RODATA-PRODUCER ✅ 2026-06-02) + Win64 frame-bias unification (D-LK10-ENTRY-ML7-FRAME-BIAS-UNIFY ✅ 2026-06-02) + extern-call opcode selection (`CallIndirectViaExtern` ✅). **Remaining Stage 2 surface**: stack-passed-arg ABI (ML7 cycle 2 `L_StackPassedArg` ⏳ — required for `WriteFile` / `printf`-style 5+ arg calls); variadic ABI for `printf`-with-formatters. **Cross-host `puts` is ✅ DONE on all 3 OSes** (the `#include puts("hello")` 3-OS string-FFI capstone, 2026-06-09 — `examples/c-subset/shipped_include_puts` prints `"hello\n"` + exits 42): **Linux libc `puts`** — ✅ **RUNTIME-PROVEN LOCALLY** on linux-x86_64 (WSL native) AND linux-arm64 (qemu-aarch64; `strace` shows `write(1,…)` THEN `exit_group(42)` = the libc-`exit(3)` flush precedes exit), re-confirmed on the native ubuntu CI legs; **macOS libSystem `puts`** — ✅ **RUNTIME-PROVEN on real Apple Silicon** (run 27212153591, `macos-clang-release` ctest #206 `shipped_include_puts` Passed → "hello\n"+exit 42; the FIRST macOS execution of `puts("hello")`→stdout — the Mach-O `_exit`=flushing-C-`exit(3)` + Model-3 libSystem resolution confirmed at runtime).

**Run-harness substrate.** Stage 1 also introduces the first test that *executes* an emitted artifact (every prior test asserted bytes in-memory). A `tests/test_support/` run-and-capture helper (spawn via `CreateProcess` on Windows / `posix_spawn` on POSIX, capture stdout + exit code, with timeout) is part of this cycle's deliverable.

---

## 3. PR breakdown

| PR  | Title                                            | Scope |
|-----|--------------------------------------------------|-------|
| ~~LK1 cycle 1~~ ✅ **relocatable .o landed 2026-05-29** | `elf.format.json` + ELF writer walker — first per-format byte-emission walker plugged into the format-blind linker engine. | **ET_REL minimal valid .o end-to-end.** New `src/dss-config/object-formats/elf64-x86_64-linux.format.json` — first shipped object-format JSON; declares ELF identity (class=ELFCLASS64, data=ELFDATA2LSB, machine=EM_X86_64=62), 5 sections (.text/.rela.text/.symtab/.strtab/.shstrtab) with sh_type/sh_flags/sh_addralign/sh_entsize, 3 relocations (R_X86_64_PC32=2, R_X86_64_64=1, R_X86_64_32=10) with format-side `nativeId`. New `ObjectFormatSectionInfo{kind,name,type,flags,addrAlign,entrySize}` schema row (closes D-LK4-2). `ObjectFormatRelocationInfo` grew `nativeId:u32` (validated `!= 0`). New `ElfIdentity{fileClass,dataEncoding,osabi,abiVersion,machine}` block on `ObjectFormatData` (populated only when `kind == Elf`; `validate()` requires non-zero class/data/machine). New `SectionKind::ShStrtab` enum entry (distinct from `Strtab` — section-name vs symbol-name table; `EnumNameTable` size 12). New `src/link/format/elf.{hpp,cpp}` ELF writer walker — emits Elf64_Ehdr + section header table + .text + .rela.text + .symtab + .strtab + .shstrtab byte-for-byte per gABI Ch. 4 + AMD64 psABI §4.4. `e_shnum` derived from headers array size (architect B-LK1-2 convergence — pre-fix was hardcoded literal `6`, would silently corrupt when sections grow). `K_NoMatchingObjectFormat=0x8003` re-added with full LK1 consumer enumeration in parse_diagnostic.hpp docstring. `link()` dispatch shell got format-keyed closed-enum switch over `ObjectFormatKind` — `Elf` arm calls `elf::encode`; `Unknown`/`Pe`/`MachO`/`Wasm`/`Spirv` arms fire `K_NoMatchingObjectFormat`. New `linkagePassed` snapshot-vs-current-errorCount gate in `link()` — skips walker dispatch when cross-reference unifier emitted any K_*; resets `resolvedFuncCount=0` to prevent ok() false-positive (architect Decision 4 convergence). Closes D-LK4-1 (ELF arm) + D-LK4-2. **7-agent review fold-in (inline)**: architect B-LK1-2 (e_shnum hardcode) → derived from headers array; architect Decision 4 + comment-analyzer (resolvedFuncCount inconsistency) → reset on linkage failure; silent-failure CRITICAL-2 + HIGH-3 (D-AS5-3 walker silent-skip) → fail-loud + hasModRm/hasImm32 guards; convention #1 (`ShStrtab` missing from enum-name round-trip test) → added; convention #2 + comment-analyzer (K_NoMatchingObjectFormat under-described) → 4-scenario docstring; comment-analyzer (fabricated "LK1 cycle 2" anchor + stale linker.hpp doc + misleading elf.hpp failure-channel claim) → rewritten; test-analyzer Gaps 3/4/5 → 6 new tests (`LinkagePassedGateSkipsWalkerOnSymbolUndefined`, Pe/MachO/Wasm/Spirv `K_NoMatchingObjectFormat` dispatch arm pins, D-AS5-3 `SymbolBearingSlotsReturnNullopt` + `Imm32SlotsCarryConcreteValue`). 14 new LK1 + D-AS5-3 tests. 113/113 ctest. **New deferred items**: D-LK1-1 (real symbol-name thread → LK7), D-LK3-1 (Mach-O segment+section split → LK3). |
| ~~LK1 cycle 2~~ ✅ **ET_EXEC landed 2026-05-30** | `elf64-x86_64-linux-exec.format.json` + ELF walker ET_EXEC arm — first executable-image walker arm plugged into the format-blind linker engine. | **Minimal valid Linux x86_64 ET_EXEC for self-contained modules end-to-end.** New `src/dss-config/object-formats/elf64-x86_64-linux-exec.format.json` — second shipped ELF schema (alongside the cycle-1 .o schema); declares `elf.type = "exec"`, sections with `virtualAddress = 0x401000` for `.text` (Linux x86_64 base 0x400000 + first-page reservation for Ehdr+PHT). Schema growth: `ElfIdentity.objectType: u16` (1=REL/2=EXEC, ET_DYN anchored D-LK1-4), `ObjectFormatSchema.entryPoint: std::string` (universal — empty defaults walker to module.functions[0]; non-empty looks up by synthesized `sym_<id>` name today, real-name resolution lands with D-LK1-1), `ObjectFormatSectionInfo.virtualAddress: u64` (universal — populated for ET_EXEC; validate-rejected when non-zero on ET_REL/PE/MachO MH_OBJECT). ELF walker ET_EXEC arm: emits `e_type=ET_EXEC`, derives `e_entry = secText.virtualAddress + funcTextStart[entryFnIdx]`, emits one PT_LOAD program header (p_type=PT_LOAD, p_flags=PF_X\|PF_R, p_align=0x1000), sets `sh_addr` on `.text`. Section ordering preserved (SHT_NULL placeholder for `.rela.text` slot keeps IDX_TEXT=1 / IDX_SYMTAB=3 / IDX_STRTAB=4 / IDX_SHSTRTAB=5 invariant). Cycle scope: self-contained modules only — modules with non-empty relocations[] fail loud `K_RelocationKindMismatch` (anchored at D-LK1-3 for reloc application paired with LK6). Closes D-LK1-2. **7-agent review fold-in (heavy)**: silent-failure CRITICAL-1 on PT_LOAD `p_offset`/`p_vaddr` congruence violation (Linux kernel ENOEXEC at exec time) → walker pads `.text` file offset to `p_align=0x1000` before layout; silent-failure HIGH-1 on `entryPoint` loaded+exposed+IGNORED → walker now honors entryPoint by synthesized-name lookup (anchored D-LK1-1 for real names); silent-failure HIGH-2 on `eEntry = ... + 0` magic → replaced with `funcTextStart[entryFnIdx]` named local; comment-analyzer CRITICAL on fabricated D-LK1-3/D-LK1-4 anchors → both added as real rows; convention review HIGH (82%) on PE test in wrong file → moved to `test_pe_writer.cpp`; type-design HIGH on per-format `virtualAddress=0` validate symmetry → ET_REL/PE/MachO MH_OBJECT all reject non-zero (added test-analyzer Mach-O coverage); architect anchor request → new D-LK1-3 (reloc application), D-LK1-4 (ET_DYN/PIE), D-LK1-5 (cross-arch p_align). 16 new LK1 cycle 2 tests + 1 new PE test + 1 new Mach-O test + 5 new D-LK1-* anchored deferred items. 117/117 ctest. **Cycle-2 trio remaining: D-LK2-1 (PE PE32+) + D-LK3-2 (Mach-O LC_MAIN).** |
| ~~LK2 cycle 1~~ ✅ **relocatable .obj landed 2026-05-29** | `pe64-x86_64-windows.format.json` + PE writer walker — second per-format byte-emission walker plugged into the format-blind linker engine. | **Minimal valid PE/COFF .obj for x86_64-windows end-to-end.** New `src/dss-config/object-formats/pe64-x86_64-windows.format.json` — second shipped object-format JSON; declares PE identity (machine=IMAGE_FILE_MACHINE_AMD64=0x8664, characteristics=0), 1 section (`.text` with Characteristics=0x60500020 = CODE\|ALIGN_16BYTES\|EXECUTE\|READ), 3 relocations (REL32=4, ADDR64=1, ADDR32=2 in `nativeId`). New `PeIdentity{machine,characteristics}` flat sub-block on `ObjectFormatData` alongside `ElfIdentity` (per architect's no-variant rationale). `validate()` requires `pe.machine != 0` when `kind == Pe` + rejects `addrAlign != 0` on PE rows (PE encodes alignment via Characteristics, not the substrate `addrAlign` field — type-design Q3 + architect Decision 4 convergence). New `src/link/format/pe.{hpp,cpp}` PE writer — emits IMAGE_FILE_HEADER + IMAGE_SECTION_HEADER[] + .text + per-section IMAGE_RELOCATION[] + IMAGE_SYMBOL[] (18 bytes packed) + string table (4-byte u32 size prefix) byte-for-byte per Microsoft PE Format spec §3-5. `NumberOfSections` DERIVED from `sectionHeaders.size()` (architect D-LK2-5 convergence). New shared substrate `src/link/format/byte_emit.hpp` — hoisted `appendU8/U16/U32/U64/I64LE` + `emit` + `requireSection` from both walkers (simplifier #1+#3 convergence; ELF walker migrated in the same fold). `linker.cpp::link()` Pe arm now dispatches to `pe::encode`; MachO/Wasm/Spirv still fire `K_NoMatchingObjectFormat` (LK3 / plan 18 / plan 17). Closes D-LK4-1 (PE arm). **7-agent review fold-in (inline)**: architect #5 + silent-failure H1 (`requireSection(Symtab/Strtab)` would spuriously fail for legitimate PE JSONs that omit those rows — PE has no section headers for symbol/string tables; switched to walker-only `secText` requirement); silent-failure C1 (relocation count silent u16 truncation → fail-loud with `K_NoMatchingObjectFormat`, D-LK2-3 anchored for the OVFL overflow path); silent-failure C2 (`addend != 0` silently dropped → fail-loud `K_RelocationKindMismatch` — PE has no Rela-style addend field, addends live in patch bytes); silent-failure H3 + simplifier #5 (symbol-dedup `nextSymIdx++` ran unconditionally on duplicate emplace → `unordered_set<SymbolId>` + emplace-result gated increment + duplicate-defined diagnostic; also collapses O(n²) linear scans to O(1) lookups); architect D-LK2-5 (NumberOfSections hardcoded `1` → derived from sectionHeaders vector size, same shape as ELF's `e_shnum`); convention review (`K_NoMatchingObjectFormat` docstring stale → updated with PE/ELF call-with-wrong-kind case); type-design Q3 + architect Decision 4 (PE `addrAlign` unused → validate-rejected, shipped JSON dropped the field); test-analyzer FOLD-NOW gaps 1/2/3 → 4 new tests (`LongSymbolNamesUseStringTableOffsetForm`, `NonPeFormatKindEmitsK_NoMatchingObjectFormat`, `ZeroMachineRejectedByValidate`, `NonZeroAddendFailsLoud`); comment-analyzer (SHN_UNDEF → IMAGE_SYM_UNDEFINED terminology). 11 new LK2 tests + 3 new D-LK2-* anchored deferred items. 114/114 ctest. |
| ~~LK1 cycle 2~~ ✅ **ET_EXEC landed 2026-05-30** | `elf64-x86_64-linux-exec.format.json` + ELF walker ET_EXEC arm — first executable-image walker arm plugged into the format-blind linker engine. | **Minimal valid Linux x86_64 ET_EXEC for self-contained modules end-to-end.** New `src/dss-config/object-formats/elf64-x86_64-linux-exec.format.json` — second shipped ELF schema (alongside the cycle-1 .o schema); declares `elf.type = "exec"`, sections with `virtualAddress = 0x401000` for `.text` (Linux x86_64 base 0x400000 + first-page reservation for Ehdr+PHT). Schema growth: `ElfIdentity.objectType: u16` (1=REL/2=EXEC, ET_DYN anchored D-LK1-4), `ObjectFormatSchema.entryPoint: std::string` (universal — empty defaults walker to module.functions[0]; non-empty looks up by synthesized `sym_<id>` name today, real-name resolution lands with D-LK1-1), `ObjectFormatSectionInfo.virtualAddress: u64` (universal — populated for ET_EXEC; validate-rejected when non-zero on ET_REL/PE/MachO MH_OBJECT). ELF walker ET_EXEC arm: emits `e_type=ET_EXEC`, derives `e_entry = secText.virtualAddress + funcTextStart[entryFnIdx]`, emits one PT_LOAD program header (p_type=PT_LOAD, p_flags=PF_X\|PF_R, p_align=0x1000), sets `sh_addr` on `.text`. Section ordering preserved (SHT_NULL placeholder for `.rela.text` slot keeps IDX_TEXT=1 / IDX_SYMTAB=3 / IDX_STRTAB=4 / IDX_SHSTRTAB=5 invariant). Cycle scope: self-contained modules only — modules with non-empty relocations[] fail loud `K_RelocationKindMismatch` (anchored at D-LK1-3 for reloc application paired with LK6). Closes D-LK1-2. **7-agent review fold-in (heavy)**: silent-failure CRITICAL-1 on PT_LOAD `p_offset`/`p_vaddr` congruence violation (Linux kernel ENOEXEC at exec time) → walker pads `.text` file offset to `p_align=0x1000` before layout; silent-failure HIGH-1 on `entryPoint` loaded+exposed+IGNORED → walker now honors entryPoint by synthesized-name lookup (anchored D-LK1-1 for real names); silent-failure HIGH-2 on `eEntry = ... + 0` magic → replaced with `funcTextStart[entryFnIdx]` named local; comment-analyzer CRITICAL on fabricated D-LK1-3/D-LK1-4 anchors → both added as real rows; convention review HIGH (82%) on PE test in wrong file → moved to `test_pe_writer.cpp`; type-design HIGH on per-format `virtualAddress=0` validate symmetry → ET_REL/PE/MachO MH_OBJECT all reject non-zero (added test-analyzer Mach-O coverage); architect anchor request → new D-LK1-3 (reloc application), D-LK1-4 (ET_DYN/PIE), D-LK1-5 (cross-arch p_align). 16 new LK1 cycle 2 tests + 1 new PE test + 1 new Mach-O test + 5 new D-LK1-* anchored deferred items. 117/117 ctest. **Cycle-2 trio remaining: D-LK2-1 (PE PE32+) + D-LK3-2 (Mach-O LC_MAIN).** |
| ~~LK2 cycle 2~~ ✅ **PE32+ `.exe` landed 2026-05-30** | PE32+ optional header + `.exe` image path — closes the `.exe` half of D-LK2-1 (`.dll` anchored at D-LK2-4). | **Minimal valid Windows x86_64 PE32+ `.exe` for self-contained modules end-to-end.** New shipped `pe64-x86_64-windows-exec.format.json` with `pe.type = "exec"`, full `IMAGE_OPTIONAL_HEADER64` declaration (Magic=PE32+/0x20B, ImageBase=0x140000000, SectionAlignment=0x1000, FileAlignment=0x200, Subsystem=WINDOWS_CUI, dllCharacteristics=ASLR+DEP+TS-aware), `.text` section with `virtualAddress`=0x1000 (RVA). Schema growth: `PeObjectType` closed enum {Obj=1, Exec, Dll} mirroring `ElfObjectType`/`MachOObjectType`; `PeOptionalHeader` flat sub-block with all load-bearing PE/COFF §3.4 fields. PE walker EXEC arm: MS-DOS stub (zero-filled, loader-legal) + PE signature + IMAGE_FILE_HEADER (EXECUTABLE_IMAGE flag) + IMAGE_OPTIONAL_HEADER64 (240 B) + section headers (derived from vector, B-LK1-2/D-LK2-5 discipline) + file-aligned section data. Intra-module relocations applied via shared `applyExecRelocations` kernel (closes simplifier 3-agent convergence — 3rd-consumer trigger like D-LK4-9); `sectionVa = ImageBase + RVA`. `.dll` arm rejected `K_NoMatchingObjectFormat` (anchored D-LK2-4). validate() growth: PE Exec/Dll requires non-zero ImageBase/Subsystem/stack/heap sizes + sectionAlignment ≥ 4096 (page size — Windows ENOEXEC trap) + sectionAlignment power-of-two + fileAlignment ∈ [512, 65536] + sectionAlignment ≥ fileAlignment + secText.virtualAddress % sectionAlignment == 0. `SizeOfImage` derived as `roundUp(secText.va + textVirtualSize, sectionAlignment)`. Closes the `.exe` half of D-LK2-1. **7-agent review folded heavy**: 4-agent on `isExecFlavor` terminal validate gate (PE/MachO arms uncommented), 3-agent on shared reloc kernel extraction, 3-agent on `numSections=1` hardcode regression (derived from vector), 2-agent on `sectionAlignment ≥ 4096` validate rule + non-multiple-of-sectionAlignment virtualAddress trap, type-design 2-agent on `MachODylibRef` row type + `MachOObjectType` enum (folded across both walkers), comment-analyzer D-LK2-1 over-claim → split: `.exe` half closed, `.dll` anchored separately as D-LK2-4. 13 new PE EXEC tests (DOS/PE sig/optional header byte-pin, intra-module rel32, extern fail-loud, 5 validate-rule lock-ins, Dll-arm K_NoMatchingObjectFormat, empty-text guard, OOB-per-fn, displacement overflow). **Plan 14 §3 LK2 cycle 2 ✅** (closes `.exe`; `.dll` deferred to D-LK2-4 paired with LK6 cycle 2 dynamic linking). |
| ~~LK2 cycle 3~~ ✅ **PE `.dll` landed 2026-07-16 (c152)** | PE32+ DLL image — closes D-LK2-4 part 1 (the entry-LESS `.dll`; the DllMain arm is the pinned follow-up `D-LK2-DLL-DLLMAIN-ENTRY`). | **`dss --target x86_64:pe64-x86_64-windows-dll` emits a `.dll` the REAL Windows loader consumes** — `ctypes.CDLL('dsslib.dll').dss_add(2, 40)` → 42 (witnessed on native Windows). NEW `pe64-x86_64-windows-dll.format.json` (`pe.type="dll"`, Characteristics 0x2022 = EXECUTABLE_IMAGE|LARGE_ADDRESS_AWARE|IMAGE_FILE_DLL per `cl /LD` dumpbin ground truth, ImageBase 0x180000000, DllCharacteristics 0x160, NO entry cluster — validate() rejects it on the dll shape exactly like the ET_DYN `.so`; artifactProfiles ["lib"]; NO tdata/tbss — `D-LK-DLL-TLS-MODEL`). pe.cpp Dll arm = the SAME `encodeExec` substrate with isDll divergences: AddressOfEntryPoint=0 (no DllMain — loader skips notifications, the `link /NOENTRY` shape), `.edata` export directory in data-directory[0] (EAT + LEXICOGRAPHICALLY SORTED Name Pointer Table — the GetProcAddress binary-search invariant, red-on-disable tested — + identity ordinals Base 1 + empty DllName mirroring absent DT_SONAME; exports = externally-visible defined functions + data globals from `module.symbols`, the c150 `.dynsym` set), `.reloc` DIR64 completeness (data-item slots incl. the c145 relro fold; ASLR-rebase PROVEN: two same-preferred-base dispatch DLLs in one process, both rebased, fn-ptr-table calls → 42), preferred-ImageBase VAs (NO ELF-style base-0 trick — the loader adjusts DIR64 sites by the load delta). Fail-loud belts: extern-DATA-targeted data slot rejects (`D-LK-IMAGE-DATA-SLOT-EXTERN-ADDR` PE half CLOSED; function-extern thunk bake stays legal per the c112 addr_import semantics), absolute text reloc on a DYNAMIC_BASE image rejects (`D-LK-PE-IMAGE-TEXT-ABS-RELOC`), imageEntryOverride-on-dll + duplicate-export-name reject. `allowsUndefinedImports()` FALSE (Windows binds every import at load from a named module). `.dll` suffix already wired (target_spec). W1/W2/W3 loader witnesses green (exports 42/42, rebase dispatch 42, msvcrt puts print + 42); 646/646 ctest incl. new `tests/link/test_pe_dll_writer.cpp`. |
| ~~LK3 cycle 1~~ ✅ **MH_OBJECT landed 2026-05-30** | `macho64-x86_64-darwin.format.json` + Mach-O writer walker — third per-format byte-emission walker plugged into the format-blind linker engine. | **Minimal valid Mach-O .o for x86_64-darwin end-to-end.** New `src/dss-config/object-formats/macho64-x86_64-darwin.format.json` — third shipped object-format JSON; declares Mach-O identity (cputype=CPU_TYPE_X86_64=0x01000007, cpusubtype=3, filetype=MH_OBJECT=1, flags=0), 1 section (`__text` in segment `__TEXT` with Characteristics=0x80000400 = S_REGULAR\|S_ATTR_PURE_INSTRUCTIONS\|S_ATTR_SOME_INSTRUCTIONS), 3 relocations (BRANCH/UNSIGNED_8/UNSIGNED_4 with packed `nativeId` = `(type<<28)\|(length<<25)\|(pcrel<<24)`). New `MachOIdentity{cputype,cpusubtype,filetype,flags}` flat sub-block alongside `ElfIdentity`+`PeIdentity` (architect-confirmed no-variant pattern scales to 3 consumers). New `ObjectFormatSectionInfo.segment: std::string` field — empty for ELF/PE (validate-rejected if set); non-empty required for Mach-O (validate-rejected if empty). Closes D-LK3-1. **D-LK4-9 closed**: new `src/link/format/string_table.hpp` substrate with `Init::NulByte` (ELF/Mach-O) + `Init::U32SizePrefix` (PE); ELF + PE walkers migrated to use it. New `src/link/format/macho.{hpp,cpp}` Mach-O writer — emits `mach_header_64` + `LC_SEGMENT_64` + `section_64` + `LC_SYMTAB` + per-section `relocation_info[]` + `nlist_64[]` + NUL-seeded string table byte-for-byte per Apple OS X ABI Mach-O File Format Reference + `<mach-o/loader.h>` / `<mach-o/nlist.h>` / `<mach-o/x86_64/reloc.h>`. `linker.cpp::link()` MachO arm now dispatches to `macho::encode`; Wasm/Spirv still fire `K_NoMatchingObjectFormat` (plan 18 / plan 17). Closes D-LK4-1 MachO arm. **7-agent review fold-in (heavy)**: 2-agent convergence on `nativeId` packing not validated → Mach-O validate-reject bits 0..23 + bit 27 (silent-corruption guard); architect "structural issue" on `kNumSections=1` hardcode → derived from explicit `numSections` constant routed through 3 emission sites (mirrors LK1 B-LK1-2 / LK2 D-LK2-5 fix); silent-failure C2 on macho walker silent `continue` for missing fmtReloc/sym lookup → fail-loud K_* like ELF; silent-failure HIGH-2 on `appendName16` truncation → validate-reject section/segment names > 16 chars for Mach-O; silent-failure HIGH-1 on `MH_SUBSECTIONS_VIA_SYMBOLS` flag advertising a contract the walker doesn't honor → dropped from JSON (flag=0); type-design Q4 on `nativeId` docstring inaccuracy → rewritten to describe Mach-O's packed-bitfield semantics correctly; test-analyzer 2 FOLD-NOW tests (multi-function module + symmetric PE segment-rejection). 13 new LK3 tests + 1 new PE test + 3 new D-LK*-* anchored deferred items (D-LK1-2 ELF ET_EXEC, D-LK3-2 Mach-O image path, D-LK4-11 symbol-index builder hoist). 115/115 ctest (LK3 added one ctest binary `test_macho_writer`, bringing the total from 114 to 115). **Cycle 2 for all 3 formats (ELF ET_EXEC + PE PE32+ + Mach-O LC_MAIN) is the natural next vertical slice** — D-LK1-2 / D-LK2-1 / D-LK3-2 paired anchors. |
| ~~LK3 cycle 2~~ ✅ **MH_EXECUTE landed 2026-05-30** | Mach-O MH_EXECUTE image path — closes the executable half of D-LK3-2 (MH_DYLIB anchored at D-LK3-3). | **Minimal valid macOS x86_64 MH_EXECUTE end-to-end.** New shipped `macho64-x86_64-darwin-exec.format.json` with `macho.filetype = "execute"`, `macho.flags` = MH_NOUNDEFS\|MH_DYLDLINK\|MH_TWOLEVEL\|MH_PIE (modern macOS exec convention), `__text` section at vmaddr=0x100001000 (after __PAGEZERO), new `image` block with pageZeroSize=0x100000000 + dylinkerPath=/usr/lib/dyld + loadDylibs=[libSystem.B.dylib]. Schema growth: `MachOObjectType` closed enum {Object=1, Execute=2, Dylib=6} replacing raw `u32 filetype` (type-design 2-agent symmetry with `ElfObjectType`/`PeObjectType`); `MachOImage` flat sub-block; `MachODylibRef` row type with `path` (anticipates future timestamp/current/compat version fields without parallel-vector anti-pattern). Mach-O walker EXEC arm: mach_header_64 (MH_EXECUTE + MH_PIE) + LC_SEGMENT_64(__PAGEZERO) + LC_SEGMENT_64(__TEXT) + section_64(__text) + LC_LOAD_DYLINKER + LC_MAIN + LC_LOAD_DYLIB[] + LC_SYMTAB, then page-padded `__text`. `__TEXT.filesize = textSecOffsetInSeg + textFileSize` (byte-exact section span — NOT page-aligned vmsize; folded from 3-agent silent-failure C2 / code-reviewer C1 convergence: page-aligned filesize would silently overlap the symtab/strtab into __TEXT). LC_MAIN.entryoff derives from textFileOff + funcTextStart[entry] (load-bearing dependency on `__TEXT.fileoff == 0` documented inline). Defensive walker check + validate rule: `sectionVa >= pageZeroSize` (silent-failure H4: prevents `sectionVa - pageZeroSize` unsigned underflow). Cross-schema `fmt.relocationByKind` check added (silent-failure H5). Reloc kernel reused via shared `applyExecRelocations`. Closes the MH_EXECUTE half of D-LK3-2. **7-agent review folded heavy** (same review as LK2 cycle 2). 13 new Mach-O EXEC tests (filetype + PAGEZERO + LC_MAIN byte-pin, intra-module branch byte-pin, extern fail-loud, 4 validate-rule lock-ins, MH_DYLIB rejection at validate, sectionVa-below-pageZero, missing dylinkerPath, empty-text, OOB-per-fn, displacement overflow). **Plan 14 §3 LK3 cycle 2 ✅** (closes MH_EXECUTE; MH_DYLIB deferred to D-LK3-3 paired with LK6 cycle 2). |
| ~~LK3 arm64 object~~ ✅ **arm64 MH_OBJECT landed 2026-07-15** | `macho64-arm64-darwin.format.json` — the arm64 sibling of the x86_64 macho object (cputype CPU_TYPE_ARM64, filetype MH_OBJECT, `__text`, the 4 ARM64_RELOC_* kinds). ZERO src change (the MH_OBJECT walker is target-blind). Cross-toolchain-witnessed on a Mac: `dss --target arm64:macho64-arm64-darwin` → a valid arm64 `.o` that system `clang` links + runs → exit 42. Closes D-LK3-MACHO-ARM64-OBJECT (leaf-only; extern-call-in-.o deferred to D-LK-OBJECT-EXTERN-CALL-MACHO). |
| ~~LK3 cycle 3~~ ✅ **MH_DYLIB landed c153 (2026-07-16, roadmap A3)** | Mach-O MH_DYLIB arm — closes D-LK3-3 (the Mach-O mirror of the c150 ELF `.so` + c152 PE `.dll`; the dynamic-library trio is COMPLETE across all three formats). | **`dss --target arm64:macho64-arm64-darwin-dylib` emits an MH_DYLIB for dlopen/dlsym on Apple Silicon.** NEW `macho64-arm64-darwin-dylib.format.json` (filetype `dylib`, flags 0x100085 = NOUNDEFS\|DYLDLINK\|TWOLEVEL\|NO_REEXPORTED_DYLIBS — no MH_PIE; `image.pageZeroSize` 0 + text VA == segmentPageSize → base-0 image validate-PINNED, the ELF-dyn mirror; NO dylinkerPath; NEW `image.installName` → LC_ID_DYLIB, REQUIRED on dylib / rejected elsewhere — config-driven + honest, never derived from the output name; no entry cluster — validate-rejected like .so/.dll; no tdata/tbss → D-LK3-DYLIB-TLS-MODEL; adhoc codeSignature). `macho.cpp`: Dylib routes through `encodeExecDynamic` UNCONDITIONALLY (zero-extern dylibs legal — the dss_add witness shape); conditional __PAGEZERO/__DATA_CONST with COMPUTED bind/rebase segment indices (`segIdxDataConst`/`segIdxData`, emission-order cross-checked fail-loud); LC_ID_DYLIB emitted; NO LC_MAIN/LC_LOAD_DYLINKER; the EXPORT TRIE (real radix prefix compression — `insertExport` splits edges at divergence so dyld's greedy first-match walk is sound for strict-prefix name pairs; fixpoint ULEB offset layout; KIND_REGULAR terminals carrying VA-minus-header imageOffsets) emitted at `LC_DYLD_INFO_ONLY.export_off`; real-named nlist for externally-visible functions (ObjectSymbolNames); WEAK exports fail loud (D-LK3-DYLIB-WEAK-EXPORT); internal abs64 slots keep the F5 REBASE stream (base-0 slide semantics = the ELF RELATIVE mirror); **D-LK-IMAGE-DATA-SLOT-EXTERN-ADDR Mach-O half CLOSED**: extern-targeted data slots (data AND function externs) emit SYMBOL-BASED dyld BIND opcodes with the slot zeroed + de-rebased (+ SET_ADDEND_SLEB for non-zero addends) — the ELF c150 symbol-based fold's semantic mirror, on the exec arm too. `macho_codesign.{hpp,cpp}`: `buildAdHocCodeSignature` gains an `execSegFlags` param — exec passes CS_EXECSEG_MAIN_BINARY (byte-identical), dylib passes 0 (codesign ground truth for a non-main image). `isImageFlavor()` Mach-O arm → `!= Object`; `allowsUndefinedImports()` documented FALSE for dylib (two-level namespace — every bind names a dylib ordinal; a flat-namespace knob would flip it). 22 new tests (`tests/link/test_macho_dylib_writer.cpp`): header/LC_ID_DYLIB pins, the export trie WALKED with dyld's own algorithm incl. the strict-prefix pair, rebase red-on-disable, the extern-slot bind pins (both flavors), execSegFlags=0, weak/entry-override/TLS belts, 9 validate shape rejects. Exec output byte-identical (all 647-suite greens). **Mac-side validation (the user's Apple-Silicon steps — no macOS host here):** (1) build any exported-fn C source with `--target arm64:macho64-arm64-darwin-dylib` → `libdss.dylib`; (2) `file libdss.dylib` → "Mach-O 64-bit dynamically linked shared library arm64"; (3) `otool -hv libdss.dylib` → filetype DYLIB, flags NOUNDEFS DYLDLINK TWOLEVEL NO_REEXPORTED_DYLIBS; (4) `otool -l libdss.dylib | grep -A3 LC_ID_DYLIB` → name @rpath/libdss.dylib; (5) `codesign -dvv libdss.dylib` → "Signature=adhoc" (DSS self-signs; if a future macOS rejects the DSS blob, `codesign -s - -f libdss.dylib` re-signs in place — report that as a bug against the adhoc builder); (6) `dyld_info -exports libdss.dylib` (or `nm -gU`) lists the exported functions; (7) the dlopen witness: `cat > main.c <<EOF` / `#include <dlfcn.h>` + `#include <stdio.h>` + `int main(void){ void *h = dlopen("./libdss.dylib", RTLD_NOW); if (!h) { printf("%s\n", dlerror()); return 1; } int (*f)(int,int) = dlsym(h, "dss_add"); if (!f) { printf("%s\n", dlerror()); return 2; } printf("%d\n", f(2,40)); return 0; }` / `EOF`, then `clang main.c -o main && ./main` → prints 42. Report the exact dlerror text on any failure — it names the rejecting layer (code signature vs platform vs bind). |
| ~~LK4 substrate~~ ✅ **substrate slice landed 2026-05-29** | `object_writer` engine substrate + `ObjectFormatSchema` | **Substrate-tier slice done. Format walkers (ELF/PE/Mach-O) anchored at LK1+.** New `src/link/object_format_schema.{hpp,cpp,_json.cpp}` mirrors `TargetSchema`'s shape — JSON-configured per-format descriptor at `src/dss-config/object-formats/<name>.format.json` (no shipped files yet; LK1+ adds them). Closed-enum vocabulary via `EnumNameTable<E,N>`: `ObjectFormatKind{Unknown=0,Elf,Pe,MachO,Wasm,Spirv}`, `SectionKind{Text,Rodata,Data,Bss,Symtab,Strtab,RelocTable,Dynamic,Note,Debug,Custom}`, `SymbolBinding{Local,Global,Weak}`, `SymbolVisibility{Default,Hidden,Protected,Internal}`. `ObjectFormatRelocationInfo{name, kind}` — format-side half of plan 13 §2.6 reloc-taxonomy unifier (the SAME opaque `RelocationKind` tag the assembler stamps; format owns the platform-native name like `R_X86_64_PC32` / `IMAGE_REL_AMD64_REL32`). New `ObjectFormatSchemaId` strong-id minted via shared `substrate::mintMonotonicId`. JSON loader `dssObjectFormatVersion=1` + `format.{name,version,kind}` + `relocations[{name,kind}]` with same discipline as TargetSchema (kind != 0, unique kind cross-row, unique name, fail-loud on duplicate / zero / non-integer kind / unknown kind name). New `K_*` diagnostic family at 0x8xxx (plan 00 §0.3 update): `K_SymbolUndefined=0x8001`, `K_RelocationKindMismatch=0x8002` (substrate-tier; per-format codes including `K_NoMatchingObjectFormat` for artifact-profile dispatch land alongside their LK* cycles, not as substrate dead-code). New `src/link/linker.{hpp,cpp}`: `link(AssembledModule, TargetSchema, ObjectFormatSchema, reporter) → LinkedImage`. `LinkedImage{format, bytes, expectedFuncCount, resolvedFuncCount, ok()}` — same parallel-index discipline that AssembledModule + LirAllocation use (`ok()` derived from `expectedFuncCount > 0 && resolved == expected`). Cycle-scope behavior: walks every relocation, fails loud if `kind` is missing from EITHER side (3-agent convergence: diagnostic message names BOTH sides when both miss — naming one would force a re-link to discover the other), fails loud on unknown symbol target INDEPENDENTLY of kind resolution (silent-failure C2 fix: BOTH diagnostics surface in one pass). Per-format byte emission (ELF/PE/Mach-O writers) is the D-LK4-1 scope. **7-agent review fold-in (inline)**: 3-agent convergence on joint-side reporting + silent-failure-hunter C2 on combined-error single-pass + type-design 85% on `Unknown=0` invalid-sentinel + comment-analyzer on `K_SymbolUndefined` doc accuracy + simplifier on `lir_pass_util::report` reuse (linker.cpp no longer reimplements the report shape) + test-analyzer on partial-resolution multi-func test + architect on D-LK4-3 FFI co-trigger (anchored). 18 LK4 tests: 5 enum + default-sentinel pin, 9 JSON loader happy/sad paths (missing/wrong version, unknown kind, duplicate name/kind, zero kind, non-array relocations, Unknown-sentinel rejected by validate), 9 linker tests (empty/intra-CU/no-relocs/unknown symbol/missing-on-format/missing-everywhere/partial multi-func/joint-side message/combined-error single-pass). 111/111 ctest. |
| LK4 cont. | format walkers + section/symbol emission        | Plug per-format byte-emission walkers into the substrate engine. (Slated for LK1+ as those format JSONs ship — substrate is structurally ready.) |
| LK5 | TLS lowering                                     | Per §2.8 table; initial-exec model on every platform. |
| ~~LK6 cycle 1~~ ✅ **intra-module reloc application landed 2026-05-30** | Structured-formula reloc applier + ELF ET_EXEC intra-module patch path — closes D-LK1-3. | **Real c-subset corpus runs as a Linux x86_64 ET_EXEC end-to-end.** Substrate growth on `TargetRelocationInfo`: structured formula triple (`pcRelative: bool` + `addendBias: i32` + `widthBytes: u8` — JSON loader restricts widthBytes to {4, 8} or absent; `formula` text field DROPPED per type-design O2.b + comment-analyzer #3 convergences — redundant with structured fields, was a desync vector). Cross-row `validate()` coherence rules: (a) `widthBytes == 0` is the explicit "linker rejects" sentinel for non-linear formulas (ARM64 `call26`); (b) `pcRelative || addendBias != 0` ⇒ `widthBytes != 0` (rejects "PC-relative without apply width"); (c) `addendBias != 0` ⇒ `pcRelative` (rejects legal-but-meaningless absolute-with-bias — type-design O2.a); (d) `|addendBias|` must fit signed in `widthBytes` (rejects bias-overflows-slot at load time — silent-failure M1). x86_64.target.json populates: rel32 (pcRel=true, bias=-4, width=4), abs64 (false, 0, 8), abs32 (false, 0, 4). arm64.target.json deliberately omits the structured fields on `call26` / `adr_prel_pg_hi21` / `abs64` (widthBytes=0 → walker rejects per D-LK6-1). ELF walker ET_EXEC arm: `applyExecRelocations` helper computes `value = S + A + (pcRelative ? -P : 0) + addendBias` for each `Relocation`, range-checks `value` fits signed in `widthBytes` (silent-failure C1), bounds-checks `rel.offset + widthBytes` against the originating function's `fn.bytes.size()` (silent-failure C2 + code-reviewer I1 — guards against inter-function patch overrun), guards empty `.text` for ET_EXEC (silent-failure H2), then writes `widthBytes` LE bytes into `.text` at the patch site. Extern symbols (target not defined by any `AssembledFunction`) fail loud `K_SymbolUndefined` (anchored D-LK6-2). Closes D-LK1-3. **7-agent review fold-in (heavy)**: 4-agent convergence on fabricated D-LK6-1/D-LK6-2 anchors → both added as real plan rows (this PR); 2-agent convergence on silent value-truncation when result doesn't fit in `widthBytes` (silent-failure C1 + code-reviewer C2) → range-check before write + `K_RelocationKindMismatch`; 3-agent convergence on per-function offset bound (silent-failure C2 + code-reviewer I1 + test-analyzer Gap 1) → bound against `fn.bytes.size()` not `text.size()`; 3-agent convergence on extract-helper + indent-regression (simplifier #1+#3 + code-reviewer I3) → `applyExecRelocations` file-local helper; type-design O2.a coherence rule (`addendBias!=0 && !pcRelative` reject); type-design O2.b + comment-analyzer #3 → DROPPED `formula` C++ field (loader still accepts the JSON key as documentation, ignores); silent-failure H2 → empty `.text` guard; silent-failure M1 → `|addendBias|` fits in `widthBytes` validate rule; test-analyzer 4 gaps + 2 silent-failure tests (8 new tests total): patch-OOB per-function, non-zero addend, abs32 + post-patch sentinel survival, validate coherence load-time (both directions), widthBytes==0 direct in-process, empty `.text`, displacement overflow. arm64.target.json updated to drop the redundant `formula` strings on the now-rejected kinds. 8 new tests + ~125 ctest. **New deferred anchors**: D-LK6-1 (bit-shift formula closed-enum reshape — ARM64 call26), D-LK6-2 (extern FFI / dynamic linking — paired with D-LK4-3), D-LK6-3 (p_align schema field — tightens D-LK1-5 with validate-reject gate). |
| ~~LK6 cycle 2a~~ ✅ **PE IAT + FFI substrate landed 2026-05-30** | FFI extern-import substrate + PE32+ `.idata`/IAT walker arm. Closes PE half of D-LK6-2. | **Real FFI substrate end-to-end (PE only this cycle).** New `ExternImport{symbol, mangledName, libraryPath}` row + `AssembledModule.externImports`. New diagnostic `K_FormatLacksImportSupport=0x8004`. Linker engine: `Relocation::target` resolves against `declaredSymbols ∪ externSymbols`; the cross-table SymbolId-uniqueness invariant is enforced by linker (intersection rejected with `K_SymbolUndefined`). Empty `mangledName` / `libraryPath` validated at linker-side (fail-loud `K_SymbolUndefined` with the entry index). Shared `applyExecRelocations` kernel signature generalized from section-relative offset map + sectionVa to **absolute symbol VA map** (caller pre-populates with both intra-module function VAs AND extern IAT slot VAs). PE walker EXEC arm: synthesizes a 2nd `.idata` section with ImageImportDescriptor[N+1] + ILT + IAT (PE32+ identical thunks at file-image time, loader patches IAT) + HINT/NAME table + DLL name strings; sets IMAGE_DATA_DIRECTORY[1] (Import Table) + [12] (IAT); REL32 calls to externs patch to IAT slot RVA - patch_VA - 4 via the kernel; SizeOfInitializedData = idataRawSize. IAT u64-alignment guard: thunkCursor padded to 8 after the descriptor block so multi-library layouts stay slot-aligned (code-reviewer convergence). ELF + MachO walkers fail loud `K_FormatLacksImportSupport` when externImports non-empty (anchored D-LK6-4 + D-LK6-5). `LinkedImage.ok()` derives from `resolvedFuncCount`, gated by post-walker `errorCount` check so a walker-side fail-loud zeros the count (architect O1). **7-agent review fold-in (heavy)**: 3-agent convergence on fabricated anchors → real plan rows D-LK6-4/5/6 added; 3-agent convergence on empty-mangledName/libraryPath silent failure → linker-side validate; 2-agent convergence on duplicate-SymbolId-across-tables → linker rejects intersection; code-reviewer IAT alignment, SizeOfInitializedData, ELF/MachO test-file split (moved to correct format files); silent-failure u32-narrowing → toU32 helper; silent-failure IAT directory size accumulation refactor; test-analyzer multi-extern + multi-library + mixed extern/intra-module + neither-set negative + HINT byte-pin + ILT==IAT byte identity + ELF ET_REL fail-loud tests (~10 new tests). Substrate proven by construction: same `ExternImport` row + `symbolVa` map drives all 3 walkers (cycles 2b/2c reuse the substrate). | First Windows `.exe` that calls a libc/kernel32 symbol through the IAT.
| ~~LK6 cycle 2b.1~~ ✅ **dynamic-linker substrate prep landed 2026-05-30** | `ElfIdentity.interpreter` PT_INTERP path field. | **Substrate slice for the cycle 2b.2 walker arm.** New `ElfIdentity.interpreter: std::string` field (`/lib64/ld-linux-x86-64.so.2` for glibc, `/lib/ld-musl-x86_64.so.1` for musl). JSON loader type-checks non-string. Shipped `elf64-x86_64-linux-exec.format.json` populates the glibc path. ELF walker's `K_FormatLacksImportSupport` diagnostic now distinguishes substrate-ready-walker-pending from substrate-missing (cites the populated interpreter path when it IS declared, surfaces the empty PT_INTERP gap when it's not). 3 new tests pin: shipped JSON loads with the field, non-string type rejected, diagnostic distinguishes the two states. Closes the substrate half of D-LK6-4. — (substrate closed) |
| ~~LK6 cycle 2b.2~~ ✅ **ELF GOT/PLT walker landed 2026-05-30** | ELF GOT/PLT walker arm — closes D-LK6-4 walker half. | **Linux x86_64 corpus calls dyld-resolved externs end-to-end (eager binding).** New `encodeElfExecDynamic()` helper emits `.interp` (PT_INTERP path from `ElfIdentity.interpreter`) + `.text` (with REL32 patched to PLT stubs) + `.plt` (6-byte `FF 25 disp32` stubs) + `.dynsym` (STN_UNDEF + N externs, STB_GLOBAL\|STT_NOTYPE, st_shndx=SHN_UNDEF) + `.dynstr` (extern names + library paths) + `.hash` (DT_HASH single-bucket) + `.rela.dyn` (R_X86_64_GLOB_DAT per GOT slot — eager binding, not lazy `.rela.plt` JUMP_SLOT) + `.got` (N slots, zero-init; dyld writes resolved fn VAs at startup) + `.dynamic` (DT_NEEDED per lib + DT_STRTAB + DT_SYMTAB + DT_SYMENT=24 + DT_STRSZ + DT_HASH + DT_RELA + DT_RELASZ + DT_RELAENT=24 + DT_FLAGS_1=DF_1_NOW + DT_NULL). 5 program headers: PT_PHDR + PT_INTERP + PT_LOAD#1(R+X) + PT_LOAD#2(R+W) + PT_DYNAMIC. **Eager binding chosen** (DF_1_NOW + GLOB_DAT in `.rela.dyn`) over lazy (PLT0 + JUMP_SLOT in `.rela.plt`) — simpler, no PLT0 needed, dyld resolves before transferring control. Plan deviation from earlier "JUMP_SLOT" wording recorded explicitly. PLT-stub-internal `disp32` is walker-synthesized (architect Obs 5 closes D-LK6-7 as "not needed — walker-synthesized sections patched inline, 3-format precedent: PE .idata + ELF .plt + ELF .got"). **7-agent review folded heavy**: code-reviewer #1 (entry-point ignored regression — fixed by mirroring LK1 cycle 2 HIGH-1 fold); silent-failure C1 (baseImageVa underflow → validate), C2 (symbolVa.emplace collision detection → fail loud), C3 (fmt.relocationByKind pre-flight check missing — added), H1 (K_RelocationKindMismatch misused for empty-text → switched to K_SymbolUndefined), H2 (numExterns=0 precondition fail-loud). Walker precondition guards: !empty externImports, !empty functions, secText.va >= pageAlign. Comment-analyzer stale dispatch block removed. 6 new tests + 117/117 ctest. New deferred anchors: D-LK6-8 (machine-specific PLT stub encoding for ARM64), D-LK6-9 (DT_GNU_HASH), D-LK6-10 (PT_GNU_RELRO + PT_GNU_STACK), D-LK6-7 closed. |
| LK6 cycle 2c | Mach-O LC_DYLD_INFO_ONLY arm (x86_64-darwin) ✅ landed 2026-05-30 | Closes D-LK6-5 (x86_64 arm). Emits `__got` (in `__DATA_CONST`) + `__stubs` (in `__TEXT`) + `__LINKEDIT` + LC_DYLD_INFO_ONLY immediate `bind_off` opcode stream + LC_DYSYMTAB + indirect-symbols table. Eager binding (`image.bindNow = true`). ARM64-darwin chained-fixups path anchored at D-LK6-14 (separate cycle — `LC_DYLD_CHAINED_FIXUPS` is structurally distinct). Reuses the same `ExternImport` substrate + `applyExecRelocations` kernel. |
| LK6 cycle 2d | HIR → AssembledModule extern thread-through ✅ landed 2026-05-30 | Closes D-LK6-6. Substrate hoist: `ExternImport` moved from `src/asm/asm.hpp` to `src/core/types/extern_import.hpp` (5-tier consumer threshold met — HIR / MIR / LIR / assembler / linker). HIR→MIR `collectExterns` pre-pass reads `HirAttribute<FfiMetadata>` to build `HirToMirResult.externImports`; `lowerToLir` propagates verbatim through `MirToLirResult.externImports`; `assemble(..., std::span<ExternImport const> externs)` copies into `AssembledModule.externImports`. Fail-loud at MIR pre-pass on missing FfiMap entry / empty `mangledName` / empty `importLibrary` / SymbolId collision with intra-module function / duplicate extern SymbolId / invalid FnSig — diagnostics anchor at the HIR node's source span (loses fidelity at linker tier). `ExternFunction` main-pass arm flipped from `unsupported` to no-op (pre-pass handles it). `HirKind::ExternGlobal` still `unsupported` (cycle scope: ExternFunction only). FFI map population by `printf` smoke-test stays paired with plan 11 FF5 binary ingestion; cycle 2d ships the thread-through substrate. |
| LK7 | Codesign hook ✅ landed 2026-05-30                | Mach-O LC_CODE_SIGNATURE placeholder + PE attribute-cert reservation, both filled by `16-codesign-publish-plan`. Schema fields `MachOImage.codeSignatureSize` + `PeOptionalHeader.attributeCertReserveSize` (both u32, default 0, multiple-of-8 when non-zero). Mach-O walker emits `LC_CODE_SIGNATURE` (cmd=0x1D, cmdsize=16) at the tail of `__LINKEDIT`; PE walker sets `IMAGE_DIRECTORY_ENTRY_SECURITY[4]` (file-offset, NOT RVA, per PE COFF §3.4.6) + zero-fills the reservation at the file tail. Mach-O static `encodeExec` path REJECTS non-zero `codeSignatureSize` (no `__LINKEDIT` segment present — kernel `cs_validate_range` would reject; the dynamic path is required for codesigning, anchored D-LK7-1). U32-overflow guards on both file-offset casts (silent-failure MEDIUM fold). ELF has no equivalent (v1 Linux signing is detached `.sig`; anchored D-LK7-2). |
| LK8 | `wasm.format.json` + engine WASM support (skeleton) ✅ landed 2026-05-30 | Ships `wasm32-v1.format.json` + `src/link/format/wasm.{hpp,cpp}` walker emitting the 8-byte WebAssembly v1 module preamble (magic `'\0asm'` + version 1) per WASM spec §5.1. Section emitters (type/import/function/table/memory/global/export/start/element/code/data) are intentionally OUT of scope — plan 18 (MIR→WAT→.wasm) owns them. Walker fails loud on (a) non-Wasm format, (b) non-empty `module.functions` (WASM bypasses LIR per plan 18 §2.1 — native-ISA bytes are not consumable here), (c) non-empty `module.externImports` (WASM Import section structure differs from `ExternImport`), (d) non-zero `expectedFuncCount`. Cross-format symmetry: `object_format_schema_json.cpp` now rejects wasm-kind schemas carrying `elf`/`pe`/`macho`/`optionalHeader`/`image` blocks at schema load. Linker dispatch flipped Wasm arm from fail-loud to the new walker; Spirv still fail-loud (LK9 / plan 17 owner). |
| LK9 | `spirv.format.json` + engine SPIR-V support (skeleton) ✅ landed 2026-05-30 | Ships `spirv-1.6.format.json` + `src/link/format/spirv.{hpp,cpp}` walker emitting the 5-word (20-byte) SPIR-V module header (magic `0x07230203` + version `0x00010600` for 1.6 + generator 0 + bound 0 + reserved 0) per SPIR-V Spec §2.3. Instruction-stream emission (`OpCapability` / `OpExtension` / `OpMemoryModel` / `OpEntryPoint` / `OpTypeFunction` / `Op*`) is intentionally OUT of scope — plan 17 §2.5 owns the MIR→SPIR-V lowering strategy. Walker mirrors LK8 WASM 4-guard contract: fails loud on (a) non-Spirv format, (b) non-empty `module.functions` (SPIR-V bypasses LIR per plan 17 §2.5), (c) non-empty `module.externImports` (SPIR-V's import model is `OpExtInstImport`, distinct from `ExternImport` row), (d) non-zero `expectedFuncCount`. Linker dispatch flipped Spirv arm from fail-loud to walker; `K_NoMatchingObjectFormat` now fires only for the `Unknown` sentinel. Test substrate hoist: `readU32LE` / `readU64LE` moved to `tests/link/link_test_support.hpp` (3rd-consumer trigger met — spirv + macho + codesign tests). **Plan 14 §3 LK1–LK9 ALL CLOSED.** |
| LK10 cycle 1 | File-emission substrate ✅ landed 2026-05-30 | Ships `src/link/writer.{hpp,cpp}` with `dss::linker::writeImage(LinkedImage, fs::path, reporter) -> bool` — commits `LinkedImage::bytes` to disk in binary truncate mode, fails loud on **6 distinct K_* codes** (K_ImageNotOk / K_ImageEmpty / K_ImageWriteParentMissing / K_ImageWriteOpenFailed / K_ImageWriteShort / K_ImageWriteCloseFailed — type-design split into remediation-distinct codes; post-fold #2 added K_ImageEmpty to separate walker-bug from caller-bug arms). Namespace `dss::linker` pre-positions for D-LK9-2's `dss::link()` → `dss::linker::link()` rename (co-landed at cycle 2). Windows-safe path encoding via `pathForDiag` (try `generic_string()`, fall back to `u8string()` on MSVC throw — post-fold #2). Format-blindness invariant: writer never reads `image.format`. |
| LK10 cycle 2 | Driver pipeline wiring ✅ landed 2026-05-30 (commit `a001c9e`) | Wires `Program::compileFiles` / `compileDirectory` through the full pipeline: parse → analyze → HIR → MIR → LIR → regalloc → rewrite → 2-addr-legalize → callconv → assemble → link → writeImage. New substrate: `src/program/target_spec.{hpp,cpp}` (`TargetSpec::parse` returns `std::expected<TargetSpec, TargetSpecError>` with 5 remediation-distinct error variants; `outputExtension(ObjectFormatSchema const&) -> string_view` closed switch over `ObjectFormatKind × objectType`) + `src/program/compile_pipeline.{hpp,cpp}` (`compileSingleUnit` kernel with per-tier `errsEntry / tierClean` snapshot discipline). **D-LK9-2 ✅ closed** (`dss::link` → `dss::linker::link` rename across 30 call sites in 9 files; clean break, no forwarder). `compileProject` fail-loud `D_PlanNotLanded` until plan 06 ships. New driver-tier diagnostic codes: `D_InvalidTargetSpec=0xD007` / `D_SchemaLoadFailed=0xD008` / `D_PlanNotLanded=0xD009`. **Removed `src/gen/`** (TargetWindowsX86_64 demo-gui PE writer — 425 LOC superseded by config-driven LK1/LK2/LK3 walkers); `--demo-gui` flag stripped from `Program::run`; integrated_tests rewritten to test ready-message + unrecognized-flag-rejection. **7-agent review folded heavy**: 7 missing K_* arms in `diagnosticCodeName` (CRITICAL silent-failure F1 — codes printed as `[Unknown]`); CU + SemanticModel + LoadResult ConfigDiagnostic draining into reporter (HIGH F1+F2 — `D_FileNotFound` from `UnitBuilder::addFile` previously never reached stderr); per-target diagnostic context stamping (`[target=<spec>]`); `recursive_directory_iterator` mid-scan error_code surfacing (F6); `D_InvalidTargetSpec` split via `expected<>` error variant (F7); whitespace rejection in TargetSpec halves (test-analyzer FOLD-NOW); `K_ImageWriteParentMissing` → `D_FileNotFound` for driver-side mkdir failure (F2); severity prefix in stderr drain (F3); `std::any_of` extension match (simplifier SIMPLIFY-NOW); Unknown-arm test; non-existent-source-file test. **Post-fold review (2026-05-30 `a001c9e`-post)** added two more codes (`D_OutputDirCreateFailed=0xD00A` + `D_DirectoryScanFailed=0xD00B`, splitting the over-loaded D_FileNotFound), hoisted `copyDiagnostics` to `compile_pipeline.hpp` (eliminating the inline drain duplicate), fixed `useAsCwd` ec swallow + anchor-typo (D-LK10-4 → D-LK10-2 in test comments). Cycle 2 acceptance scope: WIRING (output dir exists + driver routes through every IR tier) — NOT bytes on disk yet, because plan 12 ML7 cycle 2 (`arg` virtual-op lowering) + plan 13 AS cycles (`load`/`store`/`ret` operand-kind variants) haven't closed. The e2e tests soft-tighten to byte assertion when those gaps close (anchored D-LK10-2). 13 program tests + 11 target_spec tests; 124/124 ctest green. |
| ~~LK10 cycle 3~~ | ✅ **LANDED 2026-06-01** — `parseLspFlags` replaced by `parseCliArgs` (`src/program/cli_args.{hpp,cpp}` + 37 gtest cases). Rich CLI dispatcher in `Program::run`: `--compile <files>...`, `--transpile <files>...` (fail-loud → plan 10), `--directory <path>` (+ `--recursive` / `--no-recursive`), `--project <file>`, `--lsp`, `--target=<spec>` (repeatable; both space + equals forms), `--language=<name>`, `--config=debug\|release` (plan 22 wiring slot), `--warnings-as-errors`, `--suppress=<code>` (D-LK10-7 closure), `--help` / `-h`. `InputResolver::Mode {Recursive, Flat}` hoisted to `src/program/input_resolver.{hpp,cpp}` (D-LK10-1 closure). 7-agent post-fold heavy: silent-failure F1 (suppress=Unknown sentinel reject), F2 (empty/`-` positional reject), F3 (NoModeSelected with options-but-no-mode), F4 (`=`-form for every value-bearing flag), F5 (validateFiles append contract docs), F7 (ENOENT vs filesystem-error split: D_FileNotFound vs D_DirectoryScanFailed). Hoisted `Program::compileFiles` + `Program::compileDirectory` policy-aware overloads accepting `DiagnosticReporter::Config`. New `Program::transpile` fail-loud method citing plan 10. 126/126 ctest. — (closed) |
| LK10 full | 🟡 Cross-OS corpus EXECUTION ✅ 2026-06-09; strict no-system-linker hermetic gate ⏳ | The hermetic-acceptance gate: build c-subset corpus on a CI runner with NO system linker installed; produced binary runs + prints expected output on Linux/macOS/Windows × x86_64/ARM64. ✅ **Windows half achieved 2026-06-02** — `examples/c-subset/hello_puts/` compiles + links via DSS only (no system linker), produces a PE that imports kernel32 + msvcrt, runs under Windows loader, prints "hello\r\n" + exits 42 (asserted byte-for-byte). ✅ **Cross-OS EXECUTION achieved 2026-06-09** — the Linux (x86_64 + native `ubuntu-24.04-arm`) + macOS (`macos-latest` Apple Silicon arm64) CI legs build AND RUN the corpus green (`#include puts("hello")` → stdout + exit; `D-LK10-ENTRY-ARM64` ✅ CLOSED + CI-verified; the POSIX `runBinary` arm landed at D-LK10-ENTRY-POSIX-RUN-HARNESS ✅). **Remaining for "full"**: the strict no-system-linker-INSTALLED hermetic CI *configuration* (the runners run the binaries, but a clean-room-no-`ld` gate isn't a dedicated job yet) + Windows-ARM64 (no runner) + macOS-x86_64 (no leg). The cross-OS expansion is structural infrastructure now, not architectural. |
| LK11| **Cross-CU linking** (v1.x)                       | Lifts the v1 single-CU-per-image assumption. **Symbol-namespace foundation pre-landed (D-LK4-3 ✅ 2026-06-04):** the linker now keys by the compound `(cuId, SymbolId)` and fail-louds `K_CrossCuMergeUnsupported = 0x8010` on N>1 modules; LK11 fills in the actual symbol-table MERGE + weak-vs-strong resolution this fail-loud guards. **LK11a ✅ (cycle 16): the cross-CU DEFINITION merge + weak-vs-strong landed (`resolveCrossCuSymbols` → `LinkedImage::resolvedGlobalDefs`; `K_SymbolRedefinedAcrossUnits` + `K_CrossCuImageEmitDeferred` added; per-module `AssembledModule::symbols` name table). Cross-CU REFERENCE resolution ALSO landed (extern → sibling def → `LinkedImage::resolvedCrossCuRefs`; undefined reloc → `K_SymbolUndefined`) — closes D-LK11-EXTERN-VS-DEF-PRECEDENCE. LK11b ✅ (cycles 18–19): merged-image byte emission (D-LK11-IMAGE-EMIT-MERGE, cycle 18) + the multi-CU build driver + cross-CU CALL via the rodata thunk slot + PE `.reloc` ASLR base relocations (D-LK11-MULTI-CU-DRIVER, cycle 19 — the FIRST runnable cross-CU-call binary, `examples/c-subset/cross_cu_call` exit 42). Remaining LK11b: ELF/Mach-O exec thunk emission (D-LK11-ELF-MACHO-CROSSCU-THUNK-EMISSION, format-triggered) + extern-import dedup (D-LK11-EXTERN-IMPORT-DEDUP).** Engine's symbol-table merge (§2.2 flow item 1) is real; cross-CU symbol resolution + relocation application; CU-boundary diagnostics under `K_CrossCu*`. **Couples with** [`08-compilation-unit-plan`](./08-compilation-unit-plan%20-%20tbd.md) **CU6**. v1.x — triggers per §2.12. |

Substrate tier (5-agent review) for LK4 (format-blind engine + `ObjectFormatSchema` substrate) + LK7 (codesign-hook contract). Feature tier for per-format JSON onboarding (LK1/LK2/LK3/LK8/LK9 — each PR ships one `*.format.json` + any engine support its schema declarations need).

---

## 3.1 Deferred-items registry (LK)

Mirrors plan 12 §3.1 / plan 13 §3.1. Every deferred item has an explicit owner + trigger. Items struck through when their closing commit lands.

| # | Deferred item | Why deferred (not a silent gap) | Owner / closure | Trigger |
|---|---|---|---|---|
| ~~**D-LK10-ENTRY** ★ **PRIORITY**~~ | ✅ **STAGE 1 CLOSED 2026-06-02 — the FIRST DSS-PRODUCED BINARY THE OS ACTUALLY RUNS.** Linker-emitted synthetic `_start` / program-entry trampoline + process-exit (the runnable-binary spine). Full design in §2.13. Pre-closure: the executable walkers pointed `e_entry` / `AddressOfEntryPoint` / `LC_MAIN` **directly at the user entry function**, which (a) executed its `ret` epilogue against a stack with no return address → SIGSEGV/AV, and (b) never issued the OS process-exit request. The closed fix: LIR-driven trampoline (NOT the rejected post-assembly byte-patch workaround, NOT the rejected hand-coded-bytes-in-emitter shape with per-CPU if-arms); per-platform exit mechanism is bucket-1 data (Linux `exit_group` syscall / Windows `kernel32!ExitProcess` by-name import — no CRT / Mach-O `exit`), the emitter is one universal bucket-2 arm that emits LIR ops which the existing assembler encodes. **Cycle plan: Slice A ✅ LANDED 2026-06-02 (`756c5ea` + `ff7b76d` audit fold + `a966e68` 3rd-order fold) — `syscall` + `call_indirect_via_extern` + `unreachable` encoding on `x86_64.target.json` (D-LK10-ENTRY-X86-NO-MODRM-VARIANT closed; ARM64 deferred D-LK10-ENTRY-ARM64). Slice B ✅ LANDED 2026-06-02 (`7425905` + `88a8081` audit fold) — `ExitMechanism` enum + `ProcessExit` POD vocabulary + `processExit` + `entryCallingConvention` fields on ObjectFormatSchema + JSON loader + cross-field validate() (isExecFlavor gate + paired-field rule + universalFields[] wasm/spirv rejection + whitespace check); shipped PE Exec (ms_x64 / kernel32!ExitProcess) + ELF Exec (sysv_amd64 / exit_group=231 / SYSCALL `[15,5]`). **Slice C ✅ LANDED 2026-06-02 (`1a0b414` + `3541177` --output + audit fold) — `src/link/entry_trampoline.{hpp,cpp}` bucket-2 universal emitter; AssembledModule.imageEntryOverride; 5-walker integration via shared `resolveEntryFnIdx` helper; `tests/test_support/run_binary.hpp` CreateProcess Windows arm; `RunnableBinaryExitFortyTwo` Stage-1 smoke spawning the .exe and asserting OS-reported exit code 42 — ★ the first DSS-produced binary the OS actually runs.** | **Stage 1 ✅ ACHIEVED 2026-06-02** (full A+B+C cycle landed; first runnable DSS binary). **Stage 2 NEXT**: plan 11 FF6 (string `.rdata` + multi/stack/variadic-arg ABI — needs ML7 stack-arg `L_StackPassedArg` closed). Stage 2 builds on Slice C's substrate unchanged — the trampoline emitter is the same; rodata walker + multi-arg LIR are the gates. | ✅ **CLOSED (Stage 1) 2026-06-02**. Remaining sub-anchors track Stage-1 cross-host parity + future-syscall edge cases: D-LK10-ENTRY-ARM64, D-LK10-ENTRY-MACHO-EXIT, D-LK10-ENTRY-POSIX-RUN-HARNESS, D-LK10-ENTRY-MAIN-IMPLICIT-RETURN, D-LK10-ENTRY-LIRTOMIRSENTINEL, D-LK10-ENTRY-SYSCALL-IMM64 (audit fold added), D-LK10-ENTRY-MODULE-COW (audit fold added). |
| ~~D-LK10-ENTRY-ARM64~~ | 🟢 **IMPL DONE 2026-06-06 (V2-1) — committed, push HELD for CI-green-under-QEMU (user-owned DSS.DevOps).** The trampoline emitter stayed bucket-2 zero-change, BUT the plan's "JSON-only / zero code changes" premise was DISPROVEN (plan-lock): the `fixed32` walker had NO immediate or memory-addressing path, so a runnable ARM64 binary through the real pipeline needed a genuine ENCODER substrate. **Landed**: 4 new `EncodingSlotKind`s — `Imm16` (MOVZ wide-immediate, bits 5..20), `Imm9` (unscaled LDUR/STUR signed offset, 12..20), `Imm12` (ADD/SUB-immediate, 10..21), `MemBaseNoScale` (width-0 marker for the AArch64-unscaled memory-base operand) — each in lockstep across the enum/table/static_assert/slotShapeFor/isSymbolBearingSlot; the fixed32 encoder gained `ImmInt`/`MemOffset`/`MemBase` arms (range-checked + scale-checked, fail-loud `A_ImmediateOperandOutOfRange`); the `validate()` fixed32-operand gate relaxed to admit those kinds. Opcodes added to `arm64.target.json`: `syscall` (SVC #0 = 0xD4000001), MOVZ `mov`-imm variant, `load`/`store` (LDUR 0xF8400000 / STUR 0xF8000000), `add`/`sub`-immediate variants (0x91000000 / 0xD1000000), `frame_load`/`frame_store` pseudo-ops; the `bl` opcode RENAMED to the engine's generic `call` verb (so `opcodeByMnemonic("call")` + `MnemonicSlot::Call` resolve it — matching `mov`/`add`/`xor` convention). `processExit` (mechanism=syscall, number=94, gpr=x8, bytes=[1,0,0,212]) + `entryCallingConvention=aapcs64` on `elf64-aarch64-linux-exec.format.json`. Harness: `run_binary` `launcherPrefix` + `examples_runner` cross-arch `qemu-<arch>` gate (`currentHostArch` + `findOnPath` + manifest `emulator` field) → run-where-present / SkippedCrossHost. **Proof**: exact 20-byte `_start` trampoline byte-pin (`Arm64TrampolineEmitsExactExitSequence`) + MOVZ/LDUR/STUR/ADD-imm byte-pins + MOVZ round-trip + Imm16/Imm9 range fail-loud pins + `examples/c-subset/arm64_exit` (exit 42, compiles to AArch64 ELF on every host, runs under qemu-aarch64 where present). 205 ctest. **The `call_indirect_via_extern` GOT/PLT macro-op (ADRP+LDR+BLR) is NOT needed for the syscall exit path → still deferred** (first ARM64 dynamic-import corpus). **Execution verification is the user-owned DSS.DevOps half (qemu-aarch64 on the ubuntu runner) — push held for the green flag.** | **First ARM64 target needs a runnable binary** ✅ (arm64_exit) | ✅ **PUSHED + CI-VERIFIED 2026-06-08** — the native `ubuntu-24.04-arm` leg runs the arm64 corpora GREEN (real arm64 hardware, not QEMU; the user chose native over the QEMU-on-x86 path). The "held for DevOps green flag" note is superseded. |
| ~~D-LK10-ENTRY-ARM64-CONTROL-FLOW + NON-LEAF~~ | ✅ **CLOSED 2026-06-08 — BOTH halves done.** NON-LEAF/x30 closed 2026-06-08 (`D-LK10-ENTRY-ARM64-NONLEAF-LINK-REGISTER`, commit dbf84b0 + the byte-pin 0e57db4). CONTROL-FLOW closed THIS cycle: `cmp` (SUBS XZR — flags), `jcc` (B.cond imm19 + B imm26, a target-blind 2-WORD MACRO via `fixedWords`/`wordIndex`), `jmp` (B imm26), `setcc` (CSET), `zext` (ORR), `alloca`→frame-relative `lea` (ADD Xd,sp,#imm12) all land. Block-relative branch offsets are filled by the now-real `D-AS3-BLOCK-REL-IMM19/26` resolver (`src/asm/asm.cpp`: `disp=(target−patch)>>2`, no +4 bias, bit-field RMW, alignment + signed-range fail-loud); cond codes by `D-AS3-COND-CODE-ARM64` (condCodeEncoding table + the generic `condBitPos`/`condInvert` knobs). The `int n` local exercises `load`/`store` at runtime (debug pipeline keeps the alloca). **Proof**: `examples/c-subset/arm64_control_flow/` (`while (n<42) n=n+1; return n;` → exit 42, a genuine x86_64+arm64 cross-target differential) PROVEN LOCALLY under qemu-aarch64 (exit 42 ×3, while-loop + fused-if), plus host-independent byte-pins (`Arm64ControlFlowSkeletonEncodesCmpBcondBWithResolvedOffsets` 6-word + `CsetEncodesInvertedCondAtBits12` + `ZextEncodesOrrW` + `FrameRelativeLeaEncodesAddImm12`). §A.5 SATISFIED: the native `ubuntu-24.04-arm` CI leg CONFIRMED green — run 27170873148, 2026-06-08. | ✅ done (cmp/jcc/jmp/setcc/zext + non-leaf x30 + the runtime corpus) | ✅ **CLOSED 2026-06-08** (runtime CONFIRMED green on native ubuntu-24.04-arm CI, run 27170873148) |
| D-LK10-ENTRY-ARM64-WIDE-IMMEDIATE | **The MOVZ+MOVK CONSTANT arm ✅ CLOSED 2026-06-11 (FC3.5 sweep-c3); the FRAME-offset forms remain open (below).** CLOSED: wide inline integer constants no longer hit the MOVZ imm16 ceiling — `lowerConst` (mir_to_lir.cpp `materializeInlineIntConst`) splits them into the MINIMAL MOVZ + MOVK ladder as SEPARATE LIR insts. **Adjudication recorded**: lowering-side magnitude selection over a multi-word encoding variant, because an operand's VALUE cannot guard variants and a fixedWords template cannot express the per-word imm16 SHIFT without 3 new slot kinds; separate insts reuse the existing mov/Imm16 machinery and give the N-step ladder for free. NEW arm64 opcodes `movk_lsl16/32/48` (X-base 0xF2800000|hw<<21; `requires2Address` — the ISA reads Xd's other bits; ONE width-ABSENT X-form each — the MOVZ seed zeroes all 64 bits, so X-MOVK is value-correct at width 32 too, and chunks 2/3 never exist there). Capability-probed by mnemonic presence: x86's `mov r, imm32` keeps ONE inst (pinned `X64WideConstKeepsSingleMovNoLadder`); a movk-less fixed32 target keeps the encode-time `A_ImmediateOperandOutOfRange` (the ladder is ALL-OR-NOTHING per needed chunk; the encoder ceiling test remains the hand-built-LIR backstop). **The ceiling moved honestly**: the closed range is the int32 INLINE CARRIER (`LirOperand.immInt32`) — ≤0xFFFF single-MOVZ byte-identical, positive int32 → 2-op, width-32 U32 → ≤2-op, negative width-64 → the full sign-extended 4-op ladder (a MOVN-seeded 2-op chain = deliberate peephole-class non-goal). 48/64-bit literals above the carrier stay at the pre-existing LiteralPool dead end ON BOTH TARGETS (no encoder consumes LiteralIndex; x86 declares no imm64 form either) — that residue belongs to the carrier, not arm64. Pins: 5 LIR shapes (wide-chain RED-on-disable demonstrated+restored; negative 4-ladder; narrow single; width-32 2-chunk; x86 capability boundary) + movk byte-pins (hw=01/10/11 incl. x21 high-reg Rd). Corpus `examples/c-subset/wide_immediate/` (196608/100000/296608 — chunk0-zero + both-chunks + a wide 64-bit COMPARE constant; chunk drift = exit-divergent) exit 42 WITNESSED PE native + WSL x86_64 + qemu arm64; Mach-O compile-arm. **STILL OPEN (frame forms, untouched)**: LDUR/STUR ±256 (`Imm9`) → scaled LDR/STR imm12; ADD/SUB-imm 0..4095 (`Imm12`) → shifted imm12<<12; the `alloca` frame-lea ceiling stays pinned by `Arm64Encoder.FrameLeaOffsetWiderThanImm12FailsLoud` (2026-06-08, RED-on-disable demonstrated). | First ARM64 program whose FRAME exceeds the single-word forms (frame >4095 bytes / spill offset beyond ±256). | First out-of-range `A_ImmediateOperandOutOfRange` from a real ARM64 compile (frame forms — the constant arm is closed). |
| D-AS5-ROUNDTRIP-MEMORY-OPERAND-COVERAGE | **The disassembler round-trip oracle does not cover memory operands (`MemBase`/`MemOffset`) — so ARM64 `load`/`store`/`add-imm` rely on byte-pins, not round-trip.** `asm_test_support.hpp::roundTripVerify` rejects `MemBase`/`MemOffset`/`BlockRef` LirOperandKinds (pre-existing); and the disasm `fixed32_disasm.cpp::windowFor` deliberately omits `Imm9`/`Imm12`/`MemBaseNoScale` (no consumer). Extending the oracle to cover memory operands (+ a signed-`Imm9` sign-extension in disasm extract) would let LDUR/STUR/ADD-imm round-trip too. Today the exact-byte pins are the proof (strict, red-on-disable). | A 2nd consumer of fixed32 memory-operand round-trip, OR a target whose memory encoding isn't byte-pinnable. | First request for memory-operand round-trip coverage. |
| ~~D-LK10-ENTRY-X86-NO-MODRM-VARIANT~~ | ✅ **CLOSED 2026-06-02 — no substrate gap.** Code-architect verified at the d642655 audit: `ret` (`x86_64.target.json:232`) already uses `"guard": {"operandKinds": []}` + `"template": {"opcode": [195]}` shape with no `rexW`, no `resultSlot`, no `wires`. The x86-variable walker emits `tmpl.opcodeBytes` verbatim then exits because `hasModRm` is false and there are no wires. Adding `syscall` to `x86_64.target.json` with `"opcode": [15, 5]` and the same zero-operand guard fits identically. **No walker change required — JSON-row addition only.** Anchor closed before treating as a real gap; condition "verify first" was the right move per code-architect's earlier review. | — | — |
| ~~D-LK10-ENTRY-MAIN-IMPLICIT-RETURN~~ (early-anchor row, superseded) | (duplicate row — kept as strike-through for blame continuity. See the un-struck-through CLOSED entry for **`D-LK10-ENTRY-MAIN-IMPLICIT-RETURN`** below for the full closure write-up. Cite by anchor name rather than row number — markdown row positions are not stable across future plan edits.) | — | — |
| D-LK10-ENTRY-LIRTOMIRSENTINEL | **Sentinel-`MirInstId` parallel vector for synthetic Lir construction.** `assemble()` enforces `lirToMir.size() == lir.instCount()` at entry (`src/asm/asm.cpp:167`). The trampoline emitter constructs a synthetic Lir with no MIR provenance, so it must pass a parallel `vector<MirInstId>` of sentinel `MirInstId{}` entries (default-constructed, value 0) matching the Lir's instruction count. When a 2nd synthetic-Lir caller arrives (e.g. a different linker-synthesized helper, a JIT path), factor `assembleHandBuilt(Lir const&, TargetSchema const&, DiagnosticReporter&)` wrapper that supplies the sentinel internally — eliminates the boilerplate at every call site. | **2nd synthetic-Lir caller arrives.** | First non-D-LK10-ENTRY linker-synthesized helper function. |
| D-LK10-ENTRY-MACHO-EXIT | **Mach-O macOS exit mechanism — syscall vs. libSystem-import decision.** macOS programs traditionally exit via `_exit` in `libSystem.B.dylib` (which itself wraps the BSD `exit` syscall `0x2000001`). Apple's "no-CRT static binaries" path uses the raw BSD syscall directly. Choice is a trade-off: by-name-import via libSystem mirrors PE+ExitProcess (uniform across the two-out-of-three hosted platforms) but ties the binary to dyld's libSystem loading; raw BSD syscall is hermetic but Apple discourages it ("syscall numbers are unstable across macOS versions"). Plan-side decision deferred to the first Mach-O runnable-binary trigger; both shapes are expressible in the existing `ProcessExit` vocabulary. 🟢 **TRIGGER FIRED + PROMOTED TO ACTIVE 2026-06-08** (user: "add Apple Silicon to the dss cycles"): the CI `macos-clang-release` leg = `macos-latest` = Apple Silicon (arm64), a real runnable arm64-darwin host, so the "first Mach-O executable that needs to actually run" trigger is met. **DECISION: libSystem `_exit` by-name import** (over the raw BSD `exit` syscall `0x2000001`) — the `macho64-arm64-darwin-exec` binary is already on the dyld/chained-fixups dynamic-linking path (it loads libSystem for FFI anyway), so a libSystem `_exit` import is consistent + Apple-blessed + dodges Apple's "BSD syscall numbers are unstable across macOS versions" warning, and mirrors PE+ExitProcess for cross-platform uniformity. Co-lands with D-LK6-14 (the arm64 chained-fixups CPU-type dispatch in `macho.cpp`). The shared `D-LK10-ENTRY-ARM64-FFI-ISEL` (the call-with-result isel) is ✅ CLOSED 2026-06-08 (the `lea`/GlobalAddr lowering, D-AS4-3; proven on Linux-ARM64 via `extern_call_elf`) — NO LONGER a macOS blocker (the GlobalAddr→lea isel is arch-shared). De-risk-first via a `macho64-arm64-darwin-exec` exit-42 smoke that RUNS (not SKIPs) on the macos CI leg (self-confirms the runner is arm64). **§B-2 SCOPE LOCKED 2026-06-08 (user chose the FULL libSystem `_exit` import mechanism over a de-risk-spine-first option — accepting the larger scope incl. the arm64-Mach-O FFI substrate).** The ad-hoc CODESIGN BUILDER prerequisite is now LANDED (`D-LK7-ADHOC-CODESIGN-MACHO` 🟡 byte-proven, commit 983fff1 — auto-fills the LC_CODE_SIGNATURE over the final bytes on the dynamic path). **LOCKED PLAN (from a Step-3 understand):** (1) **arm64-Mach-O FFI substrate** — the import call-site is the real gap: `macho.cpp:1491-1498` emits x86 `FF 25` `__stubs` UNCONDITIONALLY (no arm64 arm) AND `arm64.target.json` has NO `call_indirect_via_extern`. Build an `emitArm64MachoStub` (ADRP x16,sym@GOTPAGE → LDR x16,[x16,sym@GOTPAGEOFF] → BR x16) + route the call via a **direct BL to the stub** (the `direct-plt` model the ELF arm64 path already uses via `emitArm64PltStub`), NOT an x86-style `call [mem]` — agnostic, reuses the fixed32 multi-word substrate. (NB: the chained-fixups PAYLOAD/binding half is already wired into `encodeExecDynamic` at macho.cpp:1538/1549-1577 — `D-LK6-14-INTEGRATION-PAYLOAD`/`-GOT-SLOTS` CLOSED; the stub/call-site is the sole remaining FFI gap.) (2) **processExit = ByNameImport** `{importMangledName:"_exit", importLibraryPath:"/usr/lib/libSystem.B.dylib"}` — the generic trampoline (entry_trampoline.cpp:422-468) appends `_exit` as a synthetic ExternImport → rides `encodeExecDynamic` (import present) → codesign auto-fills; ZERO trampoline-code change. (3) **`src/dss-config/object-formats/macho64-arm64-darwin-exec.format.json`** — clone the x86_64-darwin-exec; cputype `0x0100000C` (CPU_TYPE_ARM64), cpusubtype 0; ADD `processExit`(ByNameImport _exit) + `entryCallingConvention:"aapcs64"` + `image.codeSignature`{adhoc/sha256/**pageSize 16384** (Apple-Silicon native page)/identifier} + loadDylibs libSystem. (4) **runnable corpus** `examples/c-subset/macho_arm64_exit/` (`int main(){return f(42,0);}`-style exit-42 via the `_exit` import; target `{spec:"arm64:macho64-arm64-darwin-exec", runOn:["darwin"], emulator:""}`) — the examples_runner is ALREADY darwin/arch-capable (examples_runner.cpp:399-446, spawns natively on macos-latest=arm64) → **§A.5 RUNTIME closure: it must actually RUN + AMFI-accept the ad-hoc signature + exit 42 on the macos-latest leg (NOT byte-pin-only).** + host-independent byte-pins (the arm64 stub bytes, LC_MAIN entryoff→trampoline, codesign over final bytes) for the non-darwin legs. Substrate/new-backend cycle → plan-lock GO (the agnosticism crux RESOLVED by the ELF `emitPltStub` precedent — a localized per-cputype switch in the format writer is the sanctioned §D realization tier; it also caught + this cycle FIXED a confirmed latent Mach-O miscompile [mislabeled `indirect-slot` → `FF 15` would deref the stub code → SIGSEGV; flipped BOTH darwin formats to `direct-plt` + a coherence fail-loud guard + the trampoline dispatch made format-driven via the shared `externCallUsesIndirectShape`]). 🟢 **IMPL DONE 2026-06-08 — RUNTIME-PENDING the macos-latest leg.** Landed: `machoStubSizeFor`/`emitMachoStub`/`emitArm64MachoStub` (ADRP x16→LDR→BR, 12 B, mirroring `emitArm64PltStub`; x86 stub byte-identical) + `macho64-arm64-darwin-exec.format.json` (CPU_TYPE_ARM64, processExit=ByNameImport `_exit`, `apple_arm64` cc [closes D-FF3-5], direct-plt, ad-hoc codeSignature) + the `macho_arm64_exit` corpus + 6 host-independent byte-pins (RED-on-disable DEMONSTRATED: BR x16→x0 fails the stub pin). 212/212 ctest, agnostic, independent review + self-audit VERIFIED-CLEAN (no over-claim). **NOT YET runtime-closed** — the corpus RUNS only on the `macos-latest` (Apple Silicon) leg post-push. ⚠️ **RUNTIME RED 2026-06-09 (the §A.5 catch WORKED — byte-pins green, review+audit clean, but the real macOS load FAILED):** the `macos-latest` leg's `macho_arm64_exit` corpus FAILED — `posix_spawn(...) failed: rc=88` = **`EBADMACHO` ("Malformed Mach-O file")**: the macOS arm64 kernel rejects the binary at LOAD (never runs — NOT an AMFI/signature or codegen-runtime issue). **ROOT CAUSE (confirmed):** `macho.cpp` hardcodes `kPageSize = 0x1000` (4 KiB) at lines 902 + 1166 (the segment vmaddr/fileoff/vmsize alignment + the `vmaddr%page == fileoff%page` congruence), but **Apple Silicon requires 16 KiB (0x4000)-aligned segments** — so the arm64 segments are mis-aligned → EBADMACHO. (The codesign CD `pageSize` was correctly 16384; the SEGMENT page size is a separate, still-4K constant.) **FIX (next cycle, agnostic):** make the segment page size CONFIG-DRIVEN — a per-format `segmentPageSize` (16384 for `macho64-arm64-darwin-exec`, 4096 for x86_64 / ELF / PE, preserving x86 byte-identity); `macho.cpp` reads it instead of the hardcoded `kPageSize` at both sites; the byte-pins (`test_macho_arm64_exit`) update for the 16K-shifted stub/got VAs; investigate whether any OTHER arm64-Mach-O structural requirement (LC_BUILD_VERSION/min-os, segment/section flags) also trips the kernel; re-confirm GREEN on the macos-latest leg. **FIX LANDED 2026-06-09 (this cycle):** config-driven **`image.segmentPageSize`** (16384 arm64-darwin / 4096 default → x86 byte-identical) read at BOTH `macho.cpp` kPageSize sites; `__text.virtualAddress` 0x100001000→**0x100004000** (16K mmap congruence; textFileOff=alignUp(headerAndCmds≈864,16384)=0x4000); **+ LC_BUILD_VERSION** (`image.buildVersion {macos, minOs 11.0}` + a shared `appendBuildVersionCommand` 24-byte emit on the dynamic path) — the plan-lock's high-confidence SECOND blocker, since modern dyld (macOS 11+/Apple Silicon) rejects a main executable with no platform load command, and NO DSS Mach-O had ever loaded on macOS (zero prior evidence a platform-less binary loads); `codeSignature.pageSize` 16384→**4096** (the conventional code-hash page Apple uses on both arches — DECOUPLED from the VM segment page, which the prior cycle had conflated); validate() **power-of-two** + **text-VA congruence** (rejects the exact 0x100001000-under-16K bug at schema-load) + the two new fields added to the .o "no image block" rejection; static path fails loud on `buildVersion` (anchored D-LK10-ENTRY-MACHO-STATIC-BUILD-VERSION). Strict tests: **`Arm64SegmentsAre16KAligned`** structural red-on-disable pin (DEMONSTRATED: revert→4096 → __DATA_CONST.vmaddr 0x100005000 / __LINKEDIT fail) + `Arm64ExecEmitsBuildVersionMacOs` byte-pin (cmd 0x32, cmdsize 24, PLATFORM_MACOS, minos 0x000B0000) + `X86DarwinExecDefaultsTo4KSegmentPageSize` (knob-that-lies guard) + 2 validate fail-loud pins. Independent **plan-lock GO-WITH-FIXES** (LC_BUILD_VERSION = MUST-FIX #1, folded) + **code-review NO CRITICAL/HIGH/MEDIUM**. 212/212 ctest, agnostic (page+platform read as DATA, zero `if(arch)`), anchor-guard OK (187). **`D-LK10-ENTRY-MACHO-EXIT` ✅ CLOSED 2026-06-09 — RUNTIME-CONFIRMED on real Apple Silicon** (commit `e2cabd0`, CI run **27182845048**, the `macos-clang-release` = `macos-latest` = arm64 leg GREEN): the `examples/c-subset/macho_arm64_exit` corpus (ctest **#194**) — which COMPILES → SPAWNS → asserts exit 42 — **Passed** (the prior cycle FAILED there with `posix_spawn rc=88`/EBADMACHO; now `100% tests passed, 0 failed out of 212` on macos-latest), so the arm64 Mach-O binary genuinely LOADED on Apple Silicon (dyld accepted the 16 KiB-aligned segments + LC_BUILD_VERSION; AMFI accepted the ad-hoc signature) and the libSystem `_exit` trampoline ran → exit 42. This is the §A.5 cross-target RUNTIME closure (real execution, not byte-pins) — the macOS-ARM64 runnable-exec arc is COMPLETE. (windows-msvc leg cancelled on a ~29-min Build-step infra TIMEOUT — no compile error, Test skipped — redundant with the local Win-MSVC 212/212; the other 4 test legs incl. both arm64 legs + ASan/UBSan all GREEN.) | ✅ **CLOSED 2026-06-09 (e2cabd0) — runtime-confirmed on macos-latest (run 27182845048, corpus #194 → exit 42)** | ✅ **CLOSED 2026-06-09 — Apple Silicon RUNS the corpus → exit 42 (the §A.5 runtime witness; macos-latest leg green).** |
| ~~D-LK10-ENTRY-POSIX-RUN-HARNESS~~ | ✅ **CLOSED 2026-06-02 — POSIX `run_binary` arm landed alongside `examples/` ctest harness.** Implementation: `tests/test_support/run_binary.hpp` POSIX path uses `posix_spawn` (cross-Linux+macOS — avoids the `vfork` deprecation on macOS) → `waitpid(WNOHANG)` poll loop with deadline. Polling interval capped at `min(remaining, 10ms)` so short-running examples (single-millisecond exits) don't pay the full timeout; long timeouts adapt naturally. Exit-code translation: `WIFEXITED` → `WEXITSTATUS`; `WIFSIGNALED` → `128 + WTERMSIG` (POSIX shell convention) + diagnostic string naming the signal. Pre-spawn `chmod 0755` makes the harness robust to the linker's default 0644 file mode (no caller-side `posix_chmod` boilerplate). Includes added: `<errno.h>` + `<signal.h>` + `<spawn.h>` + `<sys/stat.h>` + `<sys/wait.h>` + `<unistd.h>` + `<thread>` (for `sleep_for` in the poll loop). `extern char** environ` declaration for the spawn environment. The arm cross-compiles cleanly on Linux + macOS; runtime correctness pin lands when CI gains Linux/macOS exec runners (the examples corpus's PE-Exec entries skip on POSIX hosts per the `runOn` array, so today the POSIX arm is dead code on the build host — but a future ELF-Linux-exec example will fire it). | — | — |
| D-LK10-ENTRY-SYSCALL-IMM64 | **`mov syscallNumGpr, imm64` for syscall numbers > 0x7FFFFFFF.** Slice C's trampoline emitter currently lowers the syscall-number load via `LirOperand::makeImmInt32(static_cast<int32_t>(syscallNumber))` — silently sign-extends to a NEGATIVE immediate when the value exceeds 0x7FFFFFFF. Linux exit_group (231) and macOS BSD exit (0x2000001) both fit signed 31-bit, so Stage 1 is safe. Anchored at the cast site in `entry_trampoline.cpp`. When the trigger fires: promote to `LirOperand::makeImmInt64` AND require the target schema to declare a `mov_imm64` opcode (the existing `mov` row uses `0xC7 /0 imm32`-sign-extended; the new variant would use `REX.W 0xB8+rd io` 10-byte form). | **First syscall number > 0x7FFFFFFF** lands in shipped JSON (no current OS / arch combination requires this; future OS-specific reserved-range numbering MIGHT). | First processExit JSON declaring such a syscallNumber. |
| D-LK10-ENTRY-MODULE-COW | **Copy-on-write substitute for the per-link AssembledModule copy.** The linker hook at `linker.cpp` copies the input module locally (`moduleCopy = inputModule`) when the trampoline emitter is invoked — full vector copy of `functions[]` + `externImports[]`. For large modules this is a hot-path allocation per link. Today's modules are small (single function for the Stage-1 smoke); the copy is irrelevant. Anchored at the assignment site. When the trigger fires: replace with a `vector::insert` overlay or a move-in API where the caller forfeits the input. | **First multi-MB AssembledModule that trips link-time perf measurably** (profiled, not speculated). | First profiled link with measurable per-MB copy overhead. |
| ~~D-LK10-OUTPUT-PIPELINE-E2E~~ | ✅ **CLOSED 2026-06-02 — source-agnostic 3-stream parallel fold.** Closure: 2 e2e disk-emit tests in `tests/program/test_compile_pipeline.cpp` (`OutputFlagSingleTargetPlacesArtifactFlat`, `OutputFlagMultiTargetPlacesArtifactsInFormatSubdirs`). Each test asserts BOTH the expected layout (`fs::exists(outDir/<binary>)` for single-target, `fs::is_directory(outDir/<formatName>/)` for multi-target) AND the negative-arm absence (`EXPECT_FALSE(fs::exists(...))` on the OTHER ternary-arm's path). A regression flipping the ternary at `program.cpp:262-264` would silently route artifacts to the wrong layout — both pins now catch it. | — | — |
| ~~D-LK10-ENTRY-MAIN-IMPLICIT-RETURN~~ | ✅ **CLOSED 2026-06-02 — source-agnostic 3-stream parallel fold.** Closure shape: source-agnostic per the standing rule — implementation has ZERO hardcoded language strings in shared HIR substrate. **Substrate**: new `DeclarationRule::implicitReturnZeroForFunctionNames: vector<string>` field on `semantic_config.hpp`; JSON loader arm in `grammar_schema_json.cpp` with fail-loud negative paths (non-array / non-string-element / empty-string-element all emit `C_InvalidSemantics`); 3 loader-negative tests in `tests/core/test_grammar_schema.cpp`. **Language config**: c-subset's `topLevelDecl` declaration in `src/dss-config/sources/c-subset.lang.json` declares `["main"]` — the C99 §5.1.2.2.3 rule lives WHERE C is defined, not in shared substrate. Other languages (Pascal `program`, Rust `fn main`) declare their own entry-fn names in their own `.lang.json`. **HIR lowering**: shared helper `maybeAppendImplicitReturnZero` in `cst_to_hir.cpp` invoked from BOTH `lowerFunctionDecl` (dedicated-rule path, e.g. toy) AND `lowerFunction` (kindByChild dispatch — c-subset's path). The helper restricts insertion to integer return cores (Bool / I8..I128 / U8..U128 / Char / Byte) so a non-conformant `float main()` or `struct S main()` falls through to the verifier's loud-fail rather than getting a silently-wrong-typed synthetic return. **Substrate adds**: `pathTerminates` templated over `Source` interface in `hir_verifier.hpp` — both `Hir` (verifier) and `HirBuilder` (lowering) satisfy it; HirBuilder gained mid-build accessors (`kind`, `flags`, `children`, `ifThen`, `ifElse`, `switchArms`, `caseArmBody`, `caseArmIsDefault`). **Tests**: `MainWithoutExplicitReturnGetsImplicitReturnZero` + `MainWithExplicitReturnCompilesCleanly` (anti-double-insert pin) + `NonMainWithoutExplicitReturnStillFailsLoud` (scope-restriction pin) in `tests/program/test_compile_pipeline.cpp`. | — | — |
| ~~D-LK10-ENTRY-EXTERN-ENTRY-DIAG~~ | ✅ **CLOSED 2026-06-02 — source-agnostic 3-stream parallel fold.** Closure: new diagnostic code `K_EntryPointResolvesToExtern = 0x800D` in `parse_diagnostic.{hpp,cpp}` + added to unsuppressable list in `unsuppressable_codes.cpp` (array size 53→54). `resolveUserEntrySymbol` in `entry_trampoline.cpp` refactored from `optional<SymbolId>` to `EntryResolution{Status, SymbolId}` tagged result — caller `switch`es on status and emits the precise code per arm (NotFound→`K_SymbolUndefined`; ResolvedToExtern→`K_EntryPointResolvesToExtern`). Tests: `ExternAsEntryFiresDistinctDiagnostic` (pins the new code) + `NoMatchingEntryFiresGenericUndefined` (negative pin — `K_EntryPointResolvesToExtern` MUST NOT fire when the name simply doesn't exist). Helper `loadPeExecWithEntryPoint` constructs a PE-Exec format with a parameterized entryPoint by patching the shipped JSON (uniqueness-checked to fail-loud on future config drift). | — | — |
| D-LK10-ENTRY-SYNTH-PREFIX-SCHEMA | **Move synthesized-name prefix (`sym_` for ELF/PE; `_sym_` for Mach-O) to a format-schema field.** Currently the prefix is hardcoded at two sites: the walker's `resolveEntryFnIdx` call (Slice C audit fold helper takes it as a parameter) AND `resolveUserEntrySymbol` in `entry_trampoline.cpp` (inside the `for (auto const& fn : module.functions)` and `for (auto const& ext : module.externImports)` loops — cited by mnemonic rather than line number per the drift-resistant discipline that the prior reflow caught at `D-LK6-14-CHAINEDPTR-FORMAT-COUPLING`). Code-architect Q3 (Slice C audit fold): a third format with a different convention would force the two sites to diverge silently. The natural resolution co-lands with D-LK1-1 (real-name thread through HIR→LIR→AssembledFunction), at which point the synthesized-name convention is superseded entirely. | **D-LK1-1 closure** OR **a fourth format arrives with a third synthesized-name convention** before D-LK1-1 closes. | First synthesized-name prefix divergence between the trampoline emitter and walker. |
| ~~D-LK10-ENTRY-TRAMP-PROLOGUE~~ | ✅ **CLOSED 2026-06-02 — first-CLI-runnable-binary fix.** Slice C's bucket-2 trampoline emitter shipped without an ABI prologue; the synthetic `_start` issued `call user_entry` + `call [iat_ExitProcess]` at the entry RSP the kernel/loader handed in. The Stage-1 smoke test (`mov eax,42; ret` user fn) hid the bug — the leaf user fn never touched the stack, and the specific `ExitProcess` code path on that build didn't hit an `movaps` store with misaligned RSP. The first real CLI emission (`int42.c` through HIR→MIR→LIR→regalloc→callconv→ASM→link, producing a user fn with `sub rsp, 16; mov [rsp], r15; mov r15, 42; mov rax, r15; ...; ret`) reliably AV'd at runtime (`STATUS_ACCESS_VIOLATION` 0xC0000005) because Win64's RtlUserThreadStart-CALL leaves RSP ≡ 8 mod 16 + ExitProcess requires 32 bytes of caller-side shadow space — both unsatisfied. **Fix**: ONE new piece of schema vocabulary (`TargetCallingConvention.entryStackPointerBias: uint16_t`) — the RSP-bias mod-stackAlignment at process entry when this cc is the entry cc (Win64=8 because post-CALL; SysV ELF/Mach-O=0 because kernel JUMPs; ARM64=0 because BL doesn't push) + ONE shared helper (`alignedSizeWithBias(rawBytes, stackAlignment, entryBias)` in `src/lir/lir_callconv.hpp`) that derives the prologue's `sub sp, N` from the cc's primitives (shadowSpaceBytes + stackAlignment + entryStackPointerBias). Win64 → 40, SysV/ARM64 → 0; no derived constants stored anywhere. Validator at `src/core/types/target_schema.cpp` enforces `entryStackPointerBias < stackAlignment` (offset INTO the alignment quantum). Trampoline emitter at `src/link/entry_trampoline.cpp` emits the `sub sp, N` op as its first LIR instruction when adjustBytes > 0; the assembler's `sub` opcode encoding handles the per-CPU byte form. Zero `if (target == ...)` branches. Byte-pin tests on PE+ms_x64 + ELF+sysv_amd64 trampoline regression-guard the (Win64=`48 81 EC 28 00 00 00`) and (SysV=`E8 ...` directly, no prologue) shapes on every host. *(UPDATE 2026-07-02 c88 [[D-RUNTIME-MAIN-ARGC-ARGV]]: the SHIPPED SysV/AAPCS64 ELF trampolines now LEAD with the 2-instruction argc/argv materialization — `mov rdi,[rsp]`+`lea rsi,[rsp+8]` / `LDUR x0,[sp]`+`ADD x1,sp,#8` — BEFORE the call; the SysV pin became a no-`sub rsp` scan + the exact prefix moved to `ShippedElf*TrampolineMaterializesArgcArgv`. The no-prologue INVARIANT itself is unchanged.)* Closes the CLI-emitted-binary STATUS_ACCESS_VIOLATION. **`int42.c` → DSS CLI → `int42.exe` → Windows runs it → exit code 42** is now achieved through the full pipeline. | — | — |
| ~~D-LK4-RODATA-SUBSTRATE~~ | ✅ **CLOSED 2026-06-02 — FF6 prerequisite substrate landing.** First slice toward plan 11 FF6 hello-world. Substrate-only: NEW `struct AssembledData` ({symbol, section, bytes, alignment, relocations}) on `AssembledModule.dataItems`; `SectionKind` extracted from `src/link/object_format_schema.hpp` to `src/core/types/section_kind.hpp` for cross-tier vocabulary; LK4 substrate tier (parallel to existing LK4 AssembledModule structure). 3-stream 8-agent audit fold layered: linker `K_NoMatchingObjectFormat` guard fires loud when a producer lands `dataItems` BEFORE any walker arm closes; FLIP-MARKER test pins that no shipped format declares rodata yet; docblock expanded with Σ-formula contract for item-base-offset translation in walker arms. Per-format walker arms anchored below — D-LK2-RODATA (PE), D-LK1-RODATA (ELF), D-LK3-RODATA (Mach-O). Producer thread-through (HIR→MIR string-literal promotion → asm populates dataItems) anchored as **D-LK4-RODATA-PRODUCER**. | — | — |
| ~~D-LK4-RODATA-PRODUCER~~ | ✅ **CLOSED 2026-06-02 — MIR globals → AssembledData thread + RIP-relative `lea` SymbolRef encoder variant.** The full producer thread for module-level globals now reaches the binary's `.rdata` section end-to-end. **Encoder substrate**: new `EncodingSlotKind::RipRelDisp32 = 12` in `core/types/target_schema.hpp` (symbol-bearing slot like `Disp32`, plus forces ModR/M.mod=00 + ModR/M.rm=101 to mark the RIP-relative form); new `ModMode::RipRel = 0b00` in `x86_variable.cpp`. **Schema**: new `lea` 1-operand variant in `x86_64.target.json` with `guard: {operandKinds: ["symbol"]}`, `resultSlot: "modrm.reg"`, `template: {rexW: true, opcode: [141]}`, `wires: [{slotKind: "riprel.disp32", relocationKind: "rel32"}]`. **MIR→LIR**: `lowerGlobalAddr` opcode swapped from `mov` (which had no SymbolRef variant — would fire A_NoMatchingEncodingVariant on any non-call-peepholed GlobalAddr) to `lea`. Updated `MirToLir.DirectCallEmitsCallOpcode` test to expect the new shape. **Assembler new pass**: `lowerMirGlobalsToDataItems(Mir, TypeInterner, reporter)` in `asm/asm.{hpp,cpp}` walks `mir.moduleGlobalCount()`, encodes each constant-init global's `MirLiteralValue` as LE wire bytes sized by the type. Primitive arms handled today: Bool/I8/U8/Char/Byte (1B); I16/U16/F16 (2B); I32/U32/F32 (4B); I64/U64/F64 (8B); I128/U128/F128 (16B); plus the std::string arm (raw bytes + 1 NUL). `lowerMirGlobalsToDataItems` is invoked in `program/compile_pipeline.cpp` immediately after `assemble()`, populating `AssembledModule::dataItems` before linker dispatch. **Linker pre-walker cross-ref check**: `linker.cpp` now builds `dataSymbols` from `module.dataItems` and accepts rodata SymbolIds as valid reloc targets alongside functions and externs (the prior "(not declared by any AssembledFunction nor by any ExternImport)" message was an unconditional reject for any rodata-targeting reloc). **D-LK4-RODATA-WALKER-RELOC-BASE-OFFSET closed**: PE walker's `symbolVa` map now extends with each rodata item at `oh.imageBase + rdata->rva + rdataItemOffsets[i]` — `rdataItemOffsets[i] = Σ aligned_size(items[0..i-1])` captured during the rdata-bytes-build loop. **End-to-end smoke** (the user-requested observable test, 2026-06-02): `examples/c-subset/global_int/main.c` declares `int answer = 42; int main() { return answer; }` — the spawned binary's OS exit code must be 42, which requires (a) HIR→MIR to lower `answer` as a constant-init MirGlobal, (b) lowerMirGlobalsToDataItems to encode `[0x2A, 0, 0, 0]` into AssembledData, (c) MIR→LIR to emit `lea` SymbolRef + load through it, (d) the encoder to produce the RIP-relative `lea` bytes + rel32 reloc, (e) the linker's symbolVa map to include the rodata symbol, and (f) the PE walker's reloc-apply kernel to patch the disp32 to the correct RVA. A regression in ANY tier flips the exit code from 42 to garbage — caught by the harness's `ASSERT_EQ(exit, 42)`. 144/144 ctest; 22/22 integrated_tests. **REJECTED across agents** (anchored): HIR→MIR string-literal-to-global promotion (D-LK4-RODATA-PRODUCER-STRING anchor — needs array-to-pointer decay in CST→HIR which is a separate cycle); HIR→MIR aggregate-global encoding (D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL — Struct/Union/Array recursive byte computation); Bss zero-init emit (D-LK4-RODATA-PRODUCER-BSS-EMIT — sized-aggregate byte-width computation); runtime-init globals (D-LK4-RODATA-PRODUCER-RUNTIME-INIT — `__module_init__` synthesized function); `arithmetic` example pre-existing bug (`42 + 0 → 84`, regalloc using dst as both operands on the EXEC format codegen path — separate cycle, anchored as D-CSUBSET-BINOP-RIGHT-CLOBBER). | — | — |
| ~~D-LK2-RODATA~~ | ✅ **CLOSED 2026-06-02 — PE walker `.rdata` emission + 8-agent audit fold.** PE walker (`src/link/format/pe.cpp`) emits `.rdata` between `.text` and `.idata` whenever any `module.dataItems` item carries `section == DataSectionKind::Rodata`. **Schema-declared capability gate (audit fold — agnosticism win)**: NEW field `ObjectFormatData::supportedDataSections: std::vector<DataSectionKind>` + accessor `ObjectFormatSchema::acceptsDataSection(k)`. JSON loader reads optional top-level `"supportedDataSections": ["rodata"]` array. PE-Exec JSON declares `["rodata"]`; ELF/Mach-O/Wasm/SPIR-V declare nothing → rejected automatically. **`linker.cpp` gate replaced**: prior `kind == ObjectFormatKind::Pe` format-name branch (agnosticism veto territory) became `for (item : dataItems) if (!schema.acceptsDataSection(item.section)) fail_loud`. Adding a 4th rodata-capable format = JSON-only change. Validate() rejects `supportedDataSections` on non-image-flavor schemas (PE Obj can't carry rodata via dataItems). **DataSectionLayout substrate type** (anchor D-LK2-RODATA-SECTION-LAYOUT-RECORD, hoist-trigger 2nd walker arm): `struct {rva, virtualSize, rawSize, rawPointer, headerIndex}` consolidates the 5-tuple per section; `std::optional<DataSectionLayout> rdata` + `std::optional<DataSectionLayout> idata` replace 5 free locals + `hasRodata` bool + magic `1u + (hasRodata ? 1u : 0u)` index arithmetic. `headerIndex` captured at sectionHeaders push time → no positional indexing. SizeOfImage / SizeOfInitializedData / sectionsEndFileOff become linear walks over present layouts. **u32 overflow guard**: rdataBytes > UINT32_MAX fail-loud (silent-failure audit fold). **Non-Rodata fail-loud discipline**: walker's pre-loop scan runs UNCONDITIONALLY across `module.dataItems` (not guarded by hasRodata) — catches Data/Bss-only modules that an earlier shape would silently drop. **Validation ordering invariant**: `validateAssembledData()` runs BEFORE the capability gate; comment block declares the ordering load-bearing. **D-LK4-RODATA-WALKER-RELOC-BASE-OFFSET** remains anchored — reloc-targeting into rodata items not yet exercised (D-LK4-RODATA-PRODUCER triggers). **Tests** (10 total + tightened gate identity pin): `PeExecWriter.{RodataSectionEmittedWhenDataItemsNonEmpty, RodataAndIdataCoexistInCorrectOrder, MultipleRodataItemsLayWithAlignmentPadding, NoRodataItemsEmitsNoRdataSection, RejectsDataKindDataItemUntilD_LK4_RODATA_PRODUCER, RejectsBssDataItemViaLinkerSchemaGate, RequireSectionRodataFailsLoudWhenSchemaOmitsRow, SizeOfInitializedDataSumsRdataAndIdata, SizeOfImageReflectsRdataExtentInRodataOnlyArm, CertTableFileOffsetShiftsPastRdataAndIdata}` + `LinkerEndToEnd.{ElfRejectsDataItemsUntilD_LK1_RODATA, MachORejectsDataItemsUntilD_LK3_RODATA}` — each pins specific diagnostic code via `countCode(rep, K_NoMatchingObjectFormat)` (no loose `EXPECT_GT(errorCount, 0u)`; audit fold tightened the gate-identity test per `ff7b76d` / `7f8b843` precedent). FLIP-MARKER `ShippedExecFormatsRodataSectionPerWalkerArm` split per-format (PE NE; ELF + Mach-O EQ). **REJECTED across agents** (anchored — single-consumer extraction premature per `38ac5a2` precedent): cross-walker `buildDataSection` kernel hoist (code-architect; trigger D-LK1-RODATA), Rva/FileOffset newtype family (type-design A2; trigger 3rd walker arm). 141/141 ctest. | — | — |
| ~~D-LK4-RODATA-WALKER-RELOC-BASE-OFFSET~~ | ✅ **CLOSED 2026-06-02 — co-landed with D-LK4-RODATA-PRODUCER.** PE walker (`src/link/format/pe.cpp`) tracks per-item section-relative offsets via `std::vector<std::uint32_t> rdataItemOffsets` populated during the rdata-bytes-build loop (`Σ aligned_size(items[0..i-1])` is the cumulative `rdataBytes.size()` AFTER per-item alignment padding, captured BEFORE the item's bytes are inserted). After the function-symbol + extern-IAT loops, the walker extends `symbolVa` with each rodata item at `oh.imageBase + rdata->rva + rdataItemOffsets[i]`. The shared `applyExecRelocations` kernel resolves `.text → .rodata` REL32 relocations correctly through this map. End-to-end validation: `examples/c-subset/global_int` returns `*p == 42` only when the disp32 patch lands at the rodata-item's exact VA — a 1-byte drift produces garbage. SymbolId collision check fail-louds with `K_DuplicateDataSymbol` (audit fold: prior `K_SymbolUndefined` was semantically wrong-domain for a redefinition). | — | — |
| ~~D-LK4-RODATA-PRODUCER-STRING~~ | ✅ **CLOSED 2026-06-02 — HIR→MIR string-literal promotion + Array→Ptr decay + trampoline-injector-aware symbol mint.** Three-tier substrate landing closes the rodata-via-string-literal path end-to-end. **Type-lattice rule** (`src/analysis/semantic/type_rules.hpp`): `isAssignable(Ptr<T>, Array<T,N>)` returns true for same-element-type — C-standard structural rvalue conversion (array-to-pointer decay). Universal across languages with the same array/pointer notion; per-language opt-out anchored at `D-LANG-STRUCTURAL-DECAY-OPT-OUT`. **HIR `coerce()`** (`src/hir/lowering/cst_to_hir.cpp`): when target is `Ptr<T>` and source is `Array<T,N>` with same element type, emits a synthetic `Cast` HIR node (the universal type-conversion marker) returning `Ptr<T>`. The HIR verifier sees a Ptr-typed value; downstream type-check passes cleanly. **HIR→MIR Cast lowering** (`src/mir/lowering/hir_to_mir.cpp::lowerExpr` Cast arm): when `fromK == Array && toK == Ptr` and the operand is a `HirLiteral` with a `std::string` literal-pool arm, mints a fresh synthetic SymbolId beyond all user-declared functions/externs/globals (lazy-seeded via `mintSyntheticGlobalSymbol`), calls `mir.literalPoolAdd` + `mir.addGlobal` to register the rodata payload, then emits `mir.addGlobalAddr(sym, ptrTy)`. The `lowerMirGlobalsToDataItems` pass (D-LK4-RODATA-PRODUCER) materializes the MirGlobal's `std::string` arm into `AssembledData{Rodata, bytes + NUL, Alignment{1}}`. **Trampoline-injector data-symbol-aware mint** (`src/link/entry_trampoline.cpp::maxExistingSymbolIdV`): audit-fold extension to also scan `module.dataItems` — without this, the synthetic `_start` trampoline would pick `maxV+1` where `maxV` ignores rodata items, colliding with the string-literal global and surfacing as `K_DuplicateDataSymbol` at the PE walker (loud but root-cause-distant). **Non-string + non-literal Array→Ptr arms fail loud**: HIR→MIR fails loudly for local-array decay (anchored `D-LK4-RODATA-PRODUCER-LOCAL-ARRAY-DECAY` — needs `AddressOf(Alloca) + Gep(0)` lowering when the local-array case lands) and for non-string-literal Array operands (anchored `D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY-LITERAL-DECAY` — needs aggregate-literal handling). Both surface a precise diagnostic naming the anchor; no silent miscompile path exists. **Always-run regression pin** (`examples/c-subset/string_literal_arg/main.c`): `int recv(const char* p) { return 42; } int main() { return recv("X"); }` — exit 42 only if (a) the literal lands in .rdata; (b) the call-arg site emits `lea rcx, [rip + sym]` resolving to the rodata RVA; (c) the trampoline injector's mint avoids the rodata symbol; (d) recv's body lowers and links cleanly. A regression in any tier flips the exit code and fails the always-run harness immediately. **NOT addressed this cycle** (anchored): char→int / bool→int / byte→int implicit promotion — pinned existing tests (`ReturnTypeMismatchOnNonAssignable`, `ReturnOfMismatchedCallResultEmitsMismatch`) explicitly require this NOT to be implicit (per the type_rules.hpp docblock "deliberately NOT arithmetic — promoting them implicitly produces silently-wrong code"). For c-family languages that DO want C-standard integer promotion, the right architecture is a per-language config (`numericPromotions` field on grammar_schema's semantic config) — anchored as `D-LANG-NUMERIC-PROMOTION-CONFIG`. 148/148 ctest; 46/46 integrated_tests. | — | — |
| D-LANG-NUMERIC-PROMOTION-CONFIG | **Per-language config for implicit sub-integer→int promotion.** Today's `type_rules.hpp::isAssignable` rejects `Char→I32`, `Bool→I32`, `Byte→I32` per the deliberate-design comment "Bool/Char/Byte are deliberately NOT arithmetic — promoting them implicitly produces silently-wrong code in the languages we ship". C-family languages want C99 §6.3.1.1 integer promotion (char→int, bool→int, etc.); Rust and Pascal require explicit casts. Closure shape: NEW `numericPromotions` field on grammar_schema's semantic config (per-language map from source TypeKind → list of admissible target TypeKinds). `isAssignable` reads it as an opt-in widening. c-subset declares `{Char, Bool, Byte} → {I8..I128}` (C99 standard promotions). The HIR `coerce()` already emits the `Cast` HIR node when this branch admits the assignment; the MIR-tier `mapCast` lowers to ZExt/SExt as appropriate — no MIR-tier changes needed. Surfaces today: any c-subset function returning `char[i]` (subscript on Char array) from an int function fails `S_ReturnTypeMismatch`. | **First c-subset example needing char→int or bool→int promotion** (e.g., FF6 hello-world's `int main() { return msg[0]; }` style returning a string byte as exit code, or any future puts/printf return-int from char). | First reproducible source-level case. |
| ~~D-LK4-RODATA-PRODUCER-LOCAL-ARRAY-DECAY~~ | ✅ **CLOSED 2026-06-18 (FC8 cycle 3, branch p10).** Trigger FIRED (corpus `array_decay` — a local `int a[3]` passed to a fn). A non-string array→pointer decay (C 6.3.2.1) now lowers in the `hir_to_mir` Cast Array→Ptr arm via `lowerLvalueAddress(operand)` (the array's base address — a local alloca, a global GlobalAddr, a struct-field Gep, an indexed sub-array) + a byte-offset-0 `Gep` re-typed `Ptr<elem>` = `&arr[0]`. Reused the existing lvalue-address + aggregate-global-rodata infrastructure (no new substrate). 321/321; x86-PE (ctest) + x86-ELF (WSL) + arm64-ELF (qemu) exit 42 incl. WRITE-THROUGH (proves the pointer aliases real storage); structural MIR pin + corpus red-on-disable. — ORIGINAL: **HIR→MIR Cast Array→Ptr arm for LOCAL-array operands (vs string literals).** Today's Cast Array→Ptr lowering only handles the string-literal arm (operand is `HirKind::Literal` with `std::string` pool value). Local arrays — `int arr[10]; foo(arr);` — would land on the fail-loud arm. Closure shape: when the Cast operand is a `Ref` to an addressable local OR a `Ref` to an Array-typed module global, emit `AddressOf(slot) + Gep(0)` to produce the element pointer. Requires the existing lvalue-address path to recognize Array-typed locals (`lowerLvalueAddress`) AND requires the alloca opcode encoding to be wired for x86_64 (anchored separately: c-subset's `alloca` has `A_NoEncodingDeclared`). | **First c-subset example using a local array passed to a function** OR **first global Array<T,N> used as a Ptr<T> source**. | First reproducible source-level case. |
| D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY-LITERAL-DECAY | **HIR→MIR Cast Array→Ptr arm for non-string Array literals.** Today's path handles only `std::string` literal-pool arms. A future `MirAggregateValue` arm with array semantics (e.g., compound-literal `(int[]){1,2,3}` decaying to `int*`) would land on the fail-loud arm. Closure shape: synthesize a MirGlobal with the aggregate-literal arm + emit GlobalAddr returning the element-pointer type. Cross-references D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL (which handles aggregate-typed globals on the lowerMirGlobalsToDataItems side). | **First c-subset / FF6 example using a non-string array compound literal in pointer-arg position**. | First reproducible source-level case. |
| ~~D-LK4-RODATA-PRODUCER-NONSTRING-GLOBAL-ARRAY-DECAY~~ | ✅ **CLOSED 2026-06-18 (FC8 cycle 3, branch p10).** Trigger FIRED (corpus `array_decay` — a non-string `int g[3]` global passed to a fn). Closed by the SAME `lowerLvalueAddress + Gep(0)` decay as the local arm (`lowerLvalueAddress` yields the global's `GlobalAddr`; its bytes are in rodata via the already-closed `D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL`). — ORIGINAL: **HIR→MIR Cast Array→Ptr arm for module-global Array operands that aren't string literals.** Today's lowering at `hir_to_mir.cpp:635-643` fail-louds when the Cast operand is not `HirKind::Literal` (the string-literal path is the only handled shape). A user-source case `int xs[3] = {1,2,3}; int* p = xs;` would land here — the operand is a `Ref` to an Array-typed module global, not a literal. Closure shape: when the Cast operand is a `Ref` to an Array-typed *global* symbol (vs. local, covered by D-LK4-RODATA-PRODUCER-LOCAL-ARRAY-DECAY), emit `GlobalAddr(sym) + Gep(0)` to produce the element-pointer. The Array-typed global must already have its bytes materialized via D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL (Array recursive byte computation) — that pair-anchor gates the data-side. **Audit-promoted 2026-06-02** (anchor was cited in production source but missing its plan row; sibling rows above covered local + non-string-literal arms). | **First c-subset / FF6 example with a non-string module-global Array passed to a Ptr-typed function arg or assigned to a Ptr-typed local**. | First reproducible source-level case. |
| D-CSUBSET-CONST-PTR-DECAY | **Const-qualified pointer admission for Array→Ptr decay.** Today's Array→Ptr rule at `type_rules.hpp:127-147` admits only same-element-type decay (`Array<T,N> → Ptr<T>`). C-standard §6.7.3 allows covariant qualifier addition on the element side — `Array<T,N> → Ptr<const T>` is admissible (the array bytes don't change; the caller promises not to mutate through the result). Closure shape: extend the Array→Ptr arm with a qualifier-compatibility check (`lhsElem const-qualified, rhsElem unqualified, same underlying type → admit`) when const-qualified pointers land on the c-subset type lattice. Pairs with the broader const-qualifier substrate (const-correctness pass + qualifier-stripping for arithmetic). Cited at production-source-code level in `type_rules.hpp:132` and `semantic_analyzer.cpp:1800`. **Audit-promoted 2026-06-02** (anchor was cited in production source but missing its plan row; sibling D-CSUBSET-* anchors are tracked in this §3.1 — e.g. D-CSUBSET-BINOP-RIGHT-CLOBBER row above). | **c-subset gains const-qualified pointer types** (or first lang shipping with them). | First reproducible source-level case `const T* p = arr;` where arr is `T[N]`. |
| D-LANG-STRUCTURAL-DECAY-OPT-OUT | **Per-language opt-out of array→pointer decay in `isAssignable`.** Today's shared rule unconditionally admits `Ptr<T> ← Array<T,N>` (matching C/C++/Go/Pascal-with-string-conversion). Some languages (Rust, Swift, some FP languages) forbid implicit decay — programmers must call `.as_ptr()` or take a slice explicitly. For agnosticism, those languages should declare the prohibition rather than getting the C-style implicit conversion. Closure shape: per-language `structuralDecayRules` config field; isAssignable reads it. | **First non-C-family language landing that needs strict-no-implicit-decay semantics.** | First reproducible source-level case. |
| D-LANG-NULL-PTR-CONST-EVAL | **Const-eval-aware null-pointer-constant admission (`0+0` / `(int)0` / `-0`).** Step 13.3a's `isLiteralIntegerZero` (in `semantic_analyzer.cpp:1706`) admits ONLY the bare integer-literal `0` form — strictly: subtree contains exactly one non-empty-space TOKEN, that token is an integer literal with decoded value 0. Per C-standard §6.6, ANY integer constant expression with value 0 is a null pointer constant — `0+0`, `(int)0`, `0*x` (when x is const), `1-1`, etc. The narrower bare-literal rule was the chosen scope to avoid HIR/semantic divergence (HIR `coerce()` matches `HirKind::Literal` only; `-0` would lower to `UnaryOp(Neg, Literal(0))` whose kind is UnaryOp). Closure shape: add a const-eval pre-pass at the semantic tier (or lift HIR's `const_eval_arith` to a tier-neutral form per plan 12.5 §0.2 anchored work) that folds the subtree to a typed integer-zero value; both semantic `admitsNullPointerConstant` AND HIR `coerce()` then admit on the folded value uniformly. Cited at production-source-code level in `semantic_analyzer.cpp:1697`. **Audit-promoted 2026-06-02** (audit caught the stepper-row-only citation; sibling D-LANG-* rows above already promoted). | **Const-eval at the semantic tier lands** (plan 12.5 §0.2 closure trigger for tier-neutral const-eval) OR **first reproducible source-level case where a non-bare-literal null-pointer constant is rejected** that hits a c-subset corpus pin. | First c-subset / FF6 corpus needing wider null-pointer admission than bare `0`. |
| D-CSUBSET-ALLOCA-COUNT-CACHE | **Cache the per-function alloca count on `LirFuncAllocation` at MIR→LIR time** (vs. the current `functionLocalAllocaCount` O(blocks × insts) re-scan at materialize time). The count IS knowable at MIR→LIR — each MIR `Alloca` maps 1:1 to a LIR `alloca` op. Caching eliminates the redundant scan for the common case "target has alloca, function has 0 allocas" (most functions in any module). Closure shape: add `numLocalAllocas: std::uint32_t` to `LirFuncAllocation::PerFunc`; populate it during `mir_to_lir.cpp::lowerAlloca` (just increment a counter); read it from `materializeOneFunc` in place of the helper call. Pre-scan-vs-actual-scan divergence becomes assert-able (`assert(count == localAllocaIndex)` at materialize completion). Today's cost is marginal (1 of 4 existing per-function pre-scans); the optimization waits for profiler evidence. Cited at production-source-code level in the `functionLocalAllocaCount` helper docblock at `lir_callconv.cpp` (search for the helper by name — line citations omitted to remain stable under refactor). **7-agent fold F2 anchor 2026-06-02**. | **Profiling shows > 0.5% of compile time in `functionLocalAllocaCount` on multi-function modules** OR **multi-MB modules surface link-time perf measurably** (same trigger family as D-LK10-ENTRY-MODULE-COW). | First profiled compile with measurable per-function pre-scan overhead. |
| ~~D-CSUBSET-LOCAL-INT-CODEGEN-NEGATIVE-PIN~~ | ✅ **CLOSED 2026-06-04 (cycle 10k, commit `a28567c`)** — the third pin of the alloca/lea family now ships. New `tests/test_support/mutate_target_schema.hpp` header-only helper `mutateShippedTargetSchemaJson(targetName, removeMnemonics)` loads shipped target JSON via `findShippedConfig`, removes named opcode rows via nlohmann_json, and re-constructs a `TargetSchema` via `loadFromText`. New test `LirCallconvAbi.AllocaWithoutLeaInSchemaFailsLoudInMaterialize` builds a LIR with `alloca` + physical-reg result, runs `materializeCallingConvention` against the lea-stripped schema, expects failure with `L_RequiredLirOpcodeMissing` (exact-1 specificity). Three attribution pins prevent helper-regression silent-pass (ASSERT_FALSE(lea) + ASSERT_TRUE(alloca) post-mutation + countCode == 1u). `findShippedConfig` also gained `DSS_EXPORT` to allow cross-DLL test consumption. | (closed) | (closed; helper is reusable for any future schema-misconfiguration negative pin) |
| D-FRAMELAYOUT-COUNT-FIELDS-AUDIT | **Audit pass: `FrameLayout` zone-size vs zone-count duplication.** Three zones carry BOTH a byte-size field AND a count source: (a) `savedRegAreaSize` + `savedRegs.size()`; (b) `spillAreaSize` + `LirFuncAllocation::numSpillSlots`; (c) `localAreaSize` + `numLocalAllocas`. Each pair is invariant-pinned in tests (`FrameLayoutInvariantsHoldPerFunction` lines 298-306). The pattern is consistent but redundant — a future zone addition would carry the same duplication. Closure shape: decide bytes-only-vs-count+bytes uniformly across all four zones (outgoing/saved/spill/local), reshape `FrameLayout` to match. Cited at production-source-code level in `lir_callconv.hpp:167-185`. **7-agent fold A1 anchor 2026-06-02**. | **First addition of a 5th frame zone** (red-zone tracking, CFI scratch area, TLS bias, etc.) when the duplication-vs-derivation tradeoff becomes worth resolving. | First 5th-zone proposal. |
| D-OPCODE-HANDLES-NAMING-SWEEP | **Rename `OpcodeHandles` fields for naming consistency.** `h.alloca_` carries a trailing underscore to avoid the C++ keyword `alloca` (defined as a `<malloc.h>`/`<stdlib.h>` macro on Windows, builtin on POSIX). Sibling fields are `arg`, `call`, `mov`, `frameLoad`, `frameStore` — no underscore. Two future-design options: (a) rename ALL fields to `<mnem>Op` suffix uniformly; (b) wrap the struct's members behind a `OpcodeHandleSlot { uint16_t op; string_view mnem; }` table-driven shape that obviates per-field naming. Cited at production-source-code level on the `alloca_` field declaration inside the `OpcodeHandles` struct in `lir_callconv.cpp` (search by struct name — line citation omitted to remain stable under refactor). **7-agent fold A2 anchor 2026-06-02**. | **Next field added to `OpcodeHandles`** OR **a refactor cycle touching the resolveOpcodes table** when the naming inconsistency becomes worth the rename churn. | First incidental edit to OpcodeHandles after the next handle addition. |
| D-LIR-ALLOCA-LOWERING-EXTRACTION | **Extract `alloca`-materialization into a separate pre-materialize-callconv pass.** Step 13.3b's `alloca` arm lives inside `materializeOneFunc` alongside `arg`/`call` arms. Arg + call have inherent coupling to the cc (read `cc.argGprs`, need `totalFrameSize` for stack-arg offsets). `alloca` is simpler — it depends only on `outLayout.localAreaOffset()` + `slotSize`. Could be a separate `lowerAllocas` pass running BEFORE materialize, reading `numLocalAllocas` from `LirFuncAllocation` (per D-CSUBSET-ALLOCA-COUNT-CACHE) and emitting `lea`s directly. Keeping it co-located today shares the OpcodeHandles cache + avoids a stub-layout for the pre-pass. Cited at production-source-code level at the alloca-arm inside `materializeOneFunc` in `lir_callconv.cpp` (search for `D-CSUBSET-LOCAL-INT-CODEGEN` docblock — line citation omitted to remain stable under refactor). **7-agent fold A3 anchor 2026-06-02**. | **FF6+ adds dynamic stack growth or guard pages** (alloca semantics expand beyond "fixed-size local slot") OR **future cycle splits `materializeCallingConvention` into per-concern passes**. | First substrate need requiring per-alloca lowering beyond fixed-size frame slots. |
| D-ENUM-NAME-TABLE-STATIC-ASSERTS | **Retrofit table-coverage + row-order `static_assert`s to the 5 sibling `EnumNameTable` instances in `target_schema.hpp`.** Step 13.3a's CallConv fold added two `static_assert`s next to `kCallConvTable` (`target_schema.hpp:139`): (a) table size MUST cover every enum value — protects against silent row-0 fallback when a new enum value is added without a matching table row; (b) rows MUST sit at indices matching the enum's underlying value — gives O(1) lookup AND surfaces row-vs-enum misorder at constexpr time. The same silent-failure shape applies to the 5 sibling tables: `kTargetAbiModelTable` / `kTargetCondCodeTable` / `kTargetResultRuleTable` / `kTargetRegClassTable` / `kTargetEncodingShapeTable`. Closure shape: add the same 2-clause `static_assert` pair next to each table. Pattern reference: `c_mangle.cpp:42`. Cited at production-source-code level in `target_schema.hpp:127-138`. | **Next maintenance cycle that touches `target_schema.hpp`** OR **a new enum value added to any of the 5 sibling enums** (which would silently fall back to row 0 without the pin). | First sibling-enum extension OR first incidental edit to the surrounding file. |
| D-SEMANTIC-SUBTREETYPE-TRANSPARENT-WRAPPERS | ✅ **CLOSED 2026-06-15 (semantic-expr-typer cycle).** `subtreeType` is now a COMPLETE semantic-tier expression typer: the `typeAt`-first stamp stays authoritative, and an unstamped wrapper is typed by its operator VERB (dispatch on `hirLowering.{binary,unary,postfix}Ops` + `ternaryExprRule`, MIRRORING `cst_to_hir.cpp` via the SHARED `type_rules.hpp` primitives — `derefResultType`/`indexResultType`/`usualArithmeticCommonType`/`integerPromotedType`/`isComparison`), with cast/sizeof/compound-literal RE-typing arms for the Pass-1.5 (sizeof-operand) path and a leaf scope-lookup. The `holdsOperator` descent-stop is DELETED. This fixes a latent unsoundness (mixed-width binary in a checked position was typed against an arbitrary DFS leaf, silently admitting narrowings — pinned by `MixedWidthBinaryArgTypedByUacNotLeaf`), unblocks `D-FC6-SIZEOF-ARRAY-DIM-VALUE-FORM` ✅, and surfaces previously-suppressed mismatches as positioned `S_*` (companion `isAssignable` Bool→arith admission, gate-`boolWidensToArith` so the verifier stays strict; the `f(-0)` canary flipped → `NegativeZeroAtVoidPtrArgFiresMismatch`). 295/295; independent review found + fixed the cast-wrapper-at-Pass-1.5 silent miscompile (review C1) before commit. — ORIGINAL row retained: **Pass-2 expression typing for type-changing wrappers (`&x`, `*p`, `-x`, `c ? a : b`, future `(int*)q` cast-expr).** Today's `subtreeType` (in `semantic_analyzer.cpp:1796`) performs DFS for the FIRST typed descendant, but stops descent at any internal node whose visible-children include a Prefix or Ternary operator-token (per the position-based F1 fix from step 13.3a's 2nd-order audit fold). The stop returns `InvalidType`, which the caller's `if (!argTy.valid()) continue;` silently suppresses — false-negative on `f(-x)` / `f(&x against wrong-ptr-type)` / `return c ? a : b;`. The proper long-term fix is pass-2 expression typing: each unary / binary / ternary / cast-expr wrapper records its evaluated type via `s.nodeToType.set(node, evaluatedTy)`, so subtreeType's initial `typeAt(node).valid()` check returns the correct wrapper-tier type before descent — eliminating both the false-negative cascade-suppression AND the operator-stop heuristic itself. Cited at production-source-code level in `semantic_analyzer.cpp:1769-1788`. **Audit-promoted 2026-06-02** (audit caught the code-cited anchor missing its plan row; the partial closure in 13.3a kept the heuristic for the c-subset corpus). | **First c-subset feature needing strict S_TypeMismatch on a unary/binary/ternary call-arg, init, or return** (e.g. a hello_writefile-tier example whose mismatch on `-x` or `&y` would silently miscompile through the cascade-suppress path) OR **castExpr lands in c-subset** (cast-expressions are TYPE-CHANGING wrappers — the SubtreeType operator-stop doesn't cover them yet, see closure rationale at the same code-cite). | First reproducible source-level case where the silently-suppressed mismatch becomes execution-verifiable. |
| D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL | ✅ **CLOSED 2026-06-17 (struct-head grammar + aggregate-global codegen cycle — trigger fired: top-level `struct P {…} v = {…};` now parses).** A new recursive `encodeAggregateValue` (`asm/asm.cpp`) walks the TYPE tree + the init-VALUE tree in lockstep (the byte-writing mirror of `collectLeaves`): struct/union via `computeLayout(...)->fieldOffsets`, array via the layout-authority stride (`elem->align.alignUp(elem->size)`), scalar leaves via a SHARED `decodeScalarLiteralBits` chokepoint (LE bytes, the F32 double→float narrow). The buffer is pre-sized + zero-filled to the layout total, so padding / partial-init tails / union slack are 0 by construction. A `MirAggregateValue` arm in `lowerMirGlobalsToDataItems` dispatches on the literal-pool VARIANT BEFORE the `primitiveByteSize` gate; `lowerMirGlobalsToDataItems` gained `std::optional<AggregateLayoutParams>` + `DataModel` params (threaded parallel to `externCallDispatch` through `lowerMirModuleToAssembly`/`lowerMergedToAssembly` + captured on `CuMirModule`). Fail-loud on missing aggregateLayout / un-sizeable type / shape mismatch / over-long init / F16-F128 leaf / relocated-pointer leaf. RUNTIME-witnessed: corpus `struct_body_top_level` exit 42 on x86_64-PE (host) + arm64-ELF (qemu) — a byte-exact pin over the c-subset-REACHABLE encoder forms (PADDING: int at the aligned offset; explicit-zero fields; UNION first-member init; NESTED aggregate; ARRAY stride); macho on the macos-latest leg. The forms a c-subset source CANNOT reach — short-init zero-fill (HIR pre-normalizes omitted slots into a full field list) + the fail-loud arms (over-long init, F16/F128 leaf, absent aggregateLayout) — are pinned synthetically + red-on-disable at the asm tier (`tests/asm/test_asm_substrate.cpp` `AsmAggregateGlobal.*`, 5 pins). Zero-init (`.bss`) stays `D-LK4-RODATA-PRODUCER-BSS-EMIT`. — ORIGINAL: **Encoding aggregate-typed globals (Struct / Array / Slice / Vector / Matrix) to AssembledData bytes.** Today `lowerMirGlobalsToDataItems` fail-louds K_NoMatchingObjectFormat on any non-primitive TypeKind. Closure: recurse through the TypeInterner — for Struct/Union read `operandsByField` + recursive `primitiveByteSize` (with padding to per-field alignment); for Array read element type × count; for Slice/Vector use ABI-specific layout (Slice = `{ptr, len}` two-word; Vector = element × lanes with platform-specific alignment). | **First aggregate-typed module-level global emitted by a producer cycle.** | First c-subset / FF6 example with `struct Point p = {1, 2};` at file scope. |
| D-LK4-RODATA-PRODUCER-BSS-EMIT | ✅ **CLOSED 2026-06-19 (writable `.data`/`.bss` cycle — see registry `D-LK4-DATA-PRODUCER-MUTABLE-GLOBAL`).** A tentative zero-init global (`int g;`) now emits `AssembledData{Bss, bytes={}, reservedSize=type-byte-width}` (the asm.cpp:720 fail-loud replaced); all 3 exec writers emit a NOBITS `.bss` (ELF `SHT_NOBITS` in a separate R+W PT_LOAD; PE `.bss` SizeOfRawData=0; Mach-O `__bss` S_ZEROFILL) from config-driven `.format.json` rows. Corpus `global_write` (gz tentative→.bss, written-then-read) exit 42 on PE/ELF/qemu. — ORIGINAL: **Zero-init globals → `.bss` AssembledData emission.** Today `lowerMirGlobalsToDataItems` fail-louds K_NoMatchingObjectFormat on globals with `initLiteralIndex == UINT32_MAX` (no constant initializer, no init function). Closure: emit `AssembledData{symbol, Bss, bytes={}, alignment}` per the Bss-invariant (zero-fill, no on-disk bytes); the wire format reserves `sh_size` from the global's TYPE byte-width. Format walkers (PE / ELF / Mach-O) emit the per-format `.bss`-class section. | **First zero-init global emitted by a producer cycle** OR **first c-subset example with `int g;` (uninitialized file-scope decl)**. | First zero-init global flowing through the producer thread. |
| D-LK4-RODATA-PRODUCER-RUNTIME-INIT | **Runtime-init globals → `__module_init__` synthesized function emission.** Today `lowerMirGlobalsToDataItems` fail-louds K_NoMatchingObjectFormat on globals with `initFunc.valid()`. Closure: emit `AssembledData{symbol, Bss/Data, bytes={zero-init or pre-init bytes}, alignment}` per the type AND ensure the `__module_init__` MirFunc lowers through MIR→LIR→assembler so its Store instructions run at module-load time (the C++ static-init equivalent). Requires linker-side wiring for CRT-style module-init invocation (or `_DllMainCRTStartup`-equivalent for Windows). | **First runtime-init global emitted by HIR→MIR** OR **first c-subset example with a file-scope init expression that doesn't const-fold**. | First non-const-foldable init expression at file scope. |
| D-LK4-RODATA-PRODUCER-EXOTIC-FLOAT | **F16 / F128 globals: literal pool's `double` arm can't represent these losslessly.** Today `lowerMirGlobalsToDataItems` fail-louds K_NoMatchingObjectFormat on F16/F128 globals (silent-failure audit fold caught the prior shape that wrote low-N bytes of a `double` bit-pattern as if it were the f16/f128 representation — yielding garbage). Closure: extend `MirLiteralValue` with typed-float variants (or store float bytes directly), or anchor F16/F128 at the front-end tier with a typed-conversion pass before the literal pool. | **First f16/f128 module-level global from a source program.** | First source with a half/quad-float global. |
| ~~D-LK4-RODATA-PRODUCER-ARM64-GLOBALADDR~~ | ✅ **CLOSED 2026-06-09 — the trigger fired + RUNTIME-PROVEN.** The arm64 `GlobalAddr`→`lea` (ADRP+ADD 2-word macro) was wired in the D-AS4-3 cycle (`arm64.target.json` `lea` fixedWords), and the `global_int` arm64-ELF corpus (`int answer=42; return answer;`) now RUNS → **exit 42 under qemu-aarch64** (the arm64 GlobalAddr→lea→load→return materializes the global's VA end-to-end). The "fail-loud A_NoEncodingDeclared today" text below describes the PRE-D-AS4-3 state and is now stale. **Original:** ARM64 `MirOpcode::GlobalAddr` → ADRP + ADD macro-op encoding. Today `lowerGlobalAddr` emits the abstract LIR `lea` mnemonic, which `x86_64.target.json` binds to the RIP-relative single-instruction form. ARM64 has no equivalent — it uses a 2-instruction macro-op: `ADRP r, sym@PAGE` (R_AARCH64_ADR_PREL_PG_HI21) + `ADD r, r, sym@PAGEOFF` (R_AARCH64_ADD_ABS_LO12_NC). The existing schema machinery can support this with two new opcode rows in `arm64.target.json` (`adrp` + `add` with `PageOffset12` slot), two new MnemonicSlot enum values, and `lowerGlobalAddr` dispatching on schema-presence of `MnemonicSlot::Lea` vs `MnemonicSlot::Adrp`. Today's behavior for ARM64 + global is fail-loud `A_NoEncodingDeclared` (the opcode is missing — not silent). Pairs with D-LK10-ENTRY-ARM64 (paired per-CPU helper substrate). | **First ARM64 build that references a module-level global** (today's c-subset corpus has no ARM64 + global example). | First ARM64 example with a file-scope decl. |
| ~~D-LK4-RODATA-LINKER-SYMBOL-KIND-MAP~~ | ✅ **CLOSED 2026-06-04 (with D-LK4-3, cycle 15).** Consolidated exactly as specified: the `unordered_map<LinkedSymbolKey, SymbolKind>` that D-LK4-3's compound key required IS this map — KEY = `(cuId, SymbolId)`, VALUE = `SymbolKind {Defined, Extern, Data}` (the which-kind-defined-this distinction this anchor called for). `buildCompoundIndex` in `linker.cpp` is the single insert chokepoint; reloc resolution is one compound `.contains()`. Closed-enum `SymbolKind` extends to TlsDefined/WeakDefined without resolution-loop churn, as anticipated. **Original anchor:** Consolidate the linker's 3 parallel symbol-kind sets into `unordered_map<SymbolId, SymbolKind>`.** Today `linker.cpp` builds `declaredSymbols` (functions), `externSymbols` (extern imports), and `dataSymbols` (rodata items) as three `unordered_set`s. The pre-walker cross-ref check does 3 `.contains()` calls per reloc. At 4+ kinds (TLS, weak-defined, etc.) the structure becomes `O(kinds²)`; the resolution semantics ALSO loses the "which-kind-defined-this" distinction the diagnostics rely on for the cross-table-uniqueness check. Closure: enum `SymbolKind { Defined, Extern, Data, TlsDefined, WeakDefined, ... }`; insert each source-of-truth pass with its kind; reloc resolution = one map-find. Today's 3-set shape is correct at n=3; defer until n=4 OR cross-CU symbol-merge (D-LK4-3). | **4th symbol kind** (TLS / weak-defined) **OR D-LK4-3** (cross-CU merge), whichever lands first. | First TLS/weak-defined symbol or cross-CU link. |
| D-LK4-RODATA-PRODUCER-RANGE-GUARD | **Explicit `appendLE` range guard on the value-vs-width invariant.** Today's `appendLE` silently drops high bytes when `value > 2^(8*width)-1`. The in-source comment names the caller invariant ("HIR/MIR const-eval clamps to the type's range before reaching the literal pool"); the guard is unverified. Closure: explicit `if (width < 8 && value >= (1ull << (width*8))) emit(K_NoMatchingObjectFormat, "value out of range...") + continue` at the `appendLE` call site. Cheap; converts an assumed invariant into a verified gate. | **First bypass of HIR/MIR const-eval that lands an out-of-range value into the pool** (no current path exists, but a future coercion / const-eval bug would silently miscompile). | First const-eval defect that surfaces as wrong on-disk bytes. |
| D-LK4-RODATA-WALKER-RELOC-EXTENSION-HOIST | **DONE 2026-06-24 — all 3 walkers (ELF + Mach-O + PE) converted; residual `D-LK-RODATA-HELPER-PE-PARITY` ✅ CLOSED** (was PARTIALLY DONE 2 of 3 until PE landed via D-LK4-DATA-PRODUCER — see the inline 2026-06-24 UPDATE below). **TRIGGER FIRED 2026-06-09 + ACTED ON**: the Mach-O rodata exec arm (D-LK3-RODATA / `D-LK1-MACHO-EXEC-DATA-SECTIONS`) would have been the 3rd inline copy of the rodata→symbolVa loop, so the hoist was performed — `elf.cpp` AND `macho.cpp` are now BOTH converted to a shared header-only helper `src/link/format/exec_data_section.hpp`. **SUPERSESSION (note explicitly)**: the realized helper is TWO free functions — `buildExecRodata` (validate-each-item [Rodata-only + fail-loud on data-item relocs] + lay out bytes with H1 max-item-align padding → an `ExecRodataLayout` of bytes + per-item offsets) and `addDataSymbolVas` (add each NAMED [M1 anonymous-skip] data item's absolute VA into the `symbolVa` map at `sectionBaseVa + itemOffset`) — which SUPERSEDE this row's originally-planned SINGLE-function `exec_reloc_apply.hpp` shape (`(vector<AssembledData> const&, uint64_t sectionBaseVa, vector<uint32_t> const& itemOffsets, unordered_map<SymbolId,uint64_t>&, reporter)`). The split was needed because each walker computes its layout (bytes + offsets) at a different point than where it knows its format-computed `sectionBaseVa`. **RESIDUAL CONSUMER — PE**: `pe.cpp` still has its OWN inline rodata→symbolVa loop (NOT converted; plan-lock Option b — defer, since PE is the Windows-critical-path runtime and its latent H1/M1 are masked by page-aligned `.rdata`). The PE retrofit is tracked as the new **`D-LK-RODATA-HELPER-PE-PARITY`** (registry). **UPDATE 2026-06-24 (deferral loop, p16): PE IS now the 3rd converted walker** — the **D-LK4-DATA-PRODUCER** cycle generalized the helper to `buildExecDataSection` and converted `pe.cpp` (calls it + `addDataSymbolVas` for all 3 section kinds; no inline rodata loop remains). So the residual `D-LK-RODATA-HELPER-PE-PARITY` is ✅ CLOSED (registry, 2026-06-24 — H1 over-aligned-item is structurally impossible since `Alignment` ≤ 256 < PE sectionAlignment ≥ 4096; M1 chokepoint-tested), and THIS row's PE-residual is RESOLVED (all 3 walkers converted). | **PE walker conversion** (`D-LK-RODATA-HELPER-PE-PARITY` — the residual 3rd consumer). | ✅ All 3 walkers (ELF + Mach-O + PE) converted to `exec_data_section.hpp`. |
| ~~D-CSUBSET-BINOP-RIGHT-CLOBBER~~ | ✅ **CLOSED 2026-06-02 — regalloc-tier 2-addr-aware allocation + 7-agent audit fold (closes the silent-miscompile bug-class for ALL `requires2Address` ops, commutative OR not, end-to-end across the no-spill AND spill-fallback paths).** Root cause traced: the linear-scan allocator's `expireActive(start)` releases v2's physical register at exactly the position where v3 (the binary-op result) begins (`v2.end == v3.start`). v3 then picked v2's just-freed register. After rewrite, `add result=r14, [r15, r14]`. The 2-addr legalize sees `ops[0]=r15 ≠ result=r14`, emits `mov r14, r15` (r14 := 42 from v1), then `add r14, [r14, r14]` = 84 — `ops[1]`'s value at r14 was CLOBBERED by the mov before the add read it. The pattern is `result = ops[0] OP ops[0]` post-clobber: 42+0 → 84; 6*7 → 36; 50-8 → 0. **Fix shape (architectural, not workaround)**: at the regalloc tier, when allocating the result of a `requires2Address` instruction, EXCLUDE operand[1..N]'s currently-assigned physical registers from the candidate set. Operand[0] alias remains permitted (legitimate 2-addr coalesce — legalize emits no mov). New `tryAllocateExcluding(free, cls, crossesCall, excluded)` in `src/lir/lir_regalloc.cpp`; excluded-set computed from `flow.positionToInst[r.start]` → producing instruction → schema's `requires2Address` flag → source operand registers (physical via `LirReg.id` field directly, or vreg via the in-progress `out.assignments` table). **Bug-class coverage**: closes the silent-miscompile family for `add`, `mul`, `sub`, `sdiv`, `smod`, `shl`, `shr_l`, `shr_a` — any `requires2Address` op uniformly. **7-agent audit fold critical fixes**: (1) **HIGH-1 silent-failure** — spill fallback was exclusion-blind; `findSpillCandidate` extended with the same `excluded` span so the evictee's physical ordinal is never in operand[1..N]'s set under register pressure (the bug would have recurred under high pressure pre-fold). (2) **HIGH-2 silent-failure** — `std::array<uint16_t, 4> excludedStorage` silently truncated mid-loop on a future N-ary 2-addr op; replaced with a fail-loud `regallocFatal` BEFORE the loop when `ops.size() > excludedStorage.size() + 1u`. Surfaces schema misconfigurations at the schema-author tier, not at runtime via a re-introduced clobber. (3) **HIGH-3 silent-failure** — use-only vregs (`r.start == 0`, the verifier-rejected shape) silently misaligned the exclusion to a wrong instruction; added `lir.instResult(producingInst) == r.vreg` equality check that skips the exclusion when the looked-up inst doesn't define this range. (4) **4-agent convergence on `commutative` field removal** — code-simplifier + type-design + code-architect + comment-analyzer + code-reviewer all flagged the prior `commutative` schema field as dead substrate: declared, JSON-loaded, declared-on-rows, but never read by any production code (`lir_2addr_legalize.cpp` doesn't consult it, despite the docblock claim). The regalloc-tier fix is "Universal across commutativity" — both arms get the same exclusion. Field + JSON loader + JSON rows removed; misleading docblock dropped. **Always-run regression pins** (3 commutativity-spanning examples): `examples/c-subset/arithmetic/main.c` (`return 6 * 7;` — commutative, mul) + `examples/c-subset/subtraction/main.c` (`return 50 - 8;` — non-commutative, sub) + `examples/c-subset/register_pressure/main.c` (7 globals + 7 chained `+` operations — exercises the exclusion under multi-operand pressure, pins the "exclusion doesn't cause spurious spills" property). All exit 42 (or 0 for implicit_return_zero); a regression in regalloc exclusion, spill-aware fallback, legalize semantics, encoder, or walker flips the exit code and fails the always-run harness immediately. **Integrated_tests CMake gate**: `add_subdirectory(integrated_tests)` moved INSIDE the `if(DSS_BUILD_TESTS)` block so `add_test()` doesn't silently no-op when the gate is off (the bare placement made the test invisible to ctest under DSS_BUILD_TESTS=OFF). 147/147 ctest; 40/40 integrated_tests. **Substrate-tier mechanism pin** (audit-fold post-commit, 2026-06-02): `tests/lir/test_lir_regalloc.cpp::LirRegAlloc.Requires2AddressResultExcludesOpsOneThroughN` walks the produced LIR, finds every `requires2Address` instruction, and asserts `result.physReg().id != ops[k].physReg().id` for all source operands k≥1. This pins the MECHANISM at the regalloc-tier boundary (a refactor that "got lucky" with the chosen e2e example inputs but broke the exclusion would still pass `arithmetic`/`subtraction`/`register_pressure` examples; this unit test catches such a regression independently). Closes the prior `D-LIR-REGALLOC-2ADDR-UNIT-TESTS` anchor in the same cycle per the standing "no gaps" rule. | — | — |
| D-LK1-RODATA | 🟡 **EXEC RODATA CLOSED 2026-06-09 (= the `D-LK1-ELF-EXEC-DATA-SECTIONS` rodata path).** The ELF ET_EXEC walker now emits `.rodata` from `dataItems` (both `elf64-*-linux-exec` declare the `.rodata` row + `supportedDataSections:["rodata"]`; the `linker.cpp` capability gate is CONSULTED, not branched — one shared elf.cpp path, arch via config). Runtime-proven: `global_int` x86_64 + arm64 ELF arms → exit 42 (WSL + qemu). DEFERRED: the **ET_REL (.o) rodata** arm (a relocatable `.o` carrying rodata rides the symbol+section table, a separate producer/format concern — `elf.cpp` fails loud on `.o` dataItems today). Writable `.data`/`.bss` ✅ CLOSED 2026-06-19 (`D-LK4-DATA-PRODUCER-MUTABLE-GLOBAL` — a 2nd R+W PT_LOAD). **Original:** Emit `.rodata` section from `AssembledModule.dataItems` in the ELF walker. Same shape as the PE arm but ELF-specific: `.rodata` is between `.text` and the section-headers. The `.rodata` row in `elf64-x86_64-linux-exec.format.json` needs `sh_flags = SHF_ALLOC`, `sh_type = SHT_PROGBITS`. ELF executable arm vs relocatable arm both need the section. Tests pin section-header table entry + bytes. Lower the linker.cpp dataItems-guard for ELF formats when this closes. | **First slice with a producer that populates `dataItems` for ELF-Exec format** OR **dedicated ELF walker rodata cycle**. | First Linux-target build with rodata content. |
| D-LK3-RODATA | ✅ **EXEC `__const` ARM RUNTIME-PROVEN 2026-06-09** (run **27192415923**, `macos-clang-release` leg ran `global_int` macos-arm64 → ctest **#184 Passed** → exit 42 on real Apple Silicon: the `__TEXT,__const` data global was READ at runtime) — the parallel of how `D-LK1-RODATA` was reconciled for ELF, and the same closure surface + witness as `D-LK1-MACHO-EXEC-DATA-SECTIONS` (registry, now ✅). The Mach-O EXEC walker (`src/link/format/macho.cpp` `encodeExecDynamic`) emits a loadable read-only `__TEXT,__const` section from `module.dataItems` (gated on `hasConst`; no-dataItems output byte-identical) via the shared `src/link/format/exec_data_section.hpp` helper; `macho64-arm64-darwin-exec.format.json` declares the `__const` row + `supportedDataSections: ["rodata"]` (the `linker.cpp` capability gate is CONSULTED, not branched). The `global_int` `arm64:macho64-arm64-darwin-exec` corpus arm (`runOn: ["darwin"]`) COMPILES — `__const` holds `2a 00 00 00`; byte-pinned by the `MachOArm64Exit.MachoArm64Const*` cases in `tests/link/test_macho_arm64_exit.cpp` — AND, per run 27192415923, RUNS on Apple Silicon → exit 42. **SCOPE CLOSED THIS CYCLE = `__const` (generic read-only) for the arm64-darwin exec only.** writable `__DATA` (`__data`+`__bss`) ✅ CLOSED 2026-06-19 (`D-LK4-DATA-PRODUCER-MUTABLE-GLOBAL`). STILL DEFERRED (mirrors how ELF left its analogues — do NOT over-close): the `__TEXT,__cstring` string-literal sub-kind (no string corpus yet — content-heuristic vs `AssembledData` sub-kind decision still open), the ET_REL `.o` rodata arm, the data→data dataItem-reloc path (`D-LK1-MACHO-RODATA-DATAITEM-RELOC`), and the macOS-x86_64 form (`D-LK1-MACHO-X8664-DARWIN-DATA-SECTION`). **Original:** Emit `__cstring` (string literals) / `__const` (other read-only data) sections in the Mach-O walker. Mach-O segregates string literals from generic const data: `__TEXT,__cstring` for null-terminated C strings, `__DATA_CONST,__const` for other const objects. The walker may need a sub-kind on `AssembledData` to distinguish ("string-like" vs "const-generic"), OR consume content heuristically (NUL-terminated ASCII → __cstring, else __const). Schema-side declares both rows. Mach-O is anchored last because its segment+section two-level naming and dyld-chained-fixups interactions require additional cycle scope. Lower the linker.cpp dataItems-guard for Mach-O formats when this closes. | **`__cstring` arm + `__DATA` + ET_REL `.o` rodata + macos-x86_64 form remain open** (the `__const` arm64-darwin exec arm is ✅ runtime-proven). | ✅ exec `__const` arm64-darwin arm RUNTIME-PROVEN (run 27192415923 #184, exit 42 on Apple Silicon); `__cstring`/`.o`/`__DATA`/x86_64 open. |
| ~~D-LK4-RODATA-BSS-INVARIANT~~ | ✅ **CLOSED 2026-06-02 — Slice 2 type-design fold.** New `validateAssembledData(span, reporter)` function at `src/asm/asm.{hpp,cpp}` enforces 3 substrate invariants on `AssembledData` items: (1) `Bss + non-empty bytes` rejected with `K_NoMatchingObjectFormat` — Bss is zero-fill so the wire format reserves `sh_size` without storing bytes; a non-empty Bss item is a substrate-shape violation. (2) Duplicate non-sentinel `SymbolId` rejected with `K_SymbolUndefined` — duplicates would silently let the linker's last-write win symbol→VA resolution. Sentinel `SymbolId{}` is exempt (anonymous-data marker; multiple anonymous items legitimate). (3) Power-of-two alignment — enforced structurally by the `Alignment` newtype (cannot construct non-pow2), documented as tautology in validate(). Wired into `src/link/linker.cpp` BEFORE the F1 walker-precondition guard so producer errors surface before missing-walker errors. Tests: `ValidateAssembledDataRejectsBssWithNonEmptyBytes` + `ValidateAssembledDataAcceptsBssWithEmptyBytes` + `ValidateAssembledDataRejectsDuplicateSymbolIds` + `ValidateAssembledDataAcceptsMultipleAnonymousItems`. | — | — |
| ~~D-LK4-RODATA-WIDE-ALIGNMENT-NEWTYPE~~ | ✅ **CLOSED 2026-06-02 — Slice 2 type-design fold.** New `class Alignment` newtype at `src/core/types/alignment.hpp` enforces power-of-two byte alignment in the range [1, 256] at construction time. Two factory paths: runtime `Alignment::fromBytes(uint32_t)` returning `optional` (validates pow2 + range; rejects 0 / non-pow2 / >256), compile-time `Alignment::of<N>()` `consteval` with static_assert on the same predicates. Stores log2 in a uint8_t. Provides `bytes()` (the alignment quantum), `log2()` (for Mach-O `__align` / PE `IMAGE_SCN_ALIGN_*` shift encoding), and `alignUp(uint64_t)` (canonical `(n + a - 1) & ~(a - 1)` kernel). `AssembledData::alignment` changed from `uint32_t` (4 GiB-wide for a quantity capped at 256) to `Alignment`. Test `AlignmentNewtypeRejectsZero` pins the rejection arms + `alignUp` kernel correctness. | — | — |
| ~~D-LK4-RODATA-SECTION-NARROW~~ | ✅ **CLOSED 2026-06-02 — Slice 2 type-design fold.** New `enum class DataSectionKind : uint8_t { Rodata, Data, Bss }` at `src/core/types/section_kind.hpp` constrains the producer-emittable subset (3 values) of the wider `SectionKind` (12 values). The 9 walker-synthesized kinds (`Text`, `Symtab`, `Strtab`, `ShStrtab`, `RelocTable`, `Dynamic`, `Note`, `Debug`, `Custom`) are now compile-time-unreachable from a producer constructing `AssembledData`. Total `toSectionKind(DataSectionKind)` + partial `dataSectionKindOf(SectionKind)` conversions plus `dataSectionKindName()` helper. `AssembledData::section` changed from `SectionKind` to `DataSectionKind`; default remains `Rodata`. Walker side still keys on `SectionKind` (the linker format walker reads format-schema rows by full kind). Test `DataSectionKindNarrowAdmitsOnlyDataSections` pins the conversion table for all 12 kinds. | — | — |
| ~~D-LK10-ENTRY-ML7-FRAME-BIAS-UNIFY~~ | ✅ **CLOSED 2026-06-02 — ML7 + trampoline now share unified frame-layout via `alignedSizeWithBias`.** Substrate: NEW `callPushBytes: uint16_t` field on `TargetCallingConvention` (target_schema.hpp) — x86_64 declares 8, ARM64 declares 0. JSON loader + validator (`< stackAlignment` invariant + `hasAbiInfo` gate inclusion). `computeFrameLayout` in `lir_callconv.cpp` extended with `hasCalls` parameter; non-leaf functions compute `totalFrameSize = alignedSizeWithBias(max(savedRegs+spill, cc.shadowSpaceBytes), cc.stackAlignment, cc.callPushBytes)` — same helper the trampoline emitter uses, with the cc's `callPushBytes` as bias instead of `entryStackPointerBias`. The two fields ARE distinct semantic facts (kernel→trampoline transition bias vs in-program CALL push) — coincide on Win64 (both 8) but diverge on Linux x86_64 (entry=0 via kernel JMP, call=8 via CALL push). NEW `functionHasCalls` helper scans LIR via `TargetOpcodeInfo::isCall` (catches both `call` AND `call_indirect_via_extern` exhaustively). NEW `FrameLayout.hasCalls` field exposed for downstream consumers (debug-info unwind, audit tests). Materialize loop in `materializeOneFunc` migrated from `op == h.call \|\| op == h.callIndirectViaExtern` to `info != nullptr && info->isCall` (silent-failure H1 fold — gates future 3rd call-shaped opcode without code edit). `resolveOpcodes` made `call_indirect_via_extern` lookup OPTIONAL so ARM64 (which doesn't declare the mnemonic yet — GOT/PLT macro-op anchored at D-LK10-ENTRY-ARM64) doesn't fail-loud (code-reviewer C1 fold). Co-closed: D-FF6-RUNTIME-PRINT (the actual blocker for hello_puts), D-FF6-HELLO-PUTS-PROMOTION-PIN, D-LK6-2A-MULTI-LIBRARY-PIN. Substrate-tier pin: `NonLeafFunctionReservesShadowSpaceAndAlignmentBias` in `tests/lir/test_lir_callconv.cpp` (discriminates non-leaf vs leaf via independent isCall scan + cross-checks SUT's hasCalls flag); MIR→LIR opcode-selection pin: `ExternCallEmitsCallIndirectViaExternOpcode` + `NoExternImportsAllCallsLowerAsDirectCall` in `tests/lir/test_mir_to_lir.cpp`. E2E pin: `examples/c-subset/hello_puts/` runOn=["windows"] expectedStdout="hello\r\n". | — | — |
| ~~D-LK4-1~~ | ✅ **CLOSED 2026-05-29..30 at LK1 / LK2 / LK3 (per-format walkers all three arms shipped).** ELF walker emits Elf64_Ehdr + section headers + .text/.rela.text/.symtab/.strtab/.shstrtab per gABI; PE walker emits IMAGE_FILE_HEADER + IMAGE_SECTION_HEADER + IMAGE_RELOCATION + IMAGE_SYMBOL + string table per Microsoft PE Format §3-5; Mach-O walker emits mach_header_64 + LC_SEGMENT_64 + section_64 + LC_SYMTAB + nlist_64 + relocation_info per Apple OS X ABI. Cycle 2 (ET_EXEC / .exe / MH_EXECUTE) shipped alongside LK6 cycle 1 reloc apply + LK6 cycle 2a–d dynamic linking. (Historical scope: per-format byte-emission walkers — `LinkedImage::bytes` was empty at substrate-only LK4.) — (closed) |
| ~~D-LK4-2~~ | ✅ **CLOSED 2026-05-29 at LK1 cycle 1.** Section / symbol-table JSON schema rows landed. `ObjectFormatSectionInfo{kind, name, type, flags, addrAlign, entrySize}` + format-specific identity sub-blocks (`ElfIdentity` / `PeIdentity` / `MachOIdentity` / `PeOptionalHeader` / `MachOImage`) all populated and validated per shipped format. Symbol-table layout fields fold into per-format walker (no shared substrate `symbolTable.layout` field needed — proved unnecessary across 3 walkers). (Historical scope: `sections[]` + `dynamic[]` + `symbolTable.layout` JSON schema rows.) — (closed) |
| ~~D-LK4-3~~ | ✅ **CLOSED 2026-06-04** (cycle 15, supervised — the LK11 symbol-namespace prerequisite, built ahead of the cross-CU merge per the user-sequenced pre-OPT7 chain). New `src/link/symbol_kind.hpp` keys the linker by the compound `(CompilationUnitId, SymbolId)` (`LinkedSymbolKey` + `std::hash`); `AssembledModule.cuId` carries the identity (stamped `= cu.id()` in `compile_pipeline.cpp`); `link()` is now a `std::span<AssembledModule const>` entry + a 1-element backward-compat overload (every single-CU caller source-unchanged); the 3 parallel symbol sets collapsed into one `unordered_map<LinkedSymbolKey, SymbolKind>` (**co-closes D-LK4-RODATA-LINKER-SYMBOL-KIND-MAP**). Collision pin `Linker.CrossCuSymbolIdCollisionDisambiguatedByCuId` (2 CUs, colliding `SymbolId#42`, distinct cuId → `symbolCount == 2` + no false dup) is REGRESSION-PROVEN RED when cuId is dropped from the key. The (b) FFI sub-trigger was already satisfiable single-CU (extern imports share the CU arena); the (a) cross-CU **merge** that consumes N>1 modules is **LK11** — N>1 today fail-louds `K_CrossCuMergeUnsupported = 0x8010` (honest, not silent). 193/193; self-audit clean. **Original anchor:** Symbol-namespace promotion: `SymbolId` → `(CuId, SymbolId)`.** v1 substrate assumes single-CU: `link()` indexes `module.functions[].symbol` into a single `unordered_set<SymbolId>` (`linker.cpp:35`) and resolves all relocs against that set. `SymbolId` is per-arena, so two CUs can mint the same integer for different symbols. Triggers in TWO independent ways (architect convergence): (a) cross-CU linking — multiple CUs in one image; (b) **FFI import resolution** — an `extern` symbol from a precompiled library arrives as a `Relocation::target` that no `AssembledFunction` declares, so LK4 today fires `K_SymbolUndefined` on every FFI ref. | **EITHER LK6** (dynamic linking + imports — FFI table arrives) **OR LK11** (cross-CU linking), whichever lands first. The compound-key promotion + the import-table consultation are one PR's worth of work and should NOT split. | First FFI import to a precompiled lib, OR first multi-CU image artifact. |
| D-LK4-4 | **`LinkedImage` strong-id (LinkedImageId).** v1 substrate uses `LinkedImage` as a value type; no monotonic id. Future incremental linking (open question #3) + LSP "show linked image" introspection would want O(1) identity. | **LK11** OR a dedicated cycle when incremental linking lands (open question #3 — post-v1). | First incremental-link cache lookup. |
| D-LK4-5 | **Wire-declared relocation addend bias** — same anchor as plan 13 D-AS4-4. Assembler currently hardcodes `Relocation::addend = 0` at the walker emit sites; linker substrate respects whatever the assembler stamped. Future ISAs whose linker formula expects a non-zero addend will need both sides updated together. | **Plan 13 D-AS4-4** is the canonical anchor (assembler-side change first); LK substrate gains formula-application logic in **LK6** (dynamic linking + imports — the first reloc actually applied to bytes). | First non-zero-addend formula. |
| ~~D-LK11-IMAGE-EMIT-MERGE~~ ✅ **CLOSED 2026-06-04 (LK11b)** — merged-image byte emission landed: N>1 `link()` pre-merges the resolved CUs into one combined `AssembledModule` (symbol remap → fresh unified ids; functions/data/externs concatenated; relocations retargeted via one chokepoint; cross-CU ambiguous-entry guard) and reuses the EXISTING single-CU format walker (zero walker rewrite). `K_CrossCuImageEmitDeferred` retired. Byte-pin `CrossCuMergeEmitsBothCusFunctionBytes` + reloc-retarget pin REGRESSION-PROVEN. Remaining (separate anchors): cross-CU REFERENCE byte-patching (extern→sibling-def reloc; `mergeModules` fail-louds today, gated on the cross-CU-call) + the source→N-CU driver (D-LK11-MULTI-CU-DRIVER) + extern-import dedup (D-LK11-EXTERN-IMPORT-DEDUP). **Original scope follows.** | **Merged-image byte emission for N>1 modules** — cross-CU section layout + VA assignment + cross-module relocation application + per-format walker, producing one runnable image from N CUs. LK11a (cycle 16, `98affef`..) resolves cross-CU DEFINITIONS (weak-vs-strong → `LinkedImage::resolvedGlobalDefs`) but emits NO merged bytes; `K_CrossCuImageEmitDeferred = 0x8012` fail-louds the byte step honestly. **Prerequisite landed (cycle 17):** `compileSingleUnit` split into `assembleUnit` (per-CU → `AssembledModule`) + `linkAndWrite` (shared link+write over a `std::span<AssembledModule const>`) — the N-CU-assembly → single-merged-link shape the pre-merge consumes. The pre-merge itself (build a combined `AssembledModule`: unified symbol remap + intra-CU reloc retarget + extern remap, then reuse the existing single-CU walker; the byte-pin test needs the full-emission format setup, not `test_linker`'s resolution-only minimal) is the remaining LK11b work. | **Multi-CU build driver lands** (D-LK11-MULTI-CU-DRIVER) OR first multi-CU runnable artifact — real merged bytes need real multi-CU input to verify. | plan 14 LK11b; pairs with D-LK11-MULTI-CU-DRIVER. |
| D-LK11-EXTERN-IMPORT-DEDUP | **Coalesce duplicate extern imports across merged CUs.** When N CUs each import the SAME library symbol (e.g. both `printf`), the LK11b merge remaps each to a distinct merged id → the combined module carries N `ExternImport`s of one `mangledName` → the walker emits N IAT/GOT entries. CORRECT (all resolve to the same dynamic symbol) but redundant. Closure: dedup by `(mangledName, libraryPath)` during the merge, routing all referencing relocations to one import id. Size/quality, NOT correctness. | **First multi-CU image importing the same library symbol from >1 CU** (needs the source→N-CU driver). | plan 14 LK11b; normal; surfaced by the LK11b review. |
| ~~D-LK11-MULTI-CU-DRIVER~~ | ✅ **CLOSED 2026-06-05 (cycle 19, the FIRST runnable cross-CU-call binary)** — `Program::compileUnits` (two overloads) builds ONE CompilationUnit PER source file → N AssembledModules → one `linker::link(span)` merge, distinct from the unchanged `compileFiles` (CU5 multi-file-single-CU). The CLI routes multi-file `--compile` to it by gcc/clang semantics (`args.sourceFiles.size() > 1 ? compileUnits : compileFiles` — a translation-UNIT-count branch, never an identity branch; single-file stays byte-identical on `compileFiles`). The "extern-without-library" cross-CU expression resolves via the **thunk slot** in `mergeModules`: each `resolvedCrossCuRefs` edge mints an 8-byte rodata GOT-like slot carrying ONE abs64 fixup (kind found by FORMULA `widthBytes==8 && !pcRelative`, agnostic) to the sibling def, retargets the indirect call's reloc to the slot, and strips the shadowed library import. The PE walker applies the data-item reloc + emits a `.reloc` base-relocation section (ASLR DIR64 fixups) + data directory [5]. BOTH corpus harnesses (in-process `examples_runner` + CLI-subprocess `integrated_tests`) read `"sources":[...]`. Proven end-to-end by `examples/c-subset/cross_cu_call` (`main.c` calls `extern add5`; `helper.c` defines it; exit 42) + the cross-platform `.reloc` byte-pin `PeExecWriter.Abs64RodataSlotEmitsDir64BaseRelocation`. 194/194. **Remaining (separate anchors):** ELF/Mach-O exec thunk emission (D-LK11-ELF-MACHO-CROSSCU-THUNK-EMISSION); extern-import dedup (D-LK11-EXTERN-IMPORT-DEDUP). **Original scope follows.** Multi-CU build-driver orchestration — N source files → N CompilationUnits → N AssembledModules → one `linker::link(span)` call. **CU-boundary finding (cycle 17):** the driver must NOT change `compileFiles` — it builds ONE multi-file CU per **CU5**; a multi-CU build is a NON-breaking new entry. | — | — |
| ~~D-LK11-EXTERN-VS-DEF-PRECEDENCE~~ | ✅ **CLOSED 2026-06-04 (cycle 16, LK11a)** — the precedence RESOLUTION landed: an `ExternImport` whose name is DEFINED in a sibling CU (Global/Weak) BINDS to that definition (the definition shadows the extern declaration), recorded as a `(reference → definition)` edge in `LinkedImage::resolvedCrossCuRefs` (`resolveCrossCuSymbols` step 2); an extern with no cross-CU definition stays a real FFI import; a relocation to an undeclared symbol fail-louds `K_SymbolUndefined` (step 3). 3 tests pin the edge keys / FFI-unchanged / undefined-reloc. **Remaining (other anchors):** patching the referencing relocations to the def's address = D-LK11-IMAGE-EMIT-MERGE; the multi-CU driver's expression of cross-CU externs (an "extern-without-library" form — today `buildCompoundIndex` requires a non-empty `libraryPath`) = D-LK11-MULTI-CU-DRIVER. | — | — |
| D-LK11-FORMAT-WEAK-SEMANTICS | **Format-specific weak / multi-definition semantics beyond ELF-lineage weak DEFINITIONS.** LK11a models the `SymbolBinding{Local,Global,Weak}` weak-DEFINITION rules (strong shadows weak; all-weak deterministic by lowest (cuId,SymbolId)). NOT yet modeled: COMDAT / identical-code-folding (PE + ELF — "one of N identical copies", e.g. C++ inline / template instantiations); Mach-O `N_WEAK_REF` (weak REFERENCE — distinct from `N_WEAK_DEF`); PE `/ALTERNATENAME` (fallback alias, no `SymbolBinding` equivalent). Each is a config / schema-declared distinction, NEVER an `if (format == ...)` branch. | **First front-end emitting COMDAT-style multi-def, OR first Mach-O weak-reference / PE alternatename symbol.** | plan 14 LK11; trigger-gated. |
| D-LK11-ELF-MACHO-CROSSCU-THUNK-EMISSION | **ELF / Mach-O EXEC emission of the cross-CU thunk slot.** The LK11b cross-CU-call merge mints a rodata thunk slot (8-byte abs64 pointer to the sibling def) the loader rebases via a base relocation. The **PE** exec walker fully ships this (data-item reloc apply + `.reloc` DIR64 section + data dir [5], cycle 19). ELF + Mach-O do NOT yet emit rodata data items in their exec walkers (the `.data/.rodata` + base-reloc arms are deferred — see D-LK1-RODATA / D-LK3-RODATA), so a cross-CU link targeting an ELF/Mach-O exec **fails LOUD** today via the schema-driven data-section capability gate (`acceptsDataSection` → `K_NoMatchingObjectFormat`), never a silent drop. Closure = the ELF (`.rodata` + `.rela.dyn`/`R_X86_64_RELATIVE` or GOT) and Mach-O (`__got` + rebase/bind opcodes) exec rodata + base-reloc walker arms; the merge substrate is already format-agnostic (the slot is a plain rodata `AssembledData` + abs64 reloc found by formula). Pinned by `tests/link/test_linker.cpp::CrossCuRetargetAndStripPatches` (the non-exec test-elf object correctly rejects the slot loudly). **NEVER an `if (format == ...)` branch** — each walker declares `supportedDataSections` + emits its native base-reloc form. | **First ELF or Mach-O cross-CU runnable artifact** (an ELF/Mach-O analogue of `cross_cu_call`), OR D-LK1-RODATA / D-LK3-RODATA closing the exec rodata arm. | plan 14 LK11b; format-triggered; pairs with D-LK1-RODATA / D-LK3-RODATA. |
| D-LK-TEXT-ABS64-BASE-RELOC | **Base relocations for in-`.text` absolute fixups (PE exec).** The cycle-19 `.reloc` collector scans only `module.dataItems` for abs64 (DIR64) fixups — the only abs64 producer today is the cross-CU thunk slot (a rodata item), and the call site itself is RIP-relative (`FF 15`, rel32, no base reloc). So an absolute address materialized into **`.text`** (e.g. a future `movabs reg, &func` / absolute jump table) would ship UN-rebased and crash under ASLR. UNREACHABLE today (no codegen path emits an in-`.text` abs64). Closure: extend the base-reloc collector to also scan `.text` function relocations for width-8 non-pcRelative kinds, OR a fail-loud guard in `applyExecRelocations` rejecting an in-`.text` absolute width-8 reloc until the collector is wired (matching the existing width!=8 fail-loud in the data-item path). | **First codegen path emitting an absolute 64-bit address into `.text`** (movabs &sym, absolute jump table, large-model addressing). | plan 14; trigger-gated; raised by the cycle-19 PE base-reloc review. |
| D-LK11-COMPILE-DIRECTORY-CU-MODE | **`--directory` compilation-unit mode (intentional single-CU, pinned).** Cycle 19 made `--compile a.c b.c` MULTI-CU (gcc/clang: each file is its own translation unit, `compileUnits`). But `compileDirectory` (the `--directory <path>` flag, `program.cpp` → `compileFiles`) deliberately stays SINGLE-CU — a directory scan is treated as ONE CU5 multi-file unit (cross-file refs resolved WITHIN it), the existing v1 contract. This is a real divergence from `--compile`'s TU-per-file semantics, recorded here so it is NOT mistaken for an oversight: whether a directory build is one module or N modules is a genuine project-model decision (it depends on the artifact profile), NOT a mechanical consistency fix. Closure = a deliberate boundary-policy decision (likely plan 06 artifact-profile-driven: a profile declares whether its source tree is one CU or per-file CUs) OR a `--directory` mode flag — never a silent flip. | **First artifact profile (plan 06) that needs a per-file-CU directory build, OR a user request for `--directory` multi-CU.** | plan 14; plan 06 AP; pinned by the cycle-19 self-audit (an intentional scope boundary, not a gap). |
| ~~D-LK4-7~~ ✅ **closed 2026-05-29 same session as LK4 substrate (+ 7-agent re-review fold)** | Cross-side shared substrate for `relocations[]` loader + validator + `DiagnosticCollector`. | Closed by `src/core/substrate/{diagnostic_collector,relocation_table}.hpp` — template `loadRelocationsTable<RowT>` (+ no-extension overload) + `validateRelocationsTable<RowT>` consumed by both `target_schema_json.cpp` and `object_format_schema_json.cpp`; `DiagnosticCollector` consumed by 3 JSON loaders (target / grammar / object-format). Plan 13 §2.6 reloc-taxonomy unifier is now identical-by-construction on both sides. **7-agent re-review fold-in (post-audit)**: 4-agent convergence on `extendRow` doc-vs-code contract → comment fixed + target-side returns false after emit on bad formula (no partial row escapes indices); 2-agent convergence on public `diagnostics` field hazard → encapsulated as private + `emitRaw(ConfigDiagnostic&&)` + `release() &&` rvalue-extract (3 callsites migrated, day-1 back-door closed); silent-failure HIGH on loader-side dup-kind silent first-wins → defense-in-depth loader detection added (validator becomes belt-and-suspenders); 3-agent convergence on empty-extension boilerplate → no-extension overload; type-design LOW on `concept relocation_row<T>` → added (better template errors + self-documents the {name, kind} contract); comment-analyzer on stale provenance + "enforced upstream" wording → fixed; test-analyzer HIGH on `extendRow == false` skip branch dead → new `tests/core/substrate/test_relocation_table.cpp` with 9 substrate tests covering the skip contract + loader-side dup-kind + zero-kind cross-pass split + DiagnosticCollector encapsulation. −170 LOC net for the substrate hoist itself; the review fold adds back ~70 LOC of encapsulation + tests. 112/112 ctest. | — (closed) |
| D-LK4-6 | **`ObjectFormatRelocationInfo` enrichment** — currently `{name, kind, nativeId}` only. Per-format reloc rows likely need `width: bits`, `offsetWithinSection: bits`, `applyAlignment: bits`, `requiresGot/Plt/Tls: bool`. Format-side counterpart to plan 13's eventual `TargetRelocationInfo` decomposition. | **LK6** (dynamic linking + imports — the cycle that actually applies a formula). The PR enriches both `TargetRelocationInfo` and `ObjectFormatRelocationInfo` in parallel + the JSON loader on both sides + validate() pairing. | First reloc actually applied to output bytes. |
| D-LK1-1 | **Real symbol-name thread through HIR → MIR → LIR → AssembledFunction.** Today `AssembledFunction.symbol` is just a `SymbolId`; the ELF writer synthesizes section-local names like `"sym_42"`. Hermetic linking against another `.o` we produce works (we resolve by SymbolId index), but consumers like GNU `objdump`/`readelf` / a system linker / a debugger see anonymized names and cross-toolchain interop breaks. The name is already known at HIR-decl time; the thread needs to flow through the IR layers + sit on `AssembledFunction` as `std::string symbolName`. | **LK7** (codesign + publish) is the natural trigger — that's the first cycle whose acceptance criterion requires the produced binary to interoperate with platform tooling (codesign reads symbol names; signtool's hash machinery consults debug info). | First cross-toolchain interop test (system linker reading our `.o`, debugger reading our DWARF). |
| ~~D-LK3-1~~ ✅ **closed 2026-05-30 (LK3 cycle 1)** | Mach-O segment+section two-level naming. | Closed by `ObjectFormatSectionInfo.segment: std::string` (empty for ELF/PE; validate-rejected if non-empty on ELF/PE rows; required non-empty on Mach-O rows). Mach-O JSON declares `"segment": "__TEXT"` for `__text`; walker writes the section_64 segname field from this. — (closed) |
| ~~D-LK2-1~~ ✅ **closed 2026-05-30 (LK2 cycle 2 — `.exe` half only)** | PE image path `.exe`. | Closed by `PeObjectType` enum + `PeOptionalHeader` sub-block + PE walker EXEC arm (MS-DOS stub + PE sig + IMAGE_OPTIONAL_HEADER64 + section headers derived from vector + applied intra-module relocs via shared kernel). New shipped `pe64-x86_64-windows-exec.format.json`. `.dll` half deferred to D-LK2-4. — (`.exe` closed) |
| ~~D-LK2-4~~ ✅ **closed 2026-07-16 (c152 — part 1, the entry-less `.dll`)** | **PE `.dll` arm** — `IMAGE_FILE_DLL` + `.edata` exports + `.reloc` DIR64 completeness, loader-proven (ctypes.CDLL → dss_add(2,40)=42; ASLR-rebased fn-ptr dispatch → 42; msvcrt puts import → print + 42). The original row's "AddressOfEntryPoint = DllMain" was deliberately NOT shipped: a DSS `.dll` is entry-less (AddressOfEntryPoint=0, the `link /NOENTRY` shape — legal PE, loader skips notifications); the DllMain arm is the pinned follow-up `D-LK2-DLL-DLLMAIN-ENTRY` (registry). See LK2 cycle 3 row for the full closure summary. | (closed — c152; follow-ups: `D-LK2-DLL-DLLMAIN-ENTRY`, `D-LK-DLL-TLS-MODEL`, `D-LK-PE-IMAGE-TEXT-ABS-RELOC` in the registry) | First Windows `.dll` end-to-end witness ✅ (native loader, 2026-07-16). |
| D-LK2-2 | **`IMAGE_FILE_HEADER.TimeDateStamp` policy** — currently hardcoded to 0 (deterministic builds). Future cycle for opt-in build-time stamping (and matching codesign-friendly modes). | **Plan 16 (codesign + publish)** — codesign acceptance criteria drive whether TimeDateStamp gets populated. Until then 0 is correct. | First signed Windows artifact. |
| ~~D-LK1-2~~ ✅ **closed 2026-05-30 (LK1 cycle 2)** | ELF ET_EXEC executable image path. | Closed by `elf.objectType` field + `ObjectFormatSchema.entryPoint` + `ObjectFormatSectionInfo.virtualAddress` + ELF walker ET_EXEC arm (PT_LOAD program header + e_entry derivation + sh_addr on .text + ENOEXEC-safe p_align/p_offset/p_vaddr congruence). Cycle scope: self-contained modules only (zero relocations — anchored at D-LK1-3 + LK6). New shipped `elf64-x86_64-linux-exec.format.json`. — (closed) |
| ~~D-LK1-3~~ ✅ **closed 2026-05-30 (LK6 cycle 1)** | ELF ET_EXEC relocation application. | Closed by structured formula triple on `TargetRelocationInfo` (`pcRelative` + `addendBias` + `widthBytes`) consumed by the ELF walker ET_EXEC arm: `value = S + A + (pcRelative ? -P : 0) + addendBias`, write `widthBytes` LE into `.text`. Extern symbols fail loud `K_SymbolUndefined` (anchored D-LK6-2). Bit-shift formulas (ARM64 `call26`, `adr_prel_pg_hi21`) declared with `widthBytes=0` → fail loud `K_RelocationKindMismatch` (anchored D-LK6-1). Closes LK6 cycle 1 scope (intra-module relocs only). — (closed) |
| ~~D-LK1-4~~ ✅ **fully closed c151 (2026-07-16)** | **ELF ET_DYN (PIE / .so) path.** ★ **SHARED-LIBRARY HALF CLOSED c150 (2026-07-16, roadmap A1 part 1)** — new shipped `elf64-x86_64-linux-dyn.format.json` (`elf.type: "dyn"`, base-0 VAs via the validate-pinned `text.virtualAddress == pageAlign`, `artifactProfiles ["lib"]`, `dataImportBinding "got-indirect"`, optional `elf.soname` → DT_SONAME) routes through the SAME `encodeElfExecDynamic` substrate with the dyn divergences: `e_type=ET_DYN`, `e_entry=0` (no trampoline — schema declares no `processExit`), no PT_PHDR/PT_INTERP/`.interp`, real-named `.dynsym` EXPORTS (externally-visible defined functions + data globals, GLOBAL/WEAK DEFAULT, real st_value/st_shndx/st_size) findable via the single-bucket SysV `.hash` chain, `R_X86_64_RELATIVE` rows in `.rela.dyn` for every internal abs64 data slot (r_addend == the base-relative slot value; the merged-relro `.data` is the RELATIVE universe), extern data via GOT slots + GLOB_DAT (copy relocs validate-rejected on dyn — exec-only ELF), undefined externs KEPT (`allowsUndefinedImports()` — the c143 gate's third flavor; `isImageFlavor()` now TRUE for ET_DYN, closing the c143 latent note). END-TO-END: WSL gcc 13.3 links `gcc main.c -L. -ldss` against the DSS `.so` and RUNS (fn+data exports, fn-ptr-table dispatch under ASLR via RELATIVE, puts through PLT). Walker belts: D-LK-DYN-TEXT-ABS-RELOC / D-LK-DYN-RODATA-ITEM-RELOC / D-LK-DYN-TLS-MODEL (registry rows). ★ **PIE HALF CLOSED c151 (2026-07-16, roadmap A1 part 2)** — new shipped `elf64-x86_64-linux-pie.format.json`: `elf.type: "dyn"` + the FULL ENTRY CLUSTER (`elf.interpreter` + `processExit` (libc `exit` by-name-import) + `entryCallingConvention` + `processArgs`) — the DISCRIMINATOR between the two ET_DYN sub-shapes is cluster presence, never a new e_type (a PIE IS ET_DYN). validate() accepts an ET_DYN schema with NONE of the cluster (.so) or ALL of it (PIE); half-configured states reject loud naming the missing members; soname + cluster rejects (a PIE is not a library); `isExecFlavor` gained the ET_DYN-with-cluster arm (entry machinery legal there). elf.cpp: `isPie = isDyn && processExit` sub-mode (walker belt re-checks interpreter/processExit coherence for hand-built schemas) — PT_PHDR + PT_INTERP + `.interp` return at base-relative VAs, e_entry = the linker-prepended `_start` trampoline's base-relative VA (functions[0], the same processExit-keyed injection as ET_EXEC — zero linker gate changes), DT_FLAGS_1 = DF_1_NOW | DF_1_PIE (0x08000001, gcc 13.3 ground truth "NOW PIE"); everything else keeps the c150 dyn shape (RELATIVE rows, symbol-based extern-address rows, got-indirect data imports, exports). POLICY: `allowsUndefinedImports()` = FALSE for the PIE (exec semantics — keyed on the cluster; the .so keeps TRUE); `outputExtension` names a PIE like an executable (no `.so` suffix). END-TO-END (WSL glibc, ./prog direct execve at randomized bases): W1 exit-42 → 42 twice; W2 const fn-ptr-table dispatch (relro→.data RELATIVE rows witnessed by readelf) → 42 twice; W3 extern puts through the PLT → prints + exit 0. readelf pins: Type "DYN (Position-Independent Executable file)", Entry 0x1000, PHDR + INTERP phdrs, FLAGS_1 "NOW PIE". New corpus example `examples/c-subset/pie_exit` (pie target arm, runOn linux — the CI linux leg runs it natively). Tests: ElfPieFormatJson.{ShippedPieFileLoadsWithPieShape, PartialEntryClusterRejectedAtLoad, SonameWithEntryClusterRejectedAtLoad} + ElfPieWriter.{HeaderPinsNonZeroEntryPhdrInterpFlags1Pie (incl. the .so NOW-only/zero-entry/no-interp control), TrampolinePrependedExactlyOnceAtFunctionsZero} + the pie arm on ElfDynLinker.ReferencedNoLibraryExternKeptForDynRejectedForExec + TargetSpec.OutputExtensionElfDynSoAndPieNoExtension. TLS-in-PIE stays behind D-LK-DYN-TLS-MODEL (schema ships no tlsAccess/tdata/tbss; local-exec would be model-correct for a PIE's own thread-locals but the PT_TLS-under-slide layout is unwitnessed). | (closed) | First Linux `.so` end-to-end ✅ (c150); first PIE executable end-to-end ✅ (c151). |
| D-LK1-5 | **Cross-architecture page size on PT_LOAD.** LK1 cycle 2 walker hardcodes `p_align = 0x1000` (Linux x86_64 4 KB pages). ARM64 Linux uses 64 KB pages on some configurations (Apple Silicon's Asahi, certain cloud kernels), and ELF executables loaded with a smaller `p_align` than the runtime page size fail with `ENOEXEC` at exec time. | **AS-cycle when first ARM64-Linux artifact ships**, OR **LK1 cycle 3** (multi-arch ELF). Promote `p_align` to a schema field on `ElfIdentity` (or compute from `target.pointerBitWidth + abi`) so the JSON declares the right value per (arch × OS) tuple. | First non-x86_64-Linux ELF artifact landing. |
| ~~D-LK3-2~~ ✅ **closed 2026-05-30 (LK3 cycle 2 — MH_EXECUTE half only)** | Mach-O MH_EXECUTE image path. | Closed by `MachOObjectType` enum + `MachOImage` sub-block (pageZeroSize + dylinkerPath + loadDylibs as `MachODylibRef` row type) + Mach-O walker EXEC arm (mach_header_64 + __PAGEZERO + __TEXT segment + LC_LOAD_DYLINKER + LC_MAIN + LC_LOAD_DYLIB[] + LC_SYMTAB). `__TEXT.filesize` set to byte-exact section span (NOT page-aligned vmsize — page-alignment would overlap symtab into __TEXT). MH_DYLIB deferred to D-LK3-3. N_ALT_ENTRY per-function subsection markers deferred to plan 16 codesign cycle. — (MH_EXECUTE closed) |
| ~~D-LK3-3~~ ✅ **closed c153 (2026-07-16)** | **Mach-O MH_DYLIB arm.** MachOObjectType::Dylib now has a full walker arm riding `encodeExecDynamic` (see the LK3 cycle 3 row above for the complete inventory: LC_ID_DYLIB + export trie + no-entry shape + conditional __PAGEZERO/__DATA_CONST + extern-slot binds + dylib ad-hoc signature with execSegFlags 0). The dynamic-library TRIO (ELF `.so` c150 / PE `.dll` c152 / Mach-O `.dylib` c153) is complete across all three formats. Chained-fixups-for-dylib deliberately NOT shipped — the export trie would need LC_DYLD_EXPORTS_TRIE (D-LK3-DYLIB-CHAINED-FIXUPS-EXPORT-TRIE; validate rejects the combo loud). Runtime witness = the user's Apple-Silicon dlopen/dlsym validation (steps in the cycle row); structural witnesses = 22 pins in `tests/link/test_macho_dylib_writer.cpp` incl. the dyld-algorithm trie walk. Residual dylib anchors: D-LK3-DYLIB-TLS-MODEL, D-LK3-DYLIB-WEAK-EXPORT, D-LK3-DYLIB-CHAINED-FIXUPS-EXPORT-TRIE (registry rows). | (closed — c153, branch feature/lk-ffi-3245345345) | First macOS dylib end-to-end test rides the user's mac validation. |
| D-LK4-11 | **Symbol-index builder substrate hoist** (simplifier deferral). PE and Mach-O walkers share verbatim shape for the `definedSet → externSyms vec + externSeen → symIdxBySymbol emplace-gated` two-pass pattern. ELF deliberately interleaves emit-and-map; hoisting now would force ELF to materialize an extra externSyms vec or PE/Mach-O to lose the two-pass structure. Defer until a 4th walker (WASM LK8) lands and the shape convergence is provable across 3+ consumers. | **WASM LK8** — the 4-walker trigger; pulls the shared shape into `src/link/format/symbol_table_index.hpp` (or similar) with format-specific record emit as the policy hook. | Fourth walker landing. |
| D-LK2-3 | **PE/COFF `IMAGE_SCN_LNK_NRELOC_OVFL` overflow path.** Spec §4: when a section's relocation count > 65534, set `IMAGE_SCN_LNK_NRELOC_OVFL` (0x01000000) in Characteristics AND store the real count in the first IMAGE_RELOCATION's VirtualAddress. LK2 cycle 1 walker fails loud with `K_NoMatchingObjectFormat` instead of truncating to u16 — cycle-1 modules never reach 65534 relocs in practice, but the path needs implementation when the first such module ships. | **LK6** (dynamic linking + imports — when typical modules grow large enough to hit the cap). The walker's existing fail-loud gate keeps cycle-1 correctness. | First module with > 65534 PE relocations. |
| ~~D-LK4-9~~ ✅ **closed 2026-05-30 (LK3 cycle 1)** | `StringTable` substrate hoist. | Closed by `src/link/format/string_table.hpp` — `Init` policy enum (`NulByte` for ELF/Mach-O; `U32SizePrefix` for PE). ELF + PE walkers migrated in same PR as the Mach-O addition; all three walkers now share `add(name)` / `view()` / `size()` / `release() &&`. — (closed) |
| D-LK4-10 | **`ObjectFormatSectionInfo::type` per-format sanity-gate** (type-design Q1). The substrate stores section attributes (ELF `sh_type` / PE `Characteristics`) in one `u32 type` field. A schema-config swap (ELF JSON section row inserted into a PE-tagged schema by accident) would silently reinterpret bits — ELF's `SHT_PROGBITS=1` would become a PE Characteristics value of 1 (no flags set), syntactically valid but semantically nonsense. No diagnostic today; the consequence is a malformed `.obj` that link.exe may accept silently. | **Plan 16 (codesign + publish)** OR **LK3** (the next cycle to add a format whose `type` semantics diverge) — paired with a `validate()` per-kind sanity gate that asserts the bit-pattern is consistent with the format kind (e.g. PE rejects `type < IMAGE_SCN_MEM_*` low-flag-only values for executable sections). | First format-kind mismatch surfacing in CI (e.g. `dumpbin` warning from a hand-mixed JSON test fixture). |
| ~~D-LK6-1~~ | ✅ **CLOSED 2026-06-01 at LK10 cycle 3 post-fold #2 sibling cycle.** `TargetRelocationInfo` reshaped from "structured triple (`pcRelative` + `addendBias` + `widthBytes`) + free-form formula string" to "closed-enum `RelocFormulaKind` discriminator + Linear-only structured triple". Variants: `Linear` (x86 rel32/abs32/abs64 + ARM64 abs64), `Aarch64Call26` ((S+A-P)>>2; signed-26-bit; OR into inst[25:0] with 4-byte alignment + base-bitfield-clean guard), `Aarch64AdrPrelPgHi21` (page-pair shift; signed-21-bit; ADRP-split immlo[30:29]+immhi[23:5]), `Aarch64AddAbsLo12` ((S+A)&0xFFF; ADD imm12[21:10]). JSON `formula` field promoted from human-documentation to load-bearing discriminator with closed string set (`"linear"` / `"aarch64_call26"` / `"aarch64_adr_prel_pg_hi21"` / `"aarch64_add_abs_lo12"`). Non-Linear coherence rules enforced at JSON-load: widthBytes=4 (auto-defaulted), pcRelative absent/false, addendBias absent/0 (the variant fully encodes the formula). `applyExecRelocations` kernel dispatches once on `formulaKind`; Linear path byte-identical to the LK6 cycle 1 shape (zero regression). Non-Linear arms read+OR+write the 32-bit instruction word (vs Linear's overwrite). 17 new tests in `tests/link/test_aarch64_reloc_formulas.cpp` (name round-trip + 4 JSON-loader-reject rules + 5 Aarch64Call26 cases incl. unaligned/out-of-range/dirty-base + 2 AdrPg + 2 AddLo12 + shipped arm64.target.json end-to-end load). `x86_64.target.json` + `arm64.target.json` shipped configs updated. **Paired anchors D-LK6-8 (ARM64 PLT stub bytes) + D-LK6-14 (Apple Silicon chained-fixups dispatch) remain OPEN — independent of the formula reshape (those are walker-side dispatch concerns).** 127/127 ctest. — (closed) |
| D-LK6-2 | **Extern symbol resolution (FFI / dynamic linking).** LK6 cycle 1 fails loud with `K_SymbolUndefined` when a `Relocation::target` is not defined by any `AssembledFunction` in the module. The proper resolution path arrives from plan 11 (FFI imports): extern symbols are looked up against a side-table of imports (PE IAT, ELF GOT/PLT, Mach-O bind opcodes), the linker emits the platform-specific stub + relocation type (R_X86_64_JUMP_SLOT, IMAGE_REL_AMD64_REL32 to IAT slot, X86_64_RELOC_BRANCH to dyld stub), and `widthBytes==0` non-linear formulas continue to fail loud until D-LK6-1 lands. Co-trigger with **D-LK4-3** (`SymbolId` → `(CuId, SymbolId)` promotion) since FFI imports cross compilation-unit boundaries even in single-image builds. | **LK6 cycle 2** — paired with plan 11 (FFI imports). | First FFI import to a precompiled library, OR first cross-CU symbol reference. |
| ~~D-LK6-3~~ ✅ **closed 2026-05-30 (LK6 cycle 1 post-audit fold)** | `p_align` schema field for cross-architecture page sizes. | Closed by `ElfIdentity.pageAlign: u64` populated from the JSON's `elf.pageAlign` key. `validate()` requires non-zero power-of-two `pageAlign` when `objectType == Exec` (rejects `0`, non-positive, non-power-of-two values). Walker reads `fmt.elf().pageAlign` at both the `padTo(bytes, ...)` call and the `p_align` field in the PT_LOAD program header — single source of truth across the file-offset / virtual-address congruence check. Shipped `elf64-x86_64-linux-exec.format.json` declares `"pageAlign": 4096`; future ARM64-Linux / Apple Silicon JSONs declare 16384 / 65536. Closes the silent-failure window where an ARM64-Linux ELF-exec JSON could ship without overriding the hardcoded 4 KB constant. — (closed) |
| ~~D-LK6-4~~ ✅ **closed 2026-05-30 (LK6 cycle 2b.1 + 2b.2)** | ELF GOT/PLT walker arm. | Closed across two sub-cycles: **2b.1** landed the `ElfIdentity.interpreter` substrate + JSON + shipped exec JSON declares `/lib64/ld-linux-x86-64.so.2`; **2b.2** landed `encodeElfExecDynamic()` emitting `.interp` + `.text` + `.plt` + `.dynsym` + `.dynstr` + `.hash` + `.rela.dyn` + `.got` + `.dynamic` + 5 program headers (PT_PHDR + PT_INTERP + 2 PT_LOAD + PT_DYNAMIC). **Eager binding** (DT_FLAGS_1 = DF_1_NOW + R_X86_64_GLOB_DAT in `.rela.dyn`, NOT lazy `.rela.plt` + JUMP_SLOT — eager has simpler PLT stubs, no PLT0 needed; lazy-upgrade path anchored at D-LK6-11, JSON-loader pin via `elf.bindNow`). 6-byte PLT stubs `FF 25 disp32` for x86_64; ARM64 PLT encoding anchored D-LK6-8. DT_HASH single-bucket; DT_GNU_HASH anchored D-LK6-9. PT_GNU_RELRO + PT_GNU_STACK anchored D-LK6-10. — (closed) |
| ~~D-LK6-5~~ ✅ **closed 2026-05-30 (LK6 cycle 2c — x86_64 arm)** | Mach-O dynamic-linker walker arm. | Closed for x86_64-darwin by `encodeExecDynamic` in `src/link/format/macho.cpp`: emits `__stubs` (in `__TEXT`) + `__got` (in `__DATA_CONST`) + `__LINKEDIT` + `LC_DYLD_INFO_ONLY` with the eager `bind_off` opcode stream + `LC_DYSYMTAB` + indirect-symbols table + nlist_64 with `N_UNDF\|N_EXT` extern entries + `LC_LOAD_DYLINKER` (`/usr/lib/dyld`) + `LC_LOAD_DYLIB[]`. 6-byte PLT-equivalent stubs (`FF 25 disp32` → `__got` slot) — same x86_64 shape as ELF cycle 2b.2's PLT. Eager-binding stance: `image.bindNow = true` (default) — `lazy_bind_off` stays zero (D-LK6-13). ARM64-darwin chained-fixups path anchored D-LK6-14 (separate cycle — `LC_DYLD_CHAINED_FIXUPS` is structurally distinct from `LC_DYLD_INFO_ONLY` — page-inline fixup encoding vs opcode-stream interpreter). Closes the Mach-O half of D-LK6-2. — (closed) |
| ~~D-LK6-6~~ ✅ **closed 2026-05-30 (LK6 cycle 2d)** | HIR → AssembledModule extern thread-through. | Closed by `collectExterns` MIR pre-pass + `HirToMirResult.externImports` + `MirToLirResult.externImports` + `assemble(..., externs)` parameter. `ExternImport` row hoisted to `src/core/types/extern_import.hpp` (5-tier substrate). Fail-loud at MIR pre-pass with 6 guards (nullptr ffiMap / missing entry / empty mangledName / empty importLibrary / SymbolId collision / duplicate extern / invalid TypeId) — diagnostics anchor at the HIR node's source span. `ExternFunction` main-pass arm now no-ops (pre-pass handles it). `ExternGlobal` arm stays `unsupported` (cycle scope: function-side only — globals-side anchored at D-LK6-16 when first extern global lands, paired with plan 11 FF5 binary global ingestion). Plan 11 FF5 thread-through HALF landed; binary-ingestion HALF still pending. First `printf` smoke-test paired with FF5 binary read of libc. — (closed) |
| ~~D-LK6-7~~ ✅ **closed 2026-05-30 (LK6 cycle 2b.2 — not needed)** | Multi-patch-section reloc kernel. | Closed as "not needed — walker-synthesized sections are patched inline, not through the kernel." Architect Obs 5 convergence after the cycle 2b.2 implementation proved the design: synthesized sections (PE `.idata`, ELF `.plt`, ELF `.got`) carry walker-internal glue with no corresponding `Relocation` records on `AssembledFunction`, so routing them through `applyExecRelocations` would require fabricating relocations and conflate two distinct patch categories. The kernel applies user-code-originated relocations only; synthesized sections compute their internal disp inline via the same `S - P - 4 + bias` formula. 3-format precedent (PE `.idata` slots inline + ELF `.plt` jmp-disp inline + ELF `.got` slot init inline). — (closed) |
| ~~D-LK6-8~~ | ✅ **CLOSED 2026-06-01** (Track A of parallel A+B). ARM64 ELF dynamic walker landed via per-machine helpers in `elf.cpp`: `pltStubSizeFor(machine)` (6 for x86_64, 16 for ARM64), `globDatTypeFor(machine)` (R_X86_64_GLOB_DAT=6 vs R_AARCH64_GLOB_DAT=1025), `emitPltStub(machine, ...)` dispatches to `emitX86_64PltStub` (6-byte `FF 25 disp32`) or `emitArm64PltStub` (16-byte ADRP+LDR+BR+NOP). ARM64 PLT stub encoding: ADRP x16 page-pair (signed-21-bit range-check, identical formula to D-LK6-1 Aarch64AdrPrelPgHi21); LDR x17 with 12-bit unsigned imm12 scaled-by-8 (rejects misaligned GOT slot VA as defense-in-depth); BR x17 (0xD61F0220); NOP (0xD503201F). Machine-guard at `elf::encode` relaxed from `!= 62` to `!= 62 && != 183`. `symbolVa[extern]` correctly uses `pltVa + i * pltStubSize` (was hardcoded `i * 6`). 3 new tests: ExternImportsOnArm64MachineSucceedsAfterDLK68Close (e2e with shipped arm64.target.json + format.json), Arm64PltStubLayoutPinsAdrpLdrBrNop (byte-pin BR+NOP fixed back-half), ExternImportsOnRiscVMachineFailsLoudCitingFutureWork (universality pin — RISC-V/MIPS/PPC64 still reject loudly). **Architect Q3 TU split deferred** — the per-machine TU split (extract `encodeElfExecDynamic` into `elf_x86_64_exec_dynamic.cpp` + `elf_aarch64_exec_dynamic.cpp` + `elf_exec_common.hpp`) anchored at D-LK6-8.1 for when a 3rd machine arrives OR elf.cpp grows further. The 3 per-machine helpers + 2 stub-emit functions add ~140 LOC to elf.cpp; at 2 machines, file-level cohesion still favors single-TU. Same treatment slated for `macho.cpp::encodeExecDynamic` paired with D-LK6-14 (chained-fixups). 128/128 ctest. |
| ~~D-LK6-8.2~~ | ✅ **CLOSED 2026-06-01 (post-fold #2).** Driver-tier `crossValidateTargetFormat(target, format, reporter) -> bool` free function in `src/program/cross_validate_target_format.{hpp,cpp}`. Closed-enum table `kTargetArchMachineCodes` maps target arch name → (ELF machine, PE machine, Mach-O cputype): x86_64 → (62, 0x8664, 0x01000007); arm64 → (183, 0xAA64, 0x0100000C). Called from `compileOneTarget` after both schemas load successfully + before `compileSingleUnit`. WASM/SPIR-V format kinds + unknown target names defer (return true); ABI-model ↔ format-kind compatibility enforced via `abiModelMatchesFormatKind()` BEFORE the per-arch machine check (silent-failure CRITICAL-1 from post-fold #1 — register-machine target paired with WASM format previously slipped through). **Post-fold #2 split** `D_TargetFormatMismatch` (0xD00C, retained as deprecated alias) into two remediation-distinct codes: `D_TargetMachineCodeMismatch` (0xD00D) for ELF/PE/MachO machine-field mismatches, `D_TargetAbiModelMismatch` (0xD00E) for abi-model ↔ format-kind mismatches. **Post-fold #2 name validation**: `target_schema_json.cpp` + `object_format_schema_json.cpp` now reject empty + whitespace-only + leading/trailing-whitespace names (silent-failure HIGH-1 from post-fold #1 — `" "` bypassed empty-check + `lookupTargetArch` returned nullptr → silent-defer). **Post-fold #2 wired pin**: `test_compile_pipeline.cpp::CrossValidateRejectsMachineMismatch` asserts that the FULL `compileFiles` pipeline rejects `(x86_64, elf64-aarch64-linux)` non-zero — pins that the program.cpp call site is invoked (pr-test-analyzer Gap 5 P9). `consteval targetArchNamesAreUnique()` static_assert prevents duplicate-row silent-dead-code (silent-failure HIGH-1 from post-fold #1). 24 tests in `tests/program/test_cross_validate_target_format.cpp` + 1 integration test. Test-exposed `targetArchMachineCodesTable()` for direct unit coverage. 130/130 ctest. Sub-anchors: D-LK6-8.1 (TU split for ≥3 ELF machines), D-LK6-8.3 (`TargetArchKind` closed enum on `TargetSchema` — first 3rd target arch), D-LK6-8.4 (`std::optional<>` row reshape for absent format-kinds — first arch with missing format). (architect Q2 + Q1 deferral expansion: free function in `src/program/`, not in `ObjectFormatData::validate()` since the target schema's arch identity isn't reachable from format-load context.) |
| D-LK6-8.3 | **`TargetArchKind` closed enum on `TargetSchema` (string-key → enum-key for cross-validation table)** — `kTargetArchMachineCodes` keys on `target.name()` as `string_view` today. A future target JSON shipping with a misspelled/variant name (e.g. `"aarch64"` instead of `"arm64"`) silently skips the cross-check (`lookupTargetArch` returns nullptr → defer), reopening the SIGILL surface D-LK6-8.2 closed. Long-term fix: `TargetSchema` declares its arch via a closed enum (parallel to `ObjectFormatKind` + the v1 closed enums); `lookupTargetArch` keys on the enum; misspelled JSON name = load-time reject. (architect Q1 post-fold #1.) | **First 3rd target arch shipped** (RISC-V / PPC64 / MIPS / etc.) OR plan 06 artifact-profile loader gaining a typed arch identity. | First 3rd-target ship OR misspelled-name regression report. |
| D-LK6-8.4 | **`TargetArchMachineCodes` row reshape: `std::optional<u16/u16/u32>` for absent format-kinds** — current row carries 3 u32 fields (ELF + PE + Mach-O machine). PPC64 has no Mach-O on Apple Silicon; RISC-V has no real PE machine code assigned. Today the row uses a "zero means uninitialized" convention, overloaded with "row author forgot to populate" + the u32 widening lies about the actual format-side `u16`/`u32` types. `std::optional<u16> elfMachine` + `std::optional<u16> peMachine` + `std::optional<u32> machoCpuType` makes "no such format kind for this arch" representable as a value. Mismatch then becomes "format has this kind but arch row says absent → emit `D_TargetFormatMismatch` citing arch incompatibility" (a distinct silent-failure class). (type-design post-fold #1.) | **First target arch shipped that lacks a format kind** (e.g. RISC-V without PE). | First arch row with `!has_value()` need. |
| D-LK6-8.1 | **Per-machine TU split for `encodeElfExecDynamic`** — D-LK6-8 added per-machine helpers (`pltStubSizeFor` / `globDatTypeFor` / `emitPltStub` + `emitX86_64PltStub` + `emitArm64PltStub`) inside `elf.cpp`. At 2 machines, single-TU cohesion still wins. When a 3rd ELF machine lands (RISC-V EM_RISCV=243, PPC64 EM_PPC64=21, MIPS EM_MIPS=8), extract into `src/link/format/elf_x86_64_exec_dynamic.cpp` + `src/link/format/elf_aarch64_exec_dynamic.cpp` + `src/link/format/elf_<machine>_exec_dynamic.cpp` each `#include`-ing a thin shared `elf_exec_common.hpp` for the machine-agnostic Ehdr/Phdr/section-header layout. Dispatch from `elf::encode` becomes a switch on `fmt.elf().machine` to per-machine entry points. Same treatment slated for `macho.cpp::encodeExecDynamic` when D-LK6-14 (Apple Silicon chained-fixups) lands. (architect anchor expansion from D-LK6-8 post-fold #1, 2026-06-01.) | **First 3rd-machine ELF dynamic walker** OR explicit code-simplifier finding that elf.cpp's per-machine helpers have grown unwieldy. | Third machine code added to `pltStubSizeFor` switch. |
| D-LK6-9 | **DT_GNU_HASH support** — LK6 cycle 2b.2 emits legacy DT_HASH (single-bucket, O(N) lookup). Modern Linux loaders prefer DT_GNU_HASH (bloom filter + bucket array + hash chain — O(1) amortized) and the plan §2.3 design committed to it. DT_HASH stays as a permanent inclusion requirement for old loaders; DT_GNU_HASH is the post-2-bucket optimization. | **Performance pass cycle** — when v1 corpus startup time becomes a measurable bottleneck OR when the first hardened-build cycle lands (D-LK6-10 paired). | Measurable startup-time regression OR hardened-build acceptance criterion. |
| D-LK6-10 | **PT_GNU_RELRO + PT_GNU_STACK program headers** — LK6 cycle 2b.2 emits only PT_PHDR + PT_INTERP + 2 PT_LOAD + PT_DYNAMIC. Modern hardened Linux builds depend on PT_GNU_RELRO (mark GOT read-only after dyld resolution — combined with DF_1_NOW already emitted, this is "full RELRO") + PT_GNU_STACK (mark stack non-executable — old kernels default to executable stack without it). The plan §2.3 program-header vocabulary lists both. | **Plan 16 (codesign + publish) hardened-build baseline** — security-hardened builds require RELRO + NX stack. | Hardened-build acceptance criterion or security-scanner integration. |
| D-LK6-11 | **ELF lazy binding (`.rela.plt` + `R_X86_64_JUMP_SLOT` + PLT0 resolver stub)** — LK6 cycle 2b.2 ships v1's documented eager-binding stance (plan §5 risk row: "Lazy-binding deferred to post-v1; v1 uses immediate binding (BIND_NOW)"): `DF_1_NOW` + `R_X86_64_GLOB_DAT` in `.rela.dyn`. Lazy upgrade flips four things — (a) `elf.bindNow` = false in JSON (the eager/lazy choice is data, pinned at the JSON-loader level via the `ElfIdentity.bindNow` field that the cycle 2b.2 post-audit fold added; walker today fails loud `K_FormatLacksImportSupport` on `bindNow = false` until this anchor closes), (b) route extern relocs from `.rela.dyn` (`GLOB_DAT`) to `.rela.plt` (`JUMP_SLOT`), (c) emit the 16-byte PLT0 resolver-trampoline stub at `.plt` offset 0 (pushes `GOT[1]` link-map cookie, jumps to `GOT[2]` runtime resolver — `_dl_runtime_resolve`; layout per SysV AMD64 ABI §5.2.4), (d) GOT slots initialized to "PLT entry + 6" (the push-instruction after the indirect jmp) so first-call falls through to PLT0. Walker stays format-blind; the kernel's absolute `symbolVa{SymbolId→u64}` map already abstracts eager-vs-lazy. **Cross-format symmetry**: PE delay-load equivalent anchored D-LK6-12; Mach-O lazy-bind equivalent anchored D-LK6-13. | **Post-v1 performance pass** — when startup-time amortization of large dynamic-import sets becomes measurable on the v1 corpus, OR when a corpus depends on dlsym-style late binding. | First measurable startup-time regression on a heavy-extern corpus, OR a use-case that needs `RTLD_LAZY` semantics. |
| D-LK6-12 | **PE delay-load imports (`.didat` section + `__delayLoadHelper2` thunk + `IMAGE_DELAYLOAD_DESCRIPTOR`)** — Windows analogue of ELF lazy binding. LK6 cycle 2a ships eager IAT (`.idata` section: import lookup table → import address table → hint/name table, all resolved by the Windows loader at process startup). Delay-loaded imports skip the load-time resolution: the linker emits a parallel `.didat` (delay-load import data) section with `IMAGE_DELAYLOAD_DESCRIPTOR` entries (RVA to module-handle slot, RVA to IAT, RVA to bound-IAT, RVA to unload-IAT) and patches each call site to jump through `__delayLoadHelper2` (compiler-supplied thunk that calls `LoadLibrary` + `GetProcAddress` on first call, caches the result). Plan-side: a `pe.delayLoad` JSON field listing which library paths qualify (mirrors `elf.bindNow` data-knob style). | **Post-v1 performance pass** — same trigger as D-LK6-11. | First Windows corpus that depends on lazy DLL load (plugin loaders, optional-dependency patterns). |
| D-LK6-13 | **Mach-O lazy-bind opcodes (`LC_DYLD_INFO_ONLY.lazy_bind_off` opcode stream)** — macOS analogue of ELF lazy binding for the legacy (pre-chained-fixups) dyld path. LK6 cycle 2c (D-LK6-5) ships `LC_DYLD_INFO_ONLY` with the immediate `bind_off` opcode stream (all symbols resolved at load via `dyld_stub_binder`). Lazy upgrade routes deferred symbols to the `lazy_bind_off` stream instead — same opcode vocabulary (`BIND_OPCODE_SET_DYLIB_ORDINAL_IMM` / `_SET_SYMBOL_TRAILING_FLAGS_IMM` / `_DO_BIND`) but evaluated on first call via `dyld_stub_binder` instead of at load. Modern chained-fixups path (Apple Silicon `LC_DYLD_CHAINED_FIXUPS`) has NO lazy mode by design — chained fixups are always eager — so this anchor is x86_64 macOS only; on Apple Silicon, "lazy" is N/A. Plan-side: `image.bindNow = false` in JSON (the same data-knob the cycle 2c post-audit fold added; walker today fails loud `K_FormatLacksImportSupport` citing this anchor). | **Post-v1 performance pass on x86_64 macOS** — same trigger as D-LK6-11 but bounded to the legacy dyld path. | First x86_64 macOS corpus with a heavy-extern startup-cost regression. |
| D-LK6-14 | **Mach-O chained-fixups walker arm (Apple Silicon — `LC_DYLD_CHAINED_FIXUPS` + `LC_DYLD_EXPORTS_TRIE`)** — LK6 cycle 2c ships LC_DYLD_INFO_ONLY for x86_64-darwin. Apple Silicon macOS 12+ binaries use the structurally distinct chained-fixups mechanism: fixup targets are encoded inline in the data pages themselves (`dyld_chained_fixups_header` in `__LINKEDIT` + chain-start info per data page + per-fixup `dyld_chained_ptr_64_bind` / `_rebase` records). No opcode-stream interpreter; chain pages are page-inline and chained-fixups are always eager (no lazy mode — see D-LK6-13 N/A note). Walker dispatches on `MachOIdentity.cputype` (`CPU_TYPE_X86_64` → LC_DYLD_INFO_ONLY arm; `CPU_TYPE_ARM64` → LC_DYLD_CHAINED_FIXUPS arm). Plan-side: ship `macho64-arm64-darwin-exec.format.json` declaring the ARM64 cputype + section flags. Paired with D-LK6-8 (ARM64 PLT stub encoding — same target architecture). | **First ARM64-darwin target lands** — paired with D-LK6-8 (ARM64 PLT) + D-LK6-15 (closed-enum reshape paired with this anchor). | First Apple Silicon `.exe` that calls a libSystem symbol. |
| D-LK6-14-ARM64E | **DYLD_CHAINED_PTR_ARM64E (pointer-authentication arm)** — Apple Silicon's arm64e architecture variant uses `pointer_format = 1` (`DYLD_CHAINED_PTR_ARM64E`) with a different bitfield layout than format=6 (DYLD_CHAINED_PTR_64): the bind variant carries a PAC discriminator instead of `addend`, and `next` uses 8-byte units rather than 4-byte. v1 supports format=6 only. The writer's gotBody bitfield loop in `macho.cpp` assumes format=6 layout (24-bit ordinal + 12-bit next + 1-bit bind at canonical positions); a pointer_format swap MUST move together with the bitfield assembly (paired D-LK6-14-CHAINEDPTR-FORMAT-COUPLING). Cited at production-source-code level in `macho_chained_fixups.hpp` (constant docblock) + `object_format_schema.hpp:583` (`useChainedFixups` docblock). Closing work: introduce `enum class ChainedPtrFormat { Sixty4 = 6, Arm64e = 1 }` discriminant + per-variant `packChainedPtr64Bind()` / `packChainedPtrArm64eBind()` free functions in `macho_chained_fixups.hpp`; introduce `kDyldChainedPtrArm64eNextStride = 1u` (8-byte stride / 8 = 1 in 8-byte units? — actually arm64e stride is 8-byte units so adjacent slots = 1) sibling constant; dispatch in `encodeExecDynamic` on the schema-selected pointer_format. Pairs with the D-LK6-15 closed-enum reshape. | **First arm64e target schema ships** (no shipped fixture today; would need `cputype=CPU_TYPE_ARM64 \| CPU_ARCH_ABI64` + `cpusubtype=CPU_SUBTYPE_ARM64E`). | First Apple Silicon `.exe` that targets arm64e (vs. the more common plain arm64). |
| D-LK6-14-REBASE | **Rebase entries via DYLD_CHAINED_PTR_64 `_rebase` variant** — chained-fixups can express BOTH binds (extern imports) AND rebases (position-independent intra-image absolute pointers — e.g. function-table entries, vtables, jump tables in `__DATA` or `__DATA_CONST`). The `bind` bit (bit 63) selects: `1` = bind (consumes import ordinal from bits[0..23]); `0` = rebase (consumes a 36-bit target VA from bits[0..35] + a 16-bit high-8-bits in bits[36..50]). v1 emits only binds (the `__got` slots in `encodeExecDynamic`). When an emitter starts producing intra-image absolute pointers in `__DATA` / `__DATA_CONST` (e.g. C-array of function pointers, switch jump table relocated to data), each such pointer becomes a rebase chain entry. Cited at production-source-code level in `object_format_schema.hpp:576` (`useChainedFixups` docblock). Closing work: extend `ChainedFixupImport` to a tagged variant `ChainedFixupEntry { Bind{import_index} \| Rebase{target_va} }` OR introduce a parallel `vector<ChainedFixupRebase>` arg to `buildChainedFixupsPayload`; teach the gotBody loop to walk a mixed sequence; ensure `dyld_chained_starts_in_segment.page_starts[i]` covers all chained pointers in a page (both binds and rebases). Pairs with D-LK6-14-CHAINEDPTR-BUILDER (typed packer). | **First emitter that produces intra-image absolute pointers in `__DATA` / `__DATA_CONST`** (today the only chained pointers are extern-import binds in `__got`). | First C-subset feature that lowers to function pointers stored in initialized data — e.g. switch-table-via-data, vtable for first-class procedures, C-string array of function pointers. |
| D-LK6-14-MACHO-WEAK-DEF | **Weak-import flag (bit 8 of `DYLD_CHAINED_IMPORT`)** — Apple's `__attribute__((weak_import))` on extern function declarations sets `weak_import = 1` in the corresponding `DYLD_CHAINED_IMPORT` row; dyld then tolerates missing symbols at load by leaving the `__got` slot zero. v1 hardcodes `weakImport = false` for every extern (cited at `src/link/format/macho.cpp:1280` + `:1288` + pinned at `tests/link/test_macho_writer.cpp:1590`). Closing work: surface the weak flag in `ExternImport` (today a 3-field POD: symbol/mangledName/libraryPath) — add `bool weakImport = false`; populate from the frontend `__attribute__((weak_import))` recognition in the C-subset compiler (paired with the FFI tier's plan 11). The walker already takes `weakImport` per-row via `ChainedFixupImport.weakImport` — only the production-call-site needs to start passing `true` when the source attribute is present. | **First C-subset corpus declaring an extern function with `__attribute__((weak_import))`** OR first FFI catalog entry tagging a libSystem symbol as optional. | First availability-conditional binary (e.g. uses a libSystem function only present on macOS 12+). |
| D-LK6-14-CHAINEDPTR-FORMAT-COUPLING | **pointer_format ↔ bitfield-layout coupling enforcement** — the constant `kDyldChainedPtrFormat64 = 6u` (emitted at `dyld_chained_starts_in_segment.pointer_format`) and the bitfield layout assembled by `encodeExecDynamic`'s gotBody loop (`ordinal:24 + addend:8 + reserved:19 + next:12 + bind:1` at canonical positions) MUST move together. A pointer_format swap (e.g. to `1 = DYLD_CHAINED_PTR_ARM64E`) without the corresponding bitfield change produces binaries dyld silently mis-walks. Today: anchor-cross-ref + Apple-spec docblock on `kDyldChainedPtrFormat64` (`macho_chained_fixups.hpp:65-71`); reciprocal cite at `object_format_schema.hpp:583` next to `useChainedFixups`. Closing work co-lands with D-LK6-14-ARM64E (when the second pointer_format value arrives): introduce `enum class ChainedPtrFormat` + per-variant `packBind()` typed free function; the discriminant moves the layout with it via dispatch. | **Paired with D-LK6-14-ARM64E** — same cycle. | Same as D-LK6-14-ARM64E. |
| D-LK6-14-32 | **32-bit Mach-O chained-fixups support (paired with FormatGuess::MachO32).** v1 chained-fixups emission is 64-bit-only (`DYLD_CHAINED_PTR_64` format=6, 8-byte slots, ordinal:24 + addend:8 + reserved:19 + next:12 + bind:1 layout). 32-bit Mach-O (i386 + armv7) uses a different chained-fixups variant — `DYLD_CHAINED_PTR_32` format=10, 4-byte slots, ordinal:20 + addend:6 + bind:1 + next:5 layout. Today's writer rejects 32-bit Mach-O via `FormatGuess::MachO32` enum (closed-set discriminator) before reaching the chained-fixups path. Cited at production-source-code level in `object_format_schema.hpp:525-527`. Closing work co-lands with the broader 32-bit Mach-O substrate: per-variant `packChainedPtr32Bind()` in `macho_chained_fixups.hpp`; introduce `ChainedPtrFormat::ThirtyTwo = 10` discriminator alongside the existing 64/Arm64e (D-LK6-14-ARM64E closure). Pairs with D-LK6-14-CHAINEDPTR-FORMAT-COUPLING for the layout-coupling enforcement. **Audit-promoted 2026-06-02** (anchor was cited in production source but missing its plan row; sibling D-LK6-14-REBASE / D-LK6-14-ARM64E rows above already promoted). | **First 32-bit Mach-O target ships** — i386 macOS (legacy, pre-Catalina) or armv7 iOS (legacy, pre-iOS 11). | First i386 / armv7 Mach-O `.exe` requested by a corpus. |
| D-LK6-14-MULTI-PAGE-GOT | **Multi-page `__DATA_CONST` chained-fixups support** — `2ba0489` (D-LK6-14-INTEGRATION-GOT-SLOTS) ships the single-page chain only: `dyld_chained_starts_in_segment.page_count = 1`, `page_starts[0] = 0`, one 4-KiB page of `__got`. Wider modules (> 512 externs at 8-byte slots on a 4-KiB page; > 2048 on a 16-KiB page) need per-page `page_starts[i]` so dyld can walk chains in each page independently. A 16-KiB-page target (arm64 macOS native page size) also surfaces this anchor regardless of extern count — the chain still fits but `page_size` must change from `0x1000` to `0x4000` and `segInfo.pageSize` must thread through. v1 fails loud `K_NoMatchingObjectFormat` citing this anchor when `gotFileSize > kPageSize`. Cited at production-source-code level in the diagnostic message; child of D-LK6-14. Closing work: lift `page_count = 1` hardcode → `ceil(gotFileSize / pageSize)`; populate `page_starts[i]` with offset-into-page of first chain entry per page (or `kDyldChainedPtrStartNone` for pages with no chain start); thread `pageSize` through `ChainedSegInfo` from target-schema. The `kDyldChainedPtrStartNone = 0xFFFFu` constant is already present in `macho_chained_fixups.hpp` reserved for this anchor. | **First Mach-O module with > 512 extern imports** OR **first arm64 Apple Silicon target** (16-KiB native page size flips `page_size` field even at low extern counts). | First Apple Silicon `.exe` with extern imports OR first big-libSystem module on x86_64-darwin. |
| D-LK6-15 | **Reshape `bindNow: bool` → `bindMode: DynamicBindMode {Eager, Lazy}` (closed-enum migration on both `ElfIdentity` and `MachOImage`)** — type-design analyst (LK6 cycle 2c review) recommended converting the bool to a closed enum to align with the codebase's invariant-expression pattern (3/5 → 5/5 score). LK6 cycle 2c kept the bool for two reasons: (a) D-LK6-14 chained-fixups is the natural third semantic-axis trigger (chained fixups is a binding *mechanism*, orthogonal to eager-vs-lazy; the right enum migration co-lands with D-LK6-14 to surface BOTH axes), (b) reshaping touches the freshly-landed ELF cycle 2b.2 surface + shipped JSON keys + tests. Paired with D-LK6-14 so the enum migration arrives once for both formats simultaneously. validate() symmetry rules (`bindMode==Lazy` on ET_REL / MH_OBJECT must reject at schema-load) ride this anchor. | **Paired with D-LK6-14** — same cycle. | Same as D-LK6-14. |
| D-LK7-1 | **Mach-O static-path `__LINKEDIT` synthesis for codesign-capable executables** — LK7 closes the codesign-placeholder substrate but the static `encodeExec` path (no `externImports`) emits NO `__LINKEDIT` segment. The Apple kernel's `cs_validate_range` requires the CD blob to fall inside a loaded segment; an `LC_CODE_SIGNATURE` pointing at file-tail bytes outside any `LC_SEGMENT_64` would be rejected at `execve()`. Today the walker dispatches `codeSignatureSize > 0 + externImports.empty()` to a fail-loud `K_FormatLacksImportSupport` citing this anchor. **Closing path (architect-anchored: this IS a real Apple use-case, not degenerate — notarized self-contained CLI tools / XPC services exist in production)**: synthesize a minimal `__LINKEDIT` segment in the static path with `vmaddr = textSegVmaddr + textSegVmsize` (next page after `__TEXT`), `vmsize = pageAlign(symtab + strtab + codesignReservation)`, `fileoff = symtabOffset`, `filesize = (codeSigFileOff + codeSignatureSize) - symtabOffset`. Bump `ncmds += 2` (LC_SEGMENT_64 __LINKEDIT + LC_CODE_SIGNATURE). `__TEXT.filesize` stays byte-exact. No cross-format impact, no substrate changes needed — the dynamic path's layout is the template. | **First signed Mach-O without extern imports** — e.g. a notarized self-contained CLI tool that avoids `libSystem` via `-static`. | First user-facing test fixture or corpus that requires it. |
| D-LK7-2 | **ELF IMA (Integrity Measurement Architecture) — xattr-stored signatures** — IMA stores HMAC/RSA signatures in `security.ima` xattrs via `evmctl ima_sign`. No ELF header change, no walker reservation — the linker emits a stock ELF, the IMA tooling computes the hash and writes the xattr post-link. Plan-16 extension reads the linker output + runs `evmctl ima_sign` with the project's signing key. | **v1.x secure-boot / IMA-friendly Linux corpus**. | First Linux corpus that needs IMA-signed binaries (e.g. embedded / IoT targets). |
| D-LK7-3 | **ELF detached `.sig` files (GPG / openssl post-link sidecar)** — production GPG-signed Linux binaries ship a `.sig` file next to the artifact (e.g. `myapp` + `myapp.sig`) generated by `gpg --detach-sign` or `openssl dgst -sign`. No ELF header change, no walker reservation. Plan-16 extension wraps the post-link signer + manages the keyring. | **v1.x Linux distribution signing** — paired with the dm-verity arm below (sometimes both paths run). | First Linux corpus that ships via signed-by-detached-file distribution. |
| D-LK7-4 | **ELF dm-verity / fs-verity (block-level Merkle tree)** — Android / ChromeOS / hardened Linux use dm-verity (block-level read-only Merkle tree appended to the partition image, NOT to the ELF). The verity superblock + Merkle leaves live in a partition trailer, not the ELF. No ELF header change, no walker reservation. Plan-16 extension builds the verity tree at the partition image level. | **v1.x Android / hardened-Linux distribution**. | First Android-style ROM or hardened-Linux image build. |
| D-LK7-5 | **ELF kernel-module-style `.module_signature` appended trailer** — Linux kernel modules (`.ko`) use the `scripts/sign-file` tool from the kernel source tree to append a `module_signature` struct + PKCS#7 to the end of the module file. The ELF itself is unchanged; the bytes are appended after the section header table's end. v1 ships userspace ELFs, not kernel modules, so this is a v1.x feature (LKMs are distinct binary type from `.so` / executable). Plan-16 extension would call `sign-file` against the linker output. | **First Linux kernel module the project compiles** — `.ko` artifact path, NOT a regular ELF userspace binary. | First LKM corpus or kernel-extension target. |
| ~~D-LK9-2~~ | ✅ **CLOSED 2026-05-30 at LK10 cycle 2.** Moved `dss::link()` → `dss::linker::link()` across 30 call sites in 9 files. `dss::link` namespace freed for the format-walker substrate (`dss::link::format::*` sub-namespaces remain — they describe per-format helpers, not the linker entry point). (Historical: Move `dss::link()` to `dss::linker::link()` + free the `dss::link` namespace — architect + type-design analyst (LK9 post-fold review) flagged the namespace-vs-function collision: `linker.hpp` declares the free function `dss::link(...)`, which forbids using `dss::link` as a namespace. The LK9 post-fold compromise added `dss::link_format::test` for test helpers (avoiding the clash) but type-design notes this inverts the logical hierarchy — `link_format` is a subset of `link`. The clean fix: rename the free function to `dss::linker::link()` (matching the `src/link/` directory boundary and parallel to `dss::elf::encode` / `dss::pe::encode` / `dss::macho::encode` / `dss::wasm::encode` / `dss::spirv::encode` namespace pattern), then `dss::link` is free for the format/walker substrate. Scope: 1 `linker.hpp` signature change + 1 `linker.cpp` definition + ~20+ test call sites + 1 `program.cpp` call site (when LK10 wires it). Deferred because the LK10 driver-emission cycle will already touch many of those call sites; co-landing the rename with LK10's pipeline wiring keeps churn in one commit and aligns the `dss::linker::link()` final name with the public CLI surface. | **LK10 (driver file-emission) — same cycle that wires `program.cpp::compileProject()` through to `link()`.** | LK10 cycle scope. |
| D-LK9-1 | **Skeleton-walker guard helper hoist** — LK8 (WASM) + LK9 (SPIR-V) skeletons share the same 4-guard input-contract pattern: (a) non-matching format kind, (b) non-empty `module.functions`, (c) non-empty `module.externImports`, (d) non-zero `expectedFuncCount`. Type-design analyst flagged the cross-format symmetry at the LK9 post-fold review (2026-05-30). The 2-consumer state is below the 3rd-consumer hoist trigger (D-LK4-9 precedent: `applyExecRelocations` hoisted at 3rd consumer). Format-specific diagnostic prose IS load-bearing per guard (each cites its owning plan + format-native vocabulary), so a naive `validateSkeletonInput()` would erase the value. The cleaner shape is a small `link/format/skeleton_guards.hpp` with each guard as a separate fail-loud helper that takes a `planAnchor` + `formatName` + format-specific message string — the helper centralizes the `if (cond) { emit(...); return {}; }` shape while letting each call site pass its anchor text. **Trigger**: when a 3rd skeleton walker would land. Note: skeletons are transient (plan 17/18 replace them with full lowerers), so the 3rd may never materialize — anchor stands as a soft pin against drift. | **First 3rd skeleton-style linker walker** (unlikely — plan 17/18 replace LK8/LK9 with full lowerers). | First sign of a 3rd skeleton walker landing, OR explicit code-simplifier finding that the 2 consumers diverge enough to warrant abstraction. |
| D-LK7-7 | **`PeOptionalHeader` struct split into PE/COFF spec sub-structures** — type-design analyst (LK7 post-fold review) flagged `PeOptionalHeader` at 14 fields (after `attributeCertReserveSize` added at LK7), past the cohesion threshold. Microsoft's PE/COFF §3.4 already provides natural seams: `PeStandardFields` (magic, linker versions, code/data sizes, entry RVA), `PeWindowsFields` (imageBase, alignments, OS/subsys versions, sizeOf*), `PeDataDirectories` (attributeCertReserveSize + future export/import/debug dir reserves). Mirrors the existing `ElfIdentity` / `PeIdentity` / `MachOImage` sub-block discipline. JSON keys stay flat (`optionalHeader.magic` etc.) for backward compatibility — the split is C++-side only. No semantic change. | **When PE adds a second data-directory reservation** — e.g. D-LK7-? for an embedded debug-info directory placeholder, or a `.didat` delay-load reservation (D-LK6-12). That's the natural moment the third axis materializes and the cohesion gain justifies the churn. | First PE extension that adds a new `optionalHeader.*` field beyond LK7's `attributeCertReserveSize`. |
| D-LK7-6 | **Cross-walker u32 file-offset audit** — LK7 added u32-overflow guards on `codeSigFileOff` (Mach-O) + `certTableFileOff` (PE). The guards are asymmetric: every OTHER u32 file-offset cast in the walkers is equally exposed at >4 GiB. Specifically: Mach-O `symtabOff` / `strtabOff` / `indirectSymtabOff` / `bindOff` / `textFileOff` / `stubsFileOff` / `gotFileOff`; PE `pointerToRawData` / IAT and ILT RVAs in `IMAGE_DATA_DIRECTORY[12]`; ELF `e_shoff` / `sh_offset[]`. All u32 per spec. A >4 GiB output binary (large WASM-style modules, or future debug-info-laden outputs) would silently truncate. Audit pass: hoist a shared `requireFitsInU32(value, anchor, reporter)` helper to `byte_emit.hpp` and apply at every file-offset emit site. | **First output binary >4 GiB observed in practice** — likely from a debug-info-laden Mach-O or a WASM `.wasm` after LK8. | First overflow report or first >4 GiB target binary. |
| ~~D-LK10-1~~ | ✅ **CLOSED 2026-06-01 at LK10 cycle 3.** `InputResolver` class hoisted to `src/program/input_resolver.{hpp,cpp}` per plan 00 §4.1.3 spec. Closed enum `InputResolver::Mode {Recursive=0, Flat=1}` is the second policy axis (the trigger anchored in this row); CLI exposes via `--recursive` / `--no-recursive`. Static methods: `resolveDirectory(path, exts, mode, out, reporter) -> bool` (sort + dedup + ec-handling for both `recursive_directory_iterator` and flat `directory_iterator`); `validateFiles(inputs, out, reporter) -> bool` (ENOENT splits D_FileNotFound; filesystem-error splits D_DirectoryScanFailed). `Program::compileDirectory` 3-arg overload delegates to policy-aware 5-arg overload. 8 new ctest cases in `tests/program/test_input_resolver.cpp`. — (closed) |
| ~~D-LK10-2~~ | ✅ **CLOSED 2026-06-01** at the AS encoder-variant + ML7 ret-arm fold cycle. Both halves landed: (a) plan 12 ML7 cycle 2 (`arg` + `call` + `ret` virtual-op materialization through the calling-convention; landed 2026-05-30); (b) plan 13 AS encoder variants (`add`/`sub reg+imm32` for prologue/epilogue SP adjustment; `load reg, [base+disp32]` + `store [base+disp32], reg` with forced-SIB for rsp/r12 base family; D-AS4-1 partial close for the no-index `[base+disp32]` form). The LK10 cycle 2 e2e test `Program_CompileFiles.ZeroArgFunctionWiresThroughPipeline` was tightened from a soft `if (rc == 0) { check ELF magic }` branch to unconditional `ASSERT_EQ(rc, 0)` + ELF magic + file-size assertions. Byte-pin tests added in `tests/asm/test_asm_x86_variable.cpp` for the new variants (sub/add rsp imm32 prologue/epilogue bytes; load/store with rsp-forced-SIB, with rbp-no-SIB, with r12-forced-SIB-plus-REX.B, and with negative displacement two's-complement). 124/124 ctest with the strict-assertion testing posture preserved. — (closed) |
| D-LK10-3 | **Migrate `TargetSpec::outputExtension` → `ObjectFormatSchema::defaultExtension()`** — LK10 cycle 2's `TargetSpec` carries a closed-switch extension table (10 `(kind × objectType)` arms) as a v1 driver-layer convention. Plan 06 (artifact profile) eventually owns the authoritative extension/output-dir policy via per-language `artifactProfiles[]` declarations. The right target home is `ObjectFormatSchema::defaultExtension()` (schema-side, format-owns-its-own-extension) — the driver becomes a thin caller, plan 06 overrides via `artifactProfile.outputExtension` when declared. The migration is mechanical: 11 lines moved from `target_spec.cpp` to `object_format_schema.cpp` + 1 call site updated in `program.cpp`. `TargetSpec::outputExtension` becomes a thin pass-through OR is removed. | **Plan 06 AP2 (codegen reads artifact profile)** lands. | First commit that wires `artifactProfile.outputExtension` into the driver — at that point the duplicate must collapse. |
| D-LK10-4 | **`SemanticModel` borrows CompilationUnit via `const&` (drop the shared_ptr null-deleter wrap)** — `compile_pipeline.cpp::compileSingleUnit` wraps the borrowed `CompilationUnit const&` parameter in a `shared_ptr` with a null deleter to satisfy `analyze(std::shared_ptr<CompilationUnit const>)`'s signature. The shared_ptr expresses ownership when the call semantics are pure borrow + read. Type-design analyst flagged this at the LK10 cycle 2 post-fold review (2026-05-30). The fix isn't in `compile_pipeline.cpp` — it's `analyze()` taking `CompilationUnit const&` (or a non-owning view type). Cross-cutting: `analyze` has many test/fixture call sites across `tests/analysis/semantic/`. Cycle 2 leaves the null-deleter as documented workaround; the fix waits for a dedicated semantic-tier cycle. | **A semantic-tier cycle that already touches `analyze`'s call boundary** — e.g. plan 09 HR12 or a multi-CU enhancement. | First commit that changes `analyze`'s parameter type for any other reason (avoid drive-by churn). |
| D-LK10-5 | **Bundle `compileSingleUnit` parameters into `CompilePipelineOptions` struct** — cycle 2's `compileSingleUnit(cu, grammar, target, format, outPath, reporter)` is 6 params. Plan 06 will add an `ArtifactProfile` (optimization level, subsystem, signing policy); plan 12 ML7 cycle 2 may add a `CallingConventionOverride`; LK11 will need an explicit `artifactName` (the cycle 2 "front-file stem" convention isn't enough for multi-CU runs). At 8+ params the signature is maintainability-negative. The natural bundle: `CompilePipelineOptions { outPath, reporter, artifactProfile = nullptr, callingConventionPolicy = nullptr, artifactName = "" }`. Stable against future option fields without signature churn. Architect anchored at the LK10 cycle 2 post-fold review (2026-05-30). | **First of plan 06 AP2 / plan 12 ML7 cycle 2 / LK11 touching `compileSingleUnit`.** | First commit that adds a 7th parameter (the threshold where the struct's cost pays off). |
| ~~D-LK10-7~~ | ✅ **CLOSED 2026-06-01 at LK10 cycle 3.** `--warnings-as-errors` + `--suppress=<code>` wired through `DiagnosticReporter::Config` → policy-aware `Program::compileFiles` / `Program::compileDirectory` overloads → run-wide reporter. `copyDiagnostics` policy-aware semantics are now the INTENDED design: `dst.report(d)` re-traverses `dst`'s policy on every drained record, so the user's `--suppress` applies uniformly across both halves of the pipeline (front: parse/HIR/semantic CU reporter; back: SemanticModel reporter post-`analyze`). The `copyDiagnosticsRaw` alternative becomes unnecessary — the routing IS the design. `compile_pipeline.hpp::copyDiagnostics` docstring updated to reflect the closed semantics. `--suppress=Unknown` sentinel reject hardened against the linear-scan match-first-hole class (silent-failure F1 post-fold). **NOTE (post-fold #2 architect HIGH)**: the "IS the design" claim holds for ONE global policy. When plan 22 introduces per-tier policies (e.g. suppress optimizer warnings independently of parser warnings) the drain WILL silently over-apply the run-wide policy to tier-local diagnostics — at that point `copyDiagnosticsRaw` (or per-tier policy threading) becomes necessary. Anchored as a plan-22 pre-condition; revisit before per-tier policy ships. — (closed for v1) |
| D-LK10-6 | **Hoist `ScratchDir` test helper to `tests/test_support/scratch_dir.hpp`** — LK10 cycle 1 (`tests/link/test_link_writer.cpp`) + LK10 cycle 2 (`tests/program/test_compile_pipeline.cpp`) both ship a `ScratchDir` class with PID-seeded atomic-counter naming. The two bodies differ MEANINGFULLY: link-writer's scratch lives at `temp_directory_path()` (cwd-walk doesn't matter for the writer-only tests), program's lives inside the repo tree at `<originalCwd>/test-scratch/program/...` (the cwd-walk MUST reach `<repo>/src/dss-config`). Hoisting needs a policy axis (`Location::Temp` vs `Location::InsideRepo`) — currently 2 consumers, below the 3rd-consumer hoist threshold. | **First 3rd test that needs scratch-dir setup.** | When a new test in `tests/asm/` or `tests/lir/` (or another future driver-adjacent test) needs the same RAII scratch-dir shape. |
| ~~D-LK10-8~~ | ✅ **CLOSED 2026-06-01 at LK10 cycle 3 post-fold #2.** `report(reporter, code, severity, msg)` shim moved to `core/types/diagnostic_reporter.hpp` as `dss::report` (inline free function, in `namespace dss`). All ~20 consumers across LIR / AS / link / driver tiers updated: 15 `using lir_pass_util::report;` → `using dss::report;`; 4 qualified `lir_pass_util::report(...)` → `dss::report(...)`; 2 namespaced `using dss::lir_pass_util::report;` → `using dss::report;`. Driver-tier `src/program/input_resolver.cpp` + `src/program/program.cpp` dropped their `#include "lir/lir_pass_util.hpp"` (layer inversion eliminated). `src/lir/lir_regalloc.cpp` removed its duplicate-shim anon-namespace `report()` — calls now resolve to `dss::report` via ADL. `lir_pass_util.{hpp,cpp}` keeps `remapBlockRef` + `emitTerminator` (LIR-specific) and notes the hoist with a comment. 126/126 ctest. — (closed) |
| D-LK6-17 | **`TargetRelocationInfo` flat struct → `std::variant<LinearReloc, Aarch64Call26, Aarch64AdrPrelPgHi21, Aarch64AddAbsLo12, ...>` reshape** — D-LK6-1 (LK10 cycle 3 sibling, 2026-06-01) closed the formula-text-vs-structured-triple desync via a `formulaKind` enum + Linear-only structured triple (rule (e) on the struct). Wide-product cost today is small (3 fields ignored on 3 ARM64 variants) and rule (e) is loader-enforced — desync window closed at JSON-load time. The variant reshape would type-encode rule (e) (illegal states unrepresentable) but the three ARM64 variants are STATELESS tag types (bit layout lives in code, not on the row); `LinearReloc` would carry the 3-field sub-aggregate, the others would be empty structs. Below the cost threshold today. **Trigger**: first RISC-V relocation row (R_RISCV_HI20/LO12_I = a shifted-pair adding new sub-state); ditto MIPS/PowerPC. At a 5th-formula-kind landing, the variant reshape's invariant-encoding value justifies the per-consumer churn. | **First non-ARM64 non-x86 ISA reloc kind** (likely RISC-V). | First new formula class that adds STATE beyond the discriminator. |
| D-LK6-18 | **`ADRP + ADD_ABS_LO12_NC` paired-relocation cross-consistency check** — `Aarch64AddAbsLo12` formula is `(S+A) & 0xFFF` — modular low-12. The kernel rejects `SA < 0` (silent-truncation guard, D-LK6-1 post-fold #1) but DOES NOT verify that the ADRP companion reloc on the same target resolves to a matching page. A misauthored input with mismatched (ADRP, ADD) addends could produce a load from the wrong page with no diagnostic — load lands at `(ADRP_page + ADD_lo12)` where the ADD_lo12 = `S+A_add & 0xFFF` while the ADRP_page came from `(S+A_adrp) >> 12`. If `A_adrp != A_add` and the symbol VA falls near a page boundary, the wrong page gets selected. The right home is an assembler-side validate-pass that walks each `AssembledFunction`'s relocation list and pairs ADRP-class kinds with ADD-low-12 kinds on the same target. (silent-failure audit H2 post-fold #1 — anchored.) | **Plan 13 AS-cycle that lands the first ARM64 c-subset corpus generating real ADRP+ADD pairs** OR **first assembler-emitted symbol reference at page-boundary depth**. | First c-subset test that emits an ADRP+ADD pair on a runtime-resolved symbol. |
| D-LK10-9 | **`CliArgs` flat struct → `std::variant` reshape** — `CliArgs` (cli_args.hpp) carries mode-flag mutual exclusivity as a runtime check (`setMode` + post-parse `if (mode == X)` ladder + `hasOptions` heuristic for `NoModeSelected`). Type-design analyst (LK10 cycle 3 post-fold #2) noted the flat shape is a "wide-product where a sum-of-products is the truth": fields like `transpileFiles`+`sourceFiles`+`lspSchemaDir` co-exist in one struct but are illegal in most modes. The variant shape `std::variant<Help, Lsp{schemaDir}, Compile{files, ...}, Transpile{...}, Directory{path, ..., mode}, Project{path}>` makes illegal states unrepresentable: `--schema-dir` outside `--lsp` becomes a parse-time impossibility rather than a post-parse runtime reject (collapsing the H1 fold from "post-parse validation" to "type-encoded"). Shared options factor as a `CompileCommon{lang, targets, cfg, policy}` sub-aggregate. **Cost**: every test + every dispatcher in `Program::run` needs the variant switch. **Trigger**: next CLI feature that grows a mode-specific field OR the plan 06 artifact-profile loader (which will need its own mode-specific options sub-block). | **Plan 06 AP-CLI cycle** (artifact-profile-aware CLI options) OR **a new CLI mode** (--query / --check / etc.) | First commit that adds a mode-specific field type-design analyst can flag as "this only belongs in mode X". |
| D-LK10-10 | **`CompileConfig {Debug, Release}` closed enum → orthogonal-axis struct** — Today `CompileConfig` is a binary `{Debug=0, Release=1}` enum. Plan 22 OPT1+ will need orthogonal axes that CMake's `Debug`/`Release`/`RelWithDebInfo`/`MinSizeRel` quartet smuggles into a one-dimensional enum and then composes poorly: **opt-level** (none / O1 / O2 / O3), **debug-info** (off / line-tables / full), **size-bias** (off / on). Type-design analyst (LK10 cycle 3 post-fold #2): long-term-correct primitive is `struct CompileConfig { OptLevel opt; DebugInfo dbg; SizeBias size; }` with `Debug`/`Release` as named factory functions. CLI surface stays `--config=debug\|release` (factory dispatch); plan 22 per-pass reads see the orthogonal axes. Reshape NOW would be premature — no OPT pass consumes it yet. **Trigger**: plan 22 OPT1 substrate PR — at the moment a pass needs to ask "is debug-info on?" independent of "is opt-level O2?". | **Plan 22 OPT1 substrate PR.** | First OPT pass that reads `CompileConfig` for a decision more granular than "any-opt vs no-opt". |
| D-LK6-16 | **HIR `ExternGlobal` thread-through (mirror of `ExternFunction`) — unified `externs` vector with `ExternImportKind` tag** — LK6 cycle 2d closes the ExternFunction half of D-LK6-6; `HirKind::ExternGlobal` still fires `H_UnsupportedLoweringForKind` at the MIR main-pass. **Architecture decision (architect anchored, LK6 cycle 2d post-fold review)**: extend the existing `ExternImport` row with an `ExternImportKind {Function, Global}` field rather than introducing a parallel `externGlobalImports` vector. Rationale: the linker walkers (PE/ELF/Mach-O) already group by `libraryPath` irrespective of function-vs-global, and `ExternImport` has no function-vs-global discriminator today — adding a kind tag is the minimum-surface change that lets both kinds flow through the same `externImports` vector. A parallel vector would double-thread every tier (MIR result → MIR/LIR result → assembler parameter → AssembledModule storage → linker grouping) for no semantic win. Closing the global half therefore requires: (a) add `ExternImportKind` to `ExternImport`, (b) extend MIR pre-pass `collectExterns` to also walk `HirKind::ExternGlobal` and emit `kind=Global` rows reading the same `HirAttribute<FfiMetadata>` side-table, (c) walker side: each walker selects PE `.idata` IAT slot vs ELF GOT slot vs Mach-O `__got` slot AND emits the correct symbol-table entry (`nlist_64.n_type = N_UNDF\|N_EXT` for globals identically to functions; differences land in section-flag bits, not in the bind/relocation taxonomy). Paired with plan 11 FF5 binary ingestion of extern globals (e.g. `extern int errno` resolving to libc's `errno` location). | **First c-subset corpus that references an extern variable** — typically C's `extern int errno` or POSIX's `extern char **environ`. | First reproducible test case where `extern T name;` (no parens) declaration needs to resolve to a dynamic-library symbol. |
| ~~★ **D-FF6-RUNTIME-PRINT**~~ | ✅ **CLOSED 2026-06-02 — the actual blocker was D-LK10-ENTRY-ML7-FRAME-BIAS-UNIFY, NOT a CRT-init gap.** The pre-fold hypothesis (msvcrt requires CRT init via `_initterm`/`__getmainargs`) was disproved at diagnosis: msvcrt.dll's own `_DllMainCRTStartup` initializes `_iob[1]` (stdout FILE*) at DLL-attach (before any user code runs). The actual 0xC0000005 had TWO layers: (a) user `main`'s prologue didn't allocate Win64 shadow space (32 bytes) + post-CALL alignment-bias (8 bytes) before calling `puts` — `puts`'s internal `movaps` instructions trapped on misaligned RSP; (b) MIR→LIR emitted `call` (E8 disp32 direct) for the extern `puts` when it should have emitted `call_indirect_via_extern` (FF 15 disp32 indirect-via-IAT) — the direct form would execute the IAT slot's bytes as code. Closure fix landed at TWO tiers: (i) ML7 `computeFrameLayout` extended with `hasCalls` pre-scan + `cc.shadowSpaceBytes` + `cc.callPushBytes` (new field) via `alignedSizeWithBias`; (ii) MIR→LIR `lowerCall` gained `externSymbols` set built from caller-supplied `externImports`, picks `MnemonicSlot::CallIndirectViaExtern` for extern callees vs `MnemonicSlot::Call` for module-internal. `hello_puts` now exits 42 + writes "hello\r\n" to a stdout pipe — both asserted byte-for-byte. **Sub-anchors `D-LK10-CRT-INIT-INVOKE` + `D-LK10-KERNEL32-WRITE-PATH` REMAIN OPEN as future enablers** (CRT-init substrate for printf/fopen/locale; WriteFile path for kernel32-only printing) — but NEITHER was the blocker for puts. | — | — |
| D-LK10-CRT-INIT-INVOKE | **Invoke msvcrt's CRT-init shim from the LK10 entry trampoline before `call main`.** msvcrt.dll exports `_initterm` (run array of init function pointers), `__getmainargs` (parse argc/argv), and `__set_app_type` (declare console-app subsystem). For a minimal stdio-only client (`puts` / `printf` / `fopen`), msvcrt's own DllMain already initializes `_iob[]` at DLL-attach — D-FF6-RUNTIME-PRINT closure proved this (2026-06-02). However: programs that call `printf` with floating-point args, `fopen` with locale-aware paths, or any function that depends on `_initterm`-initialized atexit / static-ctor / locale state DO need the user-side init sequence. Closure (when triggered): extend the trampoline emitter to emit additional `call_indirect_via_extern` LIR ops for each CRT-init import declared by a per-format `crtInit: { initImports: [...] }` schema field. Bucket-2 discipline preserved — the emitter walks a JSON-declared list of (importLibrary, mangledName) pairs and emits one call per entry; no per-OS conditional. **NOT the blocker for puts** (re-anchored 2026-06-02 — the original anchor framing as "FF6's path A blocker" was wrong; that role belonged to D-LK10-ENTRY-ML7-FRAME-BIAS-UNIFY which closed). | **First c-subset example that calls a genuinely CRT-init-requiring function** (`printf` with `%f`, `fopen` against a locale path, anything using `atexit`). | First reproducible source-level case that crashes due to uninitialized CRT state. |
| D-LK10-KERNEL32-WRITE-PATH | **`kernel32.WriteFile` as the CRT-init-free print path.** WriteFile is a kernel32-direct function — no CRT state required. Originally anchored as "FF6 path B" but D-FF6-RUNTIME-PRINT closure via the frame-bias fix made the puts path work without WriteFile. Remains a useful future capability: any program that wants direct kernel32 I/O (no msvcrt dependency at all) needs this path. Closure prereqs: (i) ML7 `L_StackPassedArg` ✅ **closed 2026-06-02 (D-ML7-2.2 + D-ML7-2.6)** — WriteFile takes 5 args; arg 5 = lpOverlapped lives on the stack under Win64 ms_x64; (ii) c-subset typing for `void*` (HANDLE) ✅ **closed 2026-06-02 (D-LANG-POINTER-VOID-CONVERT, step 13.2)** — c-subset.lang.json declares both directional conversions implicit (C-standard §6.3.2.3); (iii) GetStdHandle returns a HANDLE which now types as `Ptr<Void>` via the new SemanticConfig::PointerConversionRules block. **Both prereqs satisfied; step 13.3 ready to land.** New example `examples/c-subset/hello_writefile/` would exercise: 3-function single-DLL kernel32 IAT (ExitProcess + GetStdHandle + WriteFile), 5-arg call with stack-passed arg, HANDLE return-value flowing into next call's arg 1. **NOT the blocker for puts** (re-anchored 2026-06-02). | **ML7 stack-args close (D-ML7-2.2)** AND **c-subset gains opaque-pointer typing for HANDLE**. | First reproducible source-level case wanting CRT-free kernel32 I/O. |
| ~~D-FF6-HELLO-PUTS-PROMOTION-PIN~~ | ✅ **CLOSED 2026-06-02 — co-landed with D-FF6-RUNTIME-PRINT.** `examples/c-subset/hello_puts/expected.json` flipped from `runOn: []` to `runOn: ["windows"]` + `expectedStdout: "hello\r\n"`. The 4 promotion pins all hold at runtime: (a) two-DLL IAT (kernel32 ExitProcess + msvcrt puts) resolves under Windows loader; (b) msvcrt.dll's own DllMain populates stdout's FILE* before user code runs (no user-side CRT init needed); (c) `puts("hello")` writes `"hello\r\n"` to the captured pipe (msvcrt text-mode `\n`→`\r\n` translation happens before bytes reach the pipe); (d) `return 42` propagates through the trampoline's `call_indirect_via_extern ExitProcess` to the OS exit code. examples_runner asserts both `exitCode==42` AND `capturedStdout=="hello\r\n"` byte-for-byte. | — | — |
| ~~D-LK6-2A-MULTI-LIBRARY-PIN~~ | ✅ **CLOSED 2026-06-02 — co-landed with D-FF6-RUNTIME-PRINT.** `hello_puts` is the first DSS-emitted PE binary that imports from TWO runtime DLLs (kernel32.dll + msvcrt.dll) AND runs cleanly under Windows. The 2-DLL .idata layout (two `ImageImportDescriptor` entries + per-DLL ILT/IAT thunk blocks + DLL name strings + null sentinels) loads correctly + both IAT slots (kernel32!ExitProcess + msvcrt!puts) resolve to the loader-fixed-up entry points + the `FF 15 disp32` indirect-call form correctly dereferences each slot at runtime. **A 2-library IAT layout regression now flips hello_puts**: stdout-byte mismatch (puts call routes wrong → no output OR wrong bytes) OR exit-code mismatch (ExitProcess call routes wrong → process AVs before exiting cleanly). | — | — |
| D-LIR-CALL-EXTERN-VARIANT-UNIFY | **Unify the two-MnemonicSlot model (`Call` + `CallIndirectViaExtern`) into one opcode with two encoding variants the encoder picks based on whether the SymbolRef's target is in the externImports set.** The current D-FF6-RUNTIME-PRINT closure landed `CallIndirectViaExtern` as a distinct LIR MnemonicSlot (and a distinct opcode in `x86_64.target.json`) — semantically parallel to direct `call` but with different encoding bytes (FF 15 vs E8) + different reloc semantics (IAT slot RVA vs callee RVA). A future cycle could fold them into one opcode with two encoding variants gated on a `LirOperand::SymbolRef` `isExternImport` bit; this would reduce the LIR vocabulary by one and remove the dispatch-on-mnemonic duplication in MIR→LIR (`lowerCall`'s opcode-selection branch) + the trampoline emitter + the materialize pass (`op == h.call \|\| op == h.callIndirectViaExtern` — already migrated to `info->isCall` for exhaustiveness post-fold). Estimated scope: ~200-300 LOC across LirOperand + encoder variant-guard + JSON schema + JSON loader + MIR→LIR + trampoline + existing tests. NOT a workaround — the current two-opcode model IS architecturally honest (different bytes, different reloc semantics ⇒ different operations). The unification is elegance vs honesty trade-off; anchored for the cycle that has time to do it cleanly. | **Third caller of `call_indirect_via_extern` arrives** (e.g. a JIT path, a 3rd format with its own GOT/PLT indirect encoding) OR **a planned encoder-tier refactor cycle**. | Third caller. |
| D-ML7-EXTERNSYMBOLS-LIVE-VIEW | **Lowerer's `externSymbols` set is a snapshot at construction time — future LIR-tier extern synthesis (`__chkstk` / `__divti3` / runtime helpers) would not be visible to in-`run()` opcode selection.** silent-failure M1 fold from the D-FF6-RUNTIME-PRINT closure audit. Today the set is populated once from the caller-supplied `externImports` vector at Lowerer ctor; `lowerCall` queries it. If a future cycle starts SYNTHESIZING externs INSIDE `run()` (e.g. an i128 div lowering emits `__divti3` as a new ExternImport during MIR→LIR), those would not be in `externSymbols` → `lowerCall` would mis-classify a call to them as internal → wrong opcode (`call` instead of `call_indirect_via_extern`) → SEGV class returns. Closure: either make `externSymbols` a live view into a mutable list, OR require any in-`run()` extern synthesizer to register into the same set explicitly. | **First LIR-tier synthesizer that mints a new ExternImport during `run()` AND calls it from the same module.** | First such synthesizer (no current path; runtime helpers today live at MIR or later tiers). |
| D-RUN-HARNESS-STDIO-SPLIT | **Split STDOUT and STDERR capture in `run_binary.hpp`.** Today's Slice 1 implementation merges both streams onto the same anonymous pipe (`STARTUPINFO.hStdOutput == hStdError`). For tests that need to distinguish "the program printed to stdout" vs "the program wrote a warning to stderr", a future cycle needs two separate pipes. Closure: extend `RunResult` with `capturedStderr` field; replace the single `pipeRead/pipeWrite` pair with two pairs; drain both after `WaitForSingleObject`. Cross-arm: POSIX needs analogous `posix_spawn_file_actions_adddup2` calls for fd 2 onto a separate pipe. | **First test that pins distinct stderr bytes** (today's discipline is exit-code + merged stdout, sufficient for hello-world + warning-free programs). | First c-subset / FF6 example asserting stderr content separately. |
| D-RUN-HARNESS-LARGE-STDOUT-DEADLOCK | **Concurrent pipe-drain to handle child stdout > kernel pipe buffer (~4–64 KiB).** Today's `run_binary.hpp` drains the pipe AFTER `WaitForSingleObject` / `waitpid` returns. A child writing more than the kernel pipe buffer (typically 4–64 KiB on Windows; 64 KiB on Linux) deadlocks on `write()` because the parent hasn't drained — the parent's `WaitForSingleObject` then trips the 5-second timeout and SIGKILLs the child, silently truncating captured output and turning the run into a "timeout" diagnostic instead of "child wrote >64KiB". hello_puts is 6 bytes so safe; this is latent. Closure: Windows arm needs OVERLAPPED ReadFile + WaitForMultipleObjects (process handle + pipe-read event); POSIX needs select/poll loop alongside `waitpid(WNOHANG)`. silent-failure HIGH-3 from FF5 audit fold. | **First test that pins stdout content > 4 KiB** OR **first observed flaky failure where a child writes more than expected**. | First multi-kilobyte stdout assertion. |
| D-FFI-HEADER-VALIDATION-OPTIONAL | **Compose `ingest()` BEFORE `synthesizeFfiFromSourceDecls()` to validate source-declared signatures against shipped headers.** Today's c-subset path trusts the source's extern decl verbatim — `extern int puts(const char* s);` reaches the linker as `{mangledName: "puts", importLibrary: "msvcrt.dll"}` with NO check that msvcrt.dll actually exports a function called `puts` with a compatible signature. A future language that wants compile-time signature validation (Rust-style "extern fn declarations must match the shipped header") would `ingest()` first, then `synthesize()` for any source externs not matched. Both functions share the FF3+FF4 kernel; the compose pattern just runs `ingest()` first, takes its `ffiMap` populations, and falls through to `synthesize()` for unmatched HIR externs. | **First language opts into header-driven signature validation** OR **a c-subset cycle that wants to catch msvcrt.dll API drift at DSS compile time** (e.g. msvcrt's `puts` returns `int` historically; a hypothetical future msvcrt rev returning `int32_t` would be caught here rather than at link time). | First call site composing both producers. |
| D-FFI-ABI-GATE-HELPER | **Extract the FF3+FF4 head block shared by `ingest()` and `synthesizeFfiFromSourceDecls()` to a free helper.** Both functions open with: `auto returnWithSnapshot = [&]() { ... };` + `auto abi = resolveAbi(target, format, reporter); if (!abi) return returnWithSnapshot(); if (abi->cc == nullptr) { dss::report(F_FfiIngestAbiModelUnsupported, ...); return returnWithSnapshot(); }`. 15 LOC duplicated. The helper's signature would return `enum class AbiGateResult { Proceed, Halt }` so the caller can branch + call `returnWithSnapshot()` itself (the lambda captures local `result` so can't be hoisted with the helper). | **3rd caller** (e.g. archive ingest path, or a JIT-side FFI synthesis). | Third producer. |
| D-FFI-SYNTH-ANNOTATION-COUNT-VERIFY | **`compileSingleUnit` step 2.5 verifies `ffiResult.externsAnnotated == refs.size()`.** Today the step discards `ffiResult` via `(void)ffiResult;` and gates only on `tierClean(reporter, ffiEntry)`. A regression where `synthesize()` silently annotates zero externs (e.g. an internal early-return that doesn't emit a diagnostic) would let the pipeline continue with an empty ffiMap → MIR step then fires per-extern `H_UnsupportedLoweringForKind` (the existing fail-loud at HIR→MIR), so the gap is bounded — but the diagnostic distance from cause (synthesize bug) to symptom (MIR loud-fail) makes triage harder than necessary. Closure: assert `ffiResult.externsAnnotated == refs.size()` after the call OR add a new `F_FfiSynthAnnotationCountMismatch` code. | **Observed bug where annotation count silently mismatches the input count.** | First such regression. |
| D-FFI-EXTERN-RECORD-VIEW-UNIFY | **Unify `HirExternRecord` (owning) and `ExternDeclRef` (view) into one type.** Today's split is load-bearing: the lowerer needs `HirExternRecord` to own its canonical-name string past the SemanticModel's lifetime; the FFI stage prefers `ExternDeclRef` views to avoid mid-pipeline copies. A unified owning-or-view type (variant<string, string_view>) would force one tier to regress (either copy-on-every-call for the view consumer, or own-and-allocate for the producer). Worth revisiting if a third producer arrives OR if `ingest()`'s hot path grows. | **Third producer of ExternDeclRef arrays** OR **profiled hot-path overhead from ExternDeclRef construction**. | Third producer. |
| D-FFI-INGEST-RESULT-PRODUCER-TOKEN | **Enforce "only producers can populate `HirIngestResult.errorCountAtReturn_`" structurally via a private capability token.** Today's discipline is convention-only: the docblock says "only `ingest()` and `synthesizeFfiFromSourceDecls()` should call `snapshotErrorCountOnce`". With two producers and the doors potentially opening to a third (FFI-COMPOSE compose path, archive ingest, JIT), a stronger structural enforcement (private ctor + friend the two free functions + non-default-constructible publicly) would prevent accidental third-caller drift. The "snapshot-once" no-op semantic prevents corruption but not first-write-by-wrong-caller. | **Third HirIngestResult-producing free function** OR **observed bug where a non-producer called snapshotErrorCountOnce**. | Third producer. |

---

## 4. Open questions

| # | Question | Default if unanswered |
|---|----------|-----------------------|
| 1 | `--as-needed` / unused-import elision? | **No** — link every declared FFI import. Elision reserved post-v1. |
| 2 | Mach-O universal binaries (multi-arch one file)? | **Post-v1.** v1 ships one binary per arch. |
| 3 | Incremental linking? | **Post-v1.** |
| 4 | LTO / link-time IR-level optimization? | **Post-v1.** |
| 5 | Section ordering policy? | Page-aligned segments per format conventions (ELF: text/rodata/data/bss; PE: text/rdata/data/idata/edata/reloc; Mach-O: __TEXT/__DATA_CONST/__DATA/__LINKEDIT). |
| 6 | Build-id input space? | BLAKE3 of section contents. Deterministic. |
| 7 | DT_RPATH / DT_RUNPATH for ELF? | Honored if specified in project config; default unset. Mach-O equivalent: `@rpath` in LC_RPATH (same logic). |
| 8 | Linker scripts? | **No.** Layout templates live in the per-format JSON schema (`*.format.json`); user-provided linker scripts reserved post-v1. |

---

## 5. Acceptance criteria

- [ ] ELF writer output passes `readelf -a` / `objdump -d` clean on Linux x86_64 + ARM64.
- [ ] PE writer output passes `dumpbin /HEADERS /ALL` clean on Windows x86_64 + ARM64.
- [ ] Mach-O writer output passes `otool -l -L -h` clean on macOS x86_64 + ARM64.
- [ ] Mach-O codesign-verify-style structural check passes after `16-codesign-publish-plan` fills the LC_CODE_SIGNATURE.
- [ ] c-subset corpus binaries run + print expected output on all 6 (OS × arch) v1 targets.
- [ ] **Hermetic gate**: CI runner with NO `ld` / `link.exe` / `ld64` / `lld` / `wasm-ld` installed builds the c-subset corpus end-to-end (LK10).
- [ ] BLAKE3 build-id is deterministic: rebuilding the same source twice produces byte-identical output.

---

## 6. Risks

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Mach-O chained-fixups format complexity (intricate bind opcode trie) | High | High | **Classified as bucket-2 universal-algorithm** per §2.0 (the bind / lazy-bind / rebase opcode trie is one stream emitter walking a JSON-declared opcode vocabulary, NOT a per-format C++ tree). LK3 lands the JSON declaration + the engine's trie-emit pass; integration test on Apple Silicon CI runner pins the byte output against an oracle (real `dyld_info` dump) before LK3 ships. Same shape for chained-fixups page encoding (LC_DYLD_CHAINED_FIXUPS) — chain templates declared in JSON, the page-walker is universal. |
| PE IAT layout intricacies (32-bit vs 64-bit thunks, hint/name tables) | Medium | High | Per-import golden bytes pinned against dumpbin oracle output. |
| ELF dynamic linking edge cases (PLT lazy-binding races, GNU_HASH layout) | Medium | High | Lazy-binding deferred to post-v1; v1 uses immediate binding (BIND_NOW). Tracked at §3.1 **D-LK6-11** (ELF) + **D-LK6-12** (PE delay-load) + **D-LK6-13** (MachO lazy-bind on x86_64 legacy path). Apple Silicon chained-fixups (**D-LK6-14**) is always-eager-by-design — no lazy mode to defer. |
| TLS model bugs surface only at multi-threaded runtime | Medium | Critical | LK5 acceptance includes a multi-threaded smoke test per platform. |
| WASM / SPIR-V skeleton drifts from full plans (18 / 17) | Low | Medium | LK8 / LK9 are skeleton-only; full impl is plan-owned; cross-link discipline enforced. |
| ~~Linker engine mismatch with assembler relocation kinds~~ | Low | Low | **Resolved rev 3 via plan 13 §2.6 unifier (2026-05-29):** relocation taxonomy lives as a `relocations[]` facet on `*.target.json` (NOT a separate `relocation.hpp` enum). Assembler emits opaque `uint32_t kind = tag`; linker resolves via `schema.relocationInfo(tag)`. Both consume the same `TargetSchema` instance — there's no two-side enum to drift. The object-format-specific reloc *names* (e.g. `R_X86_64_PC32` vs `IMAGE_REL_AMD64_REL32`) live in `*.format.json` as `format-name → schema-tag` mappings, so a single bucket-1 formula serves every format that supports it. |

---

## 7. Sequencing

```
13-assembler (AS4) ─► LK4 (engine)
                         │
              ┌──────────┼──────────┐
              ▼          ▼          ▼
             LK1        LK2        LK3 ─► LK7 ─► 16-codesign-publish
            (ELF)      (PE)      (Mach-O)
              │          │          │
              └──────────┼──────────┘
                         ▼
                       LK5 ─► LK6 ─► LK10 (cycle 2 ✅ + D-LK10-ENTRY ✅ CLOSED 2026-06-09) ─► FF6 ✅ 3-OS×2-arch (puts runs) ─► variadic printf ✅ (FC12, hello_printf/printf_int run) ─► [LK10-full strict-hermetic-gate ⏳]
                                       │       (entry tramp +     (hello-world)   (cross-OS
                                       │        process-exit +                     acceptance)
                                       │        run-harness;
                                       │        FIRST runnable binary)
                          ┌────────────┼────────────┐
                          ▼            ▼            ▼
                         LK8         LK9         15-debug-info
                        (WASM)    (SPIR-V)
```

**★ D-LK10-ENTRY ✅ CLOSED 2026-06-09** (Stage 1 + ARM64 + MACHO-EXIT all landed + runtime-confirmed across the v0.0.2 cycles) — it WAS the critical-path gate between "byte-valid executable on disk" and "executable that runs"; that gate is now PASSED on all runnable targets (FF6 hello-world ✅ runs 3-OS × 2-arch). The current frontier is **V2-3 artifactProfile (AP4) / V2-4 program-api CLI**; LK10-full's only residual is the strict no-system-linker hermetic CI gate (variadic `printf` landed with FC12 — `hello_printf`/`printf_int` run).

LK1/LK2/LK3 are parallel. LK4 ties them together. LK5 + LK6 finish the substrate. LK10 is the hermetic acceptance gate. LK8/LK9 are skeletons unblocking 17/18. **LK11** (v1.x) ships post-LK10 — couples with [`08-compilation-unit-plan`](./08-compilation-unit-plan%20-%20tbd.md) CU6 to lift the single-CU-per-image assumption (see §2.12); trigger-gated, not on the v1 critical path.

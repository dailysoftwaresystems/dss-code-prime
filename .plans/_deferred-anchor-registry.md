# Deferred-Anchor Registry

> The canonical source of truth for every `D-*` anchor cited in `src/` but
> deferred to a future cycle. The leading underscore sorts this file before
> the numbered plans in directory listings — call this out as the registry,
> not a plan. Per-plan §3.1 rows are still the preferred home for anchors
> whose feature area maps cleanly onto a plan; this file catches the orphans
> + cross-cutting anchors that don't belong to any single plan.
>
> **CI/pre-commit guard** (`tools/check-anchor-registry.{sh,ps1}`): every
> `D-*` identifier appearing in `src/` MUST resolve to a row in this file
> OR a citation in any `.plans/*.md`. Pre-fix this recurred TWICE
> (memory: "the same gap silent-failure caught for MULTI-PAGE-GOT").
> The guard makes the system enforce the discipline — same shape as the
> agnosticism veto + the `CallConv` static_assert.
>
> **V2-4.X / full-C ownership (2026-06-10):** the C-language-feature deferral
> CLUSTER — modulo codegen (`D-CSUBSET-MOD-OP-CODEGEN`, …), primitive widths
> (`D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH`), linkage/visibility syntax
> (`D-CSUBSET-LINKAGE-VISIBILITY-SYNTAX`, `D-DECL-PREFIX-STRIP-SHARED-HELPER`),
> rodata aggregates (`D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY`), non-main implicit
> return (`D-LK10-ENTRY-MAIN-IMPLICIT-RETURN` — the OPEN non-main successor row,
> cross-ref `D-CSUBSET-ENTRY-NAME-SPLIT`), variadic FFI
> (`D-FFI-DESCRIPTOR-VARIADIC-SIGNATURE`), the apple-arm64 ABI divergence
> (`D-FF3-APPLE-ARM64-ABI-DIVERGENCE`), and peers — is now OWNED by **stepper
> V2-4.X / [`23-full-c-plan`](./23-full-c-plan%20-%20tbd.md)** (c-subset → full
> C23). Each closes inside the relevant FC phase (see the plan §3.1).
>
> **NOT in this cluster — already shipped, do NOT re-claim as new FC work**
> (caught by the 2026-06-10 plan-lock accuracy review). Two genuinely-CLOSED
> anchors: `D-CSUBSET-DIVISION-OP-CODEGEN` (✅ 2026-06-04 — signed division shipped;
> the modulo successor `D-CSUBSET-MOD-OP-CODEGEN` ✅ since closed too — FC1 cycle 1, 2026-06-10, above) and `D-FFI-EXTERN-CALL-DISPATCH`
> (✅ 2026-06-08 — direct-plt/indirect-slot dispatch shipped; the OPEN variadic concern
> is `D-FFI-DESCRIPTOR-VARIADIC-SIGNATURE`, above). PLUS one closed FEATURE with no
> anchor row: the `main` implicit-return-0 (shipped 2026-06-02, live in
> `c-subset.lang.json`, config-only) — the anchor `D-LK10-ENTRY-MAIN-IMPLICIT-RETURN`
> itself remains the still-OPEN non-main successor (above), NOT a closed item. The
> full-C arc supersedes each predecessor with its named successor, never by reopening it.
>
> New full-C exotic-tail exclusions land as `D-FULLC-*` rows as clusters ship.
> Individual rows are NOT rewritten here yet (the plan is the live map until a
> cycle actually closes the anchor).
>
> **Schema (one row per anchor)**:
> ```
> | Anchor | Trigger | Closing work | Cross-refs |
> ```
>
> When an anchor closes, replace its row's status with `✅ CLOSED <date>`
> and cite the commit/SymbolId. Don't delete the row — the audit trail is
> load-bearing for future regressions.
>
> **Authorization log — robustness sweep (2026-06-24, branch `feature/0-0-2-p14`):**
> the user authorized a "robustness sweep," which FIRED the trigger for three
> trigger-gated anchors and CLOSED them in one sweep (393/393 ctest, MSVC
> Release + MinGW): `D-CSUBSET-BLOCK-SCOPE-PROTOTYPE`,
> `D-CSUBSET-EXTERN-DEFINITION-MERGE`, and
> `D-SEMANTIC-ASSIGN-STMT-ASSIGNABILITY-BYPASS` (see their rows below). The
> sweep surfaced one new backlog row, `D-CSUBSET-TENTATIVE-DEFINITION-MERGE`
> (C 6.9.2 tentative definitions — still fail-loud, trigger-gated). [That backlog
> row was later ✅ CLOSED 2026-06-28 (p19 Cluster G c33) when the sqlite3.c
> re-probe fired its trigger — see its row below.]

---

## Anchor Index

| Anchor | Trigger | Closing work | Cross-refs |
|---|---|---|---|
| `D-PARSE-EXPR-DEPTH-GUARD` | ✅ **GUARD DELIVERED 2026-06-20 (FC13 hardening cycle); CAP RAISED 16→256 on 2026-06-20 (FC13 cycle 2) once both cap-raise blockers (`D-PARSE-DEEP-FRONTEND-STACK` + `D-PARSE-SPECULATION-OPERAND-QUADRATIC`) closed — the cap is now a real SEMANTIC limit, not a host-stack artifact (parse is O(N) + the frontend's deep recursion [CU build + `analyze`] runs on a 64 MiB worker stack via `core/substrate/large_stack_call`). The "lowered to 16" note below is HISTORICAL.** The Pratt-walker expression-depth guard (`ParserConfig::maxExpressionDepth`, enforced at the single chokepoint `parseExpressionAt` in `src/analysis/syntactic/parser.cpp`) was a `fatal()`/`std::abort()` on overflow, and its cap (256) was ~10x too high for the host C++ stack: the heaviest path (the paren atom re-entry `parsePrimary->parseUntilFrameDepth->stepOnce->walkExpression->parseExpressionAt`) overflows a Windows ~1 MB Debug stack at depth ~25 — BEFORE the guard could fire — so deeply-nested input RAW-crashed (rc 127) or hung. **DELIVERED:** (1) the `fatal()` is now a POSITIONED `P_ExpressionTooDeep` (0x0018) at the offending token + graceful RECOVERY (`recoverExpressionTooDeep_`: emit-once + Error leaf via `panicRecover` + unwind), never an abort/overflow; (2) the default cap lowered 256→16 so the guard fires with comfortable margin below the Debug paren overflow (~25) while still admitting realistic hand-written C nesting (≤15 deep). STEP-1 empirical finding: (B) on-path (NOT a bypass) — `expressionDepth` DOES grow per nesting level; the cap was simply unreachable before the stack blew. **CAP-RAISE follow-up → `D-PARSE-DEEP-FRONTEND-STACK`:** EXPLORED this cycle and found INSUFFICIENT — a parser-only large-stack worker thread does NOT enable a higher cap, because stage markers localized the ~25-level overflow DOWNSTREAM of the parser (semantic `analyze` / HIR-MIR lowering recurse over the expr tree on the host's MAIN ~1 MB stack); raising the cap needs the downstream stages deep-safe (`D-PARSE-DEEP-FRONTEND-STACK`) AND the speculative cost bounded (`D-PARSE-SPECULATION-OPERAND-QUADRATIC`). **SECOND follow-up (distinct, pre-existing) — c-subset speculative-operand O(N²):** the depth guard eliminates the CRASH, but on PATHOLOGICALLY deep parens the c-subset full parse is SLOW (CLI @ cap=16: depth 100 ~2s, 150-200 ~12s, 250+ >25s) because the `operand` rule is `speculative:true` (compoundLiteral/cast/paren disambiguation, lookahead 64 = 1024-token probe budget): each of the cap-many paren levels re-probes the deep tail, and nested speculation (`maxSpeculationDepth=8`) rollback-re-parses it. This is INDEPENDENT of the cap (cap=8 still times out @ depth 1000) and of THIS fix (pre-fix the deep parens crashed in Pratt recursion before the speculation finished; the fix EXPOSES the speculation cost, it does not cause it). A NON-speculative grammar (`kParenSchema`) parses 1000 parens in ~10ms — the headline unit test pins exactly that (the GUARD mechanism, isolated from c-subset speculation). The fix is a speculation-engine / grammar concern (memoize probe results, or bound speculation by expression depth), NOT the depth guard. Note: only manifests on ALREADY-INVALID input (depth > cap); valid C nests ≤4 in the entire corpus. Tests: `PrattWalker.{ExceedingMaxExpressionDepthDiagnosesAndRecovers (lowered-cap structural pin), DeeplyNestedParensDiagnoseAtDefaultCapWithoutCrashing (1000 parens @ default cap → positioned diag, no rc-127), DeepPrefixChainDiagnosesAndRecovers, DeepTernaryChainDiagnosesAndRecovers}` (test_pratt_walker.cpp) PLUS the c-subset end-to-end golden pin `tests/corpus/diagnostics/c-subset/expression_too_deeply_nested.c` (real frontend → `P_ExpressionTooDeep 1:41-1:42` golden, the exact code+position at the 17th paren where cap=16 trips). | (guard delivered; cap-raise → the 2 dedicated anchors) | plan 05 (PA2 walker); `D-PP-FATAL-HALTS-PARSE` (sibling); `D-PARSE-DEEP-FRONTEND-STACK` + `D-PARSE-SPECULATION-OPERAND-QUADRATIC` |
| `D-PP-FATAL-HALTS-PARSE` | ✅ **CLOSED 2026-06-20 (FC13 hardening cycle).** The compilation-unit PP branch (`compilation_unit.cpp` `parseAndAdd_`) parsed UNCONDITIONALLY even when a FATAL preprocessor backstop had TRUNCATED the synthesized stream (the >256 macro-expansion-nesting guard or the include-nesting guard), feeding the truncated tokens to the parser — producing an inscrutable secondary cascade / hang on top of the real PP cause. **CLOSED:** added `PreprocessResult::fatal` (set ONLY by the two stream-TRUNCATING backstops: `MacroExpander::truncated_` at the >256 guard, `SynthBuilder::fatal` ref-threaded through the recursive include builders at the include-depth guard); the CU gate now keys on `pp.fatal` and, when true, feeds the parser an EOF-ONLY stream — yielding a minimal well-formed tree that STILL carries the (remapped) PP diagnostics, halting the parse before the cascade. Crucially the gate is NARROW: a RECOVERABLE PP error (missing `#include` file, malformed directive, redefinition) or a folded LEXER error leaves the stream INTACT and the parser STILL runs (an earlier `diagnostics->hasErrors()` gate was too broad — it swallowed the rest of a file on an unresolved `#include`; caught by `HirLoweringCSubset.IncludeDirectiveIsSkippedNotFailed` + the FFI ingest tests). Tests: `CompilationUnitPPGate.{FatalMacroNestingTruncationGatesParser, OrdinaryMacroCompileIsNotGated, UnresolvedIncludeStillParsesRestOfFile}` (test_compilation_unit.cpp). | (closed) | `D-PARSE-EXPR-DEPTH-GUARD` (sibling); plan 05; FC13 |
| `D-PARSE-DEEP-FRONTEND-STACK` | ✅ **CLOSED 2026-06-20 (FC13 cycle 2 — deep-expression-nesting end-to-end).** Built the standard compiler facility for deep recursion: `src/core/substrate/large_stack_call.{hpp,cpp}` — `runOnLargeStack(stackBytes, fn)` runs `fn` on a dedicated worker thread with a `stackBytes`-RESERVE stack, JOINs (synchronous — no concurrency), and re-throws any exception `fn` threw (captured into `std::exception_ptr` on the worker, rethrown on the joiner; never escapes the entry → no `std::terminate`); value-returning convenience `callOnLargeStack`; `kDeepRecursionStackBytes = 64 MiB` (cap 256 × ~40 KB/level ≈ 10 MB, lazily-committed reserve costs ~nothing). FAIL-LOUD (`std::abort` + stderr) on every thread-API failure AND caller bug (empty `fn`, `stackBytes==0`, Windows `stackBytes > UINT_MAX` — reject, never truncate the 32-bit `_beginthreadex` arg). Windows arm = `_beginthreadex` + `STACK_SIZE_PARAM_IS_A_RESERVATION` + `WaitForSingleObject`/`CloseHandle`; POSIX arm = `pthread_attr_setstacksize`/`pthread_create`/`pthread_join` (CI-verified — no `find_package(Threads)`, raw symbols resolve as `std::thread`'s do). AGNOSTIC: pure host/OS utility, names no language/CPU/format; the `_WIN32`/POSIX `#ifdef` is host-portability. Self-test `tests/core/substrate/test_large_stack_call.cpp` (RunsTheCallable; RethrowsEscapedException value-preserving; ProvidesStackForDeepRecursion = ~1000 frames × `volatile char[8192]` ≈ 8 MB that the default stack can't hold). **KEY EMPIRICAL CORRECTION of the localization below:** wrapping JUST `analyze` was INSUFFICIENT — stage markers in the full c-subset→PE-exec pipeline showed the analyze-wrap lifted the DOWNSTREAM ceiling well past 25 (n=100..180 compile + run clean), but at the raised cap (256) a NEW overflow appeared UPSTREAM: the **CU build (parse / preprocess / type-name oracle)** in `Program::compileFiles`/`compileUnits` (`UnitBuilder::addFile`→`finish`) recurses building the expression tree and overflows the host main stack at ~n=185 (Debug-MSVC) — the low cap (16) had MASKED it (the guard fired at 16, no deep tree was ever built). So the CU build, `analyze`, AND the BUILD-half `buildCuMir` (HIR + MIR) are ALL wrapped on the 64 MiB stack: `analyze` via a file-static `analyzeImpl` + a thin public `analyze` (`callOnLargeStack(kDeepRecursionStackBytes, …)`); the two CU-build sites in `program.cpp` via `callOnLargeStack` returning the `CompilationUnit`. The BUILD half `buildCuMir` (= analyze + CST->HIR + HIR->MIR) is wrapped via a file-static `buildCuMirImpl` + a thin `buildCuMir` (`compile_pipeline.cpp`). **THIRD incompleteness, independent-audit-caught (this cycle's dss-audit):** wrapping JUST parse + analyze was STILL insufficient — CST->HIR (`cst_to_hir.cpp` `lowerExpr`/`lowerOperand`) and HIR->MIR (`hir_to_mir.cpp` `lowerExpr`) ALSO recurse one host-stack frame per nesting level, running inline on the MAIN thread inside `buildCuMir` AFTER `analyze`'s worker has joined; so a valid 41..256-deep expression that parsed + analyzed could still overflow downstream (it HAPPENED to fit at n<=250 on Debug-MSVC, but that is host/frame-size luck, NOT a guarantee — exactly the uncontrolled-margin the wrap removes). FIXED by the `buildCuMir` wrap (analyze nests as a benign inner worker; peak live-deep stack ~10 MB << 64 MiB). Only the LOWER half (MIR->LIR->codegen->link, `lowerMirModuleToAssembly`) genuinely needs no wrap — it iterates a FLAT SSA arena (`mir.hpp`), NOT a tree, so it is non-recursive in nesting depth (audit-confirmed). Cap raised 16→**256** (`parser.hpp`); now a real SEMANTIC limit (parse O(N) via `D-PARSE-SPECULATION-OPERAND-QUADRATIC`'s LL(k) prune + analyze/parse on the big stack), so deep-but-legal C compiles end-to-end and only pathological nesting past 256 trips the positioned `P_ExpressionTooDeep` guard. **Verified:** n=100/200/250 c-subset compile (rc 0, ~0.2s) + run (exit 0); n=300 fails LOUD fast (`P0018`, no hang/crash). Test harnesses that build deep CUs unwrapped were also routed onto the facility (the corpus harness `test_diagnostic_corpus.cpp` for the >256-paren fixture; the headline `PrattWalker.DeeplyNestedParensDiagnoseAtDefaultCapWithoutCrashing` 1000-paren pin at the raised default cap) and the duplicate local `runOnLargeStack` in `test_parser_speculation.cpp` was CONSOLIDATED onto the substrate facility (Win32 `CreateThread` trampoline deleted). Re-added + DEEPENED the runnable witness `examples/c-subset/deeply_nested_expression` (200 nested parens → exit 7; PE-windows + ELF-linux) — a VALID deep expression that compiles AND runs through the WHOLE BUILD+LOWER pipeline at 200 nesting levels (closes the audit's M2: the prior 40-paren example + the guard-tripping fixtures never lowered a valid deep tree past HIR). Corpus fixture `expression_too_deeply_nested.c` regenerated to 300 parens (golden `P_ExpressionTooDeep 1:281-1:282` + `P_UnexpectedToken 1:582-1:583`). 335 ctest green. — *Original localization (audit trail):* Frontend stages DOWNSTREAM of the parser (semantic `analyze`, HIR/MIR lowering) recurse over the expression tree on the host's MAIN ~1 MB stack (Windows) and OVERFLOW at ~25 nested levels (rc-127, NO diagnostic), independent of the parser (which handles deeper). This is what caps `ParserConfig::maxExpressionDepth` at 16. Localized empirically via stage markers in `program.cpp` `runCusToTargets` (parse completes; the crash is inside `compileOneTarget`→`analyze`). A parser-only large-stack worker thread was EXPLORED this cycle and does NOT help (the crash is downstream). [SUPERSEDED: the crash was ALSO upstream in the CU build once the cap allowed a deep tree — see the close note above.] | (closed — `large_stack_call` substrate; CU build + `analyze` + BUILD-half `buildCuMir` [HIR/MIR] on a 64 MiB worker stack; cap 16→256) | `D-PARSE-EXPR-DEPTH-GUARD`; `D-PARSE-SPECULATION-OPERAND-QUADRATIC`; `parser.hpp` maxExpressionDepth; `core/substrate/large_stack_call.hpp` |
| `D-PARSE-SPECULATION-OPERAND-QUADRATIC` | ✅ **CLOSED 2026-06-20 — fixed by a config-driven LL(k) predictive prune (the `Correct direction (b)` below, IMPLEMENTED): `computePredictivePrefixes` (grammar_schema_json.cpp) precomputes a per-rule FIRST_k `predictivePrefix` at schema-load (the leading single-token run + the first variable element's FIRST set, STOPPING before a NULLABLE stop element so each recorded offset is an EXACT admissible-token set ⇒ a SOUND over-approximation); `predictivePrefixPrunes` (parser.cpp) prunes a speculative candidate when an OBSERVED next-token is outside its prefix; a lone unguarded survivor DESCENDS DIRECTLY (no probe). `((` → castExpr/compoundLiteral pruned (token[+1]=`(` ∉ FIRST(castTypeRef)) ⇒ O(N); n=300 154s→0.17s (~1100×); the lone-`(Identifier)` ambiguity still speculates (binder triage unchanged). Agnostic (walks FIRST sets + the position table, names NO token) + config-driven (FIRST + the rule's `lookahead`); independent review-folded — nullable-stop soundness guard + contextual-keyword precondition → `D-PARSE-PREDICTIVE-PRUNE-CONTEXTUAL-KEYWORD` + synthetic RED-on-disable tests (`ParserSpeculation.{DeepNestCastVsParenIsLinear, PrunesFirstBranchAtOffsetOneAndDirectDescends, NullableStopElementDoesNotOverPrune}`); 333 ctest. A RESIDUAL mild wall-clock super-linearity in the deep parse itself was CHARACTERIZED 2026-06-22 and promoted to its own row `D-PARSE-DEEP-NEST-RECURSION-MEMORY` — a controlled flat-chain-vs-nested experiment PROVED the parse WORK is O(N) (the "TreeBuilder/arena bookkeeping" guess first sketched here is REFUTED: a flat `0+0+…+0` chain of equal node count parses with FLAT per-element cost), so the residual [measured ~exp 1.7, not the ~exp 1.2 first estimated] is a constant-factor cost of the N-deep HOST recursion (inferred BY ELIMINATION to be memory-hierarchy behavior — not directly cache-instrumented), bounded/moot under the 256 depth cap — LOW-priority / trigger-gated. — Original characterization (audit trail):** The c-subset `operand` rule is `speculative:true` (compoundLiteral/cast/paren cast-vs-paren disambiguation; lookahead 64 = 1024-token probe budget). Deeply-nested input parses SUPER-LINEARLY (O(N²)+) in depth — measured @ cap=16: depth 20 ~0s, 100 ~3s, 300 TIMES OUT (>40s) — so deep input HANGS before the depth guard can fail loud, even at a LOW cap (the speculation descends the deep tail at the early levels, before the guard depth is reached). INDEPENDENT of the cap (repro @ 16/64/256) and of the guard (a non-speculative `kParenSchema` parses 1000 parens in ~10ms). FIX ATTEMPT 1 FAILED 2026-06-20: a code-trace MIS-diagnosed the cost as O(depth) `SpeculationProbe` state-snapshots (walker `cursorStack_`/`wrapFrameFlags_`, builder `open_`/`scopes_`); the O(1) size-truncation fix had ZERO timing effect AND broke the general non-append-only `SchemaWalker::snapshot/restore` API (`core/test_schema_walker`) — REVERTED. **MEASURED root cause 2026-06-20 (instrumented counters, NOT a code-trace):** the O(N²) is PANIC-MODE RECOVERY running INSIDE doomed speculative `castExpr` probes — `panicInSpec == panicRecovers` (100% of panics are in-probe) and `panicScanTokens ≈ tokensAdvanced` (the parser advances tokens almost solely to panic-scan inside failing cast probes, then rolls them back). For `((((0))))` the `castExpr` probe (tried BEFORE `parenExpr` in the `operand` alt) fails at each inner `(` (castTypeRef can't start with `(`) and `panicRecover` scans forward to the end-of-expression sync point = O(min(N,64)) tokens × N levels. **FIX ATTEMPT 2 FAILED 2026-06-20:** suppressing the panic-scan inside probes (`panicRecover` early-return when `speculationDepth > 0`) was correctness-safe (ctest 333-green) but COUNTERPRODUCTIVE — deep input HUNG WORSE (n=100: ~3 s → timeout, NO diagnostic emitted): the panic-scan is LOAD-BEARING for the failing probe to TERMINATE / make forward progress, NOT pure waste. REVERTED. **Correct direction (UNTRIED — a real engine rework, not a one-liner):** a CHEAP fail-fast for the cast-probe mismatch that PRESERVES termination — either (a) dispatch-level: fail the probe immediately on a required-alt no-candidate while `speculationDepth > 0` (instead of routing through panic recovery), or (b) a k-token candidate prune at the `operand` AltChoice (don't ENTER the cast/compound-literal probe when the token after `(` cannot start `castTypeBase`); both must stay agnostic (FIRST-set / generic dispatch, no language identity) and must not break cast / compound-literal / sizeof / the binder-sketch commit triage. Only manifests on deeply-nested input (valid C nests ≤4 in the corpus). | (closed — LL(k) predictive prune, direction (b)) | `D-PARSE-EXPR-DEPTH-GUARD`; `D-PARSE-DEEP-FRONTEND-STACK`; `D-PARSE-PREDICTIVE-PRUNE-CONTEXTUAL-KEYWORD`; `c-subset.lang.json` operand rule |
| `D-PARSE-PREDICTIVE-PRUNE-CONTEXTUAL-KEYWORD` | ✅ **CLOSED 2026-06-23 (FC-deferral close-out cycle 13, branch p14).** Closing the end-to-end "a contextual keyword in a speculative candidate's fixed prefix parses correctly" needed BOTH a PRUNE half (the registry's prescribed fix) AND a deeper PROBE half (the build DISCOVERED it; the anchor did not scope it → §B-surfaced, user chose build-deep). **(1) PRUNE:** a loader-derived token-id-keyed `contextualKinds` set (`computeContextualKinds` in `grammar_schema_json.cpp`, from the per-`LexemeMeaning.contextual` flags) + an O(1) `GrammarSchema::isContextualKind(SchemaTokenId)` query; `predictivePrefixPrunes` (`parser.cpp`) now `continue`s at any offset whose OBSERVED token is contextual → the candidate survives the prune. **(2) PROBE (the crux the build exposed):** even surviving the prune, the candidate's speculative probe `recoverAt(P_UnexpectedToken)`'d — the TokenLeaf MATCH GATE in `stepOnce` tests the UN-demoted keyword kind (`effectiveKind` by design does not model demotion), so the token never reached the builder's `pushToken` contextual-demotion. FIX: the match gate now MIRRORS the builder's demotion (a contextual `tokKind` ∉ expected → treat as a match when `identifierKind` IS expected) and advances the PARSER's walker with the demoted `identifierKind`, keeping it in lockstep with the builder's walker (no spurious desync-latch trip → the probe commits). Both halves AGNOSTIC (config-derived `isContextualKind`; the demotion target REUSES the parser's EXISTING `identifierKind` — the schema's resolved `Identifier` token kind, IDENTICAL to the builder's demotion this mirrors → walker lockstep) — EMPTY `contextualKinds` for a non-contextual grammar ⇒ ZERO behavior change, the deep-nest O(N) prune win preserved. (Review-noted pre-existing residual `D-PARSE-IDENTIFIER-KIND-HARDCODED-VS-CONFIG`: the parser+builder resolve the demotion target via the literal name `"Identifier"` rather than the live `semantics.identifierToken`; latent — all shipped grammars declare `identifierToken: "Identifier"` so they coincide; trigger-gated, NOT introduced here.) 374 ctest green, ZERO regression incl. **tsql** (the `reservedWordPolicy: contextual` grammar where the gate CAN fire). Pinned RED-on-disable with BOTH halves toggled INDEPENDENTLY (prune-skip off → caseId pruned; match-gate off → caseId survives prune but probe rejected → replays wrong candidate) by the synthetic contextual-keyword speculative-alt schema in `test_parser_speculation.cpp`: `ParserSpeculation.{ContextualKeywordInSpeculativePrefixNotMispruned` (soft keyword `kw` in caseId's `[A,Identifier,;]` prefix demotes + commits + asserts the `P_ContextualKeywordResolution` demotion diagnostic fired)`, HardKeywordInSpeculativePrefixStillPruned` (negative control — a HARD keyword stays pruned; the fix doesn't over-broaden)`}`. RESIDUAL: none for this anchor. — *Original OPEN characterization (audit trail):* **registered 2026-06-20 (FC13 predictive-prune soundness fold).** The LL(k) speculative-alt PREDICTIVE PRUNE (`Parser::predictivePrefixPrunes`, the O(N) deep-nest lever that landed alongside `D-PARSE-SPECULATION-OPERAND-QUADRATIC`) compares a candidate's exact per-offset FIRST_k prefix against the observed token's `effectiveKind`. `effectiveKind` DELIBERATELY does NOT model contextual-keyword demotion (`parser.cpp` ~L63 — a soft keyword keeps its KEYWORD kind here; demotion to Identifier happens later in the builder against the live cursor expectedSet + scope stack). So for a grammar with contextual keywords (`contextual: true` per entry, or grammar-wide `reservedWordPolicy: "contextual"` — e.g. tsql), at an offset whose admissible set holds the demotion target (Identifier) the prune could see the keyword kind, find it absent, and WRONGLY drop a candidate the demoted token would in fact match — a silent mis-parse. NOT live today: c-subset's speculative prefixes are all hard keywords / punctuation, and tsql's lone `nameOrCall` speculative alt discriminates at offset 1 on `(` / qualified-name punctuation, not on a soft-keyword Identifier slot — so no shipped speculative candidate puts a contextual keyword in its FIXED prefix. The precondition ("sound only for grammars with no contextual / scope-resolvable kind in a speculative candidate's fixed prefix") is DOCUMENTED prominently at both `Parser::predictivePrefixPrunes` and `computePredictivePrefixes`. A clean conservative fix is deferred because no token-id-keyed "is this kind contextual" query exists today — the `contextual` flag is per-`LexemeMeaning` (lexeme-string-keyed); only the grammar-wide `reservedWordPolicy()` lever is exposed, which would NOT cover a per-keyword `contextual: true` under a Strict policy. | Add an agnostic token-id-keyed "is this SchemaTokenId a contextual/scope-resolvable kind" query (derive a `contextualKinds` set in the loader from the `contextual` LexemeMeaning flags + `reservedWordPolicy`), then make `predictivePrefixPrunes` CONSERVATIVE — skip the prune at any offset whose OBSERVED token has a contextual/scope-resolvable kind (do not constrain that offset). Must stay agnostic (no token/language naming) and preserve the deep-nest O(N) win for non-contextual grammars. Pin with a synthetic contextual-keyword speculative-alt schema whose candidate prefix admits Identifier at offset ≥1 + a soft-keyword input (RED-on-disable: the un-skipped prune drops the valid candidate). Priority: **normal (trigger-gated — first contextual-keyword speculative-alt consumer with a soft keyword in a candidate's fixed prefix).** | `D-PARSE-SPECULATION-OPERAND-QUADRATIC` (the prune this guards); `parser.cpp` `effectiveKind` / `peekSignificantKind`; `grammar_schema.hpp` `reservedWordPolicy` / `LexemeMeaning::contextual`; tsql `nameOrCall` |
| `D-PARSE-IDENTIFIER-KIND-HARDCODED-VS-CONFIG` | **OPEN (trigger-gated) — registered 2026-06-23 (cycle-13 pr-review LOW).** The parser (`parser.cpp:247` `identifierKind(schema->schemaTokens().find("Identifier"))`) and the tree-builder (`tree_builder.cpp` `identifierKind_`) resolve the identifier / contextual-demotion target via the LITERAL token name `"Identifier"`, NOT the live `semantics.identifierToken` config field — which the SEMANTIC + HIR layers DO read (`semantic_analyzer.cpp` `cfg.identifierToken`, `cst_to_hir.cpp` `sem.identifierToken`, 20+ sites). LATENT: all three shipped grammars (toy / c-subset / tsql) declare `"identifierToken": "Identifier"`, so the hardcoded string and the configured kind COINCIDE — no test diverges. A grammar that named its `identifierToken` something ELSE would split: name-resolution / HIR key on the configured kind while the parser/builder demotion keys on a non-existent `"Identifier"` token (invalid ⇒ contextual demotion silently no-ops, identifier-leaf handling mis-keys). PRE-EXISTING (predates the cycle-13 contextual-keyword fix, which merely REUSES the parser's existing `identifierKind`; surfaced by that cycle's independent review). | Thread `schema->semantics().identifierToken` into the parser's `identifierKind` (`parser.cpp:247`) + the builder's `identifierKind_` in place of `find("Identifier")` (keep them IDENTICAL for walker lockstep); pin with a synthetic schema whose `identifierToken` is named ≠ `"Identifier"` + a contextual-demotion / name-resolution case that RED-on-disables. Priority: **normal (trigger-gated — first shipped grammar whose identifier token is NOT named `"Identifier"`).** | `D-PARSE-PREDICTIVE-PRUNE-CONTEXTUAL-KEYWORD` (the cycle that surfaced it); `parser.cpp` `identifierKind`; `tree_builder.cpp` `identifierKind_`; `semantic_config.hpp` `identifierToken` |
| `D-PARSE-DEEP-NEST-RECURSION-MEMORY` | ✅ **CLOSED 2026-06-24 (branch feature/0-0-2-p14, HEAD 941d75f, 388/388 ctest MSVC Release + MinGW).** **FOLLOW-UP 2026-06-24 (feature/0-0-2-p16): MISSED SITE `prescanLabels` flattened.** The arc flattened lowerExpr / lowerStmt / Assign / Switch but MISSED a SEVENTH host-recursion site — the FC5 label prescan (`cst_to_hir.cpp` `prescanLabels`, run in the `lowerFunction` prologue BEFORE the flat `lowerStmt` driver). It recursed over the body CST (descending into expressions, where labels can never appear) and overflowed the host stack on a ~2000-deep right-assoc `a=a=…=a` body chain. LATENT because the close gate was MSVC **Release** + MinGW (smaller / 8 MiB frames fit) — only MSVC **Debug** (~1 MiB, larger frames) overflowed, and NO CI leg runs MSVC Debug (caught locally during the p16 deferral loop). FIX: converted to an explicit heap work-stack (children pushed reversed → popped left-to-right ⇒ byte-identical pre-order ⇒ identical label ordinals + S_DuplicateLabel order); `HirLoweringCSubset.DeepRightAssocAssignChainLowersFlatOnNormalStack` (already main's witness) now passes on MSVC Debug too; 394/394 Debug ctest. The stale lowerExpr comment claiming "Assign in lowerBinary DELEGATES to recursive helper UNCHANGED" is now also incorrect (the arc flattened Assign) — a separate doc residual, not load-bearing. *Original close note:* The full iterative expr+stmt+const-eval traversal LANDED across all six recursion families — the parser's 4 deepening expression arms (now the `exprWorkStack`/`driveExprWorkStack` driver; the paren/postfix arm is the §B-deferred Stage 5b residual, worker-bounded), ALL of cst_to_hir (expr+stmt+Assign+Switch), ALL of hir_to_mir (value+address+CFG+Call+stmt), semantic `subtreeType`+pass1/2, and BOTH const-evaluators are now explicit heap work-stacks → deep nesting is flat O(1) host-stack. `maxExpressionDepth` lifted 256→1024 CONFIG-DRIVEN (`P_ExpressionTooDeep` backstop KEPT — BC-1). See [plan 24](24-iterative-expression-traversal%20-%20tbd.md) (CLOSED). **SUPERSEDED below:** the "Convert the recursive-descent EXPRESSION parser… `parseExpressionAt`/`parsePrimary`/`parseUntilFrameDepth`" fix-direction + the `parser.cpp` `parseExpressionAt` cross-ref are now that iterative driver (stale symbol names, kept as the historical fix-sketch). — *Original (audit trail) — was 🟦 IN PROGRESS, staged build (cycle 14+, 2026-06-23). USER FIRED THE TRIGGER (4× sign-off, "execute as many cycles as needed"):* full iterative expr+stmt+const-eval traversal across SIX recursion families (parser / cst_to_hir expr+stmt / hir_to_mir expr+stmt / semantic / const-eval ×2), replacing host recursion with explicit heap work-stacks so deep nesting is flat O(N) host-stack. Plan-locked (REWORK→reworked) design at [plan 24](24-iterative-expression-traversal%20-%20tbd.md); backup branch `feature/0-0-2-p14-before-rework`. Acknowledged: NOT a crash fix (already mitigated by the 64 MiB worker), NO production consumer — a robustness goal. Cap is RAISED not removed (`P_ExpressionTooDeep` backstop kept). — *Original characterization (audit trail):* **OPEN (trigger-gated) — registered 2026-06-22 (residual deep-expr parse super-linearity characterization cycle).** A deeply-nested expression like `((((0))))` parses with a residual MILD WALL-CLOCK super-linearity (measured ~exp 1.7, Debug, cap=4000 on a large-stack thread: depth 250 ≈23 ms → 500 ≈71 ms → 1000 ≈240 ms → 2000 ≈873 ms → 3000 ≈1897 ms; ns/level rises 93k→632k). DISTINCT from `D-PARSE-SPECULATION-OPERAND-QUADRATIC` (the now-FIXED O(N²) cast-vs-paren panic-scan blowup — that made n=300 take 154 s; GONE, n=3000 is now 1.9 s); it is the SAME residual a sibling session flagged (matching ~exp-1.7). **ROOT CAUSE — decisively localized by a CONTROLLED experiment, not a code-trace:** deep `((((0))))` (host-recursion depth = N) vs a flat `0+0+…+0` left-assoc chain (recursion depth ~2, SAME O(N) node/token count, built ITERATIVELY by the Pratt climb). The FLAT chain is perfectly O(N) — ns/level FLAT at ~20.5 µs across 250→3000, node count exactly linear — while only the NESTED case grows. Both exercise the IDENTICAL shared machinery (TreeBuilder frame open/close, the node-arena `std::vector` GEOMETRIC growth, schema-walker advance), so the "TreeBuilder/arena bookkeeping" hypothesis from the QUADRATIC row is **REFUTED**. Exhaustive code reading confirms every per-level op on the direct-descent paren path is O(1) (operand AltChoice dispatch, `candidateBranches` over the fixed 6-branch set, the LL(k) `predictivePrefixPrunes` over the fixed prefix, `peekSignificantKind`, frame open/close, ALL `SchemaWalker` ops, `finish()` once) and the lone-survivor `parenExpr` DIRECT-DESCENDS with NO `SpeculationProbe`/checkpoint/snapshot (so the O(N²)-snapshot path is not taken). The residual is therefore NOT algorithmic (PROVEN: the flat control isolates the shared WORK as O(N)) — it is a constant-factor cost of the N-deep HOST recursion, attributed BY ELIMINATION (not directly cache-instrumented) to that recursion's memory-hierarchy behavior: the live C++ call stack ≈ N × {parseExpressionAt, parsePrimary, parseUntilFrameDepth, stepOnce, walkExpression} frames PLUS the strided unwind re-access of the depth-indexed `frames`/`frameRules`/`cursorStack`/`pendingChildren` vectors as the working set outgrows cache (the rising-not-constant local exponent ~1.6→1.9 is consistent with cache-tier transitions, not a clean algorithmic O(N²)). **BOUNDED / MOOT:** `ParserConfig::maxExpressionDepth` caps depth at 256 (`D-PARSE-EXPR-DEPTH-GUARD`; valid C nests ≤4 in the whole corpus; >256 FAILS LOUD via `P_ExpressionTooDeep`, never parses) AND the whole downstream frontend (semantic/HIR/MIR) is itself recursion-bound to the same 256 cap (`D-PARSE-DEEP-FRONTEND-STACK`) — so the residual only appears in a synthetic cap-raised micro-benchmark, never in production. Pinned by `ParserSpeculation.FlatChainParseWorkIsLinear` (the O(N)-WORK regression guard) + the existing `DeepNestCastVsParenIsLinear` (the deep-path O(N²)-regression guard, whose generous 8× bound is explained by this residual). | Convert the recursive-descent EXPRESSION parser (`parseExpressionAt`/`parsePrimary`/`parseUntilFrameDepth` + the `walkExpression` re-entry) to an explicit heap worklist/stack so deep nesting carries no host-recursion memory cost — AND, for any END-TO-END benefit, do the same for the recursion-bound downstream passes (`D-PARSE-DEEP-FRONTEND-STACK`'s semantic/HIR/MIR), since the 256 cap is set by THEM. Stay agnostic (no token/rule/language identity). Pin with a deep-nest wall-clock ratio the iterative form keeps ~linear. Priority: **normal (trigger-gated — fires only if a real consumer needs expressions nested beyond the 256 cap AND the cap is first lifted by an iterative frontend; NO in-cap benefit otherwise).** | `D-PARSE-SPECULATION-OPERAND-QUADRATIC` (the O(N²) sibling this is the residual OF); `D-PARSE-DEEP-FRONTEND-STACK` (the downstream recursion bound that sets the 256 cap); `D-PARSE-EXPR-DEPTH-GUARD` (the cap); `parser.cpp` `parseExpressionAt`; tests `FlatChainParseWorkIsLinear` + `DeepNestCastVsParenIsLinear` |
| `D-LAYOUT-COMPUTELAYOUT-TYPE-NODE-RECURSION` | **OPEN (trigger-gated) — registered 2026-06-24 (plan-24 close-out).** An ORTHOGONAL host-recursion family OUT of the plan-24 expression/statement arc's scope (that arc flattened expr/stmt/const-eval, NOT type-node layout): `computeLayout` (`src/core/types/type_lattice/type_layout.cpp`) recurses ONCE PER ARRAY DIMENSION (`int a[N][M][K]` → 3 nested `computeLayout(ops[0], …)` calls) and once per struct/union FIELD, on the host stack — a ~2300-level host-stack ceiling (plan-24 family C-2, "type-node/declarator recursion"). NOT reached by deep EXPRESSIONS (those are now flat); a deeply-nested aggregate TYPE (many array dimensions or deeply-nested struct fields) is the only producer. No current consumer nests aggregate types that deep (the corpus aggregates are shallow), so it stays a latent host-recursion limit. | Convert `computeLayout`'s per-dimension / per-field recursion to an explicit heap work-stack so a deeply-nested aggregate type lays out flat O(N) host-stack (mirror the plan-24 expr/stmt iterative-driver idiom). Stay agnostic (pure layout arithmetic over `TypeInterner`; names no language/target/format). Priority: **normal (trigger-gated — fires only if a real consumer declares an aggregate type nested past the ~2300 host-stack ceiling).** | plan 24 (the expr/stmt iterative-traversal arc, family C-2 = this out-of-scope sibling); `D-PARSE-DEEP-NEST-RECURSION-MEMORY` (the in-scope expr/stmt sibling); `type_layout.cpp` `computeLayout` |
| `D-FC12A-VARIADIC-CALLEE` | ✅ **SHIPPED 2026-06-19 (FC12a-core).** x86_64 System V variadic CALLEE support — `va_start`/`va_arg`/`va_end` for SCALAR types, config-driven + source/target/linker-AGNOSTIC. The SysV facts live in a NEW `vaListLayout` block on the calling convention (`target_schema.hpp` `VaListLayout` + `x86_64.target.json` `sysv_amd64`), read by a bounded per-CC algorithm — never an arch/cc/format identity branch (mirrors `aggregateClassification`/`variadicVectorCountReg`). `va_arg`/`va_start`/`va_end` are DEDICATED HIR nodes mirroring `SizeOf` (the type-arg rides a TypeRef child never value-lowered). HIR→MIR lowers va_start to 4 field Stores + the two frame-address leaves `VaRegSaveAreaAddr`/`VaOverflowArgAreaAddr`, va_arg to the reg-vs-overflow diamond (Load gp/fp_offset → ICmpUlt limit → CondBr → Phi → Load), va_end to nothing. LIR callconv reserves the register-save-area + spills the arg regs in the prologue (gated on the function containing a `va_reg_save_area` op = called va_start). FAIL-LOUD: a CC without `vaListLayout` → loud "variadic callee unsupported"; `va_arg` of a struct/union STAYS fail-loud (the FC12a-struct follow-on). **(UPDATE 2026-06-19: the InRegisters ≤16B struct path — caller vararg / `va_arg(struct)` eightbyte gather / fixed struct param — SHIPPED in FC12a-struct; only the >16B MEMORY-class + register/stack-split shapes remain fail-loud — see `D-FC12A-VARIADIC-MEMORY-CLASS-STRUCT`.)** ALSO closed a pre-existing gap: a variadic function DEFINITION (`T f(int n, ...) {…}`) now builds a variadic FnSig (declarator-level `variadicMarker` in the SHARED `applyDeclaratorSuffix` — the legacy decl-only path missed definitions + fn-ptr types). Run-witnessed on ELF x86_64: `examples/c-subset/varargs_sum` (exit 100, GPR reg path) + `varargs_double` (exit 50, SSE reg path) + `varargs_overflow` (exit 55, the OVERFLOW read path — >5 varargs spill from `overflow_arg_area`). A variadic callee whose FIXED params overflow to the stack fails loud (deferred `D-FC12A-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`). | (shipped) | `D-FC12A-VARIADIC-AL-GATED-SPILL`; `D-FC12A-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`; `D-FC7-STRUCT-BY-VALUE-ARG-RETURN`; `D-LANG-VARIADIC`; plan 23 FC12 |
| `D-FC12A-VARIADIC-AL-GATED-SPILL` | The variadic-callee prologue currently spills all `fpSaveCount` SSE arg registers UNCONDITIONALLY into the register-save-area (byte-correct — va_arg only reads fp slots the source actually consumed, so caller-garbage in unused slots is never observed). SysV §3.5.7 permits skipping the SSE block when `al == 0` (no float varargs) — a SIZE optimization (saves 8×16=128B of stores) that needs a CONDITIONAL BRANCH (`test al; je skip; movaps…; skip:`) in the prologue, which the otherwise straight-line ML7 callconv pass does not currently emit. Trigger: the callconv pass gains conditional-branch/cmp emission, OR a profile shows the 128-byte spill matters. NOT a correctness gap — varargs work without it. | Add an al-test + conditional skip block around the SSE spill in `emitVariadicRegSaveSpill` (needs cmp/jcc emission + entry-block split, mirroring the variadic-CALL al-count emission shape). Pin with a `jcc`-presence assertion in the callconv prologue test. | `D-FC12A-VARIADIC-CALLEE`; `D-TARGET-SUBREGISTER-ALIASES` (the `al` low-byte alias the test would read) |
| `D-FC12A-VARIADIC-OVERFLOW-FIXED-STACK-ARGS` | ✅ **CLOSED 2026-06-20 (FC12-deferral④, SCALAR case).** A SysV x86_64 variadic CALLEE whose FIXED scalar params overflow the 6 integer / 8 SSE arg registers onto the incoming stack now LOWERS. va_start bakes the fixed-stack-arg byte displacement `(gprOver + fprOver) * gpSlotBytes` (each overflowed fixed param = ONE 8B incoming slot, gpSlotBytes for BOTH classes) into the `VaOverflowArgAreaAddr` `uint32_t` PAYLOAD (`hir_to_mir.cpp` lowerVaStart SysVRegisterSave arm — mirrors the `VaHomeArgAreaAddr` precedent); threaded through a DEDICATED `lowerVaOverflowArgArea` in `mir_to_lir.cpp` (the generic `lowerVaFrameAddr` hardcoded payload 0 → would discard it; `VaRegSaveAreaAddr` stays on the payload-0 path); `lir_callconv.cpp` adds it to the overflow base `totalFrameSize + callPushBytes + shadowSpaceBytes + fixedStackBytes`. `gp_offset = fixedGpr*gpSlotBytes` stays uncapped (a >limit cursor routes va_arg to the overflow arm via the unsigned ICmpUlt diamond). The scalar fail-loud lifted; `VaStartFixedParamsOverflowToStackFailsLoud` INVERTED to `...Succeeds` (pins the payload VALUE == 8). LIR congruence pin `SysVVaStartFixedStackOverflowBaseCongruence` (the materialized `lea` MemOffset == base + 8, red-on-disable: the bare base appears if the payload is dropped). The AGGREGATE-straddle sub-case (a multi-slot by-value struct fixed param straddling the register/stack boundary, which the per-slot formula undercounts) stays fail-loud — `D-FC12-VARIADIC-OVERFLOW-FIXED-AGGREGATE-STACK-ARGS`. Run-witnessed ELF x86_64 (WSL native): `examples/c-subset/varargs_overflow_fixed_stack` (7 fixed ints, `g` overflows, exit 108; red-on-zero-displacement → 15). | (closed) | `D-FC12-VARIADIC-OVERFLOW-FIXED-AGGREGATE-STACK-ARGS`; `D-FC12A-VARIADIC-CALLEE`; `D-FC12C-AAPCS64-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`; plan 23 FC12 |
| `D-FC12-VARIADIC-OVERFLOW-FIXED-AGGREGATE-STACK-ARGS` | ✅ **CLOSED 2026-06-22 (p14, Item 3 deferral 1) — the COMPLETE all-or-nothing fix, NOT the registry's old va_start-only patch.** The plan-lock found the deferral was BIGGER than framed: a straddling fixed aggregate was SILENTLY SPLIT (a non-conformant register/stack split) by BOTH the caller AND the callee, masked by this fail-loud guard — affecting VARIADIC **and** NON-variadic calls (the latter an unfiled latent silent miscompile this cycle ALSO repaired). As built: **(A caller, `hir_to_mir.cpp` Call-arg loop)** the register-exhaustion check HOISTED out of the `if (calleeVariadic)` gate to run for EVERY call — a straddling InRegisters aggregate routes WHOLE to the `ByValueStackArg` carrier (never per-eightbyte split). **(B callee, NEW `RecvByValueStackParam` MIR op)** the symmetric mirror of `ByValueStackArg`: `receiveByValueParam` byte-copies the whole aggregate from the incoming overflow area (consuming ZERO arg registers) into the param slot; the op lowers (`mir_to_lir.cpp` + both target schemas' virtual opcode) to the SAME `lea [sp + totalFrameSize + callPushBytes + shadowSpaceBytes + payload]` as `va_overflow_arg_area` but is a DISTINCT handle (it occurs in non-variadic fns — reusing a va_* op would falsely trip `functionUsesVaStart`'s prologue-spill). **(C va_start, `hir_to_mir.cpp`)** the old `(gprOver+fprOver)*gpSlotBytes` slot-count REPLACED by an exact incoming-stack-byte cursor `currentFnFixedStackBytes_` accumulated in param order (stacked scalar += slot; stacked aggregate += roundUp(size, slot)); the 2 fail-loud guards + the now-dead `currentFnHasAggregateFixedParam_` removed. **★ MF-B REFINEMENT (modern-compiler comparison, the user's ask):** the plan-lock's "clamp the cursor on stack-route" CONFLATED two DIVERGENT ABIs — SysV BACKFILLS (gcc `function_arg_advance_64` leaves `cum->nregs`) vs AAPCS64 EXHAUSTS the overflowed class (gcc `aarch64_layout_arg` sets `aapcs_nextncrn=8`). Clamping for SysV would be a silent ABI miscompile. Resolved AGNOSTICALLY via a NEW config flag `aggregateStackExhaustsRegisters` (`TargetCallingConvention` + loader + x86_64=false / arm64=true), read by BOTH caller + callee cursor handling + flowing into va_start's `__gr_offs`/`__vr_offs` clamp. **Tests:** the 2 fail-loud pins INVERTED to `...Succeeds` (SysV payload==16 + gp_offset==40 backfill witness; AAPCS64 payload==24 + __vr_offs==0 exhaust + no -32) + a CALLER placement pin (`CallerNonVariadicStraddlingAggregateUsesStackCarrier`: ByValueStackArg + RecvByValueStackParam, red-on-disable revert Phase A) + the inlining gate REFUSES a callee with a `RecvByValueStackParam` (frame-sensitive, like `ReadIndirectResult`). **Runtime corpus:** `examples/c-subset/varargs_struct_straddle_fixed` (SysV native, exit 131) + `..._aapcs64` (qemu, exit 161), both under the shipped release pipeline (Inlining ON). **2 named fail-loud residuals (NO silent miscompile, rare multi-overflow shapes outside scope, cite the same anchor):** a stacked fixed scalar AFTER a stacked aggregate (the post-clamp/post-stack per-class ordinal can't site it), and a stacked aggregate preceded by another stacked fixed param (the per-class-ordinal scalar siting desyncs from the byte cursor). | (closed) | `D-FC12A-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`; `D-FC12C-AAPCS64-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`; `D-FC12A-VARIADIC-MEMORY-CLASS-STRUCT`; plan 23 FC12 |
| `D-FC12A-VARIADIC-MEMORY-CLASS-STRUCT` | ✅ **SHIPPED 2026-06-20 (FC12a-struct-memory).** SysV x86_64 struct-by-value at the variadic boundary for the MEMORY-class (>16B) + register-exhaustion-split shapes. **§B (user 2026-06-20): Option C — a first-class BY-VALUE-AGGREGATE STACK-ARG carrier** (a prior "push eightbytes as ordinary operands" design was REJECTED — an audit proved it a SILENT MISCOMPILE: the operands land in FREE registers, not the stack). SysV §3.2.3/§3.5.7 requires a MEMORY-class aggregate AND a register-exhaustion-split InRegisters aggregate to be passed ENTIRELY in the outgoing overflow (stack) area UNCONDITIONALLY (even when arg registers are free), never split. The greedy register-then-overflow callconv had no per-arg force-to-stack lever, so the carrier IS the lever: a NEW MIR op `ByValueStackArg` (operand 0 = the `freshAggregateTemp` copy address via `lowerByteWiseCopy`, same as the by-ref arg; payload = aggregate byte size; result = pointer, threaded through) sits as ONE Call operand at the aggregate's left-to-right position. `mir_to_lir` lowers it to a `Reg` (the address — kept live by liveness/regalloc) immediately followed by a NEW `LirOperandKind::ByValueStackAgg` size marker (mirroring the leading-Reg + trailing-descriptor memory-operand convention). `lir_callconv` (BOTH `computeMaxOutgoingStackArgs` pre-scan AND the materialize placement loop, via the shared `byValueStackAggSlots` helper) places it at the next overflow offset(s) — reserving ceil(size/outgoingSlot) slots + byte-copying the temp into `[sp + shadowSpaceBytes + overflowIdx*outgoingSlotSize]` (caller-saved scratch GPR avoiding every carrier addr reg + the indirect callee + the count reg + every pending arg-move / FP-dup SOURCE register — the silent-arg-clobber avoid-set, the review-caught BLOCKER-3 fold: the byte-copies run BEFORE the register arg-moves read their sources, so the scratch must not be a live arg source; a blanket argGpr exclusion was REJECTED as over-broad — a dest-only argGpr is written by its own arg-move after the copy, so its transient clobber is dead). It consumes NO arg register (runGpr/runFpr NOT advanced; class counters NOT advanced) and does NOT inflate the SSE/AL count (the GPR-class address Reg fails the FPR test; the marker is non-Reg). CC-NEUTRAL by construction (the byte size is the only datum; the overflow geometry is config-driven) so the Win64 + AAPCS64 struct-vararg cycles REUSE this carrier. Caller routing (`hir_to_mir.cpp` Call arg loop): MEMORY-class ByReference → carrier; register-exhaustion split (`runGpr+numGp > gpSaveCount || runFpr+numFp > fpSaveCount`) → WHOLE struct to carrier. Callee `va_arg(>16B)` (`lowerVaArgAggregate`): an EARLY ByReference dispatch → the overflow-ONLY arm (addr = overflow_arg_area, bump roundUp(size, gpSlotBytes)), never the register-gather diamond (numGp=numFp=0 would make the atomic-fit trivially-true → gather ZERO pieces). Wall-3 (fixed >16B struct param in a variadic fn): the fail-loud removed — `receiveByValueParam`'s ByReference arm receives the hidden pointer as 1 GPR arg (counted in `currentFnFixedGpr_`), so the common case (1 GPR + ≤5 scalars ≤ 6) lowers cleanly. The 4 fail-loud pins inverted to SUCCESS + structural pins (carrier present; va_arg overflow dispatch = no ICmpUle pair; the BLOCKER-1 force-to-stack + BLOCKER-2 outgoing-area sizing LIR pins in `test_lir_callconv.cpp`, both red-on-disable verified). Run-witnessed ELF x86_64 (WSL native): `examples/c-subset/varargs_struct_memory_class` (24B MEMORY-class, exit 60), `varargs_struct_split` (register-exhaustion split, exit 60), `varargs_struct_oddsize` (20B → roundUp(20,8)=24 tail bump, exit 66). REMAINING (own cycles, REUSE this carrier): Win64 `D-FC12B-WIN64-STRUCT-VARARG`, AAPCS64 `D-FC12C-AAPCS64-HFA-STRUCT-VARARG`; the SEPARATE `D-FC12A-VARIADIC-OVERFLOW-FIXED-STACK-ARGS` (fixed params overflowing to the stack) stays OPEN. | (shipped) | `D-FC12A-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`; `D-FC12B-WIN64-STRUCT-VARARG`; `D-FC12C-AAPCS64-HFA-STRUCT-VARARG`; `D-FC7-STRUCT-BY-VALUE-ARG-RETURN`; `D-FC12A-VARIADIC-CALLEE` |
| `D-FC12B-WIN64-VARIADIC-CALLEE` | ✅ **SHIPPED 2026-06-19 (FC12b).** **§B (user 2026-06-19): Option A — a closed `VaListStrategy` enum with the va_list TYPE DERIVED from the strategy, chosen over Option D (an explicit per-CC `vaListType` config field); derived avoids an unused-flexibility knob that must be kept consistent with the strategy. (Rejected extremes: a va_arg recipe-DSL, and a unified ptr-to-opaque-blob va_list — both break the bar.)** Microsoft x64 (ms_x64) variadic CALLEE support — `va_start`/`va_arg`/`va_end` for SCALAR + FP types, config-driven + source/target/linker-AGNOSTIC. The per-CC va_list TYPE + lowering STRATEGY is a NEW closed enum `VaListStrategy` (`aggregate_layout.hpp`, beside `AggregateClassKind`/`BitFieldStrategy` so the SEMANTIC va_list-type injection reads it without pulling the link/target substrate): `SysVRegisterSave` (FC12a, unchanged-but-now-strategy-tagged), `HomogeneousPointer` (Win64, this cycle), `Aapcs64DualCursor` (declared, fail-loud-until-FC12c). `VaListLayout` gains `strategy` (first field) + `namedArgSlotBytes`; `x86_64.target.json` ms_x64 declares `{"strategy":"homogeneous_pointer","namedArgSlotBytes":8}`. **BLOCKER-2 (va_list TYPE per ABI):** `analyze()` gains an `optional<VaListStrategy>` param threaded from the resolved CC (pipeline reads it where it reads `aggregateLayout`); the sema injection switches on it — SysV → `__va_list_tag[1]` (24B), Win64 → `char*` (8B), AAPCS64 → loud `S_VariadicCalleeUnsupported`. A wrong sizeof(va_list) would mis-size the `ap` local + corrupt the stack — pinned `sizeof(va_list)`==8 ms_x64 / 24 sysv. **BLOCKER-1 (no-shadow contiguous base):** Win64's named-arg HOME space (rcx/rdx/r8/r9 slots = the caller's shadow space) and the 5th+ stack args are CONTIGUOUS from `totalFrameSize + callPushBytes` (NO shadowSpaceBytes — adding it would skip the home and read past every named arg). A NEW MIR leaf `VaHomeArgAreaAddr` (payload = the slot-aligned named-arg count) + LIR `va_home_arg_area` op materializes to `lea [sp + totalFrameSize + callPushBytes + payload*outgoingSlotSize]`; the Win64 prologue spills the named int regs into the SAME base (spill-target == va_arg-read-target). HIR→MIR lowers va_start (Win64) to `Store(VaHomeArgAreaAddr, ap)`, va_arg to a LINEAR pointer bump (NO diamond: Load ap → Load T → Gep +namedArgSlotBytes → Store ap), va_end to nothing. **FP-dup (PART 5):** Win64 passes each FP vararg in BOTH the xmm AND the matching integer (home) reg; the callee reads va_arg(double) from the home GPR slot, so the CALLER duplicates each register-resident FP VARARG (operand past `fixedOperandCount`) into argGprs[slot] via a NEW dedicated opcode `movq_xmm_to_gpr` (`66 REX.W 0F 7E /r`; byte-pin `movq rdx, xmm1` = `66 48 0F 7E CA`). A dedicated opcode (not `movq_xclass`) because the encoding-variant guard keys only on (operandKinds, width) and cannot disambiguate the two MOVQ directions, and `movq_xclass` is consumed bidirectionally by Bitcast — overloading would force a silent miscompile on GPR→FPR Bitcast. Every va seam dispatches on `VaListStrategy` with a fail-loud default (no half-migrated site silently runs the SysV path on a Win64 layout). Run-witnessed on PE x86_64 (this Windows host): `examples/c-subset/varargs_win64_sum` (int+double varargs, anti-folded via a mutable-global seed). FAIL-LOUD: Win64 struct-by-value varargs (`D-FC12B-WIN64-STRUCT-VARARG`); Aapcs64 at every seam (FC12c). | (shipped) | `D-FC12B-WIN64-STRUCT-VARARG`; `D-FC12A-VARIADIC-CALLEE`; `D-FC7-STRUCT-BY-VALUE-ARG-RETURN`; `D-LANG-VARIADIC`; plan 23 FC12 |
| `D-FC12C-AAPCS64-VARIADIC-CALLEE` | ✅ **SHIPPED 2026-06-19 (FC12c).** AAPCS64 (ARM64-ELF) variadic CALLEE support — `va_start`/`va_arg`/`va_end` for SCALAR int+fp, config-driven + source/target/linker-AGNOSTIC, the THIRD `VaListStrategy` (`Aapcs64DualCursor`, beside SysVRegisterSave + HomogeneousPointer). `va_list = __va_list`, a typedef to the 5-field STRUCT DIRECTLY (NOT an array like SysV, NOT a pointer like Win64): `{void* __stack; void* __gr_top; void* __vr_top; int __gr_offs; int __vr_offs;}` (32B; sizeof pinned). Config = a `vaListLayout` block on `aapcs64` with the 5 field locators + GR/VR save geometry (gpSaveCount8/gpSlotBytes8/fpSaveCount8/fpSlotBytes16, regSaveAreaBytes()=192); the semantic injection + isVaList accept the struct shape for the strategy. **BLOCKER-1:** a NEW `fstur_q` opcode (STUR Qt,[Xn,#simm9], 128-bit, fixedWord `0x3C800000` — NOT `0x3C000000`=STURB which would spill 1 byte/FP slot → va_arg(double) garbage; byte-pin STUR Q0,[X1,#0]=`0x3C800020` + a disasm Imm9/MemBaseNoScale round-trip arm). The AAPCS64 prologue spills x0..x7 (GR block, 8B via the class GPR store) then v0..v7 (VR block, 16B via `fstur_q`) into the callee-local register-save-area at `vaRegSaveAreaOffset()`, ascending order. va_start inits the 5 fields: __gr_top=rsa+64, __vr_top=rsa+192, __stack=overflow base, __gr_offs=-(8-fixedGpr)*8, __vr_offs=-(8-fixedFpr)*16 (NEGATIVE i32 cursors). **BLOCKER-2:** va_arg's per-class diamond — Load the NEGATIVE class cursor, `ICmpSlt(offs,0)` (a reg slot remains while <0), reg arm: **SIGN-EXTEND the i32 cursor to i64 (SExt) BEFORE the byte Gep** (a 32-bit -40 zero-extended → 0xFFFFFFD8 ≈ +4 GiB wild addr → segfault), addr = `<gr|vr>_top + SExt(offs)`, bump by the slot stride; stack arm: addr = __stack, bump by gpSlotBytes=8 (the NSAA round-up quantum, even for a double); Phi the two → Load T. va_end = no-op. Every va seam dispatches on `VaListStrategy` with a fail-loud default. The callee reg-save-area zone is gated on strategy ∈ {SysVRegisterSave, Aapcs64DualCursor}. Run-witnessed on ELF aarch64 (qemu): `examples/c-subset/varargs_aapcs64_sum` / `_double` / `_overflow` / `_double_overflow` (the FP `va_arg(double)` __stack overflow arm — 9 doubles, the 9th from __stack, exit 60) / `_sret` (the variadic + >16B-struct-return COMPOSITION, exit 126). **Adversarial-review FOLD (2026-06-19):** the prologue save-area scratch-base picker (`add <scratch>, sp, #off`) excluded only the arg GPRs and so landed on **x8** — but on AAPCS64 x8 is `indirectResultRegister` (the incoming sret pointer); a variadic fn ALSO returning a >16B struct had its x8 sret pointer clobbered BEFORE `read_indirect_result` reads it (silent miscompile — the struct written to the callee's own frame). FIX: add `cc.indirectResultRegister`'s ordinal to the scratch avoid-set (→ x9, genuinely free). Structural pin `Aapcs64VariadicStructReturnPrologueScratchAvoidsX8` (red-on-disable verified: revert → scratch=x8) + the `_sret` qemu witness (red-on-disable: scratch=x8 → exit 0, not 126). **Also fixed a latent SHARED-regalloc bug surfaced by the FP-overflow witness:** `pickScratchRegs` (`lir_rewrite.cpp`) keyed its register-in-use mask by the GLOBAL register-table ordinal in a uint64 bitmask, but arm64's 33 GPR table slots push d31 to global ordinal 64 — past the mask → a fail-loud `out of 64-bit bound` on any function spilling an FPR up to d31 (the 9-double case). Re-keyed to an unbounded per-class `unordered_set` of global ordinals, mirroring `buildFreeLists` (lir_regalloc.cpp). Pin `LirRewrite.Arm64HighFprSpillScratchPoolHandlesOrdinalsBeyond64`. FAIL-LOUD: struct/union va_arg + struct-vararg-to-AAPCS64-variadic (`D-FC12C-AAPCS64-HFA-STRUCT-VARARG`). | (shipped) | `D-FC12C-AAPCS64-HFA-STRUCT-VARARG`; `D-FC12C-APPLE-ARM64-VARIADIC-CALLEE`; `D-FC12A-VARIADIC-CALLEE`; `D-FC12B-WIN64-VARIADIC-CALLEE`; `D-FF3-APPLE-ARM64-ABI-DIVERGENCE`; plan 23 FC12 |
| `D-FC12C-APPLE-ARM64-VARIADIC-CALLEE` | ✅ **CLOSED 2026-06-21 (FC12c) — runtime WITNESSED GREEN on the macos-15-arm64 leg.** Apple arm64 variadic CALLEE — realizes the variadic half of `D-FF3-APPLE-ARM64-ABI-DIVERGENCE`. Apple passes ALL variadic args on the stack (vs AAPCS64's register-then-stack), so `va_list` is a PLAIN POINTER (the existing `HomogeneousPointer` strategy, like Win64) that linearly bumps over the contiguous overflow area — NO register-save-area, NO dual cursor. Two NEW config flags: `variadicArgsAlwaysStack` (on the CC) forces the CALLER to stack every vararg past `fixedOperandCount` regardless of free arg registers (named args stay register-placed; gated on the flag + isVariadic so AAPCS64 + x86 CCs are unaffected — the outgoing-arg-area pre-scan mirrors the forcing so the area is sized right); `variadicUsesOverflowBase` (on `vaListLayout`) makes va_start anchor `ap` at the OVERFLOW base (`VaOverflowArgAreaAddr`) rather than the home base (Apple has no home area — named args stay in their registers, read via SSA). `apple_arm64` CC declares `{strategy:homogeneous_pointer, namedArgSlotBytes:8, variadicUsesOverflowBase:true}` + `variadicArgsAlwaysStack:true`. The prologue home-spill is gated `!variadicUsesOverflowBase` (Apple spills nothing); the callee reg-save-area zone stays 0 (HomogeneousPointer). `functionUsesVaStart` gained a THIRD op handle (`va_overflow_arg_area`, REUSED — not a new `va_stack_arg_area` op) so Apple's va_start is detected. Congruence: the va_start overflow-base lea (== totalFrameSize, no shadow/callPush on arm64) == the caller's first-vararg stack-store offset — pinned. Compiles on Windows (structural witness `examples/c-subset/varargs_apple_sum`, Mach-O `arm64:macho64-arm64-darwin-exec`); **RAN GREEN on the macos-15-arm64 CI leg at HEAD — `varargs_apple_sum` + `varargs_apple_struct` executed NATIVELY with their asserted exits (run 27891928277, 2026-06-21) → §A.5 cross-target runtime closure satisfied.** | (✅ closed — runtime witnessed, macos leg run 27891928277) | `D-FC12C-AAPCS64-VARIADIC-CALLEE`; `D-FF3-APPLE-ARM64-ABI-DIVERGENCE`; `D-FFI-DESCRIPTOR-VARIADIC-SIGNATURE`; plan 23 FC12 |
| `D-FC12C-AAPCS64-HFA-STRUCT-VARARG` | ✅ **CLOSED 2026-06-20 (FC12c-struct).** Shipped the AAPCS64 HFA dual-cursor struct `va_arg` gather (HFA → N VR pieces via `__vr_offs`/`__vr_top` @16B stride; ≤16B non-HFA → ceil(size/8) GR pieces via `__gr_offs`/`__gr_top` @8B stride; >16B → by-reference pointer deref'd from ONE GR slot; per-class atomic-fit `ICmpSlt(Add(cur_i32, N*slot), 0)` reg-vs-`__stack`; **SXTW the NEGATIVE i32 cursor to i64 before EVERY Gep** [the FC12c segfault hazard], cursor arithmetic stays i32, the SExt'd i64 never stored back; overflow arm bumps `__stack` by roundUp(size, gpSlotBytes=8); mixed-class fail-loud) + the CALLER struct-vararg placement (ByReference → `appendByValueArg` = a 1-GR-slot hidden pointer, NOT the carrier [hazard H7]; InRegisters → per-class atomic-fit, register pieces or the CC-neutral Option-C `ByValueStackArg` carrier on register exhaustion) + the **Apple Step-2.0 size-aware HomogeneousPointer bump** (slot advance = roundUp(size, namedArgSlotBytes) for InRegisters / namedArgSlotBytes for ByReference, keyed on `abi->kind` not strategy — fixed the latent Apple 16B-HFA under-bump that re-read the 2nd va_arg; Win64 byte-unchanged since its InRegisters structs are pow2 ≤8B). Agnostic: every seam dispatches on `VaListStrategy` + `AbiPassing::Kind` + `isHfa` (from piece classes) + config — zero identity branches. Run-witnessed: ELF `examples/c-subset/varargs_aapcs64_struct` (qemu-aarch64, exit 135, incl. the HFA-overflow `__stack` arm = FOLD 2) + Mach-O `examples/c-subset/varargs_apple_struct` (exit 74, the 16B-HFA-2nd-element re-read witness — macos-latest-gated runtime). Host-independent red-on-disable pins: H1 SXTW present, H2 VR-not-GR (`countLoadsAtFieldOffset` @28>0 / @24==0), H5 by-ref reg-arm deref (`countRegArmTopDeref`), H7 caller-no-carrier, the Apple bump==16 (`funcHasConstInt(16)` with Win64 `funcHasConstInt(8)` staying green), + the 2 inverted fail-loud pins. 345/345 ctest; independent design-audit (Step 3.5) + independent adversarial review + fold + self-audit. _Original deferral below._ arm64 STRUCT-by-value va_arg (the AAPCS64 HFA dual-cursor gather + Apple struct va_arg) FAILS LOUD this cycle — a genuinely separate sub-feature: the HFA piece-distribution across `__vr_offs` (each FP leaf consumes one VR slot) + the ≤16B non-HFA 2-GPR gather + the >16B by-reference indirection are materially distinct from the SCALAR diamond FC12c ships, mirroring the SysV (`D-FC12A-VARIADIC-MEMORY-CLASS-STRUCT`) + Win64 (`D-FC12B-WIN64-STRUCT-VARARG`) struct-vararg deferrals + the user-endorsed core→struct cadence. Pinned red-on-disable: (1) `lowerVaArgAggregate` Aapcs64DualCursor arm fails loud (`hir_to_mir.cpp`); (2) the caller struct-vararg path (Call arg loop, InRegisters+variadic, Aapcs64DualCursor guard) fails loud. Trigger: a struct/union va_arg lowered under Aapcs64DualCursor, OR a struct vararg to an arm64 variadic callee. | ✅ DONE — implemented exactly as planned (closing details in the description column). Plan was: Add the AAPCS64 HFA dual-cursor struct gather: classify the aggregate (HFA → N VR pieces via __vr_offs/__vr_top; ≤16B non-HFA → ceil(size/8) GR pieces via __gr_offs/__gr_top; >16B → by-reference pointer in one GR slot), gathering each piece from the right class's save area into a temp (the result address) with the atomic-fit reg-vs-stack decision, mirroring the SysV eightbyte gather but with the AAPCS64 negative-cursor geometry + HFA classification. Witness with an arm64-ELF (qemu) + macho struct-vararg corpus. | `D-FC12C-AAPCS64-VARIADIC-CALLEE`; `D-FC12A-VARIADIC-MEMORY-CLASS-STRUCT`; `D-FC12B-WIN64-STRUCT-VARARG`; `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` |
| `D-FC12C-AAPCS64-VARIADIC-OVERFLOW-FIXED-STACK-ARGS` | ✅ **CLOSED 2026-06-20 (FC12-deferral④, SCALAR case) — logic complete + structurally pinned; RUNTIME corpus now WITNESSED (qemu-aarch64 exit 210, 2026-06-21, after `D-ASM-AARCH64-LARGE-FRAME-IMM12` was closed by the imm12 cycle) — FULLY CLOSED incl. the §A.5 runtime witness.** An AAPCS64 variadic CALLEE whose FIXED scalar params overflow the 8 GR / 8 VR arg registers onto the stack now LOWERS. va_start (`hir_to_mir.cpp` lowerVaStart Aapcs64DualCursor arm): (1) bakes the fixed-stack-arg displacement `(gprOver + fprOver) * gpSlotBytes` into the `VaOverflowArgAreaAddr` payload (→ `__stack`, threaded end-to-end like the SysV arm); (2) CLAMPS `__gr_offs`/`__vr_offs` to 0 when a class is fully consumed (`fixedGpr >= gpSaveCount ? 0 : -(gpSaveCount - fixedGpr)*gpSlotBytes`) — the old `-(saveCount - fixedReg)` underflowed unsigned; cursor 0 routes that class's varargs to `__stack` (va_arg tests `ICmpSlt(offs,0)`). The scalar fail-loud lifted; `Aapcs64VaStartFixedParamsOverflowToStackFailsLoud` + `...FixedDoubleParams...FailsLoud` INVERTED to `...Succeeds` (payload VALUE == 8 + the cursor-clamp witness: __gr_offs/__vr_offs Const==0, no-negative-const for the fully-consumed class) + a new `Aapcs64VaStartMixedClassFixedStackOverflow` (9 int + 9 double → payload 16, both clamps). LIR congruence pin `Aapcs64VaStartFixedStackOverflowBaseCongruence` (the `__stack` `lea` MemOffset == base + 8). The AGGREGATE-straddle sub-case stays fail-loud (`D-FC12-VARIADIC-OVERFLOW-FIXED-AGGREGATE-STACK-ARGS`). **RUNTIME WITNESS GAP:** a ≥9-fixed-param AAPCS64 variadic callee has a 192B GR+VR register-save-area + the dual-cursor va_arg reg-arm reads, which inherently address high in the frame; the assembler's unscaled `imm9` (-256..255) load/store form overflows (offset ≥ 288 even for the leanest 9-param body) → `A_ImmediateOperandOutOfRange`. This is the PRE-EXISTING `D-ASM-AARCH64-LARGE-FRAME-IMM12` gap, PROVEN orthogonal (the identical 9-fixed-param source compiles cleanly for x86_64). The SysV runtime corpus (`varargs_overflow_fixed_stack`, exit 108) witnesses the feature end-to-end; the AAPCS64 displacement+clamp is fully pinned by the structural MIR + LIR pins (every CI leg). The arm64 RUNTIME corpus is deferred behind the imm12 gap. | (logic closed; runtime corpus deferred behind `D-ASM-AARCH64-LARGE-FRAME-IMM12`) | `D-ASM-AARCH64-LARGE-FRAME-IMM12`; `D-FC12-VARIADIC-OVERFLOW-FIXED-AGGREGATE-STACK-ARGS`; `D-FC12A-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`; `D-FC12C-AAPCS64-VARIADIC-CALLEE`; plan 23 FC12 |
| `D-ASM-AARCH64-LARGE-FRAME-IMM12` | ✅ **CLOSED 2026-06-21 (imm12 cycle).** Shipped the AArch64 imm12-scaled unsigned-offset LDR/STR form: a NEW `EncodingSlotKind::Imm12Scaled` (encodes byteOffset/accessSize at bits 10..21) + new `load_u`/`store_u` opcodes in `arm64.target.json` (fixed words 0xF9400000/0xB9400000/0x79400000/0x39400000 + store peers) + the fixed32 encoder scaling (fail-loud on negative / non-access-size-aligned / >4095) + the disasm windows (Imm12 + Imm12Scaled) + a LOWERING-TIME selection in `lir_callconv.cpp` (offset≥0 && %accessBytes==0 && /accessBytes≤4095 && >imm9 → `load_u`, else imm9 `load`, else fail-loud) — AGNOSTIC: pure arithmetic + the `loadOpcodeHandles` optional-handle table (the `h.loadU==0` sentinel no-ops on x86_64; no arch identity branch). Byte-pinned (LDR X1,[SP,#192]=0xF94063E1, STR=0xF90063E1, LDR W2,[X3,#64]=0xB9404062) + disasm round-trip (raw imm12) + 3 red-on-disable fail-louds. RUN-WITNESSED `examples/c-subset/varargs_aapcs64_overflow_fixed_stack` exit 210 under qemu-aarch64 (the 9th fixed param loads via `LDR x21,[sp,#656]`). Genuinely-unencodable residual → `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12`. _Original deferral below._ The AArch64 assembler only emits the UNSCALED `LDUR/STUR Xt, [Xn, #simm9]` load/store form (signed 9-bit byte offset, range -256..255 — `arm64.target.json` `load`/`store` `imm9` slots, validated in `src/asm/format/fixed32.cpp:~668`). A frame larger than ~256B forces a frame-relative load/store offset past that range → `A_ImmediateOperandOutOfRange` ("a larger frame needs the scaled LDR/STR imm12 form, not yet supported") → `A_FunctionEncodeAborted`. Surfaced FIRST by `D-FC12C-AAPCS64-VARIADIC-OVERFLOW-FIXED-STACK-ARGS` (a ≥9-fixed-param AAPCS64 variadic callee: the 192B GR+VR register-save-area + the dual-cursor va_arg reg-arm reads push a body load to offset ≥288 even when the body is minimal). The prologue VR spill ALREADY works around this with a scratch-base register (`add scratch, sp, #vaRegSaveAreaOffset()` then small imm9 offsets — `lir_callconv.cpp` emitVariadicPrologueSpill), but ARBITRARY body loads from a large frame do not. Priority: **normal** (only large frames; the common ≤256B frame is correct + the dominant case). | Add the scaled `LDR/STR Xt, [Xn, #imm12]` form (unsigned 12-bit, scaled by access size → reach up to 32760 for 8B) to `arm64.target.json` + the fixed32 encoder (a NEW `imm12scaled` slot or width-keyed scaling), and have the callconv/encoder SELECT it when the offset exceeds imm9 (or always for frame-relative ≥256B); then re-add the deferred `examples/c-subset/varargs_aapcs64_overflow_fixed_stack` runtime corpus (exit 210) under qemu-aarch64. | `D-FC12C-AAPCS64-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`; `D-FC12C-AAPCS64-VARIADIC-CALLEE`; plan 13 (asm encoding) |
| `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12` | ✅ **ADD/SUB/LEA-IMMEDIATE half CLOSED 2026-06-22 (FC12-deferral 2 / Item 3②)** — the PRACTICAL large-frame blocker. A function with a frame > 4095 bytes (`int big[9000]` = 36000B) failed to encode at THREE sites, all an AArch64 `add`/`sub`/`lea` IMMEDIATE > 4095 (the imm12 slot): prologue `sub sp,#36032`, epilogue `add sp,#36032`, and the high-element FRAME-ADDRESS `lea` (`&big[8999]` = 35996). FIXED scratch-free at the ENCODER (zero `lir_callconv.cpp`/`mir_to_lir.cpp` change): a new `EncodingSlotKind::Imm12HiLo24` slot + a 2-word shifted-imm12 macro variant per `add`/`sub`/`lea` — `op Xd,Xn,#(V&0xFFF)` (sh=0) then `op Xd,Xd,#(V>>12),LSL #12` (sh=1, fixedWord base|0x400000), magnitude-routed by a new optional `immMin`/`immMax` variant guard (1-word ≤4095, 2-word 4096..0xFFFFFF). Reaches 16 MiB; >16 MiB → `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-16MIB` (row below). AGNOSTIC (x86_64 has no imm12 slot → never reached; only slot-kind `==` + value-magnitude arithmetic). Byte-pinned (sub sp #36032 → 0xD13303FF/0xD14023FF; add → 0x913303FF/0x914023FF; lea → split) + boundary pins (4095/4096/16777215/16777216) + a callconv two-inst prologue/epilogue count pin; **run-witnessed `examples/c-subset/large_frame_arm64` exit 42 on qemu-aarch64 + x86_64-linux** (release pipeline). NON-leaf (saved-reg spill + call) AND register-pressure-spill large frames also confirmed correct at runtime (exit 42 / 104) — the frame layout keeps saved-regs/spills LOW (in imm12), so only the high array-element address goes beyond imm12 and routes through the now-fixed lea. Independent plan-lock + independent pr-review both CLEAN. **DIRECT frame LOAD/STORE displacement (the encodable scaled-imm12 reach) ✅ CLOSED 2026-06-24 (branch feature/0-0-2-p14, HEAD 15c728a, 389 ctest MSVC Release + MinGW + corpus `large_spill_frame_arm64` exit 42 qemu-aarch64 baseline+release).** The scaled-imm12 selection (`load`→`load_u`, `store`→`store_u` when the frame offset exceeds the unscaled imm9 ±256 reach and is encodable: non-negative, access-size-aligned, scaled ≤4095) is now applied at the SINGLE frame load/store CHOKEPOINT `selectFrameMemOp` inside `emitFrameLoad`/`emitFrameStore` (`lir_callconv.cpp`) — so EVERY frame access (spill reload/store, saved-reg save/restore, incoming + outgoing stack args, by-value copy, va spills) is covered BY CONSTRUCTION (the prior incoming-stack-arg-only selection was REMOVED and re-routed through the chokepoint; the encoder `Imm12Scaled` handler + the `load_u`/`store_u` JSON opcodes from `D-ASM-AARCH64-LARGE-FRAME-IMM12` needed NO change). AGNOSTIC: the swap is gated on `baseOp == h.store/h.load` (the universal GPR form — an FPR `fldur`/`fstur`/`fstur_q` keeps its class encoding) AND `h.storeU/h.loadU != 0` (0 on x86_64 → inert, byte-identical goldens). **The earlier "UNREACHABLE in shipped codegen / no producer emits one" claim was FALSIFIED 2026-06-24: the plan-24 `deep_stmt_mir` corpus (600-deep nested-if + the full-release optimizer spills ~590 vregs → frame > 256 B) hit this DIRECT frame `ldr/str [sp, #off]` residual on the macos-clang-release CI leg** — a real producer (heavy register-pressure spill under optimization) DOES emit a direct frame load/store beyond imm9. Closed + RUN-witnessed by a NEW deterministic corpus `examples/c-subset/large_spill_frame_arm64` (a 30-fixed-param AAPCS64 callee `g` called with 30 args → the 9th+ INCOMING stack-arg loads in `g` + the OUTGOING stack-arg stores + caller spill reload/store all exceed imm9 at offsets 256/264 → scaled `load_u`/`store_u`; returns p28+p29+p08 = 1+40+1 = 42, miscompile-sensitive; arm64-only because the un-optimized x86_64 caller exhausts its 16-GPR scratch pool materializing 30 args — a SEPARATE pre-existing regalloc limit, not this fix; qemu-aarch64 exit 42 VERIFIED + macho arm64 leg; RED-on-disable: revert the selector → the arm64 compile fails loud `A_ImmediateOperandOutOfRange` at offsets 256 AND 264 (store AND load) → cannot build) + the host-independent structural callconv pin `Aarch64HighStackParamUsesScaledImm12FrameLoad` (a 40-param callee → asserts `load_u` present in the materialized module + clean assembly, red-on-disable, runs on EVERY CI leg) + the agnostic counter-pin `X8664HighStackParamHasNoScaledImm12FrameOps` (x86_64 declares no `load_u`/`store_u`) + imm9/scaled BOUNDARY byte-pins (offset 255→imm9 STUR, 256→scaled STR, 32760→scaled imm12-max, 256 on the unscaled `store`→fail-loud). The genuinely-UNENCODABLE tail (offset NEGATIVE outside imm9, aligned >4095*accessSize=32760, or non-access-aligned outside imm9 — needs a register-offset / address-materialization form) STAYS fail-loud → the narrower residual `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-SCALED-IMM12` (row below). Priority: **CLOSED.** | (load/store displacement CLOSED 2026-06-24; the unencodable register-offset tail → `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-SCALED-IMM12`) | `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-SCALED-IMM12`; `D-ASM-AARCH64-LARGE-FRAME-IMM12`; `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-16MIB`; `D-FC12C-AAPCS64-VARIADIC-OVERFLOW-FIXED-STACK-ARGS`; plan 13 (asm encoding) |
| `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-16MIB` | ✅ **CLOSED 2026-06-23 (FC-deferral close-out c12, branch feature/0-0-2-p14, 374 ctest + corpus `large_frame_beyond_16mib` exit 42 qemu-aarch64 baseline+release).** AS-BUILT (§B Option A — encoder macro, x16 baked): a NEW `EncodingSlotKind::Imm32MovzMovk` (value-bearing fixed32 slot; the encoder splits a 32-bit value lo16→word0 / hi16→word1 into the MOVZ/MOVK imm16 windows, mirroring `materializeViaMovkLadder`'s chunk split) + a NEW 3-word `fixedWords` variant per `add`/`sub`/`lea` in `arm64.target.json`, magnitude-routed by the variant `immMin:16777216`/`immMax:2147483647` guard. The macro is `MOVZ Xs,#(V & 0xFFFF)` + `MOVK Xs,#((V>>16) & 0xFFFF),LSL #16` + an EXTENDED-register op: for the sp-adjust `sub sp,sp,x16` (0xCB3063FF) / `add sp,sp,x16` (0x8B3063FF) with **x16 (AAPCS64 IP0 scratch) BAKED** into all 3 base words (NO resultSlot — sp=31 baked into the op word, validate's rule-G `imm32.movzmovk` baked-dest exemption); for the lea `add Xd,sp,Xd` (0x8B2063E0 base) SCRATCH-FREE — the displacement materializes into the lea's OWN dest reg Xd, threaded through all 3 words via resultSlot+extraResultSlots (the operand-0→rn wire sets Rn from the ACTUAL frame-base operand — sp here, so Rn=31=sp, byte-pinned; the override is general so a non-sp base would get its own Rn). The EXTENDED-register form (bit 21, option=011 UXTX) is the make-or-break: the shifted-register form (0xCB000000) would write XZR not sp → corrupt frame. AGNOSTIC (x86_64 has no imm16/movzmovk slot → never reached; slot-kind `==` + value-magnitude arithmetic only; the x16 identity is config-baked like XZR=31/sp=31). Byte-pinned (sub sp #0x1234000 → MOVZ/MOVK + 0xCB3063FF full 32-bit word; lea → MOVZ/MOVK + 0x8B2263E2; boundary pins 0xFFFFFF→2-word / 0x1000000→3-word / 0x7FFFFFFF→3-word / 0x80000000→fail-loud) + a callconv F4 structural pin (prologue's first word = `MOVZ x16` + x16 not value-allocated, durable scratch-safety) + **objdump-confirmed + run-witnessed `examples/c-subset/large_frame_beyond_16mib` exit 42 under qemu-aarch64 (baseline+release, Inlining ON) + x86_64-linux** (RED-ON-DISABLE: removing the variant's reachable range → A_NoMatchingEncodingVariant on the sub/add sp-adjust → the arm64 leg cannot produce a binary). Genuinely-unencodable residual → `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-2GIB` (row below; a >2 GiB frame goes negative through the int32 frame size → no variant matches → fail-loud). _Original deferral below._ NEW RESIDUAL of `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12` (FC12-deferral 2, 2026-06-22): the SCRATCH-FREE shifted-imm12 word-pair (`EncodingSlotKind::Imm12HiLo24` + the `add`/`sub`/`lea` 2-word variants `op Xd,Xn,#lo` then `op Xd,Xd,#hi,LSL #12` in `arm64.target.json`, magnitude-routed by the variant `immMin`/`immMax` guard) now lands an `add`/`sub`/`lea` IMMEDIATE in (4095, 0xFFFFFF] — a 16 MiB frame reach, covering every realistic frame (e.g. `int big[9000]` = 36000B prologue + `&big[8999]` lea). A value > 0xFFFFFF (16 MiB) STILL fails loud: the add/sub/lea variant selector rejects it (`A_NoMatchingEncodingVariant`, since `immMax:16777215` excludes it), and the encoder's `writeHiLo24` 24-bit gate (`A_ImmediateOperandOutOfRange`) is the defense-in-depth backstop. No shipped corpus has a > 16 MiB single frame; this row keeps the ledger honest about the genuinely-unencodable tail. Priority: **low** (only a single function with > 16 MiB of locals). | Add a THIRD word to the macro (`#hi2,LSL #24` reaching 36 bits) OR materialize the offset into a scratch register via MOVZ/MOVK + a register-form `add/sub Xd,Xn,Xm` and select it when the value exceeds 0xFFFFFF. | `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12`; `D-ASM-AARCH64-LARGE-FRAME-IMM12`; plan 13 (asm encoding) |
| `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-SCALED-IMM12` | **OPEN (trigger-gated) — registered 2026-06-24 (scaled-imm12 frame load/store close-out, branch feature/0-0-2-p14).** NEW narrower residual of `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12` (row above): now that the ENCODABLE scaled unsigned-offset frame load/store (`load_u`/`store_u`, LDR/STR `[Xn,#imm12]`, reach 0..4095×accessSize = 0..32760 for an 8-byte slot) is selected at the `selectFrameMemOp` chokepoint, a frame `ldr/str [sp, #off]` whose `off` is GENUINELY UNENCODABLE by BOTH the unscaled imm9 (−256..255) AND the scaled unsigned imm12 STAYS fail-loud `A_ImmediateOperandOutOfRange` at the fixed32 encoder (`src/asm/format/fixed32.cpp` Imm12Scaled handler). Three sub-cases: (a) a NEGATIVE offset outside imm9 (the scaled form has no sign bit), (b) an aligned offset > 32760 (scaled field > 4095), (c) a non-access-size-aligned offset outside imm9 (the scaled field is byteOffset/accessSize — an unaligned LDR is architecturally undefined). The chokepoint keeps `baseOp` (the unscaled form) in each case → the encoder fail-louds. NOT reachable by any shipped corpus: case (a) needs a base-relative negative beyond −256 (frame offsets are non-negative sp-relative), (b) needs a single frame spill/saved-reg/stack-arg offset > 32760 B (a ~32 KB spill area — far past the deepest corpus pressure), (c) needs an unaligned frame slot (all slots are slotSize-aligned). This row keeps the ledger honest about the genuinely-unencodable tail. Priority: **low** (the closing form is the register-OFFSET LDR/STR `ldr Xt,[Xn,Xm]` or an address-materialization into a scratch — only needed for a >32 KB single-access offset or a future unaligned/negative frame access, neither of which a producer emits today). | Emit the register-offset frame form (`ldr/str Xt,[sp,Xm]` after MOVZ/MOVK-materializing the offset into a scratch `Xm`), OR materialize the address `add Xd,sp,#off` (via the shipped shifted-imm12 / MOVZ-MOVK macro) then `ldr/str [Xd,#0]` — selected when the offset fits NEITHER imm9 nor the scaled imm12; stay agnostic (slot-kind `==` + value-magnitude arithmetic, no arch/cc identity). Pin with a synthetic >32760 frame-offset case (fail-loud red-on-disable) + a runtime corpus only if a producer ever emits a >32 KB single-access frame offset. | `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12` (the parent, load/store-displacement half closed 2026-06-24); `D-ASM-AARCH64-LARGE-FRAME-IMM12` (the scaled-imm12 encoder + opcodes); `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-16MIB` (the add/sub/lea-immediate sibling); plan 13 (asm encoding) |
| `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-2GIB` | NEW RESIDUAL of `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-16MIB` (FC-deferral close-out c12, 2026-06-23): the MOVZ/MOVK + EXTENDED-register 3-word form (`EncodingSlotKind::Imm32MovzMovk` + the `add`/`sub`/`lea` 3-word variants in `arm64.target.json`, magnitude-routed by `immMin:16777216`/`immMax:2147483647`) now lands an `add`/`sub`/`lea` IMMEDIATE in (0xFFFFFF, 0x7FFFFFFF] — a ~2 GiB frame reach, far past any realistic frame. A value > 0x7FFFFFFF (2 GiB) STILL fails loud: the frame size flows through `emitSpAdjust`'s `static_cast<int32_t>(bytes)` and `variantImmMagnitude`'s `immInt32` (int32) — a >2 GiB magnitude goes NEGATIVE, so `variantImmMagnitude` returns nullopt → NO variant matches → `A_NoMatchingEncodingVariant`; the encoder's `writeMovzMovk` 0..0x7FFFFFFF gate (`A_ImmediateOperandOutOfRange`) is the defense-in-depth backstop. No shipped corpus has a > 2 GiB single stack frame (it would itself exceed most process stack limits); this row keeps the ledger honest about the genuinely-unencodable tail. Priority: **low** (a single function with > 2 GiB of locals — absurd; fail-loud is correct). | Widen the frame-size carrier from int32 to int64 (`emitSpAdjust`, the magnitude key, the LIR ImmInt payload) AND extend the macro to a 4-word MOVZ + 3×MOVK (LSL #16/#32/#48) materialization, selected when the value exceeds 0x7FFFFFFF — only if a producer ever emits a > 2 GiB frame. | `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-16MIB`; `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12`; plan 13 (asm encoding) |
| `D-WIN64-LARGE-FRAME-STACK-PROBE` | ✅ **CLOSED 2026-06-23 (FC-deferral close-out c11, branch p14, 372 ctest + corpus `large_frame_win64` exit 42 native Windows baseline+release).** AS-BUILT: an INLINE page-walking stack probe emitted by the x86_64 encoder from a NEW `stack_probe` LIR op (chosen over `__chkstk` because dss PE exes are FREESTANDING — import only kernel32, not msvcrt where __chkstk lives; verified). Config-driven `stackProbePageBytes` on TargetCallingConvention (ms_x64=4096, all others 0/absent → no probe; validate() requires a power of two). `emitPrologue` (threaded `cc`) emits `StackProbe(sp, frameBytes, pageBytes)` when `cc.stackProbePageBytes>0 && totalFrameSize>pageBytes`, else the plain `sub rsp,F` (byte-identical small-frame/non-Windows path). The encoder hand-emits the loop `mov r11d,frame / .L: sub rsp,page / or qword[rsp],0 / sub r11d,page / cmp r11d,page / ja .L (0x77, compile-time rel8, no reloc) / sub rsp,r11` — r11 is ms_x64 caller-saved, not arg/return, probe runs before arg-home. AGNOSTIC (no `if arch/format==`; page step from the op operand, not hardcoded; arm64 OMITS the op → A_NoEncodingDeclared if ever emitted). RED-ON-DISABLE (the strongest kind): the corpus CRASHES (SIGSEGV exit 139) without the probe, exit 42 with — native on the Windows box. Byte-pins (frame 36000 non-multiple + 8192 exact-page, exact 0x77 ja) + prologue structural pins (ms_x64>4096→probe; ≤4096 + sysv>4096→plain). **★ the unforgeable native witness caught a real bug in the build: a first `sub rsp,r11` mis-encoded as `49 29 DB` (=`sub r11,rbx`) segfaulted EVEN WITH the probe → fixed to `4C 29 DC`, byte-pinned.** ORIGINALLY: **OPEN — a SILENT RUNTIME CRASH** (surfaced 2026-06-22 while adding the `large_frame_arm64` corpus, but ENTIRELY ORTHOGONAL to the arm64 fix — a pre-existing x86_64-Windows gap). DSS did NOT emit a stack-probe (`__chkstk` / page-walk) for `ms_x64` / `pe64` functions whose frame exceeds one guard page (4096B). A function like `int main(){ int big[9000]; ... }` for `x86_64:pe64-x86_64-windows-exec` emits a bare prologue `sub rsp, ~36016` that jumps ~9 pages PAST the stack guard page WITHOUT touching the intermediate pages → the first deep-slot access raises `STATUS_ACCESS_VIOLATION` (0xC0000005) at runtime. The Windows x64 ABI REQUIRES a function allocating >1 page to probe each page (`__chkstk`, or an inline `sub rsp,0x1000`/`test [rsp],eax` loop) so the guard page walks down one page at a time and the kernel commits each. Empirically verified: a 400B + a 4400B frame exit 42, a 36000B frame segfaults. Linux x86_64 + AArch64-ELF do NOT need it (kernel stack-growth guard-gap absorbs the multi-page jump — which is why `large_frame_arm64` exits 42 on x86_64-linux + qemu-aarch64). This is WHY the `x86_64:pe64` target was excluded from `examples/c-subset/large_frame_arm64/expected.json` (see its `$comment`). Priority: **normal** (a real silent-crash class on >1-page Windows frames — large local buffers; converting it to FAIL-LOUD is a cheap interim). | Implement Windows large-frame stack probing in the prologue SP-adjust (`lir_callconv.cpp` emitPrologue/emitSpAdjust), gated AGNOSTICALLY on a CC/format-declared stack-probe-threshold field (mirroring `redZoneBytes`/`shadowSpaceBytes`, NOT `if(arch=="x86_64")`): when the frame exceeds the page size, emit the page-walk probe (or a `call __chkstk`) before/in place of the single `sub rsp,N`; add a runtime corpus witness (re-add the `x86_64:pe64` target to `large_frame_arm64`) + a structural pin. INTERIM (cheaper, kills the silent crash now): a fail-loud guard refusing a >1-page ms_x64 frame until probing lands. | plan 13 (asm encoding); plan 14 (linker); `D-ASM-AARCH64-FRAME-OFFSET-BEYOND-IMM12` (the arm64 sibling, closed) |
| `D-FC12B-WIN64-STRUCT-VARARG` | ✅ **CLOSED 2026-06-20 (FC12b-struct).** Win64 (ms_x64, `VaListStrategy::HomogeneousPointer`) struct-by-value at the variadic boundary + `va_arg(ap, struct)`. **NO new MIR op / LIR kind / carrier, NO lir_callconv / aggregate_abi change** — the design REUSED proven mechanisms: the Win64 ABI passes a by-value struct in EXACTLY ONE arg slot (pow2-≤8B → `classifyAggregate(Win64BySize)` InRegisters[1 GPR piece] = the bytes BY VALUE; every other size → ByReference = a hidden caller-copy POINTER), placed like a scalar by lir_callconv's existing `cc.slotAligned` walk (Win64 has NO SysV MEMORY-class-to-stack rule — one struct = one slot, register-or-stack BY POSITION). **Caller (`hir_to_mir.cpp` Call arg loop, both walls restructured into a clean VaListStrategy branch):** the two fail-loud HomogeneousPointer guards (ByReference wall + InRegisters wall) → `appendByValueArg(operands, kids[i], argTy, *abi)` (the SAME helper the non-variadic path uses: InRegisters → copy-to-temp + load 1 I64 → 1 value operand; ByReference → copy-to-temp + push the temp POINTER → 1 operand) + `runGpr += numGp` (==1) so a subsequent vararg's slot index is right; the SysV atomic-fit/`gpSaveCount` split + `appendByValueStackArg` carrier + the AAPCS64 guard are SKIPPED for HomogeneousPointer and PRESERVED byte-unchanged for SysVRegisterSave/Aapcs64DualCursor. **Callee (`lowerVaArgAggregate` HomogeneousPointer arm):** classify via `byValueClassify(T)` — InRegisters (≤8B) → the slot IS the storage, return apVal (the slot addr), NO extra Load (mirrors the SysV overflow-arm `return ovfArea`); ByReference (>8B) → Load the pointer from `*apVal` → the caller-copy addr; both bump `ap` by `namedArgSlotBytes` (one slot, value or pointer — NOT roundUp(size,8)), LINEAR (no diamond), mirroring the scalar HomogeneousPointer va_arg. **Pins (red-on-disable verified):** the 2 fail-loud pins INVERTED to success + 2 new (`Win64VaArgStructLeq8ReturnsSlotAddr` / `Gt8DerefsPointer` — no ICmpUle/carrier, the >8B deref adds a Load; `Win64StructByValueVarargCallLeq8OneSlot` / `Gt8OnePointer` — the Call has exactly 1 value/pointer operand, no carrier). RUN-WITNESSED PE-x86_64 on THIS Windows host: `examples/c-subset/varargs_win64_struct` (a ≤8B by-value `struct Small`={x,y} AND a >8B by-ref `struct Big`={a,b,c} both through `va_arg(struct)`, anti-folded via mutable global `g_seed`, baseline + full-release-like opt arm → exit 45). | (closed) | `D-FC12A-VARIADIC-MEMORY-CLASS-STRUCT`; `D-FC12B-WIN64-VARIADIC-CALLEE`; `D-FC12C-AAPCS64-HFA-STRUCT-VARARG` (✅ CLOSED 2026-06-20); `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` |
| `D-OPT-VALIST-ALLOCA-MEM2REG` | ✅✅ **ROOT-CAUSE COMPLETE 2026-06-20 — the code fix is DONE (7f321fd + 061fa23); the persistent CI red was NOT a 3rd code bug but a STALE GitHub PR merge ref.** `refs/pull/28/merge` = `e36dea16` ("Merge **7f321fd** into base", parents `[f563956, 7f321fd]`) = PRE-061fa23 source, while the PR head is correctly `061fa23` (mergeable) — GitHub never regenerated the merge ref after 061fa23, and the `pull_request` CI checks out the merge ref, so EVERY PR #28 run built pre-fix source (proven: byte-identical `.so` BuildId across the 7f321fd + 061fa23 runs despite the changed `.o`). The 061fa23 `lowerAggregateCopy` span fix is verified CORRECT + SUFFICIENT — gcc-13/clang-19 × libstdc++/libc++ × O0/O2 × ASan/valgrind/Electric-Fence + a CI-matching Ubuntu-24.04 container (unfixed 5/5 fail / fixed 5/5 clean); NO third compiler bug exists. REMEDY: a fresh push to feature/0-0-2-p12 regenerates the merge ref → CI then tests the fix. ✅ **CLOSED 2026-06-20** — e0234d8 CI GREEN on ALL 5 legs (run 27885691108: windows-msvc-release + macos-clang-release + linux-clang-asan + linux-gcc-release + linux-arm64-gcc-release all success). The varargs_win64_struct saga is fully resolved (2 code fixes 7f321fd parser + 061fa23 span, CI-confirmed across libstdc++/libc++/asan). ORIGINAL STATUS BELOW: ✅ **FIX SHIPPED 2026-06-20 (mark CLOSED after the non-Windows CI legs go green — TWO layered host-dependent-UB bugs surfaced on this corpus, fix commits 7f321fd + this one). THE §B Mem2Reg/va_list HYPOTHESIS WAS REFUTED by investigation — the name is a MISNOMER kept for traceability to the §B that spawned it.** ACTUAL ROOT CAUSE: a PRE-EXISTING use-after-`pop_back` in `TreeBuilder::closeFrame_` (`src/core/types/tree_builder.cpp`) — `Frame& fr = open_.back()` → `open_.pop_back()` → then read `fr.openerSpan`/`fr.rule` from the dead back slot. UB: MSVC-release reads the stale-but-intact bytes by luck (passed windows-msvc + the Windows-only local gate), gcc/clang-release + ASan abort (the 4 non-Windows legs). NOT optimizer- or variadic-related — `varargs_win64_struct` merely drove the parser into the speculative-branch cascade-close shape (`trySpeculativeBranch` → `closeFrameOnce` → `closeFrame_`); the crash fires during PARSING, before MIR/the optimizer. FIX: snapshot `openerSpan`+`rule` into locals BEFORE `open_.pop_back()`, then `leaveRule` uses the copies — a generic, source/target/format-AGNOSTIC parser-lifetime fix (no identity branch). VERIFIED: 345/345 ctest (no regression) + MSVC-ASan red-on-disable on BOTH the corpus AND `dss_core_test_tree_builder` (revert → `container-overflow` READ at `closeFrame_` tree_builder.cpp:500 → restore → clean). REGRESSION GUARD: the existing TreeBuilder cascade-close tests (`OutOfOrderCloseCascadesAndDoesNotDoubleDiagnose` + every close-path test) are host-independent red-on-disable under any ASan leg; the corpus is a hard-crash witness on gcc-release too. COVERAGE LESSON: the Windows-only local gate STRUCTURALLY cannot catch sanitizer-class UB — the CI ASan/release legs are that gate, so confirm each commit's CI green BEFORE stacking the next cycle (the MSVC-ASan recipe is now a known local tool: `build-asan/` + `/fsanitize=address` RelWithDebInfo + global `/bigobj`, PCH on). **SECOND BUG (HIR→MIR lowering, UNMASKED by the 7f321fd parser fix, fixed in this commit):** a dangling-`std::span`-into-`TypeInterner::operandPool_` heap-use-after-free in `lowerAggregateCopy` (`hir_to_mir.cpp:2490`) — it held `interner.operands(aggTy)` (a span into operandPool_) across the field-copy loop's `interner.pointer(...)` interns, which REALLOCATE the pool → dangling read. ASan legs = heap-use-after-free; release legs misread the freed bytes → an invalid TypeId → `MirBuilder fatal: addInst: 'load' result type invalid`. Host-dependent (MSVC vector growth 1.5× usually has spare capacity → Windows + local gate pass; libstdc++/libc++ 2× realloc → the 4 non-Windows legs abort). The TWIN of the already-fixed FC7-C1c hazard at `hir_to_mir.cpp:4660`. FIX: copy the span into an owning `std::vector<TypeId>` before the loop (agnostic; matches the 4660 precedent). VERIFIED: 345/345 ctest + MSVC-ASan forced-realloc red-on-disable (revert→heap-use-after-free at lowerAggregateCopy→restore→clean; NO third bug). The investigation audited every `operands()`/`fnParams()`/`scalars()` span site — 2490 was the SOLE remaining unfixed instance. Recurring class (4660 + 2490) → preventive hardening pinned `D-TYPEINTERNER-OPERAND-SPAN-LIFETIME-GUARD`. _Original (refuted-hypothesis) blocker text below._ 🔴 OPEN — ACTIVE BLOCKER (immediate next cycle), discovered 2026-06-20 by the FC12c-struct Step-8.5 self-audit + CI verification. `examples/c-subset/varargs_win64_struct` (FC12b ②, the ONLY variadic corpus with an `optimizedPipelines` arm) crashes the dss COMPILER ("Subprocess aborted") on ALL 4 non-Windows CI legs (linux-clang-asan, macos-clang-release, linux-gcc-release, linux-arm64-gcc-release) while compiling its optimized pipeline; **windows-msvc-release + the Windows-only local gate PASS** — which masked it through both the ② and ③ cycles. DETERMINISTIC (41a64d7's single CI run aborted on all 4 legs together — the earlier "flaky/same-sha-flip" read was a sha misattribution; the 15:08 pass was on `c74aeae` ① which predates the corpus). §A.6 (all CI legs green) is VIOLATED on the branch tip. Leading hypothesis (self-audit forensic, **UNCONFIRMED — needs a linux/asan repro**): Mem2Reg (`src/opt/passes/mem2reg.cpp`) promotes the `va_list`/`ap` backing alloca to SSA — unsound, because a va_list aliases ABI-defined memory (the prologue-spilled register-save / home area), so the post-promotion access OOBs. Trigger HAS fired. | ✅ DONE — the real fix DIFFERS from this hypothesis-based plan (see the description column for the actual TreeBuilder root cause + the copy-before-pop fix). SUPERSEDED plan was: Reproduce on linux (WSL build or the linux-clang-asan config) → confirm/refute the Mem2Reg↔va_list root cause → fix AGNOSTICALLY: disqualify any alloca whose address feeds `VaStart`/`VaArg`/`VaEnd` from Mem2Reg promotion (the rule "a va_list slot is ABI-aliased memory", uniform across every `VaListStrategy`), OR mark the `ap` alloca non-promotable at lowering — NO target/CC/format identity branch. Add a HOST-INDEPENDENT red-on-disable opt-tier pin (the va_list alloca SURVIVES Mem2Reg under the optimized pipeline; red if the exclusion is reverted). Close the coverage gap that hid it: give ≥1 variadic corpus PER ABI (SysV / Win64 / AAPCS64 / Apple) an `optimizedPipelines` arm. Confirm ALL CI legs green via `gh run list` (not local-only). | `D-FC12B-WIN64-STRUCT-VARARG`; `D-FC12A-VARIADIC-CALLEE`; `D-FC12C-AAPCS64-HFA-STRUCT-VARARG`; plan 23 FC12 |
| `D-TYPEINTERNER-OPERAND-SPAN-LIFETIME-GUARD` | ✅ **CLOSED 2026-06-22** — shipped the Option-1 DEBUG generation-guard: `operands()`/`scalars()`/`fnParams()` now return a `GuardedSpan<T>` that captures a `poolGen_` counter (bumped once per pool-mutating intern at the sole `internContent` chokepoint, AFTER the dedup early-return) and ABORTS DETERMINISTICALLY on a read after a later intern — on EVERY host, no ASan / realloc-luck needed; under `NDEBUG` it is a zero-overhead `using` alias to `std::span<T const>`, so the public contract + codegen + all ~234 call sites are byte-identical (built clean with ZERO call-site edits). Pinned by `StaleOperandSpanAfterInternAborts` (debug death test — RED-ON-DISABLE DEMONSTRATED: guard off → "failed to die"; `GTEST_SKIP` in release where the guard is intentionally absent) + `FreshSpanAndOwningCopySurviveIntern` (no false-fire on immediate read + the own-before-mutate fix pattern survives later interns). The conservative guard found NO 3rd latent instance across the suite (347/347 ctest). Independent code-review CLEAN. `name()` (a `string_view` into the SEPARATE `names_` interner) is deliberately OUT of scope (no incident; extend the same pattern if a names_-realloc hazard ever surfaces). _Original OPEN text:_ PREVENTIVE hardening for a RECURRING bug class; all current instances fixed. `TypeInterner::operands()`/`fnParams()`/`scalars()` return a `std::span` INTO `operandPool_`'s heap buffer; any caller that retains such a span across a subsequent intern (`pointer`/`array`/`internContent` → `operandPool_.insert` may REALLOCATE) gets a dangling-span heap-use-after-free. This class has bitten TWICE, both fixed reactively via copy-to-owning-vector — `hir_to_mir.cpp:4660` (FC7-C1c, function params) + `hir_to_mir.cpp:2490` (lowerAggregateCopy, the `D-OPT-VALIST-ALLOCA-MEM2REG` cycle) — both host-dependent (MSVC vector growth masks it; libstdc++/libc++ realloc → non-Windows CI aborts) and INVISIBLE to the Windows-only non-ASan local gate. Trigger: a 3rd instance, OR the next substantive edit to the interner/lowering span sites. | Add a DEBUG-only span-lifetime guard so a dangling-span read aborts DETERMINISTICALLY on every host (not only under ASan / the non-Windows legs): e.g. a generation counter on `operandPool_` bumped on each mutation + a thin debug `SpanView` wrapper capturing the gen at creation and asserting it unchanged on access (zero-cost in release; `operands()` returns the wrapper in debug), OR make `operands()`/`fnParams()`/`scalars()` return an owning small_vector by default (eliminates the class at a small copy cost). Pin with a unit test that retains a span, forces an `operandPool_` realloc, then asserts the guard fires (red-on-disable on EVERY leg). Source/target/format-agnostic. | `D-OPT-VALIST-ALLOCA-MEM2REG`; `D-FC7-STRUCT-BY-VALUE-ARG-RETURN`; plan 12 (MIR/interner) |
| `D-DECL-PREFIX-STRIP-SHARED-HELPER` | ✅ **CLOSED 2026-06-11 (FC4 c0).** *(Tracking note, audit-folded: the cycle-14 row's latent-(i) — a loader reject for a contradictory prefix+ignoredKinds+EMPTY-`linkageSpecifiers` config — remains OPEN trigger-conditioned on a real consumer; this extraction closes latent-(ii) only.)* Extracted to ONE header-only shared source of truth `src/core/types/decl_prefix_strip.hpp` (the lowest common layer — `Tree` + `DeclarationRule` are both `core/types`; all three consumers link `core`): free functions `declRoleChildren` / `descendVisibleDecl` (`std::span` path; FIRST step prefix-stripped, LATER steps RAW visible — pinned) / `specifierPrefixChild` (the prefix accessor `linkageFrom` scans). ALL THREE consumers routed through it, local copies DELETED (analyzer free functions; const-eval inline strip; cst_to_hir member helpers — call sites now call the free functions with `tree()`). **Also fixed the cycle-14 latent + its same-class siblings in cst_to_hir.cpp** (each provably behavior-preserving today — only c-subset `topLevelDecl` ships a `specifierPrefix`): `lowerVarLike` raw `visible(node)` → `declRoleChildren` (load-bearing when local decls gain specifiers); `lowerTypeDecl` likewise; lowerExternDecl's THREE `descend(node, kindByChild->childPath)` probes → `descendVisibleDecl`; `descendantsForInit` seeds from role children (a future expr-shaped specifier arg — e.g. `aligned(8)` — can never be mistaken for a global's init). Closing test `tests/core/test_decl_prefix_strip.cpp` (6 cases, synthetic `RawTreeBuilder` tree, NO schema/language): (a) prefix present → stripped (EmptySpace-leading still strips), (b) no prefix declared → untouched, (c) different-rule first child → untouched, (d) Token first child → untouched, + descend first-step-stripped/later-steps-RAW (a nested prefix-RULE child is NOT re-stripped — the RED lever) + empty-path/out-of-range pins. Grep-proof: the strip guard on `Tree` exists ONLY in the shared header. The parser's binder-sketch strip (`parser.cpp` `extractBinderName_`) is a deliberate NON-consumer: it runs MID-PARSE over `TreeBuilder` pending frames (no finished `Tree` exists) against the sketch's plain-`RuleId` row. 249/249. — **Original finding**: Cycle 13 (2026-06-04): the declaration-specifier-prefix strip guard (`specifierPrefixRule.has_value() && first-visible-child kind==Internal && rule==*specifierPrefixRule → drop the leading child`) now exists in THREE file-local copies — `declRoleChildren`/`descendVisibleDecl` (`semantic_analyzer.cpp`), the inline strip in `findInitExprInDecl` (`cst_const_eval.cpp`), and `specifierPrefix`/`declVisible`/`descendDecl` (`cst_to_hir.cpp`). The 3rd-consumer trigger has fired. Priority: **normal** (pure DRY/maintainability refactor — correctness is fine today: each copy is identical + behind a full-suite green + the cycle-12 review-fold already swept the analyzer/const-eval sites; the residual risk is future DRIFT if one copy is fixed and the others aren't — exactly the missed-site class the cycle-12 fold caught). | (closed) | plan 22 (pre-OPT7 P1); `D-DECL-SPECIFIER-PREFIX-SUBSTRATE`; `D-CSUBSET-LINKAGE-SPECIFIERS`; `D-CSUBSET-LINKAGE-UNKNOWN-SPECIFIER-DIAGNOSTIC` (whose latent-(ii) `lowerVarLike` follow-up this closes) |
| `D-CSUBSET-LINKAGE-VISIBILITY-SYNTAX` | ✅ **CLOSED 2026-06-11 (FC4 c1 — stage 2a grammar + stage 2b DCE lever)** — exactly the pinned recipe, modulo the facet-key SHAPE: `attrSpec` grew the parenthesized-string form (`Identifier ( stringLiteralExpr )`, inline sequences — bare `weak`/`static` tree shapes byte-identical), `linkageFrom` forms the COMPOSITE key `visibility:hidden` (`<identifier>:<decoded-string-body>` — richer than the sketched bare-`"hidden"` key: the identifier disambiguates future string-arg attributes), c-subset `linkageSpecifiers` carries `"visibility:hidden": {"visibility": "hidden"}`. The pinned visibility→DCE lever SHIPPED end-to-end through REAL source: `HiddenVisibilityUnusedFunctionIsDceEliminated` (tests/mir/test_mir_lowering_c_subset.cpp) compiles `__attribute__((visibility("hidden")))` c-subset → MIR (asserts `SymbolVisibility::Hidden` threaded + `isExternallyVisible` false pre-DCE) → DCE eliminates the hidden+uncalled fn while the plain Global/Default uncalled fn stays linkage-protected; corpus `examples/c-subset/attributes_syntax` runs the form 4-target with a full-release-like optimized arm. — *Original row (audit trail):* Cycle 13 (2026-06-04): c-subset shipped BINDING specifiers (`static`→Local, `__attribute__((weak))`→Weak) with `attrSpec`'s arg constrained to a bare `Identifier`. VISIBILITY (`__attribute__((visibility("hidden")))`) needs a STRING-literal arg. The SUBSTRATE already fully carries visibility (`LinkageSpecifierEffect.visibility`, the loader's `symbolVisibilityFromName` arm, `MirFunc`/`MirGlobal` visibility, DCE's `isExternallyVisible` Hidden/Internal arms); ONLY the c-subset grammar (`attrArg = Identifier` not `alt(Identifier, stringLiteralExpr)`) + `linkageFrom`'s string-literal decode are deferred. | (closed) | `D-CSUBSET-LINKAGE-SPECIFIERS`; plan 22 pre-OPT7 P1; plan 23 FC4 |
| `D-CSUBSET-LINKAGE-UNKNOWN-SPECIFIER-DIAGNOSTIC` | ✅ **CLOSED 2026-06-04 (cycle 14).** Built as decided (agnostic specifier-identity + fail-loud ERROR). **Mechanism**: new per-decl facet `linkageSpecifierIgnoredKinds` (the prefix's STRUCTURAL token KINDS to skip — c-subset: AttributeKeyword/ParenOpen/ParenClose); loader resolves names→SchemaTokenId (unknown→C_UnknownToken, mirrors variadicMarker; non-array/non-string→C_InvalidSemantics); `linkageFrom` skips ignored-kind tokens and emits `H_UnknownLinkageSpecifier=0xF00C` on any non-ignored prefix token missing from `linkageSpecifiers` (skip-list DEFAULT = fail-loud). All THREE decl-lowering arms (lowerTopLevel func+var, lowerFunctionDecl, lowerExternDecl) route through the `linkageFrom` chokepoint — coverage by-construction; the **Step-3.5 design-audit caught + closed the lowerExternDecl bypass (Gate 1)**. 2 tests (func+var `__attribute__((bogus))` → H_UnknownLinkageSpecifier + hir->ok false), REGRESSION-PROVEN (disable emit → RED). Agnostic (SchemaTokenId + text compare, no hardcode), 193/193. Step-8.5 self-audit VERIFIED-CLEAN. **Latent follow-ups (deferred — no current consumer)**: (i) `linkageFrom`'s `linkageSpecifiers.empty()` early-return means a future lang with prefix + ignoredKinds + EMPTY effects-map would silent-skip — add a loader reject for that contradictory config when a real consumer appears; (ii) `lowerVarLike` (the non-topLevelDecl local-var path) uses raw `visible` — needs `declVisible` IF a future VarDecl-dispatch rule ever gains a `specifierPrefix`. Both fold into `D-DECL-PREFIX-STRIP-SHARED-HELPER`'s shared-chokepoint extraction. — ORIGINAL §B + decision retained below. The §B is resolved — do NOT re-litigate. **Decided design**: a per-language config distinguishes specifier-IDENTITY tokens (lookup-able in `linkageSpecifiers`) from STRUCTURAL syntax (`__attribute__`, parens) — likely a small facet listing the prefix's structural token KINDS to skip (e.g. `AttributeKeyword`/`ParenOpen`/`ParenClose`), so any OTHER prefix token (`StaticKeyword`, the attr `Identifier`) NOT found in `linkageSpecifiers` emits a fail-loud Error + diagnostic; the exact facet shape to be refined via feature-dev + dss-audit next cycle (keep it agnostic — the language declares the structural set, the engine just walks it). Behavior = Error (NOT GCC warn-and-ignore), per the original design-lock + §A.4. Priority: **high (next cycle).** — Cycle-13 review finding (silent-failure-hunter, MEDIUM): an UNRECOGNIZED specifier identifier inside `__attribute__((...))` — a typo (`((wek))`) OR a valid-C attribute c-subset doesn't model (`((noinline))`, `((packed))`) — is SILENTLY ignored by `linkageFrom` (facet miss → no effect, no diagnostic); the decl silently keeps default (Global) linkage. NOT a valid-code miscompile (static/weak are correct + proven) and GCC-compatible leniency (GCC ignores unknown attributes with a `-Wattributes` warning), but misses the project's fail-loud preference on unrecognized input. **§B DECISION (needs user input before building)**: (a) the AGNOSTIC MECHANISM — `linkageFrom`'s flat token-walk cannot distinguish an unknown SPECIFIER from structural SYNTAX (`__attribute__`, parens) without language config; the reviewer's "Identifier-directly-under-attrSpec" distinguisher is c-subset-grammar-specific (would break agnosticism if hardcoded), so a clean fix needs the language to DECLARE which prefix tokens/positions are specifier-IDENTITIES vs pure-syntax (a new facet or a per-rule marker), then the engine emits on an identity-token that misses the facet while still skipping syntax; (b) the BEHAVIOR — Error (strict) vs Warning (GCC `-Wattributes`) vs silent-ignore (current). Priority: **normal** (missing-diagnostic on unrecognized input, not a correctness bug for valid input). | Resolve the §B (mechanism + behavior) with the user, then implement the agnostic specifier-identity recognition + emit the chosen diagnostic; pin with `__attribute__((bogus))` → diagnostic. Revisit the earlier "unknown-attribute fork = fail-loud error" design note in light of GCC-compat + the agnostic-mechanism cost. | `D-CSUBSET-LINKAGE-SPECIFIERS`; plan 22 pre-OPT7 P1 |
| `D-AS4-DISP8-ENCODING` | First non-test consumer needs the shorter `disp8` form (e.g. `lea [rsp + small_imm]` where the offset fits in a signed byte). | Add a `Disp8` variant alongside `Disp32` in `EncodingSlotKind`; teach the encoder to pick the narrower form when the immediate fits. | plan 13 §2.4 (encoding shapes) |
| `D-AS4-ARM64-BASE-INDEX-LEA` | ✅ **CLOSED 2026-06-16 (arm64 indexed-lea cycle, branch p8).** `lowerGep` (target-blind) emits a 4-op `lea` `[base, index, MemBase(1), MemOffset(0)]` for an indexed pointer access `p[n]`; x86 had the 4-op SIB lea variant but arm64's `lea` had only symbol (ADRP+ADD, D-AS4-3) + base+disp (ADD-imm12) — NO base+index, so EVERY `p[n]` failed loud on arm64 for int AND char alike (exposed by the char value-codegen cycle's `p[n]` witnesses — NOT a char limitation). Closed by: a NEW universal width-0 `EncodingSlotKind::MemOffsetZero` ("memoffset.zero") validator slot — the displacement twin of `MemBaseNoScale` (asserts the GEP's vestigial MemOffset IS zero, writes no bits; an Imm12 reuse was UNSAFE — bits 10..21 overlap Rm in the register-add encoding) — plus an arm64 `lea` 4-op variant `ADD Xd,Xn,Xm` (fixedWord 0x8B000000), wiring base→rn / index→rm / membase.noscale / memoffset.zero. Scale=1 + disp=0 (the GEP's only emitted shape today) IS the complete realization. Pins: `Arm64Encoder.BaseIndexLeaEncodesAddRegReg` (0x8B020020 byte-pin) + `BaseIndexLeaNonZeroDispFailsLoud` (memoffset.zero red-on-disable) + `examples/c-subset/char_ptr_indexed` GAINED arm64 targets (was x86-only because of THIS gap; runtime-witnessed char `q[n]`/`p[1]` + int `p[n]` on arm64-ELF qemu → 131/99/77, byte-identical exits to x86). | (closed) | plan 13 §2.4; commit 69dbf9f (#25); `D-AS4-3` (arm64 symbol lea) + `D-AS-ENCODINGSLOT-EXHAUSTIVE-WARN` (the slot-switch gate the new enumerator passed) + successor `D-AS4-ARM64-INDEXED-LEA-SCALE` |
| `D-AS4-ARM64-INDEXED-LEA-SCALE` | **⏸ TRIGGER-NOT-FIRED — RE-VERIFIED 2026-06-23 (FC-deferral close-out c8, branch p14): §B-surfaced as NOT a real bug, deliberately NOT built (a speculative build would violate trigger-discipline).** Empirically re-confirmed the reframe still holds: `scaleIndexToBytes` (hir_to_mir.cpp:1790/2052) pre-scales every array index to a BYTE offset at HIR→MIR, so the LIR `lea` always carries scale=1; a runtime-indexed WIDE-element array (`int a[N]; a[i]`, 4-byte stride, `i` a runtime var) RUNS on arm64-ELF under qemu TODAY — `examples/c-subset/array_storage_index` exit 42 (re-run 2026-06-23 on WSL qemu) — with NO hardware scaled-lea; the `MemBaseNoScale` slot (fixed32.cpp:895 → `A_NoMatchingEncodingVariant`) fails any scale≠1 LOUD, so a scaled stride can never silently miscompile. The hardware scaled-lea remains a pure OPT with NO consumer. **REFRAMED 2026-06-17 (array-storage cluster, Option A — `D-MIR-STORAGE-ARRAY-INDEX-GEP`): now a pure CODEGEN OPTIMIZATION, no longer a correctness gap.** The original trigger below ("a scale≠1 indexed GEP exists") now NEVER fires: Option A pre-scales every array index to a BYTE offset at HIR→MIR, so the LIR `lea` always carries scale=1 (the `membase.noscale` guard still fails any scale≠1 LOUD — never a silent wrong stride). Element scaling needs NO hardware scale for CORRECTNESS (and non-pow2 strides could never be hardware-scaled anyway — exactly why MIR pre-multiply is the complete solution). **New trigger:** a future address-mode-synthesis peephole that folds a `Mul-by-pow2 + Gep` into a hardware-scaled `lea` / `ADD Xd,Xn,Xm,LSL #k` is built (an OPT, not speculative). Priority: **opt, trigger-gated.** **Original trigger:** First consumer emits an indexed GEP with a NON-unit element scale (a typed-array `lea [base + index*sizeof(T)]`, scale ∈ {2,4,8}) — the ML6 address-mode synthesis co-design (`lowerGep` emits scale=1 today, so this is unfired). | Add the shifted-register arm64 lea form `ADD Xd, Xn, Xm, LSL #log2(scale)` (the ADD-shifted `imm6` shift-amount field, bits 10..15) + a `membase.scale → shift-amount` encoder slot; the `membase.noscale` guard already fails scale!=1 LOUD meanwhile (never a silent wrong stride). x86's 4-op lea already encodes the SIB scale. Priority: **opt (trigger-gated)** (build only when a scale-folding peephole consumer exists). | plan 13 §2.4; `D-AS4-ARM64-BASE-INDEX-LEA` (the scale=1 form it extends); `D-LK10-ENTRY-ARM64-WIDE-IMMEDIATE` (sibling shifted-immediate precedent) |
| `D-AS-ENCODINGSLOT-EXHAUSTIVE-WARN` | ✅ **CLOSED 2026-06-09** (hygiene/lockstep cycle) — the four `src/asm/format/` walkers' `switch (EncodingSlotKind)` sites (`fixed32::windowFor`, `fixed32_disasm::windowFor`, `x86_variable::wireSlot`, `x86_variable_disasm::valueForSlot`) had drifted non-exhaustive (missing the slot kinds added since they were written) → emitted `-Wswitch` on the macOS / linux-clang CI legs but INVISIBLE on local MSVC. Made every switch EXHAUSTIVE (one explicit case per enumerator, BEHAVIOR-PRESERVING — each previously-drifted slot maps to the SAME result the trailing fallback returned: `nullopt` for the three optional-returning functions, fail-loud-`false`/`A_NoMatchingEncodingVariant` for `wireSlot`) with NO `default:` arm; kept the post-switch fallback as the out-of-range-ordinal backstop (also satisfies constexpr / GCC-Clang `-Wreturn-type` / MSVC C4715 — an enum switch is not treated as exhaustive for control flow). Added per-file `-Werror=switch` (GCC/Clang) / `/we4062` (MSVC) on the four files in `src/asm/CMakeLists.txt` (mirrors `D-OPT-RUNPASS-EXHAUSTIVE-WARN`), so a future `EncodingSlotKind` enumerator added without updating a walker is now a BUILD ERROR on local MSVC too. SCOPE: the gate covers the four format-walker TUs (the `-Wswitch` sources). Other `EncodingSlotKind` consumers compiled elsewhere — notably the header-inline `slotShapeFor` classifier in `target_schema.hpp` (a fifth no-`default` switch) — are NOT under this per-file flag; a new enumerator there falls to `slotShapeFor`'s trailing `None`, caught LOUDLY at load by `checkSlotShape`'s shape-`None` rejection (an opcode declaring the new slot is rejected — never a silent miscompile) rather than at compile time. Extending the gate to those consumers (or making `slotShapeFor` exhaustiveness `static_assert`-backed) is a future completeness item, NOT a correctness gap. | (closed) | `D-OPT-RUNPASS-EXHAUSTIVE-WARN` (the optimizer-runPass precedent it mirrors); `target_schema.hpp` `EncodingSlotKind` / `slotShapeFor`; plan 13 §2.4 (encoding shapes); `D-AS-DISASM-FIXED32-SLOT-FAILLOUD` (deferral surfaced by this cycle) |
| `D-AS-DISASM-FIXED32-SLOT-FAILLOUD` | ✅ **CLOSED 2026-06-24 (deferral loop, branch feature/0-0-2-p16).** The 10 fixed32/ARM64 slots (Imm16/Imm9/MemBaseNoScale/MemOffsetZero/Imm12/Imm12Scaled/Imm12HiLo24/Imm32MovzMovk/SymbolPatchMarker/Imm19) in `x86_variable_disasm.cpp` `valueForSlot` now converge onto the EXISTING `A_RoundTripMismatch` fail-loud arm (alongside Rd/Rn/Rm/Ra/Imm26); the genuinely-undecoded x86 slots (ModRmRmMem/MemBaseScale/Disp32Mem/SibIndex/RipRelDisp32/CondCodeNibble/BlockRel32) STAY nullopt (the disasm-completeness gap D-AS5-MULTIWORD-DISASM — valid x86 slots, "value undefined here", not cross-shape). `fixed32_disasm::windowFor`'s mirror site needs NO parallel loud arm: there a no-window slot sets shapeOk=false → the variant is SKIPPED → the final no-match fail-loud fires, so a cross-shape slot can never masquerade as a VALUE; only valueForSlot, which EXTRACTS a value, carries that hazard (decision documented in-code). Pinned RED-on-disable by `DisasmDefensive.Fixed32SlotInX86VariableWalkerFailsLoud` (test_asm_roundtrip.cpp) — a synthetic schema built via `detail::TargetSchemaData` DIRECTLY (bypassing validate(), the only way to reach this otherwise-unreachable arm) feeds an Imm16-wired x86-variable opcode to `disassembleInst` and asserts A_RoundTripMismatch fires (pre-fix the slot returned silent nullopt → no diagnostic → test fails; verified by toggle). 394 ctest green. — *Original row (audit trail):* 2026-06-09 — surfaced by the `D-AS-ENCODINGSLOT-EXHAUSTIVE-WARN` cycle: `x86_variable_disasm::valueForSlot` returns SILENT `nullopt` for the fixed32 slots Imm16/Imm9/Imm12/Imm19/MemBaseNoScale/SymbolPatchMarker, whereas their siblings Rd/Rn/Rm/Imm26 FAIL LOUD (`A_RoundTripMismatch`) on the same "fixed32 slot reached the x86-variable walker" condition (the documented silent-failure-hunter CRITICAL-2 convergence). Harmless today — every such arm is unreachable under a valid schema (validate() rejects cross-shape variants) — so the exhaustiveness cycle PRESERVED the silent-`nullopt` rather than change behavior (a behavior-preserving hygiene cycle must not also fix a latent defense-in-depth gap). Priority: **normal** (defense-in-depth consistency; not a valid-program miscompile). | Route the six fixed32 slots onto the existing Rd/Rn/Rm/Imm26 fail-loud arm in `valueForSlot` (and decide `fixed32_disasm::windowFor`'s parallel treatment); pin with a synthetic cross-shape variant that reaches the arm → asserts `A_RoundTripMismatch` (RED-on-disable: the silent-`nullopt` path lets a bogus round-trip masquerade as a SymbolRef slot). | `D-AS-ENCODINGSLOT-EXHAUSTIVE-WARN` (the cycle that surfaced + deferred it); `D-AS5-MULTIWORD-DISASM` (the sibling disasm-completeness gap); silent-failure-hunter CRITICAL-2 |
| `D-DIAG-CODENAME-EXHAUSTIVE-WARN` | ✅ **CLOSED 2026-06-09** (clean-PR hygiene — the 3rd instance of the exhaustive-switch gate pattern, mirrors `D-AS-ENCODINGSLOT-EXHAUSTIVE-WARN` + `D-OPT-RUNPASS-EXHAUSTIVE-WARN`). `src/core/types/parse_diagnostic.cpp::diagnosticCodeName` (the `DiagnosticCode → name` switch) had drifted non-exhaustive — **12** enumerators unhandled (the `X_*` optimizer/pipeline family: X_UnknownPassId / X_UnknownPassName / X_PipelineVersionMismatch / X_PipelineMalformed / X_PipelineNameResolutionFailed / X_OptPassSkipped / X_OptReturnFalseWithoutDiagnostic / X_InlineMalformedCallSite; plus A_FunctionEncodeAborted / K_AbsolutePointerRelocMissing / S_NotAComposite / S_NotAPointer) → `-Wswitch` on the macOS / linux-clang CI legs, INVISIBLE on local MSVC. Added the 12 explicit `case → name` arms (BEHAVIOR-PRESERVING: each previously fell to the trailing `return "Unknown"`; now returns its real enumerator name) with NO `default:`; kept the post-switch `return "Unknown"` as the out-of-range-ordinal backstop. Added per-file `-Werror=switch` (GCC/Clang) / `/we4062` (MSVC) on `types/parse_diagnostic.cpp` in `src/core/CMakeLists.txt` → a future `DiagnosticCode` enumerator added without a name arm is now a BUILD ERROR on local MSVC too (RED-on-disable demonstrated: removing the `X_UnknownPassId` arm → `error C4062`). SCOPE: the file's only two switches (`diagnosticCodeName` + `severityName`) are both no-`default` exhaustive; the gate is per-file — does NOT touch `grammar_schema.cpp` / `target_schema.cpp` etc. whose switches use subset-with-`default` forms. | (closed) | `D-AS-ENCODINGSLOT-EXHAUSTIVE-WARN` + `D-OPT-RUNPASS-EXHAUSTIVE-WARN` (the precedents it mirrors) |
| `D-ASM-PATCH-PARTIAL-OUTPUT-FAILLOUD` | Step 13.5 cycle 1 silent-failure HIGH #3 — `(void)encodeInst` laundering partial bytes + patch loop's `continue` past failures. | ✅ **CLOSED 2026-06-03** (commit `1ad61cd`) — funcEncodeOk + truncate partial bytes + drop entire function + `A_FunctionEncodeAborted=0x1006`. | plan 13; D-ASM-ENCODE-FAILURE-FUNCTION-ROLLBACK |
| `D-CAP-MARKER-COMPILE-DIR-PIN` | First time a multi-file `--directory` compile silently picks the wrong target subset. | Add a pin in the program test suite that asserts `compileDirectory`'s file enumeration produces the expected set; surface dropped files via a new `D_*` driver code. | plan 14 §LK10 driver |
| `D-CAP-MARKER-MULTI-TARGET-E2E-PIN` | First multi-target build (e.g. `--target x86_64:pe ... --target x86_64:elf`) lands without verifying each target's binary independently. | Examples_runner extension: per-target expectedStdout / exitCode columns; multi-target verifier reads each spec's row. | plan 14 §LK10 driver; example expected.json schema |
| `D-COMPILE-ONE-TARGET-NO-LEAK` | A future bug in `compileOneTarget` leaks state into the next compile (e.g. reused arenas, stale diagnostics). | Lifetime contract tests; arena ownership invariant pinned via `static_assert` on move-only + a per-call reset hook. | plan 14 §LK10 driver |
| `D-CSUBSET-BINOP-RIGHT-CLOBBER` | ✅ **CLOSED 2026-06-02** (commit `2a6e9fe`) via regalloc 2-addr-aware exclusion-of-operand-physregs from result-allocation. | (closed) | plan 12 ML6 regalloc; MEMORY.md project_d_csubset_binop_right_clobber_2026_06_02 |
| `D-CSUBSET-DEREF-LOADSTORE-SMOKE` | ✅ **CLOSED 2026-06-04** (cycle 10h) — first compile→run gate for Deref-as-rvalue (`*p`) AND Deref-as-lvalue (`*p = v`) under c-subset. Pre-10h only param-type pointer spelling (`int*`, `char*`) was corpus-exercised — the Deref operator itself had no source→.exe assertion. | New corpus fixtures `examples/c-subset/deref_load/` + `examples/c-subset/deref_store/` (one-arg helper `int read_through(int* p){return *p;}` and `int write_through(int* p){*p=42;return 0;}` paired with `int main()` materializing a local + `&x`). Two-fixture split for independent failure attribution: load-codegen bug surfaces to deref_load, store-codegen bug to deref_store. | plan 22 (alias arc capstone pre-req); D-OPT-ALIAS-ARC-CORPUS-CAPSTONE-STRICT |
| `D-CSUBSET-LONG-PRIMITIVE` | ✅ **CLOSED 2026-06-04** (cycle 10h) — adds `long` keyword → I64 core to c-subset.lang.json. Pre-10h c-subset declared only `int`(I32), `char`(Char), `void`(Void); no non-char wider primitive existed, so `mirMayAlias` Rule 6 (distinct non-char primitives → No alias) could not be exercised on any c-subset corpus row. | Four 1-line additions to c-subset.lang.json (keyword token + typeBase alt + typeForRef alt + builtinTypes) + I32→I64 sext encoding row added to x86_64.target.json (movsxd r64, r/m32 = REX.W 0x63 /r). Smoke fixture `examples/c-subset/long_primitive_smoke/` pins end-to-end with `0 - 1` sentinel (post-fold discriminator: positive small witnesses can't distinguish sext from zext or full-width from low-32 ABI return). | plan 22 (alias arc capstone pre-req); pairs with D-OPT-ALIAS-ARC-CORPUS-CAPSTONE-STRICT; D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH |
| `D-CSUBSET-STRUCT-MEMBER-DECLARATOR` | ✅ **CLOSED 2026-06-26 (p18 Cluster G c10).** struct/union FIELDS used a LEGACY positional schema (`name:1, type:0`) so a function-pointer member `int (*f)(int);` (the SQLite-pervasive shape that derailed MOST structs → the cascade of `};`/scope-unbalance errors) was rejected at the member declarator. FIX = switch `structField`/`unionField` to declarator mode (`head:0, declarator:1`, the SHARED `declarator` rule every other position uses) so the parenDeclarator group + fnSuffix derive the fn-ptr field type for free. AGNOSTIC (config rows + the generic declarator engine). | ✅ The struct-field path was deeply legacy-positional (plan-locked COMPLETE inventory of Pass-1/1.5 — the cascade-avoider): grammar→`declarator` + `bitfieldDeclSuffix` as an OUTER sibling; rows→declarator-mode; FAM via threading `allowFlexibleArray` through `declaratorDeclaredType`/`applyDeclaratorSuffix` (the group recursion passes FALSE — a nested fn-ptr param's array is not the field's FAM); NAMED bit-field width by porting `resolveBitfieldSuffix` into the declarator-mode Pass-1.5 loop (★ searched from the structField `node`, NOT the declarator — the sibling-suffix gotcha that breaks every bitfield if missed); ANONYMOUS fields (`int :3;`/`int ;`) by minting a `<anon:…>` symbol in Pass-1 (after the declarator loop, when no named symbol bound) + a Pass-1.5 anon block (bit-field width + `S_DeclarationDeclaresNothing`). Everything downstream (composition/intern, fieldIndex densify, union-variant, designated-init, member-access, brace-init anon-skip) is MODE-INDEPENDENT (reads the symbol record) → no cascade. WITNESS: corpus `struct_fnptr_member` (member assign + CALL through → exit 42; pe-native + elf/macho/arm64) + 416/416 ctest. | p18 Cluster G c10; cited in `src/dss-config/sources/c-subset.lang.json` (structField/unionField) + `src/analysis/semantic/semantic_analyzer.cpp` (FAM/bitfield/anon) + plan-23 §3.1 F6; pairs with the FC7 struct mechanism + `D-CSUBSET-STRUCT-FNPTR-BRACE-INIT` (the brace-init follow-on). |
| `D-CSUBSET-STRUCT-FNPTR-BRACE-INIT` | ✅ **CLOSED 2026-06-26 (p18 Cluster G c12, LOCAL case)** — discovered p18 c10. A struct fn-ptr member resolves + member-assigns + CALLS (c10), but LOCAL BRACE-INIT (`struct Ops a = { dbl, 5 };`) FAILED LOUD at the HIR verifier: `H_VerifierFailure: ConstructAggregate child[0] type N ≠ field type M`. ★ ROOT (NOT two distinct FnSigs — both the declarator-`fnSuffix` and the function-decl path intern the SAME `FnSig` via the shared `collectParamTypes`+`normalizeSoleVoidParams` chokepoints + `CcSysV`): a function-to-pointer DECAY GAP — the field is `Ptr<FnSig>` but the bare function name `dbl` stayed an undecayed `FnSig` DESIGNATOR (`operands(fieldPtr)[0]` IS that FnSig). Member ASSIGNMENT passes via `isAssignable`'s decay arm (which emits no node — a bare fn Ref inherently lowers to its ADDRESS at MIR), but a brace-init element must carry the field's `Ptr<FnSig>` TYPE for `ConstructAggregate`'s strict child==field equality. | ✅ Fix = option (a) TIGHT: CST→HIR `coerce` gained an `FnSig → Ptr<FnSig>` decay arm (a synthetic `Cast` lowered as a representation-free MIR `Bitcast` over the function's GlobalAddr, `mapCast(FnSig,Ptr)→Bitcast`), gated on the SAME same-signature predicate `isAssignable` uses (`tk==Ptr && operands(target)[0]==child.type`) — a SIGNATURE MISMATCH stays a loud `ConstructAggregate` verifier failure (proven: field `int(*)(int)` init with `int(int,int)` → still red). WITNESS: corpus `struct_fnptr_brace_init` (two distinct fn-ptr members + scalar field + call-through → exit 42; pe-native + elf/macho/arm64 + release arm). ★ RESIDUAL (separate anchor `D-CSUBSET-GLOBAL-AGGREGATE-RELOC-MEMBER`): a FILE-SCOPE/GLOBAL aggregate whose member is a RELOCATION (a fn-ptr `{dbl}` OR a data-ptr `{&x}`) is a PRE-EXISTING limitation (NOT a fn-ptr-specific issue, NOT this cycle's regression). | p18 Cluster G c12; cited in `examples/c-subset/struct_fnptr_brace_init/` + `src/hir/lowering/cst_to_hir.cpp` (`coerce` FnSig→Ptr arm) + `src/mir/lowering/hir_to_mir.cpp` (`mapCast` FnSig→Ptr Bitcast); follows on from `D-CSUBSET-STRUCT-MEMBER-DECLARATOR` (c10). |
| `D-CSUBSET-GLOBAL-AGGREGATE-RELOC-MEMBER` | **OPEN — pre-existing limitation surfaced p18 c12, 2026-06-26; fail-LOUD, normal.** A FILE-SCOPE/global aggregate (`struct`/array) whose initializer contains a RELOCATION-bearing member — a function pointer (`struct Ops g = { dbl };`, the SQLite VFS/method-table vtable shape) OR a data pointer (`struct P g = { &x };`) — fails LOUD: `H_UnsupportedLoweringForKind ... aggregate compound-literal/initializer must be lowered into a slot by ADDRESS ... (D-CSUBSET-BITFIELD-RVALUE-RUNTIME)`. The global-init path (`hir_to_mir.cpp` `tryClassifyAsSymbolAddr`) recognizes only SCALAR symbol-address inits (`int* p = &x;` AddressOf(Ref), `char* g = "...";` Cast(string)) — an aggregate `ConstructAggregate` initializer returns nullopt, const-eval can't fold a reloc, and the runtime-init path can't store an aggregate RVALUE. NOT fn-ptr-specific (a `{&x}` data-ptr member fails identically); NOT a silent miscompile (fails loud); the LOCAL case works (runtime store-into-slot). The aggregate analog of F5's scalar symbol-address globals. | Closing work = extend the global-aggregate emitter to lay out per-FIELD data items with per-member abs64 relocations (mixed const + symbol-address members at their byte offsets — `lowerMirGlobalsToDataItems` per-field, the F5 scalar path generalized to aggregate members). Pin: `int x; struct P g = { &x }; … *g.p` → exit 42 AND `struct Ops g = { dbl }; … g.op(21)` → exit 42, both red-on-disable. | Surfaced p18 Cluster G c12 (`struct_fnptr_brace_init` deferral note); cited in `examples/c-subset/struct_fnptr_brace_init/expected.json`; `src/mir/lowering/hir_to_mir.cpp` (`tryClassifyAsSymbolAddr`/`emitGlobals_`); the aggregate analog of `D-CSUBSET-SYMBOL-ADDRESS-GLOBAL` (F5). |
| `D-CSUBSET-PER-TARGET-STRUCT-LAYOUT` | ✅ **MECHANISM DONE 2026-06-26 (p18 Cluster G c13, plan 25); ALL 6 linux POSIX headers DONE (c14a-e); the per-target `variants` axis EXTENDED to constants/typedefs/macros + macOS errno PROVEN (c15a 187a54a / c15b 73c5763); remaining 5 macOS headers OPEN (c15c).** §B decision (user, 2026-06-26): build the FULL per-target struct-layout mechanism (not linux-x86-64-only) so macOS/arm64 SQLite are reachable without reworking descriptors. THE GAP: shipped-library `structs` were single-layout, but a field-accessed struct's byte layout DIVERGES per target — glibc `struct stat` is 144B on x86-64-linux (st_nlink 64-bit, st_mode@24 after __pad0) but 128B on arm64-linux (st_mode@16, st_nlink 32-bit) and different again on macOS (even different field NAMES: st_mtimespec vs st_mtim). libc writes the REAL struct + we read fields at OUR offsets → one layout SILENTLY corrupts data on the other targets. ★ CRUX (plan-lock-verified vs the code): x86_64/arm64 target.json `AggregateLayoutParams` BYTE-IDENTICAL (Natural+16) + `computeLayout` param-driven (no arch branch) → per-target offsets come ENTIRELY from the FIELD LIST → per-target layout = per-target field-LIST SELECTION in the decoder; injection + layout engine UNCHANGED; selection at semantic time does NOT multiply the c9 once-per-format-kind build. | ✅ MECHANISM: `ShippedStruct` gains optional `variants:[{when:{arch?,format?},fields}]` (XOR flat `fields`); `analyze()` gains `optional<string_view> activeTarget`=`target.name()` (nullopt=back-compat → flat decode, variants-only not injected); decoder EAGER-decodes all variants (anti-lurking, mirrors `signatureByDataModel`), matches `when` by GENERIC string-equality (AGNOSTIC), >1 match → `F_ShippedStructVariantAmbiguous` (0x501E, **kUnsuppressableCodes**), 0 match → not injected (reference → S_UnknownType); a variants-only descriptor DECLARES a struct surface. FIRST consumer = `sys/stat.json` `struct stat` x86_64-elf 144B + arm64-elf 128B (gated elf). ✅ DONE c14a-e: the full os_unix surface authored behind the mechanism — errno (E*+errno macro), fcntl (O_/F_ + struct flock + variadic open/fcntl via the c14c variadic-externs FFI extension), sys/stat (S_ + fstat/lstat/mkdir + S_IS* + st_*time macros), unistd (~28 fns + SEEK_/access), sys/mman (PROT_/MAP_ + mmap/munmap/mremap), pthread (fns + pthread_mutex_t shipped as a flat max-size 48B opaque typedef sidestepping the per-arch size). ✅ DONE c15a/c15b: the `variants` axis EXTENDED from structs to constants/typedefs/macros (shared `matchVariantWhen`; macros are format-only — arch isn't threaded into preprocess; 3 ambiguous-fail-loud diagnostics 0x501F-0x5021 + kUnsuppressableCodes 64→67) + `errno.json` proven on macOS (the `errno` macro → `(*__error())` macho vs `(*__errno_location())` elf; the divergent E* per-format; corpus `shipped_errno_macho` runs on the macos-latest leg). ✅ DONE c15c (2026-06-26, 433/433): the remaining 5 macOS headers (sys.mman/unistd/fcntl/sys.stat/pthread macho variants) — value-verified vs apple-oss-distributions/xnu+libpthread; the macho `struct stat` 144B Darwin (SAME SIZE as x86-64-elf, reordered → corpus pins st_size@96 FIELD OFFSET not sizeof — the c13 same-size-swap) + `struct flock` 24B (reordered) + pthread_mutex_t 64B/mutexattr 16B typedef variants (FIRST real consumer of c15a typedef-variants) + the O_/F_ divergences + PTHREAD_MUTEX_INITIALIZER __sig magic 0x32AAABA7; mremap/MREMAP_MAYMOVE/O_TMPFILE elf-only fail-loud; fdatasync/fallocate unref-externs. 4 corpus (macos-latest leg). pr-review caught + folded a __sig hex→dec typo. **ALL 6 POSIX headers serve BOTH formats; macOS POSIX = linux completeness.** This anchor's mechanism (c13) + the full os_unix authoring (linux c14 + macOS c15) are COMPLETE; the sqlite3.c re-probe is the user's downstream step. Pin: per-arch sizeof/offset (gate 6 — `shipped_struct_stat_{x86,arm64}` corpus CI + `RealSysStatPerArchLayout` unit) red-on-disable. | p18 Cluster G c13; plan 25; `src/ffi/shipped_lib_descriptor.{hpp,cpp}` + `src/analysis/semantic/semantic_analyzer.{hpp,cpp}` + `src/program/compile_pipeline.cpp` + `src/dss-config/shippedLibs/sys/stat.json` + `examples/c-subset/shipped_struct_stat_{x86,arm64}` + `tests/ffi/test_shipped_lib_descriptor.cpp`; builds on FC7 struct mechanism + c8/c9 availability; macOS constants/typedef variance extends the (scalar-closed) `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH` theme onto this axis. |
| `D-CSUBSET-EAST-CONST-CAST` | ✅ **CLOSED 2026-06-26 (p18 Cluster G c11).** A cast/sizeof type-name `(char const *)` — EAST-const (const BETWEEN the base type and the stars) — was rejected (P0009) while west-const `(const char *)` + the DECLARATION head `char const *p` already worked: the `castTypeRef` grammar had const slots only PRE-base + POST-stars, not POST-base-pre-stars (a decl puts the `*` in the declarator so its head `char const` parses; a cast/sizeof bundles base+stars in `castTypeRef`). SQLite casts pointers to const constantly (`(unsigned char const *)` — 50+ in the sqlite3.c compile.log). | ✅ Added an optional POST-base `ConstKeyword` to `castTypeRef` (`[const?, base, const?, star*]`); DROPPED the prior POST-stars const (`char * const` cast — a useless form: const on an rvalue pointer ≡ no const) to keep it UNAMBIGUOUS (two const optionals straddling the REQUIRED base token; two adjacent across the zero-star case is `C_AmbiguousAlternatives`). The const resolver IGNORES const for type identity (counts StarOp tokens + the base — line 813-831), so all forms intern the SAME pointer type → GRAMMAR-ONLY, no semantic change. WITNESS: corpus `east_const_cast` (`(unsigned char const *)` cast + deref → exit 42; pe-native + elf/macho/arm64). ★ The "DROPPED the prior POST-stars const … to keep it UNAMBIGUOUS" decision is SUPERSEDED by `D-CSUBSET-POST-STAR-CAST-QUALIFIER` (c29): the post-star qualifier is now ADMITTED by riding inside `pointerLayer` (present only WITH a star → no zero-star adjacency), so the c11 ambiguity argument no longer forces dropping it. | p18 Cluster G c11; cited in `src/dss-config/sources/c-subset.lang.json` (castTypeRef); a sqlite3.c parse gap from the user's compile.log probe; pairs with `D-CSUBSET-STRUCT-MEMBER-DECLARATOR` (c10). |
| `D-CSUBSET-POST-STAR-CAST-QUALIFIER` | ✅ **CLOSED 2026-06-27 (p19 Cluster G c29).** ROOT (sqlite3.c:106644, the `sqlite3MemCompare`/`vdbeRecordCompareInt` pattern): `const u8 * const p1 = (const u8 * const)pKey1;` failed P0009 "expected expression — got 'const'" at the POST-STAR `const` (the pointer's own east-const). `castTypeRef` had `{repeat StarOp}` — BARE stars, no post-star qualifier slot — and c11 had DELIBERATELY dropped a post-stars-const optional citing `C_AmbiguousAlternatives` (a 2nd post-stars optional adjacent to the post-base one across the zero-star case). ★ THE AMBIGUITY WAS THE KEY RISK — VERIFIED EMPIRICALLY BEFORE the impl (milestone-1): the c-subset schema LOADS CLEAN (no `C_AmbiguousAlternatives`) with the fix, because the qualifier rides INSIDE the pointer layer, present ONLY with a star — there is NO zero-star adjacency (unlike a separate post-star optional). FIX = change `castTypeRef`'s `{repeat StarOp}` → `{repeat pointerLayer}` (`pointerLayer` = `StarOp {repeat ptrQualifier}`, the SAME generic layer every declarator position uses; `ptrQualifier` = const/volatile/restrict). The resolver's co-located cast/typeRef arm counts each `pointerLayer` child as ONE pointer level for `ptrDepth` (alongside the still-supported bare-`StarOp` token form `typeRef`/`sizeofType` keep) and STRIPS the layer's ptrQualifiers — a cast yields an RVALUE with NO top-level cv (C 6.5.4): `(int * const)p` and `(int *)p` intern the SAME `Ptr<int>`, and a post-star VOLATILE is dropped (NO `VolatileQual` on the cast POINTER). ★ KEY CORRECTNESS POINT: the c27 PRE-stars-volatile POINTEE path (`volatile u32 *`→`Ptr<VolatileQual(u32)>`, the head-volatile-before-the-first-star arm) is SEPARATE and UNBROKEN — the star-run boundary for the `baseIsVolatile` scan is now the first `pointerLayer` child, so a post-star volatile inside a layer can NEVER reach the base-volatile wrap (the declaration path's east `* volatile`→`VolatileQual(Ptr<…>)` object-volatile is a DIFFERENT engine, `declaratorDeclaredType`, and stays). AGNOSTIC: reuses the generic `pointerLayer`/`ptrQualifier` rules + `cfg.pointerToken`/`DeclaratorConfig.pointerLayerRule` (no lang/arch/format/keyword hardcode). WITNESS: 4 parser pins (exact `pointerLayer`-nested shape for `(const u8 * const)`/`(u32 * volatile)`, the 5-form swap-keeps-parsing battery incl. `(volatile u32 **)p`, updated `(int*)`/`(char const *)` shape pins) + 4 semantic TypeId pins (`(int * const)p`≡`(int *)p` SAME id; `(u32 * volatile)p` no pointer VolatileQual; `(volatile u32 *)p` keeps pointee VolatileQual c27-unbroken; `(volatile u32 **)p`→`Ptr<Ptr<VolatileQual(u32)>>`); frontier repro `(const u8 * const)pKey1` compiles clean via the CLI → elf64 x86_64-linux. NO new runtime corpus — a cast top-level-cv strip is a no-op at runtime (the TypeId pins are the strict witness; a vacuous corpus is not shipped). 451 ctest UNCHANGED (the 8 new TEST() pins land in existing gtest binaries; no new corpus → no new ctest entry). | (closed — the `pointerLayer` reuse + the resolver strip IS the fix) | p19 Cluster G c29; cited in `src/dss-config/sources/c-subset.lang.json` (`castTypeRef` `{repeat pointerLayer}`) + `src/analysis/semantic/semantic_analyzer.cpp` (the co-located cast/typeRef arm — `isPointerStar` lambda counting pointerLayer + bare StarOp, the strip); SUPERSEDES the c11 `D-CSUBSET-EAST-CONST-CAST` "dropped post-stars const" decision; builds on c27 `D-CSUBSET-VOLATILE-POINTEE` (the pre-stars pointee path) + c26 `D-CSUBSET-ABSTRACT-DECLARATOR-TYPE-NAME` (the abstract tail after the layers); a sqlite3.c re-probe frontier; plan 23 full-C arc / SQLite-green. |
| `D-CSUBSET-LOCAL-TYPEDEF` | ✅ **CLOSED 2026-06-27 (p19 Cluster G c30).** sqlite3.c:187603 (the `sqlite3_config` / `SQLITE_CONFIG_LOG` path) — a BLOCK-SCOPED typedef as a STATEMENT inside a function: `typedef void(*LOGFUNC_t)(void*,int,const char*); LOGFUNC_t xLog = va_arg(ap, LOGFUNC_t);` failed P0009 "got 'typedef'" — `typedef` parsed ONLY at top level (`typedefDecl` in `topLevelDecl`); the block-item `statement` rule did not admit it (2nd instance 191331 `typedef int(*sqlite3FaultFuncType)(int);`). | ✅ FIX = a grammar line + a one-line HIR→MIR no-op. (1) GRAMMAR: add `typedefDecl` to the `statement` alt (FIRST = `TypedefKeyword`, DISJOINT from every other statement alt → unique-production direct descent, NO speculation; schema loads clean — no `C_AmbiguousAlternatives`). The ENTIRE typedef-name machinery was ALREADY scope-keyed → CLEAN EXTENSION, NO architectural change (no §B): Pass-1 binds the alias into `current` (= the enclosing block scope) in the Ordinary namespace; the type resolver (`resolveTypeNodeImpl` SE5 arm via `s.scopes.lookup`) AND the parse-time oracle (`BinderSketch`, which already opens/closes `block` scopes) both walk the scope chain → a block typedef resolves IN the block, SHADOWS an outer same-name typedef, and does NOT leak out (CLI-PROVEN: an outer use of the block-local name fails S_UnknownType, IDENTICAL to never-defined). (2) HIR→MIR: CST→HIR's `lowerTypeDecl` already MINTS the TypeDecl node, but `lowerStmtNode` (the leaf-statement dispatch) had NO statement-position TypeDecl case → added `case HirKind::TypeDecl: return true;` (a typedef emits no code; mirrors the TOP-LEVEL TypeDecl no-op). ★ The architecture explorer's "0 downstream lines" claim was WRONG here — the milestone-1 end-to-end CORPUS caught the missing lowering (H0009 H_UnsupportedLoweringForKind at the typedef); the analyze-only semantic pins did not (the c10 lesson: an exploration's "no downstream effects" is a CLAIM). An independent adversarial audit then confirmed clean lowering in EVERY statement context (if/for/while/switch arm, anon-struct/tag typedefs, fn-ptr call-through) + the non-leak + the release pipeline. Mirrors c28 (block-scoped composite definition). WITNESS: 4 semantic pins (node-shape statement-nested `typedefDecl` + alias→`Ptr<Fn>`; the sqlite `LOGFUNC_t` frontier shape; ★ BLOCK-SCOPE NON-LEAK — an outer use of the block-local typedef-name is a PARSE error, because a leak would corrupt the `BinderSketch` oracle into parsing the name as a type; SHADOW — an inner typedef resolves DISTINCT from an outer same-name one) + runtime corpus `local_typedef_fnptr` (a block-scoped fn-ptr typedef + call-through → exit 42; 4 targets + release). 451→452 ctest; anchor guard OK. | p19 Cluster G c30; cited in `src/dss-config/sources/c-subset.lang.json` (`statement` alt); a sqlite3.c re-probe frontier; mirrors c28 `D-CSUBSET-LOCAL-TYPE-DEFINITION`; plan 23 full-C arc / SQLite-green. |
| `D-CSUBSET-LABEL-BUDGET-CLIFF` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c31).** sqlite3.c:213922 — a C label before a LARGE statement: `json_parse_restart:` preceding a ~900-case JSON `switch` failed P0001 "got ':'". ROOT: `declOrExprStmt` (`alt:[labelStmt,identVarDecl,exprStmt]`, speculative lookahead 256 ⇒ a `lookahead*16` = 4096-token probe budget) speculatively parses `labelStmt = Identifier Colon statement` to COMPLETION; a labeled statement >4096 tokens exhausts the probe budget → rollback → exprStmt → P0001 at the `:` (confirmed empirically: a small labeled switch parses, a ~900-case one does not). | ✅ FIX = a NEW general parser facet `commitAfterPrefix` (a PEG "cut"), AGNOSTIC (names no token/rule/keyword/language). `labelStmt` KEEPS its `Identifier Colon statement` shape (so a label stays correct in EVERY context, incl. a braceless control-flow body — `if(x) L: s;` ⇒ `s` IS the if's body). The facet: once a candidate's FIXED leading token-prefix (`predictivePrefixLen` — here `Identifier Colon` + the first `statement` token) is consumed in a speculative probe WITHOUT failure, the parser COMMITS (`probe.commit()`) and hands the STILL-OPEN frame to the non-speculative dispatch loop — the rest of the rule parses with NO budget (exactly the unique-production direct-descent's "open frame, let the outer loop drive it"). SOUND because after `Identifier Colon` no other `declOrExprStmt` alt can match (neither identVarDecl nor exprStmt has Colon in 2nd position), so committing discards nothing; a non-label `foo + bar;` fails at the `Colon` slot BEFORE the prefix is consumed, so the cut never fires (falls through to exprStmt). Wiring: `CompiledRule::commitAfterPrefix` (bool) ← loader parses an optional boolean `commitAfterPrefix` on the shape body (fail-loud if non-boolean; mutually-exclusive with `commitRequiresTypeName`); `GrammarSchema::commitAfterPrefix(RuleId)` accessor; the cut block inside `trySpeculativeBranch`'s probe loop after the post-`stepOnce` diag/desync rechecks; `SpeculationProbe::startPos()` accessor. Only `labelStmt` carries the flag (`"commitAfterPrefix": true`). NO HIR/MIR/verifier/lowering change (`git diff --stat`: 6 files — parser.cpp, compiled_shape.hpp, grammar_schema.{hpp,cpp}, grammar_schema_json.cpp, c-subset.lang.json). ★ BRACELESS-BODY CORRECTNESS (the case that doomed any flatten-labelStmt alternative) VERIFIED at runtime: `if(x) L: g=42;` exits 5 for x=0 (the labeled `g=42` is the if's body and is NOT executed) and 42 for x=1. WITNESS: 2 HIR pins (`LabelBeforeOversizeStatementParsesPastProbeBudget` — a label before a ~5500-token block parses clean, red-on-disable ⇒ P0001; `LabelAsBracelessIfBodyStaysInsideTheIf` — the silent-miscompile guard: the if's then-branch IS the LabelStmt with the assignment UNDER it) + runtime corpus `label_before_switch_goto` (a `goto` INTO a label before a multi-case switch, fold-resistant via feed() + 3 goto passes → exit 42; 4 targets pe/elf/qemu-arm64/macho + release arm). 452→453 ctest; anchor guard OK. | (closed — the `commitAfterPrefix` PEG-cut facet IS the fix) | p19 Cluster G c31; cited in `src/dss-config/sources/c-subset.lang.json` (`labelStmt` `commitAfterPrefix`), `src/core/types/compiled_shape.hpp` + `src/core/types/grammar_schema.hpp` + `src/core/types/grammar_schema_json.cpp` (the facet + loader), `src/analysis/syntactic/parser.cpp` (the cut); a sqlite3.c re-probe frontier; sibling facet to the FC2 `commitRequiresTypeName` type-name commit guard; plan 23 full-C arc / SQLite-green. |
| `D-CSUBSET-FNPTR-PARAM-SCOPE` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c32).** sqlite3.c:1175-1176 (`struct sqlite3_io_methods`) — the PARAMETER NAMES of a function-POINTER declarator collided across sibling declarators AND leaked into the enclosing scope: `int (*xRead)(... int iAmt ...); int (*xWrite)(... int iAmt ...);` → `error[S0002] S_RedeclaredSymbol: got iAmt`. Confirmed forms: fn-ptr struct members / fn-ptr typedefs / fn-ptr params with a shared param name all collided; `typedef int (*A)(int gv); int gv;` collided AND `…; int main(){return gv;}` resolved `gv` to the PARAM (a LEAK). ROOT: param names bind in Pass-1 `pass1Node`'s declarator-mode mint loop into `bindScope=current`; `pass1Node` opened a child scope ONLY for a `semantics.scopes` rule node — `["block","topLevelDecl","structSpec","unionSpec","enumSpec"]`. A plain prototype is a `topLevelDecl` (a scope rule) so its params isolate per-prototype, but a fn-POINTER declarator's params live under `fnSuffix`→`parenDeclarator`/`structField`/`typedefDecl`/`param` — none a scope rule — so they bound into the enclosing struct/file/block scope and collided/leaked. C 6.2.1p4: a param in a NON-definition declarator has function-prototype scope (per-declarator, terminating at the declarator end); a DEFINITION's params reach the body purely via the parent chain (`S_body → S_tld(topLevelDecl)`) with NO param re-binding, so they MUST keep binding into the topLevelDecl scope. | ✅ FIX (Option B — a per-declarator function-prototype scope; config-driven, agnostic). (1) CONFIG: a NEW OPTIONAL declarator role `prototypeParamScopeRule: "paramList"` (beside `fnSuffixParamsRule`; a language OMITTING it = prior behavior — toy/tsql declare no `declarators` block). (2) STRUCT+LOADER: `DeclaratorConfig::prototypeParamScopeRule` (+ name field) loaded via the same `readOptionalRuleRole` discipline + added to the closed-keys set (15→16). (3) SCOPE-OPEN: Pass-1 opens a THROWAWAY scope for any node of that rule UNLESS it is a function DEFINITION's OWN param list — detected by the SHARED config-driven shape tests (`directDeclaratorIsNameBearing` — the suffix sits on a NAME-bearing direct declarator, NOT a fn-pointer whose base is a `parenDeclarator`) AND `declNodeIsFunctionDefinition` (the enclosing decl's kindByChild resolves to Function — a body present). So: a fn-ptr member/typedef/param ⇒ base is a parenDeclarator ⇒ ISOLATE; a bare prototype ⇒ name-bearing but no body ⇒ ISOLATE (its name re-homes to file scope via the separate `isProto` path); a definition ⇒ name-bearing + body ⇒ do NOT open (params bind into the topLevelDecl scope, body-visible). (4) ★ 3-PASS CONSISTENCY: the whole "does this node open a child scope (and which)" decision is FACTORED into ONE shared predicate `nodeOpensChildScope` (folding the c25 `scopeByRule`+`isDefinitionAtNode` gate AND the c32 prototype case) used by ALL THREE passes — `pass1Node` (pushes), `resolveDeclTypes` + the `pass2` driver (re-derive by anchor lookup) — so a param can never bind in one scope yet be looked up in another. The fn-ptr's TYPE (FnSig param types via `collectParamTypes`, which walks the tree not the name symbols) is UNCHANGED — only NAME scoping changes. | (closed — the per-declarator prototype scope + the shared `nodeOpensChildScope` predicate IS the fix) | p19 Cluster G c32; cited in `src/dss-config/sources/c-subset.lang.json` (`declarators.prototypeParamScopeRule`) + `src/core/types/semantic_config.hpp` (`DeclaratorConfig::prototypeParamScopeRule`) + `src/core/types/grammar_schema_json.cpp` (loader) + `src/analysis/semantic/semantic_analyzer.cpp` (`nodeOpensChildScope` / `isPrototypeParamScopeNode` / `directDeclaratorIsNameBearing` / `declNodeIsFunctionDefinition`); witness = 9 semantic pins (`FnPtrStructMembersSharedParamNameNoRedecl`, `FnPtrTypedefsSharedParamNameNoRedecl`, `FnPtrParamsSharedParamNameNoRedecl`, `FnPtrParamNameDoesNotLeakToEnclosingScope` (collision-form leak) + `FnPtrParamNameDoesNotResolveOutsideDeclarator` (the DECISIVE resolve-form leak guard — an outer use of a fn-ptr-typedef param name is S_UndeclaredIdentifier; distinguishes a leaked-but-non-colliding param the type-only collision pin cannot), `NestedFnPtrParamsSharedNamesNoRedecl`, `DefinitionParamsRemainBodyVisible`, `PlainPrototypesSharedParamNameClean`, `DuplicateParamNameInSingleFnPtrStillCollides` — 7 red-on-disable, the 2 PRESERVE pins green both ways) + runtime corpus `fnptr_struct_dispatch` (a struct of fn-ptr members with SHARED param names, assigned + called through → exit 42; 4 targets pe/elf/qemu-arm64/macho + release). 453→454 ctest; reuses the c10 `hasFnSuffixOnName` / `isDefinitionAtNode` machinery; a sqlite3.c re-probe frontier; plan 23 full-C arc / SQLite-green. |
| `D-CSUBSET-ARRAY-SIZE-INFERENCE` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c34).** sqlite3.c:506 `SQLITE_API const char sqlite3_version[] = SQLITE_VERSION;` (i.e. `const char x[] = "3.x.y";`) — an array declared with an EMPTY bound `[]` AND an initializer was rejected `error[S000B] S_NonConstantArrayLength` (the resolver returned an incomplete array ONLY in a flexible-array-member context; a `[]` VARIABLE with a missing length always erred). C 6.7.9p22: such an array infers its length from the initializer — `char x[] = "abc"` → `char[4]` ("abc"+NUL), `int a[] = {1,2,3}` → `int[3]`. | ✅ FIX (config-driven, REUSE; one completion site so all downstream agree). (1) The array-suffix `[]` (absent length) already yields an INCOMPLETE array when "missing-length-is-OK" (`allowFlexibleArray`); REUSE that path for a VARIABLE by RELAXING the flag at the Pass-1.5 var/global declared-type site (`resolveDeclTypes`, semantic_analyzer.cpp) to `decl.allowFlexibleArray || <this declarator has an initializer>` — so a `[]`-with-init resolves to `T[]` (incomplete) instead of erroring, while a `[]` with NO init keeps the original flag (false for a var/global rule) ⇒ `S_NonConstantArrayLength` still fires (never a silently-sized array). The group recursion into a nested declarator still passes false (a nested fn-ptr param's `[]` errors). (2) A NEW helper `completeIncompleteArrayFromInit` (Pass-1.5, the SINGLE completion site → sizeof/layout/element-access/BOTH HIR paths observe the SAME sized array) completes the incomplete array: a STRING-literal init → N = decoded-bytes + 1 (the NUL), counted via the SHARED `decodeAdjacentStringBodies` chokepoint (escapes count once — `"\x41\x42"` → 2+NUL=3; adjacent concat `"ab" "cd"` → 5); a BRACE init → N = the count of TOP-LEVEL `initElement` children of the brace list. The element type is recovered from the incomplete array's `operands()[0]`, so a malformed escape leaves it incomplete (no guessed size; the HIR tier fails loud on the bad literal). NO HIR/MIR change — the existing explicit-size lowering (`char x[4]="abc"`, `int a[3]={…}`) applies once the type is completed to the same sized array (the HIR var/global path reads the symbol's resolved `.type`). AGNOSTIC: two new by-NAME index rules (`braceInitList`/`initElement`, resolved like `stringLiteralExprRule`) + the existing `stringLiteralExprRule`/body-token; no lang/arch/format/keyword hardcode. PRESERVE-verified: explicit `[N]` unchanged (completion is a no-op on an already-sized array — `char x[8]="abc"` stays `[8]`, not re-derived to 4); over-long explicit `char x[2]="abc"` keeps its current S0003; a bare `[]` no-init stays incomplete/error. ★ AUDIT-CAUGHT REGRESSION (fixed before commit): an EMPTY brace `int a[] = {}` (top-level count 0) returned the incomplete array with NO diagnostic — and the unguarded HIR/MIR tier LOOPED on the -1 sentinel length (a compiler HANG; a fail-loud VIOLATION the relaxation introduced — pre-c34 it was a clean S_NonConstantArrayLength). FIX: `completeIncompleteArrayFromInit`'s un-sizable returns (empty brace; OR no string/brace initializer found at all) now call `failUnsized()` → emit S_ArrayLengthOutOfRange (an inferred 0/undeterminable length = the non-positive `int a[0]` case) + return InvalidType, matching the no-init clean-error path (no hang). WITNESS: 6 semantic pins (`ArraySizeInferredFromStringInit` Array len 4 + NOT incomplete; `ArraySizeInferredFromBraceInit` Array<I32,3>; `ArraySizeInferredFromInitLocal`; `ExplicitArraySizeUnchangedByInference`; `EmptyArrayNoInitNotSized` — no-init S_NonConstantArrayLength; ★ `ArraySizeInferenceEmptyBraceFailsLoud` — empty-brace `{}` → S_ArrayLengthOutOfRange, NOT a hang) + runtime corpus `array_size_inference` (an inferred `char s[]="DSS"`, `int a[]={2,7,11,22}`, and the sqlite frontier shape `const char ver[]="9"` read across a non-inlined call → exit 42; 4 targets pe/elf/qemu-arm64/macho + release arm). 455→456 ctest; anchor guard OK. ★ NOTE (pre-existing, NOT c34): `sizeof(a[0])` mis-resolves to `sizeof(a)` for ANY array (reproduces with explicit `int a[5]`) — independent of inference; size correctness was proven via an out-of-the-explicit-bound read (`int a[]={0,0,0,0,42}; a[4]`→42) + `sizeof(x)`→4 for the string. | (closed — the relaxed-flag REUSE + the single `completeIncompleteArrayFromInit` site IS the fix) | p19 Cluster G c34; cited in `src/analysis/semantic/semantic_analyzer.cpp` (the relaxed `allowIncomplete` at the Pass-1.5 var site + `completeIncompleteArrayFromInit` + the `braceInitList`/`initElement` index rules); a sqlite3.c re-probe frontier (sqlite3_version line 506); reuses the FAM incomplete-array path (`D-CSUBSET-STRUCT-MEMBER-DECLARATOR` c10) + the adjacent-string decoder (`D-CSUBSET-ADJACENT-STRING-CONCAT` c20); plan 23 full-C arc / SQLite-green. |
| `D-CSUBSET-UNARY-PLUS` | ✅ **CLOSED 2026-06-26 (p18 Cluster G c12).** The unary `+` operator (`int x = +1;`, C 6.5.3.3) was rejected (P0009) — only unary `-` was in the prefix op-group; SQLite uses `+` as a no-op sign/grouping marker. | ✅ Grammar = add `"+"` to the prefix operator-group + a `PlusOp → Pos` `unaryOps` row. CST→HIR's `combineUnaryOp` lowers `+x` to its OPERAND DIRECTLY (identity — there is no `Pos`/`Identity` HirOpKind; the integer promotion is already realized by the lazy-consumer model, sub-int values live promoted in 32-bit regs), gated on an ARITHMETIC (or Enum) operand — a pointer/struct operand fails LOUD (`H_UnsupportedLoweringForKind` "operand of unary '+' must have arithmetic type"; STRICTER than `-`, which has no such gate). The semantic typer's `combineUnary` makes `Pos` operand-preserving (explicit, mirroring PreInc). `#if +1` works for free (`pp_if_eval` already special-cased a `Pos` target as identity). GRAMMAR + thin HIR, AGNOSTIC (config target name `Pos`, no hardcoded `+`). NOTE: making `+` a prefix op made `(a) + b` record a type-name candidate (the cast-vs-binary ambiguity) exactly like the pre-existing `(q) - x` — the `ParserCastExpr` pin was updated to match. WITNESS: corpus `unary_plus` (`+1`, `+(x+40)`, `+ +x` → exit 42; pe-native + elf/macho/arm64 + release arm). | p18 Cluster G c12; cited in `examples/c-subset/unary_plus/` + `src/dss-config/sources/c-subset.lang.json` (prefix group + `Pos` unaryOps row) + `src/hir/lowering/cst_to_hir.cpp` (`combineUnaryOp`). |
| `D-PP-IF-CHAR-CONSTANT` | ✅ **CLOSED 2026-06-26 (p18 Cluster G c12).** Character constants in the `#if` constant-expression (`#if 'A' == 65`, C 6.10.1p4 + 6.4.4.4) were rejected (P0013 "unexpected token in #if expression: '") — the `#if` Pratt evaluator had no char-literal primary. SQLite's `#if 'A' == '\301'` EBCDIC charset guard is exactly this shape. | ✅ `pp_if_eval.cpp` `parsePrimary` gained a char-literal primary: detect the `'` opener token (config `hirLowering.charStartToken`, never a hardcoded "CharStart") + consume the coalesced body (`charBodyToken`), decode via the SHARED `decodeCharLiteralBody` (so an escape means the same thing as in normal char-literal lowering); the value is the INT execution-charset code (ASCII 'A' == 65). ★ The SHARED escape decoder `decodeEscapedBytes` ALSO gained an OCTAL `\ooo` arm (1-3 octal digits, subsuming the old bare `\0`) — a GENUINE pre-existing gap: `'\101'`/`'\301'` failed even in NORMAL char/string literals; the fix corrects C 6.4.4.4 octal escapes EVERYWHERE, not just `#if`. AGNOSTIC (config char token kinds). WITNESS: corpus `pp_if_char` (`#if 'A' == 65` TRUE + `#if 'A' == '\301'` FALSE-on-ASCII octal-escape guard → exit 42; pe-native + elf/macho/arm64 + release arm) + the octal arm proven by `'\101'`==65 in normal code. | p18 Cluster G c12; cited in `examples/c-subset/pp_if_char/` + `src/analysis/preprocess/pp_if_eval.cpp` (`parsePrimary` char primary) + `src/core/types/char_decode.hpp` (octal escape arm). |
| `D-OPT6-LICM-CHAINED-INVARIANTS` | ✅ **CLOSED 2026-06-04** (cycle 10j) — LICM `analyze()` per-loop section now runs a fixed-point iteration. Each round identifies hoist candidates; the eligibility check relaxes "operand defined outside loop body" to "outside loop body OR already in `hoistedInThisLoop` set". A round that adds no new hoist sets `changed=false`; defensive iter cap at 64 (bounded above by `|loop.body inst count|`). Round-N admits `y = x*c` when round-(N-1) hoisted `x = a+b`. | New tests: `Licm.ChainedInvariantsHoistedInFixedPoint` (positive: `x = a+b` + `y = x*c` → `instructionsHoisted == 2`) + `Licm.ChainedInvariantsRejectedWhenSiblingOperandIsLoopVariant` (negative regression guard: `y = x * loop_phi` keeps y in loop even though x hoists — sibling-operand-must-also-be-invariant invariant). | plan 22 OPT6 LICM; pairs with deferred D-OPT6-LICM-PREHEADER-INSERTION (next-cycle candidate) and D-OPT6-LICM-TRAP-SAFE-HOIST (blocked on D-CSUBSET-DIVISION-OP-CODEGEN) |
| `D-CSUBSET-LOCAL-INT-CODEGEN-NEGATIVE-PIN` | ✅ **CLOSED 2026-06-04** (cycle 10k) — the third pin (in addition to `AllocaMaterializesToLeaInScanOrder` positive + `AllocaWithVirtualResultFailsLoud` post-regalloc invariant): a schema declaring `alloca` without `lea` fails loud at `materializeCallingConvention` with `L_RequiredLirOpcodeMissing`. Pre-cycle, this defense was internal-only (no shipping target hits it; `x86_64.target.json` declares both). | New `tests/test_support/mutate_target_schema.hpp` header-only helper `mutateShippedTargetSchemaJson(targetName, removeMnemonics)` — loads shipped JSON via `findShippedConfig`, removes named opcode rows via nlohmann_json, re-constructs `TargetSchema` via `loadFromText`. New test `LirCallconvAbi.AllocaWithoutLeaInSchemaFailsLoudInMaterialize` with helper-regression attribution pins (`ASSERT_FALSE(lea)` + `ASSERT_TRUE(alloca)` post-mutation) + diagnostic-code specificity (`countCode(L_RequiredLirOpcodeMissing) == 1u`). Also: `findShippedConfig` gains `DSS_EXPORT` (was missing — only the struct was exported). | plan 14 / lir_callconv negative-pin family; the helper is reusable for any future schema-misconfiguration negative pin |
| `D-CONFIG-DIAGNOSTIC-CODE-PER-KIND` | ✅ **CLOSED 2026-06-04** (cycle 10m) — added `C_InvalidTargetName = 0xC035` + `C_InvalidFormatName = 0xC036` in `parse_diagnostic.hpp`; rendering arms added in `parse_diagnostic.cpp`; updated all 3 `findShippedConfig` callers (TargetSchema, ObjectFormatSchema, mutateShippedTargetSchemaJson). Pre-cycle every "invalid-name" error from a target or format lookup carried the misnamed `C_InvalidLanguageName`. Mirrors the `D-OPT-DIAGNOSTIC-CODE-SPLIT-OOR-VS-FILE` precedent. | 2 existing tests updated for the new codes: `TargetSchema.LoadShippedRejectsPathLikeNames` + `LoadShippedReportsNotFoundForUnknownName` (now `C_InvalidTargetName`); `compile_pipeline.cpp:571` cap-fill identity pin (now `C_InvalidFormatName`). Identity-pin discipline preserved on the new kind-specific codes. | plan 03 / config substrate |
| `D-TARGET-IMPLICIT-REGISTER-CONSTRAINT` | ✅ **CLOSED 2026-06-04** (cycle 10p, `cb3ac20`) — config-driven implicit-register-constraint substrate. New `ImplicitRegisterConstraint` struct (per-opcode arrays of register names + parallel ordinals; sibling to `encoding` on `TargetOpcodeInfo`). Loader-tier resolution (post-register-load, pre-validate) + load-time rejection arms (empty-block typo-discriminator, unknown register name, within-array duplicates, unknown sub-keys per `D-CONFIG-LOADER-UNKNOWN-KEYS-FAIL-LOUD`, non-object block, non-array sub-field, non-string entry). Reusable across x86 idiv RDX:RAX / mul RAX-RDX:RAX / shift-by-CL, ARM64 fixed-operand ops, SPIR-V fixed-arg ops — no hardcoded `if (target == x86_64)` branch. **Pre-cycle-10q invariant**: every shipped target leaves the field nullopt on every opcode; the substrate ships unconsumed. 11 tests including positive idiv-shape + inputs-only shift-by-CL precedent + 6 negative pins + omission default + pre-10q invariant pin (attribution-clear failure when 10q lands its first declaration). | (closed) | substrate for cycle 10q idiv/cdq/cqo + cycle 10r corpus + cycle 10s onward c-subset division enablement; pairs with D-CSUBSET-DIVISION-OP-CODEGEN |
| `D-CSUBSET-DIVISION-CORPUS-MATERIALIZE-BUG` | ✅ **CLOSED 2026-06-04** (cycle 10r BUG FIX). Root cause identified + fixed: NOT a materialize-pass bug. The cycle-10q compound encoding packed `[REX.W 0x99, REX.W 0xF7 /7]` as a single opcode's `opcode` array. The x86-variable encoder auto-emits ONE REX prefix at instruction start computed from operand high bits (e.g., 0x41 = REX.B for R14). Per x86 decode the LAST REX before the opcode wins — the embedded second literal 0x48 (REX.W only) superseded the auto-REX, losing REX.B. IDIV operand decoded as RSI (without REX.B) instead of R14 → divide-by-zero trap. Cycle 10r SPLIT the compound into separate `cqo`/`idiv_op`/`xor_rdx_zero`/`div_op` opcodes (each a single instruction with its own auto-REX). | (closed) | plan 12 ML5 (asm encoder) + ML6 (regalloc); paired with D-CSUBSET-DIVISION-OP-CODEGEN closure |
| `D-LIR-REGALLOC-PRESSURED-IMPLICIT-CLOBBER-PIN` | ✅ **CLOSED 2026-06-11** — the pressured DIV pin landed: `LirRegAlloc.PressuredDivCoveringVregsExcludeImplicitInputAndClobberSet` (tests/lir/test_lir_regalloc.cpp) sweeps nLive 10..16 live-across-division c-subset locals through the real pipeline; EVERY vreg range COVERING a declared-implicit-op position (the cqo+idiv pair — agnostic discovery over `implicitRegisters`, no mnemonic list) must avoid (inputs ∪ clobbered); non-vacuity guards = shape-found + ≥1 phys-assigned covering range. RED-on-disable DEMONSTRATED + restored: commenting out the `implicitClobbersCrossedBy` consultation lands covering vregs on rdx(ord 2)/rax(ord 0) at EVERY sweep point. Runtime corpus witness: `examples/c-subset/division/` gained the shift_ops-style `pressured()` arm (10 locals + q/x/n live across a runtime 100/7); with the exclusion disabled the BASELINE arm's alloca-ADDRESS vreg (&a9) lands on RDX, CDQ zeroes it, the post-div reload null-derefs → deterministic STATUS_ACCESS_VIOLATION 0xC0000005 (empirically verified, 3 identical runs; the optimized arm folds the call chain to a constant — the baseline arm is the witness, exactly the shift_ops discipline); GREEN exit-14 witnessed on PE native + WSL x86_64-ELF + qemu arm64-ELF (arm64 native SDIV = no implicit registers, regression arm). **Original finding**: Cycle 10r (2026-06-04, deferred — non-blocking after 10r corpus closure): the regalloc unit pin `LirRegAlloc.DivisorVregExcludesImplicitClobberSet` is unpressured — the linear-scan picks R14 for the divisor naturally (because R3..R13 are also free), so the pin passes EVEN WITH the implicit-clobber-exclusion DISABLED. The pin proves the mechanism EXISTS but not that it FIRES. The R14 byte-pin tests (`IDivOpR14EmitsREX_B_49_F7_FE` + analogs for R8/R15) + the end-to-end corpus `division/` returning 14 jointly attest the mechanism at the byte + binary tiers; the pressured-allocator pin would add an additional unit-tier strength but is no longer load-bearing for the close. **Partial sibling closure 2026-06-11 (sweep-c1 CRITICAL fix): the SHIFT-RESULT exclusion now has exactly the pressured pin this row prescribes — `PressuredShiftResultExcludesImplicitInputAndClobberSet` sweeps 10..16 live values; with the result-def exclusion disabled, EVERY sweep point lands the shl result on rcx (ordinal 1) → RED (demonstrated + restored). The DIV-DIVISOR pressured pin (this row's original subject) remains open.** | Build a pressured synthetic LIR: 13+ live values across the compound ops, exhausting all GPRs except RDX/RAX, FORCING the allocator's hand toward those forbidden regs. With exclusion enabled: divisor spills OR picks a callee-saved reg. With exclusion disabled: divisor lands in RDX/RAX, pin RED. (The shift-result sibling pin in tests/lir/test_lir_regalloc.cpp is the template — same sweep recipe over a `a/b` div under pressure.) | tests/lir/test_lir_regalloc.cpp |
| `D-OPT-REGALLOC-EXCLUSION-BUFFER` | ✅ **CLOSED 2026-06-11** — growable exclusion scratch replaces the fixed array (src/lir/lir_regalloc.cpp): a `std::vector<uint16_t>` hoisted OUT of the range loop (`clear()` keeps capacity → allocation-free after the high-water mark; the same path already heap-allocates per covered opcode in `collectImplicitClobberPositions`, so no new cost class), `implicitClobbersCrossedBy` now appends-with-dedup (template/cap gone). **Adjudication**: (1) no small-vector utility exists in src/ (grep `small_vector|SmallVector|inline_vector|static_vector` empty); (2) the schema loader places NO cap on `implicitRegisters` list sizes (only within-array-dup / unknown-name / outputs⊆clobbered checks — bounded solely by the register table, itself unbounded) → only a growable buffer is TOTAL over every loadable schema, so enlarge-the-cap was rejected; (3) hoisted-reused `std::vector` chosen over a hand-rolled inline-8+heap-tail SBO — zero new container code to maintain, amortized allocation-free, and the SBO's zero-alloc-today benefit is illusory given the per-instruction vectors on the same path; (4) remove-vs-convert for the THREE former fail-loud arms (both `regallocFatal` overflow arms + the 2026-06-02 HIGH-2 N-ary operand pre-check): REMOVED — sound because `push_back` can never truncate the exclusion contract (the fail-loud's job — never silently drop an exclusion — holds by construction; comments at all three former sites document it). Pins (tests/lir/test_lir_regalloc.cpp, in-memory schema mutation via tests/test_support/mutate_target_schema.hpp): `ExclusionUnionBeyondFixedBufferAllocatesAndExcludesAll` (idiv_op mutated to a 14-register clobber set — the old code ABORTED at the 9th fill; now allocation succeeds with EVERY declared ordinal excluded + union>8 non-vacuity guard + ≥1 phys-assigned covering range) and `ResultDefExclusionUnionBeyondFixedBufferAllocatesAndExcludesAll` (shl mutated likewise — the result-def arm). RED-on-recap DEMONSTRATED + restored: temp re-adding the old cap as a `resize(8)` truncation flips BOTH pins RED (divisor/shl-result land on dropped tail ordinal 14 = r14). **Original finding**: Cycle 10q surfaced (2026-06-04): the regalloc's `excludedStorage` fixed-array (size 8 post-10q, was 4 pre-10q) holds the union of (a) requires2Address operand[1..N] clobber-prevention + (b) implicit-register clobbers from opcodes the range COVERS + (c, added 2026-06-11 sweep-c1) the defining op's own implicit (inputs ∪ clobbered) set when it is requires2Address (the result-def exclusion — x86 shift-by-CL's rcx). A schema whose combined union exceeds 8 entries on a single range triggers `regallocFatal` (TWO fail-loud sites now: `implicitClobbersCrossedBy` + the result-def arm in `allocateOneFunc`; both cite this anchor). The likely closure: switch `excludedStorage` to `boost::container::small_vector` or a hand-rolled fixed-capacity vector that grows-with-fail-loud. Today's worst-case union across the shipped schemas: (a) ≤ 1 (2-operand binops) + (b) ≤ 3 distinct (rax/rdx from the div family, rcx from shifts) + (c) ⊆ {rcx}, dedup'd into the same vocabulary — ≤ 4 of 8, comfortable. | Switch the fixed `std::array<uint16_t, 8>` to a growable small-buffer-optimized container OR enlarge the static cap once profiling shows the union routinely exceeds 8. Pair with `D-CSUBSET-BINOP-RIGHT-CLOBBER` buffer-overflow guard (same fail-loud message family). | plan 12 ML6 regalloc |
| `D-CSUBSET-MOD-OP-CODEGEN-OUTPUT-INDEX-CONTRACT` | ✅ **CLOSED 2026-06-10** (FC1 cycle 1, V2-4.X) — the closure trigger fired (SMod projects the NON-FIRST output). Shipped as ADDITIVE optional `inputRoles`/`outputRoles` JSON maps (role → register name) on `implicitRegisters` — a refinement of the originally sketched replace-the-array shapes, chosen because cqo/xor_rdx_zero's outputs are never projected and have no natural role, while the positional arrays remain the regalloc/`outputs ⊆ clobbered` surface. The div/mod projection in `emitImplicitPairDiv` resolves the dividend pin (role `dividend`) + the capture (role `quotient`/`remainder`) BY ROLE; positional indexing is GONE from the projection path (missing role → `L_RequiredLirOpcodeMissing` naming the role+map). Loader validates: registered role vocabulary {dividend, quotient, remainder} (typo reject), role's register ∈ the positional array, name→ordinal resolution, unknown-sub-key reject. **The input side was role-tagged too** (plan-lock MUST-FIX: `inputOrdinals[0]`-as-dividend was the same silent-reorder class). RED-on-disable: `MissingOutputRolesFailsLoudOnDivLowering` + `MissingInputRolesFailsLoudOnDivLowering` (strip the map from idiv_op via the cycle-10k mutation substrate → lowering fail-loud); `SignedModuloLowersToCqoIdivWithRemainderCapture` pins the capture register IS rdx and NOT rax. | (closed) | plan 12 ML6; closed by the same cycle as D-CSUBSET-MOD-OP-CODEGEN |
| `D-CSUBSET-UDIV-RUNTIME-HIGH-BIT-PIN` | ✅ **CLOSED 2026-06-10** (FC3 c1, V2-4.X — the trigger fired: c-subset gained the full unsigned keyword surface). `examples/c-subset/unsigned_division/` ships exactly the prescribed behavioral shape at 64-bit width: a HIGH-BIT U64 dividend (65535⁴, bit 63 set, built at runtime from imm16-safe seeds through function args — fold-resistant) whose quotient, sign-reinterpreted via `(long long)`, discriminates udiv (exit 42) from a misrouted sdiv (exit 7) — the byte-level miscompile lever the anchor demanded, beyond the opcode-routing pin. WITNESSED: PE native (ctest) + x86_64-ELF (WSL) + arm64-ELF (qemu) all exit 42; the Mach-O arm = the macos leg. The `0x80001003u` UNSIGNED-INT (U32) sibling of the same pin lands with FC3 c2's 32-bit ALU forms (U32 arithmetic is `D-CSUBSET-32BIT-ALU-FORMS`-gated until then). | (closed at U64 width; the U32 sibling rides c2) | plan 22 OPT / plan 23 FC3 c1 |
| `D-AUDIT-FUSED-CMP-WIDTH-PIN` | ✅ **CLOSED 2026-06-11 (audit-residue sweep c1)** — shipped exactly the prescription, zero engine change (the site was already correct; the closure is the missing PIN + CORPUS). (1) LIR pins `FusedI32CompareCondBrCmpCarriesThirtyTwoBitWidth` + the I64 contrast `FusedI64CompareCondBrCmpKeepsSixtyFourBitWidth` (tests/lir/test_mir_to_lir.cpp): synthetic `f(k a, k b) { if (a < b) … }` where the ICmpSlt's ONLY consumer is the CondBr (the buildFcmpBranchFn mirror with integer operands); asserts the entry block ends `cmp ; jcc`, the jcc payload IS `Slt` (proof the FUSION arm fired — the non-fused arm carries Ne), and the fused cmp's `lirInstWidthBits` == 32 (I32) / == 64 (I64) — pinned on BOTH shipped targets (the site is target-blind shared lowering). (2) Corpus `examples/c-subset/fused_negative_compare/` (4 arms): `x = sub(3,5) = -2` through two calls (Identity/baseline pipeline = no fold → runtime-live); width-32 → exit 42, a width-64 regression reads the zero-extended 0x00000000FFFFFFFE as POSITIVE → exit 7 (delta 35 ≢ 0 mod 256 — WEXITSTATUS-safe); WITNESSED exit 42 on PE native + WSL x86_64 + qemu-aarch64; Mach-O compile-arm clean (run = macos leg); HONESTY: release inline+const-fold folds the chain — the baseline arm is the witness (division/shift_ops precedent). (3) RED-ON-DISABLE DEMONSTRATED + restored: the site forced to flags-0 (width-64) → the I32 pin RED on BOTH targets (`fusedCmpWidthBits Which is: 64` vs `32`) AND the corpus PE binary compiled CLEAN (rc=0) but exited 7 — the silent-miscompile shape made loud; inverse-edit restored → 248/248 green. **arm64 finding**: the shared site threads the width there too — byte-scan of the arm64-ELF artifact shows 2× W-form `CMP Wn, Wm` (0x6B1B035F, SUBS WZR base 0x6B00001F = the FC3-c2 width-32 variant; the value-path + the fused cmp) and 0× X-form. **Original finding (external dss-audit of 1c73ce0→c2f28fa, blocker-now; claim verified against the code)**: the FUSED ICmp+CondBr cmp width-threading site (mir_to_lir.cpp ~:3063-3068, `widthFlagsForType(mir.instType(icmpOperands[0]))`) had NO pin and NO discriminating corpus — the only cmp-width pin (`U32CompareLowersWithThirtyTwoBitCmpWidth`) exercises the SETCC path, the c2 fusion tests are float-only, and no example branched on a runtime-NEGATIVE signed I32 compare; regressing that one site to width-64 silently mis-branched `int x = a-b; if (x < 0)` with all 246 tests green — the highest-traffic compare shape in C. | (closed — sweep c1 commit 597f12d) | mir_to_lir.cpp fused-CondBr arm / this sweep |
| `D-AUDIT-BITWISE-UNWALL-WITNESS` | ✅ **CLOSED 2026-06-11 (audit-residue sweep c1)** — shipped exactly the prescription, zero engine change. (1) Corpus `examples/c-subset/bitwise_and_or/` (4 arms): runtime operands a=76=0x4C, b=42=0x2A via fn args through two calls (Identity/baseline = no fold); the four misroute candidates over the SAME operands are pairwise DISTINCT and < 256 — a&b=8, a\|b=110, a^b=102, a+b=118 — so ANY single misroute (&↔\|, →+, →^) flips one of the two composed equalities (`x==8`, `y==110`) → exit 7 vs the correct exit 42; the discrimination is IN-REGISTER (full-width compares feeding branches, never exit-code truncation) and the exits 42 vs 7 (delta 35) are themselves ≢ 0 mod 256 — double WEXITSTATUS-safe; WITNESSED exit 42 on PE native + WSL x86_64 + qemu-aarch64; Mach-O compile-arm clean. (2) The stale wall comment (requireNativeIntWidth doc tail, mir_to_lir.cpp ~:699) rewritten to the per-target truth, VERIFIED against both target schemas: And/Or ENCODED end-to-end on BOTH targets (x86 21/09 reg-reg widths 64+32 from FC3.5-c2; arm64 AND 0x8A000000 / ORR 0xAA000000 X-form pre-existing, width-absent variants); **x86 xor + x86 not** = declared WITHOUT encodings → assembler `A_NoEncodingDeclared`; **arm64 xor = EOR 0xCA000000 IS encoded** (`^` walls on x86 only); **arm64 `not` mnemonic is UNDECLARED** → fails loud at LOWERING (`L_RequiredLirOpcodeMissing`), so `~` walls on both targets at DIFFERENT tiers. (3) The `D-CSUBSET-32BIT-ALU-FORMS` row's residue text corrected the same way (below). (4) xor/not deliberately NOT encoded — no consumer, trigger discipline. **Original finding (trigger = this sweep, high; claim verified)**: FC3.5-c2's x86 `and`/`or` encodings (added for the FCmp combiner) silently UN-WALLED C bitwise `a & b`/`a \| b` — the grammar admits them (prec 40/30), MirOpcode::And/Or route through the same mnemonics (mir_to_lir.cpp:1019-1020), both targets compiled AND ran them with ZERO corpus witness, while the gate comment (~:751 area) + the 32BIT-ALU registry residue still claimed "And/Or/Xor/Not have NO encodings — fail loud". | (closed — sweep c1 commit 597f12d) | mir_to_lir.cpp:751/1019 / this sweep |
| `D-AUDIT-BOOKKEEPING-TRUTH` | ✅ **CLOSED 2026-06-11 (audit-residue sweep c2) — all FIVE sub-claims VERIFIED against the actual files, then applied (none refuted)**: (a) VERIFIED (anchor in 13 files; the only registry hit was inside this row) → retroactive `D-CSUBSET-F32-CODEGEN` row CREATED (pinned FC3 c1 — the float→F32 semantic split, codegen fail-loud behind `requireF64Width`; CLOSED FC3.5 c2 commit `8c71a66` — F3-prefix encodings + materialization + the f32_arithmetic witness); (b) VERIFIED (`guard.width` throughout BOTH target schemas since FC3 c2; `trunc` encoded at x86_64.target.json ~:744; F32 rides the width key; live citations mir_to_lir.cpp:648 + :1350) → the row REWRITTEN to post-FC3.5 truth, **ORIGINAL pre-FC3 text preserved in-row** (the D-LANG-WIDTH convention); (c) VERIFIED (both ~:1210/:1261 "F64-WIDTH ONLY … when an F32 consumer exists" tails contradicted by the MOVSS variants sitting directly below in the SAME opcode) → replaced with accurate WIDTH-KEYED one-liners, NOT deleted; the adjacent :692 sext $comment carried the SAME falsified "no operand-width discriminator" premise → corrected too (now cites the (d) successor row); (d) VERIFIED → OPEN successor `D-CSUBSET-SUBNATIVE-ALU-FORMS` CREATED (8/16/128-bit + Bool-ALU + the per-target xor/not walls; trigger = packed struct fields/atomics or the first xor/not consumer, trigger-gated) and the ✅-closed 32BIT-ALU row's residue text now POINTS at it; (e) VERIFIED by fresh CLI probe (`typedef struct P { int a; } MyP;` → P0001 expected-Identifier-got-`{` at 1:18; `MyP p;` → P0009 at the declaration — both parse failures observed live) → OPEN `D-CSUBSET-TYPEDEF-DECLARATION-POSITION` CREATED (trigger FC4, high-within-FC4) + the anchor added to plan-23's FC4 Closes column. **ORIGINAL finding (trigger = this sweep, high)**: (a) `D-CSUBSET-F32-CODEGEN` cited in 10+ files but has NO registry row (closure recorded only in plan-23 — legitimate per §F but a retroactive row restores registry-greppability); (b) `D-TARGET-ENCODING-WIDTH-GUARD` row is pre-FC3 fiction cited by LIVE diagnostics (claims "only operandKinds discrimination" + "trunc has no encoding" — falsified by guard.width, the trunc encoding, and F32-on-the-width-key); (c) two contradicted `$comment`s in x86_64.target.json (~:1210/:1261 "F64-WIDTH ONLY … when an F32 consumer exists" — the MOVSS variants sit directly below); (d) the 32BIT-ALU residue (8/16/128-bit + Bool-ALU; xor/not walls) lives inside a ✅-closed row — promote to an OPEN successor row; (e) the compound-literal typedef-in-DECLARATION-position residue (`MyP p;` does not parse) is buried in a closed row — its own OPEN row, trigger FC4. | (closed — this sweep c2) | registry + x86_64.target.json / this sweep |
| `D-AUDIT-WITNESS-STRENGTHENING` | ✅ **CLOSED 2026-06-11 (audit-residue sweep c2) — all FIVE sub-claims arithmetic-VERIFIED, then applied (none refuted; one prescription adapted to implementation reality, see (e))**: (a) VERIFIED — -4 = 0xFFFFFFFC vs logical 0x0FFFFFFC share low byte 0xFC AND the additive exit delta 2^28 ≡ 0 mod 256 (the sum-exit aliases BACK TO 42 on POSIX) → added the BRANCH-observed `sar_branch(0-64, 3)` arm (`(x>>n) < 0` runtime-x/variable-n + a nested imm8-count probe; SHR misroute → huge-positive → branch flips → 42-vs-7, delta 35) + the overstating comment rewritten to the honest reach; (b) VERIFIED (the file's own comment admitted it) → added `round_away(16777216.0f)`: 2^24+1.0f rounds away under true ADDSS (ties-to-even) so `== big` holds ONLY at binary32; an all-F64 chain flips to exit 7; literal rides the F32 rodata materialization (never an inline int32 const), F32 `==` rides the width-32 fcmp variants (UCOMISS/FCMP-S — both verified declared); comment fixed; (c) VERIFIED — lt and eq are each observed with BOTH outcomes, so their polarity inversions are sum-NEUTRAL (and lt→le slips invisible without an equal-operand probe) → comment softened to the truth: the 56-cell truth-table pin (test_fcmp_lowering.cpp) is the exhaustive guarantee, the corpus witnesses branch plumbing; (d) VERIFIED (converted operand is 1; SExt==ZExt) → comment fixed to "EXERCISES, direction MIR/asm-pinned, this example witnesses the WIDTH divergence" in BOTH byte-identical files (sha256-equal re-verified post-edit); NO new witness (per the pin — a sign-extension runtime witness belongs to a future conversions example); (e) VERIFIED (0x30000/0x186A0/0x486A0 all stop at movk_lsl16) → 4-op-ladder witness ADDED with an implementation-reality adaptation, empirically probed: NO c-subset source shape mints a negative inline I64 const in the BASELINE pipeline (a `-`/`0ll-N` literal lowers as runtime NEG/SUB of the POSITIVE const — artifact-word dump proved it), and the inline-const carrier is int32 (a 64-bit-chunked negative would hit the LiteralPool dead end), so the witness = `negCheck(1234609)` with `0ll - 1234567ll` + a NEW manifest `constfold-only` optimized arm: ConstFold mints Const(-1234567) (FoldsSubOfTwoConstants pin) → the width-64 sign-extended FULL 4-op ladder MOVZ #0x2979 + MOVK #0xFFED,16 + MOVK #0xFFFF,32 + MOVK #0xFFFF,48 (shape pinned by Arm64NegativeConstEmitsFullFourOpLadder) executes; chunk-2/3 drift is invisible mod 2^32 AND mod 256, so the OBSERVATION is the 64-bit compare `neg + 1234609 != 42` → exit 7 (delta 35). EVERY new delta is 42-vs-7 = 35 ≢ 0 mod 256. Re-witnessed: see the sweep's re-witness table (PE native + WSL x86_64 + qemu-aarch64). **ORIGINAL finding (trigger = this sweep, normal)**: (a) shift_ops' SAR-vs-SHR witness is NOT runtime-discriminating on POSIX — (0-64)>>4: arithmetic 0x…FC vs logical 0x0FFF…FC share the LOW BYTE (WEXITSTATUS mod-256 aliasing) → discriminates on Windows only; add a branch-observed witness (`if ((x>>n) < 0)`, runtime x<0 + variable n) + fix the comment; (b) f32_arithmetic's values are exact in BOTH binary32/64 — an all-F64 misinterpretation passes; add a rounding discriminator (16777217.0f == 16777216.0f under binary32) observed in the exit; (c) float_compare's "ANY wrong predicate shifts the sum" comment overstates (polarity inversions are sum-neutral — the truth-table pin carries the real guarantee); fix; (d) datamodel examples' "runtime witnesses SXTW" comment false (operand 1: SExt==ZExt) — fix comment or add a negative widening witness; (e) wide_immediate runtime values reach only movk_lsl16 — add a 4-op-ladder runtime value (negative I64) exercising chunks 2/3. EVERY new delta checked against mod-256 aliasing. | (closed — this sweep c2) | examples/ / this sweep |
| `D-AUDIT-PLAN-STALENESS` | ✅ **CLOSED 2026-06-11 (audit-residue sweep c2) — both claims VERIFIED, then fixed**: (1) VERIFIED — plan-23 §0 Status ended at "FC1 ✅ + FC2 ✅ + FC3 ✅ … Next: FC4" with NO FC3.5 mention, and the §0.1 FC3.5 row read 🟡 "c1 ✅ + c2 ✅" with c3 still future-tense ("if reachable") though c3 SHIPPED at `c2f28fa` (msub fusion + riprel fold + cast residuals [array-decay/void-discard/compound-literal-typedef] + wide immediates, 246/246) → §0 Status now carries FC3.5 ✅ COMPLETE (c1+c2+c3) with next = FC4, and the §0.1 row is stamped ✅ COMPLETE with the c3 closure list; (2) VERIFIED — plan-00 §0.1 V2-4.X row tail still said "Mach-O witnesses of c1+c2 + the c4 float_cast macho arm flip at next Step 0" though the flip EXECUTED (run 27341390786, recorded at plan-00 §0 line ~75) → tail struck-through + replaced with the flip-executed note. **ORIGINAL finding (trigger = this sweep, normal)**: plan-23 §0 Status + the FC3.5 §0.1 row never got c3's completion stamp (c3 updated plan-00/plan-14/registry but not its own plan); plan-00 §0.1 V2-4.X row tail still says "Mach-O witnesses … flip at next Step 0" though the flip executed (run 27341390786, recorded at plan-00 §0). | (closed — this sweep c2) | plan-23 §0/§0.1 + plan-00 §0.1 / this sweep |
| `D-UAC-SHIFT-RESULT-RULE-CONFIG` | ✅ **CLOSED 2026-06-23 (FC-deferral close-out c6, branch p14).** The C 6.5.7 shift-result rule is now the closed config verb `shiftResult` (`ShiftResultRule {PromotedLeft, CommonType}` on `ArithmeticConversions` + the resolved `ResolvedArithmeticRules`; loader parses + fail-louds an unknown verb; c-subset declares `promotedLeft`, struct default `PromotedLeft` = back-compat for a block written without it). The rule lives in ONE shared chokepoint `shiftResultType(interner, lhs, rhs, rules)` (type_rules.hpp) that BOTH typing tiers call — cst_to_hir's shift arm (the originally-named site, now UNIFIED: `result = shiftResultType`, coerce both operands to it, no PromotedLeft-only special-case + commonType fall-through) AND the semantic-tier `subtreeType` shift arm — so the two can never diverge on the verb. The HIR const-fold mirror (const_eval.cpp shift case) was a silent THIRD site (reviewer-caught): it now READS this node's authoritative typeId (already config-correct from the funnel) instead of re-deriving promoted-left, so NO site hardcodes C's shift rule. **User-authorized speculative-open** (Directive B close-out; user chose "Build w/ synthetic validation") validated per §A.5 WITHOUT a vacuous runtime corpus, with a red-on-disable pin at EACH tier: (1) chokepoint unit pin `TypeRules.ShiftResultRuleSelectsByVerb` (i32<<i64 → I32 vs I64); (2) cst_to_hir end-to-end HIR-lowering witnesses `HirLoweringCSubset.ShiftResult{PromotedLeftIsLeftType,CommonTypeIsCommonType}`; (3) **semantic-tier behavioral pins** `Fc3ShiftResult.{PromotedLeftSizesByLeftOperand,CommonTypeSizesByCommonType,ShippedAndAbsentDefaultToPromotedLeft}` — `int a; long long b; char arr[sizeof(a << b)]` folds the SEMANTIC `subtreeType` shift type into an array dimension: promotedLeft → 4, commonType → 8 (the verb flip); (4) loader fail-loud `Fc3LoaderRejects.UnknownShiftResultVerbRejects`. c-subset behavior byte-identical (still promotedLeft); 367/367 + `shift_ops` corpus green; independent review + self-audit VERIFIED-CLEAN (the self-audit corrected an earlier over-claim that c-subset cannot fold `sizeof` of a value expression — it DOES, via `sizeofValueRule`→`subtreeType`, which is precisely what makes pin (3) possible). | (closed — c6 branch p14) | shiftResultType chokepoint (type_rules.hpp) + cst_to_hir + semantic_analyzer + const_eval + plan 23 |
| `D-COND-FLOAT-NAN-TRUTHINESS-FCMP` | ✅ **CLOSED 2026-06-11 (FC3.5 sweep-c2)** — the trigger fired (first FCmp LIR lowering) and the adjudication landed EXACTLY as pinned: per C 6.5.9 (`!=` is the negation of `==`) the general float `Ne` mapping flipped `FCmpOne` → **`FCmpUne`** in hir_to_mir `mapBinaryOp` (ONE source serving both source-level `!=` AND the truthiness `Ne(d, 0.0)` — `if (NaN)` is TRUE, NaN compares unequal to 0.0); `Eq` stays `FCmpOeq` (NaN==x false) and `!x` stays Oeq-vs-zero (`!NaN` false). The lowering itself: `floatCmpPlan` (mir_to_lir) — swap-canonicalized Olt/Ole→Fogt/Foge singles, per-target capability-declared singles vs the universal compositions (x86: Oeq=sete∧setnp, Une=setne∨setp via the NEW and/or encodings; fone=setne IS a single since UCOMI sets ZF on unordered; arm64: foeq=EQ/fune=NE/fogt=GT/foge=GE singles, One composed NE∧VC — the mirror asymmetry), float TargetCondCode arms (fogt/foge/foeq/fone/fune/fuo/ford) optional per-entry in `condCodeEncoding` (absence = the composed-realization signal, loader-validated). Pinned by the exhaustive 7-predicate × 4-outcome × 2-target truth-table vs an independent SDM/ARM-ARM flag simulator (`tests/lir/test_fcmp_lowering.cpp`) + `examples/c-subset/float_nan` (0.0/0.0 → nan!=nan true, all ordered relationals false, if(nan) true → exit 42, RUN on PE native + WSL x86_64 + qemu arm64) + the `FloatIfConditionLowersAsFCmpUneNotFpToUi` red-on-disable pin (One-flip demonstrated RED + restored). Honest residue: FP-environment signaling (Annex F: relationals should raise FE_INVALID on quiet NaN — COMISD/FCMPE vs the shipped quiet UCOMI/FCMP) is NOT modeled — DSS has no fenv access; comparison RESULTS are identical. | (closed) | mir_to_lir `floatCmpPlan`/`lowerFCmp` + hir_to_mir mapBinaryOp + tests/lir/test_fcmp_lowering.cpp + examples/c-subset/float_nan / plan 23 FC3.5 |
| `D-CSUBSET-F32-CODEGEN` | ✅ **RETROACTIVE ROW (created 2026-06-11, audit-residue sweep c2, `D-AUDIT-BOOKKEEPING-TRUTH` (a)) — the anchor was cited in 10+ src/tests/config files with NO registry row (its closure was recorded only inside plan-23's FC3.5 row; legitimate per the per-plan convention, but the registry row restores anchor-greppability).** PINNED at FC3 c1 (2026-06-10): the width/signedness cycle split `float`→F32 vs `double`→F64 SEMANTICALLY while F32 had NO codegen — fail-loud behind `requireF64Width` (every F32 reaching MIR→LIR poisoned + `L_UnsupportedLoweringForOpcode` citing this anchor), with the interim `floatLiteralTyping` pin (`1.5f`→F64). ✅ **CLOSED at FC3.5 sweep-c2 (commit `8c71a66`, 2026-06-11)**: the F3-prefix SSE scalar-single family (ADDSS/DIVSS/CVTTSS2SI/CVTSS2SD/MOVSS load+store+riprel) + arm64 S-forms (FADD-S/FDIV-S/FCVTZS-from-S/FCVT both directions/LDUR-S/STUR-S) riding the FC3-c2 width axis (`widthFlagsForType` F32→32; conversions key on the SOURCE width via widthOverride); F32 literal MATERIALIZATION (4-byte rodata items + width-exact movss/LDUR-S loads); `floatLiteralTyping` flipped the `f` suffix→F32; `requireF64Width`→`requireEncodedFloatWidth` (admits F32, walls F16/F128); runtime witness `examples/c-subset/f32_arithmetic` exit 42 on PE/WSL/qemu (STRENGTHENED by this sweep with the binary32 ROUNDING discriminator: 2^24 + 1.0f rounds away under true ADDSS — an all-F64 misinterpretation flips the exit to 7). | (closed — FC3.5 c2; this row is retroactive bookkeeping) | plan-23 §0.1 FC3.5 row / x86_64+arm64.target.json / mir_to_lir.cpp / pairs with `D-TARGET-ENCODING-WIDTH-GUARD` |
| `D-CSUBSET-ZEXT-32-TO-64` | ✅ **CLOSED 2026-06-11 (FC3.5 sweep-c1)** — exactly the pinned recipe: the MIR→LIR ZExt arm threads the SOURCE type's width onto the LIR inst (a `widthOverride` on the shared `lowerNAryOp`/`lowerCast`; every other caller keeps the result-type rule byte-identically), the x86 `zext` mnemonic gained width-KEYED variants (width 64 = the movzx byte widener, byte-identical for the Bool/setcc consumers; width 32 = `mov r32,r32` 0x8B — the auto-zero-extending U32→U64 form), arm64's W-ORR stays ONE deliberately width-ABSENT variant (width-invariant: the W-write zeroes bits 63:32 for both source shapes — the xor_rdx_zero precedent), and the `requireNativeIntWidth` ZExt gate admits {Bool, U32} (I32 stays rejected — C widens signed via SExt; narrower sources stay fail-loud). Pins: `ZExtFromU32SourceLowersCarryingSourceWidth` (the FLIP of `ZExtFromU32SourceStaysGated`; red-on-disable = revert the width thread → RED, demonstrated) + Bool-keeps-64 + U16-stays-gated + byte-pins (x86 8B C1 / 48 0F B6 C1 contrast; arm64 0x2A0103E0 at BOTH widths) + `StrippedZextWidth32VariantFailsLoudNeverFallsBack` (both variants width-keyed, no match-any fallback) + c-subset source-tier thread pin (`(unsigned long long)a` → zext width 32). Corpus: `examples/c-subset/zext_u32_to_u64/` (0xFFFFFFFF built imm16-safe through args; upper-bits-ZERO + all-low-bits-survive double witness — exit-divergent vs BOTH the sext misroute AND the byte-form misroute; 4 arms; PE+WSL x86_64+qemu arm64 exit 42 witnessed, Mach-O compile-arm). | — | `src/lir/lowering/mir_to_lir.cpp` / plan 23 FC3.5 |
| `D-CSUBSET-32BIT-ALU-FORMS` | **U32/I32 arc ✅ CLOSED 2026-06-10 (FC3 c2) — scope NARROWED, residue below.** c2 shipped: the WIDTH AXIS (LirInst.flags bit 0 carrier — survives every flags-copying rewrite pass by construction; `guard.width`∈{32,64} on encoding variants, loader-rejected otherwise, duplicate-(kinds,width) + ambiguous-mix validate rejects; ONE shared `variantMatchesInst` consulted by BOTH the x86-variable AND fixed32 walkers — format-agnostic; `widthFlagsForType` = the single TypeKind→bits point, applied at COMPUTE ops only, plumbing stays 64); x86 32-bit no-REX.W variants (mov rr+exact-imm32 / add / sub / imul / cmp / neg / CDQ / idiv_op / div_op — every byte re-derived from the SDM by the independent review) + `trunc`'s FIRST encoding (mov r32,r32 = C's mod-2³² conversion, width-keyed 32); arm64 W-forms (bit-31-cleared words, ARM-ARM-re-derived) + NEW `sext`=SXTW 0x93407C00 + `trunc`=W-ORR-mov; gate narrowed (U32 out; I32 also now computes at TRUE 32-bit width — existing int corpus exits verified unchanged); NEW conversion gates (Trunc non-32 results, SExt non-I32 sources, ZExt non-Bool sources → `D-CSUBSET-ZEXT-32-TO-64`). WITNESSED: u32_wraparound (0xFFFFFFFFu+1u→0) + uac_mixed_sign_u32 (-1>0u) + unsigned_division_u32 (0x80001003u/3u, the anchor's ORIGINAL literal shape) exit 42 on PE+WSL+qemu; **datamodel_runtime_width: BYTE-IDENTICAL source (sha256-equal) exits 42 on Windows (LLP64: U32 wraps) and 23 on linux/arm64 (LP64: U64) — the RUNTIME dataModel divergence, each arm native on its CI leg**; Mach-O arms = macos leg. Red-on-disable: strip-the-32-bit-variant = a PERMANENT in-suite fail-loud pin; width-threading-forced-64 demo'd (42→7) + restored. **RESIDUE — PROMOTED 2026-06-11 (audit-residue sweep c2, `D-AUDIT-BOOKKEEPING-TRUTH` (d)) to the OPEN successor row `D-CSUBSET-SUBNATIVE-ALU-FORMS` (below): a ✅-closed row no longer greps as open work; the details stay here as history. (Still fail-loud-gated, future FCs with their own corpus): 8/16/128-bit ALU forms + Bool-ALU; bitwise walls CORRECTED + NARROWED 2026-06-11 (audit-residue sweep c1, `D-AUDIT-BITWISE-UNWALL-WITNESS` — the prior "x86 And/Or/Xor/Not have no encodings" claim was stale post-FC3.5-c2): And/Or are ENCODED end-to-end on BOTH targets (x86 21/09 reg-reg, widths 64+32, from FC3.5-c2's composed-FCmp work; arm64 AND/ORR X-form pre-existing) and runtime-witnessed by `examples/c-subset/bitwise_and_or`; the ops that ACTUALLY still wall, per target: **x86 xor + x86 not** (mnemonics declared WITHOUT encodings → assembler `A_NoEncodingDeclared`) and **arm64 not** (mnemonic undeclared → lowering `L_RequiredLirOpcodeMissing`; arm64 xor=EOR IS encoded — `^` walls on x86 only, `~` walls on both at different tiers). SHIFTS residue ✅ CLOSED 2026-06-11 (FC3.5 sweep-c1): Shl/LShr/AShr encoded END-TO-END on both targets — x86 variable counts via the implicit-CL realization (D3 /4 /5 /7, REX.W + no-REX.W; the FC1 idiv role-contract pattern with NEW registered role "count" [loader-validated, `inputRoles={count: rcx}`; the regalloc protection is TWO rules: the declared inputs/clobbered=[rcx] feed the covered-position exclusion for OPERAND ranges covering the shift, AND the result-def implicit exclusion (CRITICAL review fix, 2026-06-11 sweep-c1) keeps the shift's RESULT off rcx — a `requires2Address` op with declared implicitRegisters excludes (inputs ∪ clobbered) for its result vreg, because the 2-addr legalize's `mov result, value` lands BEFORE the op while the result's range starts at the LATE slot, invisible to the covered-position rule; pre-fix this was a PRESSURED silent miscompile (result→rcx → `value << (value & width-1)`, witnessed exit 162-vs-42 on BOTH PE/ms_x64 and ELF/SysV with the rule disabled); pinned by the PRESSURED `PressuredShiftResultExcludesImplicitInputAndClobberSet` (red-on-disable demo'd) + the high-pressure `pressured()` shift_ops corpus arm]; the MIR→LIR `lowerShift` pins the count BY ROLE — strip the role → fail-loud, the PERMANENT `MissingCountRoleFailsLoudOnShiftLowering` pin), constant counts via the imm8 forms (C1 /4 /5 /7 ib, both widths; NEW `EncodingSlotKind::Imm8` in full lockstep [enum+table+shapeFor+walker emit+range-check 0..255 fail-loud+disasm inverse]; the lowering selects imm iff the count is a MIR Const in [0,255] AND a [reg,imm] variant is DECLARED — capability-probed, out-of-range falls back to CL); arm64 native 3-address LSLV/LSRV/ASRV (0x9AC02000/2400/2800 X-forms + sf=0 W-forms, ARM-ARM-derived; NO implicit registers — the lowering's native rule; the LSL-imm UBFM alias deliberately deferred as a peephole, constant counts ride the V-forms with the count materialized). C semantics: counts ≥ width are UB (C23 6.5.7p3) — nothing emitted, hardware masks (x86 &63/&31 incl. imm8; arm64 mod 64/32); negative `>>` defined as sign-fill (SAR/ASRV) on every target. Width axis keys on the RESULT type (C's promoted LEFT operand). Pins: byte-pins for all 6 x86 CL/imm8×width forms + REX-without-W + all 6 arm64 words; lowering shape pins (CL pin-by-role / imm-form / 300-falls-back-to-CL / arm64-native-no-pin); `StrippedShiftEncodingFailsLoud` (the encoding IS load-bearing); x86_64 implicit-register consumer pin 4→7. Corpus: `examples/c-subset/shift_ops/` (var+imm counts, all 3 operators, both widths, `(0-64)>>4 == -4` SAR witness, + the `pressured()` high-pressure arm — 12 values live across a variable-count shift force the allocator to the rcx tail, exit-divergent 162-vs-42 pre-fix; exit 42 PE + WSL x86_64 + qemu arm64 RE-WITNESSED 2026-06-11 post-fix; Mach-O compile-arm).** ORIGINAL c1 text: FC3 c1 pinned (2026-06-10, **high — c2 closes it immediately per the user's "don't stop between cycles" split decision**): every x86_64 GPR ALU encoding is REX.W-always and every arm64 GPR op is X-form (sf=1) — integer arithmetic computes 64-wide. CONFORMING for I64/U64/Ptr (native) and for I32 (signed overflow is UB, so 64-wide implements every DEFINED I32 program) but WRONG for types with DEFINED narrower wraparound/comparison semantics: U32 (`unsigned int` — modulo-2³² is C-defined), U16/U8/I16/I8/I128/U128/Char/Byte (+Bool in ALU position). FC3 c1 gates these LOUDLY: `requireNativeIntWidth` (mir_to_lir.cpp, the `requireF64Width` sibling at the dispatch chokepoint) poisons + emits `L_UnsupportedLoweringForOpcode` citing this anchor — ALU/div/shift/neg/not gate on RESULT type, comparisons on OPERAND types (Bool compare-operands stay allowed — the `!x` ICmpEq-0 shape). Such values still FLOW freely (load/store/mov/phi/args — only arithmetic semantics break at 64-wide). Red-on-disable demonstrated (guard commented → U32 silently lowers 64-wide → 3 pins RED; restored). | **c2 (immediately next)**: a WIDTH AXIS on the encoder's variant guard (the operandKinds-guard mechanism extended with a width discriminator — config-parameterized, never `if(width)` in shared tiers); x86 32-bit no-REX.W variants for add/sub/imul/cmp/mov/div-family/shifts (x86 32-bit ops auto-zero-extend = free U32→U64 zext); arm64 W-forms + NEW `sext`/`trunc` opcodes (arm64 has neither today); un-gate the guard for the c2-covered widths; the U32 corpus (0xFFFFFFFFu+1u→0 wrap, -1>0u, 0x80001003u/3 unsigned-int division) + the RUNTIME LP64/LLP64 divergent witness (unsigned-long wrap on LLP64=U32). | `src/lir/lowering/mir_to_lir.cpp` (requireNativeIntWidth) / plan 23 FC3 |
| `D-CSUBSET-SUBNATIVE-ALU-FORMS` | **CONVERSION FORMS ✅ CLOSED 2026-06-25 (Cluster F F4, branch p15)** — the sub-native integer SEXT/ZEXT/Trunc narrowing+widening conversion forms are now realized + un-gated (the `requireNativeIntWidth` Trunc/SExt/ZExt arms admit I8/I16/U8/U16): x86 `sext` movsx r/m16 (0F BF) + `zext` movzx r/m8 (0F B6) / r/m16 (0F B7); arm64 `sext` SXTH 0x93403C00 + `zext` split to width-keyed (Bool=64/U32=32 mov, U16=UXTH 0x53003C00, U8=UXTB 0x53001C00); a Trunc-to-sub-native result routes through registerOpWidthFlags → the promoted width-32 `mov` (low bits kept, narrowing realized lazily at the byte/half-exact consumer). `widthFlagsForType` maps I8/U8→8, I16/U16→16; `registerOpWidthFlags` collapses 8/16→32 (partial-register hazard). Trigger fired via D-CSUBSET-INT-SAME-SIGN-NARROW (implicit narrowing made sub-native conversions source-reachable). WITNESSED: `int_narrowing` corpus exit 42 on x86 native + arm64 qemu; LIR red-on-disable pins `SExtFromI16AndI32SourcesBothRealized` / `ZExtFromBoolKeepsDefaultWidthAndNarrowSourcesNowRealized` / `TruncToSixteenAndEightNowRealizedViaPromotedMov`. **RESIDUE STILL OPEN: sub-native ALU COMPUTE forms (8/16/128-bit add/sub etc. — unreachable under C promotion, no consumer) + Bool-ALU; the xor/not walls were closed at F2.** ORIGINAL OPEN row (created 2026-06-11, audit-residue sweep c2, `D-AUDIT-BOOKKEEPING-TRUTH` (d)) — carried the residue of the ✅-closed `D-CSUBSET-32BIT-ALU-FORMS` (above) so it greps as open work. Scope, per target, all fail-loud today: (1) sub-native INTEGER widths — 8/16-bit ALU forms (and 128-bit) have no encoded variants; C's UAC keeps them source-UNREACHABLE in expressions (everything promotes to int), so no consumer can exist until packed struct fields / atomics / `_BitInt`-class work makes an 8/16-bit COMPUTE source-reachable; the I8/I16 sext variants (x86 0F BE/BF; arm64 SXTB/SXTH) belong here too (cross-ref `D-TARGET-ENCODING-WIDTH-GUARD` — the guard.width axis makes each a declaration-only addition). (2) Bool-ALU (`(a==b)+(a!=b)` walls at S_ReturnTypeMismatch — the semantic tier types Bool arithmetic by the left operand). (3) The xor/not walls, per the sweep-c1 verification: **x86 xor + x86 not** declared WITHOUT encodings (assembler `A_NoEncodingDeclared`); **arm64 `not` mnemonic UNDECLARED** (lowering `L_RequiredLirOpcodeMissing`); arm64 xor=EOR IS encoded — `^` walls on x86 only, `~` on both at different tiers. | Trigger: packed struct fields / atomics make 8/16-bit compute source-reachable, OR the first `^`/`~` consumer lands in a corpus row. Priority trigger-gated. Work: declare the missing encodings (width-keyed variants — declaration-only per the established axis) + un-gate `requireNativeIntWidth` for the covered widths + corpus with mod-256-safe witnesses. | successor of `D-CSUBSET-32BIT-ALU-FORMS`; pairs with `D-TARGET-ENCODING-WIDTH-GUARD`; plan 23 (future FC with its own corpus) |
| `D-CSUBSET-INT-SAME-SIGN-NARROW` | ✅ **CLOSED 2026-06-25 (Cluster F F4, branch p15).** C 6.3.1.3/6.5.16.1 implicit SAME-signedness integer NARROWING (`short s = anInt;`, `signed char c = anInt;`, `int i = aLong;`) — a hard SQLite blocker — is now admitted via the config gate `intSameSignednessNarrows` (the 4th C-conformance gate after boolWidensToArith / charConvertsToArith / enumConvertsToArith / intCrossSignednessConverts; default false → toy/tsql byte-identical; c-subset opts in). The `isAssignable` signed/unsigned rank arms admit `rank(rhs) > rank(lhs)` (narrowing) only when the gate is on — widening stays unconditional; threaded to the 5 semantic-tier `isAssignable` call sites (NOT the post-coerce hir_verifier — stays strict). coerce()'s arithmetic-core arm materializes the width-exact Cast → MIR Trunc, so the BACKEND realization is `D-CSUBSET-SUBNATIVE-ALU-FORMS` (conversion forms, closed jointly this cycle). Pins: `TypeRules.IsAssignableAdmitsSameSignednessNarrowingWhenGated` (red-on-disable both signednesses + scope guards: widening gate-independent, cross-sign NOT admitted) + `Fc3WidthSemantics.NarrowingLongLiteralNowAdmittedOnBothModels` + the `int_narrowing` runtime corpus (exit 42 x86 native + arm64 qemu, all four assignment positions + truncation witnessed) + the repurposed `datamodel_long_width_llp64` runtime truncation witness (LLP64 long=I32 truncates 2147483648l → negative → exit 7). Opens nothing new; the sibling int↔float implicit conversion stays NON-implicit (anchored `D-CSUBSET-INT-FLOAT-IMPLICIT-CONVERT`). | (closed — F4 branch p15) | type_rules.hpp isAssignable + semantic_config.hpp + grammar_schema_json.cpp loader + c-subset.lang.json + the 5 semantic_analyzer.cpp call sites + plan 23 Cluster F |
| `D-CSUBSET-INT-FLOAT-IMPLICIT-CONVERT` | **OPEN (created 2026-06-25, Cluster F F4).** C 6.3.1.4/6.3.1.5: implicit int↔float conversion in assignment context (`double d = 5;`, `int i = 3.5;`) is NOT admitted — `isAssignable` rejects every int↔float pair (the `IsAssignableRejectsIntFloatCross` pin + the repurposed `MixedWidthBinaryArgTypedByUacNotLeaf` rely on this), and an explicit `(double)`/`(int)` cast is required. A SEPARATE conversion axis from the integer-narrowing work (F4 surfaced it only as the deliberately-preserved observable). | Trigger: a corpus/SQLite construct that needs an implicit int↔float conversion. Work: add an `intFloatConverts` config gate (mirroring the charConvertsToArith family) threaded to the same call sites + coerce()'s SIToFP/FPToSI Cast + a runtime corpus. Priority trigger-gated. | type_rules.hpp isAssignable int↔float arms / plan 23 Cluster F |
| `D-CSUBSET-SYMBOL-ADDRESS-GLOBAL` | **✅ CLOSED 2026-06-25 (Cluster F F5 — 8 stage commits; 405/405 ctest Release; PE+ELF runtime exit 143; Mach-O rebase byte-verified + macOS-CI-leg runtime CONFIRMED GREEN — run 28150522650 (headSha 191b756) macos-clang-release ✓).** A global initialized to the LINK-TIME-CONSTANT address of another symbol — `char* g = "...";` (string→rodata), `int* p = &x;` (&global), or a function-pointer table. F5 realizes it as a new mechanism: a `MirSymbolAddrValue` literal-pool variant (classifyGlobals recognizes the `AddressOf(Ref(global/fn))` + `Cast(Literal(string), Ptr)` shapes BEFORE const-eval, routing to the variant instead of `__module_init__`) → the assembler emits a pointer-width slot + an ABSOLUTE-64 relocation (`abs64`, found by FORMULA `widthBytes==8 && !pcRelative`, NOT by name → agnostic) against the target symbol → the linker resolves it to the target's VA, across PE (DIR64 base-reloc) / ELF (ET_EXEC in-place) / Mach-O (in-place patch **+ a dyld REBASE opcode stream** so the PIE slide is applied). **A symbol-address pointer is INHERENTLY load-writable** (the loader writes the resolved/slid address into it), so the asm arm forces it to `.data` on every format regardless of source-const — never read-only rodata (which Mach-O PIE cannot rebase). Two pr-review folds: (1) the whole-program **merge** now remaps the `MirSymbolAddrValue.symbol` embedded id through `mergedSymbolOf` (was copied verbatim → stale abs64 target in any multi-CU build → `K_SymbolUndefined`/silent-wrong-VA); (2) the DCE liveness BFS propagates through a global's symbol-address init (release-arm corpus exposed it dropping the reloc target). Host-independent pin `MachOExecWriter.SymbolAddressDataGlobalEmitsDyldRebaseStream` (red-on-disable: rebase_size→0). The `char buf[] = "...";` char-ARRAY-into-global form is SEPARATE (fail-loud `S000B`, `D-CSUBSET-GLOBAL-CHAR-ARRAY-INIT`). | (closed) | mir_literal_pool.hpp + hir_to_mir.cpp + mir_merge.cpp + opt const_fold/dce + asm.cpp + link/format/{pe,elf,macho}.cpp + exec_data_section.hpp + plan 23 Cluster F |
| `D-LIR-MOD-MSUB-FUSION` | ✅ **CLOSED 2026-06-11 (FC3.5 sweep-c3, user-directed deferral-closure sweep)** — exactly the pinned recipe: `EncodingSlotKind::Ra` (bits 10..14, the MADD/MSUB/SMADDL multiply-accumulate third-source field) added in FULL LOCKSTEP (enum + name table `"ra"` + static_assert 22→23 + `slotShapeFor`=Fixed32 + `isSymbolBearingSlot`=false + fixed32 `windowFor`{10,5} + the fixed32_disasm mirror + both x86_variable exhaustive-switch fail-loud arms); arm64 `msub` declared (MSUB Xd,Xn,Xm,Xa = Xa − Xn·Xm; X-form 0x9B008000 + W-form 0x1B008000, ARM-ARM-derived, wires rn/rm/ra); rule 3 of `emitDivLikeValue` PREFERS a declared `msub` by mnemonic presence — `msub r, q, d, n` = ONE op (2-op modulo; arm64 SMod = sdiv+msub, 5-inst fn shape, was 6) with the generic mul+sub expansion KEPT as the fallback (a declared-but-result-less msub is a misdeclaration → fail loud, never silent fall-through). Pins: `Arm64SignedModuloFusesToSdivMsub` (the renamed 6→5 shape pin, msub operand wiring q/d/n asserted; RED-on-disable demonstrated + restored) + `Arm64ModuloWithoutMsubFallsBackToSdivMulSub` (msub-STRIPPED schema via the cycle-10k mutation substrate — keeps the fallback alive: no shipped target reaches mul+sub any more) + byte-pins `MsubEncodesDataProc3Source` (X: 20 8C 02 9B) / `MsubWFormEncodesHighRegs` (W, Rm=14 + Ra=21 high-reg windows: 83 D4 0E 1B). WITNESSED: modulo corpus re-run exit 42 on qemu-aarch64 (fused msub live) + WSL x86_64 + PE native (ctest) + Mach-O compile-arm (macos leg = runtime witness). | (closed) | `src/lir/lowering/mir_to_lir.cpp` rule 3 + `arm64.target.json` msub + `target_schema.hpp` Ra / plan 22 peephole arc |
| `D-ARM64-FLOAT-SUBSTRATE` | ✅ **CLOSED 2026-06-10** (V2-4.X cycle 4 — the trigger fired by user directive "fully work in arm64 as it did in x86"). **DECLARATION-ONLY closure, exactly as pinned — ZERO engine changes** (an explorer + the independent review both verified every mechanism generic): arm64.target.json gains d0..d31 (`class:"fpr"`, hwEncoding 0-31, D = the 64-bit V views — F64-only, F32 stays behind the `requireF64Width` guards like x86) + 5 fixed32 opcodes (fadd=FADD-double 0x1E602800 NATIVE 3-op [no requires2Address — the per-opcode flag does the right thing], fp_to_si=FCVTZS Xd,Dn 0x9E780000, fmov=FMOV Dd,Dn 0x1E604000, fldur=LDUR-D 0xFC400000, fstur=STUR-D 0xFC000000 — every word independently re-derived AND cross-assembled against GNU aarch64-as by the reviewer) + the `registerClassOps` fpr row {fmov,fldur,fstur} + BOTH ccs (aapcs64 + apple_arm64): argFprs d0-d7, returnFprs d0, calleeSaved += d8-d15 (low-64-only caveat $commented), callerSaved += d0-d7∪d16-d31 (load-bearing: regalloc buckets non-callerSaved allocatables as call-safe — omitting it would plant a latent cross-call clobber; the x86 sysv xmm precedent). 6 byte-pins (incl. negative-imm9 + high-reg field placement) + 2 full-pipeline tests (FADD+FCVTZS presence, d0/d1 arg-path discrimination, d8-d15 callee-saved spill via fstur/fldur, rodata-F64 2-reloc adr_prel_pg_hi21/add_abs_lo12_nc). RED-on-disable ×2 (fpr-row strip → L_RequiredLirOpcodeMissing at callconv + MIR→LIR; FADD bit-10 perturb → pins red). **§A.5: float_cast arm64-ELF → exit 16 EXECUTED under qemu-aarch64 (local witness); the Mach-O arm ✅ RUNTIME-WITNESSED 2026-06-11 — the macos-clang-release leg ran the full suite GREEN on PR #21 (run 27341390786, commit 966c977): float_cast's darwin arm EXECUTED on Apple Silicon, alongside FC3's nine Mach-O arms + bare_int_condition.** | (closed; the F32/SIMD width axis = FC3's `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH`/UAC work; the V-register 128-bit views re-examined when F128/SIMD arrive) | plan 23 FC2/FC3; x86 template = FC2 (`registerClassOps`, `mandatoryPrefix`) |
| `D-CSUBSET-CAST-ARRAY-DECAY` | ✅ **CLOSED 2026-06-11 (FC3.5 sweep-c3)** — exactly the pinned recipe, BOTH tiers: `isExplicitCastable` re-kinds an Array OPERAND as Ptr before the legality matrix (C 6.3.2.1p3 — the operand undergoes array-to-pointer conversion first; an Array TARGET stays rejected, C 6.5.4 forbids casts to array types), and `lowerCast` (cst_to_hir.cpp) emits the SAME synthetic decay Cast the implicit path's `coerce` uses BEFORE the explicit cast — so `(char*)"abc"` = decay→Ptr<Char> + identity Bitcast and `(long)"xy"` = decay + PtrToInt; mapCast never sees a raw Array. Pins: `CastOfStringLiteralDecaysAndIsAccepted` (semantic zero-diag; RED-on-disable demonstrated via decay-off + restored) + `ExplicitCastOfStringLiteralLowersViaDecay` (HIR chain Cast(I32)←Cast(I64)←decay-Cast(Ptr<Char>)←StringLit pinned node-by-node). Corpus: `examples/c-subset/cast_decay_void_discard/` — `(void)puts((char*)"hi")` prints "hi"+exit 42 (the decayed pointer reaches the shipped puts FFI: rodata address materialized end-to-end); WITNESSED PE native (ctest+stdout assert) + WSL x86_64 + qemu arm64; Mach-O compile-arm (macos leg = witness). | (closed) | `src/analysis/semantic/type_rules.hpp` + `src/hir/lowering/cst_to_hir.cpp` lowerCast / plan 23 FC2 residual |
| `D-CSUBSET-CAST-VOID-DISCARD` | ✅ **CLOSED 2026-06-11 (FC3.5 sweep-c3)** — exactly the pinned recipe: NEW `isVoidDiscardCast` (type_rules.hpp) admits a void TARGET for EVERY operand type incl. struct values (C 6.5.4p2 / 6.3.2.2), checked BEFORE the castability matrix at the analyzer's cast site (kept a SEPARATE helper deliberately — everything the matrix admits must be mapCast-lowerable, and the discard emits NO Cast node); `lowerCast` lowers the operand for its effects and returns it Cast-free with the void result type (any enclosing VALUE use = loud type mismatch); the void-as-OPERAND direction stays rejected (`(int)(void)x` → exactly 1 S_InvalidCast, pinned). Unused-result note (the row's "pairs with suppression"): NO unused-expression-result diagnostic exists today (only S_UnusedVariable) — the zero-diag pin on `(void)` statements IS the suppression contract, trivially satisfied + future-proof. Pins: `VoidDiscardCastAcceptsAllOperandTypes` (struct/double/ptr/arith, zero diag; RED-on-disable demonstrated + restored) + `CastFromVoidValueStaysRejected` + `VoidDiscardCastLowersOperandWithoutCastNode` (HIR: the exprStmt node IS the bare Call). Corpus: `examples/c-subset/cast_decay_void_discard/` — `(void)puts(...)`'s captured "hi" output IS the operand-evaluated proof; witnessed PE + WSL x86_64 + qemu arm64 + Mach-O compile-arm. | (closed) | `src/analysis/semantic/type_rules.hpp` isVoidDiscardCast + `semantic_analyzer.cpp` cast site + `cst_to_hir.cpp` lowerCast / plan 23 residual |
| `D-CSUBSET-COMPOUND-LITERAL-TYPEDEF` | ✅ **CLOSED 2026-06-11 (FC3.5 sweep-c3) — grammar + the NEW semantic stamping; HONEST TIER: parse+semantic+HIR (struct-typed), NOT runtime.** Grammar: `compoundLiteralExpr` re-pointed at `castTypeRef` (typedef-admitting bare-Identifier base) + the SAME `commitRequiresTypeName` triage castExpr uses — `(MyT){...}` commits (the `{` follower = triage case 4 for unknown names), `(a){...}` with a KNOWN-VALUE identifier rolls back → the value reading's orphan `{` is a LOUD parse error (pinned via tree diagnostics — parse errors do NOT surface in `SemanticModel`). The sweep ALSO surfaced + fixed the deeper PRE-EXISTING gap: NO semantic rule stamped compound-literal type children at all (only struct-refs resolved, via the struct-name machinery — even `(int){42}` H0009-failed at HIR). NEW `semantics.compoundLiterals` config vocabulary (`CompoundLiteralRule{rule, typeChild}` + loader + analyzer Pass-2 stamping through the SAME type-position resolver casts use) — kept SEPARATE from `casts` deliberately: a compound literal is C 6.5.2.5 postfix syntax, no operandChild, and the cast matrix must never run against the brace-init. Pins: `TypedefCompoundLiteralResolvesAndTypes` (zero-diag) + `CompoundLiteralUnknownTypeNameFiresUnknownType` (1× S_UnknownType) + `CompoundLiteralValueIdentifierIsLoudError` + `TypedefStructCompoundLiteralLowersTyped` (HIR ok; RED-on-disable demonstrated via stamping-off + restored) + `ScalarCompoundLiteralStaysAggregateOnlyFailLoud`. **Honest residue (NOT this anchor's scope, all pre-existing):** (a) struct VALUES don't reach LIR (aggregate MIR Const/'<deferred>' ops have no lowering) → no runtime corpus arm exists for ANY compound literal; (b) scalar compound literals — valid C 6.5.2.5p9 — stay aggregate-gated at the HIR brace-init lowering (deliberate fail-loud, pinned); (c) typedef names in DECLARATION type position (`MyP p = ...;`) and the `typedef struct P {...} MyP;` body form do not PARSE (varDeclHead/typedefDecl ride `typeRefAllowingStruct`, no Identifier base) — surfaced by this sweep's probes, distinct grammar work; residue (c) PROMOTED 2026-06-11 (audit-residue sweep c2, `D-AUDIT-BOOKKEEPING-TRUTH` (e)) to the OPEN row `D-CSUBSET-TYPEDEF-DECLARATION-POSITION` (below), CLI-probe re-verified. | (closed at the typedef-admission scope; residue (a)-(c) above are pre-existing, separately triggerable) | `c-subset.lang.json` compoundLiteralExpr + semantics.compoundLiterals / `semantic_config.hpp` CompoundLiteralRule / `grammar_schema_json.cpp` loader / `semantic_analyzer.cpp` Pass-2 / plan 23 |
| `D-CSUBSET-TYPEDEF-DECLARATION-POSITION` | ✅ **CLOSED 2026-06-11 (FC4 c1) — scope: declaration STATEMENTS + params + typedef heads.** Typedef names now declare in: LOCAL declaration statements (`MyP * p;` / `myint x = 40;` — the `identVarDecl`/`declOrExprStmt` speculative path under the PreferType triage, plus the committed specifier/qualifier-led `declHead`/`kwDeclHead` Identifier arms), TOP-LEVEL declarations (`topLevelHead` Identifier arm — unambiguous, no expression statements at file scope), PARAM position (`declHeadForParam`), for-inits (`forIdentDecl`), and TYPEDEF HEADS themselves (`typedefHead` Identifier arm + the struct/union/enum `*SpecifierBody` forms — `typedef struct P { int a; } MyP;` and the ANONYMOUS `typedef struct { int z; } Anon;` both parse + mint). Pins: `TypedefNameStarDeclaresPointerLocal` / `TypedefNameStarCommitsDeclarationStatement` (semantic + parser tiers), `TypedefFnPtrAliasDeclaresSameInternedType`; corpus `typedef_decl_position` (4-target runtime 42 — `myint x` + `MyP *pp` declaration-position witnesses) + `typedef_struct_body` (tagged + anonymous bodies, runtime 42) + `fnptr_declare` (`H h = fp;`). **The REMAINDER lives in `D-CSUBSET-TYPEDEF-FIELD-EXTERN-POSITION` (below): structField/unionField/externDecl kept the legacy `typeRef` spine, so typedef-named FIELDS and EXTERNS (+ declarator-shaped externs) still do not parse — explicitly NOT closed here.** — *Original row (audit trail):* **OPEN (created 2026-06-11, audit-residue sweep c2, `D-AUDIT-BOOKKEEPING-TRUTH` (e)) — promoted from the `D-CSUBSET-COMPOUND-LITERAL-TYPEDEF` closed row's buried residue (c).** RE-VERIFIED by CLI probe this sweep: `typedef struct P { int a; } MyP;` does NOT parse (`P0001` expected Identifier got `{` at 1:18 — typedefDecl rides `typeRefAllowingStruct`, which admits the struct KEYWORD reference but not a struct BODY in typedef position) and `MyP p;` does NOT parse in DECLARATION position (`P0009` at the `;` — varDeclHead/typeBase lack the bare-Identifier base that `castTypeRef` gained in FC2/FC3.5; typedef names work in CAST and COMPOUND-LITERAL position only). So a typedef'd struct can be named in expressions but never declared as a variable's type — the declaration grammar is the gap, the binder sketch machinery (FC2) already resolves typedef names. | (closed at the declaration-statement/param/typedef-head scope; field/extern remainder = `D-CSUBSET-TYPEDEF-FIELD-EXTERN-POSITION`) | promoted from `D-CSUBSET-COMPOUND-LITERAL-TYPEDEF` residue (c); plan-23 §0.1 FC4 row; `D-CSUBSET-TYPEDEF-FIELD-EXTERN-POSITION` |
| `D-CSUBSET-FN-PROTOTYPE` | ✅ **CLOSED 2026-06-22 (FC-deferral close-out loop cycle 3, branch p14).** A C function PROTOTYPE (`int f(int);`) is now a function DECLARATION that MERGES with a later definition into ONE symbol — forward/mutual recursion works (the registry trigger). **As-built (plan-locked GO-WITH-FIXES, 4 MF folded; delegated build + 3 independent verification passes):** the 3-pass pre-order already pre-binds all symbols (forward CALLS already worked); the merge hooks the Pass-1 bind-collision site with a 4-CASE direction table keyed on proto-vs-def (`SymbolRecord.isProtoDeclaration` set SYNTACTICALLY at Pass 1 via a shared `hasFnSuffixOnName` — a fn-suffix on the name's direct declarator, distinguishing a proto from a fn-pointer): **proto→def** = the DEFINITION wins the binding (`injectBinding`), proto absorbed; **def→proto / proto→proto / extern→proto** = redundant decl, keep prior, absorb the new (its name binds to its OWN record so the HIR skip + compat compare both work — a deliberate, documented deviation); **def→def / extern→def** = the existing `S_RedeclaredSymbol` (two bodies / scope-out). Pass 1.5 UPGRADES a proto's kind Variable→Function (callable) instead of `S_InvalidFunctionDeclarator`. A post-Pass-1.5 sweep compares each merged pair's resolved FnSig TypeId (interner equality = STRUCTURAL: return type + params + count + cc + variadic) → NEW `S_IncompatibleRedeclaration` (0xE022) on mismatch. The HIR lowerer emits NO node for an `isProtoDeclaration || isAbsorbedProto` declarator (no spurious FnSig-typed Global — the pre-change silent path). **Fail-loud (all verified):** incompatible sig → S0022; a proto declared-but-called-but-never-defined → loud `H0009` unbound Ref at HIR→MIR (NOT a silent FnSig-global read — the make-or-break the plan-lock caught); two real defs / extern+def → S0002. AGNOSTIC (merge reads DeclarationKind/effectiveKind/directRule/fnSuffixRule from config, no identity branch). **★ Found+fixed a LATENT pre-existing issue:** 10 MIR/LIR variadic tests declared the callee as a BARE prototype `int sink(int n, ...);` and leaned on the now-removed spurious-FnSig-global behavior (the call resolved through a data global despite an unchecked S_InvalidFunctionDeclarator) — corrected to DEFINE the callee (assertions unchanged). **Witnesses:** corpus `fn_prototype` (mutual recursion `is_even`/`is_odd` via forward proto, value-divergent → exit 42) on x86_64-PE (ctest) + x86_64-ELF (WSL) + arm64-ELF (qemu) [macho macos leg]; `fn_prototype_incompat_error` (expectDiagnostics S_IncompatibleRedeclaration 6:5); 5 semantic unit pins (proto→def merge / idempotence / def→proto / incompatible-loud / forward-call) DEMONSTRATED red-on-disable (merge off → 3 tests + corpus fail). 364/364 ctest; independent plan-lock + pr-review (empirical, runtime-witnessed the use-resolution ordering) + my own diff/gate/CLI review — all CLEAN. **RESIDUALS (fail-loud, named exclusions — NOT silent):** (1) `D-CSUBSET-BLOCK-SCOPE-PROTOTYPE` (below) — a block-scope proto (C 6.7.6.3, `int g(int);` inside a function) SHADOWS the file-scope function rather than referring to it, so a block-proto + file-def fails loud (H0009) instead of resolving to the file def; (2) `D-CSUBSET-EXTERN-DEFINITION-MERGE` (below) — `extern int f(int); ...def` stays S_RedeclaredSymbol (the extern→ExternFunction machine differs; bare-proto+def is the C-idiomatic forward decl and the deliverable). — ORIGINAL DEFERRAL (created 2026-06-11, FC4 c1 stage 2a): the FC4 declarator grammar makes a C function PROTOTYPE (`int f(int);` — declaration-without-definition, at file or block scope) PARSE for the first time (head + declarator-with-fnSuffix + `;`), but declaration-merging (a later definition completing the prototype) and prototype-only emission are NOT implemented — `extern` declarations carry the cross-unit-declaration role today. The semantic tier fails LOUD: a Variable-kind declarator whose folded type is a bare `FnSig` emits `S_InvalidFunctionDeclarator` ("function prototype declarations are not supported here; use 'extern'") rather than silently minting an FnSig-typed data global (which would miscompile at MIR). Fn-POINTER objects (`int (*fp)(int)`) are unaffected (type = Ptr<FnSig>, fully supported). | (closed) | (closed) | FC4 stage 2b registry sweep; `semantic_analyzer.cpp` Pass-1 4-case merge + Pass-1.5 proto upgrade + compat sweep; `cst_to_hir.cpp` proto-node skip; `S_IncompatibleRedeclaration` 0xE022; plan 23 FC4 |
| `D-CSUBSET-BLOCK-SCOPE-PROTOTYPE` | ✅ **CLOSED 2026-06-24 (robustness sweep, user-authorized — the user's authorization fired the "robustness sweep" trigger; branch feature/0-0-2-p14, 393/393 ctest MSVC Release + MinGW).** As-built: a block-scope function prototype now RE-HOMES onto the file scope (new `fileScopeOf()` helper in `semantic_analyzer.cpp`; Pass-1 `if(isProto) bindScope=fileScopeOf(...)`) so the existing FC fn-prototype 4-case merge (`D-CSUBSET-FN-PROTOTYPE` ✅) resolves it against the file definition (C 6.2.2p4 — a no-linkage block decl of a function refers to the external); `+ warnIfUnused = decl.warnIfUnused && !isProto` (a re-homed proto is never an unused-variable). Corpus `block_scope_prototype` (forward mutual call → exit 42) + 4 red-on-disable pins. An undefined re-homed proto still fails loud H0009; an incompatible signature → S_IncompatibleRedeclaration. — *Original OPEN row (audit trail):* **OPEN — normal (created 2026-06-22, FC-deferral close-out cycle 3 self-audit, branch p14).** A block-scope function prototype (C 6.7.6.3 — `int g(int);` inside a function body) is treated as a DISTINCT symbol that SHADOWS the file-scope function rather than referring to it (same external linkage). Consequence: `int main(void){ int g(int); return g(20); } int g(int n){...}` FAILS LOUD — the call resolves to the bodyless block-scope proto (upgraded to Function kind, callable, but no body in its scope) → `H0009` HIR Ref to unbound symbol at HIR→MIR. NOT a silent miscompile; a fail-loud limitation with a poor (internal-shaped) diagnostic. The C-idiomatic + common forward declaration is FILE-scope (`D-CSUBSET-FN-PROTOTYPE` ✅, the deliverable + trigger); a block-scope prototype is the rarer C 6.7.6.3 form. | Trigger: a user/corpus need for block-scope prototypes, or the robustness sweep. | **Closing work:** a block-scope function-typed proto must resolve to (or merge with) the file-scope function of the same name with external linkage (C 6.2.2p4 — no-linkage block decl of a fn refers to the external) rather than minting a distinct shadowing symbol; OR, at minimum, a precise diagnostic ("block-scope function prototypes are not supported; declare at file scope") instead of the internal H0009. | `D-CSUBSET-FN-PROTOTYPE` (✅, the file-scope merge); `semantic_analyzer.cpp` Pass-1 bind scope (block vs file) |
| `D-CSUBSET-EXTERN-DEFINITION-MERGE` | ✅ **CLOSED 2026-06-24 (robustness sweep, user-authorized — the user's authorization fired the "robustness sweep" trigger; branch feature/0-0-2-p14, 393/393 ctest MSVC Release + MinGW).** As-built: a new config flag `nonDefiningDeclaration` (on `DeclarationRule`, set on c-subset's `externDecl`) + `SymbolRecord.isExternDeclaration`; the proto-merge was GENERALIZED into a shared `mergeOrCollideRedeclaration()` keyed on non-defining (proto OR extern) + a function/object category guard, called from BOTH Pass-1 mint paths (the declarator-mode path AND the legacy positional path); HIR `lowerExternDecl` suppresses the absorbed extern node. `extern int g; int g=5;` (objects AND functions, both orders, idempotent) now MERGES → the definition wins. Corpus `extern_definition_merge` (+ `extern_definition_merge_incompat_error` negative) + 8 pins, red-on-disable. An incompatible extern+def → S_IncompatibleRedeclaration; two real/tentative (non-extern) defs still collide S_RedeclaredSymbol (→ the new backlog row `D-CSUBSET-TENTATIVE-DEFINITION-MERGE`). — *Original OPEN row (audit trail):* **OPEN — normal (created 2026-06-22, FC-deferral close-out cycle 3, branch p14).** `extern int f(int);` followed by a same-TU definition `int f(int x){…}` collides `S_RedeclaredSymbol` (S0002) — the prototype/definition merge (`D-CSUBSET-FN-PROTOTYPE` ✅) does NOT extend to an explicit `extern` declaration, because an `extern` routes through a DIFFERENT machine (the `externDecl` rule → `ExternFunction` HIR → `collectExterns` import row, which requires FFI metadata) than a bare proto (Variable-kind → the new merge path). The C-idiomatic forward declaration is the BARE prototype `int f(int);` (which merges); `extern` on a function is redundant + rarer. Fail-loud (S0002), NOT silent. | Trigger: a user/corpus need for extern-decl + same-TU-definition merge. | **Closing work:** when an in-TU definition follows an `extern` function declaration of the same name, the definition WINS and the extern's `collectExterns` import row is suppressed (absorbed), mirroring the bare-proto merge but across the ExternFunction↔Function boundary. | `D-CSUBSET-FN-PROTOTYPE` (✅, bare-proto merge); `cst_to_hir.cpp` lowerExternDecl / `hir_to_mir.cpp` collectExterns |
| `D-CSUBSET-TENTATIVE-DEFINITION-MERGE` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c33).** sqlite3.c:16250 then 24217 — `SQLITE_PRIVATE u32 sqlite3WhereTrace;` (a file-scope TENTATIVE definition, no initializer) then `SQLITE_PRIVATE u32 sqlite3WhereTrace = 0;` (the real definition) collided `error[S0002] S_RedeclaredSymbol` (also `sqlite3_temp_directory`/`sqlite3_data_directory`). C 6.9.2: a file-scope object declared with NO initializer and NO `extern` is a TENTATIVE definition — any number of tentatives + at most ONE real (initialized) definition of the same name MERGE into ONE object. ROOT: only an `extern`/proto NON-DEFINING declaration was treated as mergeable (`D-CSUBSET-EXTERN-DEFINITION-MERGE` ✅, via `nonDefiningDeclaration` + `mergeOrCollideRedeclaration`); a bare tentative was minted as a full DEFINITION, so the merge's `bothDefinitions` arm fired → S0002. ✅ FIX (the non-defining axis EXTENDED, AGNOSTIC — config roles only, no lang/arch/format/keyword hardcode): the Pass-1 variable bind computes `isTentativeDefinition` = (effectiveKind Variable) ∧ ¬isProto ∧ ¬`nonDefiningDeclaration` ∧ a declarator-LIST row (`declaratorListChild` — EXCLUDES single-`declaratorChild` PARAMETER rows, so a prototype param sharing a name never merges) ∧ bindScope == `fileScopeOf(current)` (FILE-scope only — a block-scope `int x; int x;` and a struct/union MEMBER dup, bound in a body scope, STAY S0002) ∧ the init-declarator carries NO initializer (a NEW structural `declaratorHasInitializer` helper — the init-declarator's lone non-declarator visible-Internal child, mirroring the HIR global-init lowering so the two can't drift). It folds into the merge's non-defining flag (`newNonDef = isProto || isExtern || isTentativeDefinition`) AND is RECORDED on a NEW `SymbolRecord.isTentativeDefinition` (so a LATER redeclaration sees THIS symbol as `priorNonDef` and merges). The 4-case `mergeOrCollideRedeclaration` table then handles every shape WITHOUT a new state: tentative→def = the def wins the binding + its init; tentative↔tentative / def→tentative = keep the survivor (one surviving object); def→def (BOTH initialized) = S0002 STAYS. A TYPE CONFLICT (`int g; int* g = 0;`) fails loud `S_IncompatibleRedeclaration` via the EXISTING post-1.5 merged-decl type-compat sweep (interner-TypeId identity → target-aware: `int g; long g = 5;` MERGES under LLP64 where `long`≡`int` but conflicts under LP64/ELF where `long` is 64-bit — VERIFIED both targets). NO HIR/MIR change — the surviving (non-absorbed) symbol lowers to ONE global with the right init via the existing `isAbsorbedProto` skip + the `.bss` tentative-zero-init path. ★ Two existing tests UPDATED to match conformant C (real GCC accepts both): `test_declarator_engine` `SameNameTwiceInOneListRedeclares`→`…TentativeDefinitionsMerge` (`base x, x;` — two same-list tentatives — now MERGES, was asserted-collide); the FFI `DuplicateExternRedeclarationRejectedByFrontend` STAYS green (the param-exclusion preserves its prototype-param collision). WITNESS: 7 semantic pins (tentative+def / two-tentatives / static-tentative+def MERGE; ★ PRESERVE two-real-defs collide + block-scope dup collide + type-conflict fails-loud; extern+def still merges) + runtime corpus `tentative_definition` (tentative+def + def+tentative + two-tentatives, each read across a NON-INLINED call → exit 42; 4 targets pe/elf/qemu-arm64/macho + release). 454→455 ctest. — *Original OPEN row (audit trail):* **OPEN — normal (created 2026-06-24, robustness sweep, branch feature/0-0-2-p14).** Two NON-extern OBJECT declarations of the same name at file scope — `int g; int g;` or `int g; int g = 5;` (C 6.9.2 TENTATIVE definitions: a file-scope object declaration with no initializer and no `extern` is a tentative definition that coalesces with a later real or tentative definition into a single object) — still collide LOUD `S_RedeclaredSymbol`. Only an `extern` NON-DEFINING declaration merges with a definition (`D-CSUBSET-EXTERN-DEFINITION-MERGE` ✅, via the `nonDefiningDeclaration` flag); a bare tentative definition is NOT marked non-defining, so the shared `mergeOrCollideRedeclaration()` collide-arm fires. Rare, FAIL-LOUD (S0002), NOT a silent miscompile. | (closed) | (closed) | p19 Cluster G c33; cited in `src/analysis/semantic/semantic_analyzer.cpp` (`isTentativeDefinition` Pass-1 fold + `declaratorHasInitializer` + `mergeOrCollideRedeclaration` `priorNonDef`) + `src/analysis/semantic/semantic_model.hpp` (`SymbolRecord.isTentativeDefinition`) + `examples/c-subset/tentative_definition/`; GENERALIZES `D-CSUBSET-EXTERN-DEFINITION-MERGE` (the non-defining/extern axis) + `D-CSUBSET-FN-PROTOTYPE` (the 4-case merge table); a sqlite3.c re-probe frontier; plan 23 full-C arc / SQLite-green. |
| `D-CSUBSET-FNPTR-INDIRECT-CALL` | ✅ **CLOSED 2026-06-12 (FC4 c2) — indirect calls end-to-end.** `int (*fp)(int) = &helper; return fp(40) + 2;` EXECUTES exit-42 on x86_64 PE (local ctest) + x86_64 ELF (WSL native) + arm64 ELF (qemu-aarch64); **Mach-O arm RUNTIME-WITNESSED 2026-06-12: the `macos-clang-release` leg (run 27397122399 on `a095f43`) ran the fnptr_call/fnptr_callback/fnptr_regalloc_pressure darwin arms GREEN on Apple Silicon — BLR-through-fn-pointer executed exit-42 on real arm64-darwin**. Mechanism: semantic callee PEEL (descend paren-group wrappers + peel declared-`hirLowering.unaryOps`-Deref tokens with decay algebra; FnSig → checked direct, Ptr<FnSig> → unwrap → `checkCallAgainstSig` shared tail — paren-wrapped directs `(helper)(...)` UPGRADED to arity-checked) + HIR deref-of-fn decay fold (C 6.5.3.2p4 — `(*fp)(x)` no longer loads through the code pointer) + `calleeSigOf` param-coercion in BOTH call arms + LIR materializer Reg-callee branch sharing ALL arg planning + **the 3-layer cc-config-driven callee-reg exclusion** (R2 regalloc argGprs∪argFprs∪countReg exclusion, red-on-disable demonstrated; B2 spill-reload scratch filter; B1 `L_IndirectCalleeClobberedByArgSetup` 0xB00B loud backstop) + additive same-mnemonic `call` ["reg"] variants (x86 `FF /2`, arm64 `BLR`; byte-pins FF D0 / 41 FF D2 / 0xD63F0120). `S_IndirectCallNotSupported` 0xE015 RETIRED (defined, both emit arms removed); `L_IndirectCallUnsupported` stays ALIVE re-messaged (fail-loud totality for residual callee kinds). Corpus: `fnptr_call` (replaces the deleted `fnptr_call_error`), `fnptr_callback` (self-recursive apply — SCC-refusal unit-pinned, can never devirtualize; its optimized arm = the 7-pass full-release-like set — an Inlining-bearing arm is GATED on `D-OPT-RELEASE-PIPELINE-CONTROL-FLOW-COMPOSITION`, the pre-existing loud release-pipeline×recursion engine bug the c2 audit surfaced), `fnptr_regalloc_pressure` (non-vacuity audit-verified: cross-call-exhaustion spill logged). The SQL call arm shares `calleeSigOf` by construction (behavior-preserving); its Ptr<FnSig> branch awaits the first fn-ptr-bearing SQL-lowered language. 263/263. — *Original row (audit trail):* **OPEN (created 2026-06-11, FC4 c1 stage 2b). Priority: high — c2-NEXT.** Declarators TYPE fn-pointers end-to-end (`int (*fp)(int) = &helper;` — Ptr<FnSig>, `&fn` lowers to GlobalAddr, the pointer VALUE flows through locals/typedef aliases/call args at RUNTIME: corpus `fnptr_declare` exit-42 incl. a full-release-like optimized arm), but calling THROUGH the pointer has no backend encoding. The semantic tier WALLS it loudly + positioned with `S_IndirectCallNotSupported` (0xE015) at the `checkCall` gate (semantic_analyzer.cpp — TYPE-driven: lattice `Ptr` whose operand is `FnSig`, zero language identity): (a) bare-identifier callee typed Ptr<FnSig> (`fp(3)` — pre-gate this miscoded as S_NotCallable), (b) non-identifier callee whose STAMPED expression type is Ptr<FnSig> (`((H)fp)(3)` — pre-gate an opaque HIR H0001); a stamped non-callable type → S_NotCallable; an UNSTAMPED callee (e.g. `(*fp)(3)` — no deref-typing arm yet) conservatively stays silent at semantic and dies LOUD at the LIR wall `L_IndirectCallUnsupported` (callconv, the `D-ML7-2.4` comment anchor in mir_to_lir.cpp — pinned by tests/lir/test_lir_callconv.cpp). Pins: `BareFnPtrIdentifierCallFiresIndirectGate` (+Cast/NotCallable/ParenDirect/DerefSilent siblings, test_semantic_analyzer_c_subset.cpp) + corpus `fnptr_call_error` (expectDiagnostics, positioned 16:12). | **Closing work (FC4 c2):** LIR call-via-register opcode (capability-declared) + x86 `FF /2` (call r/m64) + arm64 `BLR` encodings + the callee-reg-vs-arg-reg regalloc exclusion (the callee register must not clobber/be-clobbered-by arg materialization) + flip the `fnptr_call_error` corpus example to RUNTIME exit-42 (its manifest `$comment` pre-stages the flip) + remove/re-home the semantic wall + the `D-ML7-2.4` comment anchor in mir_to_lir.cpp + revisit the stamped-FnSig silent arm in `checkCall` (deref typing). | plan 23 FC4; `D-ML7-2.4` (plan 12 — the LIR-tier indirect-call encoding owner); `S_IndirectCallNotSupported` 0xE015 |
| `D-CSUBSET-VOLATILE-SEMANTICS` | **OPEN (created 2026-06-11, FC4 c1). Trigger: FC16 (the optimizer-semantics phase).** `volatile` is grammar-ADMITTED (headQualifier/ptrQualifier rows in c-subset.lang.json) but its semantics — every access is an observable side effect: no caching, no reordering, no elision by LICM/CSE/DCE/Mem2Reg — are NOT implemented. The config-driven `gatedMarkers` rows (token `VolatileKeyword` → `S_VolatileNotSupported` 0xE014, on all six declaration rows) are the WALL: every volatile-qualified declaration fails loud + positioned at the marker token instead of silently compiling the object as plain memory (a latent miscompile for MMIO/signal-handler code). Pins: `GatedMarkers.*` (test_declarator_engine.cpp, synthetic grammar) + corpus `volatile_error` (expectDiagnostics 11:5) + the stage-2b red-on-disable demo (strip the varDecl row's gatedMarkers → the example compiles → RED). | Honor volatile in the optimizer tiers (LICM hoist refusal, CSE load-merge refusal, DCE store-elision refusal, Mem2Reg promotion refusal for volatile-qualified storage) + thread the qualifier through HIR/MIR types or attrs, then DROP the gatedMarkers rows (the wall dissolves; the grammar already parses). | plan 23 FC16; `c-subset.lang.json` gatedMarkers; `S_VolatileNotSupported` 0xE014 |
| `D-CSUBSET-LOCAL-STATIC` | ✅ **CLOSED 2026-06-22 (FC-deferral close-out loop cycle 2, branch p14).** Block-scope `static` locals get STATIC storage duration (C 6.2.4) — lowered to a hidden module-global (internal `{Local}` linkage, function-internal NAME, once-only LOAD-TIME init) — so the value PERSISTS across calls. REUSES the file-scope-global machinery (collectGlobals → globalSymbols → GlobalAddr → `.data`/`.bss`), ZERO parallel mechanism. **As-built (plan-locked, GO-WITH-FIXES, 3 MF folded):** a new STORAGE-DURATION axis `staticStorage` on `LinkageSpecifierEffect` (`semantic_config.hpp` + loader) folded from the SAME `linkageFrom` specifier-prefix scan (the `varDecl` row maps `"static"→{binding:local, staticStorage:true}`; register/auto/attrs ignored so they don't trip the unknown-specifier diagnostic) — AGNOSTIC (the engine never compares the word "static"); `cst_to_hir` routes the static local to `makeGlobal` + `recordLinkage({Local})` + `recordMutability` + a `moduleDecls_` accumulator (so it joins moduleDecls without sitting in the function body), appending NOTHING to the body (empty-Block placeholder). The NAME-MANGLING story the original deferral worried about is UNNECESSARY: distinct sibling-block statics mint distinct SymbolIds (distinct globals), and `{Local}` binding is intra-module-by-id (skipped by cross-CU resolve + linker), so the shared source name never collides. **MF-1 (plan-lock "critical silent gap") REFUTED empirically:** a non-const static init does NOT silently accept — it fails LOUD at the asm tier (`K_NoMatchingObjectFormat` / `D-LK4-RODATA-PRODUCER-RUNTIME-INIT`, even under release+DCE/dead) — the plan-lock traced classifyGlobals→runtimeInit but missed `asm.cpp`'s unconditional `if(initFn.valid())` rejection; fail-loud bar MET (precise C 6.7.9p4 diagnostic = a pinned polish residual below). **MF-2:** `static` in a for-init is a C 6.8.5p3 violation → NEW `S_StaticStorageInForInit` (0xE021) + a `forDecl` gatedMarker (fail loud, never silent auto-local). **MF-3:** fail-loud guards on the legacy path + null `moduleDecls_`. **Witnesses:** corpus `local_static` exit 42 (scalar counter persistence + static ARRAY persist+index + two SIBLING same-name statics proving distinct storage; value-divergent → a re-init/stack bug = exit 7; baseline + full-release-like + shipped-release) on x86_64-PE (ctest) + x86_64-ELF (WSL) + arm64-ELF (qemu) [macho macos leg]; `local_static_for_error` (expectDiagnostics S_StaticStorageInForInit 9:10); 2 HIR structural pins (static→1 Global; 2 siblings→2 Globals) DEMONSTRATED red-on-disable. Independent pr-review + dss-audit both VERIFIED-CLEAN (audit also swept shadowing/read-before-assign/init-refs-sibling/uncalled-fn/const/array/struct/address-taken — all work or fail-loud; ZERO silent miscompile). 362/362 ctest. **RESIDUALS (all fail-loud, pre-existing, NOT this anchor's scope):** (1) a precise C 6.7.9p4 "static initializer must be constant" diagnostic — today the generic D-LK4 asm-tier error fires (loud but cryptic) — POLISH, tied to the `D-LK4-RODATA-PRODUCER-RUNTIME-INIT` work; (2) a static (or file-scope) pointer/string init the global classify path can't const-fold to static data (`static int *p=0;` ≡ file-scope `int *gp=0;` — both fail loud identically, pre-existing global-emission limitation under D-LK4). — ORIGINAL DEFERRAL (created 2026-06-11, FC4 c1): Block-scope `static` (`static int counter = 0;` inside a function — C static-storage-duration locals) is a PARSE error today: `localDeclSpecifier` deliberately admits only `register`/`auto`/attributes (its grammar `$comment` pins the exclusion), and `static` at file scope is the LINKAGE specifier (`declSpecifiers`/Local binding), a different role. Fail-loud, never silently auto-storage. | (closed) | plan 23 FC4 residue; `semantic_config.hpp` LinkageSpecifierEffect.staticStorage; `cst_to_hir.cpp` lowerVarLikeInto/linkageFrom/moduleDecls_; `c-subset.lang.json` localDeclSpecifier/varDecl/forDecl; `D-LK4-RODATA-PRODUCER-RUNTIME-INIT` (the precise-diagnostic + pointer-init polish residuals) |
| `D-CSUBSET-STRUCT-BODY-VARDECL-POSITION` | ✅ **CLOSED 2026-06-17 (struct-head grammar + aggregate-global codegen cycle).** Both the BODY heads (`struct P { int a; } v;`, `struct P { int a; };`) and the bare tag-REF heads (`struct Tri mkTri(void)` / `struct Q g;`) now parse + compile + RUN, incl. multi-level/recursive nested-body struct fields — corpus `struct_body_nested` + `struct_body_top_level` exit 42 on x86_64-PE (host) AND arm64-ELF (qemu); macho on the macos-latest leg. **As-built:** `topLevel` folded the bare composite defs into `topLevelDecl` (init-declarator-list made OPTIONAL) + a NAMED speculative `topLevelCompositeSpec` rule (`*SpecifierBody`/`*TypeRef`, lookahead 256 — inline alts fail at decl depth, L1); the speculative-enum-body FOLLOW miss was root-caused + fixed in the schema compiler (`D-PARSE-SCHEMA-NESTED-NULLABLE-FOLLOW` ✅); semantic tags/enumerators FLOAT past the param-dominator `topLevelDecl` scope (`floatToNamespaceScope`, declByRule/fieldChildren-driven, NO rule-name branch) + the parse-time binder-sketch mirror; `fieldIndex` densified at the type-composition chokepoint. Aggregate-global codegen landed (`D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL` ✅ — initialized struct/array globals → byte-exact rodata, `struct_body_top_level` exit 42). The now-grammar-parseable `int ;` (no declarator, no tag) fails loud `S_DeclarationDeclaresNothing` (was a `tree.rule()`-on-leaf crash — fixed by a `findCompositeSpecifierIn` Internal guard). Dead `structDecl`/`unionDecl`/`enumDecl` rules + rows DELETED (no dead config). 318/318. Independent review SHIP-IT. — ORIGINAL (created 2026-06-11, FC4 c1): A struct/union/enum BODY in a NON-typedef declaration head (`struct P { int a; } v;` / `struct P { int a; };` as a bare statement or top-level declaration) does not parse: only `typedefHead` admits the `*SpecifierBody` forms; `typeSpecifierForDecl`/`topLevelHead` admit keyword seqs + (for locals) struct/union/enum REFERENCES (`struct P v;` works once P is declared via the statement-position `structDecl`). The composite-definition machinery (fieldChildren, anonymousNameAllowed) already exists — this is grammar admission + the decl-head/Specifier-body bridging only. **UPDATE 2026-06-16 (FC7 C1c):** the sibling REF-form gap surfaced — a bare `struct Tag` HEAD (no body) as a top-level RETURN or variable type (`struct Tri mkTri(…)` / `struct Tri g;`) also fails to parse, because `topLevelHead` omits `structTypeRef`/`unionTypeRef`/`enumTypeRef` (the `topLevel` alt greedily commits to `structDecl`, expecting `{`); the disambiguation needs `topLevel` speculation (the proven `typedefHead` pattern). FC7 C1c (struct returns) reaches the feature via the **typedef-name** return form instead, so this stays the pre-FC4 named-exclusion (fail-loud parse error, never a miscompile); both the BODY and REF forms close together when this grammar work lands. | Admit `structSpecifierBody`/`unionSpecifierBody`/`enumSpecifierBody` AND the `*TypeRef` (no-body) forms in the declaration-head alts (`typeSpecifierForDecl`, `topLevelHead`) + make `topLevel` speculative (mirror `typedefHead`) so `struct Tag {` → `structDecl` vs `struct Tag <declarator>` → `topLevelDecl` disambiguates; resolve the minted composite as the head type for the declarator list; corpus declaring + using a body-defined struct variable AND a `struct Tag`-returning function. | plan 23 FC4 residue; `c-subset.lang.json` typeSpecifierForDecl/topLevelHead $comments |
| `D-CSUBSET-INT-CROSS-SIGNEDNESS-CONVERT` | ✅ **CLOSED 2026-06-22 (FC-deferral close-out loop cycle 1, branch p14).** New `intCrossSignednessConverts` gate (`semantic_config.hpp`, c-subset ON; default FALSE keeps non-C schemas strict) + an `isAssignable` cross-signedness arm (`type_rules.hpp` — admit when one side is a signed-int rank and the other unsigned-int rank, BOTH directions, ALL widths incl. cross-signedness NARROWING U64→I32; RANK-keyed, ZERO target/lang identity) threaded to the 4 callers (`semantic_analyzer.cpp` 2916/2964 init, 3285 call-arg, 4112 return) + HIR `coerce()`'s arithmetic-core arm materializing the width-exact Cast (int↔unsigned both `isArithmeticCore` → reinterpret/truncate; runtime-proven). **SCOPE GUARD:** signed↔unsigned ONLY — SAME-signedness narrowing stays strict, int↔float stays rejected. **Witnesses:** corpus `cross_signedness_convert` exit 42 (init/call-arg/return cross-sign + high-bit 0xFFFFFFFF→-1 + U64→I32 narrow→42, mutable-global anti-fold, baseline + full-release-like + shipped-release) on x86_64-PE (ctest) + x86_64-ELF (native WSL) + arm64-ELF (qemu) [arm64-Mach-O macos-latest leg]; unit `IsAssignableAdmitsCrossSignednessWhenGated` (gate-ON admits — RED-on-disable: revert the arm → 4 EXPECT_TRUE flip) + the unchanged `IsAssignableRejectsCrossSignedness` (gate-OFF rejects — conservative-default guard); the fc3 `expectLiteralTypes` helper was REDESIGNED to assert the literal's EXACT stamped type via `typeAt` (the old cross-sign-rejection proxy became invalid once the gate admits it — the direct assertion is STRICTLY STRONGER). End-to-end red-on-disable ALSO demonstrated: flipping the shipped JSON gate off → the example rejects S0003 at every gated site, compile exit 1. 2 independent reviews CLEAN; 360/360 ctest. **PROSE-CORRECTION (the original premise was partly wrong):** the bare assignment STATEMENT (`x = u;`) is NOT an `isAssignable` site — it carries NO semantic assignability check (only `S_ConstViolation`), so its Cast is materialized gate-INDEPENDENTLY by `coerce()` (always was, every schema). The 4 GATED sites are init(×2)/call-arg/return; this surfaced a SEPARATE pre-existing strictness asymmetry → new `D-SEMANTIC-ASSIGN-STMT-ASSIGNABILITY-BYPASS` (below). — ORIGINAL DEFERRAL (created 2026-06-18, FC8 cycle-5 investigation, branch p10): A cross-signedness implicit conversion in an ASSIGNMENT CONTEXT — `int x = u;`, `x = u;`, `return i+u;` (int-returning fn), `f(u)` (int param) where one side is signed and the other unsigned — is REJECTED `S_TypeMismatch` (S0003); a few return forms (unsigned literal, int→unsigned) surface `S_ReturnTypeMismatch` (S0008). **This is INTENTIONAL, UNIFORM, and already DOCUMENTED:** `isAssignable` (`type_rules.hpp` lines 83-86) deliberately makes cross-signedness (and int↔float) NOT assignable — *"languages requiring them widen their config rather than getting silent C-style implicit conversions."* It is NOT a return-specific bug: init / assignment / call-arg / return ALL reject identically through the one `isAssignable` rule (the originating report's "return inconsistent with assignment" premise was DISPROVEN — `int x = u;` rejects too, same S0003). **But** C 6.3.1.3 / 6.5.16.1 MANDATE an implicit signed↔unsigned assignment conversion (value-preserving in-range, modular out-of-range), so for the full-C arc + SQLite (which mixes `int`/`unsigned`/`size_t` freely) c-subset MUST admit it — via the SAME config-gated opt-in as Bool/Char/Enum, default-strict for non-C schemas. | (closed) | **Closing work (mirrors `D-CSUBSET-ENUM-INT-CONVERSION` / `D-CSUBSET-CHAR-INT-WIDENING` EXACTLY):** a new `intCrossSignednessConverts` gate in `semantic_config.hpp` (c-subset ON; default false) + an `isAssignable` cross-signedness arm (admit signed↔unsigned within the int ranks, both widening AND narrowing per C) threaded to the 4 semantic callers (`semantic_analyzer.cpp` 2792/2840 init, 3161 call-arg, 3988 return) + HIR `coerce()` materializing the int↔unsigned Cast (a width-exact reinterpret/mov — no value change at equal width) + strict red-on-disable tests + a runtime corpus (`return i+u` exit-code witness). | plan 23 FC; `type_rules.hpp` isAssignable (lines 83-86 document the current deliberate rejection); `D-CSUBSET-ENUM-INT-CONVERSION` + `D-CSUBSET-CHAR-INT-WIDENING` (exact templates); `semantic_config.hpp` conversion gates |
| `D-SEMANTIC-ASSIGN-STMT-ASSIGNABILITY-BYPASS` | ✅ **CLOSED 2026-06-24 (robustness sweep, user-authorized — the user's authorization fired the "robustness sweep" trigger; branch feature/0-0-2-p14, 393/393 ctest MSVC Release + MinGW).** As-built: the assign-STATEMENT arm (`assignByRule`) now routes through the SAME gated `isAssignable()` chokepoint the 4 init/call/return sites use (same conversion gates + null-pointer admission, S_TypeMismatch positioned at the RHS), gated to PLAIN assign (`target=="Assign" && compoundBase.empty()`, so compound `+=` is EXCLUDED as the arithmetic-conversion path). So `x = f;` now rejects like `int x = f;`, and cross-sign at an assignment statement is consistently GATED. Corpus `assign_stmt_assignability_error` + 4 pins, red-on-disable; blast-radius probe = zero over-rejection. **REGRESSION FIX 2026-06-24 (this cycle, branch feature/0-0-2-p14, 394/394 MSVC Release + MinGW + Debug):** routing the assign-stmt RHS through `isAssignable` exposed a PRE-EXISTING GAP in that chokepoint — it had NO function-to-pointer decay arm, so a bare function NAME assigned / initialized / passed to a matching function pointer (`fp = add` — the most common fn-ptr idiom: vtables / callbacks / dispatch tables) was wrongly rejected S0003, while `fp = &add` (already a pointer) passed (the asymmetry that isolated it; dss-state 90→88, `fn_fnptr_call` + `fn_fnptr_deref_call` regressed). FIXED by adding the C 6.3.2.1p4 function-to-pointer decay arm to the SHARED `isAssignable` chokepoint (`type_rules.hpp`, mirroring the array-to-pointer decay; ungated structural-decay family per `D-LANG-STRUCTURAL-DECAY-OPT-OUT`; pinned to the SAME interned FnSig so an incompatible-signature assignment stays a loud mismatch) — one arm clears assignment + initializer + call-argument at once (also clears `fn_fnptr_callback`, the related call-arg gap), with NO HIR `coerce()` change (a bare function Ref already lowers to its ADDRESS at MIR time, which is why `fp = add` compiled AND ran pre-sweep — only the new SEMANTIC check rejected it). Corpus `fnptr_bare_decay` (exit 42; bare assign + init + callback; fold-resistant self-recursive `apply`; baseline + full-release-like + shipped-release) + semantic pin `BareFunctionNameDecaysToPointerInEveryPosition` (3 positions + the incompatible-signature negative), BOTH red-on-disable DEMONSTRATED (arm disabled → the pin fails on all 3 positions AND the example fails to build). Closes the bare-decay coverage gap the suite lacked (the whole prior fnptr corpus used `&fn`). — *Original OPEN row (audit trail):* **OPEN — normal (created 2026-06-22, FC-deferral close-out cycle 1 self-audit, branch p14).** The bare assignment STATEMENT (`x = e;`) runs NO semantic assignability check — `subtreeType(Assign)` returns the LHS type without comparing operands, and the pass-2 assign arm checks only `S_ConstViolation`. Type compatibility is enforced only DOWNSTREAM: HIR `coerce()` materializes a width-exact Cast for any arith-core↔arith-core pair, and the HIR verifier catches truly-incompatible (non-arith) shapes. CONSEQUENCE — a strictness ASYMMETRY between the assignment statement and the `isAssignable`-checked init/call-arg/return sites: `int x = f;` (init, int←float) REJECTS S0003, but `x = f;` (assignment stmt) is SILENTLY ACCEPTED (coerce truncates float→int — a correct C VALUE, but the c-subset "no silent C-style implicit conversion" strictness is bypassed); likewise a non-C schema with `intCrossSignednessConverts` OFF rejects cross-sign at init/call/return yet accepts it at an assignment statement. NOT a miscompile (coerce's conversions are correct; non-arith mismatches still fail loud via the verifier as `H_VerifierFailure`) — a DIAGNOSTIC-QUALITY + consistency gap. PRE-EXISTING (independent of the cross-sign gate); empirically confirmed via CLI (`int x=f;`→S0003 exit 1; `x=f;`→exit 0). | Trigger: the robustness sweep / first user-visible inconsistency (assignment-stmt vs init for the same incompatible pair). | **Closing work:** route the assignment-statement RHS through the SAME gated `isAssignable` check as the 4 init/call/return sites (emit `S_TypeMismatch` on failure), so `x = f;` rejects like `int x = f;` AND cross-sign at assignment-stmt becomes consistently GATED (not gate-independent coerce); blast-radius check for existing assignment-stmt tests (int↔float forms currently passing to the verifier); a red-on-disable pin per direction. | `semantic_analyzer.cpp` assign pass-2 arm (~2992-3042) + `subtreeType` Assign (~3843); `type_rules.hpp` isAssignable; `cst_to_hir.cpp` coerce |
| `D-PARSE-SCHEMA-NESTED-NULLABLE-FOLLOW` | ✅ **CLOSED 2026-06-17 (struct-head cycle — the schema-compiler root fix for `D-CSUBSET-STRUCT-BODY-VARDECL-POSITION`).** AltChoice `expectedSet` was computed EAGERLY at build time, but `repeat` uses a tie-the-knot: the loop-entry slot is reserved as a placeholder `End` (EMPTY expectedSet), the loop BODY is built against it, THEN it is overwritten with the real AltChoice — so an `optional`/`alt` inside the body whose fall-through `cont` is that loop entry captured the placeholder's EMPTY set, permanently missing the loop's exit FOLLOW. Concretely the enum `enumerator (Comma enumerator?)*` before `}`: the trailing `{optional enumerator}` never learned `}` could follow → under a SPECULATIVE probe (`topLevelCompositeSpec`) the body parse hit `P_NoAlternativeMatched` (nullableTail=false, `}` not in the optional's expectedSet) → rolled back to the ref form → `{…}` mis-parsed. FIX: new monotone fixed-point pass `recomputeAltExpectedSets` re-unions every AltChoice position's `expectedSet = ∪ branches[i].expectedSet` until stable, sequenced AFTER `detectAmbiguousAlternatives` + BEFORE `computeFollowSets` (+ `Position::setExpectedSet`). Blast radius = EVERY grammar's expectedSets, but it only WIDENS to the correct union — 318/318 across toy/tsql/c-subset. RED-on-disable pinned by `HirLoweringCSubset.D5_5_EnumTrailingCommaParses` (top-level `enum E {…,};` — VERIFIED red when the orchestration call is toggled off, restored). **SCOPE:** fixes the TOP-LEVEL speculative surface (the NAMED `topLevelCompositeSpec`); the typedef-position surface (INLINE alts in `typedefHead`) stays a SEPARATE pre-existing gap → `D-CSUBSET-TYPEDEF-ENUM-TRAILING-COMMA`. | (closed) | `grammar_schema_json.cpp recomputeAltExpectedSets` + `compiled_shape.hpp setExpectedSet`; parent `D-CSUBSET-STRUCT-BODY-VARDECL-POSITION` |
| `D-CSUBSET-ENUM-INT-CONVERSION` | ✅ **CLOSED 2026-06-17 (FC8 cycle 1 "Enums end-to-end", branch p10).** An enum now behaves AS its underlying integer (C 6.7.2.2) end-to-end: config gate `enumConvertsToArith` (c-subset ON; default false keeps a non-C schema's Enum distinct) + `isAssignable` Enum↔int arm (BOTH directions, scoped to enum↔INT — a DIFFERENT-enum pair stays a loud mismatch) threaded to 4 callers + `isExplicitCastable` Enum + `enumUnderlyingOrSelf` resolving Enum→scalars[0] in `usualArithmeticCommonType`/`integerPromotedType` + HIR `coerce()` Enum↔int bridge + the `isEnumerator` symbol flag driving an enumerator Ref→Const(enumValue) fold at HIR identifier-lowering (a STORAGE-backed `enum E e;` local is NOT folded — guarded, no dropped-load miscompile) + `++/--` computed at the underlying int (`incDecArithType`) + MIR `mapCast` enum-underlying resolution. **REVIEW-CAUGHT & folded the SAME cycle (the planned "mapCast likely identity" was WRONG):** the LIR width helpers (`widthFlagsForType`/`memAccessWidthFlags`) did NOT resolve Enum → a PACKED enum struct-field/array-element store emitted an over-wide 64-bit access that CLOBBERED the neighbour (= `D-LIR-INT-MEMORY-WIDTH-EXACT` for enums; a scalar enum LOCAL sits in its own ≥8-byte slot and MASKED it — exactly the §A.5 multi-site-miss class) — fixed via a single `reprKind` chokepoint (Enum→underlying) routed through BOTH width switches. **Witnesses:** corpus `enum_value` exit 7 (every form incl. the packed enum-field width witness; red-on-regression → exit -93) on x86_64-PE (ctest) + x86_64-ELF (native WSL) + arm64-ELF (qemu) [+ arm64-Mach-O macos-latest leg]; 7 unit pins (4 `type_rules` + 3 `mir_lowering`) + 1 LIR width pin (`MirToLir.EnumPackedFieldMemoryAccessIsWidthExactToUnderlying`), ALL red-on-disable. **Still fail-loud / out of scope:** an enum GLOBAL's static-data emission (`scalarByteSize(Enum)`→nullopt) under `D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL` — enum↔int is expression/local VALUE semantics, not aggregate-global data. 319/319; review SHIP + fold re-review SHIP. — ORIGINAL DEFERRAL (created 2026-06-17, struct-head cycle): An enumerator used where an int is expected — `return BLUE;` (fn returns int), `(int)e`, `int x = BLUE;` — FAILS LOUD (`S_ReturnTypeMismatch` S0008 / `S_InvalidCast` S0010), never a silent miscompile. Pre-existing (affects `typedef enum {…} Color;` identically — NOT introduced by the head grammar) and ORTHOGONAL to the head-PARSING anchor (this is enum VALUE semantics). Enum is witnessed at the DECLARATION tier (parse, value computation, bare-name use, trailing comma — 120 c-subset HIR tests); struct carries the cycle's RUNTIME witness. | `isAssignable` Enum↔int arm gated like `charConvertsToArith` (the closed `D-CSUBSET-CHAR-INT-WIDENING` is the exact template) + HIR `coerce()` Enum→I32 cast + MIR `mapCast` Enum↔I32 (likely identity — enum is I32-backed via `enumType(name, TypeKind::I32)`). | plan 23 FC; `type_rules.hpp` isAssignable; `D-CSUBSET-CHAR-INT-WIDENING` (template) |
| `D-CSUBSET-BITFIELD` | ✅ **CLOSED 2026-06-18 (FC8 cycle 2 "bitfields", branch p10).** C bit-fields end-to-end (`struct/union { T x : W; }`), named + ANONYMOUS (`int : 3;`) + ZERO-WIDTH (`int : 0;`), signed + unsigned, read + write (incl. compound-assign / `++` via the read+write intercepts). **Type:** the per-field bit-width rides the interned Struct/Union `scalars` pool ((width+1) encoding; EMPTY scalars when no bitfield → every pre-bitfield composite interns bit-identically) — the width is part of type identity, so `computeLayout` (TypeId-only) packs without a semantic-model dep. **Layout:** `BitFieldStrategy` (`.target.json` `aggregateLayout.bitFieldStrategy`, default None=fail-loud; `gnu_packed` on both targets) + a GNU/SysV little-endian LSB-first packing algorithm in `type_layout.cpp` (a field that would straddle its type's unit boundary starts the next aligned unit; zero-width forces a unit break; `StructLayout.bitFields[]` carries per-field {unitBytes,bitOffset,bitWidth}). **Codegen:** `hir_to_mir.cpp` member-access READ = Load unit → shift+mask (signed: sign-extend via Shl+AShr); WRITE = read-modify-write (Load → clear → OR shifted value → Store) — reuses existing MIR Shl/LShr/AShr/And/Or, NO new substrate. **Fail-loud:** non-integer base (`S_BitFieldNonIntegerType`), width negative/>type-bits/zero-on-named (`S_BitFieldWidthOutOfRange`), anonymous-non-bitfield `int;` (`S_DeclarationDeclaresNothing`). **Witnesses:** corpus `bitfields` (round-trip + packing + cross-unit + signed sign-extension + neighbour-preservation + overflow-truncation) x86_64-PE (ctest) + x86_64-ELF (native WSL) + arm64-ELF (qemu) [arm64-Mach-O runtime witness CONFIRMED on the macos-latest leg — green continuously since 2026-06-18, latest run 28150522650]; layout bit-offset pins + semantic fail-loud pins + a host-independent LIR structural pin, all red-on-disable. **Scope (all fail-loud, never silent):** the ABI-EXACT per-platform layout (MS straddling, Apple specifics) is trigger-gated → `D-CSUBSET-BITFIELD-ABI-EXACT` (the §B decision, user-approved 2026-06-18); enum-typed bit-fields → `D-CSUBSET-ENUM-BITFIELD`. **REVIEW-CAUGHT (independent review) & folded to fail-loud the same cycle:** (a) aggregate INITIALIZATION of a bit-field struct (`struct S f={1,2};`/designated/global) would write each field full-width → clobber co-resident bit-fields → now rejected at MIR + the asm static-data backstop → `D-CSUBSET-BITFIELD-INIT` (field-by-field assignment is the supported path); (b) a >32-bit-base (`long long`) bit-field needs 64-bit unit codegen (incomplete `mov`/ALU encodings) → rejected at semantic for a clean diagnostic (the `constInt`→`constIntOfType` fix already made it fail-loud not silent) → `D-CSUBSET-BITFIELD-WIDE-UNIT` (✅ CLOSED 2026-06-18 — the real gap was wide-const materialization, not 64-bit ALU). So the supported set is 32-bit-and-narrower integer bit-fields, ACCESSED (read/write/compound-assign/`++`), initialized field-by-field. | (closed) | plan 23 FC8; `type_lattice.cpp` structType/unionType; `type_layout.cpp`; `hir_to_mir.cpp`; `semantic_analyzer.cpp`; `.target.json` aggregateLayout |
| `D-CSUBSET-BITFIELD-ABI-EXACT` | ✅ **CLOSED 2026-06-18 (FC8 deferral 3/3, branch p10; user-authorized §B — build now + all 3 ABIs, despite the FFI trigger not naturally firing: a SUPERVISED build, with a structural conformance witness standing in for the absent runtime/FFI consumer — the user was informed it is consumer-less until FFI and chose to build).** Bit-field layout is now BYTE-ABI-EXACT per platform, config-selected at the FORMAT/ABI level, verified against the native compilers. **As-built:** new `BitFieldStrategy::MsvcStraddle` (`aggregate_layout.hpp` + name converters); `type_layout.cpp` Struct/Union bit-field arms `switch` on the strategy enum (GnuPacked = LSB-first declared-type-unit packing; MsvcStraddle = the MS rule: a unit is reused ONLY when the next bit-field's declared-type SIZE matches AND fits — any type-size change / intervening ordinary field / zero-width / straddle opens a fresh type-aligned unit at the high-water mark, and struct size covers the last unit's full declared-type width; None/unrealized = fail-loud) — NO target/format identity, switch on the config enum only. **Config moved to the FORMAT/ABI level** (the rule is format-determined: x86_64 serves both ELF-SysV and PE-MS): `bitFieldStrategy` added to the object-format schema (`object_format_schema.{hpp,cpp}` + loader, fail-loud on unknown spelling), resolved by `effectiveBitFieldStrategy(target, format)` (format-wins, target-fallback for back-compat), overlaid at the 3 layout consumers (semantic sizeof, HIR→MIR, asm globals) so a single program packs consistently write/read/ABI; 9 `.format.json` declare it (PE×2 `msvc_straddle`; ELF×4 + Mach-O×3 `gnu_packed`; wasm/spirv omit — ILP32 declared-only). **Per-ABI findings:** MS-x64 = `msvc_straddle` (goldens DERIVED from `cl.exe 14.51`: `{int a:1;char b:1;}`/`{char a:7;int b:25;}`/`{char a:1;int b:1;}` sizeof 8 [new unit on type-size change] vs SysV 4; `{char a:1;int b:1;char c:1;}`=12 [never reopens a closed unit]; same-type fields agree); Linux-arm64 (AAPCS64) = `gnu_packed` (identical to SysV for LE bit-fields); **Apple-arm64 = `gnu_packed`** (Apple's documented AAPCS64 divergences — char signedness, long double, va_list, red zone, stack args, empty structs — do NOT include bit-field packing; the macos-latest clang leg is the runtime witness). **Conformance witness (hybrid):** hermetic goldens in `test_type_layout.cpp` (computeLayout per strategy pinned to cl.exe/gcc-derived sizeof + bit-offsets) + an asm byte-exact MS pin (`{char a:7;int b:25;}`→`7F 00 00 00 FF FF FF 01`) + a NEW `test_bitfield_abi_conformance.cpp` that SHELLS OUT to the host native compiler (cl.exe/gcc/clang), reads its real sizeof + bit positions, and asserts dss's computeLayout (host-ABI strategy) is byte-identical (GTEST_SKIP when no toolchain) — ran locally via cl.exe (msvc_straddle == cl.exe byte-for-byte) and is the ONLY Apple witness (macOS clang leg). The existing bit-field corpora stay GREEN on the PE leg under `msvc_straddle` (layout-rule-agnostic value-computers → exit 42 regardless). Red-on-disable DEMONSTRATED (revert MsvcStraddle→GnuPacked → goldens + conformance witness + asm MS pin all RED; same-type control + union stay green; restored). Independent scoping + implementation + adversarial review (cl.exe byte-compare of the full battery + ~12 adversarial extensions + tamper-revert + all-consumer-sites consistency trace — SHIP) + own gate (ctest 326/326, conformance ran via cl.exe, corpora green on PE, anchor guard OK). **326/326 ctest.** Optional future test-depth ratchets (NON-blocking, on verified-correct code): a unit pin for the format-loader unknown-spelling fail-loud (empirically verified, matches the pre-existing target-loader untested pattern) + 64-bit-type/FAM rows in the committed conformance battery (dss verified-matching cl.exe). | (closed) | `D-CSUBSET-BITFIELD` (parent); `aggregate_layout.hpp` BitFieldStrategy; `object_format_schema.{hpp,cpp}` (format-level bitFieldStrategy); `type_layout.cpp` strategy switch; `compile_pipeline.cpp` effectiveBitFieldStrategy; `test_type_layout.cpp` + `test_bitfield_abi_conformance.cpp` |
| `D-CSUBSET-BITFIELD-INIT` | ✅ **CLOSED 2026-06-18 (FC8 cycle 4 "bit-field aggregate init", branch p10).** Aggregate INITIALIZATION of a bit-field struct/union now PACKS per allocation unit at all three sites (replacing the cycle-2 fail-louds): (1) LOCAL `struct S s = {…};` → `lowerBitfieldAggregateInitIntoSlot` (`hir_to_mir.cpp`) zeroes every bit-field allocation unit in a PRE-PASS (before any field value is written) then RMWs each field in via the SAME `emitBitfieldInsert` the field-by-field write uses (single packing chokepoint); ordinary fields among the bit-fields lower normally. (2) GLOBAL const init → the const-fold yields raw per-field values that the static-data byte encoder `encodeAggregateValue` (`asm.cpp`) now packs into each unit (OR `((val&mask)<<bitOffset)` into the pre-zeroed buffer). (3) `lowerConstructAggregate`'s fold path is unchanged (raw values → encoder packs); a NON-foldable bit-field RVALUE literal fails loud cleanly (→ `D-CSUBSET-BITFIELD-RVALUE-RUNTIME`). **Also fixed (C 6.7.9):** a POSITIONAL initializer now SKIPS an anonymous bit-field (`lowerBraceInit` in `cst_to_hir.cpp` — `skipAnon`), so `{a,b,c}` on a struct with an interior `unsigned : 0;` lands `c` on the real field, not the anon slot (was a silent miscompile the moment init worked). **REVIEW-CAUGHT (independent review) & folded the same cycle:** the LOCAL per-unit zeroing was initially LAZY (zero a unit on first touch, interleaved with field stores in declaration order) → an ordinary field declared BEFORE a unit-sharing bit-field (`struct { char x; int a:3; }` — `x` at byte 0, `a`'s int unit at bytes [0,4)) was CLOBBERED by that bit-field's blanket unit-zero, AND diverged from the asm encoder which pre-zeroes the whole buffer once (global `gt.x`==7 but local `lt.x`==0 — the F1 silent miscompile + global/local divergence). Fixed via the TWO-PASS above (zero ALL units, then write ALL fields — order-independent; the RMW preserves the zero in bits it does not set, ordinary fields occupy disjoint bytes; dedup on `(offset,unitBytes)` covers mixed-width units sharing an offset). Source/target/linker-agnostic: keys only on the config-driven layout's `bitFields[]`/`fieldOffsets[]` (LE, as the rest of the bit-field code). Tests (red-on-disable): the MIR pin flipped (`…FailsLoudAtLowering` → `BitFieldStructInitializerPacksPerUnit`, asserts the And/Or/Shl + unit-zero packing) + the F1 ordering pin `BitFieldUnitZeroPrecedesOrdinaryFieldStoreInSharedUnit` (MIR — the FIRST store must be the unit-zero, not the ordinary field's value), an asm-tier byte-exact pin (`BitFieldStructInitPacksIntoUnitByteExact` — `{5,20}`→`0xA5`) + the F1 asm pin `BitFieldUnitSharingOrdinaryFieldByteExact` (`{char x=7; u32 a:3=5}`→`{7,5,0,0}`), corpus `examples/c-subset/bitfield_init` (global+local, positional+designated, anon-skip, signed sign-ext, ordinary-field-shares-a-bit-field-unit via `struct T` `gt`/`lt`, exit 42 / 35-if-clobbered) on x86_64-PE (ctest) + x86_64-ELF (native WSL) + arm64-ELF (qemu); macho on the macos-latest leg. | (closed) | `D-CSUBSET-BITFIELD` (parent); `D-CSUBSET-BITFIELD-RVALUE-RUNTIME` (residual rvalue case); `hir_to_mir.cpp` lowerBitfieldAggregateInitIntoSlot (note: `lowerConstructAggregate` was removed in the RVALUE-RUNTIME cycle 2026-06-18 — globals now fold via `evaluateConstant`+`encodeAggregateValue`); `asm.cpp` encodeAggregateValue; `cst_to_hir.cpp` lowerBraceInit |
| `D-CSUBSET-BITFIELD-RVALUE-RUNTIME` | ✅ **CLOSED 2026-06-18 (FC8 SUBSTRATE cycle "aggregate compound-literal rvalue carrier", branch p10; design (B) — synthesized slot, the established memory-based aggregate model).** A by-value aggregate-rvalue COMPOUND LITERAL now has a runtime carrier — and the fix is GENERAL, not bit-field-specific (scoping refuted the bit-field framing: the gap was that an aggregate `ConstructAggregate` rvalue had no lvalue ADDRESS at all). **Root cause:** every by-value aggregate-rvalue consumer (`appendByValueArg`, `lowerStructReturn`, assign-from-rvalue, member-of-literal) takes the aggregate's ADDRESS via `lowerLvalueAddress` (`hir_to_mir.cpp`), which had no arm for `ConstructAggregate` (HirKind ordinal 32) → fail-loud `H0009` "lvalue kind ordinal 32 not supported" for EVERY by-value compound literal (bit-field AND non-bit-field, arg/return/assign) — reached BEFORE `lowerConstructAggregate`'s bit-field-specific fail-loud, so that diagnostic was on a path the consumer never took. **As-built (one new arm):** `lowerLvalueAddress` gains a `ConstructAggregate` arm (C11 6.5.2.5p4 — a compound literal IS an lvalue): for a Struct/Union/Array type it materializes a slot (`freshAggregateTemp`, fail-loud on un-sizeable type), inits IN PLACE (`lowerArrayInitIntoSlot` for Array; `lowerAggregateInitIntoSlot` for Struct/Union — which ALREADY routes a bit-field type to `lowerBitfieldAggregateInitIntoSlot`, the cycle-4 per-unit packing chokepoint), and returns the slot address. The unchanged FC7 by-value arg/return/assign path then consumes the address exactly as it does a named local — so struct/union/array + bit-field/non-bit-field are covered BY CONSTRUCTION. Keys ONLY on HirKind + type-kind (Struct/Union/Array) + the config-driven layout — no target/arch/format/lang identity. **Dead-path cleanup (anti-resurrection):** the former SSA-aggregate path (`lowerConstructAggregate`'s `Const(zero)` seed + `InsertValue` chain) + its recursive `zeroLiteralOf` seed-builder are now UNREACHABLE — a stderr-probe across the full ctest suite AND the entire `examples/c-subset/` corpus showed ZERO hits (every aggregate ConstructAggregate flows through VarDecl slot-init, the global static-data encoder, or the new arm). Both helpers DELETED (~232 lines); the `lowerExpr` ConstructAggregate dispatch arm is converted to a fail-loud INVARIANT ("must be lowered into a slot by address, never a bare SSA rvalue") so the chain can't silently resurrect. `evaluateConstant`/`toMirLiteral` survive (globals still fold through them); `hasBitfieldMember` preserved. **Witnesses:** corpus `examples/c-subset/aggregate_rvalue_byvalue` (bit-field + non-bit-field aggregate rvalues in ARG + RETURN + ASSIGN-from-literal positions, runtime operands via a mutable global seed so ConstFold/Mem2Reg can't fold the literal away; optimized arm survives) exit 42 on x86_64-PE (ctest) + x86_64-ELF (WSL) + arm64-ELF (qemu) [macho on the macos-latest leg]; structural MIR pins `ByValueCompoundLiteralArgMaterializesSlotElementWise` (InsertValue==0 + Alloca + per-field Stores at byte offsets) + `ByValueBitfieldCompoundLiteralArgPacksPerUnit` (And/Or/Shl packing). Red-on-disable: revert the arm → `H0009` ordinal 32 (corpus RED, MIR pins fail-loud); restore. **DISCARD POSITION (independent-review fold F2/F3):** an aggregate compound literal used as a DISCARDED statement (`(struct P){h(),2};`) now routes through the SAME slot carrier at the `ExprStmt` site (materialize into a throwaway slot so operand side effects run, then drop the address) — this removed a regression (a discarded aggregate literal had briefly fail-louded) and completes the carrier across the discard position while keeping the `lowerExpr` anti-resurrection invariant intact (now truly internal-only). Pinned `DiscardedAggregateCompoundLiteralStatementRunsSideEffects` (the discard lowers with the h() side-effect Call + a slot Alloca, no SSA-aggregate chain), red-on-disable. Independent adversarial review SHIP-WITH-FOLDS (F1 stale file-ref + F2/F3 discard-statement, all folded; deletion-deadness independently re-verified across full ctest + corpus in debug & release). | (closed) | `D-CSUBSET-BITFIELD-INIT` (parent); `D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR` (sibling — the aggregate-typed ternary/comma carrier, ✅ CLOSED 2026-06-18); `hir_to_mir.cpp` lowerLvalueAddress (new arm) + lowerExpr ConstructAggregate invariant (lowerConstructAggregate/zeroLiteralOf removed) |
| `D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR` | ✅ **CLOSED 2026-06-18 (FC8 SUBSTRATE cycle "aggregate-valued ternary/comma rvalue carrier", branch p10).** A by-value aggregate-typed `Ternary` (`cond ? A : B`) or comma/`SeqExpr` rvalue now has a runtime carrier. **As-built:** `lowerLvalueAddress` gains a Ternary arm (alloc ONE common slot via `freshAggregateTemp`, lower the cond, build a CFG diamond [IfThen/IfElse/IfJoin + `addCondBr`], materialize EACH arm INTO the common slot, return the slot — NO Phi; the memory-based aggregate model has no aggregate-width SSA value, so the "merge" is shared memory, not an SSA Phi) + a SeqExpr/comma arm (run the side-effect stmts, then recurse `lowerLvalueAddress` on the result) + helper `materializeAggregateArmIntoSlot` (a compound-literal arm inits in place via `lowerAggregateInitIntoSlot`/`lowerArrayInitIntoSlot` — so a bit-field literal arm packs per allocation unit through the slot; a named-lvalue/call/nested-ternary arm resolves to an address and is COPIED in via `lowerAggregateCopy`/`lowerByteWiseCopy`). Keys ONLY on HirKind + type-kind (Struct/Union/Array) + the config-driven layout — no target/arch/format identity. **GATE-DISCOVERED SEAL (independent review SHIP-CLEAN, but found a pre-existing perimeter hole):** a DISCARDED aggregate `Ternary`/`SeqExpr` with NAMED-LVALUE arms (`(cond ? s1 : s2);` as a bare statement) fell through `ExprStmt`'s ConstructAggregate-only special-case into `lowerExpr`'s Ternary arm → silently synthesized a FORBIDDEN Phi-of-aggregate + aggregate-width arm Loads (the MIR verifier does not reject an aggregate Phi → it flowed to LIR as a latent miscompile). SEALED this cycle: (a) a shared `lowerDiscardedExpr` chokepoint routes ANY aggregate-typed discard through `lowerLvalueAddress` from BOTH discard positions — the `ExprStmt` site AND the bare for-init/for-update clause (`lowerForClauseNode`, which delivers its clause un-wrapped, not via `ExprStmt`); a FOLLOW-UP seal review caught the for-clause as a SECOND discard position the initial `ExprStmt`-only generalization missed (silent→loud, not a regression), so coverage is now by construction with NO per-kind AND NO per-position miss; (b)+(c) `lowerExpr`'s Ternary/SeqExpr arms gain aggregate-type anti-resurrection fail-loud guards (mirroring the existing ConstructAggregate guard) so any future misroute fails loud instead of resurrecting a Phi-of-aggregate. Pins: `DiscardedAggregateTernaryNamedArmsRoutesThroughSlotNoPhi` (ExprStmt site) + `ForUpdateAggregateTernaryRoutesThroughSlotNoPhi` (for-clause site), both red-on-disable. **Witnesses:** corpus `examples/c-subset/aggregate_ternary_rvalue` (non-bf ternary in ARG both-arms-witnessed via runtime pick_t/pick_f + ASSIGN; bit-field ternary in RETURN with signed sign-extension; comma/SeqExpr with a LOCAL side effect; named-lvalue copy-path arm; anti-fold via a runtime-opaque global load) exit 42 on x86_64-PE (ctest) + x86_64-ELF (WSL) + arm64-ELF (qemu) [macho on the macos-latest leg]; 5 MIR pins (`ByValueAggregateTernaryMaterializesCommonSlotUnderDiamond`, `ByValueBitfieldAggregateTernaryPacksPerUnitUnderDiamond`, `AggregateSeqExprByValueLowersWithSideEffectAndSlot`, `ByValueAggregateTernaryNamedLvalueArmsCopyIntoCommonSlot`, `DiscardedAggregateTernaryNamedArmsRoutesThroughSlotNoPhi`) using helpers `countOpcodeAllBlocks`/`funcWithCall`/`hasAggregatePhi`. Red-on-disable: revert the carrier → `H0009` ordinal 33/39; revert the seal (a)+(b) → the discard pin catches the Phi-of-aggregate (`hasAggregatePhi` true) — demonstrated empirically (probe build → 1 FAILED at the `hasAggregatePhi` assertion), restored + re-greened. **327/327 ctest.** **ORIGINAL DEFERRAL CONTEXT (created 2026-06-18, trigger-gated, sibling of D-CSUBSET-BITFIELD-RVALUE-RUNTIME):** An AGGREGATE-typed `Ternary` (HirKind ordinal 33 — `cond ? (struct S){…} : (struct S){…}`) or comma/`SeqExpr` rvalue used BY VALUE (arg/return/assign/member) is a DIFFERENT carrier than the compound-literal slot: each control-flow arm must be materialized into a COMMON result slot under the branch (control-flow-dependent), not initialized once in place. The compound-literal arm just added to `lowerLvalueAddress` (D-CSUBSET-BITFIELD-RVALUE-RUNTIME) does NOT handle it — it stays FAIL-LOUD at the `lowerLvalueAddress` ordinal-N catch-all (`H0009` ordinal 33), cited there in a code comment so this guard resolves. A scalar ternary/comma is unaffected (it lowers as an ordinary scalar rvalue). | (closed) | **Closing work (as-built — see status cell):** give `lowerLvalueAddress` (or a sibling) an aggregate-typed Ternary/SeqExpr arm that allocates ONE result slot, lowers each arm's aggregate INTO that slot under the control-flow diamond (CondBr → arm-then-init-slot → join), and returns the slot address — reusing `lowerAggregateInitIntoSlot`/`lowerArrayInitIntoSlot`/`lowerAggregateCopy` per arm shape (a compound-literal arm inits in place; an lvalue arm copies). Keep it source/target/linker-agnostic (HirKind + type-kind only). `hir_to_mir.cpp` lowerLvalueAddress (the ordinal-N fail-loud comment cites this anchor); `D-CSUBSET-BITFIELD-RVALUE-RUNTIME` (sibling — the compound-literal carrier) |
| `D-CSUBSET-BITFIELD-WIDE-UNIT` | ✅ **CLOSED 2026-06-18 (FC8 deferral cycle, branch p10; user-authorized §B — `mov r64,imm64` strategy chosen).** 64-bit-base bit-fields (`unsigned long long x : 40;`) work end-to-end. **The anchor's premise was REFUTED by scoping:** general 64-bit `mov`/ALU was NOT the blocker (add/sub/mul/and/or/shifts/load/store all already had REX.W variants; a small-mask 64-bit bit-field already compiled) — the ONLY gap was materializing a 64-bit integer constant > int32 (the wide bit-field masks hit a literal-pool dead-end with no encoder). **As-built:** x86 encoder gained an `imm64` operand-kind + `Imm64`/`OpcodePlusReg` slots (`target_schema.hpp`, `x86_variable.cpp`+disasm) + one `mov r64,imm64` variant (REX.W B8+rd io) in `x86_64.target.json`; shared lowering `mir_to_lir.cpp` `lowerConst` routes a wide INTEGER const by CAPABILITY PROBE (`targetHasMovImm64` → mov-imm64 [x86]; else `targetHasMovkLadder` → MOVZ+MOVK [arm64, already declared]; else fail-loud) — ZERO arch identity; the float literal-pool fail-loud is unchanged; semantic guard `typeBits > 32` → `> 64` (I128/U128 still rejected). **Also fixes a broader latent fail-loud** — any wide >int32 64-bit integer constant (`unsigned long long x = 0xFFFFFFFFFF;`) previously hit the same dead-end. **Latent-bug CAUGHT + fixed:** the 3 LIR rebuild passes (`lir_2addr_legalize`/`lir_callconv`/`lir_rewrite`) copied `LiteralIndex` operands by index but never carried the literal pool (`LirLiteralPool::at` out-of-range — invisible until the first real value rode `LiteralIndex` to the encoder) → new `lir_pass_util::copyLiteralPool`, verified complete across all rebuild sites. **Witnesses:** corpus `bitfield_wide_unit` (40+20-bit pack + signed 36-bit sign-extension + a non-bit-field wide-const arm, anti-fold via a mutable `g_seed` so masks materialize at RUNTIME in baseline AND optimized) exit 42 on x86_64-PE (ctest) + x86_64-ELF (WSL) + arm64-ELF (qemu) [macho on the macos-latest leg]; x86 byte-pins (`mov rax,0xFFFFFFFFFF = 48 B8 FF FF FF FF FF 00 00 00`, r8-r15 REX.B) + arm64 MOVK-ladder pin + a semantic-flip pin, all red-on-disable (revert-probe verified ×3). Independent scoping + implementation + adversarial review (objdump byte-exact + revert-probes + rebuild-site completeness) all SHIP; my own gate re-ran 324/324 + cross-target + the 2 delicate hunks inspected. **324/324 ctest.** | (closed) | `D-CSUBSET-BITFIELD` (parent); `target_schema.hpp`/`x86_variable.cpp`(+disasm)/`x86_64.target.json` (imm64 mov); `mir_to_lir.cpp` lowerConst capability-probe; `lir_pass_util.cpp` copyLiteralPool; `semantic_analyzer.cpp:1063` guard |
| `D-CSUBSET-ENUM-BITFIELD` | ✅ **CLOSED 2026-06-18 (FC8 cycle 5 "enum-typed bit-fields", branch p10).** An enum-typed bit-field (`enum E e : W;`) works end-to-end (C 6.7.2.1) — an enum behaves AS its underlying integer (D-CSUBSET-ENUM-INT-CONVERSION). **Semantic:** `resolveBitfieldSuffix` resolves the field type via `enumUnderlyingOrSelf` before the integer-ness/width check → the width validates against the UNDERLYING (a >32-bit underlying still routes to the wide-unit reject, consistent with a plain `long` bit-field; zero-width-named still rejected). **Codegen:** a new `enumReprType` helper (Enum→underlying primitive) resolves at the bit-field chokepoints — top of `emitBitfieldExtract` (the LOAD-BEARING one: `bitfieldIsSigned` must see the underlying so a signed-underlying enum bit-field SIGN-extends), top of `emitBitfieldInsert`, and `constIntOfType` — so shift/mask constants, op result types, and signedness run at the underlying. The unit Load/Gep stay Enum-typed; the LIR `reprKind` tier resolves their WIDTH (as for all enums). **Layout** already sized the unit via `computeLayout(Enum)`→underlying (unchanged). **REVIEW-CAUGHT (independent review) & folded the same cycle:** the initial test/corpus over-claimed codegen red-on-disable via "primitive(Enum) invalid → mir.ok false" — FALSE: `primitive(Enum)` is a valid degenerate type AND the LIR `reprKind` tier masks the WIDTH, so the codegen resolve is byte-identical for v1's always-signed-I32 enums EXCEPT for SIGNEDNESS. Re-pinned honestly: the corpus uses a negative enumerator (`NEG=-3` in a 4-bit enum bit-field) that must SIGN-extend to read -3 → exit 42, not 13 → exit 74 (the genuine load-bearing red-on-disable); the MIR test asserts no bit-field op result is Enum-typed (the representation arm) + the semantic-admission arm (revert → `S_BitFieldNonIntegerType`). **Witnesses:** corpus `enum_bitfield` exit 42 (signed enum bit-field + c:4/d:4 packing + global & local init + write) x86_64-PE (ctest) + x86_64-ELF (WSL) + arm64-ELF (qemu) [macho on the macos-latest leg]; `EnumTypedBitFieldResolvesAndLowers` (representation + admission) red-on-disable. **323/323 ctest.** | (closed) | `D-CSUBSET-BITFIELD`; `D-CSUBSET-ENUM-INT-CONVERSION`; `semantic_analyzer.cpp` resolveBitfieldSuffix; `hir_to_mir.cpp` enumReprType/emitBitfieldExtract/emitBitfieldInsert/constIntOfType |
| `D-CSUBSET-TYPEDEF-ENUM-TRAILING-COMMA` | ✅ **CLOSED 2026-06-18 (FC8 cycle 6 — RECONCILIATION, branch p10).** STALE anchor: `typedef enum { A, B, } T;` ALREADY PARSES + COMPILES + RUNS correctly — verified across anon / named-tag / valued / single-element forms (correct exit codes, e.g. `typedef enum {RED,GREEN=5,BLUE,} Color;` → BLUE reads 6). The gap was closed by the GLOBAL `recomputeAltExpectedSets` schema-compiler fixpoint (`D-PARSE-SCHEMA-NESTED-NULLABLE-FOLLOW`, struct-head cycle), whose blast radius is EVERY grammar's expectedSets. **The original root-cause analysis was INACCURATE:** it blamed the INLINE-vs-NAMED `typedefHead` rule shape, but the inline `typedefHead` parses the trailing comma FINE — the determinant is the FOLLOW-set fixpoint, NOT the rule shape, so the planned `typedefCompositeSpec` extraction was a NO-OP and was correctly NOT built (the bar's no-unnecessary-change). **VERIFIED red-on-disable + causal link:** toggling off the `recomputeAltExpectedSets` orchestration call (grammar_schema_json.cpp:2829), rebuilding, reddens BOTH the top-level pin (`D5_5_EnumTrailingCommaParses`) AND the new typedef pin (`D5_5_TypedefEnumTrailingCommaParses`); restored + re-greened after. Now pinned by `D5_5_TypedefEnumTrailingCommaParses` (parses + mints exactly the listed enumerators, no phantom 4th). **323/323 ctest.** | (closed) | `grammar_schema_json.cpp recomputeAltExpectedSets`; `D-PARSE-SCHEMA-NESTED-NULLABLE-FOLLOW` (the actual closing fix); `tests/hir/test_hir_lowering_c_subset.cpp D5_5_TypedefEnumTrailingCommaParses` |
| `D-CSUBSET-TYPEDEF-FIELD-EXTERN-POSITION` | **OPEN (created 2026-06-11, FC4 c1 — the plan-lock MUST-FIX-5 honest partial-scope residue carried OUT of the `D-CSUBSET-TYPEDEF-DECLARATION-POSITION` ✅ close above).** `structField`/`unionField`/`externDecl` kept their LEGACY `typeRef`-spine rows (`name`/`type` child indices, no declarator roles), so: typedef-named FIELDS (`struct S { MyP p; };` — probe P0009 at 2:12) and declarator-SHAPED externs (`extern int (*fp)(int);` — typeRef then expects Identifier, sees `(`) do NOT parse. *(Audit-corrected 2026-06-11: typedef-named externs `extern MyP v;` DO parse — `typeRef`'s `typeBase` has carried the bare-Identifier arm since the FC2 cast work — and fail LOUD at the pre-existing typedef-AGNOSTIC `H0009` ExternGlobal-ingestion wall, positioned; that wall, not parsing, is the extern-position residue.)* ALSO in this remainder (stage-2b probe-verified): block-scope `typedef` STATEMENTS (`int main() { typedef int T; }` — the `statement` alt has no `typedefDecl` arm; file-scope typedefs are the only form, P0009 at the keyword). | Migrate the three rows to declarator mode (head + declarator roles, like varDecl/param/typedefDecl) — fields fold their own pointer/array/fn-suffix structure, externs reuse the SAME inversion engine; admit `typedefDecl` in the `statement` alt with block-scoped alias symbols. Each is mechanical against the stage-2a engine; corpus + field-typing pins per position. | plan 23 FC4 (MUST-FIX-5); `D-CSUBSET-TYPEDEF-DECLARATION-POSITION` (the closed statement/param/head scope); `D-CSUBSET-FN-PROTOTYPE` (extern-prototype overlap) |
| `D-CSUBSET-C23-AUTO-INFERENCE` | **OPEN (created 2026-06-11, FC4 c1). Trigger-gated: first real C23 corpus need.** `auto x = 1;` (C23 6.7.9 type inference) is a LOUD parse error today — `auto` is admitted ONLY as a storage-class specifier (`localDeclSpecifier`), so the head consumes `x` as the (typedef-style) TYPE name and the missing declarator fails at the `=` with `P_NoAlternativeMatched` (0x0009) positioned there (stage-2b probe; pinned by `AutoInferenceFormIsALoudParseError`, test_parser_c_subset_smoke.cpp). `auto int a2 = 20;` (the C89 storage-class form WITH a type specifier) parses + runs (corpus `storage_class_decls`). Never a silent mistype. | Grammar: admit a declarator directly after a storage-class-only head when an initializer follows; semantics: infer the declared type from the initializer's stamped type (the UAC/ladder machinery already types initializer expressions); reject the uninitialized `auto y;` per C23. | plan 23 FC residue; `AutoInferenceFormIsALoudParseError` |
| `D-CSUBSET-DECL-GRAMMAR-LOW-RESIDUES` | **PARTIAL — residue (3) CLOSED, (1)+(2) remain OPEN, (4) is a non-issue (FC-deferral close-out loop cycle 5, branch p14, 2026-06-22).** ✅ **(3) STRUCT-TAG NAMESPACE CLOSED** — `typedef struct Pair {...} Pair;` (tag == alias) now compiles (the ubiquitous C idiom). A C 6.2.3 tag-namespace AXIS: new `SymbolNamespace {Ordinary, Tag}` + a `ScopeRecord.tagBindings` map; `ScopeTree::bind/lookup/injectBinding` gain a `ns=Ordinary` param. AGNOSTIC + config-driven: a BIND is Tag iff `decl.fieldChildren.has_value()` (the existing composite gate); a LOOKUP is Tag iff the reference rule has a NEW `ReferenceRule.isTagReference` (set on structTypeRef/unionTypeRef/enumTypeRef). **The make-or-break (MF-1):** since the tag BIND moved to tagBindings, EVERY tag READ must use the Tag namespace — a NEW explicit tag-ref early-arm in `resolveTypeNodeImpl` resolves `struct Foo` via Tag, while the bare-identifier leaf arm stays Ordinary (typedef-names); plan-lock + self-audit (22 empirical probes) confirmed every tag-read form (cast/sizeof/param/return/nested-field/pointer/typedef-of-tag) routes through it (no Ordinary leak). MF-2 cross-tree injection re-keyed on (name,ns); MF-4 builtins-as-Ordinary (`__va_list_tag`) named exclusion. **Witnesses:** corpus `tag_namespace` (the idiom — alias `Pair` AND tag `struct Pair` both resolve in ONE TU, exit 42) + `tag_ordinary_coexist` (a struct tag `S` + an ordinary `S` coexist) on x86_64-PE/ELF + arm64-ELF qemu [macho macos leg]; 4 semantic unit pins (incl. `struct Nope` still S_UnknownType + the adversarial `typedef int Foo; struct Foo x;` fails loud — namespaces independent) + 2 scope_tree pins; the 2 prior HIR single-namespace "documented C divergence" pins INVERTED to the C-correct behavior (as their own comments anticipated). 367/367 ctest; plan-lock + 2 pr-reviews + self-audit all CLEAN. — **(1) EMPTY-PARENS-AT-BASE + (2) STORAGE-CLASS-AFTER-TYPE remain OPEN** (rare C-valid forms, fail-loud parse errors, low value; untouched by this cycle — see the closing-work column). — ORIGINAL (created 2026-06-11, FC4 c1, four residues): (1) EMPTY-PARENS-AT-BASE: `int f(int ())` — an abstract fn-type param whose declarator is a bare `()` group at the BASE position; the `directDeclarator` base alt's fnSuffix-vs-parenDeclarator probe needs a known-type or declarator inside the parens (the grammar `$comment` on `fnSuffix` pins the C 6.7.6.3p11 collision rules). (2) STORAGE-CLASS-AFTER-TYPE ordering: `int register r;` — C grammar admits declaration-specifiers in ANY order; ours requires storage-class specifiers FIRST (`localDeclSpecifiers` is a varDecl PREFIX, not interleaved). (3) STRUCT-TAG NAMESPACE: `typedef struct Pair {...} Pair;` (tag == alias name) is `S_RedeclaredSymbol` — the symbol table is single-namespace; C 6.2.3 puts tags in their own namespace (stage-2b probe; corpus `typedef_struct_body` uses a distinct tag + documents this). (4) `int f()` EMPTY-PARENS at definition position types as zero-param (C23 semantics, matching `(void)`) — fine for C23, but a K&R-style unspecified-params reading is not modeled (no residue unless pre-C23 compat becomes a goal). | Each unblocks on its first real corpus need: (1) teach the base-position probe the abstract-`()` form; (2) interleave storage-class specifiers into the head qualifier runs; (3) namespace-qualified symbol binding (tag vs ordinary); (4) only if a pre-C23 mode ships. | plan 23 FC4 residue; `c-subset.lang.json` fnSuffix/localDeclSpecifier $comments |
| `D-LIR-GLOBALADDR-LOAD-RIPREL-FOLD` | ✅ **CLOSED 2026-06-11 (FC3.5 sweep-c3). ADJUDICATION: mir_to_lir LOCAL FOLD chosen over a LIR peephole pass** — (1) the SymbolId is directly readable at MIR tier (`mir.globalAddrSymbol`; a LIR pass would re-derive it from the lea's operand via the heavyweight rewrite machinery), (2) single-use analysis is well-defined on MIR SSA (post-regalloc LIR reuses registers), (3) the direct-call SymbolRef fold in `lowerCall` is the established in-file precedent. Mechanism: a per-function MIR value-use census (`computeValueUses` — instOperands + Phi `phiIncomings`, which live OUTSIDE instOperands; missing them would under-count and fold away a lea a phi still needs) + ONE predicate `globalAddrRiprelFoldsIntoLoad` (use count == 1 AND the user is a Load whose sole operand is the value AND the load's result-class load mnemonic declares a 1-operand SymbolRef encoding variant at the load's effective width) consulted by BOTH fold sites via the `foldedGlobalAddrs_` set — `lowerGlobalAddr` skips the lea, `lowerLoad` emits the [SymbolRef] form directly; a site disagreement surfaces as a loud undefined-vreg failure, never a silent wrong address. CAPABILITY-DRIVEN: fires for x86 movsd_load/movss_load (the FC2/FC3.5-c2 riprel variants' FIRST pipeline consumers — F2/F3 0F 10 05 rel32); does NOT fire for x86 GPR `load` (no [symbol] variant) or arm64 `fldur` (arm64's lea is the 2-word ADRP+ADD macro — a symbol-relative LDR fold is a DIFFERENT encoding shape, honestly out of scope, pinned by `Arm64GlobalAddrLoadKeepsLeaPlusBaseFormLoad`). Pins: `FullPipelineDoublePlusRodataConstFoldsToRipRelMovsd` (riprel ModR/M mod=00 rm=101 present + lea ABSENT + reloc preserved; RED-on-disable demonstrated + restored) + `FullPipelineRodataConstTwoLoadsKeepsLeaPlusBaseForm` (NOT-single-use keeps the pair, no riprel form). WITNESSED: float_cast x86_64-ELF exit 16 + float_nan/f32_arithmetic/float_compare exit 42 under WSL with the fold LIVE (F64 + F32 riprel folds in real programs); PE arms via ctest. | (closed) | `src/lir/lowering/mir_to_lir.cpp` (computeValueUses + lowerGlobalAddr + lowerLoad) / plan 22 peephole arc |
| `D-MIR-TO-LIR-DIV-SEQUENCE-AGNOSTIC` | ✅ **CLOSED 2026-06-10** (FC1 cycle 1, V2-4.X) — the trigger fired (arm64 = the 2nd target with division) and the anchor's GOAL — ONE capability-driven MIR→LIR arm with no per-target divergence — is met, **by different means than the sketched mechanism**. `lowerDivLike`/`emitDivLikeValue` serves SDiv/UDiv/SMod/UMod on every target via mnemonic-presence probing (the sanctioned `lea`/`call` pattern): Rule 1 native result-bearing opcode (`sdiv`/`udiv`/`smod`/`umod` — arm64 SDIV/UDIV; a future RISC-V REM hits the native smod path), Rule 2 implicit-register pre/core pair with role-based projection (x86), Rule 3 the generic remainder identity rem = n − (n/d)·d over div+mul+sub (arm64 SMod/UMod), else fail-loud. Zero `if(arch)`. **PREMISE CORRECTION (plan-lock-verified, 2026-06-10): the closing-work sketch's "existing call-return-register mechanism" does NOT exist** — call results use an explicit post-regalloc `mov result, returnReg` in `materializeOneFunc` (lir_callconv.cpp), and the linear-scan allocator has NO pre-coloring path. Building the sketched one-op + regalloc-binding variant would have speculatively erected the deferred D-ML7-2.5 pre-coloring substrate for a perf-only delta (a bar-#6 violation). That variant remains legitimately deferred under **D-ML7-2.5** (plan 12 — its row now names the div/mod result-binding as a 2nd consumer). Pins: `Arm64SignedDivisionLowersToSingleNativeSdiv` (exact 4-inst shape), `Arm64SignedModuloExpandsToSdivMulSub` (exact 6-inst shape + operand wiring), `Arm64UnsignedModuloExpandsViaUdivNotSdiv` (FLAG-1 in the expansion), arm64 sdiv/udiv byte-pins, + the cross-target `modulo`/`division` corpus arms. | (closed — the one-op/pre-coloring perf variant lives under D-ML7-2.5) | plan 12 ML6 / plan 22; pairs with D-CSUBSET-DIVISION-OP-CODEGEN + D-CSUBSET-MOD-OP-CODEGEN closures |
| `D-CSUBSET-MOD-OP-CODEGEN` | ✅ **CLOSED 2026-06-10** (FC1 cycle 1, V2-4.X — the first full-C cycle). Binary `%` is now END-TO-END on x86_64 (PE+ELF) **and arm64 (ELF+Mach-O)**: c-subset config additions (the `%` token/PercentOp + precedence-70 operator row + binaryOps PercentOp→Rem — the pre-FC1 row text's claim that "the grammar parses it via PercentOp" was STALE/wrong, the token did not exist; the compound `%=` row predated this and becomes newly live), the already-wired HIR Rem → MIR SMod/UMod chain, and the NEW capability-driven MIR→LIR realization (see `D-MIR-TO-LIR-DIV-SEQUENCE-AGNOSTIC` ✅): x86 reuses cqo+idiv_op capturing the role-`remainder` output (rdx); arm64 expands rem = n − (n/d)·d via sdiv/udiv+mul+sub. UMod wired + MIR-tier-tested (the UDiv hand-built-MIR precedent; unsigned stays source-unreachable until FC3). ConstFold SMod arms ship with C truncated semantics + the %0 refusal + the NEW INT64_MIN/-1 Overflow guard (which also fixed the PRE-EXISTING latent host-UB bug in the SDiv fold arm). LICM trap-eligibility was already listed — now pinned (`TrapEligibleSModNotHoisted`). Tests: x86+arm64 MIR→LIR shape pins (incl. rdx-not-rax capture discrimination + FLAG-1 xor/div-not-cqo/idiv for UMod), arm64 sdiv/udiv byte-pins, `EffectivenessSModSurvivesShippedReleasePipeline`, const-fold semantics/guards, role-contract red-on-disable levers, `examples/c-subset/modulo` (4 target arms, truncated-vs-floored runtime discriminator, exit 42 — witnessed PE-local + WSL x86_64 + qemu arm64 + **Apple Silicon (the macos-clang-release leg ran GREEN on the FC1-c1 push, run 27276320850 — the Mach-O arm64 arm executed)**; exercises `%=` + a 0b literal) + `float_mod_error` (positioned fail-loud: `%` on F64 has no realization; the semantic-tier S_* constraint is FC3's operand-constraint vocabulary). | (closed) | plan 22 OPT / plan 23 FC1; closed with D-CSUBSET-MOD-OP-CODEGEN-OUTPUT-INDEX-CONTRACT + D-MIR-TO-LIR-DIV-SEQUENCE-AGNOSTIC |
| `D-CSUBSET-DIVISION-OP-CODEGEN` | ✅ **CLOSED 2026-06-04** (cycles 10p + 10q + 10r bug-fix + 10r 7-agent fold). 10p: substrate (`ImplicitRegisterConstraint`). 10q: first SDiv/UDiv wiring with compound encodings (`sdiv_compound`, `udiv_compound`). **10r BUG FIX: split** the compounds into 4 separate opcodes (`cqo`, `idiv_op`, `xor_rdx_zero`, `div_op`) to fix the REX-overlap silent-miscompile — each instruction now gets its own auto-REX prefix from the encoder, correctly handling high-numbered (R8-R15) divisors. MIR→LIR `lowerDiv(MirInstId, DivSlotPair)` emits a 4-LIR-op sequence (mov-to-rax + pre + core + mov-from-rax); the `DivSlotPair` named-aggregate prevents pre/core transposition at the type level. **Closure-gate items satisfied**: (a) regalloc-exclusion unit pin green; (b) `Optimizer.EffectivenessSDivSurvivesShippedReleasePipeline` + `EffectivenessUDivSurvivesShippedReleasePipeline` pin live SDiv/UDiv survives the shipped release pipeline; (c) corpus `examples/c-subset/division/` returns 14 (=100/7) end-to-end on Windows. Byte-pin tests now include R8/R14/R15 (the regression class). New `outputs ⊆ clobbered` schema-loader invariant (silent-failure-hunter F1). 7-agent fold applied 6 FOLD-NOW items: DivSlotPair (type-design 9/10) + preInfo validation (silent-failure F2 8/10) + ASSERT_EQ tightening (F3+TypeC4) + outputs⊆clobbered invariant (F1 9/10) + R8/R15 byte pins (pr-test #1 9/10) + NoLegacyCompoundDivideOpcodes anti-regression (pr-test #2 8/10) + UDiv survives-pipeline (pr-test #3 7/10) + unordered identity (F4 7/10) + unified byte-pin helper (simplifier #2) + OVERRIDE wording fix (comment-analyzer #3). **STILL OPEN sub-arcs**: SMod / UMod modulo (anchored D-CSUBSET-MOD-OP-CODEGEN — needs role-tagged output-index contract via D-CSUBSET-MOD-OP-CODEGEN-OUTPUT-INDEX-CONTRACT). **UNBLOCKS** D-OPT6-LICM-TRAP-SAFE-HOIST. | (closed for div; mod deferred to sibling) | plan 12 ML5+ (asm tier); UNBLOCKS D-OPT6-LICM-TRAP-SAFE-HOIST |
| `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH` | ✅ **CLOSED 2026-06-10** (FC3 c1, V2-4.X — the prescribed `dataModel` axis LANDED, the "SemanticConfig axis" arm of the closing-work fork, plan-lock-refined to put the FIELD on the FORMAT schema [the OS lives in the format: pe→LLP64, elf/macho→LP64, wasm/spirv→ILP32 declared-only/fail-loud-if-selected; x86_64 serves BOTH Windows and Linux so a target-level field was unrepresentable]): `ObjectFormatSchema.dataModel` (REQUIRED, closed enum, loader+validate reject missing/unknown) → resolved at `buildCuMir` → threaded into `analyze()` (defaulted-LP64 param = the identity model; the driver always passes `format.dataModel()`) and carried on `SemanticModel` to HIR. `builtinTypes`/`typeSpecifiers` rows carry `coreByDataModel` overrides (`long`→I64, LLP64→I32; `unsigned long`→U64/LLP64→U32); the literal ladder resolves type NAMES through the same table (LLP64 long=I32 falls out free); **the descriptor surface closed too**: `signatureByDataModel` per-symbol overrides (Model-3 library-map shape) on the 6 width-divergent symbols (fseek/ftell/atol/strtol/strtoul/labs — LLP64 i32/u32 forms; every declared override must parse, unknown keys reject). WITNESS: `datamodel_long_width` (LP64 runtime, 2³⁰+2³⁰=2³¹ proves 64-bit long, WSL+qemu exit 42) + `datamodel_long_width_llp64` (the SAME `long` vocabulary `expectDiagnostics`-REJECTS S_TypeMismatch@21:14 under LLP64 on the PE arm — a DEFINED-behavior runtime-divergent witness is impossible-by-construction until c2's 32-bit ALU [signed overflow=UB, unsigned-32=gated]; the runtime divergent witness rides c2). Red-on-disable: pe64 dataModel flipped LLP64→LP64 → the llp64 example RED. ORIGINAL text below. — First time a c-subset corpus row needs `long` to bind to I32 (LLP64 / Win64 ABI) instead of I64 (LP64 / SysV ABI). Today `c-subset.lang.json` binds `long → I64` unconditionally — that's correct for LP64 Linux/macOS, silently wrong for Win64 LLP64 (Windows `long`=I32). Today's corpus runs Win64 only because `pe64-x86_64-windows-exec` is the sole `targets[*].spec`, but the `long`→I64 binding bakes a data-model decision into the source-language config in a way that violates the agnosticism rule (lang config should not assume ABI). **SCOPE EXTENSION (2026-06-09, the `#include puts` 3-OS string-FFI capstone — STILL OPEN):** this anchor now ALSO covers the **shipped-lib descriptor surface**. When the 15 per-platform descriptors were collapsed into 5 neutral `shippedLibs/<h>.json` (Model 3 — see `D-FFI-SHIPPED-LIB-PLATFORM-SELECT` ✅ dissolved), the **6 ABI-WIDTH-DIVERGENT symbols** — stdio `fseek`/`ftell` + stdlib `atol`/`strtol`/`strtoul`/`labs` (the ones whose signatures involve `long`/`size_t`) — were MERGED to the **LP64 (i64/u64)** form in the neutral signatures. That is CORRECT for the runnable linux/macos targets (LP64), but the **Windows LLP64 form for these 6 (where `long` = i32) is now LATENTLY DEFERRED** — confirmed UNEXERCISED by any corpus (no shipped-`fseek`/`strtol`/etc. call compiles to a Windows target today; the running `puts`/`abs` corpora touch no width-divergent symbol). The proper fix is the SAME as the `long`-keyword case: an abstract `long`/`size_t` resolved per-target (the descriptor signatures would spell `long`/`size_t` abstractly, the data-model axis below would bind them per active target). Do NOT file a duplicate anchor — this is one and the same "config baked an ABI/width decision" gap, now spanning both the c-subset `long` keyword AND the neutral shipped descriptors. | Extend the `SemanticConfig` schema with a `dataModel` axis (e.g., `"ILP32"` / `"LP64"` / `"LLP64"`) that `builtinTypes[*].core` can vary on; OR add an ABI-conditioning layer in the HIR→MIR lowering that overrides core types per active target. Closes the silent-mismatch when the same c-subset source compiles to two different ABIs. Pairs with the multi-target capstone (D-CAP-MARKER-MULTI-TARGET-E2E-PIN). | plan 22 (alias arc); pairs with D-CSUBSET-LONG-PRIMITIVE (which made this decision provisional) |
| `D-OPT-IDENTITYREBUILD-CALLER-WRAP-REQUIRED` | ✅ **CLOSED 2026-06-04** (cycle 10h post-fold) — `identityRebuild` test helper signature refactored from `void identityRebuild(Mir const& src, Mir& out)` to `[[nodiscard]] bool identityRebuild(...)`. The void+ASSERT_NO_FATAL_FAILURE shape required caller discipline; a caller writing `identityRebuild(src, dst);` without the wrap would silently allow tests to proceed with a half-built `dst`. `[[nodiscard]]` triggers an unused-result warning at the missing-check call site, converting the silent-failure trap into a compile-time gate. | Refactor + ADD_FAILURE on the carve-out arm (vs the prior ASSERT_EQ which required void return); both existing test bodies updated to `ASSERT_TRUE(identityRebuild(...))`. | tests/opt/test_mir_rebuild_helper.cpp; same compile-time-enforcement pattern as `[[nodiscard]] Result<T>` patterns elsewhere in the codebase |
| `D-TARGET-ENCODING-WIDTH-GUARD` | **REWRITTEN to post-FC3.5 truth 2026-06-11 (audit-residue sweep c2, `D-AUDIT-BOOKKEEPING-TRUTH` (b) — the original text below had become pre-FC3 fiction cited by LIVE diagnostics).** The mechanism the original row asked for SHIPPED: FC3 c2 added the variant-guard WIDTH key (`guard.width`∈{32,64}, loader-validated, + the ONE shared `variantMatchesInst` matcher + the `kLirInstFlagWidth32` carrier) and `trunc`'s FIRST encoding (mov r32,r32); FC3.5 c2 put F32 on the SAME width key (F3-prefix SSE / arm64 S-forms at width 32 vs F2/D-forms at 64) with SOURCE-width keying for conversions (the fpcvt/fp_to_si widthOverride). What the anchor now MARKS — and what its live citations mean — is the residual width WALLS: any width with NO encoded variant fails LOUD instead of first-match-winning a wrong form. Citations: `requireEncodedFloatWidth` (mir_to_lir.cpp ~:648) walls F16/F128 scalar floats; the FPR literal-promotion guard (~:1350) cites it for non-F64 inline-literal widths (F32 literals ride the rodata path); sub-native INTEGER widths (8/16/128-bit ALU; the I8/I16 sext variants) wall at `requireNativeIntWidth` / the SExt source gate — that successor work lives at `D-CSUBSET-SUBNATIVE-ALU-FORMS`. | Encode the missing width variants WHEN a consumer arrives (F16/F128: no C producer today; I8/I16: the `D-CSUBSET-SUBNATIVE-ALU-FORMS` triggers) — the guard.width axis makes each a declaration-only addition. **ORIGINAL pre-FC3 text** (history): "First time a target needs distinct encodings for different operand WIDTHS (not just kinds like reg/imm/mem) — e.g., sext I8→I64 vs I32→I64 are different x86 opcodes (`0F BE` vs `63`). Today's variant-guard only supports `operandKinds` discrimination; same trap exists for the existing zext (BYTE→r64 single-variant) and trunc (no encoding) mnemonics. / Extend the variant `guard` schema with an `operandWidths` array discriminator + teach the encoder to dispatch on the source operand's width-of-record. Then add the missing sext variants (I8→I16, I8→I32, I8→I64, I16→I32, I16→I64) + the trunc encoding (mov r32, r32 zeroes upper 32). Pre-fold the same trap masks any I8/I16 widening / narrowing as silent miscompiles." | plan 13 §2.4 (encoding shapes); pairs with D-CSUBSET-LONG-PRIMITIVE (which closed only the I32→I64 variant); integer-width successor: `D-CSUBSET-SUBNATIVE-ALU-FORMS` |
| `D-OPT-ALIAS-ARC-CORPUS-CAPSTONE-STRICT` | ✅ **CLOSED 2026-06-04** (cycle 10i) — the integration-as-a-whole closure of cycles 10a–10g's alias arc, demonstrating `mirMayAlias` Rule 6 (distinct non-char primitives → No alias under strict-TBAA) end-to-end at both corpus-binary AND MIR-tier. Pre-10i discovery (cycle 10h): c-subset is C → `charTypesAliasAll=true` is correct → Rule 5 (char-exception) fires first on any `int*`/`char*` pair → no precision win observable on that path under default config. Strict-arm via two non-char distinct primitives (`int*` + `long*`) is the under-pinned, agnosticism-clean path. | Corpus fixture `examples/c-subset/strict_alias_cse_capstone/` (two-armed: `[Mem2Reg, Cse]` + full release-like) + MIR-tier `Optimizer.EffectivenessAliasArcStrictTBAACapstone` test (3 ARMs: Identity baseline / `[Cse, Dce]` / shipped release w/ explicit "release pipeline contains Cse" pin). All 3 ARMs assert strict-TBAA mode propagation through MirBuilder→Mir. Closes alias-arc D-OPT-LOAD-ALIAS-ANALYSIS at the corpus + effectiveness levels. | plan 22 OPT5 (CSE alias arc); built on D-CSUBSET-LONG-PRIMITIVE + D-CSUBSET-DEREF-LOADSTORE-SMOKE pre-reqs |
| `D-FC6-SIZEOF-CONST-CONTEXT` | ✅ **CLOSED 2026-06-15 (FC6-deferrals cycle).** `sizeof` now const-folds in const-expression contexts via a symmetric `resolveTypeSize`/`resolveSizeof` hook on BOTH const-eval engines, sharing the FC6 `computeLayout` kernel: **GLOBAL initializers** (`int g = sizeof(T)`, both type+value forms) fold via the HIR const-eval `EvalEnvironment::resolveTypeSize` + `HirKind::SizeOf` case (wired at the MIR-globals site); **ARRAY-dimension TYPE form** (`int a[sizeof(T)]`) folds via the CST const-eval `CstEvalEnvironment::resolveSizeof`, dispatched by `cfg.sizeofRule` BEFORE the wrapper-peel (the M1 hazard — else `sizeof x` would fold to x's VALUE). The earlier "architectural tension" was DISPROVEN: semantic analysis re-runs per-(CU×target) into its own private `TypeLattice`, so a target-dependent array-dim type is sound. The `resolveTypeLayout` hook the row predicted (`const_eval.hpp:131`) is now wired. Pins: `examples/c-subset/sizeof_const_context` (runnable, exit 42 — global-init + array-dim + expr sizeof), `ConstEval.SizeOfFoldsOnlyWithTypeSizeResolver` (hook red-on-disable), `sizeof_value_in_array_dim.c` (M1 red-on-disable). RESIDUAL: the array-dim VALUE form (`int a[sizeof expr]`) stays fail-loud → `D-FC6-SIZEOF-ARRAY-DIM-VALUE-FORM` (a DIFFERENT, narrower blocker — pass ordering, not type-dependence). | (closed) | plan 23 §3.2; commit 478daa7 (#24); superseded the dead `sizeof_in_array_dim.c` pin (deleted — that case now folds) |
| `D-FC6-SIZEOF-ARRAY-DIM-VALUE-FORM` | ✅ **CLOSED 2026-06-15 (semantic-expr-typer cycle)** — via option (a): the complete semantic-tier expression typer (`subtreeType`, closing `D-SEMANTIC-SUBTREETYPE-TRANSPARENT-WRAPPERS`) resolves a sizeof-value operand's type at Pass 1.5 (a bare identifier by scope-lookup; subscript/deref/member/CAST/etc. by the per-verb derivations + the cast/sizeof/compound re-typing arms). So `int a[sizeof(arr)/sizeof(arr[0])]` folds (`arr`→`int[8]`, `arr[0]`→`int` via Index; count = 8), and `int a[sizeof((char)y)]` re-types via the cast. Pins: `examples/c-subset/sizeof_value_array_dim` (count idiom, exit 42) + `examples/c-subset/sizeof_cast_array_dim` (cast re-types, exit 42, red-on-disable for the cast arm). Residual: a forward-reference / not-yet-declared operand at Pass 1.5 stays fail-loud → `D-EXPRTYPE-PASS15-FORWARD-REF` (fail-safe, never a miscompile). | (closed) | plan 23 §3.2; commit 478daa7 (#24); `D-SEMANTIC-SUBTREETYPE-TRANSPARENT-WRAPPERS` (the typer that resolved it) |
| `D-EXPRTYPE-PASS15-FORWARD-REF` | **Member-access case ✅ CLOSED + genuine forward-ref confirmed correct fail-loud (pre-FC7 residuals cycle, 2026-06-15).** Two sub-cases were conflated here. (1) The genuine FORWARD-REF (`int a[sizeof(b)]; int b;` — a leaf used before its declaration's type resolves) is invalid C (use-before-declaration); it resolves to InvalidType → `S_NonConstantArrayLength` (fail-loud, never a miscompile) — the CORRECT C rejection, NOT a deferral, nothing to close. (2) The adjacent MEMBER-ACCESS case (`int a[sizeof(s.y)]`, `s` declared) is now ✅ CLOSED via R1: the shared `resolveMemberAccess` types `s.y` at Pass 1.5 (base by scope-lookup → struct scope via `compositeScopeByType` → field type), so the dim folds. Pins: `ForwardRefSizeofArrayDimensionStillRejected` (forward-ref stays rejected) + `MemberAccessSizeofResolvesArrayDimension` (member-dim folds → Array<I32,4>; red-on-disable S000B) + `BadFieldSizeofArrayDimensionRejected`. Priority: **resolved**. | (no remaining gap — forward-ref is invalid C; supporting it would need a Pass-2 dim re-resolution C does not require) | plan 23 §3.2; `semantic_analyzer.cpp` `subtreeType` member arm + `resolveMemberAccess`; commit 478daa7 (#24) |
| `D-SEMANTIC-NULL-CONSTANT-FOLDING` | ✅ **CLOSED 2026-06-15 (pre-FC7 residuals cycle) — R2 multi-tier close.** `admitsNullPointerConstant` keeps the literal-`0` fast path and ADDS a folded path: an INTEGER-typed operand (`subtreeType`, signed/unsigned int ranks — excludes a float `1.5-1.5`) that `constIntExpr` folds to 0 (`-0`, `0+0`, `1-1`, `(int)(2-2)`) is admitted AND the node is MARKED (`EngineState::nullPointerConstantNodes` → `SemanticModel::isNullPointerConstant`); the CST→HIR lowerer materializes a synthetic `Literal 0` in its place (lowerExpr/lowerFlatExpr entry short-circuit) so the existing literal-0 coerce arm emits `Cast(0→Ptr)` — no tier divergence (the operator tree never lowers). Admits at all 3 conversion contexts (call-arg/return/init). Pins: `FoldedZeroAdmitsAs{PointerArg,Return,Init}` + `NegativeZeroAdmitsAsNullPointerConstant` (the flipped pin) + `FloatZeroRejectsAsPointerArg` (float-zero rejected — integer-kind gate AND a `constIntExpr`-nullopt-on-float backstop, double-guard) + `examples/c-subset/null_constant_folded` (runtime exit 42 = NULL; red-on-disable BOTH tiers — semantic revert → S_TypeMismatch, HIR revert → H0003 verifier reject). | (closed) | plan 23 §3.2; `semantic_analyzer.cpp` `admitsNullPointerConstant` + `cst_to_hir.cpp` `nullPointerConstantLiteral`; commit 478daa7 (#24) |
| `D-FC6-FAM-SEMANTIC-CONSTRAINTS` | ✅ **CLOSED 2026-06-15 (FC6-deferrals cycle).** A semantic FAM-constraint pass at the struct-composition site (`semantic_analyzer.cpp`) emits POSITIONED diagnostics for the C99 §6.7.2.1 violations: `S_FlexibleArrayNotLast` (0xE01B), `S_FlexibleArraySoleMember` (0xE01C — fixes the silent `sizeof(sole-FAM)==0`), `S_FlexibleArrayInAggregate` (0xE01D — a FAM-bearing struct/array embedded as a member, via the recursive `typeContainsFlexibleArray` helper over the acyclic interned-operand DAG). Positioned at the offending field (each field's `declRuleNode` carried through the composition). The valid FAM (last + non-sole) stays clean; the layout-engine non-last `nullopt` is the backstop. Pins: `fam_not_last.c` / `fam_sole_member.c` / `fam_in_aggregate.c` diagnostic goldens (code + position). | (closed) | plan 23 §3.2; commit 478daa7 (#24); `parse_diagnostic.{hpp,cpp}` (the 3 codes) |
| `D-CSUBSET-SIZEOF-CHAR-STRING-FOLD` | ✅ **CLOSED 2026-06-15 (sizeof char/string cycle, branch p8).** **R1 (crash):** `(int)sizeof('c')` / `("str")` HARD-CRASHED (`TypeInterner::get: TypeId out of range`) — `lowerCast` called `interner.kind()` on the failed-sizeof's InvalidType operand (bare `sizeof('c')` already failed loud H0009); fixed by `if (!operand.type.valid()) return operand;` right after the operand lowers. **R2 (fold):** a char literal types `int` (C 6.4.4.4 → `sizeof('c')`==4) via a `{CharLiteral,I32}` literalTypes row; a string literal types `Array<Char,N+1>` (C 6.4.5 → `sizeof("abcd")`==5) via a `{StringLiteral,Char,stringArray:true}` row DIVERTED (length is per-occurrence) to a dedicated `SchemaIndexes` field + a Pass-2 arm decoding via the SHARED `decodeStringLiteralBody` (same helper the HIR uses → no tier divergence); the `charConvertsToArith` flag adds a UNIDIRECTIONAL int→char `isAssignable` arm so typing the literal `int` does not regress `char x='c';`. Pins: `examples/c-subset/sizeof_char_string_fold` (exit 42 = 4+5+33; red-on-disable: revert typing → H0009) + `sizeof_unsizeable_in_cast_error` (malformed `"\q"` → H_UnsupportedLoweringForKind; red-on-disable: revert the lowerCast guard → crash) + `CharLiteralIsTypedIntNotUntyped` + `StringLiteralIsTypedCharArrayNotUntyped` + `CharLiteralInitializesCharSlotCleanly`. | (closed) | plan 23; commit 69dbf9f (#25); `type_rules.hpp` char arm + `semantic_analyzer.cpp` string arm + `cst_to_hir.cpp` lowerCast guard |
| `D-CSUBSET-CHAR-INT-WIDENING` | ✅ **CLOSED 2026-06-16 (char value codegen cycle, branch p8).** The `charConvertsToArith` `isAssignable` arm is now BIDIRECTIONAL — char→int WIDENING (`int f(char c){return c;}`, `char h();…return h();`) is admitted alongside int→char, gated by the SAME config flag (a non-C schema keeps Char strict). Codegen realizes BOTH directions: char→int = the byte-source SExt (movsx r/m8 / SXTB); int→char = the width-32 Trunc (`mov r32,r32`, the narrowing-to-byte lazy at the next byte consumer). The pre-existing strict pins were INVERTED to clean-direction pins (`CharParamReturnedAsIntIsClean` / `CharResultCallReturnedAsIntIsClean`); the mismatch MECHANISM stays non-vacuously pinned by `DistinctTypedReturnRemainsMismatch` (`char*`→`int*` → 1 mismatch). Pins: `CharToIntSExtCarriesByteSourceWidth8` + `IntToCharTruncLowersAtPromotedWidth32` (LIR width, red-on-disable) + `char_value` runtime corpus (exit 132, sign-correct). | (closed) | plan 23; commit 69dbf9f (#25); `type_rules.hpp` isAssignable char arm + `mir_to_lir.cpp` SExt/Trunc gates |
| `D-CSUBSET-CHAR-STRING-VALUE-CODEGEN` | ✅ **CLOSED 2026-06-16 (char value codegen cycle, branch p8).** Full `char` VALUE codegen on BOTH targets via a sub-native BYTE width axis (`kLirInstFlagWidth8`): byte LOAD (x86 movzx r/m8 / arm64 LDURB — zero-extending, byte-exact), byte STORE (x86 mov r/m8,r8 + forceRexPrefix / arm64 STURB), byte SEXT (x86 movsx r/m8 / arm64 SXTB — char→int widening). The width-8 flag is CONFINED to {load, store, sext}: `memAccessWidthFlags(Char)=8` at every memory site (load/store/folded-rip-load+predicate/Extract/InsertValue); `registerOpWidthFlags` collapses char's byte width→32 for PLUMBING (const/copy/Trunc — no 8-bit register form; value consumed low-bits-only); char COMPUTE never occurs (integer promotion → I32) and would fail LOUD (no width-8 ALU variant). String-value rodata (`char* p="abc"; *p`) loads via the existing rodata producer + byte load. Indexed char access (`p[n]`, x86 SIB byte forms) works on x86; arm64 indexed is blocked by a PRE-EXISTING arm64 `lea` (symbol-only) gap affecting int+char alike — NOT a char limitation. Pins: 5 LIR width flags (red-on-disable demonstrated) + 2 asm byte-word pins (exact bytes, both targets) + `char_value` (cross-target, exit 132, sign-correct) + `char_ptr_indexed` (x86 indexed, exit 131). RUNTIME-WITNESSED: x86 native PE + arm64-ELF qemu (132/131); macho-arm64 + x86-ELF on CI legs. | (closed) | plan 23; commit 69dbf9f (#25); `mir_to_lir.cpp` width derivations + x86_64/arm64 `.target.json` byte variants |
| `D-FC7-MEMBER-ACCESS` | ✅ **CLOSED 2026-06-16 (FC7 Phase 1, branch p9).** Struct/union member-access CODEGEN end-to-end on BOTH targets: `s.x`/`p->x` read+write + `&s.x` lower to a 2-op byte-offset GEP `[base, Const(fieldByteOffset)]` (resolved at HIR→MIR via FC6 `computeLayout`) → the base+disp `lea` both targets ship (`lowerGep` splits 2-op-Const→base+disp vs 2-op-vreg→base+index; the ≥3-op array-Index arm is untouched). By-value struct/union LOCALS are frame-sized correctly: the Alloca payload carries the layout byte size and `lir_callconv` reserves `ceil(size/slotWidth)` slots per alloca via the SINGLE shared `allocaSlotCount` (the frame-size sum + the materialize offset progression in lockstep — a divergence would overlap a struct onto its stack neighbour). Aggregate initializers (`{3,4}` / designated `{.y=7}`) lower ELEMENT-WISE (one Store-per-field at the FC6 offsets, bypassing the no-LIR aggregate InsertValue). Struct COPY by VALUE (`a = b`, `T a = b;`, `*pa = *pb`) ALSO copies field-wise (`lowerAggregateCopy`) — closing the self-audit-caught silent 8-byte truncation; union/aggregate-field copy fails loud (`D-FC7-AGGREGATE-COPY-MEMCPY`). Pins: 3 flipped MIR shape tests (2-op + byte-offset value asserts) + `struct_member_access` (exit 42) + `struct_designated_init` (exit 42) + `struct_big_neighbor_guard` (exit 0 — the alloca-sizing correctness pin; red-on-disable DEMONSTRATED: a 1-slot revert clobbers the guard → exit 1) + `struct_copy_value` (exit 99 — far field survives the field-wise copy). RUNTIME-WITNESSED: x86 native PE + arm64-ELF qemu (42/42/0/99); macho + x86-ELF on CI. | (closed) | plan 23 §3.1 FC7; commit 688e94b; `hir_to_mir.cpp` (fieldByteOffset + element-wise init) + `mir_to_lir.cpp` (lowerGep base+disp) + `lir_callconv.cpp` (per-alloca sizing); Phase-2 successor `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` |
| `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` | ✅ **CLOSED 2026-06-16 (FC7 C3, branch p9, commits d25696d + 03ba02d) — ALL 4 ABIs done.** C3 = AAPCS64 + Apple arm64 via the **IRR-reroute design** (§B Option A — explicit indirect-result register, refined at build time from the plan-lock's `WriteIndirectResult` op, which was UNSOUND because x8 is allocatable: emitting `mov x8,R` before arg-setup could clobber an arg parked in x8). As-built: the caller reuses the existing sret-pointer PREPEND (operands[1]) + a new `call_payload::kIndirectResultBit` (bit 30) that lir_callconv reads to ROUTE that operand to `cc.indirectResultRegister` (x8) via the SAME `argMoves` parallel-move set (inherits the hazard detection; R is a real Call operand ⇒ regalloc keeps it live; an arg-in-x8 is read by its own earlier arg-move before the appended IRR move writes x8 — no clobber, genuine cycle fails loud like D-ML7-2.3). Callee reads x8 via a new `ReadIndirectResult` op (mirror of `Arg`) + returns VOID (`WriteIndirectResult` was DESIGNED but proved dead → removed). HFA classifier (`aapcs64_hfa`: 1–4 same-FP-type leaves → that many FPR pieces, width-honoring `pieceType` F32/F64; non-HFA ≤16B → ceil(size/8) GPR; >16B → x8-sret) + config (both arm64 CC rows: strategy/maxRegBytes=16/indirectResultRegister=x8/returnFprs=d0..d3). SF-1 ✅ (the `emitParallelRegMoves` >2-cycle guard raised — the scratch-break linearizes any cycle). **★ FOUND + FIXED a HIGH PRE-EXISTING silent miscompile (the crux of this cycle):** every MIR `Return`-CLONE site (`mir_merge.cpp` cross-CU + `mir_rebuild_helper.cpp` shared rebuild [the baseline `runPruneUnreachableBlocks` path] + `inlining.cpp` caller-clone) mapped ONLY `oldOps[0]`, TRUNCATING every multi-piece struct return to piece 0 — and `Return.maxOperands` was `1` (the verifier's upper guard of the same bug). C1c's multi-piece return therefore NEVER worked end-to-end; it was masked on x86_64 because the dropped piece's value usually still aliased its arg register at the return register (a 3rd field passed in rdx == returnGprs[1]) — AAPCS64's distinct arg/return mapping exposed it (`mkTri` read field 3 as field 2). Fix: all clone sites map ALL operands via `addReturnMulti` + `Return.maxOperands`→N + a fail-loud guard for inlining a multi-piece return (`D-FC7-INLINE-MULTI-PIECE-RETURN`). RUNTIME-WITNESSED: `struct_byval_aapcs64` arm64-ELF qemu exit 42 (HFA double-pair d0:d1 + float-HFA s0:s2 + 2-GPR x0:x1 + >16B x8-sret with a real arg in x0 [N-2] + HFA arg + >16B by-ref arg) — and ALL 133 c-subset examples pass baseline+optimized (x86 native + arm64 qemu), proving the multi-piece fix end-to-end; macho-arm64 + x86-ELF on CI. Pins: 7 AAPCS64 engine pins + `IdentityRoundTripPreservesMultiPieceReturnAllOperands` (the clone-truncation guard, host-independent) + `ThreeFprHfaReturnCycleBreaksViaScratch` (SF-1) + `Aapcs64StructSretUsesReadIndirectResultAndFlagsCall` (N-3 MIR: callee ReadIndirectResult+void return + caller IRR-flag) + the CallPayload IRR-bit roundtrip + corpus. Closes the struct-return half of `D-FF3-APPLE-ARM64-ABI-DIVERGENCE` (variadic half stays open). **NOTE (FC7 Phase 2 — the user REQUESTED struct by-value args/returns).** | Pass/return a struct/union BY VALUE — needs per-ABI classification: SysV-x86_64 eightbyte (INTEGER/SSE register pairs + MEMORY spill), AAPCS64 (HFA + ≤16-byte register pass + sret hidden-pointer), Win64 (by-reference over 8 bytes). Today FAIL-LOUD at the call site AND the function definition (`hir_to_mir.cpp` lowerCall arg/return guard + lowerFunction param/result guard) — a struct arg/return emits a positioned `unsupported` diagnostic, never a silent pointer-pass (which would diverge from by-value-copy semantics and miscompile). Priority: **high — FC7 closeout Cycle C (IN PROGRESS).** **§B RESOLVED 2026-06-16 (user):** lowering boundary = **HIR→MIR** (classify + synthesize register-pieces / sret-pointer / by-ref-copy at HIR→MIR, reusing `lowerByteWiseCopy`/alloca/Gep/Load/Store; the MIR/LIR arg machinery + register pools stay UNCHANGED; thread the resolved CC into HIR→MIR like FC3 threaded `dataModel`) — over the MIR→LIR alternative. AGNOSTIC design: extend `TargetCallingConvention` with by-value params + a CLOSED `aggregateClassification` STRATEGY enum (sysv_eightbyte / aapcs64_hfa / win64_by_size); a bounded per-strategy classify algorithm in the realization tier switches on the enum (the `cc.slotAligned` precedent), NEVER target/cc.name identity. PHASED: C1 substrate + SysV (x86_64 ELF/MachO, runtime-closed ELF+qemu), C2 Win64 (by-ref>8 + sret + shadow), C3 AAPCS64+Apple (HFA + ≤16-in-regs + x8-sret); the fail-loud guard becomes strategy-availability-keyed so un-built CCs stay loud; anchor CLOSES at C3 (all 4). Array-storage gaps (`D-MIR-STORAGE-ARRAY-INDEX-GEP`, `D-MIR-ARRAY-FIELD-AGGREGATE-INIT`) DEFERRED to a dedicated post-FC7 cycle (user). **C1a ✅ (commit b0a140c)**: config substrate + the `aggregate_abi` SysV eightbyte classifier (13 engine pins) + caller/callee synthesis for SINGLE-register (≤8B, 1 eightbyte) + BY-REFERENCE (>16B) struct ARGS — runtime-witnessed x86_64-ELF exit 42. The 2-eightbyte (9–16B) in-register case + struct RETURNS are fail-loud, pinned below (the multi-register MIR-Arg extension = §B-resolved Option A, the C1b step). **C1b ✅ (commit b7f547d): 2-eightbyte structs pass/receive IN REGISTERS — `D-FC7-SYSV-STRUCT-ARG-MULTIREG` ✅ + the latent `D-ML7-2.10` ✅. C1c ✅ (commit 1016d47): SysV struct RETURNS by value — reg-pair via the new `ReturnPiece` opcode + sret + mixed-class — `D-FC7-SYSV-STRUCT-RETURN-IN-REGS` ✅. FULL x86_64 SysV struct-by-value ABI (args + returns) DONE. C2 Win64 + C3 AAPCS64/Apple remain; THIS parent anchor closes at C3.** **C2 ✅ (commit a72d249): MS x64 (Win64) struct by-value — a bounded `win64_by_size` classifier arm (power-of-2 ≤8 → 1 GPR; every other size incl. 3/5/6/7 + >8 → by-reference; NO SSE/HFA — a small aggregate is an integer of its size) + the `ms_x64` CC's `aggregateClassification`/`aggregateMaxRegBytes=8`; REUSED the C1a+C1c arg/return synthesis (single-reg / by-ref / sret) verbatim — no new mechanism. Runtime-witnessed x86_64-PE NATIVELY on Windows: exit 42 (8B in-GPR + 12B by-ref/sret + 3B non-pow2 by-ref/sret). Engine pins (1/2/4/8→1 GPR, 3/12→by-ref, float-struct→GPR-not-SSE) + `struct_byval_win64` corpus. C3 AAPCS64+Apple (HFA + ≤16B in regs + x8-sret) is the LAST step; THIS parent anchor closes there.** **C3 §B RESOLVED + DESIGN PLAN-LOCKED (2026-06-16; ready to BUILD next cycle, resume = `.temp/c3-aapcs64-plan.md`).** §B (user): the x8 sret = **Option A — explicit indirect-result-register path** (a dedicated IRR role, over B sentinel-arg-ordinal / C hidden-arg-rejected-as-ABI-non-conformant). Independent plan-lock = GO-WITH-FIXES, all folded into the resume doc: **MF-1** — the IRR pointer R must be a dedicated `WriteIndirectResult` MIR op (caller mirror of `ReadIndirectResult`), NOT a Call operand (else `mir_to_lir::lowerCall` routes R into x0 as arg0 = silent ABI arg-shift, the exact thing A prevents); **MF-2** — `mir_verifier` must accept F32 return pieces (else float-HFA returns fail loud); **SF-1** — raise the `emitParallelRegMoves` >2-cycle guard for 3/4-FPR HFA returns; + the HFA classifier (float/double width via a width-honoring `pieceType`), config (both arm64 CC rows: strategy/maxRegBytes=16/indirectResultRegister=x8/returnFprs=d0..d3), and a host-independent x8-siting structural pin. Closes `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` (all 4 ABIs) + the struct-return half of `D-FF3-APPLE-ARM64-ABI-DIVERGENCE`.** | plan 23 §3.1 FC7; `hir_to_mir.cpp` lowerCall + lowerFunction guards; `target_schema.hpp` `TargetCallingConvention`; `abi_catalog.cpp` CC selection; built on `D-FC7-MEMBER-ACCESS` + `D-FC7-AGGREGATE-COPY-MEMCPY` |
| `D-FC7-SYSV-STRUCT-ARG-MULTIREG` | ✅ **CLOSED 2026-06-16 (FC7 C1b, §B Option A, commit b7f547d).** A 2-eightbyte (9–16 byte) struct now passes/receives IN REGISTERS. **As-built (simpler than the registered plan):** (1) HIR→MIR emits each param + struct-piece `Arg` with a MONOTONIC PER-CLASS counter (`ArgOrdinalCounter` — GPR/FPR separate for an independent CC, flat for a slot-aligned one; `argSlotAligned` threaded from the CC like `dataModel`), so the payload is the per-class physical register ordinal and N pieces land in consecutive regs. (2) The `mir_verifier` bound changed from `argIndex < FnSig paramCount` to `argIndex < (number of physical `Arg` instructions in the function)` — sound because a per-class payload < its class count <= the total, and a flat payload < the total — so NO classifier/ABI threading into the verifier was needed (simpler than the planned "classifier threaded into mir_verifier"). The C1a caller + callee multi-register fail-loud guards were removed. **ALSO CLOSED `D-ML7-2.10`** (the per-class change fixes the latent mixed-class scalar-arg payload — a scalar param's payload is now its per-class index, not the param index). Runtime-witnessed x86_64-ELF: `{int,y,z}` 12B in rdi:rsi (exit 42, debug+release) + `{long,long}` 16B + the D-ML7-2.10 mixed int/double/int call (the double correctly in xmm0, exit 42). Pins: `SysVTwoEightbyteStructParamReceivesTwoRegisterPieces` (2 I64 Args, per-class payloads 0,1) + the `struct_byval_sysv` corpus (now single+multi+by-ref) + `mixed_class_args` corpus (D-ML7-2.10). | plan 23 §3.1 FC7; commit b7f547d; `mir_verifier.cpp` physical-arg-count bound + `hir_to_mir.cpp` `ArgOrdinalCounter`; `D-ML7-2.10` ✅ (closed with it); `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` (still open — returns + C2/C3) |
| `D-FC7-SYSV-STRUCT-RETURN-IN-REGS` | ✅ **CLOSED 2026-06-16 (FC7 C1c, §B Option A, commit 1016d47).** A struct/union RETURNED by value now works across ALL SysV return classes. **§B (user) = Option A: a new MIR `ReturnPiece` opcode** (the caller-side mirror of `Arg`) for the multi-register capture — over (B) a side-table + post-regalloc memory synthesis or (C) sret-everything (ABI-non-conformant for ≤16B, rejected). **As-built:** (1) **callee** loads the eightbyte pieces and emits a MULTI-operand `Return` (maxOp bumped to N); the lir_callconv materialization moves each piece → `returnReg[class][per-class cursor]`. (2) **caller** (`emitStructReturningCall`): the struct Call's result IS piece 0 (returnReg ordinal 0 of its class) and each further eightbyte is a `ReturnPiece(call, ordinal)` read; all pieces store into the result slot `R` (a frame Alloca), and `R`'s address is the call's value (consumed by every aggregate-by-address consumer via the new `lowerLvalueAddress(Call)` arm). (3) **sret** (>16B MEMORY): a hidden result-pointer `Arg` (GPR ordinal 0, shifting the real params via the shared `ArgOrdinalCounter`) the callee copies the result through (`lowerByteWiseCopy`) + returns; no new opcode. (4) **MF-1 cycle-break** (BUILT — the arg path only REJECTS cycles, D-ML7-2.3): `emitParallelRegMoves` breaks a return-register 2-swap with a caller-saved scratch (≥3-cycle = AAPCS64 HFA, fails loud → C3). **Latent-crash fix:** `lowerFunction` copied the `interner.fnParams` SPAN into an owning vector — the sret/by-value `interner.pointer(…)` interning can REALLOCATE the pool and dangle a retained span (an interner-state-dependent "TypeId out of range"; also hardened the C1a/C1b struct-arg path). Verifier relaxed: a struct-returning func's Return may carry the first-class aggregate VALUE, N register pieces (I64/F64), or an sret Ptr. Runtime-witnessed x86_64-ELF exit 42 (reg-pair rax:rdx + sret + mixed {double;long} xmm0:rax, debug AND release). Pins: MF-2 callconv swap unit pin (pre-colored {rdx,rax}, value-correct sim) + 4 MIR pins (multi-Return shape, sret hidden-ptr, mixed-class split, call-once SF-2) + `struct_return_sysv` corpus. Reachable via the typedef-name return form (`struct Tag` top-level return type = the pre-FC4 grammar residue `D-CSUBSET-STRUCT-BODY-VARDECL-POSITION`). | plan 23 §3.1 FC7; commit 1016d47; `mir_opcode.hpp` ReturnPiece + `hir_to_mir.cpp` emitStructReturningCall/lowerStructReturn + `lir_callconv.cpp` emitParallelRegMoves; `D-FC7-SYSV-STRUCT-ARG-MULTIREG` (shared substrate); parent `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` (C2/C3 remain) |
| `D-FC7-NESTED-STRUCT-FIELD` | ✅ **CLOSED 2026-06-16 (FC7 closeout Cycle A, branch p9).** A struct/union FIELD whose type is a user struct/union (typedef-name) now PARSES. The entire gap was a SINGLE agnostic chokepoint: the c-subset grammar's `typeBaseAllowingStruct` (consumed ONLY by `structField`/`unionField`) was missing the bare-`Identifier` alt that `typeBase`/`declHeadForParam`/`castTypeBase` already carry — added it (symmetric; UNAMBIGUOUS because a struct/union body has no expression-statement production, so no binder-sketch guard, and a non-type name fails loud at semantic scope-lookup — verified `S0006`). Everything downstream was ALREADY ready (VERIFIED against code, not assumed): recursive `computeLayout`, `lowerLvalueAddress` nested `a.b.c` recursion, SE5 typedef lookup, post-order composition. ALSO closed the nested-INITIALIZER gap in the SAME cycle: `lowerAggregateInitIntoSlot` now RECURSES into a struct/union-typed field whose initializer is a brace-init (a nested `ConstructAggregate`), element-wise into the sub-slot (an aggregate VALUE into an aggregate field stays fail-loud → `D-FC7-AGGREGATE-COPY-MEMCPY`, not a NEW anchor). Reconciled the now-stale FC2 `$fc2CastExprComment` that had claimed this alt was deliberately omitted (its premise — that `typeRefAllowingStruct` feeds varDecl/param — was inaccurate; those use their own heads). Pins: `NestedMemberAccessComposesChainedGepOffsets` (chained-GEP offset composition 0→4, base-chain identity) + `UnionWithStructTypedFieldParsesAndAccesses` (union half of the multi-form contract) + `StructLocalNestedInitRecursesIntoSubSlot` (3 leaf stores, offset multiset {0,0,4,8}, 0 InsertValue) + `struct_nested_field` (exit 42, member access) + `struct_nested_init` (exit 42, nested brace init, fold-resistant via non-inlined `pick()`). RED-ON-DISABLE DEMONSTRATED: disabling the recursion → `struct_nested_init` fails LOUD at LIR (no aggregate InsertValue realization), `struct_nested_field` stays 42 (lever isolated). RUNTIME-WITNESSED x86 PE + arm64-ELF qemu (42/42); macho-arm64 + x86-ELF on CI. Independent review CLEAN (no HIGH/MED/LOW). | (closed) | plan 23 §3.1 FC7; commit 45ccc25; `c-subset.lang.json` `typeBaseAllowingStruct` Identifier alt + `hir_to_mir.cpp` `lowerAggregateInitIntoSlot` recursion; `D-FC7-MEMBER-ACCESS` (the codegen it unblocked) |
| `D-FC7-AGGREGATE-COPY-MEMCPY` | ✅ **CLOSED 2026-06-16 (FC7 closeout Cycle B, branch p9).** A new `lowerByteWiseCopy(src,dst,size)` primitive copies an aggregate's whole object representation via the widest available integer chunks — I64 (8B) → one I32 (4B) → `Char` (1B) for the 0–3 byte tail (NO width-16 form exists; `I8` would silently widen to a 64-bit access — `Char` is the only safe 1-byte vehicle; memAccessWidthFlags: Char→8, I32→32, else→64). `lowerAggregateCopy` now dispatches: a UNION (variants overlap → field-wise of one misses the rest) or a struct with ANY aggregate field (struct/union/ARRAY) → byte-wise whole; a flat scalar struct keeps the existing width-exact field-wise path. ALSO closed the path Cycle A had deferred here: an aggregate field INITIALIZED from an aggregate VALUE (`struct Outer o = { existingInner, … }`) now copies via `lowerAggregateCopy` (was fail-loud). Greedy chunking proven gap-free + overrun-free for all sizes (independent review traced 0,1,2,3,4,5,7,8,12,16,20). Pins: `UnionCopyIsByteWiseNotFieldWise` (I64 chunk, NOT the 4-byte `n` variant — the discriminator) + `StructWithStructFieldCopyIsByteWise` (I64@0+I32@8, far field z@8 survives) + `StructWithArrayFieldCopyIsByteWise` (MIR-tier — array READ-back blocked by the separate `D-MIR-STORAGE-ARRAY-INDEX-GEP`) + corpus `struct_union_copy` / `struct_nested_copy` / `struct_init_from_value` (exit 42, fold-resistant). RED-ON-DISABLE DEMONSTRATED: disable the byte-wise arms → all three corpus fail loud. RUNTIME-WITNESSED x86 PE + arm64-ELF qemu (42); macho + x86-ELF on CI. Independent review CLEAN (no HIGH/MED). | (closed) | plan 23 §3.1 FC7; commit 0107a2c; `hir_to_mir.cpp` `lowerByteWiseCopy`+`lowerAggregateCopy`; `D-FC7-MEMBER-ACCESS`; surfaced `D-MIR-STORAGE-ARRAY-INDEX-GEP` |
| `D-FC7-INLINE-MULTI-PIECE-RETURN` | The OPT7 inliner is now SAFE for by-value-struct-returning callees (the silent-miscompile half ✅ CLOSED 2026-06-16, FC7 C3 follow-up); the *optimization* of actually inlining such a callee stays deferred (first need to inline a multi-piece-`Return` / x8-sret callee profitably). DISCOVERED 2026-06-16 (FC7 C3) while fixing the multi-piece-`Return`-clone truncation cluster; the inlining HALF was caught by the post-commit independent review (two single-block splice paths + the `ReadIndirectResult` frame-splice were initially unguarded). | **Silent-miscompile ✅ CLOSED via a chokepoint REFUSAL:** `inlineLegalityGate` (`inlining.cpp` ~:277) now REFUSES (`return std::nullopt` — fail-SAFE, the call survives, the program compiles) any callee whose body contains a multi-piece `Return` (N>1 operands) OR a `ReadIndirectResult` (x8-sret entry read). This covers EVERY splice path at once — the original review found the single-block `spliceCallee`/`spliceSingleBlock` Return clones (~:518/:892) took only operand 0 (truncating the struct to field 0) AND a spliced `ReadIndirectResult` would read the CALLER's x8 (garbage) — both REACHABLE under the shipped `release.pipeline.json` (which DOES run `Inlining`, threshold 50; the earlier "unreachable today" note was WRONG and is corrected here). The multi-block continuation-Phi `cops.size()>1` abort (~:1115) is now a defensive backstop. **Deferred (optimization):** actually inlining a struct-returning callee (reconstruct the aggregate / thread N piece-Phis / splice the sret pointer to the caller's allocated storage). Priority: **normal** (OPT7 territory — not autonomously openable). Pin: `InliningRefusesMultiPieceStructReturningCallee` (red-on-disable: remove the gate refusal → the callee inlines + truncates). | `inlining.cpp` `inlineLegalityGate` refusal; the multi-piece-`Return`-clone fixes landed this cycle (`mir_merge.cpp` + `mir_rebuild_helper.cpp` + `inlining.cpp` caller-clone, all map ALL operands via `addReturnMulti`; `mir_text.cpp` round-trip too); `D-FC7-SYSV-STRUCT-RETURN-IN-REGS` (the multi-piece machinery) |
| `D-FC7-INDIRECT-X8-SRET-CALLEE-EXCLUSION` | ✅ **CLOSED 2026-06-23 (FC-deferral close-out c7, branch p14).** The regalloc indirect-callee exclusion set (`lir_regalloc.cpp`, the FC4 c2 R2 rule in `allocateOneFunc`) now ALSO excludes the cc's `indirectResultRegister` (x8 on AAPCS64) for an indirect callee whose call carries the indirect-result payload bit — a 4-part MIRROR of the existing variadic-vector-count-reg exclusion: `IndirectCalleeAt.indirectResult` (set from `call_payload::hasIndirectResult`), the resolved `ccIndirectResultRegOrdinal`, and the per-callee `if (ic.indirectResult && ...)` application (added to `excludedScratch` BEFORE the span is formed, so it propagates to BOTH `tryAllocateExcluding` AND `findSpillCandidate` — the spill fallback can't recreate the clobber). AGNOSTIC: reads `cc->indirectResultRegister` (config), NO hardcoded x8; the `.has_value()` guard makes it a no-op for SysV/Win64 (hidden-arg sret, no indirectResultRegister) and for any function without an indirect struct-returning call. Closes the fail-loud edge: an indirect (fn-ptr) >16B-struct-returning call could park the callee in x8 → the FC7-C3 `mov x8, sretPtr` reroute clobbered it → the loud L_IndirectCalleeClobberedByArgSetup backstop (a VALID program failed to COMPILE, not a miscompile). **Validation:** host-independent RED-ON-DISABLE structural pin `LirRegAlloc.Aapcs64PressuredIndirectStructReturnCalleeExcludesX8` — a register-pressure sweep (k=5..11) lands the callee ON x8 at k=10 the moment the exclusion is removed (empirically demonstrated callee=13,12,11,10,9,**8**,20 + restored) + the END-TO-END runtime corpus `examples/c-subset/struct_byval_indirect_aapcs64` (`Big (*fp)(void)=&makeBig; b=fp();` 24B x8-sret fn-ptr call → qemu-aarch64 exit 42). The simple corpus does NOT force x8 (unpressured, fp parks on callee-saved x29) — it is the path-WORKS witness, the structural pin is the red-on-disable guard. 368/368 Windows + WSL qemu; independent review VERIFIED-CLEAN (it rebuilt + toggled the fix red/green + ran qemu + disassembled, and caught an over-claim in the corpus prose — folded). | (closed — c7 branch p14) | `lir_regalloc.cpp` allocateOneFunc indirect-callee consumer; `D-FC7-STRUCT-BY-VALUE-ARG-RETURN` (the IRR move it pairs with) |
| `D-MIR-STORAGE-ARRAY-INDEX-GEP` | ✅ **CLOSED 2026-06-17 (array-storage cluster, branch p9, Option A — user §B).** The index is PRE-SCALED to a BYTE offset at HIR→MIR (`scaleIndexToBytes`, element stride via the FC6 `computeLayout` engine — agnostic, all element sizes incl. non-pow2), so the storage-array Index arm COLLAPSES the vestigial leading 0 to the SAME 2-op byte-offset GEP the pointer path uses (the 3-op form is no longer produced; the LIR `lea` stays scale=1 — NO hardware scale). Also FIXED the latent non-`char` `p[i]` scale-1 silent miscompile + array-LOCAL alloca sizing (8-byte slot → full byte size) + a `stride==0` fail-loud guard. Runtime-witnessed: `array_storage_index` exit 42 (storage `a[i]` runtime index + `int*` `p[2]` + array-local brace-init) on x86-PE + x86-ELF + arm64-ELF qemu under the REAL release pipeline. Pins: `IndexIntoIntArrayCollapsesToTwoOpGepAndScales`, `IndexOverIntPointerScalesIndexToByteOffset` (miscompile-fix), `IndexIntoCharArrayDoesNotScale`, `ArrayLocalAllocaReservesFullByteSize`, `IndexIntoZeroSizeElementArrayFailsLoud`. **Was:** First need to index a LOCAL or struct-FIELD storage array's elements at runtime (`int a[3]; a[i]` or `s.arr[i]` as an r/l-value). DISCOVERED 2026-06-16 during FC7 closeout Cycle B — NOT an FC7 anchor (pre-existing, general array-indexing gap, unrelated to aggregate copy). | Indexing a STORAGE array (an array LOCAL or struct field, baseKind≠Ptr) lowers in `hir_to_mir.cpp` `lowerLvalueAddress` Index arm to a **3-op GEP `[basePtr, 0, idx]`** (the leading 0 steps through the pointer-to-array), but `mir_to_lir.cpp` `lowerGep` does NOT lower the 3-op shape → fail-loud `L_UnsupportedLoweringForOpcode` (deferred opcode) on BOTH targets. POINTER indexing (`p[i]`, 2-op `[ptr,idx]`) works; storage-array indexing never did. Closing work: lower the 3-op array-index GEP — the vestigial `0` makes it equivalent to base+index, so collapse to the 2-op base+index form + apply the ELEMENT SCALE (x86 SIB scale / arm64 LSL), coordinating with `D-AS4-ARM64-INDEXED-LEA-SCALE` for scale≠1. Until then a struct with an array field COPIES correctly (byte-wise, `D-FC7-AGGREGATE-COPY-MEMCPY`) but its elements can't be read (MIR-tier pin only: `StructWithArrayFieldCopyIsByteWise`). Priority: **normal** (a general codegen gap; likely an early post-FC7 / FC8 cycle). | `mir_to_lir.cpp` `lowerGep` (3-op arm) + `hir_to_mir.cpp` `lowerLvalueAddress` Index arm; `D-AS4-ARM64-INDEXED-LEA-SCALE` (the scale half) |
| `D-MIR-ARRAY-FIELD-AGGREGATE-INIT` | ✅ **CLOSED 2026-06-17 (array-storage cluster, branch p9).** New `lowerArrayInitIntoSlot` lowers an array ConstructAggregate ELEMENT-WISE (one Store/recurse per element at the CONSTANT byte offset `j·stride`), mutually recursive with `lowerAggregateInitIntoSlot` for struct elements + itself for multi-dim. Wired at BOTH array-init sites (the array-field recurse-guard's new `Array` arm AND the VarDecl array-LOCAL arm — `int a[3]={1,2,3}` was an additional manifestation closed the same way), so every array brace-init funnels through one helper (§A.5 by-construction). Pins: `ArrayFieldBraceInitStoresPerElementAtStride` (offsets 0,4,8,12), `ArrayLocalBraceInitStoresPerElementAtStride`, `ArrayLocalRuntimeInitStoresElementWise` (flipped the obsolete InsertValue-chain pin — that chain never lowered at LIR). **Was:** First need to brace-INITIALIZE an array-typed struct/union field (`struct S { int a[3]; }; struct S x = { {1,2,3} };`). DISCOVERED 2026-06-16 (FC7 closeout Cycle B self-audit) — NOT an FC7 anchor (array-STORAGE codegen, a sibling of `D-MIR-STORAGE-ARRAY-INDEX-GEP`, not aggregate member-access/copy which FC7 owns). | `lowerAggregateInitIntoSlot`'s recurse-guard is `Struct || Union` (no `Array`), so an array-typed field with a brace initializer falls to `lowerExpr` → a multi-element `InsertValue` chain → FAIL-LOUD at LIR (`lowerInsertValue` rejects non-zero indices). Fail-loud, NOT a silent miscompile. Closing work: handle `Array` in the recurse-guard — but `fieldByteOffset` is struct-field-offset-based, so it needs array-ELEMENT offset logic (i·stride from the FC6 array layout), naturally bundled with the array-storage cluster (`D-MIR-STORAGE-ARRAY-INDEX-GEP`). A struct with an array field still COPIES correctly (byte-wise, `D-FC7-AGGREGATE-COPY-MEMCPY`); only brace-init of the array field is gated. Priority: **normal** (array-storage cluster). | `hir_to_mir.cpp` `lowerAggregateInitIntoSlot` recurse-guard; `D-MIR-STORAGE-ARRAY-INDEX-GEP` (same cluster) |
| `D-LIR-INT-MEMORY-WIDTH-EXACT` | ✅ **CLOSED 2026-06-17 (array-storage cluster, branch p9, user §B "all widths, one cycle").** DISCOVERED by the array work's self-audit: integer memory access was 8-byte-wide for every int except `char` (`memAccessWidthFlags` returned width-default; the x86/arm64 schemas had only width-64 + width-8 mem variants), so a 64-bit load/store of a PACKED sub-8-byte element (array element or struct field) overran — `array_storage_index` CRASHED (ACCESS_VIOLATION) under the REAL release pipeline; FC7 struct-field writes were latently affected too. FIX (width-exact integer memory access): a new `kLirInstFlagWidth16` LIR flag; `memAccessWidthFlags` now returns the EXACT width per integer byte size (8: Char/I8/U8/Bool/Byte; 16: I16/U16; 32: I32/U32; default-64: I64/U64/Ptr — incl. `long`→I32 under LLP64); NEW width-32 + width-16 memory load/store encoder variants on BOTH targets (x86 `mov r32` 0x8B / `mov r16` 0x66 0x89 / `movzx r32,word` 0F B7; arm64 LDUR/STUR Wt + LDURH/STURH — no-index only, the index folds into the `lea`); the schema-loader accepts `width:16`. Also fixed the latent `lowerByteWiseCopy` I32-chunk overrun (it was silently a width-64 access). Witness tiers: int(32) RUNTIME-witnessed (`array_storage_index` exit 42, x86-PE/ELF + arm64 qemu, under the REAL release pipeline) + char(8) by the pre-existing `char_ptr_indexed`; I8/U8/Bool(8) + I16/U16(16) are BYTE-PINNED (`WidthAxisX86/Arm64.Int32And16MemFormsEmit*`) + fail-loud-safe — their VALUE codegen (Trunc/SExt/ZExt to/from sub-32) is the SEPARATE `D-CSUBSET-SUBNATIVE-ALU-FORMS` gap, so they cannot be runtime-exercised yet (no miscompile — they fail loud at value codegen). Loader pin: `WidthAxisLoader.GuardWidthSixteenAcceptedOutOfVocabularyRejected`. Full corpus (314 examples) re-validated x86-native + arm64-qemu (every int load/store changed bytes). | (closed) | `mir_to_lir.cpp` `memAccessWidthFlags`; `lir_node.hpp` `kLirInstFlagWidth16`; `x86_64.target.json`/`arm64.target.json` load/store; `target_schema_json.cpp` width loader; surfaced by the array-storage cluster; pairs with `D-CSUBSET-SUBNATIVE-ALU-FORMS` (sub-32 VALUE codegen) |
| `D-CSUBSET-INDEX-INTEGER-PROMOTION` | ✅ **CLOSED 2026-06-22 (FC-deferral close-out loop cycle 4, branch p14).** A `char` subscript of a WIDE-element array/pointer (`int a[N]; char c; a[c]`) now integer-PROMOTES to `int` (C 6.3.1.1 / 6.5.2.1) BEFORE the index arithmetic. **As-built (small, front-end, the BEST-long-term placement — not a MIR band-aid):** the CST→HIR `Index` lowering (`cst_to_hir.cpp` ~1862) coerces a sub-int index via the SAME `integerPromotedType`+`coerce` the binary-op path uses (config-driven C 6.3.1; gated on `arith_.has_value()` so block-less toy/tsql are byte-identical) — so the HIR index node is already `int` and `scaleIndexToBytes` sees a promoted index. **`builder.makeIndex` has EXACTLY ONE call site** in all of `src/` (independently verified), so EVERY subscript — rvalue load, lvalue store, compound-assign, `&a[i]`, `a[i]++` — funnels through the one promoted chokepoint (no §A.5 multi-site miss). The crux is OVERFLOW: an un-promoted `(char)100 * 4` = 400 wraps mod 256 → reads/writes the WRONG element; promotion computes 400 at int width. Signedness preserved (signed `char`→SExt, unsigned→ZExt; a negative index sign-extends). int/long index untouched (`integerPromotedType` returns the same TypeId → no coerce). **Witnesses:** corpus `index_integer_promotion` exit 42 — char load AND char-indexed STORE both overflow-sensitive (`c=100`/`d=120`, value-divergent) + signed-char — on x86_64-PE (ctest) + x86_64-ELF (WSL) + arm64-ELF (qemu) [macho macos leg]; MIR pin `CharIndexIntegerPromotesToI32BeforeStrideMul` (the stride `Mul` is I32-typed, its lhs is the SExt/ZExt promotion not the raw char Arg) DEMONSTRATED red-on-disable (promotion off → inst-count 8→7, Mul Char-typed, example walls/overflows). Independent review VERIFIED-CLEAN. 365/365 ctest. **SHORT residual (orthogonal, fail-loud, NOT this anchor):** a `short` index promotes CORRECTLY (the cycle emits the SExt) but the I16 SExt/Trunc ENCODING is unrealized at the backend → loud `L_UnsupportedLoweringForOpcode` on x86 + arm64 — the separate sub-native I16-codegen gap `D-CSUBSET-32BIT-ALU-FORMS`/`D-CSUBSET-SUBNATIVE-ALU-FORMS`, NOT the promotion. — ORIGINAL: First need a `char`/`short`-typed subscript of a WIDE-element array/pointer (`int* a; a[c]` where `c` is `char`) to compile. DISCOVERED 2026-06-17 (array-storage cluster re-audit, NIT). Today FAIL-LOUD (NOT a miscompile — and strictly better than the pre-cycle SILENT scale-1 form): `scaleIndexToBytes` types the scaling `Mul(idx, stride)` at the index's OWN type (`hir.typeId(kids[1])`), so a `char` subscript makes a `Char`-typed Mul that walls at the Char-ALU gap (`D-CSUBSET-SUBNATIVE-ALU-FORMS`). Per C 6.3.1.1 the subscript should integer-PROMOTE to `int` before the index arithmetic — a pre-existing FRONT-END promotion gap the scaling faithfully reflects. `char`-index-of-`char`-array (stride 1, no Mul) and `int`-index both work. | (closed) | `cst_to_hir.cpp` Index lowering (integerPromotedType+coerce); `D-CSUBSET-32BIT-ALU-FORMS`/`D-CSUBSET-SUBNATIVE-ALU-FORMS` (the orthogonal I16-codegen gap blocking the short end-to-end); `D-MIR-STORAGE-ARRAY-INDEX-GEP` (the cycle that surfaced it) |
| `D-FF1-DISPATCH-TABLE` | First time the FFI binary-reader dispatch is hot in a profile (i.e., reading >100 .o per CU). | Replace the if-else chain with a compile-time dispatch table indexed by `ObjectFormatKind`. | plan 11 FF1 |
| `D-FF1-MACHO-32` | First i386 / 32-bit ARM corpus needs MachO Mach-O 32-bit reader. | Add the `mach_header` (vs `mach_header_64`) shape variant + dispatch on cputype. | plan 11 FF1 |
| `D-FF1-MACHO-FAT` | First Apple "fat binary" (multiple slices for ARM64 + x86_64) needs slice picking. | Parse the `fat_arch[]` index + select the slice matching the current target. | plan 11 FF1 |
| `D-FF1-MACHO-SECT-KIND` | First MachO reader use case needs section-kind classification beyond text/data (e.g. coalesced sections, dyld stubs). | Map `section_64.flags` to a closed-enum `MachOSectionKind`. | plan 11 FF1 |
| `D-FF1-MACHO-VARIANT-KIND` | First MachO reader needs the Variant-class section semantics. | Closed-enum `MachOVariantKind`; per-variant emitter. | plan 11 FF1 |
| `D-FF1-MACHO-WEAK-DEF` | First MachO module declares a weak definition (`__attribute__((weak))`). | `N_WEAK_DEF` bit + symbol-table emit; linker side dedups. | plan 11 FF1 |
| `D-FFI-SHIPPED-LIB-PLATFORM-SELECT` | ✅ **CLOSED 2026-06-09 — DISSOLVED by Model 3** (the `#include puts("hello")` 3-OS string-FFI capstone cycle). The per-platform-dir SELECTION problem this anchor tracked no longer exists: the 15 per-platform `shippedLibs/<os-arch>/<h>.json` descriptors were COLLAPSED into 5 **NEUTRAL** `shippedLibs/<h>.json`, each carrying platform-neutral signatures + a per-format **`library` MAP** `{pe,elf,macho}`; `shippedLibDirs` became the single neutral root `["shippedLibs"]`. The map is threaded target-AGNOSTICALLY through the front-end (descriptor→`ShippedExternSymbol`→`HirExternRecord`) and resolved to ONE library string per-target LATE, at `compile_pipeline.cpp` (via `objectFormatKindName(format.kind())`; missing key → `externLibraryByFormat` fallback; unknown key → fail loud) — so there is no longer a *directory* to pick by active target, and the front-end stays target-blind. This is **dissolution, NOT the registry's hinted per-dir platform-predicate remediation** (that fix is no longer needed — the platform axis moved INTO the neutral descriptor as the format-keyed library map). Cite: the Model-3 §B decision (plan-00 §0.2). — ORIGINAL trigger/context retained below. FF11 (2026-06-05, cycle 18+; was STILL OPEN after v0.0.2 V2-2 2026-06-06): the angle-form `#include <h>` system search path (`SemanticConfig.shippedLibDirs`) lists its platform EXPLICITLY (c-subset ships `["shippedLibs/windows-x86_64"]`, the only dir). Trigger = the FIRST time a SECOND platform's shipped-lib descriptors land (e.g. a `shippedLibs/linux-x86_64/` with a libc-flavored `stdio.json` descriptor) AND a single c-subset source must compile to BOTH — the resolver would then need to PICK the dir matching the active target (`ObjectFormatKind` / OS / arch) instead of searching every declared dir. Today: `applySystemDirs` (program.cpp) resolves EVERY `shippedLibDirs` entry and the resolver searches all of them, so a Windows + Linux shipped pair would both be on the path (first-match wins → wrong-platform descriptor could shadow). Priority: **normal** (the single-dir path is correct for the Win64-only corpus). UPDATE (2026-06-08, shippedLibs build-out): the `linux-x86_64` + `macos-arm64` descriptor sets NOW EXIST (stdio/stdlib/string/ctype/math each, all decode-validated by `AllShippedDescriptorsDecode`) — so the trigger's "a SECOND platform's descriptors land" half HAS fired. But they are STAGED data only: c-subset `shippedLibDirs` still lists ONLY `["shippedLibs/windows-x86_64"]`, so nothing is on the path to shadow and no source compiles to a non-Windows target yet. The REMAINING (unfired) trigger half is purely the resolver needing to SELECT the dir by active target. NOTE: V2-2 (2026-06-06) replaced the c-subset-source `stdio.h` with the neutral JSON descriptor (`D-FFI-SHIPPED-LIB-DESCRIPTOR-AGNOSTIC` ✅); the platform-select mechanism is unchanged. | Add a per-`shippedLibDirs`-entry platform predicate (e.g. `{ "dir": "shippedLibs/linux-x86_64", "format": "elf", "os": "linux" }`) OR derive the platform subdir from the active target spec at `applySystemDirs` time; the resolver then searches only the matching dir(s). Keep it config-driven (the language declares the platform axis; the engine selects) — NO `if (format == pe)` in the resolver. Pin with two shipped dirs (win + linux) each holding a same-named descriptor with a DISCRIMINATING extern, compiled to each target, asserting the right one resolved. | plan 11 FFI (FF11 angle-include); pairs with D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH (both are "config baked an ABI/platform decision" anchors); D-CAP-MARKER-MULTI-TARGET-E2E-PIN; D-FFI-SHIPPED-LIB-DESCRIPTOR-AGNOSTIC (the descriptor that replaced the `.h`) |
| `D-FFI-DESCRIPTOR-VARIADIC-SIGNATURE` | A shipped-lib descriptor (`shippedLibs/<platform>/*.json`, V2-2 2026-06-06) needs a VARIADIC function (e.g. `printf`) OR a signature form the shared `parseTypeFromText` codec does not yet accept. Today the hir-text `fn(...) -> ...` type grammar parses the param list + a trailing `cc` but has NO variadic marker, while the `FnSig` `isVariadic` scalar EXISTS in the IR but is not spellable in type-text — so a descriptor cannot express `printf`'s `(ptr<char>, ...)`. Priority: **normal** (the shipped surface [stdio/stdlib/string/ctype/math, broadened 2026-06-08] is ALL non-variadic BY DESIGN — `printf`/`fprintf`/`scanf`/`sscanf`/`snprintf` and the rest of the variadic family are deliberately EXCLUDED from the descriptors and documented as such in `shippedLibs/README.md`; a variadic-needing descriptor signature fails LOUD via `F_ShippedLibUnsupportedType` [0x501C] — never silent). | Extend the hir-text `fn` type production (`parseType` in `hir_text.cpp`) + the shared `parseTypeFromText` to parse a variadic marker (e.g. `fn(ptr<char>, ...) -> i32`) into the `FnSig` `isVariadic` scalar; then a descriptor can express `printf`. Pin: a `stdio.json` `printf` row round-trips through `parseTypeFromText` with `isVariadic` set, and the symbol injects + resolves end-to-end. | `D-FFI-SHIPPED-LIB-DESCRIPTOR-AGNOSTIC` (the consumer); `docs/ir-type-text.md` (the `fn`-type grammar); `D-LANG-VARIADIC`; do NOT cite in `src/` (future-work pin only) |
| `D-FFI-EXTERN-CALL-DISPATCH` | ✅ **CLOSED 2026-06-08** — format-driven extern-call dispatch (the fix for a CONFIRMED latent x86_64-ELF FFI miscompile). The plan-lock proved byte-level that the ELF linker points an extern's `symbolVa` at its PLT STUB (code: `jmp [GOT]`), yet MIR→LIR unconditionally emitted `call_indirect_via_extern` (`FF 15` = `call [rip+disp32]`), dereferencing the stub's bytes as a function pointer → SIGSEGV. The extern-call shape is a property of the OBJECT FORMAT, not the CPU target (x86_64-PE vs x86_64-ELF need OPPOSITE opcodes). Fix: new closed enum `ExternCallDispatch` (`core/types/object_format_kind.hpp`) + a per-format `externCallDispatch` field on `ObjectFormatSchema` (`indirect-slot` = PE IAT / Mach-O __got, deref a pointer slot via `call_indirect_via_extern`; `direct-plt` = ELF, plain direct `call`/BL to the linker-synthesized PLT stub). `lowerToLir`/`lowerCall` selects the opcode from the active format's value as DATA — ZERO `if (arch/format == …)`; ELF needs NO new codegen opcode (reuses the existing `call`/BL→PLT-stub). The 4 live exec formats declare it (pe64 + macho64-x86_64 = `indirect-slot`; elf64-x86_64 + elf64-aarch64 = `direct-plt`). NOT validate-required (an exec format built for a non-FFI purpose may omit it); the real fail-loud is at LOWERING — a module with extern imports under a nullopt-dispatch format → `L_RequiredLirOpcodeMissing`; an unknown value → load fail-loud. Pins: `MirToLir.{ExternCallEmitsCallIndirectViaExternOpcode, ExternCallOnDirectPltFormatEmitsPlainCall, ExternImportsWithNoDispatchFailLoud, IndirectSlotWithoutOpcodeFailsLoud}` + the `extern_call_elf` runtime corpus (x86_64-ELF `abs` FFI → exit 42; RED-on-disable by flipping the ELF format to `indirect-slot`). | (closed) | plan 14 linker + plan 11 FFI |
| `D-LK10-ENTRY-ARM64-FFI-ISEL` | ✅ **CLOSED 2026-06-08** — the END-TO-END ARM64-ELF runnable FFI call-WITH-RESULT path is complete. **The gap was the `lea`/GlobalAddr lowering, NOT a "reg-move-class op"** (the D-FFI-EXTERN-CALL-DISPATCH cycle's first guess) — empirically re-confirmed this cycle: with the arm64 `lea` opcode disabled, BOTH the MIR→LIR fixture AND the `extern_call_elf` arm64 compile fail with the EXACT diagnostic `L_RequiredLirOpcodeMissing: target 'arm64' declares no 'lea' opcode (required for lowering MIR GlobalAddr)` — the raw `lowerToLir` lowers a `Call`'s callee `GlobalAddr` via `lowerGlobalAddr`→`lea` (the peephole that folds the symbol into the call's SymbolRef is an OPTIMIZED-path thing, absent in the raw lowering). The D-AS4-3 ARM64 `lea` (ADRP+ADD, commit 92386d3) supplied the missing opcode; this cycle PROVES the FFI path: (1) the MIR→LIR pin `MirToLir.Arm64ExternCallWithResultLowersUnderDirectPlt` (the dropped pin, reconstructed — lowers clean, 2 direct calls, 0 indirect; RED-on-disable demonstrated above), (2) the runnable corpus `examples/c-subset/extern_call_elf` gained an `arm64:elf64-aarch64-linux-exec` arm (compiles host-independently → valid AArch64 ELF; RUNS on the native `ubuntu-24.04-arm` CI leg + qemu via the cross-arch gate → exit 42). The ELF-aarch64 PLT/GOT (`emitArm64PltStub`, D-LK6-8 ✅) + the format-driven `direct-plt` dispatch (D-FFI-EXTERN-CALL-DISPATCH ✅) complete the chain. **⚠️ CORRECTION (2026-06-08, same-day next cycle): the "→ exit 42" RUNTIME claim above was an OVER-CLAIM** (exactly the runtime risk that cycle's own self-audit flagged). The compile-to-valid-AArch64-ELF half was real, but the binary SIGSEGV'd at runtime (CI exit 139; locally reproduced under qemu once the file's execute bit was set). Root cause = a SEPARATE codegen bug, NOT the FFI path: non-leaf ARM64 functions did not spill the link register x30, so `main`'s epilogue `ret` returned to a clobbered LR (an infinite self-loop overrunning the stack). The FFI dispatch itself was always correct — `abs(7-49)` returned 42 in x0. The corpus GENUINELY runs to exit 42 only after `D-LK10-ENTRY-ARM64-NONLEAF-LINK-REGISTER` (below) landed the x30 spill. | (closed — runtime corrected by the NONLEAF-LINK-REGISTER fix) | plan 14 (D-LK10-ENTRY-ARM64 family); D-AS4-3 (the lea that closed the compile path); D-FFI-EXTERN-CALL-DISPATCH + D-LK6-8 (the PLT/dispatch chain); D-LK10-ENTRY-ARM64-NONLEAF-LINK-REGISTER (the x30 fix that made it actually run) |
| `D-LK10-ENTRY-ARM64-NONLEAF-LINK-REGISTER` | ✅ **CLOSED 2026-06-08** — non-leaf ARM64 functions now spill + reload the link register x30. **This is the ACTUAL root cause of the arm64-FFI SIGSEGV (CI exit 139)** that `D-LK10-ENTRY-ARM64-FFI-ISEL`'s "→ exit 42" over-claimed away (the prior cycle's self-audit had flagged exactly this runtime risk). A calling convention that carries the return address in a LINK REGISTER (AAPCS64 x30) rather than pushing it on the stack (x86_64 `call`; callPushBytes=8) makes x30 live across the whole body of a non-leaf function — every `bl`/`call` clobbers it. The frame logic spilled the `calleeSaved` set (x19–x29) but NOT x30 (deliberately the `linkRegister`, NOT in `calleeSaved`, so it stays un-allocatable), so `main`'s epilogue `ret` returned to the clobbered LR (the address after the last call) → an infinite self-loop whose repeated epilogue `add sp` walked SP off the stack into a guard page → SIGSEGV. **The FFI dispatch was always correct** — `abs(7-49)` returned 42 in x0; the PLT stub (ADRP x16/LDR x17/BR x17) + the GLOB_DAT relocation both worked. **Fix** (`src/lir/lir_callconv.cpp::materializeOneFunc`, agnostic + config-driven): when `hasCalls && cc.linkRegister.has_value()`, fold the link register into `savedRegs` so the existing prologue/epilogue machinery spills it at entry + reloads it before `ret`. ZERO `if(arch/format==…)` — x86_64 (no `linkRegister`) takes the no-op path; the schema's long-declared `linkRegister` field (whose `target_schema.hpp` comment PROMISED "ML7 lowering checks linkRegister.has_value()") is finally consumed. **Pins**: `LirCallconv.NonLeafAarch64FunctionSpillsLinkRegister` (non-leaf SAVES x30 + leaf does NOT; RED-on-disable: revert the fold → x30 absent → fail) + `LirCallconv.NonLeafX8664FunctionHasNoLinkRegisterToSpill` (agnosticism control: x86_64 cc has no linkRegister → no-op path) + the `extern_call_elf` arm64 runtime corpus (now genuinely exit 42 under qemu + native-arm64; was SIGSEGV → CI red — so red-on-disable by construction). **+ HOST-INDEPENDENT BYTE-PIN (2026-06-08, same-day hardening)**: `LirCallconv.NonLeafAarch64FramePrologueEpilogueByteExact` drives the real production tail (legalizeTwoAddress → materializeCallingConvention → assemble) and byte-pins the ASSEMBLED non-leaf prologue+epilogue EXACT — incl. `stur x30,[sp,#16]` (f80103fe) at prologue offset 12 + `ldur x30,[sp,#16]` (f84103fe) reloaded before `ret` — against hand-verified ARM-ARM literals, plus a leaf-control (g spills x28/x29 but no `stur x30`). RED-on-disable DEMONSTRATED (disable the fold → frame 0x20→0x10 → both byte windows diverge). Closes the gap where the COMPOSED frame's byte-correctness was witnessed ONLY by the native-arm64 runtime leg (the per-instruction arm64 byte-pins prove instructions in isolation, never the composed frame). The THREE layers — structural (savedRegs, every leg) + this byte-pin (assembled bytes, every leg incl. Windows MSVC) + runtime corpus (exit 42, native-arm64 leg) — are complementary; none replaces another. 207/207 (no frame-size regression — x86_64 has no linkRegister so its frames are byte-identical; no arm64 non-leaf frame-size pin existed pre-fix). | (closed) | plan 14 (D-LK10-ENTRY-ARM64 family); `D-LK10-ENTRY-ARM64-FFI-ISEL` (the over-claim this corrected); `D-LK10-ENTRY-ML7-FRAME-BIAS-UNIFY` (the frame substrate it extends); `target_schema.hpp` linkRegister field |
| `D-AS3-BLOCK-REL-IMM19/26` | ✅ **CLOSED 2026-06-08** — AArch64 intra-function branch displacement resolution. The cycle-13.5 substrate (`BlockRelPatchKind { X86Rel32, Arm64Imm19, Arm64Imm26 }` on `BlockRelPatch`) left the two ARM64 arms as a fail-loud stub (`A_NoEncodingShapeWalker`); this cycle implements the real resolver in `src/asm/asm.cpp`. The displacement is PC-relative TO THE INSTRUCTION ITSELF (`disp = (target - patchOffset) >> 2`, NO +4 bias — unlike x86 rel32's relative-to-after-the-disp) and SCALED by 4 (branch targets are word-aligned); Imm19 (B.cond) occupies bits 5..23, Imm26 (B) bits 0..25. The resolver READ-MODIFY-WRITES only that bit-field (a `writeU32LEAt` would clobber the opcode/cond bits), masks the two's-complement displacement to the field width BEFORE the shift (negative-disp sign bits cannot bleed past the field), asserts 4-byte alignment (`A_NoMatchingEncodingVariant` on a non-multiple delta — never a silent low-bit drop), and signed-range-checks against the field width (`A_ImmediateOperandOutOfRange`; >reach → `D-CSUBSET-LONG-BRANCH` thunk is the future generalization). The fixed32 encoder selects `Arm64Imm19`/`Arm64Imm26` purely from `wire.slotKind` (config-driven; the new `EncodingSlotKind::Imm19={5,19}` added in lockstep) and queues a `BlockRelPatch` into `blockPatches`. ZERO `if(arch/format==…)` — the dispatch is on config-selected patch-kind DATA, symmetric with the x86 `X86Rel32` arm. **Pins**: `LirCallconv.Arm64ControlFlowSkeletonEncodesCmpBcondBWithResolvedOffsets` — a 6-word byte-exact pin through the real `assemble()` path covering forward Imm19 (+2), forward Imm26 (+3), and the **negative Imm26 back-edge** (−4 → `0x17FFFFFC`, two's-complement); RED-on-disable DEMONSTRATED (remove the `>>2` scaling → `0x14000003`→`0x1400000C` + `0x17FFFFFC`→`0x17FFFFF0`). **Runtime**: the `arm64_control_flow` while-loop corpus (exit 42) executes the resolved branches end-to-end — PROVEN LOCALLY under qemu-aarch64 (exit 42 ×3); the native `ubuntu-24.04-arm` CI leg (real arm64 hardware) CONFIRMED it GREEN — run 27170873148, 2026-06-08 (§A.5 cross-target runtime closure SATISFIED). | (closed — runtime CONFIRMED green on the native ubuntu-24.04-arm CI leg, run 27170873148) | plan 13 §AS3 (block-rel substrate, cycle-13.5); plan 14 (D-LK10-ENTRY-ARM64-CONTROL-FLOW); `D-CSUBSET-LONG-BRANCH` (the >reach thunk, still open) |
| `D-AS3-COND-CODE-ARM64` | ✅ **CLOSED 2026-06-08** — AArch64's condition-code table + the generic cond-placement knobs. `arm64.target.json` now declares a `condCodeEncoding` table (10 entries: eq:0 ne:1 slt:11 sle:13 sgt:12 sge:10 ult:3 ule:9 ugt:8 uge:2) consumed by BOTH `jcc` (B.cond, the cond nibble at bits 0..3) AND `setcc` (CSET). The cycle generalized `condCodeFromPayload` with two NEW template fields — `condBitPos` (uint8, default 0) + `condInvert` (bool, default false) — so the SAME generic encoder line (`words[0] |= (condNib & 0x0F) << condBitPos`) serves x86 (the defaults → byte-identical to pre-knob) AND ARM64 B.cond (condBitPos=0) AND ARM64 CSET (condBitPos=12, condInvert=true: `CSET Xd,cond` = `CSINC Xd,XZR,XZR,invert(cond)`, the AArch64 cond-inversion `cond ^ 1`). ZERO `if(arch==…)` — the bit position + inversion are config DATA. Fail-loud: a variant declaring `condCodeFromPayload` on a schema with no `condCodeEncoding` table errors. **Pins**: `Arm64Encoder.CsetEncodesInvertedCondAtBits12` (cset x0,gt=`0x9A9FD7E0` GT 0xC→0xD; cset x0,eq=`0x9A9F17E0` EQ 0x0→0x1 — two conditions defeat a "constant nibble" bug; RED-on-disable DEMONSTRATED: disable `condNib ^= 1u` → 0xD7→0xC7) + `Arm64Encoder.ZextEncodesOrrW` (zext x0,w1=`0x2A0103E0`) + the B.cond cond-at-bit-0 path pinned via the skeleton's `b.sgt`=`0x5400004C`. **Runtime**: the `arm64_control_flow` corpus exercises cmp+jcc end-to-end (PROVEN LOCALLY under qemu, exit 42; native-arm64 CI CONFIRMED green, run 27170873148). Note: lowerCondBr FUSES the loop's ICmp into cmp+B.cond, so the separately-lowered setcc(CSET)/zext are emitted-but-dead in that corpus — their CORRECTNESS is pinned host-independently by the byte-tests, not the corpus. | (closed — runtime CONFIRMED green on the native ubuntu-24.04-arm CI leg, run 27170873148) | plan 13 §AS3 (cond-code table, cycle-13.5); pairs with `D-AS3-BLOCK-REL-IMM19/26` |
| `D-LK10-ENTRY-ARM64-CONTROL-FLOW` | ✅ **CLOSED 2026-06-08** — the CONTROL-FLOW half of plan-14's combined `D-LK10-ENTRY-ARM64-CONTROL-FLOW + NON-LEAF` row (the NON-LEAF/x30 half closed 2026-06-08 by `D-LK10-ENTRY-ARM64-NONLEAF-LINK-REGISTER`). A c-subset function with `if`/`while`/loops now compiles + runs on ARM64: `cmp` (SUBS XZR,Rn,Rm — flags), `jcc` (B.cond imm19 → ifTrue + B imm26 → ifFalse, a target-blind 2-WORD MACRO via `fixedWords`+`wordIndex`), `jmp` (B imm26), `setcc` (CSET), `zext` (ORR Wd,WZR,Wm), and `alloca` (virtual → callconv materializes a frame-relative `lea` = ADD Xd,sp,#imm12) all land. The block-relative branch offsets are filled by the `D-AS3-BLOCK-REL-IMM19/26` resolver; the cond codes by `D-AS3-COND-CODE-ARM64`. **Proof**: the `examples/c-subset/arm64_control_flow/` runtime corpus (`while (n<42) n=n+1; return n;` → exit 42) — a genuine cross-target differential (x86_64 + arm64 arms, both must exit 42), PROVEN LOCALLY under qemu-aarch64 (exit 42 ×3 for both the while-loop AND a fused-if `max(a,b)`), plus the host-independent byte-pins (skeleton 6-word + cset/zext + frame-lea). §A.5 closure SATISFIED: the native `ubuntu-24.04-arm` CI leg CONFIRMED green — run 27170873148, 2026-06-08. | (closed — runtime CONFIRMED green on the native ubuntu-24.04-arm CI leg, run 27170873148) | plan 14 line 324 (the combined row); `D-AS3-BLOCK-REL-IMM19/26` + `D-AS3-COND-CODE-ARM64` (the encoding substrates it consumes); `D-LK10-ENTRY-ARM64-NONLEAF-LINK-REGISTER` (the NON-LEAF sibling half) |
| `D-OUTPUT-EXEC-BIT` | ✅ **CLOSED 2026-06-24 (deferral loop iteration 1, branch feature/0-0-2-p16).** `writeImage` now sets the POSIX execute bits (owner/group/others, `std::filesystem::permissions(..., perm_options::add)`) on an executable-flavor output; `compile_pipeline::linkAndWrite` decides executability CONFIG-DRIVENLY via `ObjectFormatSchema::isImageFlavor()` (no arch/format identity branch — `writeImage` switches on a `bool executable = false`, staying format-blind). A rare chmod failure is a fail-loud WARNING `K_ImageExecBitFailed` (0x8014), NOT a write failure: the bytes are valid, the exec bit a best-effort convenience. No-op on Windows (PE ignores Unix modes). Pins: `LinkWriter.ExecutableOutputCarriesExecuteBitPosix` (#ifndef _WIN32 — red-on-disable BOTH ways: `true`→`+x` present, `false`→absent; the POSIX-CI-leg witness) + `LinkWriter.ExecutableWriteSucceedsEveryPlatform` (host-independent smoke, runs on the MSVC gate). `run_binary.hpp`'s test chmod kept as a now-no-op safety net (by its own design). — *Original row (audit trail):* The compiler writes the output executable (`src/link/writer.cpp`, the `--output` path) at the host umask default — mode 0644 on POSIX, NO execute bit. A user running the produced binary directly (`dss-code-prime --output foo; ./foo`) hits "Permission denied"/ENOEXEC (qemu's `prepare_binprm` rejects a file lacking `mode & 0111`; the kernel's execve too). NOT currently blocking: the examples/integrated test runners `chmod +x` the spawned binary before `posix_spawn` (`tests/test_support/run_binary.hpp`), so the corpus runs green on every CI leg. Discovered 2026-06-08 while root-causing the arm64-FFI failure — the 0644 mode masked the real x30 SIGSEGV under a *manual* `qemu-aarch64 <file>` invocation (ENOEXEC fired before any guest code ran). Trigger = the first non-test consumer running a `--output` binary directly, OR a decision to ship runnable-out-of-the-box output. Priority: **normal**. | When the output object-format is EXECUTABLE (`format.type == "exec"` — config-driven, read from the active format, NOT an arch/format identity branch), set the execute bits on the written file via `std::filesystem::permissions(path, perms::owner_exec \| perms::group_exec \| perms::others_exec, perm_options::add)` (portable; effectively a no-op on Windows where PE ignores Unix modes). Pin: write an exec-format output, assert the file carries the execute bit on POSIX. | plan 14 linker (`writer.cpp`); pairs with `run_binary.hpp` (which chmods for tests today) |
| `D-FF1-PARTIAL-CORRUPTION-LOUD` | First time a partially-truncated input `.o` (network read interrupted) reaches the reader. | Pre-flight size + checksum check; fail-loud `F_FfiInputTruncated` before any per-section walker runs. | plan 11 FF1 |
| `D-FF1-PARTIAL-CORRUPTION-MACHO` | Same shape but Mach-O specific (a truncated `LC_*` command). | Per-command bounds check inside the Mach-O walker. | plan 11 FF1 |
| `D-FF1-PE-OBJECT-EXPORTS` | First PE `.lib` archive's member `.obj` has exports (symbols destined for the export table). | Read `IMAGE_EXPORT_DIRECTORY`; thread exports through to the symbol table. | plan 11 FF1 |
| `D-FF2-MSG-JARGON` | First time a user-facing FF2 diagnostic's jargon is flagged by a non-DSS-fluent dev. | Sweep FF2 diagnostic strings; replace internal-implementation jargon with end-user terms. | plan 11 FF2 |
| `D-FF2-UNSUPP-FULL-SWEEP` | First time a deferred FF2 declaration shape (e.g. nested type, opaque enum) lands in real corpus. | Full SWEEP across the FF2 declaration-shape switch; promote every deferred arm to a real handler OR a per-arm fail-loud code. | plan 11 FF2 |
| `D-FF2-UNSUPP-INFO-WAE-ASYMMETRY` | First user observes the `Info`-vs-`Warning-as-Error` asymmetry in unsupported-decl diagnostics. | Unify the severity policy under one knob; document. | plan 11 FF2 |
| `D-FF5-EXTERNDECLREF-VALIDATE` | First time an `ExternDeclRef`'s symbol-name passes synthesis but doesn't resolve at link time. | Add a pre-synthesis validation that walks every `ExternDeclRef` against the FFI registry. | plan 11 FF5 |
| `D-LIR-SETCC-MOVZX-COMPOUND` | ✅ **CLOSED 2026-06-03** (commit `1ad61cd`) — superseded by D-LIR-SETCC-WIDTH-CONTRACT closure (zext encoding + lowerICmp emits cmp→setcc-b8→zext-result). | (closed) | plan 12 ML5; D-LIR-SETCC-WIDTH-CONTRACT |
| `D-LK10-ENTRY-MAIN-IMPLICIT-RETURN` | ✅ **CLOSED 2026-06-15** (FC5) — the `entryFunctionNames` split LANDED: a NEW `DeclarationRule.entryFunctionNames` field (loader-validated array-of-non-empty-strings + dup check) carries the program-ENTRY name set, de-conflated from `implicitReturnZeroForFunctionNames` (the C `main`-style return-0 set). The driver's entry-symbol resolution (`program.cpp` merged path + `compile_pipeline.cpp` single-CU `resolveSingleCuUserEntry`) now reads `entryFunctionNames`, falling back per-declaration to the return-0 set when absent (so toy/tsql which set neither are unchanged; c-subset sets BOTH to `["main"]`). Data-level independence pinned by `tests/core/test_grammar_schema.cpp::SemanticsEntryFunctionNamesLoadIndependentlyFromReturnZeroSet` (a synthetic config where the two lists DIFFER — the consuming shape no shipped language exercises) + a dup-check test; consumer behavior covered by the full suite (c-subset binaries still resolve `main` as entry → run). De-conflation only (no c-subset runtime change). *(Original status, preserved:)* A future c-subset function decl uses implicit-return-0 semantics for a non-`main` function name. Split `implicitReturnZeroForFunctionNames` from a separate `entryFunctionNames` list per architect Q1 in cycle-2 fold. | plan 14 §LK10 entry; D-CSUBSET-ENTRY-NAME-SPLIT |
| `D-LK10-ENTRY-RESOLVE-ENTRY-FN-IDX` | First time the entry-fn resolution shape needs a 3-agent FOLD-NOW (already happened at Slice C). | ✅ **CLOSED 2026-06-02** (commit `1a0b414`) — shared `resolveEntryFnIdx` helper; 5 walker sites collapsed to single-line calls. | plan 14 §LK10 entry |
| `D-LK4-RODATA-PRODUCER-EXOTIC` | First exotic literal type (F16 / F128 / bfloat / decimal) needs rodata emission. | Encoder arm per type's bit width + endianness. | plan 14 LK4 rodata producer |
| ~~`D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY`~~ | ✅ **CLOSED — superseded by `D-LK4-RODATA-PRODUCER-AGGREGATE-GLOBAL` (struct-head cycle, 2026-06-17); confirmed FC8 cycle 3 (2026-06-18).** A non-string array literal at file scope (`int arr[3] = {1,2,3};`) is emitted byte-exact to rodata by `encodeAggregateValue`'s Array arm — verified working this cycle (element read / sum / const-qualified all correct at runtime). (The remaining gap was the array→ptr DECAY, closed separately by `D-LK4-RODATA-PRODUCER-LOCAL-ARRAY-DECAY` / `-NONSTRING-GLOBAL-ARRAY-DECAY` in plan 14; a non-string array LITERAL/compound-literal decay stays deferred → `D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY-LITERAL-DECAY`.) | (closed) | plan 14 LK4; `asm.cpp` encodeAggregateValue |
| `D-LK4-RODATA-PRODUCER-NONSTRING-GLOBAL` | First non-string global initializer beyond the existing string-literal path. | Generalized constant-encoding walker. | plan 14 LK4 |
| `D-LK4-DATA-PRODUCER` / `D-LK4-DATA-PRODUCER-MUTABLE-GLOBAL` | ✅ **CLOSED 2026-06-19 — writable data sections cycle (commit 38b5cef, pushed; CI macos+arm64 legs green).** The store-into-a-mutable-global crash is fixed: `asm.cpp` `lowerMirGlobalsToDataItems` now routes an INITIALIZED global to read-only `.rodata` (const) vs writable `.data` (mutable) keyed on `MirGlobal.isConst`, and a TENTATIVE zero-init global to `.bss` (zero-fill, empty bytes + `AssembledData.reservedSize`) — this CLOSES the former fail-loud `D-LK4-RODATA-PRODUCER-BSS-EMIT`. Const-ness is threaded source→codegen via a dedicated `MutabilityAttr` HIR side-table (`src/hir/attributes/mutability_attr.hpp` → `HirMutabilityMap` populated in `cst_to_hir` from `SymbolRecord.isConst` → read at `hir_to_mir` → `MirGlobal.isConst` + `globalIsConst()` accessor; preserved across DCE / rebuild / merge). The shared `exec_data_section.hpp` helper was generalized `buildExecRodata`→`buildExecDataSection(kind,…)` (kind-parameterized: Rodata/Data/Bss; Bss = empty bytes + spanSize). All THREE exec writers emit `.data` (writable, on-disk bytes) + `.bss` (NOBITS / zero-fill): ELF (`.data`+`.bss` in a SEPARATE R+W PT_LOAD #2; bss extends p_memsz not p_filesz — both static + dynamic arms), PE (new `.data`/`.bss` section headers; bss SizeOfRawData=0; Characteristics incl. MEM_WRITE read from the schema row), Mach-O (new `__DATA` segment rw- after `__DATA_CONST`; `__data` S_REGULAR + `__bss` S_ZEROFILL last; flags read from schema). Section flags/types come from the format JSON rows (zero identity branches). All FOUR exec formats declare `data`+`bss` rows + `supportedDataSections`. RUNTIME-PROVEN locally: corpus `examples/c-subset/global_write` (mutable→.data + tentative→.bss, both written-then-read) → **exit 42** on PE (Windows host, was SIGSEGV 139 before), ELF x86_64 (WSL), arm64-ELF (qemu-aarch64); macho arm64 structurally verified (CI-only — Apple Silicon leg runs it). Pins: `tests/asm/test_asm_substrate.cpp` `AsmDataSection.*` (section selection) + `tests/link/test_{pe,elf_exec}_writer.cpp` writable-bit writer pins (the former Rejects* tests flipped to Accepts*). | (closed) | plan 14 LK4 / LK1; supersedes the writable-`.data`/`.bss` "STILL DEFERRED" notes on `D-LK1-ELF-EXEC-DATA-SECTIONS` + `D-LK1-MACHO-EXEC-DATA-SECTIONS`; closes `D-LK4-RODATA-PRODUCER-BSS-EMIT`; runtime-init globals stay fail-loud (`D-LK4-RODATA-PRODUCER-RUNTIME-INIT`, out of scope) |
| `D-MIR-TEXT-CONST-ROUNDTRIP` | A pipeline inserts a `mir_text` serialize→parse round-trip on the source→object COMPILE path (today `mir_text` is debug/test-only, NOT on any compile path — no `src/program/` reference). | `mir_text` writer + parser carry `MirGlobal.isConst` (the writable-data-section bit). Without it, a global serialized→parsed through `.dsstext` reverts to mutable → a const global would silently land in writable `.data`. Priority: **trigger-gated** (no compile-path consumer yet; fail-safe default is mutable). | trigger-gated; discovered by the writable-data-sections cycle review (2026-06-19); `src/mir/mir_text.cpp`; pairs with `D-LK4-DATA-PRODUCER-MUTABLE-GLOBAL` |
| `D-LK4-RODATA-WALKER-RELOC-BASE` | First non-PE format needs the rodata-walker reloc-base extension. | Per-format `rodataRelocBase` lookup driven by format schema. | plan 14 LK4 |
| `D-LK1-ELF-RODATA-DATAITEM-RELOC` | First ELF ET_EXEC module whose `AssembledData` rodata item carries its OWN relocations (a data→data reference — e.g. a vtable / const pointer table / 8-byte rodata pointer slot). The ELF ET_EXEC writer (`src/link/format/elf.cpp`) currently fails loud on a non-empty `dataItem.relocations` (it patches FUNCTION relocations only, via `applyExecRelocations`); `int g=42;` carries none. Priority: **normal** (fail-loud today; no producer emits data→data rodata relocations yet — the PE walker's analogous `D-LK4-RODATA-WALKER-RELOC-BASE-OFFSET` path is the precedent to mirror). | Patch each rodata-item relocation into `rodataBytes` at the item's section-relative offset + the reloc's intra-item offset (the def's VA already lives in `symbolVa`), mirroring the PE walker's data-item-relocation arm (`pe.cpp` "Apply DATA-ITEM relocations into the .rdata bytes"). | plan 14 LK1 (ELF exec data sections); mirrors PE `D-LK4-RODATA-WALKER-RELOC-BASE-OFFSET` |
| `D-LK1-ELF-EXEC-DATA-SECTIONS` | 🟡 **RODATA PATH CLOSED (rodata-only scope)** — the ELF ET_EXEC writer (`src/link/format/elf.cpp`) now emits a loadable `.rodata` section from `module.dataItems` (SHF_ALLOC, folded into the single R+X `.text` PT_LOAD via `shFlagsToPFlags`; data-symbol VAs added to the static `symbolVa` map so code→rodata relocations resolve through `applyExecRelocations`). Both `elf64-x86_64-linux-exec` + `elf64-aarch64-linux-exec` declare `supportedDataSections: ["rodata"]` + a `.rodata` row (one shared elf.cpp code path; arch differs only via config). Byte-pinned host-independently (`ElfExecWriter.Rodata*` in `test_elf_exec_writer.cpp`, incl. the PT_LOAD-extension RED-on-disable). **RUNTIME-PROVEN LOCALLY** (2026-06-09): the `global_int` ELF arms (`int answer=42; return answer;`) compile + RUN → **exit 42 on BOTH x86_64-ELF (WSL native) AND arm64-ELF (qemu-aarch64)**; `strace` confirms `exit_group(42)`, and inspection confirms the global's 42 is read from `.rodata` (`lea [rip+disp]` → 0x401048 → load) under a single R+X PT_LOAD whose `p_filesz` covers the rodata extent. **CI RE-CONFIRMED 2026-06-09** (commit `8040410`, run **27187309300**): the `linux-gcc-release` (x86_64) + **`linux-arm64-gcc-release` (native `ubuntu-24.04-arm` hardware)** + `linux-clang-asan` + `macos-clang-release` legs all GREEN — `global_int` RAN → exit 42 on real arm64 + x86_64. (The `windows-msvc-release` leg failed at the **Setup-CMake** step = a CI-infra flake, NOT a code failure — redundant with the local Win-MSVC 212/212; re-triggered.) The rodata-path runtime is now witnessed BOTH locally (WSL+qemu) AND on the authoritative native CI legs. **DYNAMIC-PATH ARM ALSO CLOSED (2026-06-09, the `#include puts` string-FFI capstone):** the `.rodata` emission was originally wired into the STATIC `encodeElfExec` writer ONLY; the capstone extended it to `encodeElfExecDynamic` (the dynamic-linked ELF exec path, which every libc-importing program now takes — string-literal args reach a DYNAMIC extern, and exit routes through libc) via the SAME shared `buildExecRodata`/`addDataSymbolVas` helper — byte-IDENTICAL when no rodata. So a string-literal pointer arg to a dynamic FFI call (`puts("hello")`) now lands its data in `.rodata` on the dynamic path too; runtime-proven by the `shipped_include_puts` corpus → stdout `"hello\n"` + exit 42 on linux-x86_64 (WSL) AND linux-arm64 (qemu). **WRITABLE PATH CLOSED 2026-06-19** (by `D-LK4-DATA-PRODUCER-MUTABLE-GLOBAL`): writable `.data` + zero-fill `.bss` now emit into a SEPARATE 2nd R+W PT_LOAD (W^X; bss extends p_memsz not p_filesz), on BOTH the static + dynamic ELF arms — runtime-proven (`global_write` exit 42 x86_64-ELF WSL + arm64-ELF qemu). **STILL DEFERRED:** the data→data dataItem-reloc path (`D-LK1-ELF-RODATA-DATAITEM-RELOC`). Historical context — the original blocker: the ELF ET_EXEC writer emitted only `.text`/`.symtab`/`.strtab`/`.shstrtab` — NO data section. So a module-level data global (`int g = 42;`) could not reach an ELF executable. This is the blocker for a RUNNABLE ARM64 `lea` corpus: the D-AS4-3 ARM64 `lea` (ADRP+ADD) encoder is built + proven by byte-pins (`Arm64Encoder.Lea*`) + the linker reloc-formula tests (`test_aarch64_reloc_formulas.cpp`), but an end-to-end `static int g=42; int *p=&g; return *p;` corpus that exercises lea→load→exit-42 needs `g` in a loadable data section (and a 2nd R+W PT_LOAD), which the static ET_EXEC path lacks. Same gap affects x86_64-ELF data globals (the format-dispatch cycle's `extern_call_elf` corpus avoided strings for exactly this reason). NOTE (2026-06-08): the FFI direct-call path that REQUIRES the lea to COMPILE now runs end-to-end (`extern_call_elf` arm64 arm → `D-LK10-ENTRY-ARM64-FFI-ISEL` ✅ CLOSED) — but that exercises the lea on the COMPILE path only (the lea materializes `&abs` yet the direct BL targets the symbol via the PLT, so the lea's runtime RESULT is dead there). OBSERVING the lea's runtime VALUE still needs either this data-global deref path OR an indirect call through a function POINTER (`D-ML7-2.4`, a separate isel gap). **UPDATE 2026-06-12 (FC4 c2): the fn-pointer option LANDED — `D-ML7-2.4` closed; the `fnptr_call`/`fnptr_callback` arm64-ELF arms BLR through an ADRP+ADD-materialized `&helper` at RUNTIME (qemu exit 42), so the arm64 lea's runtime VALUE is now observed end-to-end.** Priority: **normal** (the lea ENCODER, the user-directed deliverable, is complete + strongly pinned; only the lea's runtime-VALUE observation is gated). | Extend the ELF ET_EXEC writer to lay out a `.rodata`/`.data` section with the module's `AssembledData` bytes, assign each data symbol a VA, add a 2nd page-aligned PT_LOAD (R[+W]), and let the existing relocation applier patch code→data references. Then reuse `global_int`-style sources (`int g=42; return g;`) with an `arm64:elf64-aarch64-linux-exec` target arm under the qemu/native cross-arch gate → exit 42 proves lea→load end-to-end. | plan 14 LK1 (ELF exec data sections); pairs with `D-AS4-3` (the lea encoder it would make runnable) + the `D-LK4-RODATA-PRODUCER-*` family (the constant-encoding walker) |
| `D-LK1-MACHO-EXEC-DATA-SECTIONS` | ✅ **CLOSED 2026-06-09 — RUNTIME-PROVEN on Apple Silicon** (run **27192415923**, the `macos-clang-release` leg ran `global_int` macos-arm64 → ctest **#184 Passed** → exit 42 on real Apple Silicon: the `__TEXT,__const` data global was READ at runtime). The macOS analogue of `D-LK1-ELF-EXEC-DATA-SECTIONS`. The Mach-O exec writer (`src/link/format/macho.cpp` `encodeExecDynamic`) emits a loadable read-only `__TEXT,__const` section from `module.dataItems` (placed inside `__TEXT` after `__stubs`, gated on `hasConst`; the no-dataItems output is byte-identical to before), built via the shared `src/link/format/exec_data_section.hpp` helper (`buildExecRodata` + `addDataSymbolVas` — the SAME H1 max-align + M1 anonymous-skip + non-Rodata/data-item-reloc fail-loud as the ELF arm); `macho.cpp` cites the ELF precedent `D-LK1-ELF-EXEC-DATA-SECTIONS` as the mirror. A c-subset global `int answer=42;` COMPILES to an arm64-darwin binary whose `__TEXT,__const` holds `2a 00 00 00` (byte-pinned host-independently by the `MachOArm64Exit.MachoArm64Const*` cases in `tests/link/test_macho_arm64_exit.cpp`) AND, per run 27192415923, RUNS on Apple Silicon → exit 42. **WRITABLE PATH CLOSED 2026-06-19** (by `D-LK4-DATA-PRODUCER-MUTABLE-GLOBAL`): a writable `__DATA` segment (rw-, after `__DATA_CONST`) now carries `__data` (S_REGULAR) + `__bss` (S_ZEROFILL) — structurally built + CI-gated on the Apple-Silicon `macos-latest` leg (`global_write`). **STILL DEFERRED** (mirrors ELF): the ET_REL `.o` rodata arm, the data→data dataItem-reloc path (`D-LK1-MACHO-RODATA-DATAITEM-RELOC`), and the macOS-x86_64 form (`D-LK1-MACHO-X8664-DARWIN-DATA-SECTION`). | (closed) — the `macos-clang-release` leg of run 27192415923 ran `global_int` macos-arm64 → exit 42 (ctest #184). The §A.5 RUNTIME witness is shared with `D-LK6-AARCH64-ADDABSLO12-HIGH-VA`. | plan 14 LK1 / LK3-RODATA (Mach-O exec data sections); mirrors `D-LK1-ELF-EXEC-DATA-SECTIONS`; shares `exec_data_section.hpp` with the ELF arm; prerequisite `D-LK6-AARCH64-ADDABSLO12-HIGH-VA` (the reloc-guard relaxation that let the high-VA `__const` access compile) |
| `D-LK1-MACHO-RODATA-DATAITEM-RELOC` | First Mach-O `__const` rodata item carrying its OWN relocation (a data→data reference — e.g. a const pointer table / 8-byte rodata pointer slot). **F5 UPDATE (2026-06-25):** `exec_data_section.hpp` now PATCHES such relocs in-place (`applyDataItemRelocations`, `allowItemRelocations=true`), but `macho.cpp` then fails loud (`K_NoMatchingObjectFormat`) on any abs64 site collected from `__TEXT,__const` — because that segment is read-execute + code-signed and dyld CANNOT PIE-rebase it (a relocated pointer is inherently load-writable). F5's own symbol-address pointers DODGE this: the asm arm forces them to `.data` (writable). So this trigger now fires only for a FUTURE const-with-relocs item the front-end places in rodata. Priority: **normal** (fail-loud today; no producer emits rodata data→data relocations). | Route reloc-bearing rodata to a WRITABLE-AT-LOAD `__DATA_CONST` segment (Apple's const-reloc idiom — writable during dyld fixup, read-only after) + emit its rebase entries (the F5 `dyldRebaseBlob` machinery, segment index `__DATA_CONST`), instead of the sealed `__TEXT,__const`. | plan 14 LK1 / LK3-RODATA (Mach-O exec data sections); shares the `exec_data_section.hpp` guard with `D-LK1-ELF-RODATA-DATAITEM-RELOC`; cited by `macho.cpp` F5 const-site guard; mirrors PE `D-LK4-RODATA-WALKER-RELOC-BASE-OFFSET` |
| `D-LK1-MACHO-X8664-DARWIN-DATA-SECTION` | The `macho64-x86_64-darwin-exec` format was NOT given a `__const` row / `supportedDataSections` this cycle (only `macho64-arm64-darwin-exec` was). Rationale: there is NO macOS-x86_64 CI leg (`macos-latest` is Apple Silicon), so enabling it would ship an UNTESTED §A.5 form (a data section that no leg ever runs). The `macho.cpp` walker code is format-blind (it WOULD work — the gate is the `linker.cpp` capability consult, never a branch), but the x86_64-darwin JSON declares no data section. Priority: **normal** (no consumer; declaring it would be untested gold-plating). | When a macOS-x86_64 runner OR a Rosetta-run gate appears: add the `__const` row + `supportedDataSections: ["rodata"]` to `macho64-x86_64-darwin-exec.format.json` + a corpus arm under that gate → exit 42. | plan 14 LK1 / LK3-RODATA; mirrors the §A.5 "do not ship an untested form" discipline; pairs with `D-LK1-MACHO-EXEC-DATA-SECTIONS` (the arm64-darwin arm that DID land) |
| `D-LK-RODATA-HELPER-PE-PARITY` | ✅ **CLOSED 2026-06-24 (deferral loop, branch feature/0-0-2-p16) — ALREADY-SATISFIED (stale-open row), verified this cycle.** PE was retrofitted onto the shared helper by the **D-LK4-DATA-PRODUCER** cycle: `pe.cpp` calls `buildExecDataSection` (660-677) + `addDataSymbolVas` (921-938) for ALL three section kinds — NO inline rodata→symbolVa loop remains (the premise of this row is stale). **M1** (anonymous-`SymbolId{}` skip) is handled by `addDataSymbolVas` and chokepoint-tested via `test_elf_exec_writer.cpp:2107` (PE inherits it by-construction through the shared chokepoint). **H1** (over-aligned item) is **STRUCTURALLY IMPOSSIBLE** for PE, not merely masked: the `Alignment` type CAPS at 256 bytes (`alignment.hpp` `fromBytes`/`of<N>` reject > 256) while PE `sectionAlignment` is ≥ page (4096), so a section's `maxAlign` can NEVER exceed its alignment — a `checkMaxAlign` fail-loud guard would be untestable dead code (no constructible over-aligned item). Verified the live code state directly; no honest code or test to add. — *Original row (audit trail):* PE (`src/link/format/pe.cpp`) is NOT yet converted to the shared `src/link/format/exec_data_section.hpp` helper (`buildExecRodata` + `addDataSymbolVas`) — elf.cpp + macho.cpp are. Plan-lock chose to DEFER PE (Option b): PE is the Windows-critical-path runtime and its latent H1 (max-item-align) is MASKED by page-aligned `.rdata`, and its M1 (anonymous-symbol-skip) is dormant — so retrofitting it carries Windows-runtime risk for no current correctness gain. Priority: **normal** (DRY/maintainability + closes PE's latent H1/M1; not a correctness bug today). | Retrofit `pe.cpp`'s inline rodata→symbolVa loop onto `buildExecRodata`/`addDataSymbolVas` (this ALSO fixes PE's latent H1 over-aligned-item + M1 anonymous-item gaps). Trigger: the next PE-touching cycle, OR a producer ever emits an anonymous / over-aligned data item (which would surface PE's H1/M1 as a real bug). Supersedes the residual-consumer half of `D-LK4-RODATA-WALKER-RELOC-EXTENSION-HOIST`. | plan 14 LK1/LK2-RODATA; residual of `D-LK4-RODATA-WALKER-RELOC-EXTENSION-HOIST` (the hoist's 3rd consumer); shares `exec_data_section.hpp` with the ELF + Mach-O arms |
| `D-LK6-14-INTEGRATION-PAYLOAD` | ✅ **CLOSED 2026-06-01** (cited at `parse_diagnostic.hpp:698-699` + `macho.cpp:1194-1197` as the closed pair to D-LK6-14-INTEGRATION-GOT-SLOTS). Chained-fixups payload-encoder unit-test substrate. | (closed) | plan 14 LK6 cycle 14; companion D-LK6-14-INTEGRATION-GOT-SLOTS |
| `D-LK6-14-NAME-OFFSET-OVERFLOW` | First Mach-O chained-fixups module whose import-name table exceeds u32 range. | Promote `nameOffset` to u64; or paginate the name table. | plan 14 LK6 cycle 14 |
| `D-LK6-AARCH64-ADDABSLO12-HIGH-VA` | ✅ **CLOSED 2026-06-09 — RUNTIME-PROVEN on Apple Silicon** (run **27192415923**, same witness as `D-LK1-MACHO-EXEC-DATA-SECTIONS`: the `macos-clang-release` leg ran `global_int` macos-arm64 → ctest **#184 Passed** → exit 42, which means the high-VA ADD_ABS_LO12_NC reloc RESOLVED at runtime — else exit ≠ 42). Cited at `exec_reloc_apply.hpp` `Aarch64AddAbsLo12`. The ADD_ABS_LO12_NC ("No Check") reloc applier carried an ELF-centric `S+A ≤ UINT32_MAX` upper bound that WRONGLY rejected valid Mach-O PIE VAs (which sit above the 4 GiB `__PAGEZERO`, e.g. 0x1'0000'4040) — it blocked the macOS-arm64 data-global access path (`global_int` → `__const`). Root cause: `(S+A) & 0xFFF` is a magnitude-INDEPENDENT low-12 extraction (the uint32 cast cannot perturb the low 12 bits), and the address magnitude is range-checked by the PAIRED ADRP companion (`adr_prel_pg_hi21`, signed-21-bit page delta) — so the ADD itself needs no upper bound. A code-correctness relaxation, byte-pinned host-independently by `Aarch64AddAbsLo12.ResolvesSplusAAboveUint32Max` (`test_aarch64_reloc_formulas.cpp`) + `MachOArm64Exit.MachoArm64ConstAdrpAddLeaHighVaResolvesLowTwelve` (`test_macho_arm64_exit.cpp`), with RED-on-disable demonstrated (restore the `> UINT32_MAX` clause → the high-VA pins fail). The relaxation is confirmed NO-OP for ELF/PE (their VAs are < UINT32_MAX) by the `linux-gcc-release` + `linux-clang-asan` + `linux-arm64-gcc-release` + `windows-msvc-release` legs of run 27192415923 all staying GREEN. | (closed) — dropped the `S+A > UINT32_MAX` clause; kept `S+A ≥ 0` (a negative VA is nonsensical). RUNTIME witness = run 27192415923 `macos-clang-release` #184 (`global_int` macos-arm64 → exit 42). | plan 14 LK6; macOS data-section cycle; runtime witness shared with `D-LK1-MACHO-EXEC-DATA-SECTIONS` |
| `D-MERGE-CONTEXT-PREFIX-CLOBBER` | First time `mergeWithTargetContext` clobbers a prefix from a sibling import. | Per-prefix-source side-table (the `D-MERGE-CONTEXT-PREFIX-SIDE-TABLE` companion). | plan 11 FF / merge policy |
| `D-MERGE-CONTEXT-PREFIX-SIDE-TABLE` | Companion to D-MERGE-CONTEXT-PREFIX-CLOBBER — same closure cycle. | (paired with above) | plan 11 FF / merge |
| `D-MERGE-DEDUP-PREFIX-COLLISION` | First time two distinct prefix sources dedup to the same key but produce different values. | Fail-loud on collision with both source citations. | plan 11 FF / merge |
| `D-MERGE-POLICY-IDEMPOTENCY` | First time the merge policy needs idempotency (re-running the merge on already-merged input changes the output). | Define an `isMerged()` predicate + skip-when-already-merged guard. | plan 11 FF / merge |
| `D-MERGE-SCRATCH-FRESH` | First time the merge's scratch context is silently reused across calls. | Reset / fresh-construct the scratch on each merge entry. | plan 11 FF / merge |
| `D-FFI-ANGLE-INCLUDE-LINE-SCOPED-HEADER` | A UX pass on include diagnostics, OR a header name legitimately needing multi-token content. Priority: **normal** (fail-loud today — a malformed `#include <foo` is reported, see `tests/tokenizer/test_tokenizer.cpp::FF11MalformedAngleIncludeNoCloserFailsLoud`; only the diagnostic flavor + consumption scope are deferred polish). | A malformed `#include <foo` (no closing `>`) currently runs the coalescing `header-body` mode to EOF and reports the generic `P_UnterminatedString` (matching `string`-body semantics — `header-body` mirrors the `string` body: coalesced `defaultToken`, `endsAt: ">"`, no `popAtNewline`). The long-term polish is a header-specific unterminated flavor (a new `UnterminatedFlavor::Header` → a dedicated `P_*` code) PLUS line-scoped termination (stop the header body at the newline, like `popAtNewline`) so the error is header-specific AND the rest of the file is not consumed past the directive line. Do NOT re-engineer this cycle (scope creep) — the current behavior is consistent established fail-loud, NOT a regression. | FF11 angle-include (`c-subset.lang.json` `include-directive`/`header-body` modes); pairs with the general `popAtNewline` capability; do NOT cite in `src/` (future-work pin only) |
| `D-FFI-CROSS-HEADER-ODR-FIDELITY` | Full C ODR fidelity becomes a goal, OR a corpus surfaces two included headers declaring the same name as DISTINCT symbols. Priority: **normal** (rare — the common case of two headers declaring the SAME `extern` is correctly idempotent). | FF11 goal-2 flags a PROGRAM `extern` conflicting with an INCLUDED header's decl (`S_RedeclaredSymbol`), but NOT the header-vs-header case: two included headers each declaring the same name as DISTINCT symbols currently last-writer-wins silently (idempotent cross-tree `injectBinding` visibility — deliberately not flagged, correct for FF11's program-vs-header scope). Closing work = extend the cross-tree redeclaration check to also flag header-vs-header same-name conflicts (distinct symbols), leaving the program-vs-header path + the same-symbol idempotent case unchanged. | FF11 goal-2 (`semantic_analyzer.cpp` cross-tree redeclaration loop); surfaced by the cycle-21 self-audit; do NOT cite in `src/` (future-work pin only) |
| `D-FFI-STDINT-PTR-WIDTH-ILP32` | The first ILP32 (32-bit-pointer) target lands (e.g. i386 / armv7 / wasm32). Today EVERY DSS exec target is 64-bit (x86_64-PE/ELF, arm64-ELF/Mach-O) and LP64 + LLP64 agree on pointer width (8 bytes), so the pointer-width + greatest-width typedefs in the neutral `stdint.json`/`stddef.json` (`intptr_t`/`uintptr_t`/`intmax_t`/`uintmax_t`/`size_t`/`ptrdiff_t`) are correctly i64/u64. Priority: **normal** (trigger-gated — the 64-bit-only values are correct for every current target; first surfaced by the p18 SQLite-readiness Cluster G headers cycle as the FIRST consumer of the descriptor typedef path). | When an ILP32 target is added, the pointer-width typedefs (`intptr_t`/`uintptr_t`/`size_t`/`ptrdiff_t`) must become 4-byte for THAT target while staying 8-byte on the 64-bit ones. A single literal hir-text typedef can't express "pointer-width" across both ABIs, so the close is a per-target width resolution: the descriptor declares the typedef as a width-CLASS the semantic phase resolves against the active target's `dataModel`/pointer width (NOT an `if (target == i386)` in the engine — the descriptor/loader declares the width axis, the engine reads it). Pin: an ILP32 target compiles a program asserting `sizeof(intptr_t)==4` while a 64-bit target sees 8. | cited in `src/dss-config/shippedLibs/{stdint,stddef}.json` ($comment); pairs with `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH` + `D-FFI-WCHAR-WIDTH` (both "a descriptor baked an ABI/platform width") + `D-FFI-STDINT-LIMIT-MACROS` (the width-dependent *_MAX inherit this); SQLite-readiness Cluster G (p18) |
| `D-FFI-STDINT-LIMIT-MACROS` | A program references a `<stdint.h>` limit macro — `INT8_MAX`/`INT64_MIN`/`UINT32_MAX`/`SIZE_MAX`/`INTPTR_MAX`/etc. Today the neutral `stdint.json` ships ONLY the typedefs (no `constants` surface), so a direct limit-macro reference fails loud as an undefined identifier (never silent). Priority: **normal** (trigger-gated — SQLite's amalgamation mostly self-defines these via `#if !defined(...)`, but a direct reference would need them; first noted p18 Cluster G). | Add a `constants` surface to `stdint.json` carrying the *_MAX/*_MIN integer macros (INT8_MIN=-128 … INT64_MAX=9223372036854775807, the UINT*_MAX, plus SIZE_MAX/PTRDIFF_MIN/PTRDIFF_MAX/INTPTR_*/INTMAX_*), each integer-typed (rides as i64/u64 via the descriptor `constants` mechanism). The pointer-width + greatest-width limits (SIZE_MAX/INTPTR_MAX/INTMAX_MAX) are 64-bit-target values that inherit the ILP32 deferral [[D-FFI-STDINT-PTR-WIDTH-ILP32]]. Pin: a program using `INT32_MAX` compiles and the value is byte-exact. | cited in `src/dss-config/shippedLibs/stdint.json` ($comment); pairs with `D-FFI-STDINT-PTR-WIDTH-ILP32` (width-dependent limits) + the descriptor `constants` mechanism (FC14 item-1, cf. `limits.json`); SQLite-readiness Cluster G (p18) |
| `D-FFI-WCHAR-WIDTH` | A program needs `wchar_t` at its platform-correct width — 16-bit on Windows (PE), 32-bit on POSIX/Linux+macOS (ELF/Mach-O). The neutral `stddef.json` ships `wchar_t = i32` (the Linux+macOS width); on a PE/Windows target the conforming C width is 16-bit. Priority: **normal** (trigger-gated — SQLite is UTF-8/`char` internally and uses no `wchar_t`, so the i32 default is correct for the Linux+macOS deliverables and no current corpus exercises it; first noted p18 Cluster G). | Make `wchar_t`'s width per-target (2 bytes on PE/Windows, 4 on ELF/Mach-O) via the SAME per-target width resolution as [[D-FFI-STDINT-PTR-WIDTH-ILP32]] (a `dataModel`/format-keyed typedef value the semantic phase resolves against the active target — NOT an `if (format == pe)` in the engine). Pin: a Windows-target program sees `sizeof(wchar_t)==2`, a Linux-target one sees `4`. | cited in `src/dss-config/shippedLibs/stddef.json` ($comment); pairs with `D-FFI-STDINT-PTR-WIDTH-ILP32` + `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH`; SQLite-readiness Cluster G (p18) |
| `D-FFI-OFFSETOF-MACRO` | A program references `offsetof(type, member)` from `<stddef.h>`. Today `stddef.json` ships `size_t`/`ptrdiff_t`/`wchar_t`/`NULL` but NOT `offsetof` (a function-like macro — no descriptor surface for it yet), so an `offsetof` reference fails loud. Priority: **normal** (trigger-gated — SQLite uses `offsetof` in a few spots, so this may fire during the SQLite compile; first noted p18 Cluster G). | `offsetof` is the function-like macro `((size_t)&((type*)0)->member)`. The cleanest agnostic home is a `__builtin_offsetof`-style intrinsic the semantic phase recognizes and const-folds to the member's layout byte-offset via the EXISTING FC6 aggregate-layout machinery (exact, no UB null-pointer arithmetic) — preferred over (b) shipping a textual function-like macro in the descriptor (which needs a function-like-macro surface the descriptor schema lacks today). Pin: `offsetof(struct S, b)` const-folds to the correct byte offset for a known-layout struct. | cited in `src/dss-config/shippedLibs/stddef.json` ($comment); pairs with the FC6 aggregate-layout offset computation + the descriptor `typedefs`/`constants` mechanism; SQLite-readiness Cluster G (p18) |
| `D-OPT-VARIADIC-RELEASE-MISCOMPILE` | ✅ **CLOSED 2026-06-25 (p18 Cluster G c2 — ROOT CAUSE = INLINING a variadic callee).** A variadic function SILENTLY MISCOMPILED under the RELEASE optimizer pipeline. Minimal repro (`examples`-shaped): `static int vsum(int n, ...){ va_list ap; int acc=0,i; va_start(ap,n); for(i=0;i<n;i=i+1) acc=acc+va_arg(ap,int); va_end(ap); return acc; } int main(void){ return vsum(3,10,13,19); }` → **baseline (debug pipeline) exit 42 (CORRECT); release (`--config=release`) exit 0 (WRONG)** on x86_64-PE (reproduced HEAD 0e3a550, `build/bin/dss/Release`). **Priority: HIGH — correctness-critical, silent miscompile.** BLOCKS SQLite-green RELEASE: SQLite is variadic-heavy (`sqlite3_mprintf`/`sqlite3_snprintf`/`sqlite3VXPrintf`/its test harnesses). ROOT of the blind spot: FC12 shipped the variadic feature BASELINE-ONLY (its corpus carried no `{"shippedPipeline":"release"}` arm), so the optimizer×`va_list` composition was NEVER witnessed — exactly the masked-effectiveness trap the dss-cycle `release`-arm corpus rule exists to prevent. | ✅ (CLOSED) ROOT CAUSE = the INLINER inlined the static single-call variadic callee into `main`; the spliced `va_start` leaves (`VaRegSaveAreaAddr`/`VaOverflowArgAreaAddr`/`VaHomeArgAreaAddr`) bound to MAIN's frame (which never spilled the varargs) → `ap` read garbage. FIX = `inlineLegalityGate` (`src/opt/passes/inlining.cpp`) refuses any callee whose body contains one of those three va_start leaves — the universal "a variadic fn that calls va_start is not inlinable" rule; agnostic (MIR opcodes only), sibling of the `RecvByValueStackParam` refusal. PINS (both red-on-disable): `tests/opt/test_inlining.cpp::VariadicCalleeCallingVaStartIsNotInlined` (callsInlined==0) + corpus `examples/c-subset/varargs_inline_release` (release arm exit 42, PE-runnable). ★ Closing this surfaced 2 MORE variadic-release optimizer bugs the FC12 BASELINE-ONLY corpus masked (a CLUSTER, NOT a single bug) → [[D-OPT-VARIADIC-RELEASE-MEM2REG-PHI]] + [[D-OPT-VARIADIC-RELEASE-ARGINDEX]]; the broad "add a `release` arm to EVERY variadic example" closure follows each fix (this cycle adds only `varargs_inline_release` — the other arms stay off until their bug lands). | CLOSED 2026-06-25 (p18 c2); cited in `src/opt/passes/inlining.cpp` (gate refusal) + `tests/opt/test_inlining.cpp` + `examples/c-subset/varargs_inline_release/`; pairs with `D-OPT7-INLINE-FRAME-SENSITIVE-INTRINSIC` (the hypothetical-intrinsic sibling — va_start in DSS is these dedicated MIR leaves, not an IntrinsicCall) + `D-FFI-DESCRIPTOR-VARIADIC-SIGNATURE` + the FC12 variadic cycle + the dss-cycle `release`-arm corpus discipline |
| `D-OPT-VARIADIC-RELEASE-MEM2REG-PHI` | ✅ **CLOSED 2026-06-25 (p18 Cluster G c3) — was the variadic SYMPTOM of a GENERAL Mem2Reg bug; fixed together as [[D-OPT-MEM2REG-LOOP-BODY-LOCAL-DEAD-PHI]].** Compiling `examples/c-subset/varargs_win64_sum` under RELEASE aborted FAIL-LOUD `Mem2Reg fatal: phi ... has no reaching value from predecessor`. ★ The c3 PROBE OVERTURNED the scope: it is NOT Win64/variadic-specific — the trigger is the `int iv = va_arg(ap,int); double dv = va_arg(ap,double);` LOOP-BODY-LOCALS, and a minimal NON-variadic `int f(int n){int t=0,i=0;while(i<n){int iv=i*2;t=t+iv;i=i+1;}return t;}` reproduces the IDENTICAL fatal. The variadic example was just the FIRST to exercise it under release (FC12 shipped the variadic corpus baseline-only). | ✅ (CLOSED) FIXED by [[D-OPT-MEM2REG-LOOP-BODY-LOCAL-DEAD-PHI]] (semi-pruned SSA — see that row for the mechanism). `varargs_win64_sum` gained a `{"shippedPipeline":"release"}` arm (exit 39, was Mem2Reg-fatal); the general witness is `examples/c-subset/loop_body_local`. | CLOSED 2026-06-25 (p18 c3); subsumed by [[D-OPT-MEM2REG-LOOP-BODY-LOCAL-DEAD-PHI]]; pairs with [[D-OPT-VARIADIC-RELEASE-MISCOMPILE]] (the inlining sibling) + [[D-OPT-VARIADIC-RELEASE-ARGINDEX]] (the still-open 3rd cluster member) |
| `D-OPT-MEM2REG-DEAD-PHI-PRUNE` | ✅ **CLOSED 2026-06-25 (p18 Cluster G c3 — the COMPLETE fix; HIGH, GENERAL Mem2Reg correctness bug).** Under the RELEASE pipeline Mem2Reg ABORTED FAIL-LOUD (`phi at block ... has no reaching value from predecessor`) whenever a promoted alloca got a DEAD phi at an IDF block where it was not LIVE but its incoming was undefined. TWO classes: (1) a loop-BODY-LOCAL (`while(){ int x=expr; use(x); }`, dead at the header) — the first-found symptom [[D-OPT-MEM2REG-LOOP-BODY-LOCAL-DEAD-PHI]]; (2) the SUBTLER NESTED-LOOP case — an inner counter `j` re-initialized each OUTER iteration is LIVE across the inner back-edge yet DEAD at the OUTER header. Class (2) (and a single loop whose body contains an `if`-merge) crashes with ZERO body-locals → an ENORMOUS class of ordinary C (nested loops saturate SQLite). The c3 pr-review's RUNTIME battery caught (2) after the initial semi-pruned commit (ecf40c3) only handled (1) — the green-but-incomplete trap; upgraded to the complete fix the same cycle. | ✅ (CLOSED) FIX = FULLY-PRUNED SSA (Choi/Cytron/Sarkar) in `src/opt/passes/mem2reg.cpp`: place a phi for alloca A at IDF block S ONLY IF A is LIVE-IN at S (a backward live-in dataflow: `live_in[B]=use[B] ∪ (live_out[B]\def[B])`, use=upward-exposed Load, def=Store, alloca-site NOT a def). Strictly stronger than semi-pruned (upward-exposure): a name DEAD at one IDF block but live elsewhere (the nested counter) is pruned THERE specifically. PROVABLY SAFE: every KEPT phi is at a block where A is live → a needed merge is never pruned (no miscompile, verified by a base==release battery: diamond/induction/nested/triple-nested/body-local-live-across all correct on both pipelines). PRESERVES fail-loud: a genuine uninitialized read IS live-in at its use → phi kept → still aborts (verified). PINS (red-on-disable): `tests/opt/test_mem2reg.cpp::{LoopBodyLocalAllocaNeedsNoPhi, NestedLoopInnerCounterNoOuterHeaderPhi}` + corpus `examples/c-subset/{loop_body_local (exit 20), nested_loop_release (exit 4)}` release arms. Agnostic (MIR/SSA only). | CLOSED 2026-06-25 (p18 c3); cited in `src/opt/passes/mem2reg.cpp` (the live-in gate) + `tests/opt/test_mem2reg.cpp` + `examples/c-subset/nested_loop_release/`; subsumes [[D-OPT-MEM2REG-LOOP-BODY-LOCAL-DEAD-PHI]] (class 1); pairs with the Choi/Cytron/Sarkar pruned-SSA literature |
| `D-OPT-MEM2REG-LOOP-BODY-LOCAL-DEAD-PHI` | ✅ **CLOSED 2026-06-25 (p18 c3) — ★ SUPERSEDED by [[D-OPT-MEM2REG-DEAD-PHI-PRUNE]] (the COMPLETE fix).** The SEMI-PRUNED fix this row first described (commit ecf40c3, gate phi placement on UPWARD-EXPOSURE) was INSUFFICIENT: the c3 pr-review's runtime battery found NESTED loops still crashed (a name DEAD at one IDF block but live elsewhere — upward-exposure cannot prune that); upgraded to FULLY-PRUNED (live-in) the SAME cycle. The loop-body-local class IS fixed by the general fix. Original c3 record (the first-found symptom):** GENERAL Mem2Reg correctness bug — HIGH, was masked. Under the RELEASE pipeline, Mem2Reg ABORTED FAIL-LOUD (`phi at block ... has no reaching value from predecessor ... uninit/unreachable input`) on ANY function with a local declared AND used entirely inside a LOOP BODY (`while (...) { int x = expr; use(x); }`) — an enormous, ordinary class of C (it would have blocked vast swaths of SQLite). ROOT: minimal (un-pruned) SSA placed a DEAD phi for the block-local at the loop HEADER (the body-Store's iterated dominance frontier reaches the header via the back-edge); that phi's entry-predecessor incoming was genuinely undefined (the slot was never stored before the loop) → the rename walk's empty-stack guard aborted. Surfaced (re-scoped) from the variadic symptom [[D-OPT-VARIADIC-RELEASE-MEM2REG-PHI]] by the c3 probe (a non-variadic `while(){int iv=...}` reproduces it). Was masked because release-arm corpus examples never declared a loop-body-local (the dss-cycle masked-effectiveness trap, again). | ✅ (CLOSED) FIX = SEMI-PRUNED SSA (Briggs/Cooper) in `src/opt/passes/mem2reg.cpp`: gate phi PLACEMENT on UPWARD-EXPOSURE — only place a phi for an alloca read-before-written in SOME block (a "global" name). A block-local (Store-before-Load in its block) is not upward-exposed → no dead phi. PROVABLY SAFE: a global alloca's placement is BYTE-IDENTICAL to before (induction vars / diamond joins unchanged), so the fix ONLY removes the spurious dead phis. CRUCIALLY PRESERVES fail-loud: a genuine uninitialized read (`int x; if(c) x=1; use(x)`) IS upward-exposed at the use → stays global → still gets its phi → still aborts on the undefined path (verified: that case still fatals). PINS: `tests/opt/test_mem2reg.cpp::LoopBodyLocalAllocaNeedsNoPhi` (induction var=1 phi, body-local=0 phi; red-on-disable = the pass aborts) + corpus `examples/c-subset/loop_body_local` (release arm exit 20, PE-runnable) + the `varargs_win64_sum` release arm (exit 39). Agnostic (MIR-only, no identity branch). | CLOSED 2026-06-25 (p18 c3); cited in `src/opt/passes/mem2reg.cpp` (the semi-pruned gate) + `tests/opt/test_mem2reg.cpp` + `examples/c-subset/loop_body_local/`; pairs with [[D-OPT-VARIADIC-RELEASE-MEM2REG-PHI]] (the variadic symptom it subsumes) + the Cytron-Ferrante / Briggs-Cooper SSA literature |
| `D-OPT-VARIADIC-RELEASE-ARGINDEX` | ✅ **CLOSED 2026-06-25 (p18 Cluster G c4) — ROOT CAUSE = DCE; GENERAL (not variadic), the LAST cluster member.** Compiling `examples/c-subset/varargs_overflow_fixed_stack` under RELEASE aborted FAIL-LOUD `I_ArgIndexOutOfRange: argIndex 6 >= physical-arg count 2`. ★ The c4 probe BISECTED it to **Dce** (without `Dce` it compiles) and re-scoped it GENERAL: `pick(a,b,c,d,e,f,g,...)` uses only `a` + `g`, so DCE elided the 5 unused param `Arg`s → only 2 `Arg`s survived, but the still-live `Arg(6)` (g, the 7th int → SysV stack) kept its physical ordinal 6 → the MirVerifier's FC7 count-based bound (`argIndex < count(Arg)`, valid only while every Arg survives) spuriously rejected it. A NON-variadic `int f(int a..g){return a+g;}` reproduces it identically (any fn with an unused param before a used high-ordinal one). The codegen was always correct (ordinals preserved); only the verifier over-rejected. | ✅ (CLOSED) FIX = treat `Arg` as an ABI-parameter ROOT in DCE (`isSideEffectRoot` in `src/opt/passes/dce.cpp` returns true for `MirOpcode::Arg`): a parameter is part of the fixed ABI signature, never dead code (LLVM models params as non-instruction `Argument` roots) — so DCE never elides an unused param's `Arg`, holding `count(Arg)` == the physical-arg count the verifier bounds against. No codegen cost (an unused param's value is dropped at the LIR tier). Agnostic (MIR-opcode only). PINS (red-on-disable): `tests/opt/test_dce.cpp::UnusedParameterArgIsPreservedAsAbiRoot` (3 Args, middle unused → all 3 survive) + the `varargs_overflow_fixed_stack` `{"shippedPipeline":"release"}` arm (exit 108, was the verifier fatal). | CLOSED 2026-06-25 (p18 c4); cited in `src/opt/passes/dce.cpp` (the Arg root) + `tests/opt/test_dce.cpp` + `examples/c-subset/varargs_overflow_fixed_stack/`; pairs with [[D-OPT-VARIADIC-RELEASE-MISCOMPILE]] + [[D-OPT-MEM2REG-DEAD-PHI-PRUNE]] (the other 2 cluster members) + the FC7 `D-FC7-SYSV-STRUCT-ARG-MULTIREG` verifier bound it interacts with |
| `D-CSUBSET-DESCRIPTOR-TYPEDEF-POINTER-DECL` | ★ **FIRED — discovered 2026-06-25 (p18 SQLite-readiness Cluster G c1, while strengthening the `shipped_int_headers` corpus with a NULL witness).** A DESCRIPTOR-injected typedef name (from a shipped-header JSON descriptor — `int32_t`/`uint8_t`/`size_t`/…) used in POINTER-DECLARATOR position (`int32_t* p` / `size_t *p`) is NOT recognized as a type-name by the PARSER, so `int32_t * p` parses as a MULTIPLICATION expression (`int32_t` × `p`) → fails loud `S_UndeclaredIdentifier` (p) + `S_TypeMismatch` (initializer). ROOT: descriptor typedefs are injected SEMANTICALLY (post-parse, into cuRoot by `semantic_analyzer`), so the parser's `T * x` declaration-vs-multiply disambiguation never learns them. CONTRAST (all verified this cycle): a USER `typedef int myint; myint* p` WORKS (the parser sees the typedef decl → type-name visible); a BUILTIN `int* p` WORKS (keyword); and descriptor typedefs WORK in the UNAMBIGUOUS positions — scalar decl (`int32_t r`) + cast (`(int32_t)n`) — which need no parser type-name table. Only the pointer-declarator ambiguity exposes it. **FAILS LOUD (never a silent miscompile).** Priority: **normal — SQLite blast-radius UNMEASURED** (SQLite mostly uses its OWN amalgamation typedefs [`u8`/`u32`/`sqlite3_int64`, parser-visible → fine], but a direct `size_t *`/`uint8_t *` on a stdint/stddef typedef would mis-parse; re-probe sqlite3.c for descriptor-typedef pointer usage to set the real priority). | Make descriptor typedef-names parser-visible BEFORE the `T * x` disambiguation. Two designs (likely a §B fork): **(a)** pre-scan the source's `#include <...>` set → resolve descriptors → SEED the parser's type-name table before parsing (today includes resolve POST-parse in `import_resolver`, so this needs include-set-at-parse-time — the classic C lexer-hack ordering); **(b)** make the declaration-vs-multiply disambiguation SEMANTIC — speculatively parse both, resolve by whether `T` names a type in the semantic scope (incl. descriptor injections). Pin: `#include <stdint.h>\nint32_t g=42; int main(){ int32_t* p=&g; return (int)*p; }` compiles + exits 42, RED-on-disable. | discovered by p18 Cluster G c1 (`examples/c-subset/shipped_int_headers/main.c` references it in a comment — examples/ is NOT anchor-guard-scanned, so informational); pairs with the FC14 descriptor typedef-injection path (`semantic_analyzer.cpp` inject) + the c-subset declaration-vs-expression speculative parse + `D-PARSE-IDENTIFIER-KIND-HARDCODED-VS-CONFIG`; SQLite-readiness Cluster G (p18); do NOT cite in `src/` yet (discovered-gap pin) |
| `D-PP-DESCRIPTOR-MACRO-INJECT` | ✅ **CLOSED 2026-06-25 (p18 Cluster G c5 — the descriptor `macros` surface; §B Option 1, USER-APPROVED).** SQLite's `#include <assert.h>` needs `assert(e)` as a FUNCTION-LIKE MACRO, but the neutral shipped descriptors carried only symbols/constants/typedefs (typed surfaces injected SEMANTICALLY post-parse) — a macro must reach the PREPROCESSOR before parse. §B fork (descriptor-`macros` surface vs grammar-builtin `assert` vs ship a real `.h` file); the user chose Option 1 (the descriptor surface — agnostic, neutral-config-preserving, generalizes to `offsetof`/other macro headers). | ✅ (CLOSED) Added a `macros` surface to `ShippedLibDescriptor` (`src/ffi/shipped_lib_descriptor.{hpp,cpp}`: `ShippedMacro{name, params?, replacement, variadic}`; `params` ABSENT=object-like / PRESENT=function-like; interner-FREE) + a standalone `readShippedLibMacros(path)` (NO TypeInterner — macros are pure preprocessor token text). INJECTION: `SynthBuilder` (the preprocessor text-splicer) resolves an angle `#include <h>`'s descriptor (`resolveSystemDescriptor`, threaded `systemDirs`) and splices a synthetic `#define name(params) replacement` into the synth buffer BEFORE the include line — so the macro is in the table for the rest of the source AND its replacement tokens are SPAN-VALID in the final buffer (they point into the synthText prefix, like ordinary source — the crux); the include line is KEPT for the post-parse import resolver (typed surfaces). Reuses the proven `handleDefine` machinery (function-like/param/redefinition-idempotent). `assert.json` ships `assert(e)->((void)0)` (the C 7.2 NDEBUG no-op — correct for a release SQLite). AGNOSTIC (config angle token + systemDirs; no language/format branch). PINS (red-on-disable): `tests/ffi/test_shipped_lib_descriptor.cpp` (8 macro loader tests — all 3 forms + 3 fail-loud incl. the newline-in-field guard that prevents a spliced-`#define` directive break + interner-free reader + the header-less-leniency pin that guards the ImportResolver regression) + corpus `examples/c-subset/shipped_assert_macro` (FALSE assert → no-op → exit 42; without the injection `assert` is undeclared → compile fails). | CLOSED 2026-06-25 (p18 c5); cited in `src/analysis/preprocess/preprocessor.cpp` (the SynthBuilder splice) + `src/ffi/shipped_lib_descriptor.{hpp,cpp}` + `examples/c-subset/shipped_assert_macro/`; SQLite-readiness Cluster G; pairs with the FC14 typed-descriptor surfaces (the semantic-injection siblings) + the FC15 macro table (`handleDefine`) + `D-FFI-DESCRIPTOR-VARIADIC-SIGNATURE`. NEXT SQLite headers: `errno` (E* = a clean `constants` surface), `time` (`time_t` clean; `struct tm` substrate), `sys/*`+`unistd`+`fcntl`+`dlfcn`+`pthread` (POSIX — likely avoided via a `SQLITE_OS_OTHER`/`SQLITE_THREADSAFE=0` build config rather than shipped). |
| `D-SHIPPED-HEADER-PER-TARGET-AVAILABILITY` | ✅ **CLOSED 2026-06-25 (p18 Cluster G c8 Phase-1 716fb3d/302fa72 + c9 Phase-2; §B user chose the FULL fix).** The shipped-descriptor model served ONE neutral dir to EVERY target — a POSIX `<sys/time.h>` SILENTLY resolved on windows-pe (real MSVC errors C1083) + `__has_include(<sys/time.h>)` LIED `true` there (a wrong-platform silent miscompile, the worst class). FIX = a per-descriptor `availableObjectFormats` SET (which object-formats the header EXISTS on; empty=all=back-compat), the sibling per-format axis to `library`, keyed by the SAME `objectFormatKindFromName` vocabulary. AGNOSTIC (a config set + generic membership test, never an `if(format==…)`). | **Phase-1 (`#include` fail-loud) ✅ DONE:** the descriptor `availableObjectFormats` decode (`shipped_lib_descriptor.{hpp,cpp}` + the shared `decodeShippedAvailability` chokepoint + the interner-free `readShippedLibAvailability` reader) + `F_ShippedHeaderUnavailableForTarget` (0x501D) + the SEMANTIC gate (the front-end is target-AGNOSTIC — the CU is built ONCE + compiled N targets, so the active object-format is only known per-target at semantic time: `analyze()` gets an `std::optional<ObjectFormatKind> activeFormat` [nullopt=back-compat for LSP/tests/direct-API], and the descriptor loop emits the F-code POSITIONED on the carried `#include` span [`ShippedDescriptorRef{path,span,buffer}` threaded import-resolver→CU→semantic] + injects nothing when the active format ∉ a non-empty `availableObjectFormats`). `sys/types.json`+`sys/time.json` now declare `["elf","macho"]`. **Phase-2 (`__has_include` per-target TRUTH) ✅ DONE (c9, 2026-06-25):** `__has_include` is now per-target. `preprocess()` gained `std::optional<ObjectFormatKind> activeFormat` (nullopt=back-compat for LSP/tests), threaded to the `hasIncludeCb` AND the `SynthBuilder` macro-splice (★ MUST-FIX-3 from the plan-lock: `#include <H>` is NOT only checked at semantic time — the splice is the 3rd descriptor consumer, now gated identically so import-resolver / macro-splice / `__has_include` all agree). `UnitBuilder::setActiveFormat`. The DRIVER builds the front-end ONCE PER DISTINCT ObjectFormatKind: `runCusToTargets` takes a `buildCus(kind)` closure + memoizes by kind (single-format compile = ONE build, unchanged; a malformed spec routes to `compileOneTarget` for its D_InvalidTargetSpec/D_SchemaLoadFailed, never dropped). The predicate `ffi::objectFormatInAvailabilitySet` is now the SINGLE funnel for all 3 consumers (the semantic gate refactored onto it too). ★ the plan-lock's feared blast radius was mostly ABSENT (FC2 oracle reuses ppTokens — no re-preprocess; multi-CU LTO is post-buildCuMir; LSP + ~15 test callers untouched via the nullopt default). WITNESS: pe→exit 7 (Windows native, `__has_include` false→#else) + elf compiles the #if-true branch; corpus `has_include_unavailable_pe` (pe RUN, exit 7) + `has_include_available` (elf/macho, spans 2 kinds — exercises the multi-kind build) + 3 predicate unit tests. NOTE SQLite uses `__has_include` 0× → GENERAL-correctness; the user chose the full fix for completeness. | p18 Cluster G c8 Phase-1 (716fb3d/302fa72) + c9 Phase-2; cited in `src/ffi/shipped_lib_descriptor.{hpp,cpp}` + `src/core/types/parse_diagnostic.{hpp,cpp}` + `src/analysis/semantic/semantic_analyzer.cpp` (the gate) + `src/analysis/preprocess/preprocessor.{hpp,cpp}` (c9 `__has_include` + macro-splice) + `src/analysis/compilation_unit/compilation_unit.{hpp,cpp}` (setActiveFormat) + `src/program/program.cpp` (per-format-kind driver); pairs with the `library` per-format map (sibling axis) + `D-LANG-PLATFORM-DEPENDENT-PRIMITIVE-WIDTH` + the FC15c `__has_include` funnel. |
| `D-SHIPPED-SYMBOL-PER-TARGET-AVAILABILITY` | ✅ **DONE 2026-06-26 (p18 Cluster G c15d — the CI-red repair of c15b/c15c).** The SYMBOL-granularity sibling of `D-SHIPPED-HEADER-PER-TARGET-AVAILABILITY`. THE BUG (CI-red, run 28240524858): DSS imports EVERY DECLARED shipped extern (referenced or not — `readelf --dyn-syms` proved the elf errno binary imports BOTH `__errno_location` AND `__error`), so c15b's errno declaring two accessors with DIFFERENT names per format (`__errno_location` glibc / `__error` Darwin) planted an undefined import on each format → `symbol lookup error: undefined symbol: __error` on linux, the mirror on macOS; same for c15c's Linux-only `fdatasync`/`fallocate`/`mremap` declared-but-absent on macho. The c15b "declare both, only the referenced one links" assumption was FALSE and never CI-verified (those runs were concurrency-cancelled). My LOCAL gate missed it because elf/macho corpora are CROSS-COMPILE-ONLY on the Windows host (compile-checked, never RUN) and the constant/symbol use compiles either way — only the runtime DYNAMIC LINK on the native CI leg exposes it. | ✅ FIX: a per-symbol `availableObjectFormats` on `ShippedSymbol` (EMPTY=every format=back-compat), decoded through the SAME `decodeShippedAvailability` chokepoint (read off the per-symbol json) + the `rejectUnknownKeys` allow-list; the SEMANTIC injection loop skips a symbol whose non-empty set excludes `activeFormat` (mirrors the header gate — not injected → not declared → not imported → a reference fails loud as an undefined name). AGNOSTIC: the SAME `ffi::objectFormatInAvailabilitySet` membership predicate, no `if(format==)`. Config: errno `__errno_location`["elf"]/`__error`["macho"]; unistd `fdatasync`/`fallocate`["elf"]; mman `mremap`["elf"]. ★ ALSO closes a verification gap: the elf/macho corpora must be RUN (not just cross-compiled) before claiming green — done via WSL for elf + the native CI legs for macho. | p18 Cluster G c15d; cited in `src/ffi/shipped_lib_descriptor.{hpp,cpp}` + `src/analysis/semantic/semantic_analyzer.cpp` (the injection gate); config `src/dss-config/shippedLibs/{errno,unistd,sys/mman}.json`; the symbol-granularity sibling of `D-SHIPPED-HEADER-PER-TARGET-AVAILABILITY`. |
| `D-OPT7-INLINE-LEGALITY-GATE` | OPT7 cycle 1 (2026-06-05): the §2.9 inline-legality gate is LIVE in `src/opt/passes/inlining.cpp` (`inlineLegalityGate` — refuses Weak/recursion/address-escape/IntrinsicCall-non-leaf/out-of-range-arity; the Weak refusal is the correctness rule the `D-OPT7-WEAK-INLINE-NEGATIVE-PIN` corpus proves). **Cycle 27 (2026-06-05) CLOSED the conservative recursion arm + the regular-`Call` non-leaf arm:** (1) the gate now LIFTS the non-leaf refusal for a callee whose body contains a regular `Call` — admitted, since the splice's generic arm clones an inner Call correctly (operands remapped via the `local` map; the inner Call's `GlobalAddr` re-emits the SAME symbol → same callee), pinned GREEN by `tests/opt/test_inlining.cpp::NonLeafCalleeIsInlined` + `MultiBlockNonLeafCalleeIsInlined` (both RED-on-disable when the line-209 `Call`-leaf refusal is re-added) + the runtime corpus `examples/c-subset/non_leaf_inline/` (`twice`/`helper`/`main`, `["Inlining"]` arm == baseline 41). (2) The recursion refusal is now SCC-based: a NEW agnostic substrate `src/opt/analysis/call_graph_scc.{hpp,cpp}` (`computeCallGraphSccs(Mir const&)` → `funcId.v → sccId`, iterative Tarjan over the self-contained direct call graph) lets the gate refuse ALL recursive-cycle inlining via `funcToScc.at(caller.v) == funcToScc.at(callee.v)` — which SUBSUMES self-recursion (a self-call is the singleton-SCC case, caller==callee → same id) AND generalizes to MUTUAL recursion (`f→g→f` share a multi-member SCC). Pinned by `tests/opt/test_call_graph_scc.cpp` (3-cycle / acyclic-chain / self-edge / mixed, RED-on-disable) + `tests/opt/test_inlining.cpp::MutualRecursiveCallIsNotInlined` (mutual f↔g refused + an ACYCLIC non-leaf A→B→C control that DOES inline; RED-on-disable: disabling the SCC refusal → `callsInlined` jumps to 2) + the retained `SelfRecursiveCallIsNotInlined` (now ALSO RED-on-disable via the SCC path, proving subsumption). The anchor stays OPEN for the STILL-DEFERRED relaxations: **(a) depth-bounded RECURSIVE inlining** — allow BOUNDED recursion (a depth/unroll policy), ADDITIVE on top of the SCC refusal; each bounded form needs its OWN miscompile pin (a wrongly-unrolled recursion = miscompile). **(b) the SOPHISTICATED cost model — PARTIALLY CLOSED cycle 28:** the conservative SIZE/cost bloat bound LANDED — `OptPipeline.inlineThreshold` (default 50, max `kMaxInlineThreshold`, config-driven via the pipeline JSON's optional `inlineThreshold`, loader-validated `[1, kMaxInlineThreshold]` → `X_PipelineMalformed`, threaded `runInlining(...,inlineThreshold)` → `inlineLegalityGate`) so the gate now REFUSES a callee whose instruction-count (summed across all blocks during the body scan) EXCEEDS the threshold (`if (instCount > inlineThreshold) return std::nullopt;` — `>` not `>=`, so an at-threshold callee inlines), and **`Inlining` now SHIPS in `release.pipeline.json`** (at index 1, after Identity, before ConstFold, with `inlineThreshold: 50`) bounded by that size cap. Pinned GREEN by `tests/opt/test_inlining.cpp::{InlineCostModelRefusesLargeCallee, InlineCostModelBoundaryIsExclusiveUpper, InlineCostModelZeroThresholdRefusesAll}` (a real >threshold Add-chain callee refused while a small one inlines; the EXACT-threshold/over-by-one boundary; the threshold-0 fail-safe refusing all — all RED-on-disable when the cost gate is removed, and the boundary arm RED if `>` is flipped to `>=`) + `tests/opt/test_pipeline_loader.cpp::{InlineThresholdZeroRejects, InlineThresholdOverflowRejects, InlineThresholdNonIntegerRejects, InlineThresholdDefaultsWhenAbsent, InlineThresholdInRange, ShippedReleaseLoadsAllPasses (9-pass exact list + index pin), ShippedReleaseContainsInlining}`. What STAYS deferred under (b): the SOPHISTICATED cost model (call-site hotness / inlining-savings estimation / growth-vs-benefit), NOT the conservative size heuristic. **BLOAT BOUND for the shipped inlining (today):** bounded by (i) the size threshold `inlineThreshold` (default 50) — no callee over the cap inlines, AND (ii) the optimizer's `maxIterations` fixed-point cap — the inliner is one-level-per-run (reads the immutable `mir`, writes a fresh builder), so a deep acyclic chain flattens at most `maxIterations` levels per `optimize` call and the loop terminates. (The pre-cycle-28 bound "(ii) `Inlining` is NOT in release.pipeline.json" is NO LONGER TRUE — inlining ships, and the SIZE threshold is now the load-bearing bloat bound in its place.) **(c)** tightening the conservative address-escape refusal once an escape-analysis substrate can prove a taken address never reaches an indirect call. **(d) IntrinsicCall inlining — ✅ CLOSED cycle 29 (2026-06-05):** the gate's `IntrinsicCall` refusal was REMOVED — a callee whose body contains an `IntrinsicCall` (single-block OR multi-block) is now ADMITTED. It clones SSA-correctly via the splice's generic arm: the intrinsic id lives in the inst PAYLOAD (a module-stable integer the clone copies verbatim) and its operands (the args) remap through the `local` map — no IntrinsicCall-specific field the generic arm drops. Pinned GREEN by `tests/opt/test_inlining.cpp::{IntrinsicCalleeIsInlined (value-threaded single-block — main's Return reads the spliced intrinsic), MultiBlockIntrinsicCalleeIsInlined (intrinsic in a non-entry block, multi-block clone path), VoidIntrinsicCalleeIsInlined (result-unused / void splice path)}` — all RED-on-disable (re-adding the `IntrinsicCall` refusal → `callsInlined`→0, the Call survives; demonstrated). The FRAME-SENSITIVITY hazard (a `va_start`/`frameaddress`/`setjmp`-class intrinsic must NOT be inlined — it would bind to the caller's frame) is split out to the trigger-gated `D-OPT7-INLINE-FRAME-SENSITIVE-INTRINSIC`: no shipped frontend emits ANY intrinsic today (the intrinsic registry has no inline-safety attribute and is empty through every real compile — pinned by the c-subset lowering tripwire `tests/hir/test_hir_lowering_c_subset.cpp::NoShippedConstructLowersToIntrinsic`), so blanket admission is correct for the current intrinsic model. Priority: **normal** (the shipped gate is sound — every relaxation only ADMITS more inlines behind a proof; nothing miscompiles today). | The conservative SIZE/cost bound (b) is CLOSED (cycle 28 — `inlineThreshold`, shipped in release); the IntrinsicCall arm (d) is CLOSED (cycle 29 — blanket admission + the trigger-gated frame-sensitivity anchor). REMAINING: build the SOPHISTICATED cost model on top of the size heuristic (call-site hotness / inlining-savings / growth-vs-benefit) (b'); extend the recursion arm from "refuse all cycles" to a depth-bounded policy (a) — each bounded form gets its own negative pin; thread an escape-analysis result so a non-escaping-but-address-taken callee can still inline its direct calls (c). Each relaxation needs its own negative pin (a wrongly-admitted inline = miscompile). | plan 22 §2.9 OPT7; cited in `src/opt/passes/inlining.cpp` (gate) + `src/opt/optimizer.hpp` (`inlineThreshold` / `kDefaultInlineThreshold` / `kMaxInlineThreshold`, cycle 28) + `src/dss-config/pipelines/release.pipeline.json` (shipped, cycle 28) + `src/opt/analysis/call_graph_scc.{hpp,cpp}` (SCC recursion substrate, cycle 27) + `parse_diagnostic.hpp` (`X_InlineMalformedCallSite`); pairs with `D-OPT7-MULTIBLOCK-SPLICE` + `D-OPT7-MULTIBLOCK-SPLICE-PHI` + `D-OPT7-INLINE-FRAME-SENSITIVE-INTRINSIC` (the intrinsic frame-sensitivity follow-on) + `D-OPT7-1` (cross-CU) |
| `D-OPT7-INLINE-FRAME-SENSITIVE-INTRINSIC` | The first FRAME-SENSITIVE intrinsic is registered/emitted by a frontend — a `va_start`/`va_arg`, `frameaddress`/`returnaddress`, `stacksave`/`stackrestore`, or `setjmp`-class intrinsic whose semantics depend on the ENCLOSING function's stack frame. Detected LOUDLY by the c-subset lowering tripwire `tests/hir/test_hir_lowering_c_subset.cpp::NoShippedConstructLowersToIntrinsic`, which goes RED the moment ANY frontend lowers to an intrinsic (the HIR intrinsic registry becomes non-empty through a real compile). Priority: **trigger-gated** (do NOT build until a frame-sensitive intrinsic exists — today NO shipped frontend emits ANY intrinsic, and the only known intrinsics [`sqrt`, `textureSample`] are pure/frame-insensitive, so the MIR inliner's cycle-29 blanket IntrinsicCall admission is correct; building an inline-safety attribute now would be a consumer-less SPECULATIVE mechanism — the §E#5 over-engineering violation). | OPT7 cycle 29 (2026-06-05) made `inlineLegalityGate` blanket-ADMIT IntrinsicCall-bearing callees (`D-OPT7-INLINE-LEGALITY-GATE` arm d). That is SSA-correct for every frame-INSENSITIVE intrinsic but would MISCOMPILE a frame-sensitive one: an inlined `va_start`/`frameaddress` binds to the CALLER's frame, not the callee's. The MIR inliner sees only the bare intrinsic id (no semantics), so it cannot distinguish safe from unsafe. Closing work = add a per-intrinsic INLINE-SAFETY attribute to `HirIntrinsicDescriptor` (`src/hir/hir_intrinsic_registry.hpp` — e.g. `frameSensitive`/`inlineSafe`, registration-driven, default inline-safe), thread it into the MIR `IntrinsicCall` (a side-table or inst flag carried from `hir_to_mir` lowering since the MIR id is bare), and gate the inliner to REFUSE a callee containing a frame-sensitive IntrinsicCall. Pin: a frame-sensitive intrinsic in a callee → NOT inlined (`callsInlined==0`), RED-on-disable; the tripwire updated to register the new intrinsic + assert the gate refuses it. | plan 22 §2.9 OPT7; cited in `src/opt/passes/inlining.cpp` (gate comments at the IntrinsicCall admission + rule 5) + `src/opt/passes/inlining.hpp` (header doc rule 5); the fail-loud tripwire is `tests/hir/test_hir_lowering_c_subset.cpp::NoShippedConstructLowersToIntrinsic`; follows on from `D-OPT7-INLINE-LEGALITY-GATE` arm (d) |
| `D-MERGE-CROSSCU-INTRINSIC-ID-NAMESPACE` | A frontend emits `IntrinsicCall` AND a multi-CU program is merged (`mergeCuMirs`) where two CUs have INDEPENDENT HIR intrinsic registries that minted the SAME bare id for DIFFERENT intrinsics. Detected by the same `NoShippedConstructLowersToIntrinsic` tripwire (no frontend emits intrinsics today → unreachable). Priority: **trigger-gated** (a PRE-EXISTING latent in the c25 merge, NOT introduced by cycle 29's inliner relaxation; unreachable today — surfaced during the cycle-29 design analysis). | `mergeCuMirs` (`src/mir/merge/mir_merge.cpp`) clones an `IntrinsicCall` payload (the bare intrinsic id) VERBATIM across CUs — it remaps TypeIds (`reinternType`) + SymbolIds but NOT intrinsic ids, which are per-HIR-registry indices. Two CUs whose registries independently minted id=1 for different intrinsics (e.g. CU0 `sqrt`, CU1 `textureSample`) would COLLIDE in the merged module. Closing work = unify the intrinsic id space at merge (re-key each CU's `IntrinsicCall` payloads through a merged intrinsic registry, analogous to `reinternType` for TypeIds) OR carry intrinsics by name through the merge. Pin: a 2-CU merge where each CU mints a distinct intrinsic at the same id → the merged `IntrinsicCall`s keep distinct resolved identities. | plan 22 §2.9 OPT7 cross-CU; `src/mir/merge/mir_merge.cpp` (IntrinsicCall clone); future-work pin (do NOT cite in src); pairs with `D-OPT7-CROSSCU-MIR-MERGE` + `D-OPT7-INLINE-FRAME-SENSITIVE-INTRINSIC` |
| `D-OPT7-MULTIBLOCK-SPLICE` | ✅ **CLOSED 2026-06-05** (OPT7 cycle 2). The inliner now splices a GENERAL MULTI-BLOCK LEAF callee (cycle 1 was single-block-only). `inlineLegalityGate` LIFTED the `funcBlockCount(callee) != 1` refusal but keeps the leaf scan across ALL callee blocks (no `Call`/`IntrinsicCall` anywhere). Because the `tryRewrite` hook (1 Call → 1 value, mid-block-fill) structurally cannot create blocks, a function with any multi-block target is rebuilt by a new `MultiBlockInliner` (`src/opt/passes/inlining.cpp`): per original caller block kept as a branch target (`blockMap_`); the call-site block SPLIT at the Call (a fresh CONTINUATION block holds the after-Call insts); the callee CFG CLONED in RPO (fresh block+inst ids, shared callee→caller `local` map); each cloned `Return` rewritten to `Br(continuation)` collecting (value, cloned-pred) edges; a RETURN-MERGE PHI in the continuation joins them (elided to the single value for a 1-return callee; void callee → no Phi). StructCfMarker RE-DERIVED to `Linear` for every cloned + continuation block (SimplifyCfg cycle-9 precedent) — a clone of the callee's `EntryBlock`/`ExitBlock` would violate the verifier's entry/exit rules, and transplanting If/Loop marker COUNTS would break caller marker-parity; `Linear` is parity-neutral. Caller `Phi` incomings redirect `pred` through `blockExitMap_` (the last continuation of a split chain). **Proven**: runtime corpus `examples/c-subset/multiblock_inline/` (`pick(x){if(x>0)return 7;return 9;}`; `main` = `pick(1)+pick(0)` = 16) with an `optimizedPipelines:["Inlining"]` arm == baseline 16; **REGRESSION-PROVEN RED** — mis-wiring the merge to always pick the first return edge makes the optimized arm fail to compile (the then-clone value doesn't dominate the else→continuation path → MirVerifier rejects via verify-after-every-pass → differential ASSERT fires). MIR-tier pins in `tests/opt/test_inlining.cpp`: `MultiBlockLeafCalleeIsInlined` (Call gone + caller block count +4 [3 clones + 1 cont] + exactly 1 merge Phi + merge-Phi incomings CORRECTLY value↔pred paired [`instBlock(value)==pred` for the 7-from-then + 9-from-else edges; a swapped incoming → RED, catching a mis-merge the MIR tier alone would miss] + verifier-clean, RED-on-disable) + (at cycle-2 time) `MultiBlockNonLeafCalleeIsNotInlined` (scope boundary — **SUPERSEDED in cycle 27**: that scope boundary was the multi-block leaf restriction, which `D-OPT7-INLINE-LEGALITY-GATE` cycle 27 LIFTED; the test was flipped to `MultiBlockNonLeafCalleeIsInlined`, and a multi-block non-leaf callee now inlines). **Robustness gate (review fold 2026-06-05):** `inlineLegalityGate` ALSO refuses a leaf callee with NO RETURNING PATH (no `Return` in any block; every path ends in `Unreachable` — e.g. `int f(){ while(1){} }`). Without the refusal the splice routes each callee `Return` to a continuation block; a never-returning callee leaves that continuation predecessor-less → the MirVerifier (run after every pass) rejects the module → an otherwise-valid program becomes a build error UNDER inlining. Refusing keeps the Call out-of-line so the program compiles (conservative, never a miscompile). Pinned by `tests/opt/test_inlining.cpp::NonReturningLeafCalleeIsNotInlined` (multi-block entry→`Unreachable` leaf → callsInlined==0, Call survives, module still verifies; RED-on-disable: removing `if (!hasReturn) return nullopt;` admits it → the splice's `mapCallerValue` fail-loud aborts the build). | (closed) | plan 22 §2.9 OPT7; cited in `src/opt/passes/inlining.{hpp,cpp}` + `tests/opt/test_inlining.cpp` + `examples/c-subset/multiblock_inline/`; pairs with `D-OPT7-MULTIBLOCK-SPLICE-PHI` (callee-Phi deferral) + `D-OPT7-INLINE-LEGALITY-GATE` |
| `D-OPT7-MULTIBLOCK-SPLICE-PHI` | ✅ **CLOSED 2026-06-13** (OPT7 callee-Phi cycle — §B design brief signed off, then built to the bar). `inlineLegalityGate`'s callee-`Phi` refusal is DROPPED (TOTAL — no shape gate replaces it); `MultiBlockInliner` now CLONES callee Phis via a DEFERRED-incoming-flush, a sibling of the return-merge `returnEdges` with a DISJOINT map (they compose without interference): a placeholder `dst_.addPhi(instType)` is emitted in the RPO clone loop (recorded into `deferredCalleePhis` + `local`, so later insts referencing the phi resolve), and AFTER the whole clone loop each incoming is flushed — the VALUE remapped via the shared `local` map (`mapCalleeOperand`), the PRED via the `calleeBlockMap` (callee-block→caller-block clone), with a fail-loud `std::abort` on a pred-not-in-clone-map (structurally unreachable — every callee block is pre-created in phase 1). The deferral is LOAD-BEARING for a LOOP-phi: the back-edge incoming VALUE is defined later in RPO (in the latch), so an inline flush would hit `mapCalleeOperand`'s def-before-use abort — flushing after the loop (when `local` is complete) is what makes it resolve. The `emitCalleeInst` Phi arm is KEPT as a defensive fail-loud guard (a Phi is handled in the clone loop and never reaches it; rationale comment corrected). Correctness comes from the remap, NOT from restricting shape — handles any incoming count / multiple phis / Arg-or-instruction incomings / loop back-edges uniformly. AGNOSTIC: purely opcode/map-structural; no language/CPU/format branch (routing keys only on `funcBlockCount != 1`). **Pins** (`tests/opt/test_inlining.cpp`, all RED-on-disable): `CalleePhiJoinAndReturnMergeCoexist` (a callee join-phi AND the inserted return-merge phi coexist + compose via disjoint maps), `LoopPhiCalleeInlinesBackEdgeRemapped` (the back-edge deferral — the ONLY shape that needs it; verifier-clean incl. the `I_LayoutUseBeforeDef` layout rule), `ArgKeyedTernaryPhiCalleeInlinesPaired` (the corpus wiring mirror — `pick(k){return k?7:9;}`), `CalleePhiValueTranspositionIsCaughtOnlyStructurally` (the MirVerifier provably CANNOT catch a value↔pred transposition when both values dominate from a common dominator — both modules verify GREEN; the structural pairing is the only signal), `CalleePhiPredMisremapGoesVerifierRed` (the complementary catchable case — a pred mis-remap DOES go verifier-red via `I_PhiPredNotInCfg`/`I_NotDominated`), and `CalleePhiCommonDominatorArmsPairedThroughInliner` (the design-audit's headline blind-spot, closed THROUGH the real inliner: an Arg-valued ternary `f(int k,int a,int b){return k?a:b;}` called `f(0,7,9)` — after the splice the cloned phi's values are main's consts {7,9} in a COMMON DOMINATOR, so a transposition is verifier-GREEN, making the source-intent pairing [then→7/else→9, navigated via the spliced CondBr since `instBlock(value)==pred` cannot apply] the SOLE red-on-disable guard; mutation-proven RED-on-transposition while `verify()` stayed GREEN). Runtime witness: `examples/c-subset/phi_inline/` (`pick(0)` → exit 9, the `["Inlining"]`-arm corpus). ctest 281→282. Independent review = CLEARS-THE-BAR (no FOLD-NOW); the verifier-blind sole-guard test was folded from the review's NIT-1 (also subsuming NIT-2's Arg-incoming coverage). *(Original status, preserved:)* **🔓 USER-AUTHORIZED 2026-06-13 — the OPT7 hard-stop (`G-406`) is OPENED for this anchor (the user explicitly authorized addressing it: best-long-term + no-workaround + 100% source/target/linker agnostic). It REMAINS a SUPERVISED cycle: per the OPT7 discipline, the closing cycle must open with a §B DESIGN BRIEF for the user's sign-off BEFORE building (the Phi-clone-with-deferred-incoming-flush design needs review). Schedule after the pre-FC5 HIR mini-arc.** OPT7 cycle 2 (2026-06-05): the multi-block splice (`D-OPT7-MULTIBLOCK-SPLICE`) admits a multi-block LEAF callee but `inlineLegalityGate` STILL REFUSES any callee containing a `Phi` in ANY block (`tests`-covered via the leaf+Phi gate). This is a CONSERVATIVE refusal (sound — nothing miscompiles, just a missed inline), but the gap is **FRONTEND-REACHABLE**, NOT a post-optimization edge case: the c-subset frontend emits a Phi-bearing multi-block LEAF callee DIRECTLY, because `hir_to_mir` lowers the ternary `?:` AND the short-circuit `&&` / `||` operators to a MIR `Phi` at the merge — independent of Mem2Reg. So a leaf such as `int f(int x){ return x>0 ? 7 : 9; }` (and likewise any `&&`/`||` returning a value) is a frontend-producible multi-block-leaf-with-Phi that cycle 2 refuses today. (A post-Mem2Reg promoted-slot join is one MORE way to reach a callee Phi, but the ternary/short-circuit frontend path is the common, optimization-independent one.) Trigger = the cycle that admits a callee Phi. Closing work = clone the callee Phi with its incomings REMAPPED through the callee-block→caller-block clone map, deferring the incoming flush to a phase-3 pass (the `MirFunctionRebuilder` phase-3 / `Mem2Reg::finalizePhiIncomings` deferral discipline) so an incoming whose value/pred is emitted later in the clone resolves correctly. Pin with a multi-block leaf callee whose body has a Phi (two stores to one promoted slot joined) → the cloned Phi must keep both incomings + the spliced module must verify; RED-on-disable = a dropped/mis-remapped incoming → I_NotDominated / I_PhiPredNotInCfg. Priority: **normal** (today the gate is SOUND — a callee Phi is conservatively refused, nothing miscompiles; the two-return diamond the cycle ships has no Phi). | Drop the callee-`Phi` refusal in `inlineLegalityGate`; clone callee Phis in `MultiBlockInliner` with a deferred incoming-flush (remap value via the shared `local` map, pred via the callee-block clone map). | plan 22 §2.9 OPT7; cited in `src/opt/passes/inlining.cpp`; pairs with `D-OPT7-MULTIBLOCK-SPLICE` |
| `D-OPT7-CROSSCU-MIR-MERGE` | ✅ **CLOSED 2026-06-05** (cycle 25). The MIR-tier whole-program merge LANDED: built all N CU MIRs, then merged into ONE `Mir` — UNIFIED the per-CU `TypeInterner`s (topological re-intern of `TypeId`s, each interner is `owner`-stamped by `CompilationUnitId`), remapped `MirInstId` arena tags to the merged module, merged the const/symbol/global pools into a unified id space, dropped shadowed-weak functions (via `resolveCrossCuDefs`), and rewired cross-CU `extern` Calls → intra-module DIRECT calls; `MirAliasingMode` = most-conservative (AND of strict flags). The merged module lowers as ONE → the existing single-CU linker path. Built `reinternType` (topological type re-intern, every TypeKind — `type_reintern.{hpp,cpp}`) + `mergeCuMirs` (cross-module clone + TypeId/SymbolId remap + cross-CU call→direct rewire + drop-shadowed-weak + initFunc remap + same-name FFI-import dedup — `mir/merge/mir_merge.{hpp,cpp}`) + driver wiring (N>1 merge-then-lower-once; N==1 byte-identical). **Behavior-preserving**: `cross_cu_call` exit 42 via DIRECT call (thunk-gone pin `CrossCuCallIsDirectNoThunkSlot`: add5 defined-in-merged + stripped-from-externImports + `resolvedCrossCuRefs.empty()`); single-CU byte-identical. Tests: `test_type_reintern` (every TypeKind, structural-equality + idempotence + dedup, RED-on-disable) + `test_mir_merge` (rewire RED-on-swap, multi-block clone+rewire, host re-intern, drop-weak, initFunc remap, FFI dedup, conflict). **The cross-CU INLINING + Weak negative pin remain c26 (`D-OPT7-1`).** | (closed for the merge; cross-CU inlining is `D-OPT7-1`, c26) | plan 22 §2.9 "Cross-CU inlining architecture — §B decision (2026-06-05)"; built on the cycle-24 substrate; the c26 consumer is `D-OPT7-1`; cited in `src/mir/merge/mir_merge.cpp` + `src/core/types/type_lattice/type_reintern.cpp` |
| `D-OPT7-CROSSCU-LTO-SINGLE-OPTIMIZE` | Cycle 26 (2026-06-05, D-OPT7-1): the N>1 whole-program path now optimizes EACH CU's MIR in `buildCuMir` AND optimizes the MERGED module once in `Program::compileOneTarget` (`optimizeModule(merged->mir, ...)`) — a DOUBLE-OPT. This is correctness-neutral (a cross-CU call's per-CU inline is a no-op since the callee is an unresolvable extern per-CU; the merged inline does the work) but does redundant optimization work for multi-CU builds. A true LTO shape would SKIP the per-CU optimize for multi-CU builds and optimize the whole-program merged module ONCE. Not done now because `buildCuMir` is the SHARED build half used by the N==1 path too (which must still optimize per-CU since it never merges), so skipping requires a build-half variant or a "defer optimize" flag threaded through `buildCuMir`. Priority: **normal** (efficiency-only — the redundant per-CU pass changes no output; the merged optimize is the load-bearing one for cross-CU inlining). | Thread a "skip per-CU optimize" signal into `buildCuMir` for the N>1 path (e.g. a `CompileOptions` field or a separate build-half entry) so multi-CU builds optimize ONLY the merged module; pin with a metrics/effectiveness check that the per-CU optimize is skipped while the merged optimize still inlines the cross-CU call. | plan 22 §2.9 OPT7 (D-OPT7-1); cited in `src/program/program.cpp` (the merged-optimize site); pairs with `D-OPT7-CROSSCU-MIR-MERGE` |
| `D-OPT7-CROSSCU-THUNK-RESERVED-FOR-SEPARATE-COMPILATION` | **Separate compilation lands** (compile `.c`→`.o` independently, then link pre-assembled objects that carry no MIR). Once the c25 MIR merge lands, compiled-TOGETHER programs route cross-CU calls through the MIR merge → intra-module DIRECT calls, so the assembled-tier cross-CU merge + thunk-slot path (`mergeModules` N>1, cycles 18–19) goes DORMANT for that path. Per the §B 2026-06-05 "keep linker merge + share resolver" decision it STAYS as the linker's general object-merge capability for future separate compilation, sharing the `resolveCrossCuDefs` resolver (no duplicate logic). NOT a gap — the thunk path is correct + remains live until c25 routes compiled-together CUs around it. Priority: **trigger-gated** (do NOT build/exercise separate-compilation until that capability is a real goal; the reserved code is justified by the linker's object-merge role, not speculative). | When separate compilation is a goal: the assembled-tier merge becomes the live path for linking pre-assembled `.o` modules; add a test exercising the thunk path via separate compilation (today only compiled-together exercises it, which c25 routes around). | plan 22 §2.9 "Cross-CU inlining architecture"; cycles 18–19 thunk substrate (`mergeModules`); pairs with `D-OPT7-CROSSCU-MIR-MERGE` |
| `D-OPT7-WEAK-INLINE-NEGATIVE-PIN` | ✅ **CLOSED 2026-06-05** (OPT7 cycle 1). THE OPT7 correctness deliverable: the §2.9 gate REFUSES to inline a `Weak` callee (a strong def of the same name may replace it at link → inlining the weak body bakes in the wrong one = silent miscompile). **Proven end-to-end**: runtime corpus `examples/c-subset/weak_inline_crosscu/` (2-CU — `cu_a.c` weak `f()→7` + `main` calls `f` intra-CU; `cu_b.c` strong `f()→42`) with an `optimizedPipelines` arm `["Inlining"]` → the gate refuses → linker resolves strong-over-weak → exit 42, matching baseline. **REGRESSION-PROVEN RED**: removing the Weak check makes the inliner bake in `return 7` → optimized arm exit 7 vs baseline 42 → the differential ASSERT fires ("optimized arm 'inlining' produced exit code 7 vs baseline 42"). MIR-tier pins in `tests/opt/test_inlining.cpp` (`WeakCalleeIsNotInlined` RED-on-disable + the non-vacuous `GlobalLeafCalleeIsInlined` + recursion/address-taken/multi-block/non-leaf refusals). Required an EMISSION-tier completion of strong-over-weak in `mergeModules` (drop the shadowed weak body — `Linker.CrossCuStrongShadowsWeakEmitsOnlyStrongBody`, also RED-on-disable). | (closed) | plan 22 §2.9 OPT7; cited in `src/opt/passes/inlining.{hpp,cpp}` + `tests/opt/test_inlining.cpp` + `examples/c-subset/weak_inline_crosscu/`; built on cycle-16 `resolvedGlobalDefs` substrate |
| `D-OPT-RELEASE-PIPELINE-CONTROL-FLOW-COMPOSITION` | **✅ CLOSED 2026-06-13 (D15 cycle C).** Runtime-witnessed exit-42 on x86_64 PE (local) + x86_64 ELF (WSL) + arm64 ELF (qemu) for BOTH probe shapes (self-recursion `rec(5)=42` and while-loop `f(5)=42`) under `--config=release`. RC-A (`I_UnreachableBlock`) fixed by SimplifyCfg post-fold reachability pruning the dead arm in the same rebuild; RC-B (`rewriteOperand` abort) fixed by MultiBlockInliner topological-layout-by-construction, now enforced for EVERY producer by the new `I_LayoutUseBeforeDef` (0xA010) verifier rule. Corpus arms landed: new `release_pipeline_recursion` / `release_pipeline_loop` (`shippedPipeline:"release"`) + `fnptr_callback` `["Inlining"]` arm + release arms on `fnptr_callback` / `fnptr_regalloc_pressure`; chip `task_1ddbc93c` resolved; 267→269 ctest. arm64-Linux re-confirmed post-push by the `ubuntu-24.04-arm` native CI leg. *(Original status, preserved for audit trail:)* **OPEN (created 2026-06-12, FC4 c2 pre-commit audit discovery — PRE-EXISTING, not a c2 regression). Priority: normal (fail-loud; promotable at the next stepper consultation).** The SHIPPED `release.pipeline.json` composition (Identity+Inlining+the 7 passes, maxIterations 4) fails LOUD on control-flow-bearing programs: a minimal self-RECURSIVE function (no fn-ptrs) → `I_UnreachableBlock` (rc=1, no artifact); a plain WHILE-LOOP → `MirFunctionRebuilder` abort citing `D-OPT2-REWRITE-MAP-COMPLETENESS` (rc=3) — **that anchor's promote-trigger has FIRED** (recorded plan-22 §3.1). Attribution audit-verified: the failing delta (Inlining/iteration composition) is untouched by the c2 diff; the same sources pass the 7-pass no-Inlining arms; `recursive_factorial` (the only prior recursive example) declares no optimized arms — NO corpus arm ever composed recursion/loops with Inlining or the shipped release pipeline. Fail-loud throughout, no silent miscompile. Consequence: plan-22 c28's "Inlining SHIPS in release" is true as config but unusable on loop/recursion programs today; `fnptr_callback`'s Inlining arm is GATED here. Chip `task_1ddbc93c` carries the full repro. **SEQUENCING (2026-06-12, plan-00 §0.2 D15 — root-caused + plan-LOCKED, closure scheduled)**: the 2026-06-12 cycle root-caused BOTH failure modes (SimplifyCfg folds a constant CondBr but never re-derives post-fold reachability → the dead arm trips the per-pass verifier; the MultiBlockInliner creates the continuation block BEFORE the cloned callee blocks → non-topological block LAYOUT → the 1-return degenerate-Phi elision produces a layout use-before-def the dominance-only verifier cannot see) and the fix plan passed an independent plan-lock (GO-WITH-FIXES: edge-based `acceptPhiIncoming`, marker repair, `I_LayoutUseBeforeDef` 0xA010 verifier rule, `shippedPipeline` corpus arms, fnptr_callback `["Inlining"]` arm, recursion×Inlining pin). Per the user's §B answer the close runs AFTER the marker-rederivation substrate cycle (`rederiveMarkers(cfg)` — D15 cycle B) which dissolves the plan-lock's marker-repair MUST-FIX; this anchor closes in D15 cycle C. **CYCLE B SHIPPED 2026-06-12** (`deriveStructCfMarkers` + post-dom + verifier equality + the incremental-repair deletions; `D-OPT4-1-NON-LINEAR-MARKER-MERGE` ✅) — **C is NEXT**: the marker-repair question is retired (SimplifyCfg's dead-arm pruning now just re-derives post-rebuild), and cycle B's branch-fold phi-edge fix (`acceptPhiIncoming` edge-level filter + abandoned-(b→notTaken)-edge recording) already landed the fold-time phi hygiene C needs. | Root-cause the FunctionRebuilder/unreachable-block interaction under the Inlining+iteration composition; then add release-pipeline corpus arms (incl. `fnptr_callback` `["Inlining"]` — expected exit-42 once fixed) + a recursion×Inlining unit pin. | plan-22 OPT7 c28 (ship-in-release) + `D-OPT2-REWRITE-MAP-COMPLETENESS` (plan-22 §3.1, trigger FIRED 2026-06-12); `examples/c-subset/fnptr_callback` manifest `$comment`; chip task_1ddbc93c |
| `D-MERGE-UNREACHABLE-BLOCK` | Cycle 25 Stage B (2026-06-05): `mergeCuMirs` (`src/mir/merge/mir_merge.cpp`, `FunctionCloner::clone`) clones each surviving function's REACHABLE CFG — it pre-creates every natural block then fills in RPO from the entry (a valid def-before-use order for SSA). A pre-created block NOT reached by RPO (genuinely unreachable from the entry) is left unfilled, which the `MirBuilder`'s every-block-must-be-filled invariant would abort on at `finish()`; the cloner instead FAILS LOUD first (named abort) with attribution. This is correct for the merge's real inputs because the **c-subset frontend does not emit entry-unreachable blocks** (every block it lowers is reachable from the function entry; DCE, when the active pipeline runs it, additionally removes any dead block), and the hand-built tests are likewise all-reachable — so the unfilled-block case never arises today and the limitation is unobservable. The reachable-only assumption is NOT load-bearing for soundness, though: the **fail-loud abort is the actual backstop** — if a future input ever carried an unreachable block, the merge aborts with attribution rather than emitting a malformed function, so it is never a silent miscompile. Trigger = the first merge input carrying an unreachable block (e.g. a future frontend that emits an entry-unreachable block, or a debug/identity pipeline that runs no DCE on input that happens to contain one). Priority: **normal** (fail-loud today; never a silent miscompile). | Either (a) run DCE / dead-block elimination on each CU's MIR BEFORE the merge so only reachable blocks remain, OR (b) extend `FunctionCloner` to clone unreachable blocks too (fill them after the RPO pass in natural order; their intra-block SSA is self-contained, and cross-block operands into a dead block can't be live, so a second natural-order fill pass over the unfilled set suffices). Pin with a hand-built callee carrying a provably-unreachable block → the merged function preserves it (option b) or the merge runs post-DCE (option a). | plan 22 §2.9 OPT7 (cross-CU MIR merge); cited in `src/mir/merge/mir_merge.cpp`; pairs with `D-OPT7-CROSSCU-MIR-MERGE` |
| `D-CRYPTO-SHA256-HOME-CONVERGENCE` | **Plan-lock Condition 1 (2026-06-08, `D-LK7-ADHOC-CODESIGN-MACHO` cycle).** The ad-hoc codesign cycle homes SHA-256 at **`src/core/crypto/sha256.{hpp,cpp}`** (`dss::crypto`), but Plan 16 §2.1 (`16-codesign-publish-plan`) plans crypto at **`src/codesign/crypto/sha256`** paired with SHA-384 inside a full DER/PKCS7/x509 CS1 substrate. `src/core/crypto` is the DELIBERATE choice (a hash is a core utility; the consumer `macho.cpp` is in `src/link`, so a `src/link`→`src/codesign` dependency would be a layering inversion). Trigger = **Plan 16 CS1 lands** (the CMS/cert substrate). Priority: **trigger-gated** (do NOT pre-build; pin to prevent a silent 2nd SHA-256). | When Plan-16 CS1 builds its CMS substrate it MUST consume `core/crypto/sha256` (adding SHA-384 BESIDE it, or re-homing BOTH with all consumers updated) — never fork a second SHA-256. Update Plan 16 §2.1's file table to reference `src/core/crypto/sha256`. | plan 16 §2.1 (CS1 crypto); `D-LK7-ADHOC-CODESIGN-MACHO` (the consumer that created `core/crypto/sha256`); do NOT cite in `src/` (future-work pin only) |
| `D-FF3-APPLE-ARM64-ABI-DIVERGENCE` | The `apple_arm64` calling convention (`CcApple`, shipped 2026-06-08 with the macOS-ARM64 exec cycle in `arm64.target.json`) carries the SAME general-purpose register vocabulary as AAPCS64 (correct for scalar register-passing), but Apple's ARM64 ABI has TWO documented divergences NOT yet modeled: (1) variadic args are ALWAYS passed on the stack (vs AAPCS64's register-then-stack); (2) a different indirect-result-location rule. Neither is reachable at today's scalar/non-variadic asm tier, so the row equals aapcs64 in its register fields — a SEPARATE named `CcApple` row keeps the ABI identity explicit so the divergences land HERE, never via a silent aapcs64 reuse. Trigger = the first variadic OR struct-return (indirect-result) call lowered under `apple_arm64`. **The struct-return / indirect-result half is ✅ CLOSED 2026-06-16 (FC7 C3):** the x8 sret path is modeled (`apple_arm64` row: `indirectResultRegister=x8`, `aggregateSretViaHiddenArg=false`, the same `aapcs64_hfa` strategy — Apple's struct register-assignment is identical to AAPCS64), exercised by the `struct_byval_aapcs64` corpus on the macos-latest CI leg (Mach-O). The VARIADIC-always-stack half stays OPEN (trigger: a variadic apple_arm64 call). Priority: **trigger-gated (variadic half).** (Distinct from `D-FF3-4` = the Microsoft `CcMSARM64` split — a different cc variant.) | Model the always-stack variadic placement + the indirect-result-location rule in the `CcApple` lowering (the variadic half also gates on `D-FFI-DESCRIPTOR-VARIADIC-SIGNATURE`); pin with an arm64-Apple variadic / struct-return corpus. | plan 11 FF3 (the ABI catalog / CcApple); cited in `src/dss-config/targets/arm64.target.json` (the apple_arm64 cc comment); pairs with `D-FFI-DESCRIPTOR-VARIADIC-SIGNATURE` + `D-FF3-4` |
| `D-LK10-ENTRY-MACHO-STATIC-BUILD-VERSION` | The macOS-ARM64 EBADMACHO fix (2026-06-09, `D-LK10-ENTRY-MACHO-EXIT` cycle) emits `LC_BUILD_VERSION` (the platform load command modern dyld requires on macOS 11+ / Apple Silicon to identify a main executable's platform) ONLY on the **dynamic** Mach-O exec path (`encodeExecDynamic` — the sole path the runnable arm64-darwin corpus uses, and the only one that can synthesize `__LINKEDIT` + codesign). The **static** path (`encodeExec`) FAILS LOUD if a format declares `image.buildVersion`, mirroring its existing codesign rejection (D-LK7-1): a static exec + buildVersion is a legitimate future combination (e.g. an x86_64-darwin static exec wanting a platform LC) but has NO shipped consumer today, so emitting it there would be untested dead code. Not a gap — the dynamic path (the live consumer) emits it correctly, byte-pinned; the static path fails loud rather than silently dropping the field. Priority: **trigger-gated** (first static Mach-O exec format that declares `image.buildVersion`). | Add the LC_BUILD_VERSION emission + ncmds/sizeofcmds accounting to `encodeExec` (the shared `appendBuildVersionCommand` helper is already the single chokepoint), and a static-path byte-pin via a synthetic no-extern darwin-exec format. | `D-LK10-ENTRY-MACHO-EXIT` (the EBADMACHO fix that added LC_BUILD_VERSION); cited in `src/link/format/macho.cpp` (the static-path fail-loud guard); pairs with the parallel static-path codesign rejection (D-LK7-1) |
| `D-LK10-ENTRY-MACHO-SECTIONVA-COMPUTED` | ✅ **CLOSED 2026-06-24 (deferral loop, branch feature/0-0-2-p16).** Both Mach-O exec walkers (`encodeExec` + `encodeExecDynamic`) now FAIL LOUD via a shared chokepoint helper `textSegmentVaMatchesFileOff` (`macho.cpp`) when the schema's __text.virtualAddress-derived offset (`sectionVa - pageZeroSize`) does NOT equal this module's computed `textFileOff = alignUp(headerAndCmds, segmentPageSize)` — the stronger equality the existing congruence guard (`>= pageZeroSize`, `% segmentPageSize`) omits. **FORK resolved (fail-loud over compute-from-textFileOff):** the compute form would need reordering BOTH walkers' layout computation (`headerAndCmds` is computed AFTER `sectionVa`'s first use at lines 859/864) AND speculatively supports a non-existent huge-load-command darwin-exec (§A.2/§D no-speculative-build), whereas fail-loud closes the SILENT inconsistency minimally and matches the existing multi-page-unsupported acknowledgment (macho.cpp ~2057). Byte-identical for the shipped corpus (arm64/x86_64-darwin `headerAndCmds` ~840 ≪ `segmentPageSize` ⇒ `impliedOffset == textFileOff`, check passes silently — 394 ctest green, no corpus change). Pinned RED-on-disable by `MachOExecWriter.SchemaTextVaInconsistentWithTextFileOffFailsLoud` — a synthetic exec format (`loadFromText`, VA = `pageZeroSize + 2×segmentPageSize` = 0x100002000, congruent so validate ACCEPTS it but the textFileOff inequality fires) + trivial module ⇒ encode aborts (`bytes.empty()`); toggle-verified (revert ⇒ test fails). The two walker call sites route through the ONE tested chokepoint helper (§A.5 option-a by-construction coverage; static path end-to-end pinned, dynamic path's identical call verified by independent review). — *Original row (audit trail):* The Mach-O exec walker reads `__text.virtualAddress` from the format JSON and trusts it equals `pageZeroSize + alignUp(headerAndCmds, segmentPageSize)` (i.e. `textSecOffsetInSeg == textFileOff`). For the arm64-darwin corpus this holds because headerAndCmds (~864 bytes) ≪ segmentPageSize (16384), so textFileOff = 0x4000 and the shipped VA is 0x100004000. The walker's congruence guard (`macho.cpp` `(sectionVa - pageZeroSize) % segmentPageSize`) + validate() catch a NON-congruent VA loud, but NEITHER enforces the stronger `== textFileOff` equality (which is module-dependent — headerAndCmds depends on the load commands of the module being linked, not the schema alone). So a future format whose load commands exceed `segmentPageSize` (many dylibs / a huge symtab) would push textFileOff past the hardcoded VA and emit an inconsistent section addr. Strictly BETTER than the pre-16K state (headroom grew from 4096 → 16384 bytes). Priority: **trigger-gated / low** (first darwin-exec whose header+load-commands exceed segmentPageSize). | Compute `sectionVa = pageZeroSize + textFileOff` in the walker (making the schema `virtualAddress` advisory / a cross-check) OR add an encode-time `textSecOffsetInSeg == textFileOff` fail-loud assertion. Pin with a synthetic darwin-exec carrying enough load commands to exceed one segmentPageSize. | `D-LK10-ENTRY-MACHO-EXIT`; `src/link/format/macho.cpp` (the sectionVa derivation); future-work pin (do NOT cite in src) |
| `D-LK7-CODESIGN-PAGESIZE-OVERFLOW-HARDENING` | The ad-hoc codesign `pageSize` is now schema-validated to a power-of-two in **[4096, 65536]** (`object_format_schema_json.cpp` — closes the reachable `pageSize:1` slot-table u32 overflow: `nCodeSlots*32` could wrap on a multi-GiB binary; pinned by `PageSizeBelowFloorFailsLoud`/`AboveCeilingFailsLoud`). One RESIDUAL truly-unreachable overflow remains: `codeSlotCount`'s `(codeLimit + pageSize - 1u)` could wrap in u32 only if `codeLimit` is within `pageSize` of `UINT32_MAX` (a >~4 GiB signed binary) — and `macho.cpp` already fails loud if `codeSigFileOff > u32 max` before the builder runs, AND the `sig.size() != reservation` guard fails loud on any size disagreement, so it is fail-loud-protected + outside this compiler's operating range. Priority: **trigger-gated** (a >~4 GiB output binary reaches the codesign builder). | Widen `codeSlotCount` + `codeDirectoryLength` to compute in `std::uint64_t` with an explicit `> u32 max` fail-loud on the final blob length; OR raise the pageSize floor further. Pin with a synthetic ~4 GiB codeLimit. | `D-LK7-ADHOC-CODESIGN-MACHO` (the builder); cited in `src/link/object_format_schema_json.cpp` (the pageSize range validate) |
| `D-LK7-ADHOC-CODESIGN-MACHO` | ✅ **CLOSED 2026-06-09 — runtime-ACCEPTED on Apple Silicon.** The closure-consistency witness: `D-LK10-ENTRY-MACHO-EXIT` is ✅ CLOSED runtime-confirmed (the `macho_arm64_exit` ad-hoc-signed arm64 binary RUNS on the `macos-latest` leg, CI run 27182845048 #194; the `global_int` + `shipped_include_puts` arm64-darwin corpora likewise execute) — and Apple Silicon's kernel/AMFI SIGKILLs an unsigned or bad-signature arm64 binary, so a green run is direct proof AMFI ACCEPTED DSS's ad-hoc `CodeDirectory`+`SuperBlob`. The runtime acceptance the builder cycle explicitly deferred has thus arrived (via the macho-exit + data-section cycles). Remaining future work stays separately anchored: CMS/cert signing (Plan 16 CS2), `codesign --verify` as an explicit test, the static-path `__LINKEDIT` (`D-LK7-1`). — ORIGINAL 🟡 BUILDER-BYTE-PROVEN status (2026-06-08, increment 2/2 landed) retained below for the audit trail: The ad-hoc CodeDirectory+SuperBlob *builder* is COMPLETE + byte-pinned: structure pinned against Apple `cs_blobs.h` literals (BE magics 0xFADE0CC0/0xFADE0C02, version 0x20400, CS_ADHOC, hashType=2, log2-pageSize, execSegFlags) + page hashes byte-matched against the FIPS-vector-proven `dss::crypto::sha256` (an INDEPENDENT oracle → the recompute is NOT circular), reservation DERIVED (Condition 2, `blob.size()==reservation==datasize` pinned + a fail-loud size-disagreement guard), config-driven + agnostic, 2 red-on-disable levers MANUALLY DEMONSTRATED (BE→LE magic flip; zeroed page hash — out-of-band, not standing pins). **NOT YET PROVEN (the next cycle):** that macOS/Apple-Silicon AMFI ACCEPTS + EXECUTES a binary carrying these bytes — there is no `macho64-arm64-darwin-exec` format JSON, no emitted-and-run signed binary, and `codesign --verify` was deferred (no binary to verify). **Do NOT claim "macOS-verified" / "runs on Apple Silicon" / "AMFI-accepted" this cycle — only the signature BYTES are proven (§A.5: pure-substrate close by byte-pins+FIPS-oracle, NOT a runtime claim).** **§B decision (2026-06-08):** picking up "add Apple Silicon to the dss cycles" (the macOS-ARM64 exec arc, `D-LK10-ENTRY-MACHO-EXIT`) surfaced a hard blocker — **Apple Silicon's kernel SIGKILLs any unsigned arm64 binary**, and DSS only RESERVES placeholder bytes for `LC_CODE_SIGNATURE` (`macho.cpp:1401/1583/1901`; "Plan 16 fills the reserved bytes" = V3-2, unbuilt). So a DSS macho64-arm64-darwin-exec could be byte-pinned but **could not RUN** on `macos-latest` → the §A.5 runtime closure is unreachable (exactly the FFI-ISEL over-claim trap). User chose **AD-HOC CODESIGN FIRST** (a self-contained prerequisite — NO CMS/cert, just a CodeDirectory + cdhash) over a byte-pin-only macOS cycle; macOS-ARM64 exec is the cycle AFTER this. **The locked plan** (from a Step-3 understand): (1) **SHA-256 is ABSENT** in `src/` — add a self-contained FIPS-180-4 `src/core/crypto/sha256.{hpp,cpp}` (`dss::crypto`, ~180 lines, FIPS-vector-tested; NOT the heavyweight Plan-16 BearSSL vendoring — ad-hoc needs only SHA-256). (2) **Big-endian** emit helpers (`byte_emit.hpp` is LE-only today; codesign blob headers are BE — the universal gotcha). (3) A `codeSignature` config block on `MachOImage` (`object_format_schema.{hpp,cpp,_json.cpp}`: `{kind:"adhoc", hashAlgorithm:"sha256", pageSize:4096, identifier}` — closed-enum + power-of-two + non-empty + MH_OBJECT-reject fail-loud). (4) **The fill** in a unit-testable `src/link/format/macho_codesign.{hpp,cpp}` (peer of `macho_chained_fixups`): compute `codeLimit=codeSigFileOff`, `nCodeSlots=ceil(codeLimit/pageSize)`, SHA-256 each page, assemble `CS_CodeDirectory`(magic 0xFADE0C02, version 0x20400 w/ execSeg fields [execSegFlags=CS_EXECSEG_MAIN_BINARY], flags=CS_ADHOC 0x2, hashType=2 SHA-256, pageSize=12) + `CS_SuperBlob`(0xFADE0CC0, 1 index), write into the reserved region. **AGNOSTIC**: all in the `macho.cpp` per-format walker (the Mach-O realization layer, like `elf.cpp`) DRIVEN by the format-JSON `codeSignature` block — ZERO shared-substrate `if(format==macho)`. **Verification**: host-independent byte-pins (`tests/link/test_macho_codesign.cpp` — BE magics, CS_ADHOC, hashType, nCodeSlots, codeLimit, page-hashes recomputed via `dss::crypto::sha256` byte-match the embedded slots [the CD-structure + page-slot hashes are pinned; a distinct cdhash=SHA256(whole-CD) pin is deferred — the kernel recomputes cdhash, so a literal would be layout-brittle]) + FIPS-vector pin for SHA-256 + an OPTIONAL `__APPLE__`-gated `codesign --verify` acceptance test on the macos leg (the system tool accepts our bytes; **hermetic-invariant judgment call — plan 16 §0 forbids shelling to codesign to PRODUCE, but read-only verify-our-own-bytes in a test is different; gate behind `__APPLE__`, byte-pins stand alone if deferred**). Proven on the existing x86_64-darwin DYNAMIC path (signature structure is arch-independent; the dynamic path synthesizes `__LINKEDIT`, the static path does not = D-LK7-1, left deferred). **No circularity** (signature is tail-appended; `codeLimit` is fixed before the signature is built — derive the exact reservation size up-front, replacing the hand-typed `codeSignatureSize`). Priority: **high** (the active prerequisite; runs next cycle with fresh context). DEFERS (pin later): cert-based CMS fill (Plan 16 CS2), SHA-256↔BearSSL convergence, the static-path `__LINKEDIT` (D-LK7-1), the macho64-arm64-darwin-exec format JSON + the runnable Apple-Silicon corpus (the cycle-after). | (closed 2026-06-09 — runtime-accepted) | plan 16 (codesign/publish — the ad-hoc subset pulled forward; CMS/cert = CS2, future); plan 14 LK7 (the LC_CODE_SIGNATURE reservation, now filled + runtime-accepted); `D-LK10-ENTRY-MACHO-EXIT` ✅ (the macOS-ARM64 exec arc this unblocked — the runtime witness); `D-LK6-14` (arm64 chained-fixups, the sibling Apple-Silicon mechanism) |
| `D-MERGE-LOWER-EMPTY-NAME-GUARD` | Cycle 25 Stage C (2026-06-05): the lower-half refactor into the shared `lowerMirModuleToAssembly` body (`src/program/compile_pipeline.cpp`) routes symbol names through a `nameOf(SymbolId)→string` seam. The pre-cycle-25 monolith FAILED LOUD (`K_SymbolUndefined`) on an externally-visible symbol (`bind != Local`) that had a SemanticModel record but an EMPTY name; the refactor's `appendSym` now silently SKIPS any empty-name symbol. A naive restoration of the guard keyed on `nameOf().empty()` is WRONG: `nameOf` returns "" for BOTH "record-with-empty-name" AND "no-record", and a real `extern puts(...)` produces an externally-visible symbol with NO record (→ "" legitimately, MUST be skipped) — so the naive guard regresses real FFI programs (proven: `hello_puts`/`hello_printf`/`printf_int`/`string_literal_arg` → `K_SymbolUndefined`). The guarded state (a record WITH an empty name for an externally-visible symbol) is never produced by the analyzer today, so this is defense-in-depth, NOT a current behavior change (the guard never fired pre-refactor → single-CU byte-identity holds). Priority: **normal**. | Thread a record-EXISTENCE predicate through the shared body (single-CU passes `model.recordFor(sym) != nullptr`; the guard fires on `hasRecord && name.empty() && bind != Local`; the merged path passes a predicate that still skips synthesized externally-visible empty-named symbols). Pin with a (synthetic) externally-visible symbol carrying a record but an empty name → `K_SymbolUndefined`. | cycle-25 review (silent-failure reviewer); `src/program/compile_pipeline.cpp` (`lowerMirModuleToAssembly`); future-work pin (do NOT cite in src) |
| `D-CI-WINDOWS-MSVC-BUILD-TIME-CEILING` | ✅ **CLOSED 2026-06-10 — MSVC PCH landed (option b) + the `windows-msvc-release` CI witness is GREEN.** The PCH push (`1284aea`) completed ALL 5 CI legs SUCCESS (run **27245366191**); the `windows-msvc-release` job ran ~40 min END-TO-END (00:44→01:24 UTC — checkout+configure+FetchContent+Build+ctest), so the Build STEP is comfortably under the 45-min ceiling that the immediately-prior `ddac0de` Part-B push (run 27241095576) **TIMED OUT** at. The ceiling is cleared with margin; if the tree keeps growing, the next lever is the test-side already on shared `REUSE_FROM` + optionally `sccache`. — ORIGINAL §B + mechanism (2026-06-09): chose **(b) Windows build caching via MSVC PCH** over (a) bumping the timeout (the timeout knob lives in the EXTERNAL `DSS.DevOps` reusable workflow, not this repo; PCH is the in-repo durable fix as the tree grows). **Landed this cycle** (`feature/0-0-2-p2`): per-lib `PRIVATE` precompiled headers across the 21 `src/` object libs + ONE shared `REUSE_FROM` PCH for the test executables (`dss_test_pch` — the larger half of the compile) + a **Debug canary** (`CMAKE_DISABLE_PRECOMPILE_HEADERS` in Debug → the linux-clang-asan leg builds PCH-free, proving every TU's includes are self-sufficient so the PCH can never silently mask a missing include). STL-only header set (no project headers → src edits never invalidate the PCH, and it cannot mask a project-header include). Behavior-preserving: **214/214 ctest GREEN on BOTH the PCH Release path AND the PCH-free Debug canary path** (PCH changes how the compiler is built, never what it emits — every byte-pin/runtime test still passes). Local clean Release/Ninja build 61s→56s on a 32-core GCC box; the real leverage is the 2-core MSVC CI runner where the per-TU STL reparse dominates + serializes. Per-lib `PRIVATE` (not a shared `REUSE_FROM`) was chosen for `src/` because the local toolchain is GCC/MinGW → MSVC's stricter `.pch` cross-target reuse-compat is unverifiable pre-push, and per-lib `PRIVATE` is MSVC-bulletproof (the test-side `REUSE_FROM` is safe — its consumers share identical defines AND include dirs). **STAYS OPEN until a `windows-msvc-release` run completes Build comfortably under the 45-min ceiling — §A.5: the push is not the witness, the CI leg is.** — ORIGINAL ⚠️ context retained for the audit trail: The `windows-msvc-release` CI **Build** step is at the **45-minute timeout ceiling** and the margin is gone: the prior cycle's Build took **42m42s**, and the macOS `__TEXT,__const` data-section cycle's run (**27192415923**) had its FIRST `windows-msvc-release` attempt **TIME OUT** the 45-min Build step by a thin margin — it then PASSED on a re-run at **45m31s**. This is a codebase-growth infra issue (compile time scaling with the source tree), NOT a code defect — local Win-MSVC is **212/212**, and the other 4 legs of run 27192415923 (linux-gcc-release, linux-clang-asan, linux-arm64-gcc-release, macos-clang-release) were GREEN. Left here so the `/dss-cycle` loop SURFACES it at Step 0 rather than treating a future timeout as a regression. Priority: **normal (user/infra decision).** | **✅ (b) MSVC PCH landed 2026-06-09 — closure now gated SOLELY on the `windows-msvc-release` CI Build-time witness.** Original options (for the audit trail): Pick one (a user/infra call): (a) **raise the CI Build step timeout 45 → 60 min** (the cheap immediate unblock); OR (b) **add Windows build caching** — MSVC PCH (precompiled headers) and/or `ccache`/`sccache` — to cut the cold-build time structurally (the durable fix as the tree keeps growing). Do NOT mark closed until one lands AND a `windows-msvc-release` run completes Build comfortably under the (new) ceiling. | CI workflow (`.github/workflows/*` Build-step timeout); surfaced by run 27192415923 (first Windows attempt timed out, re-run 45m31s); pairs with the §0 status note in plan-00; future-work pin (do NOT cite in src) |
| `D-PARSE-RECOVERY-MULTI-LEVEL-UNWIND` | The 2026-06-10 missing-initializer recovery fix (`synthesizeMissingRule`, `src/analysis/syntactic/parser.cpp`) synthesizes ONE absent required rule and advances the PARENT cursor ONE slot, so the enclosing frame consumes the stop-point — perfect for `int x = ;` (the missing `initValue` is one level below `varDeclHead`, which then eats the `;` + the block scope-closes its `}`). It does NOT unwind MULTIPLE frame levels to the resync owner: a missing production whose enclosing context spans several rule frames (e.g. tsql `SELECT FROM Users;` — the absent select-list sits under a multi-level select-statement) still gets a clean POSITIONED diagnostic at the error (an improvement) but leaves a RESIDUAL EOF `P_MissingRequiredChild` because the outer select-statement frames don't fully resync (pre-existing — `broken_select.sql.diag`'s EOF cascade predates this fix and only shifted LATERALLY this cycle, `P_NoAlternativeMatched`→`P_UnexpectedToken` at the `;`). NOT a regression; a recovery-COMPLETENESS gap. Priority: **low/normal** (positioned diagnostics already land; only the residual outer-frame cascade remains). | Generalize recovery to unwind the parser-frame stack down to the frame whose FOLLOW/sync OWNS the resync token (the "frame-aware panic" the plan-lock sketched), so ALL abandoned inner frames close cleanly + scope-balanced in one recovery event. Pin with a multi-level synthetic grammar + the tsql `broken_select` golden shedding its residual EOF `P_MissingRequiredChild`. | 2026-06-10 parser-recovery fix (`synthesizeMissingRule` in `src/analysis/syntactic/parser.cpp`); closes the single-level case of the V2-4 Part-B `task_5647346f` deficiency; future-work pin (do NOT cite in src) |
| `D-PARSE-INFIX-ASSOCIATIVITY-STRUCTURAL` | ✅ **CLOSED 2026-06-12 (created + closed same cycle — the audit-trail row for a SILENT-MISCOMPILE class).** Same-precedence infix chains parsed RIGHT-associative in every Pratt-walked grammar (c-subset + toy; tsql IMMUNE — its `flatBinaryOpRule` path already left-folded in `lowerFlatExpr`; that path is table-blind for PRECEDENCE too, a sibling latent class pinned as `D-HR10-FLAT-EXPR-TABLE-BLIND`, trigger-gated): `10 - 3 + 1` → 6 (correct 8), `a/b/c` (100,5,2) → 50 (correct 10), the dss-state `ptr_swap_through` probe's `return x - y + 4;` → 34 (correct 42) — target-independent (PE + ELF-x86_64 + ELF-arm64 all wrong identically). ROOT CAUSE: `DefaultPrattWalker::parseExpressionAt` parsed the infix RHS at `op.precedence` unconditionally (right-recursive trees) while its class-doc claimed "a downstream semantic pass reads associativity from the operator table" — **no such consumer ever existed**; the declared `associativity` was a knob-that-lies, and every tree consumer (semantic const-eval, HIR combineBinary, MIR ConstFold) inherited the wrong grouping. Survived 263 green tests because NO corpus example contained a result-changing same-prec chain. FIX (engine, table-driven, zero identity): infix + ternary arms switched to **wrap-in-place** (`wrapLastChildExprFrame` — the mechanism the postfix arm already used) with **RHS minPrec = (assoc==Right ? prec : prec+1)** (Left AND None → structurally left); the climb-level WalkerSnapshot rollback-replay machinery DELETED (caller-less; diag-snapshot + sketch-truncate equivalence argument recorded at the site); **trivia hold-then-place** at the climb top (held until the wrap decision, placed inside the wrapper before the operator) — which ALSO fixed the pre-existing spaced-postfix bug (`f (40)` previously wrapped WHITESPACE → internal H0001, red-on-baseline demonstrated). Left chains now build ITERATIVELY (a 2000-op chain parses clean; previously the depth guard aborted). Pins: exact-tree-shape tests c-subset + synthetic (left/right/None-omitted/ternary-chain/postfix-mix/mixed-prec/spaced/comment-trivia/2000-op) with red-on-disable DEMONSTRATED (rhsMin reverted → right-nested shapes captured → restored hash-identical); semantic const-eval pin (`int g = 10-3+1;` == 8); MIR ConstFold pin; corpus `ptr_swap_through` + `assoc_chains` (LEFT→42, old-RIGHT→26/34 documented in manifests; one deliberately SPACED call) — exit-42 witnessed PE + WSL x86_64-ELF + qemu arm64-ELF; Mach-O = macos leg. dss-state battery: MISCOMPILES section EMPTY, `--strict` exit 0. 265/265. | (closed in the same cycle that created it — recorded for the audit trail; the closing work IS the row above) | `src/analysis/syntactic/parser.cpp` DefaultPrattWalker; dss-state probe `ptr_swap_through`; plan-05 (walker design history — superseded note added); plan-00 §0.2 D15 (the cycle-sequencing decision) |
| `D-HR10-FLAT-EXPR-TABLE-BLIND` | **OPEN (created 2026-06-12, the associativity-fix audit). Priority: trigger-gated.** The HR10 FLAT expression path (`lowerFlatExpr`, `src/hir/lowering/cst_to_hir.cpp` — grammars whose expression rule is a flat `operand (binaryOp operand)*` sequence instead of a Pratt `expr` shape, e.g. tsql-subset) left-folds `acc = combineBinary(op, acc, next)` and consults the declared operator table NOT AT ALL — **precedence included**: tsql declares precedence groups 10..40 that nothing consumes, so a flat-path `1 + 2 * 3` would fold `(1+2)*3` (≠ real T-SQL), and a hypothetical right-assoc flat operator would silently mis-group — the same knob-that-lies class the 2026-06-12 associativity fix closed for the Pratt path, surviving in the sibling path. Pre-existing HR10 documented design; no corpus case exercises flat-path mixed precedence; no runtime consumer today (tsql has no codegen-observable arithmetic corpus). Fail-quiet-by-absence, NOT a current miscompile — but a KNOWN latent class now that the Pratt sibling is fixed. | Trigger: the first flat-path grammar with MIXED-precedence operators or a non-left-assoc operator (or tsql gaining a runtime-observable arithmetic corpus). Closing work: make `lowerFlatExpr` precedence-climb over the declared table (or re-route flat grammars through the Pratt walker), with exact-shape pins + a corpus witness; until then the loader could warn on declared-but-unconsumed precedence groups for flat-only rules. | `D-PARSE-INFIX-ASSOCIATIVITY-STRUCTURAL` (the Pratt-path closure whose audit surfaced this); HR10 (plan-09); `src/hir/lowering/cst_to_hir.cpp` `lowerFlatExpr` |
| `D-HIR-SEALED-JOIN-UNREACHABLE-BLOCK` | ✅ **CLOSED 2026-06-13 (MIR unreachable-prune cycle).** Fixed by the mandatory MIR reachability-prune normalize (`D-MIR-UNREACHABLE-PRUNE-NORMALIZE`): hir_to_mir's eagerly-created continuation blocks that orphan when all arms seal are dropped at the `optimizeModule` chokepoint BEFORE any verify, so valid C (`if(c){return 1;}else{return 2;}`, switch-all-return, &&/|| sealed arms, loop-with-returning-body) now compiles AND runs; dss-state `cf_if_else_chain` + `cf_switch` flipped FAIL→PASS; reproducer exit-1 on PE+ELF+arm64; 272/272 ctest. *(Original status, preserved for audit trail:)* **OPEN (created 2026-06-12, the D15 cycle-B plan-lock + implementation both surfaced it — PRE-EXISTING, not introduced). Priority: normal — PROMOTE before plan-23 FC5+ corpus work (it will block any both-arms-return shape, and SQLite is full of them).** hir_to_mir eagerly creates join/continue blocks (the if-join at ~1410; do-while's continueBB) and emits them even when EVERY arm seals (returns/breaks) → the block is UNREACHABLE in the emitted MIR → the production verifier (run at merge, mir_merge.cpp:664) rejects the program with `I_UnreachableBlock`. REAL-CLI evidence: `int main(){ if(c){return 1;} else {return 2;} }` FAILS TODAY (error[I_UnreachableBlock] mir block #4); the dss-state battery probes `cf_if_else_chain` + `cf_switch` are blocked by exactly this; the `degenerate_loop_break` corpus example deliberately excludes the do-while-break sibling for the same reason (its manifest $comment records it). Fail-loud (a rejection, never a miscompile) — but it rejects VALID C. | Fix at the frontend: drop (or never-emit) eagerly-created blocks that end function lowering with zero predecessors and zero instructions-begun (a finalize sweep in hir_to_mir, or lazy join creation). Pin with: corpus `if_both_arms_return` (exit 42) + a do-while-break example + the `cf_if_else_chain`/`cf_switch` battery probes flipping green + a lowering-tier exact-block-count pin. | hir_to_mir.cpp ~1404-1452 (if), do-while continueBB; dss-state probes cf_if_else_chain/cf_switch; plan-23 FC5+ (the blocker-to-be); surfaced by the D15 cycle-B plan-lock NIT-2 + the implementer's out-of-scope flag |
| `D-AP2-SOURCES-GLOB` | Plan 06 AP2 (2026-06-09): the project-config `sources[]` are LITERAL paths — no glob expansion. The §2.2 example shows `"src/**/*.c"`; AP2 routes over the literal file COUNT (so a glob string is one "file" today). Trigger: first project config that needs a glob pattern expanded to multiple files. Priority: **normal** (convenience; literal multi-file lists work today). | Add glob expansion in `parseProjectConfig` / a driver pre-pass (resolve `sources[]` patterns against the project dir into a concrete file list) BEFORE the `routesToMultiUnit` count is taken, so an expanded N>1 still routes to `compileUnits`. Pin with a 2-match glob → 2 CUs. | plan 06 §2.2; cited in `src/program/project_config.hpp`; pairs with `D-AP2-MULTIPLE-SOURCES-SEMANTICS` |
| `D-AP2-OUTPUT-ROUTING` | Plan 06 AP2 (2026-06-09): the project-config `output` field is PARSED + type-validated (non-empty string when present) and carried on `ProjectConfig.output`, but its path ROUTING is not wired — `compileProject` delegates to the existing per-target output convention (`Program::setOutputDir` / `<cwd>/target/<format>/<bin>`), ignoring the value. Trigger: first cycle that must route artifacts to the project-declared `output` path. Priority: **normal**. | Decide required-vs-optional (today optional) + the path semantics (single-target file vs multi-target dir, mirroring `setOutputDir`'s convention), then wire `ProjectConfig.output` → the driver's output-dir selection. Pin with a project whose `output` redirects the artifact + assert the file lands there. | plan 06 §2.2/§2.4; cited in `src/program/project_config.{hpp,cpp}`; pairs with `D-CAP-MARKER-MULTI-TARGET-E2E-PIN` |
| `D-AP2-COMPILATION-CONTEXT` | Plan 06 AP2 (2026-06-09): AP2 VALIDATES the artifact profile (fail-loud `D_ArtifactProfileNotSupported`) and delegates the build, but does NOT thread the resolved profile to codegen — there is no path to do so today (`compileFiles`/`compileUnits` → `runCusToTargets` build `CompileOptions` internally with no profile field; adding one would be a dead knob, the plan-lock Contest-2 finding). Plan 06 §2.3.4 stamps the profile onto a **CompilationContext**. **REPOINTED 2026-06-09 (AP3):** AP3 did NOT thread the profile to codegen — a §B + the AP3 plan-lock established that **NO shipped language/profile has a codegen effect the chosen `(target:format)` spec doesn't already encode** (cli→exec-format→trampoline; lib→dll/dyn-format; staticlib→relocatable/archive), so threading-to-codegen now would be a DEAD KNOB. AP3 instead landed the profile's first REAL effect = the **profile↔format compatibility gate** (`D_ArtifactProfileFormatMismatch`, driver-side). The codegen-threading therefore stays deferred — its trigger is NOT "AP3" but **the first profile that drives a NON-format-redundant codegen difference** (a `gui` profile, or a `cli`-vs-`gui` split within ONE format — different entry-symbol + PE subsystem from the same exec format). Until such a consumer exists, threading the profile to codegen has no reader → would be a speculative build. Priority: **trigger-gated (do NOT build until a non-format-redundant profile consumer lands; e.g. gui codegen)**. | When a non-format-redundant profile consumer lands: introduce the profile-carrying context (or thread `artifactProfile` through the driver→pipeline call chain) so codegen reads it; pin with an interface test that codegen sees the resolved profile + a runtime/behavioral pin that the profile changes the artifact (e.g. subsystem/entry-symbol). | plan 06 §2.3/§2.4 + §5 (AP3/AP4); cited in `src/program/program.cpp` + `src/program/project_config.hpp`; the AP2 plan-lock Contest-2 + the AP3 §B (compatibility-gate-not-threading) |
| `D-AP2-TARGET-NAME-DEFAULT-FORMAT` | Plan 06 AP2 (2026-06-09): the project-config `targets[]` are `"<targetName>:<formatName>"` specs (the as-built driver convention, validated downstream by `D_InvalidTargetSpec`). The §2.2 example uses BARE target names (`"linux-x86_64"`); resolving a bare name to a default object-format is deferred. Trigger: first project config (or UX decision) that wants bare target names with an inferred default format. Priority: **normal** (specs work today; bare-name is sugar). | Add a per-target default-format resolution (e.g. a `defaultObjectFormat` on the target schema, or a name→format table) consulted when a `targets[]` entry has no `:format` half; keep fail-loud on an ambiguous/absent default. Pin with a bare-name target → resolved spec. | plan 06 §2.2; cited in `src/program/project_config.hpp`; pairs with `D_InvalidTargetSpec` (the downstream spec validator) |
| `D-AP2-MULTIPLE-SOURCES-SEMANTICS` | Plan 06 AP2 (2026-06-09): the basic N>1 source ROUTING — N independent CUs the linker merges (`compileUnits`) vs the single-CU path (`compileFiles`) — IS landed in AP2 via the shared `routesToMultiUnit` threshold (mirrors the CLI dispatcher). Only ADVANCED multi-source semantics are deferred: a per-source artifact-profile override, an explicit unity-build (`compileFiles` with N>1 → one CU5 unit) opt-in surface, or per-source target subsetting. Trigger: a real consumer needs one of those advanced shapes. Priority: **normal**. | Decide the advanced-semantics surface (per-source profile / unity opt-in flag in the project schema) when a consumer needs it; the routing default stays count-keyed + agnostic. Pin per the chosen shape. | plan 06 §2.2; re-scoped per the AP2 plan-lock MUST-FIX 5 (routing is IN AP2); future-work pin (do NOT cite in src) |
| `D-AP2-ENTRY-POINT-CONVENTION` | Plan 06 AP2 (2026-06-09): the profile-derived ENTRY-POINT selection (entry source + entry-symbol convention — `main` vs `WinMain`/`DllMain`, declared by the language config with per-target overrides per §2.4 thesis decision #4) is NOT consumed by AP2 — AP2 only validates the profile. **REPOINTED 2026-06-09 (AP4):** neither AP3 (the format gate) nor AP4 (the onboarding test matrix) consumed the entry convention — resolving the entry symbol is part of the SAME profile→codegen threading that `D-AP2-COMPILATION-CONTEXT` defers (no reader until codegen keys off the profile). The original "Trigger: AP3/AP4" was a mis-tag; the real trigger is the **first non-format-redundant profile consumer** (e.g. gui — the same trigger as `D-AP2-COMPILATION-CONTEXT`, of which this is a sibling). Priority: **trigger-gated (do NOT build until a non-format-redundant profile consumer lands)**. | When that consumer lands (alongside `D-AP2-COMPILATION-CONTEXT`): read the language-config-declared entry convention + profile/target overrides to resolve the entry-point symbol; pin with a profile×target → expected entry symbol mapping (config-driven, no `if(profile==…)` ladder). | plan 06 §2.3.5 + §2.4; `08-compilation-unit-plan` §2.6 (entry-agnostic CU); sibling of `D-AP2-COMPILATION-CONTEXT`; future-work pin (do NOT cite in src) |
| `D-DIAG-CLI-POSITION-RENDER-AND-ASSERT` | ✅ **CLOSED 2026-06-10 — A+B+C all shipped (this is the V2-4 "very good error detection" acceptance arc).** **User decision 2026-06-09** (asked "do we test miscompilations / invalid syntax / assert errors, and does the error show line:column + do we explicitly assert it?" before AP3). **Investigation (file:line evidence):** (1) position IS carried end-to-end — `ParseDiagnostic { BufferId buffer; SourceSpan span; vector<RelatedLocation> related; }` (parse_diagnostic.hpp:1146-1168) + `SourceBuffer::lineCol(offset)`; (2) DSS's OWN positioned renderer EXISTS + is unit-tested (`DiagnosticReporter::format()`/`formatAll()`, diagnostic_reporter.cpp:298-413 — hand-written over our SourceBuffer/SourceSpan, **NO clang/LLVM**; `--> file:line:col` + source line + `^`/`^^^` caret; `test_diagnostic_reporter.cpp` pins the caret) — **as of 2026-06-09 (Part A) it now HAS a production caller** (before: the CLI driver's `drainDiagnosticsToStderr` (program.cpp:83) printed a code-only one-liner (`error[CODE] text`) with NO file/line/col/caret; the LSP path uses spans→Ranges, separate); (3) the `P_*`/`S_*` parser+semantic error tests (~60-80) assert the CODE + sometimes `.actual`, **almost never `.span`/position** (only ~1 related-location span assertion found) → a right-code-wrong-position regression passes silently; (4) the corpus runner (`examples_runner.cpp` + `expected.json`) only supports successful-compile assertions (`exitCode`/`expectedStdout`) — **no `expectError`/`expectDiagnostic` field**, so no example asserts malformed-source→specific-diagnostic-at-position (runtime negative-miscompile pins DO exist via optimized==baseline + the div-trap corpus). **DECISION: defer to V2-4 (program-api CLI), which already scopes the "clang-quality diagnostic renderer" — do NOT insert a separate cycle before AP3.** Trigger: **FIRED — V2-4 is the owning arc** (this anchor is V2-4's explicit, strict-test-gated acceptance content, recorded in §0.1 V2-4). Priority: **high (lands in V2-4, blocks V2-4 closure).** | V2-4 lands all three with STRICT tests, one `/dss-cycle` each (user-directed "A, then B then C", 2026-06-09): **(A) ✅ DONE 2026-06-09 (commit `686095a`):** wired the CLI stderr drain → `DiagnosticReporter::format()` via a `BufferRegistry` built in `runCusToTargets` from the CUs' trees, so a CLI compile of bad source prints `file:line:col` + caret. **§B architecture decision (user 2026-06-09):** "driver builds + threads a BufferRegistry" (chosen over "reporter owns the registry" — keeps the reporter a pure sink + matches the existing `formatAll(BufferRegistry const&)` API). Per-diagnostic routing on `BufferId::valid()`: buffer-bearing → positioned caret; buffer-less driver `D_*` → code-only (no spurious `got `/`<unknown-buffer>`). `Tree::sourceShared()` added; e2e `CaptureStderr` pin (`bad.c:2:12`+caret) with RED-on-disable demonstrated. NO clang/LLVM (user constraint — DSS's own renderer). **Scope** = ANY buffer-bearing diagnostic (parser / semantic / HIR `H_*`) — the routing is on `BufferId::valid()`, not a phase identity. **Operator-visible format note:** positioned diagnostics render the NUMERIC code band (`error[P000E]:`, the renderer's existing `diagnosticCodePrefix` convention) rather than the symbolic `error[P_xxx]`; buffer-less driver `D_*` keep the symbolic code-only line. (Surfacing the symbolic name alongside the numeric band is a possible future polish, not in Part A's scope.) **(B) ✅ DONE 2026-06-09 (commit `ddac0de`) — §B re-scope:** the user chose a **golden-DIAGNOSTIC harness** over per-test `.span` assertions (the ~199 code-only `P_*`/`S_*` assertions made hand-writing offsets error-prone + §A.5-subset; the golden is by-construction). New `tests/analysis/test_diagnostic_corpus.cpp` + `tests/test_support/golden_file.hpp` (hoisted golden infra, 2nd consumer): a data-driven harness discovers every `tests/corpus/diagnostics/<lang>/` (dir name = shipped schema — agnostic, no language list), parses + `analyze()`s each malformed source, and pins EVERY emitted diagnostic's code + line:col span (+ `related=[…]` positions) to a `<file>.diag` golden, refreshable via `DSS_REFRESH_GOLDENS=1`. A span-stamping regression flips the golden → RED (DEMONSTRATED: perturbing ONLY the `S_RedeclaredSymbol` related-location span in `semantic_analyzer.cpp` — code + primary span unchanged — changed `redeclared_param.c.diag`'s `related=[…]` away from the golden's `1:11-1:12` → mismatch → RED; a code-only test would have stayed green). Corpus covers multi-diagnostic-per-file, a lexer+parser+semantic mix in one file, and related-locations across c-subset/toy/tsql-subset. Fail-loud on empty discovery / zero-diagnostic file / missing-or-orphan golden. The harness is the by-construction chokepoint, so "every code" is now a trivial pinned **follow-up** (drop a file + refresh) — `D-DIAG-CORPUS-EVERY-CODE`, normal. The ~199 existing unit tests stay code-only (the harness is the position guard). Independent plan-lock GO-WITH-FIXES (4 MUST-FIX folded) + code-review SHIP-IT. **(C) ✅ DONE 2026-06-10:** added an `expectDiagnostics` manifest field (`[{code, line, col}]`) to BOTH corpus harnesses — the in-process `examples_runner` (`runErrorTarget`) drives the full `Program::compileFiles` path + asserts the EXACT diagnostic multiset (`diagnosticCodeName` + `SourceBuffer::lineCol`-resolved 1-based line:col) over `rep.all()`; the CLI-subprocess `integrated_tests` (`runErrorExampleViaCli`) asserts the CLI REJECTS (rc!=0) + emits each diagnostic's positioned `:line:col` on stderr (the Part-A `--> file:line:col` renderer). New `examples/c-subset/undeclared_var_error` (`return missing_var;` → `S_UndeclaredIdentifier` at 2:12); RED-on-disable DEMONSTRATED (perturb col 12→99 → exact-set mismatch dump). `.gitattributes examples/** text eol=lf` pins the position-sensitive source bytes so the runner's binary read aligns with the compiler's byte offsets on every host. The miscompile-identification (optimized==baseline differential) discipline already lives in the runner (`OptimizedArm`/`optimizedPipelines`) — confirmed as part of program-api acceptance. Multi-source / multi-level error examples deferred (single-source assert). Agnostic (JSON-declared expectations + generic comparison, no language/code hardcoded); independent review SHIP-IT (traced `runCusToTargets` → a semantic error returns before per-target codegen, so the diagnostic set is host-independent). 215/215 ctest + integrated_tests green. **ALL THREE (A+B+C) SHIPPED — arc CLOSED 2026-06-10.** | plan-00 §0.1 V2-4; §8 program-api (step 16); `diagnostic_reporter.cpp` `format`/`formatAll`; `program.cpp` `drainDiagnosticsToStderr`; `tests/examples/examples_runner.cpp` + `expected.json`; future-work pin (do NOT cite in src) |
| `D-DIAG-CORPUS-EVERY-CODE` | V2-4 Part B (2026-06-09): the golden-DIAGNOSTIC harness (`tests/analysis/test_diagnostic_corpus.cpp`) pins code + line:col for every diagnostic in its `tests/corpus/diagnostics/<lang>/` corpus — by construction, so coverage GROWS by dropping a malformed source + `DSS_REFRESH_GOLDENS=1`. The initial corpus covers a representative set (multi-diag, a lexer+parser+semantic mix, related-locations) across c-subset/toy/tsql-subset, but NOT yet every one of the ~28 `P_*`/`S_*` codes. Trigger: a position-regression slips through an unexercised code, OR a hardening pass. Priority: **normal** (the chokepoint exists; adding a code is a pure file-drop, no test-code edit). | Add a malformed corpus file per uncovered `P_*`/`S_*` code (+ refresh the `.diag` golden). One file may cover several codes. Optionally also pin the `missing_init_expr.c` case once the `P_BuilderInvariant`-on-`int x = ;` recovery is fixed (separate task). | plan-00 §0.1 V2-4 (B); `D-DIAG-CLI-POSITION-RENDER-AND-ASSERT`; `tests/analysis/test_diagnostic_corpus.cpp`; future-work pin (do NOT cite in src) |
| `D-AP2-PROFILE-REGISTRY-SPLIT` | ✅ **CLOSED 2026-06-09 (AP3).** The trigger FIRED: AP3's object-format `artifactProfiles[]` loader is the SECOND consumer of the registered profile vocabulary. The vocab + the human-readable list were extracted from `grammar_schema_json.cpp` into the shared `src/core/types/artifact_profile.hpp` (`kRegisteredArtifactProfiles` + `constexpr isRegisteredArtifactProfile()` + `registeredArtifactProfileList()`); the grammar loader (AP1) was rewired to it (behavior-preserving — same `C_UnknownArtifactProfile`/`C_ConflictingField` codes + identical message, `core/test_grammar_schema` green) and the new object-format loader validates its served-profile entries against the SAME predicate (a typo like `"clii"` fails loud at format-load → `C_UnknownArtifactProfile`; RED-on-disable demonstrated for BOTH consumers). NOT a standalone `ArtifactProfileRegistry` class (plan 06 §3's `registerProfile` idea) — a shared header + predicate is the agnostic minimum; a class only if a 3rd, registration-style consumer (`C_ProfileBackendMissing`) appears. — ORIGINAL: AP2 deferred the split (single grammar-loader home) until a 2nd consumer; the plan-lock A4 resolution. | (closed by AP3 — shared `artifact_profile.hpp`) | plan 06 §3; the AP2 plan-lock A4 resolution; closed by the AP3 plan-lock Q1(a); cited in `src/core/types/artifact_profile.hpp` |
| `D-MIR-UNREACHABLE-PRUNE-NORMALIZE` | ✅ **CLOSED 2026-06-13 (MIR unreachable-prune cycle).** The §B-decided (Design 1) MANDATORY, unconditional MIR normalize that drops every block unreachable-from-entry, REUSING the shared `MirFunctionRebuilder`/`MirRebuildPolicy` substrate (policy: `selectBlocks` = RPO-reachable from entry, `acceptPhiIncoming` = pred-in-blockMap — DCE's exact shape) + a new `cloneGlobalsVerbatim` (NO carve-out — it must process every module; func-ids are stable since the prune drops BLOCKS only, so `initFunc` is tag-re-stamped `MirFuncId{old.v, newTag}`, never ordinal-remapped) + `rederiveStructCfMarkers`. Wired at the TOP of `optimizeModule` (compile_pipeline.cpp ~74), the universal post-lowering chokepoint that runs on every CU BEFORE `mergeCuMirs` AND on the merged module, BEFORE `opt::optimize`'s verify-after-every-pass — NOT a `PassId` (a pipeline config must never omit it); idempotent. This is the single BY-CONSTRUCTION chokepoint that owns the orphan-block class for ALL constructs (if / loop / switch / &&-\|\| / dead-code) and the FUTURE goto (FC5). §B alternatives REJECTED: Design 2 (lazy per-construct) = multi-site (the do-while-break sibling was already missed once that way); Design 3 (verifier exempts `Unreachable`-terminated blocks) = weakens a load-bearing invariant. Closes `D-HIR-SEALED-JOIN-UNREACHABLE-BLOCK`; unblocks plan-23 FC5. 272/272 ctest; 8 strict pins (incl. globals-not-carved-out + phi-pred-drop + red-on-disable) + 2 runnable 4-target corpus examples. | (closed — the prune IS the closing mechanism) | src/opt/passes/prune_unreachable.{hpp,cpp}; `cloneGlobalsVerbatim` in mir_rebuild_helper.{hpp,cpp}; wired in src/program/compile_pipeline.cpp `optimizeModule`; closes `D-HIR-SEALED-JOIN-UNREACHABLE-BLOCK`; unblocks plan-23 FC5 |
| `D-HIR-LOOP-BODY-ONLY-RETURN-DOUBLE-ATTACH` | ✅ **CLOSED 2026-06-13 (pre-FC5 deferral mini-arc).** Root cause was NOT loop-specific: `maybeAppendImplicitReturnZero` (src/hir/lowering/cst_to_hir.cpp) re-wrapped the body's ALREADY-PARENTED children via `makeBlock(oldKids+ret)`, and `makeBlock`→`addParent` aborts on a re-parented child (HIR is immutable) — so ANY non-empty non-terminating `main` crashed (the infinite-loop shapes AND `int main(){int x;x=1;}` are instances; the empty `int main(){}` survived only because oldKids was empty). FIX: immutability-safe nest — wrap the (unparented) body block + the synthetic return in a fresh outer block (`makeBlock({body,ret})`), never re-parenting children; the `addParent` guard is untouched. Infinite-loop mains now compile + run (the dead `return 0` is dropped by the MIR unreachable-prune); 274/274 ctest; corpus `infinite_loop_return_main` (exit 5) + `for_inf_return_main` (exit 9) + 2 HIR pins; red-on-disable demonstrated. The SEPARATE non-`main` infinite-loop over-rejection is `D-HIR-INFINITE-LOOP-NOT-TERMINATING` (next in the mini-arc). *(Original status, preserved:)* **OPEN (surfaced 2026-06-13 by the MIR unreachable-prune cycle's test-shape exploration — PRE-EXISTING; the prune diff touches NO src/hir/ or src/mir/lowering loop builder). Priority: normal.** A loop whose body is ONLY a return/seal (`while(1){return X;}`, `do{return X;}while(c);`, `for(;;){return X;}`) trips a HirBuilder fatal `addParent: HirNodeId=N already has a parent (double-attach)` during HIR construction — the tree-shaped-HIR structural guard fires on a VALID-C shape. Orthogonal to the MIR prune (upstream, in HIR build); the prune's loop-orphan coverage is instead via `for(i=0;i<10;i=i+1){return 11;}` (update-block orphan). | Root-cause the double-attach in HirBuilder for a loop body that seals immediately (a node re-parented between the body slot and a synthesized continue/exit edge); pin with the three loop-only-return shapes compiling + running. | hir_to_mir.cpp loop lowering; HirBuilder `addParent` guard; surfaced by the prune cycle; future-work pin (do NOT cite in src) |
| `D-HIR-DEAD-CODE-AFTER-RETURN-REJECTED` | ✅ **CLOSED 2026-06-13 (pre-FC5 deferral mini-arc).** Relaxed the HIR verifier's dead-code-after-terminator rejection: `checkBlockTermination` now emits a NEW `H_UnreachableCode` (0xF00D) WARNING instead of the `H_VerifierFailure` ERROR, so `verify()` passes and `return X; <dead>;` COMPILES — the dead statement reaches MIR, lowers via the Block-lowering fresh-dead-block, and is dropped by the unreachable-prune (runtime = the reachable `return X`). ISO-C-aligned (warn, don't reject). Scope: ONLY `checkBlockTermination`; the non-void-fall-through `H_VerifierFailure` ERROR (`checkReturnCompleteness`) is UNTOUCHED (still rejects). The new code is suppressible (not in the unsuppressable set). 281/281 ctest; corpus `dead_code_after_return` (exit 42) + 5 HIR warning pins; red-on-disable pinned. *(Original status, preserved:)* **OPEN (surfaced 2026-06-13 by the MIR unreachable-prune cycle — PRE-EXISTING). Priority: normal (matters for the full-C / FC arc).** Statements after a `return` in the same block (`return 42; return 7;`) are REJECTED by the HIR verifier (`H0003`, "statement is unreachable") BEFORE MIR — so the MIR prune is not their guard. This is stricter than ISO C (which permits unreachable code, typically with a warning); a real full-C program with dead-code-after-terminator would be rejected. | For the FC arc: relax the HIR verifier to ACCEPT (optionally warn on) unreachable-after-terminator statements, letting the MIR prune drop the resulting dead blocks; pin with `return X; <dead>;` compiling + running X. | HIR verifier `H0003`; `D-MIR-UNREACHABLE-PRUNE-NORMALIZE` is the downstream dead-block owner once HIR admits it; plan-23 full-C arc; future-work pin (do NOT cite in src) |
| `D-HIR-INFINITE-LOOP-NOT-TERMINATING` | ✅ **CLOSED 2026-06-13 (pre-FC5 deferral mini-arc).** Implemented the documented design: `lowerWhile`/`lowerFor` (cst_to_hir.cpp) now wrap a PROVABLY-INFINITE loop (const-truthy/absent condition via the shared `evalCstConstInt` const-eval, AND no `break` reachable in this loop's own frame — a frame-respecting body scan that does NOT descend into nested loops/switches) as `Block{ loop, Unreachable }`, so `pathTerminates` (recursing to the Block's last child) recognizes the function terminates. `int f(){while(1){return 5;}}` now compiles + runs (exit 5); `for(;;)`/`do-while(1)` likewise. NEGATIVE-pinned (no false positive = no miscompile): a breakable `while(1){if(x)break;}`, `while(0)`, `while(x)`, and a nested-inner-break are NOT wrapped. A statement-position `Unreachable` gained MIR lowering (`addUnreachable`), and MIR Block-lowering opens a fresh dead block for any sibling after a mid-block seal (dropped by the MIR unreachable-prune) — which INCIDENTALLY fixed a pre-existing latent crash of `<both-arms-return-if> <sibling>` (`int f(){if(x){return 1;}else{return 2;} return 99;}`), now pinned (corpus `nonmain_if_both_ret_dead_tail`). `pathTerminates`/the verifier/H0003/the inliner UNTOUCHED. 280/280 ctest; red-on-disable pinned. *(Original status, preserved:)* **OPEN (surfaced 2026-06-13 by the pre-FC5 deferral mini-arc — PRE-EXISTING). Priority: normal (next in the mini-arc, before FC5).** `pathTerminates` (the HIR verifier-substrate SSOT, src/hir/hir_verifier.hpp) is deliberately CONSERVATIVE — it does NOT count a loop body as terminating; the doc says lowering should append an `Unreachable` after a PROVABLY-INFINITE loop to make the structural answer match the dynamic one, but that step was NEVER implemented (no Unreachable-synthesis in cst_to_hir.cpp). Consequence: a non-`main` function whose only exit is inside a provably-infinite loop (`int f(){ while(1){ return 5; } }`, `for(;;){...}`) is OVER-REJECTED by the non-void-return-completeness check (pathTerminates(body)==false). Valid ISO C. (`main` is unaffected as of 2026-06-13: the implicit-return-0 nest gives it a terminating return; this anchor is the NON-`main` case.) Sibling of `D-HIR-DEAD-CODE-AFTER-RETURN-REJECTED` (both are pathTerminates/termination over-rejections of valid C). | Implement the documented design: detect a provably-infinite loop (constant-truthy or absent condition AND no `break`/`goto` that can exit this loop's frame) in cst_to_hir lowering and append a synthetic `Unreachable` after it, so `pathTerminates` recognizes the function terminates (the MIR unreachable-prune drops the resulting dead block). Pin: `int f(){while(1){return 5;}}` called from main → exit 5; a breakable loop still requires a real return after it. | src/hir/hir_verifier.hpp `pathTerminates` doc (the unimplemented append-`Unreachable` contract); src/hir/lowering/cst_to_hir.cpp loop lowering; the non-void-completeness check in hir_verifier.cpp; sibling `D-HIR-DEAD-CODE-AFTER-RETURN-REJECTED`; downstream owner `D-MIR-UNREACHABLE-PRUNE-NORMALIZE`; future-work pin (do NOT cite in src) |
| `D-PP-FUNCTION-LIKE-MACRO` | ✅ **CLOSED 2026-06-19 (FC13 cycle 2 — function-like macros).** The config-selected C preprocessor now implements function-like `#define F(a,b) ...` definitions AND invocations over the common direct-invocation subset of §6.10.3 (full hide-set deferred → `D-PP-MACRO-HIDESET-PRECISE`). `MacroExpander::handleDefine` parses the parameter list (comma-separated `Word` params, adjacency `(`-after-name detection on raw spans, empty `()` ⇒ zero params); the redefinition-compatibility check compares kind + params + replacement text (`sameDefinition`). The cycle-1 token-by-token `expandInto` was reworked into a STREAM expander `expand(std::span<Token const>, painting, depth)` (object- AND function-like uniformly): a function-like name is an invocation ONLY if the next significant token is the configured `(` (else emitted verbatim, §6.10.3p10); `collectArgs` collects paren-balance-tracked arguments (depth via the configured open/close paren; a depth-1 comma separates, the depth-0 close ends; trivia-trimmed); each argument is PRE-EXPANDED (`expand` in the current paint context, the invoked macro not yet painted, §6.10.3.1); `substitute` maps param-name tokens → pre-expanded arg tokens; the result is RESCANNED with the macro painted blue (§6.10.3.4 self-reference freeze). Zero-PARAM `M()` is normalized to zero args at the call site. FAIL-LOUD: arity mismatch + unterminated invocation (`P_PreprocessorMacroArgument`, new 0x0017); duplicate parameter + malformed param list (`P_PreprocessorDirective`); VARIADIC `#define V(...)` (`P_PreprocessorUnsupported` → `D-PP-VARIADIC-MACRO`, deferred). AGNOSTIC: the close-paren is config (`preprocess.functionLikeCloseToken`, mirroring the opener — `)` lexes as core `Punctuation`, so it MUST come from config). The cycle-1 `FunctionLikeMacroDefinitionFailsLoud` test was FLIPPED to `FunctionLikeMacroSimpleInvocationExpands`; 12 new function-like unit tests (nested, arg-is-macro pre-expansion, arity, verbatim-name, mixing, recursive-freeze, empty/zero-paren, variadic, dup-param, unterminated) + runnable 4-target corpus `examples/c-subset/preprocessor_function_macros` (exit 42). 331 ctest; object-macro tests + corpus unregressed; RED-on-disable demonstrated (neutering `substitute`). *(Original status, preserved:)* FC13 cycle 1 (2026-06-19): the config-selected C preprocessor (`src/analysis/preprocess/`) implements OBJECT-like `#define`/`#undef` only. A FUNCTION-like macro DEFINITION (`#define F(x) ...` - a `(` immediately adjacent to the macro name) is recognized and FAILS LOUD (`P_PreprocessorUnsupported`, `MacroExpander::handleDefine`), never silently passed through as object-like (which would mis-expand). Priority: **normal (cycle 2)** - the function-like macro feature (parameter capture, argument substitution; the `#`/`##` operators are SEPARATE = FC15) is the next preprocessor cycle. | Build function-like macro definition + invocation: parse the parameter list, capture call-site arguments (paren-balanced), substitute parameters into the replacement, then rescan. Pin: `#define F(x) ((x)+1)` + `F(2)` expands to `((2)+1)`; arity mismatch fails loud. Closing test in `tests/analysis/preprocess/test_preprocessor.cpp` (today `FunctionLikeMacroDefinitionFailsLoud` pins the fail-loud guard - flip it to expand-correctly when the feature lands). | plan 23 FC13; `tests/analysis/preprocess/test_preprocessor.cpp`; successor cycle 2 |
| `D-PP-MACRO-HIDESET-PRECISE` | ✅ **CLOSED 2026-06-19 (FC13 cycle 4 — the precise per-token hide set).** The recursion-scoped blue-PAINT set was REPLACED by Prosser's precise per-token hide set (the standard realization of C §6.10.3.4) — within FC13's no-`#`/`##` scope this now realizes the FULL §6.10.3.4, not merely the cycle-2/3-softened "common direct-invocation subset of §6.10.3". `Token` is a 16B trivially-copyable POD that cannot carry a set, so the expansion WORKING set is a wrapper `struct ExpToken { Token tok; HideSet hide; }` where `HideSet = shared_ptr<set<string> const>` (IMMUTABLE-once-built → copies are a refcount bump, every token of one replacement shares the SAME set object; a null pointer is the canonical EMPTY hide set, so the parser-bound body lifts in with `nullptr` and pays no allocation; `std::set` makes the hot intersection/union a linear merge). `run` lifts `vector<Token>` → `vector<ExpToken>` (empty hide sets), runs `expand`, drops hide sets back to `vector<Token>`. `expand` is now a SPLICE-RESCAN cursor (no longer isolated recursion): on each expansion it splices the produced tokens — each carrying its computed hide set — back over the consumed `[i,past)` region of the working set and rescans from the splice point, so a function-like name and a `(` that become adjacent only ACROSS the replacement/parent boundary RE-PAIR. A token (name M) expands iff `M ∉ hideset(T)`. OBJECT-like M: replacement tokens carry `hideset(T) ∪ {M}`. FUNCTION-like M (next significant token is the configured `(`, matching `)` exists): `HS' = (hideset(name) ∩ hideset(closeParen)) ∪ {M}` (the Prosser intersection — `collectArgs` surfaces the close paren's hide set; an argument's own accreted hide set is UNIONED with `HS'`, never dropped). The cycle-2/3 cross-boundary witnesses were FLIPPED: `#define A(x) x`+`#define F(x) ((x)+100)`+`A(F)(3)` → `((3)+100)` (was the literal `F(3)`); `#define NAME SQ`+`#define SQ(x) ((x)*(x))`+`NAME(4)` → `((4)*(4))` (was a frozen bare `SQ`). PRESERVED: direct object/function self-reference freeze (`#define X X`→`X`; `#define F(x) F(x)`+`F(1)`→`F(1)`), mutual recursion termination (`P↔Q`+`P(1)`), nested/argument-pre-expansion, variadic, object — all unchanged. The `depth` backstop is RETAINED as defense-in-depth but now bounds NESTING (argument pre-expansion) rather than chain length: a finite object CHAIN (`M0`→…→`M300`→0) now expands ITERATIVELY in one frame to `0` (the cycle-2/3 `DeepMacroExpansionFailsLoudNotSilent` premise was a recursive-engine artifact — FLIPPED to `DeepFiniteMacroChainExpandsToValue`), while a pathological 256-deep-nested argument (`F(F(…F(0)…))`) still FAILS LOUD (`DeepNestedMacroArgumentFailsLoudNotSilent`). AGNOSTIC: the new machinery operates on `Token`/`SchemaTokenId` + macro NAMES only; `(`/`)`/`,`/`...`/`__VA_ARGS__` remain config-driven (no new hard-coded lexeme/arch/format string). +6 net preprocessor unit tests (2 cross-boundary FLIPs, 4 preserved-freeze) + reframed deep-chain (1→2) + runnable 4-target corpus `examples/c-subset/preprocessor_hideset` (object-macro-names-function-macro + function-returns-function-name, runtime-seeded via `ident`, exit 42). 333 ctest; all object/function/variadic preprocessor tests + corpora unregressed; RED-on-disable demonstrated (reverting object-like to isolated recursion mis-freezes `NAME(4)` → bare `SQ`, `test_preprocessor.cpp` `HideSetObjectMacroNamingFunctionMacroExpands` fails). *(Original status, preserved:)* FC13 cycle 2 (2026-06-19): the function-like macro rescan uses a recursive blue-PAINT set (`expand`'s `painting` keyed on the macro NAME) — an APPROXIMATION of the precise per-token §6.10.3.4 hide-set. It is correct for the common DIRECT-invocation forms (a name immediately or near-adjacently invoked, self-reference freeze, nested/argument-as-macro pre-expansion — all pinned), but does NOT expand cases where a function-like name and its `(` are produced across SEPARATE expansion streams: `#define A(x) x` + `#define F(x) ((x)+100)` + `A(F)(3)` → the `A(F)` yields a bare `F`, and the trailing `(3)` is NOT re-paired with it, so the result is the literal `F(3)`; likewise `#define NAME SQ` + `#define SQ(x) ((x)*(x))` + `NAME(4)` freezes to bare `SQ` (the object expansion of `NAME` does not re-scan to collect `(4)`). These residual cases are a COMPLETENESS gap, NOT a silent miscompile: each FAILS LOUD downstream at the PARSER (verified: `got F` / `got SQ` — a bare function-like name with a stray `(…)` is a syntax error), never producing wrong code. `A(F)(3)` is the constructible loud-failure witness. Co-designed with FC15's `#`/`##` token-marker cluster (the precise per-token hide-set is the same machinery that tracks `#`/`##` placemarkers). Priority: **normal / trigger-gated** — closes with FC15, or sooner if a real corpus needs cross-stream re-pairing. | Replace the name-keyed blue-paint with a precise per-token hide-set (each replacement token carries the set of macro names disabled for it, per §6.10.3.4): a token's hide-set is the intersection of the invoked-macro's hide-set with the next-token's, plus the invoked macro; re-pairing of a function-like name with a `(` produced by a SEPARATE expansion then follows. Pin: `A(F)(3)` → `((3)+100)`; `NAME(4)` → `((4)*(4))`. Implement alongside the FC15 `#`/`##` placemarker tokens. | plan 23 FC13/FC15; `src/analysis/preprocess/preprocessor.cpp` `expand`/`painting`; witness `A(F)(3)` (today fails loud `got F` at the parser); FC15-area cluster (precise hide-set co-designed with `#`/`##`) |
| `D-PP-VARIADIC-MACRO` | ✅ **CLOSED 2026-06-19 (FC13 cycle 3 — variadic `__VA_ARGS__` macros).** The config-selected C preprocessor now implements PLAIN variadic function-like macros over the C99/C23 `__VA_ARGS__` subset (NO `#`/`##`, so NO GNU `,##__VA_ARGS__` comma-elision — that stays FC15). `MacroDef` gained `bool isVariadic`; `parseParamList` ACCEPTS a trailing `...` (the configured `variadicMarkerToken` KIND) as the LAST parameter-list element (`(...)` or `(a, b, ...)`) — setting `isVariadic`, replacing the cycle-2 `P_PreprocessorUnsupported` fail-loud — and FAILS LOUD (`P_PreprocessorDirective`) when `...` is NOT last (`(a, ..., b)` / a stray token after it). The catch-all identifier is CONFIG-DRIVEN: new `PreprocessConfig::variadicArgsName` (c-subset `"__VA_ARGS__"`, matched by TEXT in the replacement like the directive words; loader OPTIONAL + non-empty-string-validated, `C_InvalidPreprocess`). Invocation of `F(named..., ...)`: the first `params.size()` args bind to the named params; the REST form `__VA_ARGS__` as ONE comma-joined run that PRESERVES the ORIGINAL source separator commas (captured by `collectArgs` into a `separators` vector), each trailing arg PRE-EXPANDED like a named arg (C 6.10.3.1). ARITY floor = the NAMED count (`args.size() < params.size()` → too-few fail-loud, `P_PreprocessorMacroArgument`); the variadic portion MAY be EMPTY (C23 — `__VA_ARGS__` then substitutes to nothing). `substitute` replaces a replacement `Word` whose text == `variadicArgsName` IN A VARIADIC macro with the `__VA_ARGS__` run; `__VA_ARGS__` in a NON-variadic (object- or function-like) macro is REJECTED at DEFINITION (`P_PreprocessorDirective`, C 6.10.3p5). AGNOSTIC: `__VA_ARGS__` appears as a STRING nowhere in `src/*.{cpp,hpp}` (only in comments + the c-subset config JSON) — the engine reads `cfg().variadicArgsName`; a red-on-disable rebind test (`variadicArgsName` → `__REST__`) proves it. The cycle-2 `VariadicMacroDefinitionFailsLoud` test was FLIPPED to `VariadicMacroNamedPlusVaArgsExpands`; +7 new variadic unit tests (named+__VA_ARGS__, zero-named, empty-C23, trailing-arg-pre-expansion, `...`-not-last, too-few-args, `__VA_ARGS__`-in-non-variadic) + a config-driven `VaArgsNameIsConfigDrivenNotHardcoded` pin + runnable 4-target corpus `examples/c-subset/preprocessor_variadic_macros` (runtime-seeded via `ident`, exit 42). All function/object preprocessor corpora + tests unregressed; RED-on-disable demonstrated (neutering the `__VA_ARGS__` arm in `substitute`). *(Original status, preserved:)* FC13 cycle 2 (2026-06-19): the function-like macro engine (`src/analysis/preprocess/preprocessor.cpp`, `MacroExpander::parseParamList`) RECOGNISES a VARIADIC parameter list (`#define V(...) ...` — a `...` ellipsis lexeme in parameter position) and FAILS LOUD (`P_PreprocessorUnsupported`), never silently accepting it as a named parameter (which would mis-substitute) and never dropping it. Variadic macros need the `__VA_ARGS__` substitution mechanism + the `,##__VA_ARGS__` GNU comma-elision, which pair naturally with the `#`/`##` operators — both are FC15-area, so this is parked with that cluster. Priority: **trigger-gated** — closes when FC15 (`#`/`##`/predefined + variadic) lands, or sooner if a real corpus needs `__VA_ARGS__`. | Implement `...` in the parameter list + the `__VA_ARGS__` identifier in the replacement (collect the trailing arguments incl. their separating commas into one token run; substitute at `__VA_ARGS__`), plus the GNU `,##__VA_ARGS__` empty-elision once `##` exists. Pin: `#define LOG(fmt, ...) printf(fmt, __VA_ARGS__)` expands a call; the zero-trailing-arg case elides the comma. The closing test flips `VariadicMacroDefinitionFailsLoud` in `tests/analysis/preprocess/test_preprocessor.cpp`. | plan 23 FC13/FC15; `src/analysis/preprocess/preprocessor.cpp` `parseParamList`; `tests/analysis/preprocess/test_preprocessor.cpp::VariadicMacroDefinitionFailsLoud`; FC15-area cluster |
| `D-PP-FC2-ORACLE-RETIRE` | FC13 cycle 1 (2026-06-19): the FC2 cross-file-typedef ORACLE (`compilation_unit.cpp` finish()-reparse + the `AmbiguousTypeNameCandidate` channel) is now SUPERSEDED for the common C case - the preprocessor splices a quote-`#include`d header's TEXT inline BEFORE parsing, so a header typedef is in scope on the includer's FIRST parse and the cross-file cast commits WITHOUT a reparse (verified: `examples/c-subset/include_typedef_cast` still exits 42 inline; `test_type_name_oracle` now asserts reparseCount 0 + one tree for typedef-before-use). NOT fully retired: it STILL fires for an in-buffer FORWARD reference across an inline-include boundary (`test_type_name_oracle.cpp::BothDirectionsResolveInlineInOneBuffer`, reparseCount 1). MACHINERY retained this cycle (language-agnostic; a future non-preprocessed-include language could exercise it). Priority: **normal** - removable only once no path (forward-ref included) needs it. | Decide whether the residual forward-reference case justifies keeping the oracle, or whether a single-pass two-phase type-name collection over the synth buffer can replace it; if removable, delete the finish()-reparse arm + the candidate channel + the sidecar `ppTokens`/`ppRemap` reparse plumbing. | plan 23 FC13/FC2; `compilation_unit.cpp` finish() oracle; `tests/analysis/compilation_unit/test_type_name_oracle.cpp` |
| `D-PP-INCLUDE-SPLICE-POSITION-ATTRIBUTION` | ✅ **CLOSED 2026-06-19 (FC13 cycle-1 review fold — Fix 2).** Resolved via option (a): `makeRemap` now redirects BOTH header- AND main-origin diagnostics off the synth buffer onto their real origin buffer (the main-origin early-return was removed), and the line-map's per-segment `originStart` (already built across splice boundaries by `copyVerbatim`/`appendWithContinuationSplice`) shifts a post-`#include` main-file synth offset back to its ORIGINAL main.c offset -- so a main-file error after a leading quote-`#include` now reports the real main.c line, not a splice-shifted synth line. The missing pieces were: (1) `PreprocessResult::originBuffers` (the distinct origin buffers the line-map references, ex-synth) surfaced as `CompilationUnit::auxiliaryBuffers()`, and (2) registering them with the diagnostic `BufferRegistry` at every render site (`program.cpp runCusToTargets` + the `test_diagnostic_corpus` harness) so a remapped diagnostic resolves instead of rendering `--> <unknown-buffer:N>`. PINNED: `test_preprocessor.cpp::IncludeSpliceDiagnosticsRenderToRealFilesAndLines` formats a real TU (leading `#include` whose header errors + a main error on line 2) through `DiagnosticReporter::formatAll` over the assembled registry and asserts `bad.h:1:` + `main.c:2:` + NO `<unknown-buffer`; RED-on-disable demonstrated (starving `originBuffers` -> both render `<unknown-buffer>`). No residual: header AND main both attribute to the correct file:line. *(Original status, preserved:)* FC13 cycle 1 (2026-06-19): the preprocessor line-map remaps a HEADER-origin diagnostic to the header file, but a MAIN-file-origin diagnostic is intentionally LEFT on the synthesized buffer (`PreprocessResult::makeRemap` skips the main-source origin). For a no-include TU the synth buffer is byte-identical to the main file so line:col matches; for a TU with a LEADING quote-`#include` the spliced header text SHIFTS subsequent main-file content, so a main-file diagnostic renders at the SYNTH line:col, not the original main.c line:col. | (closed by Fix 2 — option (a) implemented) | plan 23 FC13; `src/analysis/preprocess/preprocessor.cpp` `makeRemap`/`LineMap`/`originBuffers`; `CompilationUnit::auxiliaryBuffers`; `program.cpp` registry; `tests/analysis/preprocess/test_preprocessor.cpp` |
| `D-FFI-SHIPPED-DESCRIPTOR-CONSTANTS-TYPEDEFS` | ✅ **CLOSED 2026-06-22 (Item 1 — shipped-header constants/typedefs via the neutral JSON descriptor).** §B redesign of the angle-`#include <h>` "next-up": the FIRST design (textual splice of a C `.h` from `shippedLibDirs`) was REJECTED on agnosticism (a C-syntax `.h` in `src/dss-config/` couples the language-NEUTRAL shipped config to C); the user chose DESCRIPTOR-ONLY (the angle-textual-splice was reverted). The neutral `*.json` shipped-lib descriptor was EXTENDED with `constants` (a header's `#define`-as-named-integer-constant surface, e.g. `CHAR_BIT` — name + int64 BIT-PATTERN value + integer-scalar hir-text type) + `typedefs` (name + hir-text type). The semantic phase injects them at the descriptor seam (BEFORE Pass 1.5, sharing the symbols' goal-2/first-wins sets): a constant → `isConst` Variable + `isInjectedConstant`, folded to a literal by the ONE shared `constantLiteralForSymbol` builder; a typedef → `DeclarationKind::Type`, resolved in type position. CRUX: constants fold not only in VALUE position (HIR Ref→Const) but in CONSTANT-EXPRESSION position (`int a[CHAR_BIT]` / `case INT_MAX:`) via a NEW `CstEvalEnvironment::resolveSymbolValue` direct-value arm in BOTH const-eval engines — which ALSO closes the latent enum-enumerator-in-array-dim gap. Reader fails loud on non-integer-scalar constant type / out-of-range / negative-for-unsigned / unknown key / empty descriptor; the unsigned-max (`UINT_MAX`/`ULLONG_MAX`) int64 bit-pattern round-trips losslessly (128-bit constants limited to 64-bit magnitude, commented). Neutral `limits.json` (constants-only) + `examples/c-subset/shipped_include_limits` (CHAR_BIT in array-dim + value → exit 42, all 4 targets + release arm). 6 decode pins + 4 semantic integration pins (array-dim extent=8, typedef resolution, goal-2 selective, const-violation) + RED-ON-DISABLE demonstrated (neutering the direct-value arm → S_NonConstantArrayLength). Independent code-review CLEAN (no CRIT/HIGH; 2 LOW folded). 355/355 ctest. | (closed — feature delivered) | plan 11 FF11 / plan 23 angle-include next-up; `src/ffi/shipped_lib_descriptor.{hpp,cpp}`; `src/analysis/semantic/constant_symbol_fold.hpp` + `semantic_analyzer.cpp` injection; `src/hir/cst_const_eval.{hpp,cpp}` resolveSymbolValue; `src/dss-config/shippedLibs/limits.json`; `examples/c-subset/shipped_include_limits` |
| `D-PP-CONDITIONAL-COMPILATION` | ✅ **CLOSED 2026-06-22 (Item 2 / FC14 — C conditional compilation).** `#if`/`#ifdef`/`#ifndef`/`#elif`/`#else`/`#endif` + the `defined` operator in the config-selected C preprocessor (`MacroExpander`). A single-pass CONDITION STACK (`CondFrame{enclosingActive, anyBranchTaken, thisBranchActive, seenElse}`): the 6 conditional directives are dispatched UNCONDITIONALLY (always balance-track nesting), while `#define`/`#undef`/`#include` + the unsupported-directive diagnostic + body-token emission are gated on `stackActive()` (a dead `#if 0` group is elided — only nesting is tracked, no errors, C 6.10.1); `seenElse` fail-loud on `#elif`/`#else` after `#else`; unterminated-at-EOF fail-loud. The `#if` integer-constant-expression evaluator is a NEW LIGHTWEIGHT preprocess-time TOKEN-Pratt (`src/analysis/preprocess/pp_if_eval.{hpp,cpp}`) that REUSES the main const-eval with ZERO duplication (§B): `opFromName`/`opEntryFor` extracted to a shared `src/hir/const_eval_operators.hpp` (CST evaluator byte-identical), the `const_eval_arith` core (`applyBinaryInt`/`applyUnaryInt`/`asBool`/`makeBoolLiteral`), `decodeInteger`, and `schema->operatorTable()` for PRECEDENCE (config-driven — no hardcoded operator/token names). `defined(X)`/`defined X` resolved BEFORE expansion (config parens, MF-1) into synthetic int-literals; then macro-expand the operand; surviving identifiers → 0 (incl. `sizeof`, which is NOT keyword-special-cased — C-faithful per C 6.10.1p4, so `sizeof(int)` fails loud as malformed; the agnosticism-breaking `"SizeofKeyword"` hardcode was removed in review-fold). Short-circuit `&&`/`||` + ternary fold only the live arm (C 6.6 unevaluated operands not folded → `0 && (1/0)` is safe); div0/shift/overflow refuse via `EvalOptions` → `P_Preprocessor*`. 20 strict unit pins (precedence `1+2*3==7` vs `==9`, `defined` paren+no-paren, dead-`#elif` operand-not-evaluated, `#elif`/`#else`-after-`#else`, config-rebind agnosticism) + runnable `examples/c-subset/preprocessor_conditionals` (exit 42, 4 targets + release arm). Plan-lock GO-WITH-FIXES (5 MF folded) + independent code-review (no CRIT/HIGH; MED `SizeofKeyword` + 2 LOW folded). 356 ctest. | (closed — feature delivered) | plan 23 FC14; `src/analysis/preprocess/preprocessor.{cpp,hpp}` + `pp_if_eval.{hpp,cpp}`; `src/hir/const_eval_operators.hpp`; `src/core/types/preprocess_config.hpp` + `grammar_schema_json.cpp`; `c-subset.lang.json`; `tests/analysis/preprocess/test_preprocessor.cpp`; `examples/c-subset/preprocessor_conditionals` |
| `D-PP-CONDITIONAL-INCLUDE-ORDERING` | ✅ **CLOSED 2026-06-26 (p18 Cluster G c17).** Both symptoms fixed: a quote-`#include` guarded by a dead `#if`/`#ifdef` is no longer resolved (P0016), and an illegal char (`$`/`@`/`` ` ``) in a dead branch no longer reports (P000E) while a LIVE one STILL fails loud. **★ The originally-planned Option A (a pre-scan dead-region oracle) was IMPLEMENTED then CAUGHT by the in-cycle review as a CRITICAL SILENT MISCOMPILE: the SynthBuilder pre-scan cannot see predefined (or header-supplied) macros, so a predefined-macro-guarded LIVE branch (`#if __STDC__`) was folded to 0 -> recorded DEAD -> a genuinely-live illegal char consumed before the parser (e.g. on a `#define` line) was SILENTLY suppressed (reproduced end-to-end via the CLI: `#if __STDC__\n#define M $\n#endif` compiled with zero diagnostics).** §B (user, 2026-06-26): **Option 1 — authoritative dead-regions.** The P000E suppression is now driven by the AUTHORITATIVE `MacroExpander` pass's liveness (`MacroExpander::deadRanges()`, recorded in `run()` from the full `table_`+`predefined_` state) — the SAME liveness the compiler emits/skips tokens on — so the illegal-char oracle can NEVER diverge from the real branch decision (the silent class is gone by construction). The P0016 include gate stays in the conditional-aware `SynthBuilder` (it must precede the macro pass), gated on `includeResolvable()`; its pre-scan dead-region machinery was REMOVED (now dead code). Plan-locked GO-WITH-FIXES (3 blocking folded: byte-interval recorder spec, per-directive re-sync, the proven-RED predefined test + false-green guard). Pins: the c17 `Preprocessor.*` block in `test_preprocessor.cpp` (`DeadBranch*`, `LiveBranch*`, `ActiveIllegalChar*` incl. `#define`-line/`#`-stringize/uninvoked-body, `PredefinedMacroGuardedLiveIllegalCharStillErrors` + false-green guard, `LiveOuterDeadInnerNest*`, `UnterminatedDeadBranch*`, `DeadRegionCloseUsesConfigEndifWord*`, FIX-3 function-like + `UnevaluableGuard*`) + runnable corpus `examples/c-subset/preprocessor_dead_branch_include` (predefined-guard live arm; exit 42, 4 targets + release). A residual conservative LOUD edge (predefined-VALUE-position guards in the include gate) is pinned separately as `D-PP-PRESCAN-PREDEFINED-VALUE-INCLUDE-GATE`. — ORIGINAL TRIGGER-FIRED note (2026-06-26): SQLite is the first real input to hit it: `#if SQLITE_OS_WIN`→`#include "windows.h"` resolves on linux/macos (P0016 ×5) AND a TCL gen-script in `#if 0` (sqlite3.c:32562) trips the illegal-char `$` (P000E ×10). §B DECISION (user, 2026-06-26): **Option A — a conditional pre-scan** (evaluate the `#if`/`#ifdef`/`#endif` structure right after tokenization + STRIP dead-group tokens BEFORE SynthBuilder splices quote-includes AND before illegal-char diagnostics finalize; share the conditional eval with the existing MacroExpander pass — DRY). Chosen over Option B (fold include-splice into the MacroExpander + separately defer the tokenizer illegal-char) for the single-mechanism cleanliness. — ORIGINAL deferral below. FC14 named exclusion (2026-06-22): a quote-`#include` GUARDED by a conditional is spliced by `SynthBuilder` BEFORE the `MacroExpander` conditional pass runs, so `#if 0\n#include "x.h"\n#endif` still RESOLVES x.h (its spliced CONTENT is then correctly elided by the conditional pass, but x.h's resolution side-effects — recursive includes, a missing-file `P_PreprocessorIncludeError` — already happened). NOT a silent miscompile: the elision is correct, and a missing dead-branch include errors LOUDLY (over-approximation, never a silent mis-include). The same class covers an illegal char (`@`) inside a dead branch (the single global tokenize precedes elision → a `P_IllegalChar`, loud). Priority: **normal** (rare — conditionally-guarded includes; the common case works). | Reorder so conditional elision precedes quote-include splicing — i.e. run a conditional/`#if` pre-scan that strips dead groups from the token stream BEFORE `SynthBuilder` resolves quote includes (or fold include resolution INTO the conditional-aware macro pass). Pin: `#if 0\n#include "does_not_exist.h"\n#endif\nint x;` compiles clean (no include error). | plan 23 FC14; `src/analysis/preprocess/preprocessor.cpp` (`SynthBuilder` splice vs the conditional pass ordering) |
| `D-PP-PRESCAN-PREDEFINED-VALUE-INCLUDE-GATE` | NEW (2026-06-26, p18 Cluster G c17): the `SynthBuilder` quote-`#include` gate (`includeResolvable`) evaluates `#if`/`#elif` guards with a pre-scan that sees only this file's live `#define`s + `defined()`/`#ifdef` (which already AGREE with the macro pass — both treat predefined macros as not-`defined`). It does NOT materialize a predefined macro used in VALUE position (`#if __STDC_VERSION__ >= 201112L`), folding it to 0. So a LIVE such branch's quote-`#include` is conservatively SKIPPED (→ loud missing-symbol downstream) and an angle-`#include`'s descriptor MACRO surface is not spliced (→ loud undefined-macro). **NEVER silent** (the c17 illegal-char oracle is authoritative via `MacroExpander::deadRanges()`; only the include gate is affected, whose every failure mode is loud). Rare — predefined-VALUE-guarded includes (version checks usually guard ANGLE includes, which the import resolver handles, not project QUOTE includes); NOT hit by sqlite3.c (its conditional includes use `#ifdef SQLITE_OS_WIN` / `#if 0`, which evaluate correctly). The user-approved "seed predefined macros into the pre-scan" deferred here per the c17 plan-lock (loud-only + rare → disproportionate to build the predefined value-materialization machinery now). Priority: **normal**. | Materialize predefined CONSTANT macro values in the pre-scan `#if` evaluation via the existing `PpProductText` product-text convention (`sbExpand`/`sbEvalIfOperand` emit a predefined name's value tokens with spans in the product region; `productCb` returns the product). Pin: `#if __STDC_VERSION__ >= 201112L\n#include "missing_live.h"\n#endif` → P_PreprocessorIncludeError (the live include IS resolved) + converse `#if __STDC_VERSION__ < 0` skips. | plan 25 c17; `src/analysis/preprocess/preprocessor.cpp` (`SynthBuilder::sbEvalIfOperand`/`sbExpand`/`includeResolvable`); `src/analysis/preprocess/pp_if_eval.hpp` (`PpProductText`) |
| `D-PP-LINE-COMMENT-BEFORE-DIRECTIVE` | ✅ CLOSED 2026-06-26 (c22) — ROOT CAUSE: the `//` token's `stringStyle.endsAt:"\n"` CONSUMED the terminating newline as a comment-body token, so a `code // comment` line's only newline vanished → a following `#` directive lost its line boundary → `firstOnLine` judged it not-first-on-line → leaked to the parser (P0001). FIX: a new GENERIC `StringStyle.endsAtExclusive` (config-driven, agnostic) — the `//` mode ends AT but EXCLUDES the `\n`, which survives as a Newline token; handled at BOTH endsAt-consume sites (per-codepoint + coalesced). The c19 `firstOnLine` hypothesis was correctly DISPROVEN — the real gate was the TOKENIZER, not firstOnLine. Indep review+audit VERIFIED-CLEAN (adversarial: corpus exit 42 WITH the fix, directive-leak + NO binary WITHOUT); 445/445. (EOF line comment w/o trailing newline still fails loud P_UnterminatedComment — pre-existing, intended, C-conforming.) [orig NEW c18, refined c19] A `//` line comment AFTER code on a line, IMMEDIATELY followed by a `#` directive on the next line, makes the preprocessor FAIL to recognize the directive: it leaks to the parser → `D0004 <malformed include>` + `P0001 expected 'IncludeKeyword' got 'define'`, then a full cascade. Precise repro (CLI-confirmed): `int a; // c\n#define Z 1\nint b=Z;` → P0001. A `//` comment ALONE on its line before a directive (`// c\n#define Z 1`) WORKS; a trailing `//` comment ON the directive line (`#define Z 1 // c`) WORKS; a `/* */` comment before a directive WORKS — only `<code> // comment` then a directive breaks. NOT silent (loud parse error). Likely root (confirm at fix time): `MacroExpander::firstOnLine` + the `//`-comment token swallowing its trailing newline, so the `#` after `code // comment` scans back through the comment (trivia, not-newline) to the code `;` and is judged not-first-on-line → not stripped as a directive. PRE-EXISTING (FC14-era `firstOnLine`; NOT introduced by c17/c18 — neither touched directive recognition). NOT on the SQLite path (sqlite3.c uses `/* */`, so the c18 re-probe did not hit it — IncludeKeyword count 0). Priority: **normal** (rare pattern; loud, not silent; NOT on the SQLite path). ★ **c19 UPDATE (2026-06-26): the `firstOnLine` hypothesis was TESTED and DISPROVEN** — adding a LineComment line-boundary check to `MacroExpander::firstOnLine` did NOT fix it (a clean rebuild still failed `int a; // c\n#define Z 1`). The `#` IS first-on-line, so `firstOnLine` is not the gate. Variant probes narrowed it: `int a; // c\n#define` FAILS, but `int a;\n// c\n#define` (comment on its own line) and `int a;\n\n#define` (blank line) both WORK — so the trigger is specifically a `//` comment SHARING A LINE WITH CODE, before a directive. The real mechanism is a preprocess-tier `//`-comment interaction not yet root-caused (`ppLexemes` at the preprocess tier shows `//` lexed as `/`+chars, but the full CLI pipeline handles `//` elsewhere — so the dump is misleading; needs a parse-tier token dump of the failing vs passing case). DEEPER than a one-liner — its own focused investigation cycle. | Root-cause the `code // comment`-then-directive desync (dump the synth + parse token streams for `int a; // c\n#define Z 1` vs `int a;\n// c\n#define Z 1`); fix at the real gate. Pin: `int a; // c\n#define Z 1\nint b=Z;` compiles clean AND `b` expands to `1`. | plan 25 c18/c19; `src/analysis/preprocess/preprocessor.cpp` + the `//`-comment tokenizer mode |
| `D-CSUBSET-ADJACENT-STRING-CONCAT` | ✅ **CLOSED 2026-06-26 (p18 Cluster G c20, 441/441).** Adjacent string-literal concatenation (C 5.1.1.2 phase 6) implemented. Grammar `stringLiteralExpr` repeats the `StringStart StringLiteral` pair; a SINGLE chokepoint `decodeAdjacentStringBodies` (new `src/core/types/string_literal_decode.hpp`) decodes EACH body separately then byte-joins (phase-5-before-6 — `"\x41" "1"`→"A1", NOT a raw merge `\x411`); fail-loud `nullopt` on a bad escape. ★ The MULTI-SITE contract was honored: the EXACT consumer set is 4 (independently re-grep-confirmed, no 5th) — A `lowerStringLiteral` (cst_to_hir.cpp:1764, routed), B attribute-arg (fail-loud `H_UnknownLinkageSpecifier` on a 2nd opener, not silent-truncate), C `extractLibraryOverride` (routed), D the SEMANTIC typer (semantic_analyzer.cpp:2936 — restructured from per-body-TOKEN typing to typing the stringLiteralExpr RULE node via the SAME chokepoint, so `subtreeType` short-circuits on the rule node's `Array<Char,N+1>`; per-token fallback gated `!parentIsStringLiteralExprRule` keeps toy/tsql green). HIR + semantic provably agree on N (same chokepoint). Pins: parser (2/4/6 flat children), semantic (`Array<Char,12>` for `"hello" " world"` + `typedBodyTokens==0`), HIR (2-way/3-way/escape-boundary), corpus `adjacent_string_{concat,escape_boundary,macro}` (4 targets + release arm; escape-boundary runtime-confirmed "A1"). Plan-locked (architect blueprint) + independent review VERIFIED-CLEAN + independent audit VERIFIED-CLEAN. Single-string byte-identical (repeat fires 0×). — ORIGINAL: NEW (2026-06-26, p18 Cluster G c19 — a sqlite3.c re-probe blocker). Adjacent string literals (C 5.1.1.2 phase 6: `"a" "b"` ≡ `"ab"`) are NOT concatenated. The grammar `stringLiteralExpr` (`{ "sequence": ["StringStart","StringLiteral"] }`) matches exactly ONE string; a second adjacent string fails loud (`P0002 expected ')' — got '"'`, ×50 in sqlite3.c at line ~129544). NOT silent. ★ MULTI-SITE CONTRACT: the fix is grammar (repeat the `StringStart StringLiteral` pair) PLUS concatenating ALL body children in EVERY consumer of a string-literal body — `lowerStringLiteral` (cst_to_hir.cpp:1743, the primary path), the attribute-arg path (cst_to_hir.cpp:~776), `bodyLiteralNodeOf` + the extern asm-label path (~5914), and `hir_text.cpp` (~1137/1185). A consumer that reads only the FIRST body silently drops the rest → silent miscompile. Must funnel through ONE concat chokepoint OR fix+test every consumer (incl. forms no shipped lang hits yet). Priority: **high** (re-probe blocker). | Grammar: `stringLiteralExpr` → `StringStart StringLiteral { repeat StringStart StringLiteral }`. Lowering: a shared `decodeAdjacentStringBodies(node)` chokepoint that decodes+concatenates all `StringLiteral` children; route every body consumer through it. Tests: `"a" "b"` → "ab" (primary), in an attribute arg, in an asm label; + a runnable corpus (exit reflects the concatenated bytes). Pin: `char s[]="a" "b"; return s[1];` → 'b'. | plan 25 c19; `src/dss-config/sources/c-subset.lang.json` (`stringLiteralExpr`); `src/hir/lowering/cst_to_hir.cpp` (`lowerStringLiteral` + all body consumers); `src/hir/hir_text.cpp` |
| `D-CSUBSET-VOLATILE-QUALIFIER` | ✅ CLOSED 2026-06-26 (c21, model B) — shipped: per-symbol/member `SymbolRecord.isVolatile` → `HirVolatileMap` (access-node keyed) → `MirInstFlags::Volatile` on EVERY user load/store — objects, struct/union MEMBERS (incl. sqlite's `volatile int isInterrupted`), `int * volatile p` pointer-objects, whole-volatile aggregate copies, volatile params. Pointer-to-volatile-POINTEE fail-loud-deferred → `D-CSUBSET-VOLATILE-POINTEE`. Indep plan-lock + review + audit CLEAN; §D negative pin genuine (disassembly-verified RED-on-disable); 444/444. [orig NEW c19, §B own cycle] — `volatile` is DELIBERATELY fail-loud-gated (`S_VolatileNotSupported` at every decl site; the struct-member position doesn't even parse it → `P0009`). sqlite3.c uses `volatile int isInterrupted;` (line 18852) → blocks the compile. ★ GOOD NEWS: the OPTIMIZER already fully respects a `MirInstFlags::Volatile` flag — DCE treats it as a side-effect root (dce.cpp:53), CSE/LICM/Mem2Reg all SKIP volatile loads/stores (cse.cpp:207, licm.cpp:283, mem2reg.cpp:255/284). So the backend is volatile-correct. The FRONTEND has NO volatile support: the type system doesn't track volatile-qualification, the flag is NEVER set, hence the gate. ★ CORRECTNESS-CRITICAL MULTI-SITE: supporting it = grammar (accept `volatile` everywhere incl. struct members; remove the `S_VolatileNotSupported` gates) + TYPE-QUALIFIER tracking + HIR→MIR threading to set `MirInstFlags::Volatile` on EVERY volatile access. A single missed access = a silent no-op = the optimizer eliminates a volatile read → silent miscompile (the exact bar violation). Funnel load/store lowering through ONE chokepoint that sets the flag from the lvalue's volatile-ness, or test every access form. NOT a no-op-accept (silent risk) and NOT keep-gated (blocks SQLite). Priority: **high** (re-probe blocker; own cycle — feature-dev + plan-lock + multi-site threading tests + a negative miscompile-pin: a volatile read the optimizer would fold iff the flag is missing). | Grammar accept + remove gates; thread volatile through the type system to the MIR `Volatile` flag at the load/store chokepoint; negative pin + per-form tests. Pin: `volatile int x; ... x; x;` keeps BOTH reads (not CSE'd/DCE'd). | plan 25 c19; `c-subset.lang.json` (gates + struct-member grammar); the type system; `src/hir/lowering` (load/store → `MirInstFlags::Volatile`); optimizer infra already done (`src/mir/mir_node.hpp:45`, `src/opt/passes/*`) |
| `D-LK-MACHO-BSS-ONLY-LINKEDIT-STRUCTURAL-PIN` | NEW (2026-06-26, p18 Cluster G c19). The macho unused-`.bss`-global `__LINKEDIT`-fileoff bug (fixed in c19: `macho.cpp` `linkeditFileOff` predicate `hasDataSeg`→`hasData`) is currently guarded only by the RUNTIME witness `examples/c-subset/preprocessor_positional_define` (its macho-arm64 arm, red-on-disable but ONLY on the `macos-latest` CI leg — macho can't run off-Mac). The bar wants ALSO a HOST-INDEPENDENT structural pin (red-on-disable on EVERY leg): a low-level test that emits a `__bss`-only exec via `macho::encodeExecDynamic` (build an `AssembledModule` with a single unused tentative-def global, no `__data`) and asserts `__LINKEDIT.fileoff != 0` (== the GOT-following offset). Defense-in-depth so a future `linkeditFileOff` regression is caught even when the macОS leg is unavailable. The fix itself IS verified (binary inspection: `__LINKEDIT` 0x0→0xc000) + the corpus caught the bug on c18's macОS leg. Priority: **normal** (the fix is verified + CI-guarded; this is defense-in-depth). | Add a `MachOExecWriter` test building a bss-only `AssembledModule` → assert `__LINKEDIT.fileoff` (via `macho_test_support.hpp::findSegment` + `readU64LE` at segOff+40) is non-zero + page-aligned after the GOT. | plan 25 c19; `tests/link/test_macho_writer.cpp`; `src/link/format/macho.cpp` (`encodeExecDynamic` `linkeditFileOff`) |
| `D-PP-IF-UNSIGNED-INTMAX` | FC14 named exclusion (2026-06-22): a `#if`/`#elif` controlling expression is evaluated in SIGNED int64 (the shared `applyBinaryInt` core), NOT C's `intmax_t`/`uintmax_t` (C 6.10.1p1).
| `D-PP-IF-UNSIGNED-INTMAX` | FC14 named exclusion (2026-06-22): a `#if`/`#elif` controlling expression is evaluated in SIGNED int64 (the shared `applyBinaryInt` core), NOT C's `intmax_t`/`uintmax_t` (C 6.10.1p1). An unsigned literal whose value exceeds `INT64_MAX` (e.g. `#if 0xFFFFFFFFFFFFFFFFu == -1`, true under C unsigned semantics) FAILS LOUD (`asInt64` → "not an integer constant") instead of evaluating with unsigned wraparound. NOT silently wrong — fail-loud. Priority: **normal** (rare in real `#if` usage). | Carry an unsigned/`uintmax` arm through the `#if` Pratt + the `const_eval_arith` fold (the arith core already has uint64 — thread the literal's signedness from the C 6.4.4.1 suffix/value ladder, mirroring the semantic literal typer). Pin: `#if 0xFFFFFFFFFFFFFFFFu > 0` takes the branch. | plan 23 FC14; `src/analysis/preprocess/pp_if_eval.cpp` (the I32/int64 literal decode + fold) |
| `D-PP-PASTE-OBJECT-LIKE` | ✅ **CLOSED 2026-06-24 (cycle d — FC15 paste residuals).** Object-like `##` now routes the object-like replacement through the SHARED `collapsePastes` chokepoint the function-like arm already uses (no duplicated paste logic); a genuine dangling `##` (`#define OBJ a ##`) still fails loud (`FC15ObjectLikeDanglingPasteFailsLoud`). Pin `FC15ObjectLikePasteYieldsOneToken`/`…Chain`/`…ProductRescanned` + corpus `preprocessor_paste_residuals` (MK3→add3, exit 42). Historical: FC15a named exclusion (2026-06-24): the `##` (token-paste) operator is implemented ONLY in FUNCTION-like macros (the `substitute` chokepoint, per the locked design). A `##` in an OBJECT-like macro replacement (`#define HW a ## b`, valid C 6.10.3.3 — paste applies to object- AND function-like) is NOT collapsed: the object-like expand path copies the replacement verbatim, so `a`, `##`, `b` pass through. NOT a silent miscompile — it FAILS LOUD at the parser (`##`=HashHashOp is not a valid expression operator → P0001), verified. Priority: **normal / trigger-gated** (rare — object-like paste; the common `CONCAT(a,b) a##b` is function-like and works). | Route the OBJECT-like replacement through `collapsePastes` too (object-like has no params/stringize, so only `##` between literal replacement tokens needs handling). Pin: `#define HW a ## b` + `HW` → the single token `ab`. | plan 23 FC15b; `src/analysis/preprocess/preprocessor.cpp` (object-like arm of `expand`); `tests/analysis/preprocess/test_preprocessor.cpp` |
| `D-PP-PASTE-PLACEMARKER` | ✅ **CLOSED 2026-06-24 (cycle d — FC15 paste residuals).** An EMPTY `##`-operand argument now becomes a PLACEMARKER (`bool placemarker` on `ExpToken`, stamped in Phase A by `stampArgOrPM`): `collapsePastes` resolves `pm ## X`→X, `X ## pm`→X, `pm ## pm`→pm, and drops survivors at its return (`run()`/`expandTokens` backstop). The genuine-dangling-`##` boundary check is UNCHANGED + fires first, so `J(x,)`→`x` while `#define B(a) a ##` still fails loud. Pins `FC15Placemarker{RightEmpty,LeftEmpty,BothEmpty,MidChain}` + `FC15PasteEmptyVaArgsIsPlacemarkerNotDangling`. Historical: FC15a named exclusion (2026-06-24): pasting with an EMPTY operand (C 6.10.3.3p2 placemarker: `a ## b` where `b`'s arg is empty yields `a`) is NOT supported. An empty `##` operand leaves the `##` dangling (no token on that side) → it is reported as a start/end paste error OR the `##` pastes against the wrong neighbor and re-tokenizes to >1 token → P_PreprocessorPaste. NOT a silent miscompile — fail-loud. Priority: **normal** (rare — empty macro args with `##`). | Model placemarker tokens: an empty `#`/`##` operand collapses to nothing, and `x ## <empty>` → `x` (and `<empty> ## <empty>` → nothing). Pin: `#define J(a,b) a ## b` + `J(x,)` → `x`. | plan 23 FC15b; `src/analysis/preprocess/preprocessor.cpp` `substitute`/`collapsePastes` |
| `D-PP-VARIADIC-GNU-COMMA-ELISION` | ✅ **CLOSED 2026-06-24 (cycle d — FC15 paste residuals).** The GNU `,##__VA_ARGS__` idiom is now CONFIG-GATED (`preprocess.variadicCommaElision`, c-subset opts in): in Phase A's `isVaArgsName` arm, when the `##` immediately precedes `__VA_ARGS__` AND the prior `items` token is the config-declared arg-separator, an EMPTY `__VA_ARGS__` drops both the separator and the `##` (`LOG("x")`→`f("x")`), a NON-empty one keeps the separator and emits the pre-expanded args with no paste (`LOG("x",1)`→`f("x",1)`). Separator matched by `isArgSeparator` (config KIND), `__VA_ARGS__` by `isVaArgsName` — never a hardcoded `,`/name. AGNOSTIC: flag default-false → standard placemarker (separator survives). Pins `FC15GnuCommaElision{EmptyVaArgs,NonEmptyVaArgs,IsConfigDriven}` + corpus. Historical: FC15a named exclusion (2026-06-24): the GNU `,##__VA_ARGS__` comma-elision extension is NOT implemented. FC15a delivers the standard `#` (stringize, C 6.10.3.2) + `##` (token-paste, C 6.10.3.3) operators, but a `, ## __VA_ARGS__` paste where `__VA_ARGS__` is EMPTY does NOT drop the preceding comma (GNU/C++20 `__VA_OPT__`-adjacent behavior): the literal comma stays (`f(a,)`), and the `##` against an empty operand pastes the comma with nothing (a no-op paste → the lone comma survives, NOT a fail-loud — the comma `,` is a single valid token). NOT a silent miscompile (a stray trailing comma is a parse-visible artifact, not wrong code). Priority: **trigger-gated** (FC15b — a real corpus needing variadic logging `LOG(fmt, ...)` with zero trailing args, or `__VA_OPT__`). | Implement the GNU comma-elision special case: in `substitute`, when a `##` has a `__VA_ARGS__` right operand that expands to EMPTY and a `,` left operand, drop the comma (and the `##`) entirely. Consider also C23 `__VA_OPT__(...)`. Pin: `#define LOG(fmt, ...) f(fmt, ## __VA_ARGS__)` + `LOG("x")` → `f("x")` (no trailing comma); `LOG("x", 1)` → `f("x", 1)`. | plan 23 FC15b; `src/analysis/preprocess/preprocessor.cpp` `substitute`/`collapsePastes`; `tests/analysis/preprocess/test_preprocessor.cpp` |
| `D-PP-PRAGMA-OPERATOR-FORM` | FC15c named exclusion (2026-06-24, `#pragma` + `__has_include` + `__has_c_attribute`): the `_Pragma("...")` OPERATOR form (C 6.10.9) is NOT implemented. FC15c delivers the `#pragma` DIRECTIVE form (C 6.10.6 — consumed-and-dropped in `handleDirective`); `_Pragma` is a DISTINCT, EXPANSION-TIME operator (`_Pragma(string-literal)` destringizes its argument and processes it as if it were a `#pragma` line), so it can appear in a macro replacement and is resolved during macro expansion, not at directive scan. NOT a silent miscompile — a `_Pragma(...)` token sequence today expands as an ordinary identifier + call shape (or fails loud at the parser), never altering codegen. Priority: **normal / trigger-gated** (rare in hand-written C; common in library headers that wrap pragmas in macros). | Add a `_Pragma` operator (config `pragmaOperator` word, matched by TEXT like `defined`/`__has_include`): in the expander, recognize `_Pragma ( string-literal )`, destringize the literal (strip the `"`, un-escape `\"`/`\\`, C 6.10.9p1), and treat the result as a `#pragma` line — i.e. consume-and-drop it (DSS recognizes no pragma), so `_Pragma("once")` expands to nothing. Pin: `_Pragma("GCC optimize(\"O2\")")` in a replacement list expands to nothing (no error, no stray tokens). | plan 23 FC15c; `src/analysis/preprocess/preprocessor.cpp` (`expand`); `src/core/types/preprocess_config.hpp`; `tests/analysis/preprocess/test_preprocessor.cpp` |
| `D-PP-PRAGMA-RECOGNIZED-SEMANTICS` | FC15c named exclusion (2026-06-24, `#pragma`): specific `#pragma` EFFECTS beyond consume-ignore are NOT implemented. FC15c consumes-and-DROPS every `#pragma` line (C 6.10.6p2 explicitly licenses an implementation to ignore a pragma it does not recognize, so consume-drop is CONFORMING). The standard pragmas `#pragma STDC FP_CONTRACT`/`FENV_ACCESS`/`CX_LIMITED_RANGE` (C 6.10.6p2) and the ubiquitous `#pragma once` (header-guard idiom) currently have NO effect — `#pragma once` does not dedup a doubly-included header (the `#include` machinery's existing canonical-path cycle guard catches a CYCLE, but a non-cyclic double-include is re-spliced). NOT a silent miscompile — dropping a pragma is standard-permitted, and the FP/FENV pragmas only relax codegen latitude DSS does not currently take. Priority: **normal / trigger-gated** (closes when a real corpus needs `#pragma once` dedup or an `STDC` pragma's codegen effect). | Give the recognized pragmas effect: parse the pragma payload (after the consume scan), dispatch on a config-declared recognized-pragma set. `#pragma once` records the current file's canonical path in a once-set the `SynthBuilder` consults to SKIP a second splice; the `STDC` pragmas set a per-region float-eval/fenv flag the lowering honors. Pin: a header with `#pragma once` included twice is spliced ONCE; `#pragma STDC FP_CONTRACT OFF` disables FMA contraction in the following region. | plan 23 FC15c; `src/analysis/preprocess/preprocessor.cpp` (`SynthBuilder` splice + `handleDirective` pragma arm); `tests/analysis/preprocess/test_preprocessor.cpp` |
| `D-PP-LINE-DIRECTIVE` | FC15b named exclusion (2026-06-24, predefined macros): the `#line N` / `#line N "file"` directive (C 6.10.5) is NOT implemented. FC15b delivers the PREDEFINED macros (`__LINE__`/`__FILE__`/`__STDC__`/`__STDC_VERSION__`/`__STDC_HOSTED__`/`__DATE__`/`__TIME__`, C 6.10.8) — `__LINE__`/`__FILE__` resolve against the INVOCATION offset through the line-map to the PHYSICAL line / origin file name. A `#line` directive would OVERRIDE that presumed line number + file name for the tokens that follow, but until it lands `__LINE__`/`__FILE__` always report the PHYSICAL source line/name. NOT a silent miscompile — a `#line` directive currently falls through to the generic `P_PreprocessorUnsupported` fail-loud arm in `handleDirective` (an unsupported directive, never silently ignored); and `__LINE__`/`__FILE__` reporting the physical line/name (when no `#line` is in play, the universal case) is CORRECT, so this is a fail-SOFT gap that only bites a TU that actually uses `#line` (rare in hand-written C; common in generated code / `yacc`/`lex` output). Priority: **normal / trigger-gated** (closes when a real corpus needs `#line` — e.g. a generated lexer, or `__FILE__`/`__LINE__` accuracy under a `#line`-rewritten preamble). | Add a `#line` directive handler (config `lineDirective` word, mirroring `defineDirective` &c.): parse `#line <digit-sequence> [ "<filename>" ]`, then maintain a PRESUMED-position override (a per-region (line-delta, file-name) shift) that `materializePredefined`'s Line/File arms consult AHEAD of the raw line-map resolve. The line-map already gives the physical origin; `#line` layers a logical override on top. Pin: `#line 100 "gen.c"` then `__LINE__` on the next physical line → 100 (not the physical line), and `__FILE__` → `"gen.c"`. | plan 23 FC15b; `src/analysis/preprocess/preprocessor.cpp` (`handleDirective`, `materializePredefined`); `src/core/types/preprocess_config.hpp`; `tests/analysis/preprocess/test_preprocessor.cpp` |
| `D-CSUBSET-INCDEC-CONST-LVALUE` | **OPEN — pre-existing gap, fail-SOFT, normal (Cluster F item F1, 2026-06-24).** A `const`-qualified lvalue used as the operand of `++`/`--` (`const int x; x++;`) is NOT rejected — `classifyLvalue` / `simpleLvalue` do not model `const` today, so an `x++` on a `const` object lowers as an ordinary modifiable-lvalue read-modify-write. NOT introduced by F1: the SAME gap exists on plain assignment (`const int x; x = 5;`) and compound-assign (`x += 1;`), all of which route through the same `const`-unaware lvalue classification. F1's ++/-- lvalue guard DOES reject a manifest RVALUE operand (`5++`/`++5` → `S_IncDecNeedsModifiableLvalue`), but a `const` *lvalue* is addressable, so the shape guard admits it. Not a silent miscompile of VALUE (the increment is arithmetically correct); it is a missing CONSTRAINT diagnostic (C 6.5.2.4p1 / 6.5.16p2 require a MODIFIABLE lvalue). Priority: **normal**. | Add a modifiable-lvalue / `const`-qualifier check SHARED with assignment + compound-assign (the single lvalue-classification chokepoint), emitting `S_ConstViolation` (or `S_IncDecNeedsModifiableLvalue` for the ++/-- arm) when the lvalue's type carries the language's `const` qualifier. Requires the type lattice to carry a const-qualified marker the classifier can read. Pin: `const int x = 0; x++;` and `const int y = 0; y = 1;` both fail loud. | plan 23 Cluster F (F1); `src/hir/lowering/cst_to_hir.cpp` (`classifyLvalue`/`classifyIncDecLvalue`/`simpleLvalue` + the assignment path); `tests/hir/test_hir_lowering_c_subset.cpp` |
| `D-PP-VA-OPT` | FC15-paste-residuals deferral (2026-06-24, cycle d): the C23 `__VA_OPT__(content)` construct (C23 6.10.3.1) is NOT implemented. It is a DISTINCT, more general mechanism than the GNU `,##__VA_ARGS__` comma-elision THIS cycle delivered: `__VA_OPT__(content)` emits `content` (an arbitrary balanced-paren token sequence) iff `__VA_ARGS__` expands to a non-empty list, and elides the whole `__VA_OPT__(...)` otherwise — it requires recognizing `__VA_OPT__` + balanced-paren body extraction + conditional emission, none of which the placemarker/comma-elision machinery does. PRE-EXISTING gap (not introduced this cycle): a `__VA_OPT__` token in a replacement is left as an ORDINARY identifier (so a user writing it gets the literal `__VA_OPT__ ( … )` tokens through, a parse-visible wrong result, NOT a silent miscompile and NOT a fail-loud). Priority: **normal / trigger-gated** (closes when a real corpus / SQLite needs `__VA_OPT__`; the GNU `,##__VA_ARGS__` idiom — far more common in existing C — is already covered). | Add a config `vaOptOperator` word (matched by TEXT like `defined`/`__has_include`); in `substitute`, recognize `__VA_OPT__ ( <balanced> )`, and emit the `<balanced>` content (pre-expanded) iff `rawVaArgs` is non-empty, else nothing. Pin: `#define F(...) g(__VA_OPT__(0,) __VA_ARGS__)` + `F()` → `g()`, `F(1)` → `g(0, 1)`. | plan 23 FC15; `src/analysis/preprocess/preprocessor.cpp` (`substitute`); `src/core/types/preprocess_config.hpp`; `tests/analysis/preprocess/test_preprocessor.cpp` |
| `D-CSUBSET-FLOAT-NEG-ENCODING` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c78; §B USER folded into c78) — fneg realized capability-dispatched: arm64 native FNEG (0x1E614000/0x1E214000); x86 `xorpd/xorps xmm,[rip+16B-signmask]` (a minted 16-byte rodata sign-mask + the RipRelDisp32 slot). Objdump-confirmed 16-aligned masks on all 3 legs INCL. pe64 (4096 PE section + per-item 16-offset; the USER's PE-align concern refuted); signed-zero IEEE-correct (-（+0.0)=-0.0). Witness `examples/c-subset/float_negate`=42 (RED-ON-DISABLE → A_NoEncodingDeclared). Was a Cluster-F F3 deferral (2026-06-24, cycle 7): float unary minus `-x` AND a negative float LITERAL (`-42.0`) lower to the MIR `fneg` op, which has NO encoding declared — `A_NoEncodingDeclared` (x86) / `L_RequiredLirOpcodeMissing` (arm64). FAIL-LOUD (the function is dropped with a positioned diagnostic; NEVER a silent miscompile). NOT a simple mirror of fadd/fmul/fsub: x86 float negate is not a 2-reg ALU op — it is `XORPS xmm, signmask` (needs a `0x8000…0` sign-bit mask constant in rodata) OR `0.0 - x` (an FSUB from a materialized zero); arm64 FNEG IS a single 1-source FP instruction (`FNEG Dd, Dn` = 0x1E614000 / S-form 0x1E214000). Surfaced when the F3 `float_mul_sub` corpus used `-42.0` literals (the corpus was rewritten to avoid it via `(a-b)-(b-a)`). Priority: **normal / trigger-gated** (closes when a real corpus / SQLite needs float negation — common in real C). | x86: add an `fneg` encoding (an XORPS-with-rodata-signmask sequence, OR lower MIR FNeg → `0.0 - x` FSUB at MIR→LIR — the simpler, arguably-better long-term path since it reuses the now-encoded fsub). arm64: add an `fneg` opcode row (FNEG single-source, mirror the `fmov`/`fpcvt` 1-source fixed32 shape). Pin: `double f(double x){return -x;}` compiles + `-x` is runtime-correct on both targets; byte-pin the encoding. | plan 23 Cluster F; `src/dss-config/targets/{x86_64,arm64}.target.json`; possibly `src/lir/lowering/mir_to_lir.cpp` (FNeg→FSub lowering) |

---

## Allowlist (code-internal pins, NOT deferred work)

Anchor-shaped strings in `src/` that are **not** deferred-work markers and
should be **ignored** by the CI guard. Add a row when an in-code constant
or diagnostic-message identifier matches the `D-*` regex but isn't a
deferred-work anchor.

| Pattern | Reason |
|---|---|
| `D-MERGE-GLOBAL-INITFUNC` | Cycle 25 Stage B code-internal INVARIANT pin (`src/mir/merge/mir_merge.cpp`), NOT deferred work. Cited in the fail-loud abort that fires if a merged global's `initFunc` MirFuncId was not cloned into the merged module (`plan.funcMerged` miss). The invariant always holds for valid input — a global's module-init function is a defined function in the same CU, which the merge always clones (an init function is never a cross-CU weak loser). The abort is defense-in-depth: a breach means the planning pass dropped a function a global still references (a structural contradiction), so failing loud beats emitting a global whose initializer points at a stale/absent func. No closing work — it is a permanent guard, like the other `mergeFatal` invariant aborts. |
| `D-CSUBSET-COMPUTED-GOTO` | ✅ **CLOSED 2026-06-23 (FC-deferral close-out c9, branch p14 — USER OPTED IN, overriding the named-exclusion).** GNU computed goto (`&&label` label-address + `goto *p` indirect branch) implemented END-TO-END, correct under the full release pipeline. §B-signed-off (full-mechanism-correct-under-opt + synthetic-per-block-symbol reloc); independently plan-locked (MF-1 CFG-correctness verified by construction; 8 opt passes cleared) + independently pr-reviewed. AS-BUILT: grammar (a dedicated `labelAddressExpr` operand rule `[AndAndOp, Identifier]` — NOT a prefix-group `&&`, because the parser rejects `&<undeclared-id>`; a 2-alt `gotoStmt`) + HIR `LabelAddressOf`/`IndirectGotoStmt` + MIR `IndirectBr` terminator (variadic successors = ALL address-taken blocks, like Switch) + `BlockAddress` value op (side-effecting → DCE-stable; `isBlockAddressTaken` DERIVED from BlockAddress ops, drift-proof) + the clone arms at mir_merge + mir_rebuild_helper + a SimplifyCfg guard (don't fold/merge an address-taken TARGET block) + Inlining refusal + LIR/asm (x86 NEW `jmp FF /4` / arm64 NEW `BR Xn` 0xD61F0000; BlockAddress→lea/adrp+add to a synthetic per-block symbol) + a NEW format-neutral linker interior-block-symbol VA (`interior_block_symbol_va.hpp`, ELF/PE/Mach-O, `SymbolKind::BlockLocal`). Witnessed: `examples/c-subset/computed_goto` (threaded interpreter exit 17) + `computed_goto_inline_host` (exit 11) on x86-PE + WSL arm64-qemu, under BOTH baseline AND release pipelines; red-on-disable pins (IndirectBr-successors-truncated, SimplifyCfg-address-taken-fold, inline-into-host-routing). 369→370 ctest. **The pr-review caught a reachable silent miscompile the plan-lock + build BOTH missed**: the `MultiBlockInliner`'s hand-rolled caller-host emit (a 3rd clone family) mis-copied a BlockAddress payload + aborted on IndirectBr when inlining INTO a computed-goto host — folded by routing computed-goto hosts to the single-block rebuilder (correct remap), red-on-disable demonstrated. **NAMED FOLLOW-UP (open):** `D-CG-INLINE-MULTIBLOCK-INTO-COMPUTED-GOTO-HOST`. | (closed — c9 branch p14) | grammar + HIR + MIR + opt + lir + asm + link (interior_block_symbol_va.hpp) / plan-23 full-C arc |
| `D-CG-INLINE-MULTIBLOCK-INTO-COMPUTED-GOTO-HOST` | **OPEN, OPT, trigger-gated (FC-deferral close-out c9, 2026-06-23).** A function that ITSELF contains a computed goto (`BlockAddress`/`IndirectBr`) is currently routed through the SINGLE-block `MirFunctionRebuilder` inliner (which remaps both correctly), NEVER the `MultiBlockInliner` — so a MULTI-block callee is NOT inlined into a computed-goto host (single-block callees still are). Correct + fail-safe (no miscompile); the only cost is a missed optimization. Closing work (only if a real perf consumer needs it): add `BlockAddress` (block-id payload remap via `blockMap_`) + `IndirectBr` (successor remap) arms to `MultiBlockInliner::emitCallerInst`/`emitTerminator` (mirroring the rebuild-helper arms), + a test inlining a multi-block helper INTO a computed-goto host under the release pipeline. Trigger: a benchmark showing the missed inline matters. | Add the 2 caller-host emit arms to MultiBlockInliner + a red-on-disable test. Priority: **opt (trigger-gated)**. | `src/opt/passes/inlining.cpp` MultiBlockInliner (the `functionHasComputedGoto` routing guard) + `D-CSUBSET-COMPUTED-GOTO` |
| `D-CSUBSET-LABEL-BEFORE-CASE` | ✅ **CLOSED 2026-06-23 (FC-deferral close-out c10, branch p14, 371 ctest + corpus `label_before_case` exit 25 PE/ELF/qemu baseline+release).** AS-BUILT = Option A (user-chosen — label NESTS over case, C-faithful): a new `caseStmt = [caseLabel, statement]` grammar form added to the `statement` alt + `switchBodyItem` made **speculative** (plan-lock CRITICAL catch: a non-speculative alt would trip the loader's `C_AmbiguousAlternatives` gate at CONFIG LOAD once {Case,Default} enters FIRST(statement) — c-subset would not load AT ALL) + a `caseStmtRule` config pointer (AGNOSTIC; toy/tsql don't declare it → `.valid()`==false → the new arms no-op, exactly like `caseLabelRule`). `lowerSwitch` peels a (multi-)label-wrapped `caseStmt` into arms via an iterative chain (handles `foo: case 1: case 2: stmt`), producing HIR **identical** to the long-supported `case 1: foo: stmt` — the label STAYS a real `LabelStmt` node, so the CST pre-scan + `goto`-INTO-switch resolution + duplicate/undefined-label checks all work UNCHANGED. A `caseStmt` reached as a non-direct-switch-item fails loud via NEW `S_CaseLabelNotInSwitch` (0xE023). Pins (all red-on-disable): HIR-equivalence (`foo: case 1: S` ≡ `case 1: foo: S`), bare-case-stays-flat (Finding 2), outside-switch guard; corpus `goto`s INTO the switch at the label → exit 25 baseline+release. Residual: [[D-CSUBSET-CASE-LABEL-IN-NESTED-BLOCK]]. ORIGINALLY: NAMED LIMITATION (FC5, 2026-06-15) — a goto-label immediately preceding a `case`/`default` label (`switch(x){ foo: case 1: … }`) is legal-but-rare ISO C that FC5 did NOT parse: `labelStmt`'s body is a `statement`, while `caseLabel` is reachable only via `switchBodyItem`, so `foo: case 1:` is a clean fail-loud parse error (P0001 "expected EndStatement", no binary — NEVER a silent mis-parse). All OTHER label positions (before any ordinary statement, forward/backward goto, goto into/out of loops, label on an empty statement, goto-over-initialization) are fully supported. Priority: **normal** (rare form; fails loud). | The clean fix is the case-labels-as-statements switch-grammar refactor — model `case N : stmt` / `default : stmt` as `statement` forms (so labels nest uniformly: `foo: case 1: stmt` = `label(foo, case(1, stmt))`) and re-point `lowerSwitch` at the nested shape. That is its own piece of work (touches the switch lowering's flat case-item consumption), disproportionate to bolt onto FC5 — hence the named blocker. Pin: `switch(x){ foo: case 1: return 1; }` parses + runs. | plan-23 full-C arc (FC5); c-subset `labelStmt`/`switchBodyItem`/`caseLabel`/`caseStmt`/`caseStmtRule`; `src/hir/lowering/cst_to_hir.cpp` lowerSwitch + the lowerStmt CaseStmt guard |
| `D-CSUBSET-CASE-LABEL-IN-NESTED-BLOCK` | **OPEN — NAMED LIMITATION, fail-loud, rare (FC-deferral close-out c10, 2026-06-23).** A `case`/`default` label nested inside an INNER block of a switch arm (`switch(x){ case 1: { foo: case 2: y(); } }`) is not associated with the switch by the dss flat-switch model — `lowerSwitch` groups only DIRECT switch-body items, so a case in a nested block reaches `lowerStmt` and fails loud with `S_CaseLabelNotInSwitch` (C 6.8.1). This is a PRE-EXISTING limit of the flat model (it was P0001 before c10 too — `case` was not a `statement`), surfaced (not introduced) by the [[D-CSUBSET-LABEL-BEFORE-CASE]] grammar work; correct + fail-loud, never a miscompile. Priority: **normal** (rare; fails loud). | Closing it = lowerSwitch recursing into nested blocks to harvest their case labels (Duff's-device territory) — a switch-lowering generalization, only if a real consumer needs it. Pin: `switch(x){case 1:{foo: case 2: return 2;}}` runs. | `src/hir/lowering/cst_to_hir.cpp` lowerSwitch (direct-switch-body-item grouping) + the `S_CaseLabelNotInSwitch` guard |
| `D-CSUBSET-VOLATILE-POINTEE` | ✅ **CLOSED 2026-06-27 (p19 Cluster G c27) — model A: `volatile` as a TYPE QUALIFIER.** ★ §B (user, 2026-06-27): **Design B** — a qualified `TypeId` (`TypeKind::VolatileQual`, the registry's anticipated "model A") — chosen OVER a Ptr-pointee bit. TRANSPARENT wrapper: `kind()`/`operands()`/`scalars()` route through `materialId_` so the ~128 kind-dispatch sites are correct BY CONSTRUCTION (the wrapper is observable only via `isVolatileQualified`/`get()`); idempotent; `const` gets NO wrapper (stays ignored for type identity). Accesses set `MirInstFlags::Volatile` via a new `volatileFlagForType` ORed with the c21 `volatileFlagFor` at EVERY access form. c21's per-symbol `SymbolRecord.isVolatile` RETIRED → migrated to the type (fixed a c21 double-count: `volatile int *p` wrongly marked the pointer OBJECT volatile). The two `S_VolatilePointeeNotSupported` (0xE025) reject sites DELETED + retired from kUnsuppressable (enum kept for ordinal stability). ★★ The independent post-build AUDIT CAUGHT a silent-miscompile the local Windows suite missed: member/index/by-value access of a volatile CONTAINER (`volatile struct S *p; p->a` — the sqlite Kahan-summation `volatile SumCtx*` + WAL `volatile WalCkptInfo*` shapes) dropped the flag — a REGRESSION from c21's loud reject to silently-wrong. FIXED by type-propagation: a member/index access of a volatile container yields a volatile-qualified RESULT type, covering nested chains + complex-lvalue compound-assign (`pSum->rErr += r`) by-construction, + explicit by-value-arg/struct-return/brace-init threading; 10 per-form red-on-disable container pins. A reintern silent-drop (transparent `kind()` would reintern `volatile T` as `T` cross-CU) also caught+fixed. WITNESS: 29 MIR volatile pins (red-on-disable, exact-count) + runtime `volatile_pointee_cse` (CSE folds iff the flag is missing: exit 3 vs 2, WSL-verified) + `volatile_pointee`/`volatile_pointee_member` (the c21 reject examples INVERTED to positives); 449→450. ★ LESSON: a multi-site volatile contract needs EVERY container-access form tested — the c21 lesson recurred (the local Windows suite was green-but-wrong; only the WSL runtime pin + the audit's per-form MIR probes caught the container hole). [orig:] **OPEN — NAMED LIMITATION, fail-loud, trigger-gated (c21, D-CSUBSET-VOLATILE-QUALIFIER, 2026-06-26).** c21 shipped `volatile` via **model B** (per-symbol/member `SymbolRecord.isVolatile` → `HirVolatileMap` on the access node → `MirInstFlags::Volatile` on every user Load/Store; the optimizer's DCE/CSE/Mem2Reg/LICM already honor the flag). Model B handles the OBJECT / struct-MEMBER / pointer-OBJECT (`int * volatile p`) forms — everything the SQLite frontier (`volatile int isInterrupted;`) needs. It CANNOT express a pointer-to-volatile **POINTEE** (`volatile int *p; *p`): there the volatility rides the DEREFERENCED access, which requires type-level cv-qualification (model A — a cv-qualified `TypeId` threaded through the interner + type-equality + every TypeId consumer + the optimizer's type inspection, a large core change). Building model A NOW, before a re-probe proves SQLite needs pointer-to-volatile, would be a speculative over-build, so the pointee form is REJECTED LOUD (`S_VolatilePointeeNotSupported`, 0xE025, unsuppressable) at BOTH type-name sites — the per-declarator typing arm (head `volatile` + a declarator star) AND the co-located pointer arm (head `volatile` before the first star) — making the fail-loud COMPLETE BY CONSTRUCTION (no pointer-to-volatile-pointee TYPE can be built ⇒ no `Deref` can silently drop the flag, the silent miscompile this gate forbids). | **Trigger:** a SQLite re-probe (or any real consumer) shows a genuine need for pointer-to-volatile-pointee. **Closing work (model A):** add cv-qualification to the type lattice (a const/volatile-qualified `TypeId`), thread it through type-equality + every TypeId consumer + the optimizer's type-inspection, and lower a volatile-pointee `Deref` to a flagged Load; then DELETE the two `S_VolatilePointeeNotSupported` reject sites (and the diagnostic + its kUnsuppressable membership). Pin: `volatile int *p; int a = *p; int b = *p;` keeps BOTH loads under the release pipeline (red-on-disable, exit-code witness mirroring `volatile_member`). | `D-CSUBSET-VOLATILE-QUALIFIER` (the c21 model-B parent); `src/core/types/parse_diagnostic.hpp` `S_VolatilePointeeNotSupported`; `src/analysis/semantic/semantic_analyzer.cpp` (the two reject arms); plan 23 full-C arc |
| `D-CSUBSET-STRUCT-MULTI-DECLARATOR` | ✅ **CLOSED 2026-06-26 (p18 Cluster G c23).** A struct/union body admitted only ONE declarator per member (`int *a, *b;` → P0009 "got ';'"; sqlite3.c:15516 `struct HashElem { HashElem *next, *prev; ... };` the frontier). C 6.7.2.1 allows a comma-separated declarator LIST sharing one base type. FIX = REUSE the existing `declaratorList` mechanism (the engine's `collectDeclarators`/`declaratorNameNode` role dispatch that top-level/local decls already use): new grammar rules `structMemberDeclarator` = `{declarator?}{bitfieldDeclSuffix?}` (the suffix moved INSIDE each slot so `int a:3,b:5;` resolves per-slot) + `structMemberDeclaratorList`; `structField`/`unionField` head + `{structMemberDeclaratorList?}` + `EndStatement`; rows `declarator:1` → `declaratorList:1`; two OPTIONAL `DeclaratorConfig` roles `memberDeclaratorRule`/`memberListRule` (nullopt for toy/tsql → zero change). AGNOSTIC (config rows + the generic declarator engine; no lang identity branch). | ✅ THE CRUX (silent layout miscompile, handled by-construction): each declarator gets its OWN pointer/array/bitfield suffix; only the HEAD base type is shared — `int *a, b;` = Ptr<int> (a) + int (b). 3 mandatory plan-lock fixes folded: **FIX 1** (the feature is DEAD without it — breaks single-declarator too) — added the `memberDeclaratorRule` arm to `declaratorDeclaredType` (`semantic_analyzer.cpp`), mirroring the initDeclaratorRule arm, else every field type → InvalidType → struct never composes; the head base TypeId is taken BY VALUE into the append-only interner so no per-slot star leaks. **FIX 2** — added `memberDeclaratorRule`/`memberListRule` to the CLOSED `kDeclaratorKeys` array + OPTIONAL parse in `grammar_schema_json.cpp` (else c-subset config fails to load). **FIX 3** — the `int *a, b;` test asserts per-field TYPES distinctly (a=Ptr, b=I32) not just sizeof (==16 is ambiguous). THE ONE SEMANTIC CHANGE — the bitfield resolve search root moved `node`→`dNode` (per-slot) so widths resolve independently; the anonymous path stays from `node`. WITNESS: corpus `struct_multi_member` (`struct P{int *a,b;}; struct Q{int x,y[2],*z;}` write+read each field → exit 42; pe-native + elf/macho/arm64 release arm) + the per-slot/regression/sqlite-repro semantic tests + 445→446 ctest (the new corpus). MINOR pin: `D-CSUBSET-MIXED-NAMED-ANON-BITFIELD`. ALSO surfaced a PRE-EXISTING orthogonal limitation `D-CSUBSET-SELF-REFERENTIAL-STRUCT` (an inline `struct N { struct N *next; }` self-reference fails loud S_UnknownType — the tag binds after its body's fields type-resolve; reproduces single-declarator too, so NOT caused by c23). The grammar change (member stars moved out of the head into per-declarator) also re-routed the pre-existing pointer-to-volatile-POINTEE struct-member reject from the co-located head arm to the per-declarator arm (now positioned on `*p`, no spurious S_UnknownType cascade — a cleaner diagnostic; reject stays loud); updated `volatile_member_error` golden + the MIR `StructMemberPointeeVolatileFailsLoudViaPerDeclaratorArm` test. | p18 Cluster G c23; cited in `src/dss-config/sources/c-subset.lang.json` (structField/unionField/structMemberDeclarator*/declarators block) + `src/core/types/semantic_config.hpp` (DeclaratorConfig roles) + `src/core/types/declarator_walk.hpp` (walk arms) + `src/analysis/semantic/semantic_analyzer.cpp` (FIX 1 + bitfield root) + `src/core/types/grammar_schema_json.cpp` (FIX 2); supersedes the single-declarator `D-CSUBSET-STRUCT-MEMBER-DECLARATOR` (c10). |
| `D-CSUBSET-MIXED-NAMED-ANON-BITFIELD` | **OPEN — NAMED LIMITATION, fail-aware (not silent), rare, off the SQLite frontier (p18 Cluster G c23, 2026-06-26).** A struct/union member list that MIXES a NAMED bit-field with a TRAILING ANONYMOUS bit-field in the SAME declaration (`struct S { int a : 3, : 0; };` — `a` named, `: 0` anonymous) drops the trailing anonymous slot: the named slot sets `resolvedNamed = true`, which SKIPS the post-loop anonymous-field path (`semantic_analyzer.cpp` ~2435, gated on `!resolvedNamed`). This is a PRE-EXISTING single-anonymous assumption (the c10 anonymous path was written for a field whose SOLE slot is anonymous), surfaced — not introduced — by c23's multi-slot member list. NOT a silent miscompile in practice: the anonymous zero-width bit-field is a layout PADDING/alignment-break hint; dropping it can only UNDER-pad (a layout difference), and the common single-anonymous and all-named forms are correct. Off the SQLite frontier (sqlite3.c does not use mixed named+trailing-anon bit-fields in one declaration). Priority: **minor (trigger-gated).** | **Trigger:** a real consumer needs a mixed named + trailing-anonymous bit-field declaration to lay out correctly. **Closing work:** make the anonymous-field path per-SLOT (iterate the member-list slots whose inner declarator is absent but whose own `bitfieldDeclSuffix` is present, minting an `<anon:…>` symbol for EACH, instead of the single post-loop `!resolvedNamed` block) — i.e. fold the anonymous handling INTO the per-`dNode` loop keyed on the slot's own absent-declarator+present-suffix shape. Pin: `struct S { int a : 3, : 0; int b; };` lays `b` out at the post-zero-width-break alignment (red-on-disable vs the dropped-slot layout). | `D-CSUBSET-STRUCT-MULTI-DECLARATOR` (the c23 parent); `src/analysis/semantic/semantic_analyzer.cpp` (the `!resolvedNamed` anonymous-field block); plan 23 full-C arc |
| `D-CSUBSET-SELF-REFERENTIAL-STRUCT` | ✅ **CLOSED 2026-06-27 (p18 Cluster G c24) via NOMINAL composite typing.** ★ §B DECISION (user, 2026-06-26): nominal typing for composites — "100% config driven, no workaround, best long term" — chosen OVER an incomplete-sentinel workaround. FIX: struct/union types are now NOMINAL — identity = (kind, name, DECL-SITE/scope-id), NOT structural field-shape; fields live in an IMMUTABLE side-table (`compositeFields_`) that `operands()`/`scalars()` REDIRECT to for composites (so ~18 consumers unchanged); Pass-1 forward-mints ALL tags pre-order → a self-ref field resolves to the nominal TypeId (incomplete), completed in Pass-1.5. The minimal `struct N { struct N *next; }` compiles (next=Ptr<N>, sizeof 16) + the full `struct sqlite3` parses. ★ The plan-lock REJECTED the FIRST blueprint (name-only identity = silent miscompile for block-scoped same-name structs + breaks `test_type_interner.cpp` StructIsNominalAndStructural; record back-patch violates the append-only interner `arena_container.hpp:62`; frame-open parse-fix infeasible) — shipped the CORRECTED design: decl-site identity + side-table + MID-FRAME tag record (the REAL P0009 fix — `binder_sketch` recorded tags only at frame-CLOSE, so mid-body the decl-vs-expr oracle saw `sqlite3` as Unknown → multiply mis-parse; NOT lookahead) + explicit incomplete-FLAG (not operands-empty: `struct E{}` is a legal COMPLETE zero-field struct) + cycle-safe reintern (memo-before-recursion). Fail-loud: a DIRECT non-pointer incomplete member → `S_IncompleteTypeMember` (0xE026, in kUnsuppressableCodes); sizeof-incomplete → no layout. Indep plan-lock + review + audit all VERIFIED-CLEAN (both rebuilt + empirically tested every silent-miscompile surface incl. the descriptor/extern identity surface = one TypeId). WITNESS: corpus `struct_self_ref` (linked-list build+traverse → exit 42) + `struct_block_scope_distinct` (two different-layout same-name BLOCK-SCOPED structs stay DISTINCT → exit 11, the identity-safety pin) + self-ref/typedef/mutually-recursive/incomplete-member tests; 446→448. Mutually-recursive via implicit `struct B*` works (Pass-1 pre-order mints both tags — no forward decl needed); a BARE `struct B;` statement is the separate deferral `D-CSUBSET-FORWARD-STRUCT-DECLARATION`. [orig:] An INLINE self-referential struct/union-tag pointer member — `struct N { struct N *next; ... };` (and the SQLite `struct HashElem { struct HashElem *next, *prev; ... };`) — fails loud with `S_UnknownType` on the inner `struct N`. ROOT: the composite TAG (`N`) is minted/bound in Pass 1 at the struct-declaration node (`semantic_analyzer.cpp` ~2007, `floatToNamespaceScope`), but the post-order semantic walk visits the BODY's field declarations BEFORE the parent struct node, so when the field `struct N *next` resolves its head tag, `N` is not yet bound → unknown type. INDEPENDENT of declarator mode: it reproduces identically for a SINGLE declarator (`struct N { struct N *next; }`) AND a multi-declarator list, so c23 (multi-declarator members) neither caused it nor is blocked by it — a multi-declarator pointer pair to a SEPARATELY-DEFINED tag (`struct T{...}; struct S { struct T *a, *b; };`) composes correctly (proven by `MultiMemberSqliteHashElemRepro`). Correct + fail-loud (a clean `S_UnknownType`, never a silent miscompile). Off the immediate c23 scope; on the SQLite path (HashElem is self-referential), so a re-probe will need it. Priority: **normal (re-probe blocker for the linked-list/tree shapes).** | **Trigger:** a SQLite re-probe (or any real consumer) needs an inline self-referential record. **Closing work:** a Pass-1 FORWARD-BIND tag pre-pass — bind every composite TAG into its namespace scope BEFORE descending its body (or a two-phase tag resolution: declare-tag-then-resolve-fields), so a field's reference to the enclosing (or a mutually-recursive) tag resolves to the incomplete type. An incomplete struct pointer is legal C (a pointer to an incomplete type has known size); the field type is `Ptr<incomplete N>`, completed when the body finishes. Pin: `struct N { struct N *next; int v; };` composes (next = Ptr<struct N>) — flip `SelfReferentialStructStillFailsLoudPreExisting` (currently expects `S_UnknownType`). | `D-CSUBSET-STRUCT-MULTI-DECLARATOR` (surfaced it); `D-CSUBSET-STRUCT-MEMBER-DECLARATOR` (c10, the field substrate); `src/analysis/semantic/semantic_analyzer.cpp` (Pass-1 tag bind order); plan 23 full-C arc |
| `D-CSUBSET-UNIFIED-COMPOSITE-SPECIFIER` | ✅ **CLOSED 2026-06-27 (p18 Cluster G c25) — the D1 grammar-unify + dual-mode binder.** ROOT (the c25 frontier): `struct sqlite3` (and any large struct/union/enum BODY) failed P0009 because the body parsed INSIDE the SPECULATIVE probe that disambiguated body-vs-ref (the `XxxSpecifierBody | XxxTypeRef` pair in 4 declaration-head alts, `lookahead 256` ⇒ a 4096-token budget) — a large member list exhausted the budget → the body-alt failed → orphan `};` → P0009. NOT a member COUNT limit; a TOKEN-budget cliff (a 130-member control parsed; a mixed `struct sqlite3` hit it at ~80 members). ★ §B-approved **D1** (the rejected D2 was a FOLLOW-prune — empirically unsound: `BlockOpen ∈ FOLLOW(structTypeRef)` via a struct-returning fn def, so no prefix prune can drop the ref candidate). FIX: UNIFY each pair into ONE rule `XxxSpec = Kw {opt Identifier} {opt XxxBody}` (mirrors C's `struct-or-union-specifier`) in the 4 body-bearing alts (topLevelCompositeSpec / typeSpecifierForDecl / typedefHead / typeBaseAllowingStruct); now the SOLE candidate keyed on its lead keyword → the parser's EXISTING LL(k) unique-production DIRECT DESCENT (`parser.cpp` ~1804-1844) parses it NON-speculatively, so the member body has NO budget. Body-vs-ref becomes an internal 1-token (`BlockOpen`) decision via `{optional XxxBody}`. The BINDER is DUAL-MODE on the same node via a NEW generic facet `definesWhenChild: "<bodyRule>"` (`DeclarationRule.definesWhenChildRule`, child-RULE-keyed — agnostic, mirrors `kindByChild`): body PRESENT ⇒ the EXACT former define path (mint type / bind tag in the Tag namespace / open scope / compose fields from the XxxBody subtree — Pass-1.5 walks the spec's scope, not direct children, so the extra nesting is transparent); body ABSENT ⇒ the EXACT former `XxxTypeRef` tag-reference path (a PAIRED `references[]` row, isTagReference; the loader cross-validates the paired row exists). Gated at every define site (Pass-1 mint+scope-open, Pass-1.5 type-resolve, the type-position inline-type arm → falls through to the tag-ref arm, the Pass-2 reference walker skips a body-present definition, and `cst_to_hir.findCompositeSpecifierIn` requires the body child). `structTypeRef`/`unionTypeRef`/`enumTypeRef` KEPT AS-IS for the ref-only param/cast positions (declHeadForParam / castTypeBase) — minimal blast radius. The 3 `XxxSpecifierBody` rules RETIRED (folded). FULL struct+union+enum this cycle. **NOTE (behavior delta, intentional + still-loud):** a bare `struct S;` (no body, no object) PREVIOUSLY did not parse (a parse error); it now PARSES as a tag REFERENCE and fails loud with `S_UnknownType` (undefined tag) + `S_DeclarationDeclaresNothing` at HIR lowering — still loud, never silent; minting an incomplete tag from `struct S;` stays the separate deferral `D-CSUBSET-FORWARD-STRUCT-DECLARATION`. WITNESS: `LargeStructBody`/`LargeUnionBody`/`LargeEnumBody` (1500-member, GREEN) + `MixedLargeStructBody` (nested anon struct/union + fn-ptr + bitfield + array + multi-declarator past budget) + the parser-shape define-vs-ref pin + 10 semantic dual-mode pins (struct/union/enum define-mints / ref-resolves / member-access-types / undefined-tag-S_UnknownType / redefinition-S_RedeclaredSymbol / anonymous-typedef / nested-inline-body) + the HIR forward-decl + with-object pins; the retired-rule-absence + new-rule presence grammar pins. | (closed — the unify IS the fix) | p18 Cluster G c25; `src/dss-config/sources/c-subset.lang.json` (`structSpec`/`unionSpec`/`enumSpec` + `structBody`/`unionBody`/`enumBody` + the 4 alts + the dual-mode declarations/references/scopes rows); `src/core/types/semantic_config.hpp` (`DeclarationRule.definesWhenChildRule`); `src/core/types/grammar_schema_json.cpp` (load + paired-reference cross-validation); `src/analysis/semantic/semantic_analyzer.cpp` (`isDefinitionAtNode`/`nodeHasVisibleChildOfRule` + the 4 gated sites); `src/hir/lowering/cst_to_hir.cpp` (`compositeSpecifierIsDefinition`); `D-CSUBSET-SELF-REFERENTIAL-STRUCT` (the c24 nominal substrate it builds on); plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-LOCAL-TYPE-DEFINITION` | ✅ **CLOSED 2026-06-27 (p19 Cluster G c28) — the LOCAL twin of c25's no-declarator top-level path.** ROOT (the c28 frontier): a BLOCK-SCOPED struct/union/enum DEFINITION with NO declarator — `struct Sublist { int nList; ht_slot *aList; };` as a STATEMENT inside a function (sqlite3.c:68508, `walMergesort`) — failed P0009 at the `};`. `topLevelDecl` made `initDeclaratorList` OPTIONAL (so a bare top-level `struct S { … };` defines a type with no declarator) but the LOCAL `varDecl` REQUIRED the list, so a bare local type-definition statement had no grammar production. FIX (GRAMMAR-mostly, REUSE c25): make `varDecl`'s `initDeclaratorList` OPTIONAL in BOTH branches (the storage-class-led `localDeclSpecifiers declHead …` and the keyword/qualifier-led `kwDeclHead …`) — a local `struct S { … };` routes through `kwDeclHead` (Struct/Union/EnumKeyword-led → the unified c25 `structSpec`/`unionSpec`/`enumSpec` DEFINES the type via `definesWhenChild`, minting the tag into the ENCLOSING BLOCK scope), takes NO declarator, ends at `EndStatement`. `identVarDecl`'s list is DELIBERATELY left REQUIRED (a bare typedef-name `T;` must stay an expression statement, NOT a no-declarator decl — the struct/union/enum definition is keyword-led so only `varDecl` needs the optional list). BLOCK-SCOPING is automatic: the `structSpec` define machinery binds the tag into `current` (the enclosing scope = the BLOCK scope during a body walk) via the c24 nominal decl-site identity, so a local `struct S { … }` does NOT leak to an outer/sibling scope and a same-name outer `struct S` with a different layout stays DISTINCT. SEMANTIC: NO new code needed — the existing Pass-1 `structSpec` define path mints + interns the type in the block scope. CST→HIR: the ONE C++ change — `lowerVarLikeInto` gains the LOCAL twin of `lowerTopLevelInto`'s no-object branch: a LIST-mode (non-single) declaration with an EMPTY declarator list + `requireNamedDeclarators` either (a) DEFINES a composite (`findCompositeSpecifierIn` hits a body-present spec) ⇒ emit nothing (the type was already minted/interned semantically — a later `struct S v;` resolves through it; a body TypeDecl carries no MIR effect), or (b) introduces NO composite (`int;`) ⇒ FAIL LOUD `S_DeclarationDeclaresNothing` (C 6.7p2) — the SAME diagnostic + tier as the top-level `int ;`. The guard is gated on an EMPTY list so an ABSTRACT declarator (`int *;`, list non-empty) is NOT double-reported (the semantic `requireNamedDeclarators` arm already rejects it once). FAIL-LOUD preserved: `int;` loud (declares nothing); a ref to an undefined tag still `S_UnknownType`; no silent accept. WITNESS: corpus `local_struct_def_stmt` (the walMergesort shape — a block-scoped `struct Sublist { int nList; int aList; };` no-declarator definition used to compute a return value across a non-inlined call → exit 42, with a file-scope same-name 1-field `struct Sublist` proving decl-site distinctness; pe-native + elf/macho/arm64 + a `{shippedPipeline:release}` arm) + 7 semantic pins (`C28LocalStructDefineNodeShapeAndType` asserting the varDecl/structSpec/structBody node shape + NO initDeclaratorList + the 2-field Struct type + the in-block ref resolving to the SAME TypeId; union+enum local defines; `C28LocalStructDoesNotLeakToOuterScope` block-scope non-leak → outer ref S_UnknownType; `C28InnerStructShadowsOuterDistinctType` nominal distinctness; `int;` semantically-clean; `int *;` single-diagnostic; ordinary-local-decls-unaffected) + 3 HIR pins (`LocalStructDefinitionLowersClean`; define+declare+ref lower clean; `LocalDeclaresNothingFailsLoud`); 451→454 ctest (1 new corpus + the new pins). | (closed — the grammar tweak + the lowerVarLikeInto twin IS the fix) | p19 Cluster G c28; `src/dss-config/sources/c-subset.lang.json` (`varDecl` optional `initDeclaratorList` in both branches); `src/hir/lowering/cst_to_hir.cpp` (`lowerVarLikeInto` no-object local twin); `D-CSUBSET-UNIFIED-COMPOSITE-SPECIFIER` (c25 — the `definesWhenChild` machinery it reuses); `D-CSUBSET-STRUCT-BODY-VARDECL-POSITION` (the top-level no-declarator path this mirrors); `D-CSUBSET-SELF-REFERENTIAL-STRUCT` (c24 nominal decl-site identity → the block-scope distinctness); plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-ABSTRACT-DECLARATOR-TYPE-NAME` | ✅ **CLOSED 2026-06-27 (p18 Cluster G c26).** ROOT (the c26 frontier): `((uid_t(*)(void))aSyscall[21].pCurrent)` (sqlite3.c:40989, the `osGeteuid` define) failed P9006/P0009 — a cast to an ABSTRACT FUNCTION-POINTER type `(T (*)(params))expr` was unsupported: `castTypeRef` admitted only `{repeat StarOp}` after the base, not the parenthesized abstract declarator `(*)(params)`. FIX = REUSE the existing declarator machinery, no new mechanism: `castTypeRef` gains an OPTIONAL `abstractDirectDeclarator` tail (the abstract twin of `directDeclarator`, sharing `parenDeclarator`/`fnSuffix`/`arrayDeclSuffix` verbatim); the semantic resolver folds it via the SHARED `directDeclaredType` engine — factored out of `declaratorDeclaredType` (the part after its pointer-layer loop) so the abstract type-name fold and the declaration-position fold can NEVER drift — yielding `Ptr<Fn(params)->base>` (the SAME type params get). Covers cast + sizeof + compound-literal + va_arg (all share `castTypeRef`). The abstract twin's base EXCLUDES `Identifier` (C 6.7.7 direct-abstract-declarator has no identifier production) — THE CRUX: a bare-identifier base would make `(c * c)` (a multiplication, sqlite/zext `(c*c)*(c*c)`) speculatively COMMIT as a bogus cast (the type child gains >1 leaf → commit-triage rule-1 'not a lone identifier' fires). AGNOSTIC: config grammar + the generic declarator engine; the optional `DeclaratorConfig.directAbstractRule` is nullopt for toy/tsql (zero change). FAIL-LOUD: a NAMED type-name declarator (a name nested in parens, `(int (x))expr`) is rejected with `S_TypeNameDeclaratorNotAbstract` (0xE027) — never silently parsed as the bare base; an unknown base type still `S_UnknownType` (the declarator fold never masks a missing base). ★ §B FORK (reported + resolved empirically): approach B (reuse the full `directDeclarator`, Identifier included) was tried FIRST and REGRESSED `(c * c)` → bogus cast → S_UnknownType (caught by the zext_u32_to_u64 corpus); approach A (the dedicated Identifier-excluding twin) is FORCED + correct. WITNESS: corpus `abstract_fnptr_cast` (a `void*` slot cast to `(int(*)(int))` and CALLED — the osGeteuid shape — exit 42, pe-native + elf/macho/arm64 + a `{shippedPipeline:release}` arm) + 8 parser pins (exact `abstractDirectDeclarator` nesting for cast/sizeof/compound/va_arg + `(c*c)`-stays-multiplication regression + base+stars no-tail regression) + 5 semantic pins (cast types `Ptr<Fn(void)->int>` / `Ptr<Fn(int)->int>` exact shape, named-cast fail-loud, unknown-base S_UnknownType, sizeof-resolves-clean); 448→449 ctest. NOTE: `sizeof(fn-ptr-type)` as an ARRAY DIMENSION does not fold (stays `S_NonConstantArrayLength`) — a PRE-EXISTING limit that equally affects `sizeof(int*)` as an array dim (orthogonal to c26, fails loud not silent). | (closed — the reuse IS the fix) | p18 Cluster G c26; `src/dss-config/sources/c-subset.lang.json` (`castTypeRef` tail + `abstractDirectDeclarator` rule + `declarators.directAbstractRule`); `src/core/types/semantic_config.hpp` (`DeclaratorConfig.directAbstractRule`); `src/core/types/grammar_schema_json.cpp` (load the optional role + closed-key); `src/core/types/declarator_walk.hpp` (`declaratorNameNode` abstract-twin arm); `src/core/types/parse_diagnostic.{hpp,cpp}` (`S_TypeNameDeclaratorNotAbstract` 0xE027); `src/analysis/semantic/semantic_analyzer.cpp` (`directDeclaredType` extraction + the type-name fold + named-reject); `D-CSUBSET-STRUCT-MEMBER-DECLARATOR`/`D-CSUBSET-STRUCT-MULTI-DECLARATOR` (the declarator engine it reuses); plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-ABSTRACT-ARRAY-TYPE-NAME` | **OPEN — NAMED LIMITATION, fail-loud, off the SQLite frontier (surfaced p18 Cluster G c26, 2026-06-27).** The BARE-array abstract type-name `(int[10])` / `sizeof(int[10])` (a type-name whose abstract declarator is a top-level `[N]` array suffix) is NOT supported: c26's `abstractDirectDeclarator` deliberately EXCLUDES `arrayDeclSuffix` from its BASE alt because `(b[0])` — a pervasive array-INDEX value expression — would otherwise speculatively COMMIT as a type-name `b[0]` (base `b` + array suffix; the type child gains >1 leaf → commit-triage rule-1 'not a lone identifier' fires before the binder-sketch value check), regressing `sizeof(b) / sizeof(b[0])` (the element-count idiom — the `sizeof_value_array_dim` corpus). C's direct-abstract-declarator DOES allow a bare `[N]` base, but it collides with the index expression under the speculative type-vs-value triage. The `(*)[N]` array-OF-pointer form DOES work (its base is `parenDeclarator`; the `[N]` rides the suffix repeat). FAIL-LOUD: `(int[10])x` is a parse error / `sizeof(int[10])` resolves the bare `int` (no array) — never a silent miscompile. OFF the c26 fn-ptr frontier (sqlite3.c:40989 is the `(uid_t(*)(void))` fn-ptr cast); `sizeof` of a known array OBJECT (`sizeof(arr)`) is the value form and works. Priority: **normal.** | **Trigger:** a real consumer needs a bare-array abstract type-name (`sizeof(int[N])` / a `(int[N]){...}` compound literal). **Closing work:** teach the speculative type-vs-value commit triage (parser `typeNameCommitApproved_`) that when the type child's BASE is a lone Identifier (not a keyword) followed by an array/other abstract suffix, it must still consult the binder sketch (rules 2/3 — a known VALUE rolls back) instead of blindly committing via rule-1; THEN re-add `arrayDeclSuffix` to `abstractDirectDeclarator`'s base alt. Pin: `sizeof(int[4])` folds to 16 AND `sizeof(b)/sizeof(b[0])` stays the value form (both green). | `D-CSUBSET-ABSTRACT-DECLARATOR-TYPE-NAME` (the c26 parent — its grammar comment carries the exclusion rationale); `src/dss-config/sources/c-subset.lang.json` (`abstractDirectDeclarator` base alt); `src/analysis/syntactic/parser.cpp` (`typeNameCommitApproved_` rule-1); plan 23 full-C arc |
| `D-CSUBSET-FORWARD-STRUCT-DECLARATION` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c35) — OPAQUE / forward-declared (incomplete) struct types, via REUSE of the c24 incomplete-composite machinery (no new type state).** ON the SQLite path — opaque handles `sqlite3_stmt` / `sqlite3_blob` / … are exactly this pattern (`typedef struct sqlite3_stmt sqlite3_stmt;` at sqlite3.c:4649, used ONLY as `sqlite3_stmt *`); the prior "off the SQLite path" framing was wrong (sqlite3_stmt is a direct counterexample). ROOT (the c25-noted gap): a body-ABSENT named composite specifier never MINTED a tag — c24 forward-mints only when a body opens an inner scope (`rec.structScope.valid()`), so an opaque-tag reference MISSED → `S_UnknownType`, and a bare `struct S;` was body-absent → `S_DeclarationDeclaresNothing` at HIR. FIX (3 edits, config-driven, agnostic): (1) the `resolveTypeNodeImpl` isTagReference arm, on a tag MISS, FORWARD-MINTS an INCOMPLETE composite (`forwardComposite(kind,name,declSiteKey)`) and binds it into the Tag namespace of the nearest enclosing namespace scope (`floatToNamespaceScope`) — covers the opaque typedef base (`typedef struct S S;`), the opaque pointer base (`struct S *p`), and the bare `struct S;` head; the Tag binding deduplicates so repeat references resolve to ONE TypeId; driven by a NEW config field `references[].compositeKind` (struct/union mint, ENUM keeps the fail-loud miss — value-typed, no incomplete representation). (2) the HIR declares-nothing sites (top-level + local) gain a `findForwardCompositeSpecifierIn` sibling: a body-absent NAMED composite specifier (a forward declaration) emits NOTHING instead of `S_DeclarationDeclaresNothing` (the tag was minted semantically); `int ;` / `int;` (no tag) stays loud. ★ EDIT 2 (tag-merge relaxation for forward-then-define) proved UNNECESSARY — Pass-1's whole-tree PRE-ORDER definition-binding guarantees a same-TU definition's COMPLETE tag is bound BEFORE any reference resolves, so the isTagReference forward-mint only fires for a GENUINELY-never-defined tag; a forward-then-define (`struct S; struct S {…};`) resolves every reference to the definition's complete TypeId (no two-TypeId split, no collision) — verified by a type-identity round-trip pin (opaque `S*` from one fn, dereferenced as the complete type elsewhere → 42). The `completeComposite` conflicting-re-completion fatal remains the backstop. ★★ THE FAIL-LOUD AXIS IS PRESERVED (the no-silent-zero-size backstop): the c24 `computeLayout` incomplete guard (`type_layout.cpp` ~279) and its 5 loud consumers are UNTOUCHED — a VALUE / by-value-member / sizeof of an incomplete still fails loud (a local `struct S v;` → MIR `allocaForLocal` H0009; a member `p->x` → S_NotAComposite; sizeof → no layout; a global `struct S g;` → the .bss data-producer fails loud; a by-value param/return → H0009). WITNESS: 6 semantic pins (the INVERTED `C35BareStructForwardDeclMintsIncompleteTag` [was `C25BareStructForwardDeclFailsLoud`, which asserted the pre-c35 S_UnknownType] + opaque-typedef-via-ptr + forward-then-define-completes-no-collision + member/sizeof-of-incomplete-fail-loud + two-definitions-still-collide) + 2 HIR forward-decl-lowers-to-nothing pins (top-level + local) + 4 MIR end-to-end pins (value/member/sizeof-of-incomplete fail loud + opaque-handle-via-ptr lowers clean) + runtime corpus `opaque_handle_ptr` (an opaque `struct Stmt;` never defined, passed by pointer through a non-inlined call, the real object a `struct Vdbe` reached via `void*` — the Vdbe-behind-sqlite3_stmt* pattern → exit 42, 4 targets + release arm). 456→457 ctest. | (closed — the 3 edits ARE the fix; EDIT 2 was a no-op given Pass-1 ordering) | p19 Cluster G c35; `D-CSUBSET-SELF-REFERENTIAL-STRUCT` (c24 — the incomplete-composite substrate REUSED: forwardComposite/completeComposite/isIncompleteComposite + the computeLayout guard); `D-CSUBSET-UNIFIED-COMPOSITE-SPECIFIER` (c25 — made `struct S;` parse as a body-absent ref); `src/core/types/semantic_config.hpp` (`ReferenceRule.compositeKind`); `src/core/types/grammar_schema_json.cpp` (load `compositeKind` on tag-ref rows); `src/dss-config/sources/c-subset.lang.json` (6 tag-ref rows + compositeKind); `src/analysis/semantic/semantic_analyzer.cpp` (the isTagReference forward-mint arm); `src/hir/lowering/cst_to_hir.cpp` (`findForwardCompositeSpecifierIn` + the 2 declares-nothing sites); plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-MUTABLE-POINTER-TO-CONST` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c36) — object-const computed from the OUTERMOST pointer layer, not a coarse whole-decl scan.** ROOT (sqlite3.c, ~50× e.g. `zFormat += 4` / `zDate++`): DSS wrongly rejected `const char *p; p += n` with `S_ConstViolation` — `const char *p` is a MUTABLE pointer to const (C 6.7.3: const qualifies the POINTEE; only `char * const p` qualifies the pointer OBJECT). `rec.isConst` was set by `subtreeContainsToken(const)` over the WHOLE declaration, which saw the head/pointee const and wrongly marked the pointer object const. SAME bug c27 fixed for `volatile` — the code comment even said "`isConst` keeps its scan", a deliberate c27 deferral now forced. FIX (single file, agnostic, NOT §B): a new `declaratorObjectIsConst` helper (semantic_analyzer.cpp) — descend the per-slot declarator to its inner declaratorRule, and if it has ≥1 pointer layer, the object is const iff the LAST (outermost, source-order) pointer layer carries the const marker; with NO pointer layer the legacy whole-decl scan applies (scalar / typedef'd-pointer head const). Mirrors the c27 per-layer volatile walk in `declaratorDeclaredType`. Const stays a SYMBOL flag (NOT a type qualifier like c27's `VolatileQual`) because — UNLIKE volatile — const has no per-access codegen effect; the architect's Option-2 (a `ConstQual` lattice type) was rejected as a ~100-site surface buying nothing the helper doesn't. ★ CODEGEN NUANCE (the architect's "no codegen effect" claim was INCOMPLETE — caught + verified): `isConst` DOES thread to `MirGlobal.isConst` → `.rodata`/`.data` global placement (asm.cpp). The fix only ever REMOVES false-const (a `const char *g` global moves .rodata→.data, the SAFE/writable direction — placing a mutable pointer in read-only memory was a latent fault), NEVER the reverse; the string-literal `.rodata` override is unaffected. Sites B (anonymous field — never an assignment LHS) + C (legacy positional — no c-subset constMarker row reaches it) left coarse by design (documented in-code). WITNESS: 12 semantic pins (pointer-to-const param/east/multi-level/multi-declarator/const+volatile CLEAN; const-pointer / const-ptr-to-const / outer-const VIOLATE; east-const scalar still violates) + runtime corpus `const_ptr_walk` (a `const int *p` walks an array via `p++` → 42; RED-ON-DISABLE: pre-c36 `p++` was a spurious S_ConstViolation → would not compile; int ops dodge the deferred sub-32-bit ALU gap, p++ dodges the flagged p+n stride). 457→458 ctest. | (closed — the helper + the Site-A swap IS the fix) | p19 Cluster G c36; builds on c27 `D-CSUBSET-VOLATILE-POINTEE` (the volatile twin + the per-layer walk) + c29 `D-CSUBSET-POST-STAR-CAST-QUALIFIER` (pointerLayer/ptrQualifier); `src/analysis/semantic/semantic_analyzer.cpp` (`declaratorObjectIsConst` + Site A); defers `D-CSUBSET-POINTEE-CONST-ENFORCEMENT` + `D-CSUBSET-GROUPED-DECLARATOR-CONST`; a sqlite3.c re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-POINTEE-CONST-ENFORCEMENT` | ⏳ **DEFERRED (surfaced p19 Cluster G c36; the c36 audit broadened it).** The const-violation check (semantic_analyzer.cpp ~4404) only examines a PLAIN-IDENTIFIER LHS, so const enforcement is MISSING for every NON-plain-identifier const lvalue: (i) deref `const char *p; *p='x';` (pointee-const), (ii) member `struct S{const int v;}; s.v=2;`, (iii) array-subscript `const int a[2]; a[0]=5;`. All compile CLEAN today (a const GLOBAL-array element write would be a `.rodata` fault at runtime; a const local is silently wrong). c36 made the pointer OBJECT's const correct + did NOT touch this check (the const NAME `a`/`p`/member is correctly flagged — `const int a[2]; a=b;`→S0007 — only the check's LHS coverage is narrow); these gaps are PRE-EXISTING, an independent axis, never a silent miscompile of c36's fix. NOT on the SQLite compile path (sqlite is const-correct). **Trigger:** a consumer that writes through a const pointer/member/element, or a desire for full const-correctness diagnostics. **Closing work:** (a) widen the const-violation check's LHS to deref/member/subscript lvalues; (b) for pointee-const specifically, model const in the type lattice (a transparent `ConstQual` mirroring/generalizing c27's `VolatileQual`) to know a deref target's const-ness — a §B type-system decision (separate `ConstQual` vs a unified cv-qualifier). Pins: `const char *p; *p=0;` / `const int a[2]; a[0]=5;` / `struct S{const int v;}; s.v=2;` → each emits S_ConstViolation. | (open) | p19 Cluster G c36; `D-CSUBSET-MUTABLE-POINTER-TO-CONST` (the object-const fix that scoped this out); `D-CSUBSET-VOLATILE-POINTEE` (c27 — the VolatileQual machinery a ConstQual would mirror); plan 23 full-C arc |
| `D-CSUBSET-GROUPED-DECLARATOR-CONST` | ⏳ **DEFERRED (surfaced p19 Cluster G c36).** c36's `declaratorObjectIsConst` reads the outermost pointer layer that is a DIRECT child of the declarator; a GROUPED pointer declarator `char (* const p)` hides its layer inside the parenthesized group, so it falls through to the legacy whole-decl scan (STATUS QUO — no regression; the coarse scan was used for ALL forms before c36). Consequence: a grouped-with-head-const form like `const char (*p)` stays an over-approximation (the head const is seen by the fallback → p wrongly marked const) — IDENTICAL to pre-c36 behavior, never a silent miscompile (a LOUD false-positive S_ConstViolation on legal code, or a const global redundantly in .rodata). The common grouped case — a function pointer `void (*fp)(int)` with NO const — is correct (the fallback finds no const). NOT observed on the SQLite path. **Trigger:** a consumer using a grouped pointer declarator whose object-const matters. **Closing work:** descend the group (parenDeclarator) following the same recursion `directDeclaredType` uses, to find the TRUE outermost pointer layer. Pin: `const char (*p); p = 0;` → clean. | (open) | p19 Cluster G c36; `D-CSUBSET-MUTABLE-POINTER-TO-CONST` (the helper that scoped this out); plan 23 full-C arc |
| `D-CSUBSET-FUNCTION-DESIGNATOR-CAST` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c37) — a function DESIGNATOR cast to a fn-ptr type.** ROOT (sqlite3.c, 29× — `#define SQLITE_DYNAMIC ((sqlite3_destructor_type)sqlite3RowSetClear)`, `(sqlite3_syscall_ptr)posixOpen`): casting a bare function name to a function-pointer type failed error[S0010] S_InvalidCast. C 6.3.2.1p4 + 6.3.2.3p8: a function designator decays to a pointer-to-function, and a fn-ptr may be cast to ANY fn-ptr type (cross-signature included — calling THROUGH an incompatible type is UB, but the CAST is legal + value-preserving = the function's code address). ROOT: `isExplicitCastable` (type_rules.hpp) had NO arm for an `FnSig` OPERAND → `isExplicitCastable(Ptr<FnSig>, FnSig)` returned false (a function symbol's type is the raw `FnSig`; DSS decays per-consumption-site, not globally, and the CAST site lacked it — the assignment/init/arg path ALREADY decays via `isAssignable`'s Ptr/FnSig arm, so `int(*p)(int)=g;` already worked). FIX (NOT §B, ONE semantic arm, NO grammar/config/HIR/MIR change): add `if (tk==Ptr && ok==FnSig) return true;` to `isExplicitCastable`. The HIR `combineCast` already emits a Cast node for an FnSig operand + `mapCast(FnSig,Ptr)`→Bitcast over the GlobalAddr (present from c12 brace-init) → the cast lowers as a value-preserving NO-OP (the function's address); a call THROUGH the cast pointer reaches the function. ★ OVER-ADMISSION BOUNDARY (the audit surface, verified red-on-disable): the arm is tk==Ptr + ok==FnSig ONLY → struct/union/void OPERANDS (not FnSig) stay rejected, and a function→INTEGER target (`(long)g`, tk not Ptr) stays rejected. WITNESS: 4 semantic pins (same-sig + cross-sig fn-ptr cast ACCEPT; struct→fnptr + fn→int still REJECT) + runtime corpus `fnptr_designator_cast` (`(fp_t)helper` then call-through → exit 42; red-on-disable: pre-c37 it would not compile). 458→459 ctest. | (closed — the one cast arm IS the fix) | p19 Cluster G c37; builds on c12 (the FnSig→Ptr mapCast Bitcast + the isAssignable Ptr/FnSig decay arm) + c26 `D-CSUBSET-ABSTRACT-DECLARATOR-TYPE-NAME` (the abstract fn-ptr cast TYPE); `src/analysis/semantic/type_rules.hpp` (`isExplicitCastable`); a sqlite3.c re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-NESTED-TAG-SCOPE` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c38) — a struct/union/enum TAG nested in a struct body has ENCLOSING (block/file) scope, not the inner struct's member scope (C 6.2.1p4).** ROOT (the single largest sqlite S000D class, ~50× — `struct WalIterator { … struct WalSegment {…} aSegment[FLEXARRAY]; }` then `walIteratorNext` refs `struct WalSegment *pSegment`; also sColMap/ExprList_item/IdList_item): a tag defined NESTED inside a composite body bound into the INNER struct's MEMBER scope → a by-name reference `struct Inner *p` from the enclosing scope MISSED → c35 forward-minted an INCOMPLETE tag → `p->x` failed S_NotAComposite / `struct Inner v;` failed S_IncompleteTypeObject. (Member access `o->m.x` ALREADY worked — the member TYPE resolves via structScope, independent of the tag BIND scope; only by-NAME lookup failed.) ROOT SITE: `floatToNamespaceScope` (semantic_analyzer.cpp) STOPPED (`break`) at a composite-body scope (a `fieldChildren` scope), treating it as a namespace — but C 6.2.1p4: a struct-body is a MEMBER namespace, NOT a tag namespace; a nested tag belongs to the nearest ENCLOSING block/file scope. FIX (NOT §B; ONE line + comment; NO scope-model/HIR/MIR change): change the composite-body `break` to `scope = parent` (float a nested tag PAST a composite body, exactly as it already floats past a declarator-dominator scope) — only a BLOCK or the file/CU root now stops the float. ★ INVARIANTS PRESERVED (architect-verified, plan-lock-confirmed): nominal identity (the c24 decl-site key is independent of the BIND scope → the by-name `struct Inner *` and the member `Outer.m` are the SAME TypeId); member composition (c25 `structScope = here`, not bindScope → `o->m.x` unbroken); pre-order forward-mint (Pass-1 binds the COMPLETE nested tag at file scope BEFORE any function-body reference resolves → no c35 forward-mint, no two-TypeId split); BLOCK-scope correctness (a block-LOCAL nested tag floats only to its BLOCK scope via the not-in-declByRule break → never leaks to file); SHADOWING (a nested tag same-name as a file-scope tag → both at file scope → S_RedeclaredSymbol, correct per C 6.7p3). WITNESS: 7 semantic pins (nested ref/value/union/deeply-nested ACCEPT; member-access REGRESSION guard; block-non-leak OVER-FLOAT guard; same-name shadowing COLLISION) + runtime corpus `nested_tag_scope_ptr` (the WalSegment shape — a nested tag by-name pointer + arrow reads → exit 7; red-on-disable: pre-c38 it would not compile). 459→460 ctest. | (closed — the one-line float fix IS the fix) | p19 Cluster G c38; builds on c24 `D-CSUBSET-SELF-REFERENTIAL-STRUCT` (nominal decl-site identity) + c25 `D-CSUBSET-UNIFIED-COMPOSITE-SPECIFIER` (the structScope/define path) + c35 `D-CSUBSET-FORWARD-STRUCT-DECLARATION` (the forward-mint that masked the miss); `src/analysis/semantic/semantic_analyzer.cpp` (`floatToNamespaceScope`); a sqlite3.c re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-FFI-STDINT-CONSTANT-MACROS` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c39) — the C99 7.18.4 `*_C` integer-constant macros shipped from <stdint.h>.** ROOT (the dominant sqlite S0001/S_UndeclaredIdentifier class, 48× — sqlite uses `UINT64_C(0x…)` for 64-bit bitmask constants): DSS's shipped <stdint.h> descriptor (c1) shipped the TYPEDEFs (uint64_t…) but NOT the `*_C` constant macros → `UINT64_C` undeclared. C99 7.18.4: `UINT64_C(v)` yields an integer constant of type uint_least64_t (`v##ULL`). FIX (NOT §B; ONE descriptor file, NO C++ change): add a `macros` array to `shippedLibs/stdint.json` with the 10-member family (INT8/16/32_C + UINT8/16_C → `v`; UINT32_C → `v##U`; INT64_C → `v##LL`; UINT64_C/UINTMAX_C → `v##ULL`; INTMAX_C → `v##LL`). The c5 D-PP-DESCRIPTOR-MACRO-INJECT mechanism (the assert.h `macros` surface) splices `#define UINT64_C(v) v##ULL` into the synth buffer → the SAME PP expands it (function-like + `##` token-paste BOTH verified working; the config is RUNTIME-loaded — the repro compiled WITHOUT a rebuild). ★ UNIVERSAL suffix (no per-target branching): `unsigned long long` is ≥64-bit on EVERY DSS target (LP64 linux/macOS + LLP64 windows) so `ULL` satisfies uint_least64_t everywhere; the integer-literal ladder types `0x…ULL` as U64 (no 32-bit truncation — the architect traced the decode: strip ULL → magnitude → magnitudeFits(U64) → U64; a >64-bit literal fails loud S_IntegerLiteralTooLarge). WITNESS: runtime corpus `stdint_constant_macros` (`UINT64_C(0x100000000)>>32` == 1 ONLY if full 64-bit, + INT64_C/UINT32_C → exit 42; RED-ON-DISABLE: pre-c39 UINT64_C undeclared → would not compile; a 32-bit truncation → exit 13) + the existing descriptor macros-surface parse test covers the load. 460→461 ctest. NOTE: the remaining 2 S0001 (`pthread_create`/`pthread_join`) are pthread.json FFI symbols (a DIFFERENT mechanism) → c40 candidate, scoped out. | (closed — the descriptor `macros` array IS the fix) | p19 Cluster G c39; reuses c5 `D-PP-DESCRIPTOR-MACRO-INJECT` (the shipped `macros` surface) + c1 `D-CSUBSET-DESCRIPTOR-TYPEDEF-POINTER-DECL` (the stdint descriptor); `src/dss-config/shippedLibs/stdint.json`; sibling deferrals D-FFI-STDINT-LIMIT-MACROS (*_MAX/*_MIN) + D-FFI-STDINT-PTR-WIDTH-ILP32; a sqlite3.c re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-POINTER-SUBTRACTION` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c40) — `p - q` is ptrdiff_t (C 6.5.6p9); a COMPILE+RUN-green fix.** ROOT (the biggest remaining sqlite S0003/S_TypeMismatch class, ~50× — `sqlite3StrAppend64(pAccum, bufpt, fmt - bufpt)`, `length = &zOut[nOut-1] - bufpt`): `combineBinary` typed `Ptr<T> - Ptr<T>` as `Ptr<T>` (the "first valid type wins" fallback, since commonArithType returns invalid for non-arith operands) — WRONG. C 6.5.6p9: `p - q` (same pointer type) yields a SIGNED ptrdiff_t (i64) whose VALUE is the ELEMENT count (byte_difference / sizeof(*p)). ★ The ASYMMETRY that pinpointed it: `long n = a - b;` WORKED (the var-init check's single-child DFS skips the binary node + coerce passes the Ptr straight into the I64 slot — numerically OK for char) but `g(a - b)` FAILED S0003 (the call-arg check uses full `subtreeType` → Ptr<T> → `isAssignable(I64param, Ptr)` = false). FIX (NOT §B; 3 sites, p-q ONLY): (1) cst_to_hir `combineBinary` + (2) semantic `subtreeType`'s combineBinary lambda — both detect Sub + both-Ptr + SAME-pointee → result type I64 (operands stay Ptr, `common` was invalid so no coerce); (3) hir_to_mir `combineBinaryOp` — when op=Sub, operand kind=Ptr, result kind=I64 → PtrToInt both sides + Sub (byte diff) + (unless sizeof(pointee)==1) SDiv by `elementStride(pointee)` → the SIGNED element count. ★★ DOUBLE VALUE: fixes the COMPILE error (arg type matches) AND the flagged RUN-time silent bug's p-q half (an `int*` subtraction now gives the element count, NOT the 1-byte-unscaled byte count). ★ SCOPE: p-q ONLY; `p ± n` (pointer ± integer → Ptr result — the OTHER half of the flagged combineBinary bug, keeps the Ptr type, only the VALUE needs sizeof scaling) is architecturally distinct → DEFERRED `D-CSUBSET-POINTER-INTEGER-ARITH-SCALING` (c41). Mismatched-pointee `char* - int*` falls through to a Ptr-typed result — caught ONLY in numeric-ARG context (a non-arg `(int)(a-b)` is a deferred silent gap, `D-CSUBSET-POINTER-DIFF-EDGE-CASES`, which ALSO covers `p - arrayName` where the array operand never decays); incomplete/void pointee → fail loud (no element stride). ★ INDEPENDENT AUDIT VERIFIED the CORE fix CLEAN (scaling correct for int/long/double/struct12/char incl. NEGATIVES + the SDiv-not-UDiv proof, debug+release; result type ptrdiff_t in 9 contexts; NO regression to p++/p[i]/int-int/compare/deferred-p±n; void*/incomplete fail loud) — the 2 silent gaps it surfaced are PRE-EXISTING + outside the same-pointee scope. WITNESS: 4 semantic pins (char*/int* p-q as call-arg CLEAN; return+assign CLEAN; mismatched-pointee STILL fails) + runtime corpus `ptr_subtraction` (int* → 3 ELEMENTS not 12 bytes, char* → 5, SIGNED p1-p4 == -3, all passed through a `long` arg → exit 42; red-on-disable: pre-c40 won't compile / an unscaled int* = 12). 461→462 ctest. | (closed — the 3-site ptrdiff result-type + the MIR sizeof-division IS the fix) | p19 Cluster G c40; the run-green half of the flagged p-q silent bug (combineBinary); DEFERS `D-CSUBSET-POINTER-INTEGER-ARITH-SCALING` (p±n); `src/hir/lowering/cst_to_hir.cpp` (combineBinary) + `src/analysis/semantic/semantic_analyzer.cpp` (subtreeType combineBinary) + `src/mir/lowering/hir_to_mir.cpp` (combineBinaryOp); a sqlite3.c re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-POINTER-DIFF-EDGE-CASES` | ⏳ **PARTIALLY CLOSED — ✅ c65 (p19 Cluster G, 2026-06-30) closed part (1) `p - arrayName`; part (2) mismatched-pointee diagnostic REMAINS DEFERRED.** ★ c65: by the c64 era part (1) had become a COMPILER CRASH (not just garbage) — the un-decayed Array operand made the HIR type `p - array` as pointer-INTEGER arith → the MIR `combineBinaryOp` `p±n` branch tried to widen the Array index via `mapCast(Array,I64)` → `MirOpcode::Invalid` → `addInst(Invalid)` std::abort (exit 134). UN-MASKED (not caused) by c64 clearing the ternary so the sqlite compile reached vdbeSorter `pSorter->iPrev = (u8)(pTask - pSorter->aTask)` (sqlite3.c:107252). FIX (NOT §B; C 6.3.2.1p3 + 6.5.6): (HIR cst_to_hir.cpp combineBinary) an ARRAY operand of pointer `Sub` decays to Ptr<elem> [covers `p-arr`, `arr-p`, `arr-arr`; `arr - scalarIndex` stays the c59 array-index Ptr form], and `ptrSub` now reads the decayed `lc/rc` → result I64 → the existing c40 ptrSub path (PtrToInt+Sub+SDiv by sizeof(elem)); (MIR hir_to_mir.cpp:1208) a fail-loud guard so a remaining non-int index emits a clean diagnostic instead of the addInst abort. ★ THE INDEPENDENT AUDIT caught a FAIL-LOUD REGRESSION: an UNCONDITIONAL decay made `int* - char[]` (mismatched pointee, gcc rejects) a SILENT miscompile (exit 8 — decayed to int*/char*, non-ptrSub → slips into the c41 p±n path treating the array address as an index) where c64 had ABORTED loud → FIXED: the decay fires ONLY when element/pointee types MATCH, so a mismatched array pairing stays un-decayed → fails LOUD (S0008 in a typed context, or the H0009 MIR guard in a pointer context). All valid-C paths verified regression-free (304/304 on all 3 legs, arm64 `sdiv`-by-stride proven). WITNESS corpus `pointer_minus_array` (16-byte element so the stride DIVISION is load-bearing; ptr-member-array + bare-array + the c40 `&arr[i]-&arr[j]` control → 42; RED-ON-DISABLE → the guard's clean H0009 'index TypeKind 21 has no widening cast', or the addInst abort if the guard is also reverted). 3-leg RUN green (x86_64-elf SysV + arm64-qemu AAPCS64 + Win-pe → 42, gcc-cross-checked -O0/-O2/arm64). The c59 `array - scalarIndex` + c40 `p - q` + c41 `p ± n` paths UNCHANGED. The enum-index sibling at the same MIR site → [[D-CSUBSET-POINTER-ARITH-ENUM-INDEX]] (guarded fail-loud, deferred). — *Original deferral (audit trail):* ⏳ DEFERRED (surfaced by the c40 audit, 2026-06-28). Two PRE-EXISTING silent-miscompile gaps in pointer SUBTRACTION that fall OUTSIDE c40's same-pointee-both-Ptr scope (c40 fixed `p - q` correctly; these were untouched, NOT regressions): (1) **`p - arrayName`** (`int *p=&arr[1]; (int)(p - arr)`) compiles clean + returns GARBAGE — the array operand never decays to Ptr (combineBinary: a pointer-pair makes `common` invalid → no coerce → the array stays `Array` → c40's `kind(rhs)==Ptr` guard misses → the fallback types it `Ptr<char>` → MIR raw unscaled Sub). `p - arr` is IDIOMATIC C (`&buf[i] - buf`) → a real RUN-green hazard (flagged via spawn_task). (2) **mismatched-pointee `char* - int*`** in a NON-arg context (`(int)(a-b)`, `long n=a-b`) compiles silently with a raw unscaled Ptr Sub — caught ONLY when the result coerces into a numeric param (an incidental `isAssignable`), never at the subtraction itself. **Trigger:** a consumer (or sqlite re-probe RUN) that hits `p - arrayName` or a mismatched/non-arg pointer difference. **Closing work:** (a) decay an Array operand to Ptr<element> in the subtraction (then c40's same-pointee path handles `p - arr` correctly); (b) when op==Sub and both operands are pointer/array but pointees DIFFER, emit a loud diagnostic at the binary node (not just incidentally in arg context). Pins: `int a[4]; int*p=&a[1]; (int)(p - a)` → 1 (not garbage); `(int)(charptr - intptr)` → a loud diagnostic. | (open) | p19 c40 audit; `D-CSUBSET-POINTER-SUBTRACTION` (c40 — the same-pointee fix that scoped these out); `D-CSUBSET-POINTER-INTEGER-ARITH-SCALING` (the related p±n value-scaling deferral, NOW CLOSED as `D-CSUBSET-POINTER-INT-ARITHMETIC` c41); plan 23 full-C arc |
| `D-CSUBSET-POINTER-ARITH-ENUM-INDEX` | ⏳ **DEFERRED (surfaced by the c65 root-cause + audit, 2026-06-30; guarded fail-loud, NOT a crash).** An ENUM-typed index in pointer arithmetic (`enum E{B=2}; int *p; … *(p + e)` / `p - e`) reaches the MIR `combineBinaryOp` `p±n` index-widen site (`hir_to_mir.cpp:1208`) with TypeKind Enum; `mapCast(Enum, I64)` returns Invalid (its `isInt` excludes Enum) — the SAME unguarded site as the c65 `p - arrayName` crash. c65 added a fail-loud guard there, so this now emits a CLEAN diagnostic ("index TypeKind … has no widening cast … D-CSUBSET-POINTER-ARITH-ENUM-INDEX"), NOT the addInst std::abort. NOT confirmed in sqlite3.c (the c65 root-cause found the array case as the actual construct; the enum case was a reproducer probe). DEFERRED to its OWN cycle because the fix — resolve the enum index to its underlying integer kind before the widen (the `combineCast` `enumUnderlying` precedent at `hir_to_mir.cpp:1270`) — is a SIGNED/UNSIGNED widen (SExt vs ZExt by the underlying's signedness) that warrants an arm64-focused audit (the c57 SCVTF lesson: a numeric conversion can silently miscompile on arm64 while green on the host). FIX (when needed): at `hir_to_mir.cpp:1207-1208` resolve `indexTy`'s enum → underlying kind, then `mapCast(underlying, I64)`; corpus an enum-indexed `p + enumConst`, value-correct on all 3 legs. | (open) | p19 c65 root-cause/audit; the same MIR site as [[D-CSUBSET-POINTER-DIFF-EDGE-CASES]] (c65); the `combineCast` enumUnderlying precedent; [[D-CSUBSET-ENUM-INT-CONVERSION]] (enum-as-underlying); plan 23 full-C arc |
| `D-CSUBSET-TERNARY-NULL-STRING-LITERAL` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c66; NOT §B) — a conditional with a STRING-LITERAL arm and a null-pointer-constant (literal-0) arm (`cond ? "%s" : 0`) decays the string literal to char* and treats 0 as a null pointer → the POINTER type, not an aggregate.** ROOT (the c65 re-probe's FIRST error — c65 cleared the p-arrayName crash so the compile reached this; H0009): sqlite `(sParse.zErrMsg ? "%s" : 0)` (sqlite3.c:161910, a printf-family format arg). combineTernary (BOTH tiers): the c49 null-ptr arm needs the non-0 arm already a Ptr (the string literal is an un-decayed Array); the c56 FnSig arm misses; the c64 array-decay arm needs BOTH arms arrays (the 0 is an int) → all miss → the fallback typed it Array<char,3> → the by-address aggregate-ternary lowering tried to string-materialize the literal-0 arm into a char[3] slot → the Literal pool entry is an INTEGER, not a string → H0009 ([[D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY-LITERAL-DECAY]] producer guard, hir_to_mir.cpp:2335). FIX (NOT §B; C 6.3.2.1p3 + 6.5.15p6; 2 symmetric edits, the c49/c56/c64 sibling): both combineTernary null-pointer blocks (semantic_analyzer.cpp + cst_to_hir.cpp) gain an Array-arm + literal-0 arm → the array decays to Ptr<elem> (interner.operands(arr)[0] → interner.pointer(elem)); at the HIR tier `common`=Ptr<elem> drives the coerce → the Array arm hits the Array→Ptr decay Cast (a GlobalAddr to the rodata string), the literal-0 the null-ptr Cast. Handles BOTH arm orders; non-char arrays decay too (`cond ? intArr : 0` → int*). PRESERVE: c49 `ptr:0`, c56 `fn:0`, c64 `arr:arr`/`arr:ptr` unchanged; a NON-zero int arm (`cond ? "%s" : 1`) is not literal-0 → not caught; a genuine struct arm is not an Array → c66 skips it (the by-address lowering). WITNESS: corpus `ternary_string_null` (the `take(cond?"%s":0)` call-arg shape + the fmt() return form + the reverse `cond ? 0 : "err"`; reads each string through the decayed ptr + each null arm == 0 → 42; RED-ON-DISABLE → the exact sqlite H0009 'the Literal pool entry is not a string arm'). ★★ 3-LEG RUN: x86_64-elf SysV + arm64-qemu AAPCS64 RAN 42, gcc-cross-checked (-O0/-O2/arm64); Win 490/490 + WSL c-subset 305/305; independent adversarial audit + 3-leg gate. | (closed — the combineTernary Array+literal-0 arm, both tiers) | p19 Cluster G c66; the c65 re-probe's FIRST error; the c49/c56/c64 ternary-arm-decay siblings ([[D-CSUBSET-TERNARY-NULL-POINTER]] / [[D-CSUBSET-TERNARY-NULL-FUNCTION-POINTER]] / [[D-CSUBSET-TERNARY-ARRAY-DECAY]]); routes around [[D-LK4-RODATA-PRODUCER-NONSTRING-ARRAY-LITERAL-DECAY]] (the producer guard, untouched); `src/analysis/semantic/semantic_analyzer.cpp` + `src/hir/lowering/cst_to_hir.cpp` (combineTernary, both tiers); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-TERNARY-ARRAY-ARM-INCOMPATIBLE` | ⏳ **DEFERRED (pre-existing, surfaced by c66's audit, 2026-06-30; NOT a miscompile).** Two residual `array : X` conditional cases c66 does NOT cover (c66 closed `array : literal-0`): (1) **`cond ? "%s" : 1`** (an array arm + a NON-zero int) — a C 6.5.15 constraint violation (gcc warns / `-pedantic-errors` rejects); dss accepts it WITHOUT a diagnostic but emits sound codegen MATCHING gcc-default error-recovery runtime (both exit the same → NO miscompile, only a diagnostic-completeness gap). (2) **`cond ? "%s" : (void*)0`** (an array arm + an explicit `void*`, not a bare literal-0) — bypasses c66 (the 0 isn't a bare literal) and c64's array-decay (char ≠ void element) → reaches the aggregate-Ternary lowering → H0009 (fails LOUD, safe, not silent); the broader `array : void*`/incompatible-pointee conditional. NOT confirmed in sqlite (sqlite's null-format idiom is the bare `? "%s" : 0` that c66 handles). FIX (when needed): (1) a loud S-diagnostic for an array/pointer arm paired with a non-null-constant int; (2) decay the array arm to Ptr<elem> then admit the `void*` arm via the c50 implicitToVoidPtr rule (→ a `void*` conditional). | (open) | p19 c66 audit; [[D-CSUBSET-TERNARY-NULL-STRING-LITERAL]] (c66, the literal-0 case it extends); the c50 array→void* + c64 array-decay precedents; plan 23 full-C arc |
| `D-CSUBSET-AGGREGATE-GLOBAL-SYMBOL-ADDRESS` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c67; §B Option A USER-chosen) — a file-scope aggregate global with LINK-TIME-CONSTANT members (function address / &global / string literal) is emitted as STATIC DATA with abs64 RELOCATIONS at the member offsets (the F5 [[D-CSUBSET-SYMBOL-ADDRESS-GLOBAL]] scalar mechanism generalized to aggregate MEMBERS).** ROOT (the c66 re-probe's FIRST error, H_UnsupportedLoweringForKind ×64 — UN-MASKED by c66, NOT a regression: a gdb/diagnostic agent proved the raise line predates Cluster G + c66 cleared the last FUNCTION-body blocker so emitGlobals_ runs for the 1st time): sqlite `aSyscall[]={{"open",(sqlite3_syscall_ptr)posixOpen,0},…}` (sqlite3.c:40875) + ~64 global tables (Sqlite3Config, sqlite3_io_methods, FuncDef arrays…). evaluateConstant folds link-time constants (&x, fn addresses) only for TOP-LEVEL scalars (F5 tryClassifyAsSymbolAddr), NOT aggregate members → the brace-init fell to runtimeInit → emitGlobals_'s lowerExpr(aggregate) tripped the FC8 anti-resurrection guard (hir_to_mir.cpp:1067, [[D-CSUBSET-BITFIELD-RVALUE-RUNTIME]] — routed AROUND, not re-opened). §B RESOLVED by the USER → Option A (static data + relocs; NOT runtime-init, which would segfault a const aggregate writing to .rodata). FIX (NOT a workaround; C-correct, gcc-matching; 4 edits): (hir_to_mir.cpp) a recursive `tryClassifyAggregateConst` builds a MirAggregateValue resolving each member via tryClassifyAsSymbolAddr / nested recurse / a new `tryClassifyNullPointerConst` (the `0`/`(void*)0` member) / evaluateConstant [any unresolved → whole-aggregate fallback], + `tryClassifyAsSymbolAddr` extended (a bare Ref to a FUNCTION symbol = decayed fn-designator → its address; a Ptr-result Cast wrapping a symbol-address → peel+recurse, Ptr-gated so `(long)&x` can't reach it), wired into classifyGlobals; (asm.cpp) `encodeAggregateValue` threads a relocs out-param + on a MirSymbolAddrValue leaf pushes a Relocation{base+memberOffset, target, abs64, addend} over the pre-zeroed 8-byte slot, + the aggregate data arm forces `.data` when relocs present (a reloc-bearing aggregate is load-writable even if const — the F5 .data rationale; Mach-O PIE can't rebase __TEXT,__const); (dce.cpp) the F5 liveness fixpoint now recursively descends MirAggregateValue (`collectSymbolAddrTargets`) so a function referenced ONLY via an aggregate-member reloc isn't DCE'd → K_SymbolUndefined in release. WITNESS: corpus `aggregate_global_symbol_addr` (struct + array-of-struct with fn-address + &global + string + null-fn-ptr members; runtime-verified — calls the fn-ptr, derefs &target through a nested struct, reads the rodata string, checks the null fn-ptr → 42; RED-ON-DISABLE → H_UnsupportedLoweringForKind). ★★ INDEPENDENT AUDIT SOUND, all 3 RUN legs (x86_64-elf SysV + arm64-qemu AAPCS64 + Win-pe → 42, gcc-checked): objdump both arches = .data + abs64 relocs with correct nested struct-in-struct offsets + array-of-struct stride 24 + string addends + null=zero-slot; const-aggregate-with-fn-ptr → .data; the c65-style OVER-FIRE hunt CLEAN [`(long)&x` fails loud; `struct{int v;}g2={n}` folds the VALUE not a spurious reloc to n]; DCE keeps the reloc-only fn, removes the dead one; 307/307 WSL+Win. Residual (pre-existing, fail-loud, NOT c67): [[D-CSUBSET-AGGREGATE-GLOBAL-NONSYMBOL-PTR-MEMBER]]. | (closed — tryClassifyAggregateConst + the encoder reloc-threading + the DCE aggregate-awareness) | p19 Cluster G c67; §B Option A USER; the c66 re-probe's H_UnsupportedLoweringForKind ×64 (un-masked); generalizes F5 [[D-CSUBSET-SYMBOL-ADDRESS-GLOBAL]]; routes around [[D-CSUBSET-BITFIELD-RVALUE-RUNTIME]] (FC8 guard); `src/mir/lowering/hir_to_mir.cpp` + `src/asm/asm.cpp` + `src/opt/passes/dce.cpp`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-MUTABLE-CHAR-ARRAY-RODATA` | ✅ **CLOSED 2026-06-30 (folded into the c67 cycle by USER request — c67 was editing the same asm.cpp global-emission code, so the user said fix it WHILE there, not post-green) — a MUTABLE file-scope `char m[N]="str"` global is the array OBJECT (C 6.7.9 mutable storage) → writable `.data`, NOT read-only `.rodata`.** ROOT (PRE-EXISTING, NOT a regression; was a user-queued post-green follow-up [[project_deferred_mutable_char_array_rodata_fix]]): asm.cpp lowerMirGlobalsToDataItems' string-literal arm forced `d.section = Rodata` UNCONDITIONALLY for EVERY std::string-valued global → a runtime `m[0]='J'` SEGFAULTs (the array is in .rodata). Correct ONLY for the SYNTHETIC string-literal-pool global a `char *p="hi"` points to (immutable bytes), WRONG for a NAMED user array (the array IS the mutable storage). The const flag ALONE was insufficient: materializeStringLiteralGlobal minted its synthetic string global isConst=FALSE (default) yet its bytes must stay rodata. FIX (2 edits): (hir_to_mir.cpp materializeStringLiteralGlobal) mint the synthetic string-pool global isConst=TRUE (the bytes are immutable — matching tryClassifyAsSymbolAddr's string arm which already did) → routes to .rodata via the const selection; (asm.cpp) DROP the unconditional .rodata override → `isConst` ALONE selects the section: a SYNTHETIC pool global (now const) → .rodata, a NAMED mutable `char arr[N]="str"` (non-const declaration) → writable .data. WITNESS: corpus `mutable_char_array_global_write` (write `m[0]='J'` to a mutable global + read a const char[] + read `char *p="hi"` bytes → 42; RED-ON-DISABLE → restore the override → the write SIGSEGVs exit 139 both arches). ★★ INDEPENDENT AUDIT SOUND, all 3 legs: objdump = `m`("hello") in .data, `c`("abc") + a function-body literal in .rodata, `p`→"hi" in .rodata (preserved — a write THROUGH p is UB, must still fault); no regression (decl_string_global + string_array_zero_fill [c62] green, 307/307). | (closed — synthetic-string isConst=true + drop the asm override) | folded into p19 c67 by USER request; [[project_deferred_mutable_char_array_rodata_fix]] (DONE); the F5/D-LK4 string-producer precedents; `src/mir/lowering/hir_to_mir.cpp` (materializeStringLiteralGlobal) + `src/asm/asm.cpp`; plan 23 full-C arc |
| `D-CSUBSET-AGGREGATE-GLOBAL-NONSYMBOL-PTR-MEMBER` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c68; NOT §B — clean generalizations of c43 + F5/c67) — the sqlite-blocking shapes of an aggregate-global member c67's tryClassifyAggregateConst couldn't fold.** ROOT (the post-c67 re-probe took the FC8 aggregate-rvalue guard [[D-CSUBSET-BITFIELD-RVALUE-RUNTIME]] from 64→3; the 3 survivors = sqlite's static FuncDef tables aBuiltinFunc[]@136608 / aJsonFunc[]@217996 / aWindowFuncs[]@176870, each an array of struct FuncDef with ONE un-foldable member → whole-aggregate bail → runtimeInit → guard): a delegated diagnostic agent (instrument classifyGlobals + quick_exit before the 12-min fn-lowering = front-end-time root-cause) pinpointed TWO member shapes. **Class A — pUserData = SQLITE_INT_TO_PTR(X) = `(void*)&((char*)0)[X]`** (a NULL-base array-element address constant = the integer X as a pointer): new `tryClassifyNullBaseIndexConst` (sibling of c67's tryClassifyNullPointerConst) peels ptr cast(s)→AddressOf→Index(base,X), folds X, emits `X*computeLayout(elem).size` as a reloc-free Ptr leaf; base MUST fold NULL else nullopt (CONSERVATIVE — `&realArray[i]` stays fail-loud, never silently the integer; stride=sizeof(elem) not 1). The array-element sibling of c43's offsetof MEMBER folding [[D-CSUBSET-ADDRESS-CONSTANT-FOLD]] (which the CST const-eval punted to "the HIR/global-init engine"). **Class B — zName = `name##Name`** (a bare Ref to a static const char[] global → array-decay to &arr[0]): tryClassifyAsSymbolAddr's bare-Ref arm extended from functionSymbols to ALSO accept a globalSymbols Ref whose type is Array → {sym,0} (scalar-global Ref stays an rvalue load, rejected); c67's reloc encoder + DCE-liveness handle the rest. WITNESS: corpus `aggregate_global_nonsymbol_ptr_member` (array-of-struct: pUserData=INT_TO_PTR(7/11/0) + zName=array-ref nameA/B/C; byte-content verified → 42; RED-ON-DISABLE both classes → H_UnsupportedLoweringForKind). ★★ INDEPENDENT AUDIT SOUND, all 3 RUN legs (Win-pe 493/493 + x86_64-elf + arm64-qemu RAN, gcc-cross-checked -O0/-O2): objdump = pUserData raw ints 7/11/0 NO reloc + zName abs64 relocs addend 0 (gcc byte-equivalent R_X86_64_64 .rodata+0/+6/+0xb); OVER-FIRE CLEAN [symbol-base `&realArray[2]`→fail-loud not (void*)2; scalar-global value→fail-loud; ★ STRIDE `&((int*)0)[3]`==12 elf+arm64]; 308/308 WSL no regression. ★ RESIDUAL (deferred, fail-loud, non-sqlite): [[D-CSUBSET-AGGREGATE-GLOBAL-SYMBOL-BASE-INDEX]] (a SYMBOL-base `&realArray[N]`/`&s.field`, + an all-null/non-null-`(void*)0x1000` aggregate). ★ DISCOVERED (pre-existing, NOT c68): [[D-CSUBSET-ARRAY-DECAY-POINTER-IDENTITY]] (a silent all-target run-green miscompile on `p == arrayName`). | (closed — tryClassifyNullBaseIndexConst + the tryClassifyAsSymbolAddr Array-Ref arm) | p19 Cluster G c68; the post-c67 re-probe's 3 survivors; generalizes c43 [[D-CSUBSET-ADDRESS-CONSTANT-FOLD]] (Index form) + F5/c67 [[D-CSUBSET-AGGREGATE-GLOBAL-SYMBOL-ADDRESS]] (Array-Ref); `src/mir/lowering/hir_to_mir.cpp`; splits [[D-CSUBSET-AGGREGATE-GLOBAL-SYMBOL-BASE-INDEX]]; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-AGGREGATE-GLOBAL-SYMBOL-BASE-INDEX` | ✅ **CLOSED 2026-07-02 (p19 Cluster G c80) — a symbol-base address constant with a computed offset folds to an abs64 reloc + ADDEND in EVERY position (top-level scalar AND aggregate member): `&arr[CONST-EXPR]` (Index, stride-scaled), `&rec.field` (MemberAccess, fieldByteOffset), nested `&grid[i][j]` / `&rec.tail[k]` (recursive composition).** ROOT (the c80 sqlite frontier, INSTRUMENTED, agent a57cb9b6): 3 of the 9 K_NoMatchingObjectFormat runtime-init globals were sqlite's `sqlite3aLTb/aEQb/aGTb = &sqlite3UpperToLower[256-OP_Ne]` (sqlite3.c:24077-79) — the TOP-LEVEL SCALAR form of this anchor (C 6.6p9 address constants; gcc emits `.quad sym+addend`); the other 6 were bare `T* g = 0;` top-level null-pointer-const scalars (c67's tryClassifyNullPointerConst was wired only into the aggregate member loop). FIX (hir_to_mir.cpp + asm.cpp, NO §B — the c67/c68 fold-extension path): a new recursive `tryResolveConstLvaluePath` (Ref→{sym,0}; Index→+idx×elementStride; MemberAccess→+fieldByteOffset; conservative gates: Index base must be Array-typed [`&ptrGlobal[3]` stays fail-loud, gcc rejects it too], MemberAccess base Struct/Union); tryClassifyAsSymbolAddr's AddressOf arm delegates to it; the classifyGlobals cascade gains tryClassifyNullPointerConst + tryClassifyNullBaseIndexConst arms for top-level scalars; the asm.cpp scalar arm sizes pointer-class scalars via the DataModel chokepoint. WITNESSES: examples/c-subset/global_symbol_base_index (the sqlite trio shape + stride>1, =42) + global_null_pointer_init (bare `T*=0`, mutable, write-through+fn-ptr, =42) + global_address_path_members (member/field/nested positions + write-through, =42); .data addends byte-exact vs gcc; RED-ON-DISABLE → K_NoMatchingObjectFormat. Post-fix sqlite: 9→0 runtime-init errors — the LAST compile-stage frontier cleared; sqlite3.c's OWN compile surface is CLEAN (the sole remaining probe error = K_SymbolUndefined entry-trampoline — a main-less LIBRARY TU vs an -exec format = the c81 entry/driver decision, NOT a compile gap). RESIDUALS (fail-loud, NOT sqlite, named): `(T*)0x1000` non-null int-to-pointer; the no-AddressOf decay form `arr + 3`. Was: DEFERRED (residual of c68, 2026-06-30; fail-loud, non-sqlite). An aggregate-global member that is a SYMBOL-base address constant with a (possibly non-zero) offset — `&realArray[N]` (AddressOf(Index) on a REAL array global) or `&s.field` (AddressOf(MemberAccess)) — fails LOUD (H_UnsupportedLoweringForKind) where gcc emits a reloc with the element/field-offset addend. c68's tryClassifyNullBaseIndexConst handles only a NULL base (→ a bare integer, no reloc); a symbol base returns nullopt BY DESIGN (its value is symbol+N*stride, a reloc — NOT the bare integer; must not be mis-folded, the c68 audit confirmed `&realArray[2]`→fail-loud). Also an all-null `(void*)0`-ONLY aggregate or a non-null `(void*)0x1000` integer-to-pointer member (no AddressOf) still falls through. Fail-loud (safe), NOT a silent miscompile. NOT confirmed in sqlite. FIX (when needed): extend tryClassifyAsSymbolAddr / tryClassifyNullBaseIndexConst to AddressOf(Index(symbolBase,N)) / AddressOf(MemberAccess(symbolBase,field)) → {sym, N*stride or fieldOffset} (c67's encoder already threads the addend); a zero/non-null-ptr-aggregate path for the no-AddressOf cases. | (open) | p19 c68 residual; [[D-CSUBSET-AGGREGATE-GLOBAL-NONSYMBOL-PTR-MEMBER]] (c68, the null-base sibling it closed); overlaps c43 [[D-CSUBSET-ADDRESS-CONSTANT-GLOBAL-FOLD]] (the symbol-base address-fold); plan 23 full-C arc |
| `D-CSUBSET-ARRAY-DECAY-POINTER-IDENTITY` | ⏳ **DEFERRED (discovered by c68's corpus build, confirmed pre-existing + all-target by c68's audit, 2026-06-30; a SILENT run-green MISCOMPILE).** Two array-to-pointer DECAYS of the same `static const char[]` global compare pointer-UNEQUAL: `const char *p = nameA; (p == nameA)` is FALSE under dss (a corpus returns 1) where gcc returns 42 — the bytes each pointer addresses ARE correct (deref works), only the IDENTITY is wrong. ★ Reproduces on ALL THREE targets (pe + x86_64-elf + arm64-qemu → 1; gcc → 42) — NOT platform-specific (the c68 audit refined an initial pe-only read). c68-INDEPENDENT (a scalar-local compare, never touching the global-init classifier — proven with zero c68 code). Pure-F5 `int *pg=&g; pg==&g` PASSES → specific to ARRAY-decay (an array name decaying to a pointer at two sites yields inequal values), not scalar `&global`. NOT how sqlite uses the c68 members (zName→strcmp, pUserData→PTR_TO_INT, fn-ptrs CALLED), so c68's corpus verifies by byte-content not identity. ★ Per [[feedback_address_any_sqlite_green_blocker_2026_06_30]]: IN-SCOPE the moment a re-probe shows sqlite mis-RUNNING due to it (sqlite has `zName == <globalStr>` fast-paths) — the HIGHEST-priority deferred run-green miscompile; we are still at the linux COMPILE stage so it is not the current blocker. FIX (when needed): make array-to-pointer decay produce a consistent symbol-relative pointer at every decay site (likely a cst_to_hir / codegen decay-lowering issue, NOT the global-init path); corpus `array_decay_identity` (`p==arrayName`, `&arr[0]==arr`, cross-TU). | (open) | discovered p19 c68 (corpus build + audit); a SILENT all-target run-green miscompile (the user's run-green-miscompile-in-loop mandate applies once sqlite RUN surfaces it); plan 23 full-C arc / SQLite-RUN-green |
| `D-CSUBSET-POINTER-INT-ARITHMETIC` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c41) — `p ± n` (pointer ± integer) stride-scaling; a RUN-green MISCOMPILE FIX** (user: include miscompile fixes in the cycles; this CLOSES the c40-deferred p±n a.k.a. `D-CSUBSET-POINTER-INTEGER-ARITH-SCALING`). ROOT (the most pervasive flagged silent miscompile — sqlite indexes pointers everywhere): binary `p + n` / `n + p` / `p - n` in `combineBinary` emitted a RAW Add/Sub with NO sizeof scaling (a 1-BYTE stride for ALL element types) — commonArithType returns invalid for a Ptr+Int pair → the fallback typed it `Ptr<T>` (correct type) but MIR `mapBinaryOp(Add, Ptr)` emitted a raw byte add. char* (stride 1) correct; int*/struct* WRONG (off by sizeof). COMPILES (re-probe can't catch it) but miscompiles at RUN time. C 6.5.6p8: `p + n` = the n-th element address = p + n*sizeof(*p). (`n + p` was even mis-TYPED as Int.) FIX (NOT §B; 3 sites; REUSES F1's scaleIndexToBytes/Gep — the SAME stride machinery p[i]/p++ use): (1) cst_to_hir `combineBinary` — CANONICALIZE `n + p` (swap so the Ptr operand is always kids[0]) + type the result Ptr; (2) hir_to_mir `combineBinaryOp` — when op∈{Add,Sub} + result-kind Ptr + kids[0]-kind Ptr → scaleIndexToBytes(n, pointee) then Gep(p, byteOff); for Sub Neg(n) first → Gep(p, -(n*stride)); (3) semantic `subtreeType` combineBinary — type `n + p` as Ptr (not Int). ★ MUTUALLY EXCLUSIVE: c40's p-q fires on result-kind I64 (both-Ptr); c41 on result-kind Ptr (one-Ptr-one-int); F1's p[i]/p++ never reach combineBinaryOp (the Index path) → NO double-scaling. `n - p` (no C meaning) + void*/fn-ptr pointee → unhandled / fail-loud. WITNESS: 4 semantic pins (p+n/n+p/p-n typed Ptr; int+int unaffected) + runtime corpus `pointer_int_arith` (int* stride-4 + struct* stride-8, p+n/n+p/p-n/NEGATIVE-n → exit 42; red-on-disable: revert the Gep intercept → 1-byte stride → wrong slot → 1). The existing F1 p++/p[i] + c40 p-q MIR pins stay GREEN. 462→463 ctest. | (closed — canonicalize + Gep stride scaling IS the fix) | p19 Cluster G c41; the run-green pointer-arith fix family (with c40 `D-CSUBSET-POINTER-SUBTRACTION`); reuses F1 scaleIndexToBytes/Gep; `src/hir/lowering/cst_to_hir.cpp` + `src/mir/lowering/hir_to_mir.cpp` + `src/analysis/semantic/semantic_analyzer.cpp`; a flagged miscompile fixed IN-loop (user); plan 23 full-C arc / SQLite-RUN-green |
| `D-CSUBSET-INDEX-NEGATIVE-WIDEN` | ✅ **CLOSED 2026-06-28 (p19 Cluster G c42) — a NEGATIVE array subscript `p[-n]` no longer segfaults; a RUN-green MISCOMPILE FIX** (user: include miscompile fixes in the cycles; surfaced by the c41 audit as a PRE-EXISTING crash one path over from c41's p±n). ROOT (run-green-critical — sqlite uses `p[-1]` lookback): the F1 Index path's Gep runtime index is a byte offset (a 32-bit `int` subscript, optionally scaled by a stride `Mul`) that reached the 64-bit `lea` / arm64-`ADD` address computation UN-widened — a NEGATIVE 32-bit value (high bit set, e.g. -1*12 = 0xFFFFFFF4) was consumed with garbage upper-32-bits → a huge positive address → SIGSEGV. The SAME 32-bit-negative-zero-extend bug c41 fixed for p±n, but in the Index path (`combineIndexAddr` / `scaleIndexToBytes` feed). ★ DEBUG-ONLY crash: in release ConstFold collapses a constant `Mul(-1,stride)` to a Const that takes the `lowerGep` constant-disp32 path (hardware sign-extends disp32) → masks it; the BASELINE/DEBUG pipeline is the witness. (`*(p-1)` worked — a pointer-arith Deref, a DISTINCT path; only `p[-1]` crashed.) FIX (NOT §B; ONE function `lowerGep`, LIR tier; NO HIR/MIR/grammar/config change): in the runtime-index branch (AFTER the constant-disp guard), inspect `interner.kind(mir.instType(operands[1]))` and widen a sub-64-bit index to a full 64-bit register BEFORE the lea — SExt for a signed source (Char/I8/I16/I32), ZExt for unsigned (U8/U16/U32), NO widen for I64/U64/Ptr (c41's pre-widened p±n offset → no double-extend) or enum/other (unchanged behaviour). The kind lists MIRROR the SExt/ZExt source-width dispatch gates in the same file → every synthesized widen is one those gates accept; the width flag is `widthFlagsForType(idxTy)` (the SOURCE width). ★★ WHY LIR not MIR (the architect's re-architecture): a FIRST attempt widened in `scaleIndexToBytes` (MIR) and REGRESSED register pressure — `deep_lvalue_chain` (150-deep, all-Const(0) indices) blew the regalloc scratch pool because a MIR SExt added an address-spanning vreg per index. The LIR fix sits AFTER the constant-disp guard, so deep_lvalue_chain's Const(0) indices take the disp path and NEVER reach the widen (no added vregs); a runtime-index widen vreg is short-lived (consumed by the next lea) + coalesceable. WITNESS: runtime corpus `index_negative` (strides 1/4/12 — char*/int*/struct-Trip*; CONSTANT `p[-1]`/`p[-4]` [debug: a runtime stride Mul → the crash] AND RUNTIME `int k=-3; p[k]`; 8 sub-cases → exit 42; RED-ON-DISABLE [PROVEN]: revert the lowerGep widen + rebuild → 0xC0000005 ACCESS_VIOLATION on the first runtime-Mul subscript in debug). deep_lvalue_chain + c40 ptr_subtraction + c41 pointer_int_arith stay GREEN. 463→464 ctest. ★ INDEPENDENT AUDIT (87 probes, debug+release, x86): the CORE widen VERIFIED CORRECT + regression-free — every stride/sign/width/expression-form/enum index reads the right element, no-regression paths (positive p[i], multi-dim, `*(p-1)`, member access) intact, and the register-pressure hypothesis CONFIRMED by a causation bisect (the debug n=11 regalloc cliff is PRE-EXISTING — identical for `int` [widened] vs `long` [not] indices → the widen does not shift it). ★★ THE AUDIT CAUGHT AN OVER-ATTESTATION (fixed before commit): the corpus claimed all-4-targets/release but arm64-RELEASE does NOT compile — ConstFold folds the negative subscript to a NEGATIVE-displacement lea, which arm64 cannot encode (its `lea` has only non-negative ADD-imm12 variants — a SEPARATE PRE-EXISTING gap, proven c42-independent: the constant-disp path that c42 never touches fails identically). FIXED HONESTLY: the corpus now runs BASELINE-only on all 4 targets (the bug is debug-only; the runtime variable-subscript cases exercise the widen via the register path incl. arm64 SXTW under qemu) with NO optimizedPipelines arm (a global ConstFold arm would emit the un-encodable arm64 negative-disp lea on a qemu-CI runner); the arm64 negative-disp lea is DEFERRED `D-AS4-ARM64-NEGATIVE-DISP-LEA` (c43 — run-green-critical for sqlite arm64-release). Comments corrected (the audit's minor flags): a subscript integer-PROMOTES (C 6.3.1.1) so only the I32-SExt / U32-ZExt / 64-bit-noop arms fire — the narrow Char/I8/I16/U8/U16 arms are DEFENSIVE (mirror the dispatch gates), and an enum index → I32 → SExt (NOT the `default` arm the old comment claimed). | (closed — the one-function LIR index widen + the honest baseline-only corpus IS the fix; arm64-release neg-disp lea split to c43) | p19 Cluster G c42; the run-green pointer-arith fix family (c40 p-q `D-CSUBSET-POINTER-SUBTRACTION` + c41 p±n `D-CSUBSET-POINTER-INT-ARITHMETIC` — c42 is the Index-path sibling of c41's I64 widen); reuses the SExt/ZExt LIR dispatch gates; `src/lir/lowering/mir_to_lir.cpp` (`lowerGep` runtime-index branch); a flagged miscompile fixed IN-loop (user); plan 23 full-C arc / SQLite-RUN-green |
| `D-AS4-ARM64-NEGATIVE-DISP-LEA` | ⏳ **DEFERRED (surfaced by the c42 audit, 2026-06-28).** arm64's `lea` mnemonic (arm64.target.json ~line 1146) has NO negative-displacement variant — its frame-relative forms are `ADD Xd,Xn,#imm12` guarded NON-NEGATIVE (immMax:4095 / shifted imm / movz-movk, all ≥0) and the base+index form requires `memoffset.zero`. So a NEGATIVE constant displacement (e.g. `p[-1]` once ConstFold folds `Mul(-1,sizeof)` → a negative Const disp) matches NO variant → `A_NoMatchingEncodingVariant opcode 'lea' at width 64`. PRE-EXISTING + c42-INDEPENDENT (the constant-disp `lowerGep` path c42 never touched fails identically; c42's `index_negative` corpus is just the FIRST example to emit a negative-disp lea, surfacing it). x86 is unaffected (`lea [base+disp32]` sign-extends disp32 in hardware). ★ CI EXPOSURE: harmless on a Windows host (arm64 cross-skipped) but on an x86-linux runner with qemu-aarch64 (the project's bar — host+wsl+qemu) the arm64-elf baseline RUNS → the optimized arm compiles for arm64 → FAILS; so a negative-subscript corpus must run BASELINE-only on arm64 until this lands (c42's `index_negative` does exactly that). **Trigger:** sqlite arm64-RELEASE run-green (sqlite uses `p[-1]` lookback heavily → ConstFold emits negative-disp leas), or re-enabling a ConstFold/release arm on a negative-subscript corpus. **Closing work (c43; config + likely a small encoder slot):** add an arm64 `lea` variant guarded on a NEGATIVE memoffset emitting `SUB Xd,Xn,#imm12` (0xD1000000) over the ABSOLUTE disp — needs a slotKind that negates a negative memoffset into imm12 (mirror the positive ADD-imm12 / shifted / movz-movk ladder for the full range), OR fold a negative constant disp into the index-register path (disp=0) at lowerGep. Then re-add `release` + `full-release-like` to `index_negative/expected.json` + a constant-`p[-1]`-on-arm64-release pin. | (open) | p19 c42 audit; `D-CSUBSET-INDEX-NEGATIVE-WIDEN` (c42 — the LIR widen handling the arm64 DEBUG/register path; this is the arm64 RELEASE/disp companion); `src/dss-config/targets/arm64.target.json` (`lea` variants ~1146-1237); a sqlite arm64-release run-green frontier; plan 23 full-C arc / SQLite-RUN-green |
| `D-CSUBSET-ADDRESS-CONSTANT-FOLD` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c43) — compile-time `offsetof` folding via a general ADDRESS-CONSTANT value kind (Option A).** ROOT (sqlite `struct uKey { u8 keyinfoSpace[SZ_KEYINFO_0]; }` @sqlite3.c:24984, SZ_KEYINFO_0=offsetof(KeyInfo,aColl) → S001C): DSS's CST const-evaluator (folds ARRAY DIMENSIONS at Pass 1.5) was integer-only and could not fold the portable offsetof idiom `((size_t)((char*)&((T*)0)->M - (char*)0))`, so an array sized by offsetof degraded to an incomplete-array FAM → S000B (plain) / S001C (sole member). §B RESOLVED by the user → Option A (a GENERAL address constant, NOT a null-only special-case): add `HirAddressValue{base(0=NULL),byteOffset,pointeeType}` to the HirLiteralValue variant (mirrors F5's MirSymbolAddrValue so toMirLiteral is a field copy) + teach the CST evaluator the address arms — `(T*)0`→{NULL,0}; `->M` adds the field's computeLayout offset; `&` passes the lvalue address through; `(char*)` retypes; `- (char*)0` collapses to the integer byte offset; `(size_t)`/`(unsigned long)` casts it. Two interner-backed closures (resolveCastTarget = classify the cast type incl. integer width/signedness for C-correct narrowing; resolveFieldOffset = compositeScopeByType→memberScope→fieldIndex + computeLayout.fieldOffsets[idx] + a bit-field guard) keep the engine interner-free; cast/postfix rules added to the wrapperChild isDispatched set so they reach the arms. ★ INDEPENDENT AUDIT (36 min, 120 probes, a PRE-c43-binary diff): the fold is CORRECT + REGRESSION-FREE — value-correct across 11 shapes incl. alignment + nested members + CROSS-ABI REAL RUNS (PE/ELF-WSL/arm64-qemu exit 42 on the exact sqlite KeyInfo-aColl@24 shape); 12 fail-loud cases (`(T*)5` non-null base / bit-field member / runtime-var dim / `(int)&g` symbol-addr / null deref / index → S000B/S000C); narrowing C-correct ((char)300→44); no silent miscompiles; no regression (casts didn't fold pre-c43). ★ The audit's lone over-attestation (`(size_t)` "parses as a call, doesn't fold") was a PROBE ARTIFACT (size_t UNDEFINED in the probe) — VERIFIED `(size_t)` FOLDS when size_t is a typedef (as sqlite has via <stddef.h>; sizet_form→exit 4); RESOLVED by STRENGTHENING the corpus with the canonical `(size_t)` form. ★ SCOPE: the value kind is the GENERAL Option-A kind (symbol-or-null base) but the CST engine PRODUCES only null-base (offsetof) this cycle; the &global/string-addr folding + the F5 unification (the symbol-base USE) → DEFERRED `D-CSUBSET-ADDRESS-CONSTANT-GLOBAL-FOLD` (c44). WITNESS: corpus `offsetof_const` (sole-member structs sized by offsetof — alignment {char;double}→8, multi-field, the sqlite uKey shape, BOTH `(unsigned long)` + `(size_t)` cast forms → exit 42; RED-ON-DISABLE: revert the address arms → S001C). 464→465 ctest. NOTE: the `(undeclared-typedef)(expr)` cast-vs-call ambiguity (the audit's probe case) is a separate pre-existing parser characteristic, NOT a sqlite blocker. | (closed — the value kind + the CST address arms + the 2 closures IS the fix) | p19 Cluster G c43; the address-constant value kind (Option A, §B user-chosen); reuses F5 `D-CSUBSET-SYMBOL-ADDRESS-GLOBAL` MirSymbolAddrValue (toMirLiteral mirror) + computeLayout; `src/hir/hir_literal_pool.hpp` + `src/hir/cst_const_eval.{hpp,cpp}` + `src/analysis/semantic/semantic_analyzer.cpp` + `src/hir/hir_text.cpp` + `src/mir/lowering/hir_to_mir.cpp` + `src/opt/passes/const_fold.cpp`; defers `D-CSUBSET-ADDRESS-CONSTANT-GLOBAL-FOLD`; the sqlite S001C@24984 frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-ADDRESS-CONSTANT-GLOBAL-FOLD` | ⏳ **DEFERRED (surfaced by c43, 2026-06-29).** c43 added a GENERAL address-constant value kind `HirAddressValue{base,byteOffset,pointeeType}` (base = a SYMBOL or NULL) + CST offsetof folding (NULL-base only). The SYMBOL-base capability — folding `&global`, `&global + N`, `&arr[i]`, string-literal addresses as compile/link-time address constants — is NOT yet PRODUCED by either evaluator (the CST engine fails loud on `(int)&g`; the HIR engine's AddressOf/MemberAccess arms are unwritten). The `toMirLiteral` `HirAddressValue→MirSymbolAddrValue` mapping IS in (forward scaffolding, untested-until-used; the asm symbol-0 guard is the safety net). **Closing work (c44, completes Option A):** (a) HIR-engine address arms (const_eval.cpp: AddressOf of a global→{sym,0}, MemberAccess/Index offset accumulation, addr±int, Cast) sharing a `const_eval_address.hpp` helper with the CST engine; (b) the F5 UNIFICATION seam at hir_to_mir.cpp:7040 `classifyGlobals` — route a folded `HirAddressValue` into `symbolAddrInit`/`symbolAddrAddend` (KEEP `tryClassifyAsSymbolAddr`'s side-effecting string-rodata mint this cycle; regression guard = `int*p=&g;` emits an identical reloc + `&g+2` gains the addend F5 lacks); (c) corpus `address_const` (`&arr[3]-arr`==3, `int*p=&g+2`→reloc vs g addend 8). NOT on the sqlite compile path (sqlite's offsetof is null-base) — this is the Option-A generality. **Trigger:** completing Option A, or a consumer needing `&global+offset` constant inits. | (open) | p19 c43; `D-CSUBSET-ADDRESS-CONSTANT-FOLD` (c43 — the value kind + null-base offsetof folding this extends); F5 `D-CSUBSET-SYMBOL-ADDRESS-GLOBAL` (the MIR symbol-address path to unify); plan 23 full-C arc |
| `D-CSUBSET-SHIPPED-TYPEDEF-CAST-PARSE` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c44) — a SHIPPED-typedef cast `(size_t)(expr)` parses as a CAST, not a call (the REAL sqlite S001C blocker; c43's offsetof fold never reached it).** ROOT: shipped-header typedefs (`size_t` from <stddef.h>, injected SEMANTICALLY post-parse) are invisible to the parser's binder-sketch type-name table, so `(size_t)((char*)&((ST*)0)->M-(char*)0)` (sqlite's offsetof macro) disambiguated as a CALL `size_t(...)` → the `keyinfoSpace[offsetof(KeyInfo,aColl)]` array dim was non-constant → an incomplete-array FAM → S001C@sqlite3.c:24984. ★★ c43 (D-CSUBSET-ADDRESS-CONSTANT-FOLD) folds offsetof CORRECTLY but never RECEIVED it — the c43 corpus used a SOURCE typedef (which the parser DOES track), masking this; the RE-PROBE (ground truth, NOT a local repro) caught that c43 left S001C unchanged + that the c43 audit's `(size_t)` flag was RIGHT (my dismissal used a source typedef — the lesson: verify against the ACTUAL sqlite construct, shipped headers + the exact macro). §B RESOLVED by the user → Option A "oracle harvest" (over B "seed the first parse" = a bigger pipeline reorder): DSS already has a post-parse cast-vs-call ORACLE (compilation_unit.cpp finish()) that reparses a tree ONCE with cross-file/forward-ref typedef NAMES seeded as parse-time global types; the fix HARVESTS each resolved shipped descriptor's typedef NAMES into that `oracle` set — a NEW interner-free `readShippedLibTypedefNames` (mirrors readShippedLibAvailability: reads `typedefs[].name` only) + ~8 lines in finish() (scratch reporter, no double-report). So a tree that recorded an AmbiguousTypeNameCandidate for `size_t` now resolves it → reparses → commits the cast → c43 folds the offsetof. ★ ZERO mis-parse risk: a typedef name is NEVER callable in C, so treating a known-type `(IDENT)(expr)` as a cast is unconditionally correct; the fix only ADDS type-name knowledge (the binder sketch's rule-3 value-shadowing still wins; an undeclared `(notAType)(x)` stays a call). WITNESS: corpus `offsetof_shipped_typedef` (#include <stddef.h>; the bare `(size_t)(2+3)`==5 + the sqlite uKey shape `keyinfoSpace[(size_t)offsetof(K,m)]`→char[4] → exit 42; RED-ON-DISABLE: revert the harvest → S0004 + S001C). 465→466 ctest, no regression. ★ HONEST: the architect notes the SAME harvest LIKELY also resolves the sibling `D-CSUBSET-DESCRIPTOR-TYPEDEF-POINTER-DECL` (`int32_t * p` declaration-vs-multiply — same seedGlobalTypeNames→binder-sketch axis) but it is NOT independently verified here — do NOT close that anchor without a test. | (closed — the typedef-name reader + the oracle harvest IS the fix) | p19 Cluster G c44; the parser piece c43 (`D-CSUBSET-ADDRESS-CONSTANT-FOLD`) needed; §B Option A user-chosen; reuses the existing post-parse type-name oracle (the source-forward-ref reparse) + the readShippedLibAvailability interner-free-read precedent; `src/ffi/shipped_lib_descriptor.{hpp,cpp}` (readShippedLibTypedefNames) + `src/analysis/compilation_unit/compilation_unit.cpp` (the oracle harvest in finish()); likely also addresses `D-CSUBSET-DESCRIPTOR-TYPEDEF-POINTER-DECL` (untested); the sqlite S001C@24984 frontier; plan 23 full-C arc / SQLite-green |
| `D-FFI-STDIO-FILENAME-MAX` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c45) — FILENAME_MAX (<stdio.h>) shipped as a PER-TARGET compile-time constant.** ROOT (the sqlite frontier AFTER c43+c44 cleared the offsetof S001C — confirmed by the c44 re-probe that S001C@24984 was GONE, advancing to this): sqlite `#define SQLITE_MAX_PATHLEN FILENAME_MAX` (sqlite3.c:16681) sizes path buffers — a constant-expression array dim — but DSS's shipped <stdio.h> (stdio.json) carried only function SYMBOLS, no `constants` surface → FILENAME_MAX undefined → the dim non-constant → S000B. FIX (NOT §B; config-ONLY, NO C++): add FILENAME_MAX to stdio.json's `constants` surface, PER-TARGET via `variants` (the shared per-target `when` selector the structs/typedefs/macros surfaces use): elf 4096 (glibc) / macho 1024 (Darwin) / pe 260 (msvcrt), type i32. It folds at HIR + in const-expr position (the c-subset `constants` mechanism, like <limits.h> CHAR_BIT). Config is RUNTIME-loaded → the repro folds WITHOUT a compiler rebuild (VERIFIED directly: pe→260 [`char[FILENAME_MAX]==4096`? returns 1], elf→4096 [compiles]). WITNESS: corpus `shipped_filename_max` (#include <stdio.h>; `char buf[FILENAME_MAX]` → a TARGET-INVARIANT range [260,4096] → exit 42; a single exitCode can't assert the per-target value [pe 260 / elf 4096 / macho 1024], which lives in the descriptor + is verified directly; RED-ON-DISABLE: revert the constants addition → FILENAME_MAX undefined → S000B). 466→467 ctest, no regression. | (closed — the stdio.json constants entry IS the fix) | p19 Cluster G c45; the sqlite frontier after c43/c44 (S001C cleared); the c-subset `constants` shipped surface (c39's `D-FFI-STDINT-CONSTANT-MACROS` sibling — constants vs macros) + the per-target `variants` mechanism (c13/c15); `src/dss-config/shippedLibs/stdio.json`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-FFI-TIME-STRUCT-TIMESPEC` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c46) — ship `struct timespec` from <time.h>.** ROOT (the sqlite frontier after c45 cleared FILENAME_MAX — confirmed by the c45 re-probe ADVANCING S000B→S0028): sqlite os_unix `struct timespec sp;` (a BY-VALUE local, sqlite3.c:47379, fed to clock_gettime) → error[S0028] S_IncompleteTypeObject, because DSS's shipped <time.h> (time.json, c16) shipped `struct tm` + time_t/clock_t but NOT `struct timespec`. FIX (NOT §B; config-ONLY, NO C++): add `struct timespec` to time.json's `structs` surface — `{tv_sec:i64, tv_nsec:i64}` (16 bytes, 8-aligned), FLAT (no per-target variant) because BYTE-IDENTICAL on glibc(elf) + Darwin(macho) LP64 (tv_sec=time_t=i64; tv_nsec=`long`=i64 on LP64), like the existing `struct tm`. Gated availableObjectFormats:[elf,macho] (the FULL-UNIX-build targets; a pe build uses os_win, a separate axis). Config RUNTIME-loaded (folds without a rebuild). VERIFIED directly: an x86_64-elf binary RUNS in WSL → 42 (sizeof==16; tv_sec/tv_nsec round-trip at distinct 8-byte offsets, writing tv_sec doesn't clobber tv_nsec; a valid dynamically-linked elf vs libc.so.6). WITNESS: corpus `shipped_timespec` (#include <time.h>; `struct timespec sp` by-value, sizeof==16, 64-bit field round-trip + distinct-offset → exit 42; elf+macho targets [NO pe — time.json gated]; RED-ON-DISABLE: remove timespec from time.json → S0028). 467→468 ctest, no regression. | (closed — the time.json structs entry IS the fix) | p19 Cluster G c46; the sqlite frontier after c45 (FILENAME_MAX cleared → S0028); the c-subset `structs` shipped surface (c7 struct-body mechanism + c16 time.json struct tm + sys/stat.json per-target struct variants); `src/dss-config/shippedLibs/time.json`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-FNPTR-ARRAY-SIZE-INFERENCE` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c47) — an inferred-size `[]` array of FUNCTION POINTERS sizes from its initializer (the GROUPED-declarator generalization of c34).** ROOT (the sqlite frontier after c46 cleared S0028 — the c46 re-probe advanced to a NEW first error): sqlite `static int (*const sqlite3BuiltinExtensions[])(sqlite3*) = { sqlite3Fts5Init, … };` (sqlite3.c:186647) fired error[S000B] S_NonConstantArrayLength on the empty `[]`. c34 (D-CSUBSET-ARRAY-SIZE-INFERENCE) infers a SIMPLE array's size from its init, but here the `[]` lives on the INNER declarator inside the parenthesized group `(*const NAME[])(sig)`, folded by directDeclaredType's GROUP-recursion arm (semantic_analyzer.cpp:1970) which HARDCODED allowFlexibleArray=false — the c10 gate (FAM must not cross into a grouped inner declarator) → the inner `[]` hit emit(S_NonConstantArrayLength) at :1884. FIX (NOT §B; routine generalization): c34 CONFLATED init-inference into the single `allowFlexibleArray` flag (`decl.allowFlexibleArray || initNode.valid()`); blindly threading THAT into the group arm would SILENTLY turn `struct { int (*field[])(void); }` from S000B into an accepted FAM (a c10 behavior change — green≠clean). So SEPARATE the two semantics: a NEW `allowInitInferredArray` bool threaded through directDeclaredType/declaratorDeclaredType (default false on the forward decls), set from `initNode.valid()` at the Pass-1.5 site (:3086); the group arm passes `allowFlexibleArray=allowInitInferredArray` (init-inference crosses in; FAM stays false → c10 PRESERVED EXACTLY) + threads allowInitInferredArray for nested groups. applyDeclaratorSuffix UNTOUCHED; completion (completeIncompleteArrayFromInit) reused VERBATIM (one site sizes from the brace count → layout/sizeof/index/both-HIR agree). WITNESS: corpus `fnptr_array_inferred` (`int (*tbl[])(int) = {cb_a,cb_b,cb_c}` — a LOCAL; a RUNTIME-indexed indirect call tbl[i](45)+tbl[0](1)==40+2==42; all 4 targets + release; RED-ON-DISABLE: revert the group arm to false → S000B). ★ SURFACED the NEXT frontier (c48): the file-scope `static`/GLOBAL form (sqlite's exact shape — an array of fn-ptr ADDRESSES) trips error[H_UnsupportedLoweringForKind] (the `D-CSUBSET-BITFIELD-RVALUE-RUNTIME` aggregate-lowering guard) — the HIR→MIR lowering can't emit a global aggregate fn-ptr init as a .data relocation table; the corpus uses a LOCAL (stack-slot aggregate init, which DOES lower) to isolate c47's size-inference from that SEPARATE lowering gap. PRESERVED (audit): a no-init `int (*NAME[])(int);` + a fn-ptr PARAM `int (*f)(int x[])` keep the ordinary S000B/decay (the param path = declRowDeclaredType, allowInitInferredArray=false). 468→469 ctest, no regression. | (closed — the allowInitInferredArray thread IS the fix) | p19 Cluster G c47; the sqlite frontier after c46 (S0028→S000B); GENERALIZES c34 `D-CSUBSET-ARRAY-SIZE-INFERENCE` (simple→grouped fn-ptr array) + PRESERVES c10 `D-CSUBSET-STRUCT-MEMBER-DECLARATOR` (FAM-not-in-group); SURFACES the c48 global-aggregate-fn-ptr-init lowering gap (`D-CSUBSET-BITFIELD-RVALUE-RUNTIME` guard); `src/analysis/semantic/semantic_analyzer.cpp` (directDeclaredType/declaratorDeclaredType group arm + Pass-1.5 site); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-BOOL-CHAR-WIDENING` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c48) — a comparison/logical result (typed Bool) assigns to a plain `char` lhs.** ROOT (the c47 re-probe linux FIRST error, after S000B@186647 cleared): sqlite `p->nFloor = (p->D==31);` (sqlite3.c:25926; `nFloor` is `char`) fired error[S0003] S_TypeMismatch. A comparison/logical result is typed `TypeKind::Bool` (cst_to_hir.cpp:2402 + semantic_analyzer.cpp:5429); the `isAssignable` `boolWidensToArith` arm (type_rules.hpp:182) admitted Bool into the signed/unsigned/float RANK kinds but NOT plain `char` (interned `TypeKind::Char`, OUTSIDE the ranks) → `char = (a==b)` REJECTED, while `char = (a-b)` (int, in rank) was ACCEPTED via the separate `charConvertsToArith` arm — the exact differentiator, verified by execution (==/&&/< fail; - passes; signed/unsigned char + short + int lhs already pass). FIX (NOT §B; routine — ONE disjunct): add `|| (charConvertsToArith && lk == TypeKind::Char)` to the boolWidensToArith arm. Gated on BOTH flags (a non-C schema with char strictly-distinct keeps Bool→char rejected). NO codegen change — coerce() (cst_to_hir.cpp:515) already materializes a Bool→Char Cast (mapCast → i1→i8 ZExt), so the semantic gate was the ONLY blocker; the fix makes the semantic tier AGREE with coerce. WITNESS: corpus `char_cmp_assign` (`c=(a==b)` + `c+=(a<b)` + `d=(a&&b)` → char, all assign-stmt; `e=a-b` int→char differentiator; a,b runtime via io() → exit 42; RED-ON-DISABLE: revert the Char term → `c=(a==b)` → S0003). 469→470 ctest, no regression, all 3 legs. ★ SCOPE: clears the Bool→char family ONLY (assign + char-param call-arg + char-fn return); the sibling re-probe S0003s (`buf`,`"-Inf`,`"unistr('`,`x` — string-literal/identifier pointer mismatches) + the S0008 ternary `n<=0?0:sqlite3Malloc(n)` (int-vs-ptr) are SEPARATE frontiers. | (closed — the one-disjunct add IS the fix) | p19 Cluster G c48; the sqlite frontier after c47 (S000B→S0003); EXTENDS the Bool-widen arm beside C's `D-CSUBSET-CHAR-INT-WIDENING` (the charConvertsToArith int↔char arm) + F4 `D-CSUBSET-INT-SAME-SIGN-NARROW`; `src/analysis/semantic/type_rules.hpp` (isAssignable boolWidensToArith arm); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-TERNARY-NULL-POINTER` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c49) — a conditional `cond ? 0 : ptr` (or `cond ? ptr : 0`) has the POINTER type (C 6.5.15p6).** ROOT (the c48 re-probe linux FIRST error, after S0003 Bool→char cleared): sqlite `return n<=0 ? 0 : sqlite3Malloc(n);` (sqlite3.c:31769) fired error[S0008] S_ReturnTypeMismatch. `combineTernary` computes the result via `commonArithType(thenT,elseT)` which returns Invalid for `0(int) : Ptr` (a pointer has no int rank), then FALLS THROUGH to the FIRST arm (I32) → the leading `0` SHADOWS the pointer arm → `return <ternary>` (typed I32) mismatches the fn's pointer return. The asymmetry localized it: `n>0 ? gp : 0` does NOT fail (then-arm=Ptr → fallthrough returns Ptr). Same root cause at BOTH tiers (semantic subtreeType:5508 + HIR cst_to_hir:2679) and across contexts (return→S0008; assign/call-arg→S0003). FIX (NOT §B; REUSES the existing null-ptr-const model — `isLiteralIntegerZero` + flag `nullPointerConstantFromIntegerZero` + the HIR coerce null-ptr arm cst_to_hir.cpp:465): a coordinated TWO-TIER rule — BEFORE the first-arm fallthrough, if one arm is `TypeKind::Ptr` and the OTHER arm is a literal-0, the conditional type = the POINTER arm's type. Tier-1 semantic (combineTernary lambda + threading the 2 arm CST node-ids from the call site :5670); Tier-2 HIR (combineTernary sets `common`=the ptr type when the other arm is an integer `HirKind::Literal` → the EXISTING coerce materializes `Cast(0→Ptr)`; gated structurally, trusting semantic admission). Both arm orders. PRESERVED (verified by execution): `n?5:gp` (non-zero int) + `n?n:gp` (non-literal) STILL S0008; both-int + both-ptr ternaries unchanged. WITNESS: corpus `ternary_null_pointer` (`void *f(int n){return n<=0?0:gp;}` + reverse `n>0?gp:0`, n runtime via pick(), f(5)==&obj + f(-1)==0 → 42; RED-ON-DISABLE: revert the arms → S0008). 470→471 ctest, 3-LEG gate. ★ SCOPE: ternary/null-ptr ONLY; the sibling re-probe S0003s (`buf`/`"-Inf`/`"unistr('`/`x` — string-literal/identifier pointer mismatches; root-cause verified string-literal ternaries do NOT S0003; a `const char*`-arg case → H0009) are SEPARATE next frontiers. | (closed — the two-tier combineTernary null-ptr arm IS the fix) | p19 Cluster G c49; the sqlite frontier after c48 (S0003→S0008); REUSES `D-LANG-NULL-POINTER-CONSTANT` (isLiteralIntegerZero / admitsNullPointerConstant / the HIR coerce arm) extended to the C 6.5.15p6 ternary type-balance; `src/analysis/semantic/semantic_analyzer.cpp` (combineTernary :5508) + `src/hir/lowering/cst_to_hir.cpp` (combineTernary :2679); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-ARRAY-DECAY-TO-VOID-PTR` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c50) — an ARRAY / string-literal arg decays + converts to a `void*` parameter (C 6.3.2.1 + 6.3.2.3).** ROOT (the c49 re-probe linux FIRST error, after S0008 ternary cleared): sqlite `memcpy(buf, "-Inf", 5)` (sqlite3VXPrintf; compile.log @32931 macro-shifted, real ~33080; `buf` is `char[N]`) fired error[S0003] on BOTH `buf` (array) and `"-Inf"` (string-literal). The isAssignable array-decay arm (type_rules.hpp:263) only admitted `Array<T>→Ptr<T>` (same element); `Array<char>→Ptr<void>` (void≠char) FELL THROUGH, and the Ptr→void arm (:314) never fired (rhs kind is Array not Ptr). So `char*`/`u8*` POINTER args (≈every sqlite memcpy → via the Ptr→void arm) passed, but BARE ARRAY names + string literals failed — only ~6 of thousands of memcpys (the differentiator, verified by execution: `char*p;memcpy(p,p,1)` CLEAN vs `char a[8];memcpy(a,a,1)` S0003). FIX (NOT §B; ONE disjunct per tier, REUSES the existing `implicitToVoidPtr` flag): semantic isAssignable (`|| ptrRules.implicitToVoidPtr && kind(lhsElem[0])==Void`) + HIR coerce (cst_to_hir.cpp:334 emits the synthetic Array→Ptr Cast for the void* target). NO MIR change (the Array→Ptr Cast re-types by the TARGET, element-agnostic — hir_to_mir.cpp:943) + NO verifier change (post-coerce sees the Cast type Ptr<void> → isAssignable(void*,void*)=sameType). WITNESS: corpus `memcpy_array_arg` (`char a[8]; memset(a,0,8); memcpy(a,"xyz",4)` → byte-check → 42; RED-ON-DISABLE: revert either arm → S0003; a char* pointer arg stays fine = the differentiator). 471→472 ctest, 3-LEG gate. ★ SCOPE: the array/string-literal→void* family (buf, "-Inf", "unistr('", probably x); `chacha20_init`→void* is a SEPARATE next-frontier (FnSig→Ptr<void>, the c37 sibling — D-CSUBSET-FUNCTION-DESIGNATOR-TO-VOID-PTR); `sqlite3Prng` to re-classify (the &struct→void* hypothesis was REFUTED by execution — that already works). | (closed — the two-disjunct array→void* decay IS the fix) | p19 Cluster G c50; the sqlite frontier after c49 (S0008→S0003); EXTENDS the array-decay arm (D-LANG-STRUCTURAL-DECAY-OPT-OUT) + the void-conversion flag (D-LANG-POINTER-VOID-CONVERT) to compose array→void*; `src/analysis/semantic/type_rules.hpp` + `src/hir/lowering/cst_to_hir.cpp` (the two decay arms); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-FFI-PTHREAD-CREATE-JOIN` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c51) — ship pthread_create + pthread_join (<pthread.h>).** ROOT (the c50 re-probe linux S0001 batch, after the array→void* S0003 cleared): sqlite's threaded sorter `pthread_create(&p->tid, 0, xTask, pIn)` (sqlite3.c:35646) + the matching pthread_join were undeclared — the rest of the pthread surface (mutex API + pthread_self/equal + the opaque pthread_mutex_t/_t typedefs) was shipped c14/c15; only these two missing. FIX (config-only; NOT §B — routine 2-symbol extension): pthread.json `symbols` += `pthread_create: fn(ptr<void>,ptr<void>,ptr<void>,ptr<void>) -> i32` (all four args pointer-position) + `pthread_join: fn(u64, ptr<void>) -> i32` (the pthread_t thread-id BY VALUE = u64, the established pthread_self/equal ABI — NOT layout-bearing). No new types (pthread_t=u64 already shipped). Gated [elf,macho] (pe→os_win). WITNESS: corpus `shipped_pthread_thread` (a fn-POINTER-var start routine [sqlite's xTask shape → the Ptr→void* arm, NOT the unshipped fn-designator→void* gap]; create→join a real libc thread that sets g=42 → 42; red-on-disable → S0001; the thread RUNS on WSL-elf + qemu-arm64 per the 3-leg gate). | (closed — the 2 pthread.json symbols) | p19 Cluster G c51; the sqlite frontier after c50 (S0003→S0001 shipped-symbol batch); extends the c14/c15 pthread.json; `src/dss-config/shippedLibs/pthread.json`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-FFI-STRING-STRSPN` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c51) — ship strspn + strcspn (<string.h>).** ROOT (the c50 re-probe S0001 batch): sqlite `strspn(z, "...")` (sqlite3.c:37894 + json/fts) undeclared; strcspn (134104) is the guaranteed sibling shipped alongside. FIX (config-only; NOT §B): string.json `symbols` += `strspn`/`strcspn: fn(ptr<char>, ptr<char>) -> u64` (size_t — sqlite casts the int result). All formats (string.json maps pe msvcrt + elf/macho libc). WITNESS: corpus `shipped_strspn` (strspn("12 34",digits)==2 + strcspn("ab 12",digits)==3 → 42; red→S0001; all 4 targets). | (closed — the 2 string.json symbols) | p19 Cluster G c51; the c50 S0001 batch; extends the c14 string.json; `src/dss-config/shippedLibs/string.json`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-FFI-SYSTIME-UTIMES` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c51) — ship utimes (<sys/time.h>).** ROOT (the c50 re-probe S0001 batch): sqlite os_unix `utimes(zLockFile, NULL)` (sqlite3.c:42959, the active #else of #ifdef HAVE_UTIME) undeclared — sys/time.json had only `structs` (struct timeval, the struct-mechanism seed). FIX (config-only; NOT §B): add sys/time.json's FIRST `symbols` array + a `library` map (elf libc.so.6 / macho libSystem) + `utimes: fn(ptr<char>, ptr<void>) -> i32` (the 2nd arg = const struct timeval[2]*, here NULL = "now"). Gated [elf,macho] (pe→os_win). WITNESS: corpus `shipped_utimes` (utimes(nonexistent,0)==-1 [ENOENT, no fs touch] → 42; red→S0001). | (closed — the sys/time.json symbols entry) | p19 Cluster G c51; the c50 S0001 batch; extends the c-seed sys/time.json (structs→+symbols+library); `src/dss-config/shippedLibs/sys/time.json`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-FFI-MATH-INFINITY` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c52) — INFINITY via a NEW shipped FLOAT-CONSTANT surface (the §B the user resolved → Option A).** ROOT (the c50/c51 re-probe S0001 batch): sqlite `return (double)INFINITY` (sqlite3.c:37393/37423, a RUNTIME expr) → S0001 undeclared — the shipped-descriptor `constants` surface is INTEGER-ONLY (isIntegerScalarKind gate @shipped_lib_descriptor.cpp:543, fails loud F_ShippedLibUnsupportedType), and no finite literal text yields +inf (overflow→ERANGE fails loud). §B RESOLVED by the USER → Option A (a float-valued shipped constant, NOT a macro/builtin). FIX (REAL CODE, NOT a workaround): a NEW `floatConstants` descriptor surface PARALLEL to integer `constants` (the integer gate KEPT = the negative pin) — `ShippedFloatConstant{name,value,type f16..f128}` + `isFloatScalarKind` + `decodeFloatConstantValue` (`"inf"`/`"+inf"`/`"-inf"`→IEEE ±inf bits; else the existing `decodeFloat` for a finite value); injection MIRRORS the integer-constant inject (the double rides the `enumValue` int64 carrier as its bit_cast'd IEEE-754 pattern); the SHARED fold chokepoint `constant_symbol_fold.hpp` gains a float-core arm (bit_cast back to double). math.json += `floatConstants INFINITY="inf" f64` (target-invariant, no variant). ★ the cst inf-guard was ALREADY constant-expr-only (it lives in the Float→Int cast arm const_eval.cpp:282 per C99 6.3.1.4; `(double)INFINITY` is a Float→Float identity, +inf preserved) — NO fix needed. WITNESS: corpus `shipped_infinity` (`(double)INFINITY > 1e308` → 42, baseline + release [+inf survives ConstFold, no trap/NaN] on the pe RUN + x86_64 DISASM of the .data f64 const = 0x7FF0000000000000) + 4 NEW unit tests (float inf/-inf/finite/f32 decode) + 3 NEGATIVE pins (float-in-`constants` + integer-in-`floatConstants` + `1e400`-overflow ALL fail loud — the new surface did NOT widen the integer gate). 475→476 ctest. ★★ HONESTY (audit-caught): the corpus's elf/arm64 RUN is COMPILE-ONLY (`runOn:[]`) — `#include <math.h>` emits the math externs (sqrt etc.) needing `libm.so.6`, but only `libc.so.6` is DT_NEEDED (math.json `library`=libc) → the elf/arm64 binary fails to LOAD (exit 127, undefined `sqrt`); PRE-EXISTING (a bare `sqrt(4.0)` reproduces it), NOT c52's fold — INFINITY's value is pe-RUN + disasm verified, and it COMPILES clean on all 4 targets. The math.h/libm DT_NEEDED gap (DCE unused shipped externs + the libm library attribution) is a SEPARATE run-green-critical cycle (it also blocks sqlite's own math.h RUN). So c52 = Win pe RUN + all-4-target COMPILE + disasm; the elf/arm64 RUN is restored by the libm cycle. ★ GENERAL payoff (the Option-A rationale): NAN/HUGE_VAL/DBL_MAX/FLT_EPSILON are now expressible via floatConstants. | (closed — the floatConstants surface + the shared-fold float arm IS the fix) | p19 Cluster G c52; the sqlite frontier after c51 (the S0001 shipped-symbol/constant batch); §B Option A USER-chosen; EXTENDS the c-subset shipped `constants` mechanism (c39/c45) with a FLOAT surface, reusing the single `decodeFloat` (number_decode.hpp) + the shared `constant_symbol_fold` chokepoint; `src/ffi/shipped_lib_descriptor.{hpp,cpp}` + `src/analysis/semantic/{semantic_analyzer.cpp,constant_symbol_fold.hpp,semantic_model.hpp}` + `src/dss-config/shippedLibs/math.json`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-FFI-MATH-LIBM-DT-NEEDED` | ⏳ **DEFERRED (run-green-critical; surfaced by c52's audit, 2026-06-29).** A program that `#include <math.h>` emits ALL math.json externs (sqrt/pow/floor/…) as BIND_NOW undefined dynsyms, but the binary's ONLY DT_NEEDED is `libc.so.6` (math.json `library`=libc), while glibc's math symbols live in `libm.so.6` → the elf/arm64 binary fails to LOAD: `undefined symbol: sqrt`, exit 127 (WSL glibc 2.39 + qemu). PRE-EXISTING (a bare `sqrt(4.0)` reproduces it, no INFINITY); c52's `shipped_infinity` is the first linux RUN to depend on math.h → its elf/arm64 targets are COMPILE-ONLY (`runOn:[]`) pending this. ★ RUN-GREEN-CRITICAL: sqlite ITSELF uses math.h (sqrt/pow/etc.) → the sqlite linux binary will hit the SAME load failure → blocks the sqlite RUN-green goal — but only AFTER sqlite fully COMPILES (the compile frontiers like chacha20_init come first), so this is a LATER cycle, not the immediate next. FIX: (a) emit `libm.so.6` as a DT_NEEDED for glibc math symbols (+ keep libc for the CRT/non-math), AND/OR (b) DCE unused shipped externs from the dynsym table (only emit REFERENCED symbols — a math.h-include using no math fn shouldn't pull in sqrt/libm). Verify on all 3 RUN legs; then restore the `shipped_infinity` elf/arm64 runOn. **Trigger:** when sqlite fully compiles + the linux RUN is attempted (or sooner if another corpus needs math.h at runtime). | (open) | p19 c52 audit; `src/dss-config/shippedLibs/math.json` (`library`) + the shipped-extern dynsym/DT_NEEDED emission (the linker); run-green-critical for sqlite RUN-green; plan 23 full-C arc / SQLite-green |
| `D-FFI-UNISTD-LSEEK-SYSCONF-GETPID` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c53) — ship lseek + sysconf + getpid + the _SC_PAGESIZE constant (<unistd.h>).** ROOT (the c52 re-probe linux S0001 batch, after INFINITY cleared): sqlite os_unix `lseek` (sqlite3.c:43723/:43852), `sysconf(_SC_PAGESIZE)` (:44743), `getpid` (:40473) undeclared. FIX (config-only; NOT §B — the c51 shipped-symbol mechanism): unistd.json `symbols` += `lseek: fn(i32,i64,i32) -> i64` (off_t result = i64; a 32-bit return would sign-truncate >2GB file offsets; ship PLAIN `lseek` not lseek64 — the default model leaves __USE_FILE_OFFSET64 off, so libc exports the plain name with off_t=8) + `sysconf: fn(i32) -> i64` (long) + `getpid: fn() -> i32` (pid_t); `constants` += `_SC_PAGESIZE` PER-FORMAT (elf=30 / macho=29, the FILENAME_MAX `variants` mechanism; both linux arches share 30 — glibc bits/confname.h is arch-generic). Gated [elf,macho] (pe→os_win). WITNESS: corpus `shipped_posix_batch` (getpid()>0; `sysconf(_SC_PAGESIZE)==getpagesize()>0` [a STRONG per-target constant check — a wrong per-format value would make sysconf return a different quantity]; `lseek(fd,5000000000,SEEK_SET)==5000000000` [>2^32, proves the i64 return width]; → contributes to 42; red-on-disable → S0001; RUN on WSL-elf + qemu-arm64 per the 3-leg gate). | (closed — the unistd.json lseek/sysconf/getpid symbols + the _SC_PAGESIZE constant) | p19 Cluster G c53; the c52 re-probe S0001 batch; extends the c14/c15 unistd.json; `src/dss-config/shippedLibs/unistd.json`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-FFI-TIME-NANOSLEEP` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c53) — ship nanosleep (<time.h>).** ROOT (the c52 re-probe S0001 batch): sqlite os_unix `nanosleep(&sp, NULL)` (sqlite3.c:47388) undeclared. FIX (config-only; NOT §B): time.json `symbols` += `nanosleep: fn(ptr<void>, ptr<void>) -> i32` (both args = `struct timespec*` → ptr<void> per the time.json house style; the timespec struct was shipped c46). Gated [elf,macho] (pe→os_win). WITNESS: corpus `shipped_posix_batch` (nanosleep(1 microsecond)==0 → contributes to 42; red-on-disable → S0001; RUN on WSL-elf + qemu-arm64). | (closed — the time.json nanosleep symbol) | p19 Cluster G c53; the c52 re-probe S0001 batch; extends the c16 time.json; `src/dss-config/shippedLibs/time.json`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-FFI-SYSTIME-GETTIMEOFDAY` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c53) — ship gettimeofday (<sys/time.h>).** ROOT (the c52 re-probe S0001 batch): sqlite os_unix `gettimeofday(&sNow, 0)` (sqlite3.c:47437) undeclared — sys/time.json's $comment had explicitly FLAGGED gettimeofday as "added in that header's own cycle"; c53 is that cycle. FIX (config-only; NOT §B): sys/time.json `symbols` += `gettimeofday: fn(ptr<void>, ptr<void>) -> i32` (`struct timeval*` + `void* tz`; sqlite passes &tv + a 0/NULL tz, so struct timezone needs no shipped body; the timeval struct was the c-seed of the struct mechanism). Gated [elf,macho] (pe→os_win). WITNESS: corpus `shipped_posix_batch` (gettimeofday(&tv,0)==0 && tv.tv_sec>1e9 → contributes to 42; red-on-disable → S0001; RUN on WSL-elf + qemu-arm64). | (closed — the sys/time.json gettimeofday symbol, alongside c51's utimes) | p19 Cluster G c53; the c52 re-probe S0001 batch; extends the c51 sys/time.json; `src/dss-config/shippedLibs/sys/time.json`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-FUNCTION-POINTER-DEREF` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c54) — calling through a DEREFERENCED pointer-to-a-function-pointer.** ROOT (the c53 re-probe's first error, S0004 S_NotCallable): sqlite os_unix `pLockingStyle = (**(finder_type*)pVfs->pAppData)(zFilename, pNew)` (sqlite3.c:46575; `finder_type` = a fn-ptr typedef @46519, so `finder_type*` = Ptr<Ptr<FnSig>>). checkCall's structural deref-peel (semantic_analyzer.cpp ~5039) descended PAST each `*` and typed the INNER node — correct for the function-designator collapse (`*fp == fp` when fp is FnSig/Ptr<FnSig>, C 6.5.3.2p4, why `(**f)` on a real fn-ptr value already worked) but it DROPPED a real pointer level for a data pointer, so the callee landed as Ptr<Ptr<FnSig>> and was rejected. FIX (NOT §B; semantic-only, low-risk): after the peel, compute `subtreeType(calleeNode)` (the WHOLE callee, applying the C-correct `derefResultType` per `*`); when it yields FnSig/Ptr<FnSig> it GOVERNS the triage (semantic_analyzer.cpp ~5080); a non-callable full-callee type leaves the landed type untouched (so `int* p; (*p)(3)` stays S0004) and the landed-identifier path is preserved for variadicBuiltin + undeclared-dedup. The fn-ptr deref + indirect-call LOWERING was ALREADY correct (one data Load of the fn-ptr, no Load through the code pointer — derefResultType identity at cst_to_hir.cpp:2529), so the semantic fix unblocks it end-to-end. WITNESS: corpus `fnptr_deref_call` (a=`(**(finder_t*)app)(2)` the exact sqlite cast+double-deref + b=`(*pf)(2)` single-deref-through-ptr, both add40(2)=42; red-on-disable → S0004; all 4 targets, pe RAN to 42 on Windows + elf/arm64 on WSL/qemu). | (closed — the checkCall whole-callee-type upgrade) | p19 Cluster G c54; the c53 re-probe's S0004 frontier; reuses derefResultType (type_rules.hpp:497) + isFnPointerType; `src/analysis/semantic/semantic_analyzer.cpp`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-CALL-IN-PAREN-CAST-AMBIGUITY` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c55) — a parenthesized call followed by a binary operator mis-parsed as a C-style cast.** ROOT (the c54 re-probe's first error, S0006 S_UnknownType): sqlite `journalFileSize = ROUND8(sqlite3JournalSize(pVfs))` (sqlite3.c:63552; ROUND8(x)=(((x)+7)&~7)) → `((sqlite3JournalSize(pVfs))+7)&~7` — the parenthesized call `(f(p))` (identifier arg) immediately followed by `+` was mis-read as a CAST to a type named `f(p)` (base `f` + abstract fn-declarator `(p)`, p an unnamed param type); the cast's type child failed resolveTypeNode → S_UnknownType (semantic_analyzer.cpp:1280 via the Pass-2 cast arm :4076). NOT the static-forward-prototype (a RED HERRING — that compiles clean; the agent's H1-H4 all rejected). FIX (NOT §B; the c26 function-call sibling of the `(c*c)`/`(b[0])` abstract-declarator exclusions): the parser cast-vs-value triage `typeNameCommitApproved_` (parser.cpp Rule 1 ~864) committed on ANY >1-leaf type-child; an IDENTIFIER base with an abstract-declarator tail (`f(p)`, `name*`) is still type-vs-value AMBIGUOUS — so Rule 1 now keys on the BASE (leftmost) leaf: an identifier base falls through to rules 2-4 (sketch lookup on the base name + the follower-operator test, which sees the trailing `+`/`&`/`*` and rolls back to the call reading); a keyword/struct/star/const base keeps the unconditional commit. A grammar-only fnSuffix removal is INSUFFICIENT (falls to parenDeclarator → S0027); the triage is the locus. OVER-ADMISSION GUARD: a genuinely-undeclared callee still errors (rollback re-reads the call → checkCall reports S_UndeclaredIdentifier). WITNESS: corpus `paren_call_binop` (r=(((idn(35))+7)&~7)=40 the EXACT ROUND8 shape + s=((idn(20))+22)=42; red-on-disable → S0006; all 4 targets, pe RAN to 42 on Windows + the c26 cast tests stay green [11/11]). | (closed — the Rule 1 base-leaf discriminator) | p19 Cluster G c55; the c54 re-probe's S0006 frontier; the c26 cast-ambiguity sibling; `src/analysis/syntactic/parser.cpp` (`typeNameCommitApproved_`); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-TERNARY-NULL-FUNCTION-POINTER` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c56) — a ternary with a function-DESIGNATOR arm + a literal-0 arm, assigned to a fn-ptr lvalue.** ROOT (the c55 re-probe's first error, S0003 S_TypeMismatch on the NC_NoSelect flag `0x080000` — a MACRO-SHIFTED attribution to line 20547; the real site is the assignment): sqlite name-resolution `w.xSelectCallback = (pNC->ncFlags & NC_NoSelect) ? 0 : resolveSelectStep` (sqlite3.c:111984; xSelectCallback is a fn-ptr field, resolveSelectStep a function NAME = a designator of type FnSig). `combineTernary`'s c49 null-pointer-constant rule matched only `TypeKind::Ptr`, NOT `TypeKind::FnSig` (the un-decayed designator), so `0 : resolveSelectStep` fell through to the first-arm I32 → the assign-stmt `isAssignable(Ptr<FnSig>, I32)` failed (the OR-assign site @111359 is CLEAN — site A is not the bug; the agent's bisection proved it). FIX (NOT §B; the FUNCTION-POINTER SIBLING of c49 [[D-CSUBSET-TERNARY-NULL-POINTER]]): both `combineTernary` tiers (semantic_analyzer.cpp ~5556 + cst_to_hir.cpp ~2724) gain FnSig arms — a `FnSig` arm opposite a literal-0 returns the DECAYED `interner.pointer(FnSig)` = Ptr<FnSig> (C 6.3.2.1p4 function-to-pointer decay + 6.5.15p6), so the designator coerces via a FnSig→Ptr Bitcast and the literal-0 via a null-ptr Cast; handles BOTH arm orders (returning pointer-not-bare-FnSig also CURES a PRE-EXISTING latent silent-miscompile in the `designator : 0` order, where the 0 arm never got its null-ptr Cast). Reuses isLiteralIntegerZero + the nullPointerConstantFromIntegerZero flag gate + the existing coerce arms; no new lattice/flag/codegen. OVER-REJECTION GUARD: the arms fire only when one arm is structurally FnSig AND the other a literal-0 (a genuine int?int:int stays int). WITNESS: corpus `ternary_null_fnptr` (order1 `w.cb=flag?0:add40`→add40(2)=42 + order2 `w.cb=t?sub1:0`→sub1(43)=42, both CALLED to prove the right non-null target; red-on-disable → S0003; all 4 targets, pe RAN to 42 on Windows + the c49 ternary_null_pointer stays green). | (closed — the combineTernary FnSig arms, both tiers) | p19 Cluster G c56; the c55 re-probe's S0003 frontier; the c49 fn-pointer sibling; `src/analysis/semantic/semantic_analyzer.cpp` + `src/hir/lowering/cst_to_hir.cpp`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-INT-FLOAT-CONVERSION` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c57; §B Full-matrix, USER) — implicit int↔float conversion (BOTH directions).** ROOT (the c56 re-probe's first error, S0003 on `iBig`/`iSm` @sqlite3.c:135194-195 — macro-shifted 134835-836): `kahanBabuskaNeumaierStep(pSum, iBig)` passes an `i64` to a `volatile double` param. The agent's bisection proved DSS had NO implicit int↔float conversion AT ALL — `double d=5;` itself fails S0003 in every context (init/assign/arg/return); sqlite reached line 135194 only because it casts explicitly almost everywhere, and this is a rare BARE i64→double. §B RESOLVED by the USER → the FULL int↔float matrix (int→float AND float→int). FIX: (semantic) two `isAssignable` arms (type_rules.hpp) gated by new config bools `intConvertsToFloat`/`floatConvertsToInt` (semantic_config.hpp + grammar_schema_json.cpp + c-subset.lang.json), threaded through all 5 conversion call sites (the hir_verifier strict post-coerce site stays un-gated = correct); the arms key on int-rank↔float-rank so pointers/structs (rank 0) stay rejected. (codegen) the `si_to_fp` opcode had NO asm encoding on EITHER target — added x86_64 `cvtsi2sd` (F2 [REX.W] 0F 2A) + arm64 `SCVTF` (newly declared) + a mir_to_lir SIToFP/UIToFP source-int-width thread (FAILS LOUD if the result float ≠ F64). WITNESS: corpus `int_float_convert` (int→f64 at a `volatile double` arg [the exact sqlite shape] + init/assign + float→int truncation → 42; red-on-disable → S0003; pe RAN to 42; ★★ the arm64 SCVTF/UCVTF encoding was DECIMAL-SLIPPED by the impl agent (the 4 fixedWords decoded to FCVTNS/FCVTNU + malformed words — a host-green arm64 MISCOMPILE the Win leg cannot catch) → CAUGHT by the INDEPENDENT AUDIT (decode + objdump) and FIXED to the correct 0x9E620000/0x1E620000/0x9E630000/0x1E630000, then proven on the qemu-arm64 3-leg RUN leg — [[feedback_gate_all_three_run_legs_2026_06_29]] realized. 4 existing tests + 1 corpus that used int↔float as a guaranteed-REJECTED pair were re-pointed to still-rejected char*↔int* pairs (audit-verified intent preserved). | (closed — the 2 isAssignable arms + the si_to_fp encodings) | p19 Cluster G c57; §B user-chosen full matrix; the c56 re-probe S0003 frontier; `src/analysis/semantic/type_rules.hpp` + `semantic_analyzer.cpp` + the 2 target.json si_to_fp encodings + mir_to_lir; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-INT-TO-F32-CODEGEN` | ⏳ **DEFERRED (c57, 2026-06-29) — int→FLOAT (f32) codegen, the single-width-axis casualty.** cvtsi2sd/SCVTF need TWO width selectors (the SOURCE int width → REX.W/Xn-vs-Wn AND the DEST float width → F2/F3 prefix / Sd-vs-Dd), but the variant guard has ONE width axis. c57 gave the axis to the source-int width and FIXED the destination at F64; consequently `float f = anInt;` (int→f32) FAILS LOUD (not silent). SQLite uses `double` only, so the run-green goal is unaffected. FIX (when a c-subset program needs int→f32): a second width axis on the si_to_fp variant guard (or a dest-width-keyed variant set) emitting cvtsi2ss (F3 …) / SCVTF Sd. NOT run-green-critical for sqlite. | (open) | p19 c57; the int↔float matrix's f32 corner; `src/lir/lowering/mir_to_lir.cpp` + the target.json si_to_fp variants; plan 23 / SQLite-green |
| `D-CSUBSET-UI-TO-FP-UNSIGNED-I64` | ⏳ **DEFERRED (c57, 2026-06-29) — unsigned-i64→double on x86 (the cvtsi2sd-reuse corner).** x86 SSE2 has no direct u64→double; c57's `ui_to_fp` reuses the SIGNED `cvtsi2sd` path — correct for u32 (fits signed-64 exactly) and in-range u64, but a u64 with the top bit set mis-converts as negative. x86-ONLY (arm64 UCVTF is natively correct). SQLite does not hit it. FIX (when needed): the standard SSE2 u64→double sequence (the sign-bit split-and-add idiom) for ui_to_fp on x86_64. NOT run-green-critical for sqlite. | (open) | p19 c57; the int↔float matrix's unsigned-i64 corner; `src/dss-config/targets/x86_64.target.json` ui_to_fp; plan 23 / SQLite-green |
| `D-CSUBSET-INITIALIZER-CONST-TOKEN-LEAK` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c58) — a `const` token in a SCALAR local's INITIALIZER wrongly marked the OBJECT const.** ROOT (the c57 re-probe's LAST linux error, S0007 S_ConstViolation): sqlite `nocaseCollatingFunc` (sqlite3.c:188075) `int r = sqlite3StrNICmp((const char*)pKey1, (const char*)pKey2, …); if(0==r){ r = nKey1-nKey2; }` — reassigning the plain `int r` fired S0007 (the clean sibling binCollFunc declares `int rc, n;` with NO initializer, so it's fine). The agent bisected the minimal delta: the `const` TOKEN appearing ANYWHERE in a scalar local's initializer subtree (even `int r = (int)sizeof(const char)`) marks the local const — purely the token, no type-flow. `declaratorObjectIsConst`'s SCALAR fallback (semantic_analyzer.cpp:675) scanned the WHOLE varDecl node (which spans the initializer) for the const marker; the pointer cases (c36) were correctly scoped to the outermost pointer layer — only the scalar fallback over-scanned. FIX (NOT §B; a localized scan-scope correction, C 6.7.3 — an object's const derives from its own declaration, never a cast inside its initializer): added a `headNode` param; the scalar fallback now scans the type-specifier HEAD (`kids[*decl.headChild]`, base type, no pointer stars) instead of declNode — mirrors the legacy positional path's `kids[*decl.typeChild]` scoping. Also closes a latent miscompile (the false isConst would make resolveConstSymbolInit try to const-fold r's function-call initializer at use sites). OVER-REJECTION GUARD (verified with the rebuilt compiler): a genuine `const int c=5; c=6;` STILL errors S0007 (its const lives in the HEAD); `char * const p` stays a const object (the c36 lastLayer path untouched); `const char *p` stays a mutable pointer. WITNESS: corpus `local_init_const_leak` (`int r=firstByte((const char*)buf)`=5 then `r=37+r`=42 → 42; red-on-disable → S0007; the const/volatile/mutable suite 16/16 stays green). | (closed — the headNode-scoped scalar fallback) | p19 Cluster G c58; the c57 re-probe's LAST compile error; the c29/c36 const-handling neighborhood; `src/analysis/semantic/semantic_analyzer.cpp` (declaratorObjectIsConst); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-ARRAY-DECAY-IN-ADDITIVE` | ✅ **CLOSED 2026-06-29 (p19 Cluster G c59) — an ARRAY operand of `+`/`-` decays to `Ptr<elem>` (C 6.3.2.1p3) BEFORE the additive arm types the result.** ROOT (the c58 re-probe's FIRST error — the FIRST HIR-LOWERING frontier after the semantic phase went fully green): sqlite `AtomicStore(pInfo->aReadMark + i, v)` == `*(aReadMark + i) = v` (sqlite3.c:68868/69822; `aReadMark` is `u32 aReadMark[N]`, an array member) fired error[H0009] (assignment-as-value: lvalue-classify on a typeless target), and the sibling `AtomicLoad(aReadMark + i)` == `*(aReadMark + i)` fired error[H0001] (HirKind Deref unresolved, ordinal 39). ★ H0009 was a SYMPTOM not the cause — the assignment-as-value machinery already worked: the agent's bisection proved `*(p+i)=v` (true pointer) PASSES and `a[i]=v` (subscript Index node) PASSES, so the trigger is array-decay-specific, NOT assignment-specific. `combineBinary`'s additive arm (cst_to_hir.cpp:2402) gated the c41 `ptrIntArith` recognizer on `TypeKind::Ptr`; an array operand stayed `TypeKind::Array` → ptrIntArith FALSE → the BinaryOp fell through to the array type → the downstream `*` Deref's `derefResultType` returned Invalid → a TYPELESS Deref → H0001 (rvalue) / H0009 (assignment LHS, lvalue-classify bails on the invalid type). FIX (NOT §B; REUSES the existing `coerce` array-decay — the SAME synthetic Array→Ptr Cast the cast path at cst_to_hir.cpp:4299 emits — + the c41 ptrIntArith→MIR stride-Gep): combineBinary now decays an array additive operand to `Ptr<elem>` BEFORE the c41 swap/recognizer; GUARDED to fire only when the OTHER operand is a scalar index (non-Array AND non-Ptr), and the RIGHT operand decays for Add only — so `array - array` / `p - arrayName` / `array + ptr` stay UNTOUCHED on their pre-existing deferred path ([[D-CSUBSET-POINTER-DIFF-EDGE-CASES]]). NO MIR change (the decayed array → the existing `p ± n` stride-Gep, hir_to_mir.cpp:1142, with the c41/c42 SExt/ZExt index-widen). INCIDENTALLY clears the 5 H0001 ordinal-39 entries (the AtomicLoad reads share the cause) = 7 of the 23 frontier errors. WITNESS: corpus `array_decay_additive` (RUNTIME-index `*(a+i)=v` store [the AtomicStore form] + `*(a+i)` read [the AtomicLoad form] + `*(3+a)` int+array, int elements stride 4: 3+6+9+12=30, +a[3]=12 → 42; red-on-disable → H0009 + H0001; STRIDE-SENSITIVE — a 1-byte stride would corrupt the value). ★★ AUDIT-VERIFIED on all 3 RUN legs (the c57 lesson — host-green ≠ arm64-correct): x86_64-elf `movslq`+`imul $4`+lea (NOT 1-byte) RAN to 42 native; arm64-elf `sxtw`+`mul #4`+add+stur/ldur RAN to 42 under qemu, instruction-for-instruction equivalent to x86_64 in the address computation; the decayed-array codegen is IDENTICAL to the true-pointer `*(p+i)` path on both targets; double-eval once-only confirmed; 10 additive-arm-adjacent corpora green on x86_64-elf AND arm64-qemu (no collateral break). | (closed — the combineBinary array-decay, guarded to the scalar-index case) | p19 Cluster G c59; the c58 re-probe's FIRST HIR-lowering error (the post-semantic frontier); the additive sibling of `D-CSUBSET-POINTER-DIFF-EDGE-CASES`; reuses `coerce` (D-LANG-STRUCTURAL-DECAY-OPT-OUT) + the c41 `D-CSUBSET-POINTER-INT-ARITHMETIC` stride-Gep; `src/hir/lowering/cst_to_hir.cpp` (combineBinary); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-POINTER-ARITH-FLOAT-OPERAND` | ⏳ **DEFERRED (pre-existing; surfaced by c59's audit, 2026-06-29).** A non-integer (float) pointer-arithmetic operand — `p + f` / `a + f` where `f` is a float — is NOT diagnosed as the C 6.5.6p2 constraint violation it is; instead the float is truncated to int (x86 `cvttss2si` / arm64 the c57 float→int path) and ×elem-scaled. PRE-EXISTING + shared by BOTH the true-pointer form and the array form (the c41 ptrIntArith recognizer + the c57 float→int conversion compose to admit it); c59 only makes the array form CONSISTENT with the already-permissive pointer form — it did NOT introduce this. NOT run-green-critical (sqlite never does float pointer-arith). FIX (when strictness is wanted): an integer-operand guard on `ptrIntArith` (require the non-pointer additive operand be integer-ranked) spanning BOTH the pointer and array forms, with a fail-loud diagnostic. | (open) | p19 c59 audit; `src/hir/lowering/cst_to_hir.cpp` (the ptrIntArith recognizer) + `src/analysis/semantic` (the constraint check); plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-SWITCH-NESTED-CASE` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c60; §B approach I = flatten-to-one-CFG, USER-chosen) — `case`/`default` nested inside an inner block of a switch (C 6.8.1 jump-into-block).** ROOT (the c59 re-probe's FIRST error, S0023 S_CaseLabelNotInSwitch ×7): sqlite's "shared-tail with local declarations" idiom — a `case`/`default` nested INSIDE the brace-block opened by a PRECEDING case (braces forced by local decls): VDBE `case OP_SeekRowid: { VdbeCursor*pC; …; goto notExistsWithKey; } /*fall through*/ case OP_NotExists: …; notExistsWithKey: …` (sqlite3.c:101365) + the JSON parser `jsonTranslateTextToBlob` (`case '+': { …; case '.': …; case '-': …; case '0'..'9': …; parse_number: … }`, 11 cases + 3 labels in ONE block). DSS's FLAT-SWITCH model (lowerSwitch + the pumpSwitch driver grouped only TOP-LEVEL switchBodyItems into disjoint CaseArms; MIR minted one block per arm) could not represent a jump target inside another arm's body → the nested CaseStmt reached the general statement dispatch (cst_to_hir.cpp:3297) and failed loud. §B RESOLVED by the USER → approach (I): flatten the switch body to ONE CFG, case/default = labels at any depth. FIX (Design I-A): the HIR `SwitchStmt` becomes `[disc, body Block, dispatch arms]` — `CaseArm` repurposed as a dispatch entry (value? + a synthetic LABEL ORDINAL, no body); a per-switch prescan (`switchPrescanDispatch`, NOT descending into a nested switch) assigns every case/default a synthetic ordinal from the SHARED per-function `nextLabelOrdinal_` counter (named-label + case-label ordinals never collide; MIR's getOrCreateLabelBlock keys both); the body lowers as ONE flat Block where each case/default → a synthetic `LabelStmt` marker (reusing makeLabelStmt + the MIR label-block + goto jump-into-block machinery); both MIR Switch forms target those label-blocks via `addSwitch` (the EXISTING cascading-compare dispatch — NOT a jump-table, which DSS never had: [[D-OPT-SWITCH-JUMP-TABLE]]) + lower the flat body. PRESERVED: label-before-case ([[D-CSUBSET-LABEL-BEFORE-CASE]]), adjacent cases, fall-through (now straight-line), break→join, default-anywhere; S0023 STILL fires for a case genuinely OUTSIDE any switch (switchBodyDepth_ gating). `pathTerminates(SwitchStmt)` is now strictly more accurate (models fall-through). WITNESS: corpus `switch_nested_case` (the VDBE shared-tail `case 1:{int t;…goto shared;} case 2:…; shared:…` + a case inside a deeper if-block `case 3:{ if(k>0){ case 4:… } }`; classify(2)+classify(3)+classify(1)-8 = 32+6+12-8 = 42; RED-ON-DISABLE → S0023 on the nested case 2/case 4). ★★ INDEPENDENT AUDIT (the c57 lesson, all 3 RUN legs): x86_64-elf native + arm64-qemu both RAN to 42, every value gcc-cross-checked; the nested-case jump-into-block (dispatch DIRECTLY to a case past a decl inside an if-block → correct value), fall-through into/out of nested blocks, 8 edge cases (default-only/no-default/single/default-not-last/enum+char values/empty-body/Duff's-device/inner-loop-break-vs-switch-break), ordinal-collision + nested-switch-isolation + discriminant-once — ALL correct on both legs; no dead blocks in the final binary. ★ c60 clears the 7 S0023 ONLY — H0002 (continue-in-switch) is PRE-EXISTING and NOT cleared (a separate later cycle; the audit proved c59 has the identical H0002). | (closed — the flat-CFG switch lowering: HIR shape + oracle + driver + 2 MIR forms) | p19 Cluster G c60; §B approach-I USER-chosen; the c59 re-probe's S0023 frontier; reuses the goto/label infra (makeLabelStmt + getOrCreateLabelBlock) + addSwitch; `src/hir/hir_node.hpp` + `hir.{cpp,hpp}` + `hir_text.cpp` + `hir_verifier.hpp` + `src/hir/lowering/cst_to_hir.cpp` + `src/mir/lowering/hir_to_mir.cpp`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-OPT-SWITCH-JUMP-TABLE` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c70; §B USER-chosen jump-table over a minimal immediate-compare fix) — a dense `switch` lowers to an O(1) JUMP TABLE (was: a linear cascading-compare chain that, for a >255-case switch, ALSO exhausted the regalloc).** ROOT (the post-c69 re-probe compiled sqlite3.c END-TO-END → I_VerifierFailure operand count 349; with the verifier fix [[D-MIR-VERIFIER-UNBOUNDED-OPERAND-SENTINEL]] the switch reached LIR and EXHAUSTED the regalloc, L_VirtualRegInPostRegalloc): sqlite's ~348-case VDBE opcode dispatch (sqlite3VdbeExec) — lowerSwitch's O(n) chain materialized EVERY case constant into a live register → ~348 vregs live from the switch header. FIX (opus impl + opus audit, LIR-tier, ZERO new opcodes): dense (caseCount>=8 && span<=3*caseCount && span<=2^20) → widen discrim (SExt/ZExt) + idx=discrim-minCase + ONE unsigned `cmp idx,span-1`+`jcc(Ugt) default` + off=idx*8 + lea table + indexed load + jmp_indirect; the table = an AssembledData in .data (Mach-O rejects reloc-bearing .rodata — the c67 placement), span*8, one abs64 reloc per slot to the target block's synthetic symbol (gaps->default), block symbols bound POST-assemble() via a new AssembledFunction::blockByteOffsets (emitted in compile_pipeline.cpp — NO dead per-target lea). Sparse/small → the compare chain but each small non-negative case is an IMMEDIATE `cmp discrim,imm` (window [0,4095]=arm64 imm12; wide/negative → register fallback) — this alone cures the pressure. WITNESS: 6 corpora (switch_vdbe_dispatch 350-dense=sqlite's shape [.data=2800=350*8], switch_jump_table_boundaries [first/last/GAP->default/below/above/negative-min], switch_wide_bound, switch_over_255_cases 300, switch_sparse_immediate, switch_unsigned_dense) → 42 all 4 targets gcc-cross-checked. ★★ INDEPENDENT AUDIT SOUND (the WRONG-CASE hunt, the control-flow analog of c69's wrong-slot): every discrim below-min->above-max for dense/gappy/negative/unsigned CHECKSUMMED vs gcc across 6 legs → ALL AGREE; objdump both arches = slot j->case(min+j), gaps->default, idx=discrim-min, NO off-by-one (VDBE slot0->case0 slot349->case349); Win 500/500 + WSL/qemu 315/315; red-on-disable reproduces L_VirtualRegInPostRegalloc. Deferred residual [[D-CSUBSET-NARROW-SWITCH-DISCRIMINANT-CMP]] (a char/short discriminant, pre-existing fail-loud). | (closed — the LIR jump-table dual-path + the post-assemble block-symbol binding) | p19 Cluster G c70; §B USER-chosen (jump table now); the c60-deferred perf item, now run-green-relevant (the VDBE hot loop); reuses computed-goto block-address + c67 aggregate-reloc placement; `src/lir/lowering/mir_to_lir.{cpp,hpp}` + `src/program/compile_pipeline.cpp` + `src/asm/asm.{hpp,cpp}`; with [[D-MIR-VERIFIER-UNBOUNDED-OPERAND-SENTINEL]]; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-NARROW-SWITCH-DISCRIMINANT-CMP` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c78) — the switch controlling expression integer-PROMOTES to int at HIR (switchPrologue, reusing c71's integerPromotedType+coerce), so the dispatch cmp runs at ≥32-bit width with a SExt/ZExt'd operand. Was the 313× DOMINANT of the post-c77 sqlite encoder frontier (a LOWERING fix — neither x86 NOR arm64 has a sub-32 cmp). Witness `examples/c-subset/switch_subint_discriminant`=154 (RED-ON-DISABLE → A_NoMatchingEncodingVariant cmp width 8). Surfaced + confirmed PRE-EXISTING by the c70 audit; fail-loud, NOT c70.** A `switch` on a `char`/`short` (sub-32-bit) discriminant fails to compile on x86 + arm64: the dispatch emits `cmp` at the narrow discriminant width → `A_NoMatchingEncodingVariant: opcode 'cmp': no encoding variant matches at width 8` (the D-CSUBSET-32BIT-ALU-FORMS family — no native narrow-width ALU form). PROVEN pre-existing: fails identically with c70's immediate-compare change reverted AND the jump table disabled (the old `cmp reg8,reg8` chain has no width-8 cmp either). Fail-loud (a compile error, never a silent miscompile). No corpus exercises a narrow-width switch discriminant. NOT confirmed in sqlite (sqlite switches on int opcodes/tokens). FIX (when needed): integer-PROMOTE the switch discriminant to int (C 6.8.4.2 — integer promotions apply to the controlling expression) before the dispatch cmp, so it runs at >=32-bit width. | (open) | discovered p19 c70 audit; the D-CSUBSET-32BIT-ALU-FORMS family; `src/lir/lowering/mir_to_lir.cpp` (lowerSwitch cmp width) OR the HIR switch-discriminant promotion; plan 23 full-C arc |
| `D-MIR-VERIFIER-UNBOUNDED-OPERAND-SENTINEL` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c70) — the MIR verifier honors the variadic-operand sentinel (`maxOperands == kMirUnboundedOperands == 0xFF`) instead of reading it as a literal max of 255.** ROOT (the c69 re-probe's FIRST error, after sqlite3.c finally compiled end-to-end): `I_VerifierFailure` "opcode switch operand count 349 outside [1, 255]" — the c60 switch-flatten produces ONE MIR `Switch` opcode whose operands are `[discriminant, caseValue*N]`; sqlite's ~348-case VDBE dispatch (`sqlite3VdbeExec`) = 349 operands. `MirOpcode::Switch` (and `IndirectBr`) declare `maxOperands == kMirUnboundedOperands` (0xFF) — genuinely variadic — and the BUILDER (`mir.cpp` addInst) has always exempted that sentinel from the upper-bound check (`maxOperands != kMirUnboundedOperands && count > maxOperands`), so it accepted the 349-operand Switch. But `MirVerifier::checkStructuralInvariants` did a plain `operands.size() > info.maxOperands`, reading the 0xFF sentinel as a literal 255 → it REJECTED the Switch the builder had already built. FIX (1 edit, `src/mir/mir_verifier.cpp`): mirror the builder's exemption — `bool const overMax = info.maxOperands != kMirUnboundedOperands && operands.size() > info.maxOperands;` — so a variadic-operand opcode is not upper-bounded. WITNESS: corpus `switch_over_255_cases` (a 300-case switch → 301 operands > 255; classify(k)=1000+k for k in [0,299] else 7 → 42; RED-ON-DISABLE → I_VerifierFailure "operand count 301 outside [1, 255]"). This UN-MASKED the register-pressure exhaustion the jump table + immediate-compare chain fix ([[D-OPT-SWITCH-JUMP-TABLE]]). | (closed — the verifier's variadic-sentinel exemption, matching the builder) | p19 Cluster G c70; the c69 re-probe's FIRST error (sqlite finally compiled end-to-end); `src/mir/mir_verifier.cpp`; paired with the jump-table lowering; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-CONTINUE-SKIPS-SWITCH` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c61) — `continue` inside a `switch` inside a loop targets the enclosing LOOP, not the switch (C 6.8.6.2).** ROOT (the c60 re-probe's FIRST error, H0002 H_InvalidBreak ×2): sqlite `sqlite3VXPrintf` (sqlite3.c:33119/33311) — a format-char loop whose body `switch`es on the conversion type, with `continue` inside the switch to advance the format string → "continue resolves to a switch; continue can only target a loop". `isBranchTargetKind` (hir_node.hpp:163) counts a SwitchStmt as a branch target, and a `continue`'s depth-0 resolution (makeContinue(0) — C has no labeled continue) landed on the innermost switch → the verifier (hir_verifier.cpp:193) rejected it. PRE-EXISTING (the c60 audit proved c59 has the identical H0002; c60's switch-flatten did NOT touch the continue model — it only un-masked the H0002 by clearing the S0023 wall). FIX (NOT §B; C 6.8.6.2 — a switch is a `break` target but TRANSPARENT to `continue`; 2 symmetric edits): (a) the verifier `checkBreakContinueScoping` drops SwitchStmt frames from a continue's `enclosingBranchTargets` list (`std::erase_if`) before the depth/range check — the loops-only index space, depth 0 = innermost loop; the old "resolves to a switch" rejection is gone (a continue with NO enclosing loop now fails the range check → "no enclosing loop"); (b) the MIR ContinueStmt lowering (hir_to_mir.cpp ~6211) walks the branchStack SKIPPING switch frames (a switch frame has continueBB invalid) to the depth-th loop frame. `break` is UNCHANGED (keeps the full loops+switches stack — a switch IS a break target). The verifier's loops-only view and the MIR's skip-invalid-continueBB walk drop the IDENTICAL frames (a SwitchStmt HIR node ⟺ a MIR frame with invalid continueBB), both depth 0 = innermost loop → consistent by construction. WITNESS: corpus `continue_in_switch` (a for-loop with `case 2: continue;` inside a switch; the continue must skip the post-switch `acc += 5` → 42, a wrong target [break out of switch] would yield 47; RED-ON-DISABLE → H0002). ★★ INDEPENDENT AUDIT all 3 RUN legs (x86_64-elf native + arm64-qemu + windows-pe, every value gcc-cross-checked): the corpus (42) + adversarial nesting [continue in while/do-while; nested switch-in-inner-loop-in-outer-loop → advances the INNER loop only; loop-in-switch-in-loop → targets the INNERMOST loop] + break-unchanged [break-in-switch=47, break-in-loop=15] + no-over-admission [continue-no-loop / bare-continue / cross-function still rejected] + arm64 objdump [the continue `b`-branches to the correct loop LATCH, not the post-switch code] — ALL correct; a faithful sqlite3VXPrintf miniature ran identically on gcc/DSS-x64/DSS-arm64. The audit ALSO caught a stale c60-committed unit test (`ContinueSkippingSwitchToOuterLoopIsValid` used makeContinue(1), the old depth-1-skips-switch model) → updated to makeContinue(0) (the real-C loops-only form — a better regression test). | (closed — the verifier erase-switches + the MIR skip-switch-frames continue resolution) | p19 Cluster G c61; the c60 re-probe's H0002 frontier (PRE-EXISTING, un-masked by the c60 switch-flatten clearing the S0023 wall); `src/hir/hir_verifier.cpp` + `src/mir/lowering/hir_to_mir.cpp` + `tests/hir/test_hir_verifier.cpp`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-STRING-LITERAL-ARRAY-ZERO-FILL` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c62; §B Option A pad-at-producer, USER-chosen) — a string literal initializing a `char[N]` array zero-fills the trailing N−len bytes (C 6.7.9p14).** ROOT (the c61 re-probe's FIRST error, H_VerifierFailure ×4): sqlite's date/time `aXformType[]` table (sqlite3.c:26283) has a `char zName[7]` field initialized by string literals SHORTER than 7 (`"hour"`=Array<char,5>, `"day"`=4, `"month"`=6, `"year"`=5) → ConstructAggregate child[1] type Array<char,M> ≠ field Array<char,7> (the 6-char names fit [7] exactly via sameType + pass). DSS did not implement the zero-fill (the short string's Array<char,M> never widened to Array<char,N>). The SAME gap rejected the scalar `char x[7]="hi"` with S0003. §B RESOLVED by the USER → Option A (pad at the producer). FIX (3 tiers, admit⟺realize parity): (semantic) isAssignable gains a `charArrayFromStringLiteralInit`-gated arm admitting `char[N]<-char[M]` (N≥M, char both, string-literal init only) + `initIsStringLiteral` threaded through the scalar VarDecl sites; (HIR) `coerce` re-types the string Literal to Array<char,N> by minting a fresh Literal SHARING the literal-pool index (no double-decode); (MIR/asm) the global is recorded at the target Array<char,N> type + the asm string-literal arm ZERO-PADS the global's bytes to the type size N (the Option-A producer step — avoids the latent OOB the design was chosen to prevent: a short M-byte global + an N-byte aggregate-copy would read adjacent rodata as padding), + `lowerAggregateCopy` gains an Array arm + `encodeAggregateValue` an Array string-char arm (const-folded aggregate). EXACT-FIT (N==M) stays via sameType; OVER-LONG (N<M, `char[3]="hello"`) stays a loud S0003; `char *p="hi"` (POINTER) UNCHANGED (3-byte rodata). WITNESS: corpus `string_array_zero_fill` (the aXformType shape + a scalar `char s[7]="hi"`; byte-checks the padding == 0 AND the string bytes → 42; RED-ON-DISABLE → H_VerifierFailure / S0003). ★★ INDEPENDENT AUDIT all 3 RUN legs (the c57 lesson — the impl could only RUN pe; the audit closed arm64): the rodata global is BYTE-IDENTICAL N-byte zero-padded on x86_64 AND arm64 (`68 69 00 00 00 00 00`, NOT a short global + OOB), the corpus + repro RAN to 42/4 on x86_64-elf + arm64-qemu (gcc-cross-checked all 4), a 0x7f-neighbor probe confirmed the padding is genuinely ZERO at runtime on arm64, admit⟺realize parity + over-long-S0003 + char*-unchanged + scalar+aggregate ALL correct; the pre-existing mutable-char[N]-in-rodata bug confirmed NOT sqlite-critical (all 17 sqlite file-scope char[]=string globals are const). KNOWN-LIMITATION (fails LOUD, not silent; sqlite uses the bare form): brace-wrapped `char x[7]={"hi"}` → H_VerifierFailure [[D-CSUBSET-STRING-ARRAY-BRACED-INIT]]. | (closed — the 3-tier pad-at-producer: isAssignable arm + coerce retype + asm zero-pad) | p19 Cluster G c62; §B Option A USER-chosen; the c61 re-probe's H_VerifierFailure frontier; `src/analysis/semantic/type_rules.hpp` + `semantic_analyzer.cpp` + `src/hir/lowering/cst_to_hir.cpp` + `src/mir/lowering/hir_to_mir.cpp` + `src/asm/asm.cpp`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-STRING-ARRAY-BRACED-INIT` | ⏳ **DEFERRED (known-limitation, fails LOUD; surfaced by c62's audit, 2026-06-30).** A brace-WRAPPED string-literal char-array init `char x[7] = {"hi"}` (valid C — gcc accepts) is REJECTED with H_VerifierFailure on all targets: the c62 zero-fill mechanism ([[D-CSUBSET-STRING-LITERAL-ARRAY-ZERO-FILL]]) handles only the BARE `char x[7]="hi"` form (a string Literal reaches coerce); the inner-brace form reaches MIR as a ConstructAggregate (a 1-element brace list), not a string Literal, so the c62 retype never fires. FAILS LOUD (H_VerifierFailure), NOT a silent miscompile — acceptable under the bar. NOT run-green-critical: sqlite's aXformType + all its char[]=string inits use the BARE form. FIX (when needed): detect a single-string-literal brace list in the aggregate/brace-init lowering and route it through the same c62 coerce retype. | (open) | p19 c62 audit; `src/hir/lowering/cst_to_hir.cpp` (lowerBraceInit / the aggregate path) + the c62 coerce retype; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-VA-LIST-PARAM-SLOT` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c63; NOT §B) — a `va_list` PARAMETER consumed by `va_arg` needs a storage slot.** ROOT (the c62 re-probe's FIRST + ONLY error — sqlite's LAST compile error, H0009): sqlite `sqlite3_str_vappendf(sqlite3_str*, const char*, va_list ap)` (the vprintf/vappendf family) at `pArgList = va_arg(ap, PrintfArguments*)` → "symbol N has no storage slot (non-addressable param or unbound) — required by lvalue use" (hir_to_mir.cpp:2359). va_arg READS + ADVANCES the va_list in place (lvalue use), so `ap` needs a slot; a va_list PARAM was received as a pure-SSA Arg (a LOCAL va_list is slot-backed by its VarDecl — only the PARAM form failed). PER-TARGET va_list ABI (agnosticism bar — config/strategy-driven, NO arch branch): SysVRegisterSave x86_64 = Array `__va_list_tag[1]`; HomogeneousPointer Win64/Apple = `char*` (Ptr<I8>); Aapcs64DualCursor arm64 = `__va_list` struct (already slot-backed by receiveByValueParam — unchanged). FIX (2 edits, hir_to_mir.cpp): (a) collectAddressTakenSymbols marks a va_arg/va_start/va_end operand address-taken → routes the Win64/Apple `char*` param onto the addressTaken-scalar arm (alloca + store) BY USAGE (its Ptr<I8> type is indistinguishable from a plain char*); (b) a dedicated reception arm — a SysV `va_list` param's incoming GPR is a POINTER to the CALLER's tag (C 6.7.6.3p7 array-param adjustment), so register that pointer AS the symbol's address (NO alloca, NO 24-byte copy — a copy reads a stale tag = a silent miscompile; mirrors receiveByValueParam's ByReference precedent). ★★ the FORWARDING half is [[D-CSUBSET-ARRAY-DECAY-AT-CALL-ARG]] (needed TOGETHER for the vappendf family). WITNESS: corpus `va_list_param_forward` (a va_list-PARAM callee + a `...`-forwarder that va_start's + forwards; 10+11+12+9 = 42; red→H0009). ★★ INDEPENDENT AUDIT all 3 RUN legs (the c57 lesson + this cycle's near-miss): the FIRST audit CAUGHT a silent SysV SIGSEGV (the forwarding gap, INVISIBLE on Windows which skips the elf RUN) → fixed; the fix RAN 42/15/24 on x86_64-elf + arm64-qemu + Win64-pe, gcc-cross-checked, objdump = pointer-reception (no 24-byte copy). | (closed — the va-operand address-taken mark + the SysV-va_list-param reception arm) | p19 Cluster G c63; the c62 re-probe's LAST compile error; `src/mir/lowering/hir_to_mir.cpp`; the vprintf/vappendf shape; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-ARRAY-DECAY-AT-CALL-ARG` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c63; NOT §B) — an ARRAY used as an rvalue / call argument decays to a pointer to its first element (C 6.3.2.1p3).** ROOT (surfaced by c63's first audit as a SILENT SysV SIGSEGV): a SysV `va_list` (`__va_list_tag[1]`, an Array) FORWARDED as a call arg (`vsum(n, ap)`) must decay to `&ap[0]`, but `lowerExpr(Ref(array))`'s addressableLocal arm emitted `Load(slot, Array)` = the tag's FIRST WORD (the gp_offset/fp_offset int) → the callee dereferenced garbage → SIGSEGV. The HIR inserts a `Cast<Array→Ptr>` (→ lowerLvalueAddress) ONLY when the arg type DIFFERS from the param; a va_list arg (Array) forwarded to a va_list param (Array) has MATCHING types → no Cast → the bare Load. (AAPCS64 struct + Win64 char* va_lists escaped; only SysV Array crashed — INVISIBLE on Windows, which skips the elf RUN.) FIX (hir_to_mir.cpp lowerExprNode Ref arm, Option A): a Ref to an Array-typed addressableLocal symbol returns `Gep(slot, 0)` typed Ptr<elem> instead of the invalid Load — byte-identical to the proven Cast<Array→Ptr> decay arm, purely TYPE-driven (TypeKind::Array gate, no arch branch), reusing the scalar call-arg GPR accounting. Cannot mis-fire: every other array-rvalue context carries an explicit Cast (→ lowerLvalueAddress); only a bare same-type array call arg reaches this arm. WITNESS: the [[D-CSUBSET-VA-LIST-PARAM-SLOT]] corpus + the audit's probe_fwd(15)/probe_passthru(24) RAN green on SysV (was 139 SIGSEGV) + arm64 + Win64, gcc-cross-checked; SysV objdump now `lea`/address-of; the array-decay family (array_decay / array_decay_additive[c59] / array_storage_index / memcpy_array_arg / string_array_zero_fill) stays green. | (closed — the Array-typed Ref-rvalue decay-to-Gep arm) | p19 Cluster G c63; surfaced by c63's audit (the forwarding half of the va_list-param fix); `src/mir/lowering/hir_to_mir.cpp` (lowerExprNode Ref arm); general C 6.3.2.1p3 array decay at call sites; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-TERNARY-ARRAY-DECAY` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c64; NOT §B) — an ARRAY arm of a conditional (`?:`) decays to a pointer-to-element (C 6.3.2.1p3 + 6.5.15), so a string-literal/array ternary has the common POINTER type, never an aggregate.** ROOT (the c63 re-probe's FIRST + ONLY error — c63 un-masked it; H0009 D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR): sqlite `sqlite3BtreeIntegrityCheck`/`checkAppendMsg` (sqlite3.c:83149) `checkAppendMsg(pCheck, "%s is %u but should be %u", isFreeList ? "size" : "overflow list length", …)` — the ternary is the FIRST VARIADIC arg (matching %s), so NO param-type coercion decays it. combineTernary (BOTH tiers) computed the type via commonArithType(thenT,elseT) → Invalid for two arrays, the c49 null-ptr + c56 fn-designator arms missed, and the fallback returned the FIRST arm's Array<char,5> → the conditional typed as an aggregate → an aggregate-valued ternary used by-value has no aggregate-width SSA value (a phi-of-aggregate cannot exist) → hir_to_mir's ternaryAggregateGuard fired H0009. [[D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR]] (FC8, closed) is the GENUINE-struct ternary by-address carrier — UNAFFECTED (structs/unions do not decay). FIX (NOT §B; C 6.3.2.1p3; 2 symmetric edits, the array-decay sibling of the c49/c56 null-ptr/fn-designator arms): combineTernary (semantic_analyzer.cpp + cst_to_hir.cpp) gains a decayArray arm — when both arms decay to the SAME interned pointer type (interner.operands(arr)[0] → interner.pointer(elem), matching element type, ≥1 arm an Array), the result is that pointer; at the HIR tier `common` then drives the EXISTING coerce() Array→Ptr decay arm (the sameElem Cast → a GlobalAddr to each rodata string) on both arms. Conservative: BOTH arms must decay to the SAME pointer type; genuine struct/union arms (no decay) keep the aggregate by-address lowering; incompatible-element conditionals stay on the existing fallback (fail-loud, not silent). WITNESS: corpus `ternary_string_arms` (the EXACT sqlite shape — the ternary as a variadic %s arg via `grab(const char*,...)` + a char*-returning `pick` form + inline both arm orders; reads each selected arm's bytes through the decayed pointer → 42; RED-ON-DISABLE → H0009 on the variadic form, the exact sqlite error). ★★ INDEPENDENT AUDIT all 3 RUN legs (the c57 lesson — the impl ran only pe): x86_64-elf SysV + arm64-qemu AAPCS64 + Win64-pe ALL RAN to 42, gcc-cross-checked (-O0/-O2 x86 + aarch64-gcc-O2, all 42); the genuine-struct `aggregate_ternary_rvalue` stays green (decayArray returns structs unchanged → not caught). | (closed — the combineTernary array-decay arm, both tiers) | p19 Cluster G c64; the c63 re-probe's FIRST error (c63 un-masked it); the c49/c56 ternary-arm-decay siblings ([[D-CSUBSET-TERNARY-NULL-POINTER]]/[[D-CSUBSET-TERNARY-NULL-FUNCTION-POINTER]]); [[D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR]] (the genuine-struct by-address carrier, distinct); `src/analysis/semantic/semantic_analyzer.cpp` + `src/hir/lowering/cst_to_hir.cpp` (combineTernary, both tiers); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-INCOMPATIBLE-ELEMENT-ARRAY-TERNARY` | ⏳ **DEFERRED (pre-existing fail-SOFT, NOT a c64 regression; surfaced by c64's audit, 2026-06-30).** A conditional whose two ARRAY arms have DIFFERENT element types (`cond ? "ab" : intArr` — char[] vs int[]) compiles to a type-confused byte-copy into the first arm's aggregate slot with only a WARNING (S000A), instead of a hard error. The c64 array-decay arm ([[D-CSUBSET-TERNARY-ARRAY-DECAY]]) correctly SKIPS it (the decayed pointers differ → `thenD != elseD` → falls through to the pre-existing FC8 aggregate-ternary by-address path [[D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR]]) — the audit VERIFIED identical behavior with the c64 arm reverted, so NOT a c64 defect. NOT run-green-critical: sqlite's ternaries are all same-element string literals (the c64 arm handles them), and an incompatible-element array conditional is a C constraint violation no real code relies on. FIX (when desired): make the aggregate-ternary lowering reject mismatched-element array arms LOUD (or apply the C 6.5.15 composite-type / constraint rule). | (open) | p19 c64 audit; [[D-CSUBSET-TERNARY-ARRAY-DECAY]] (the c64 arm that correctly skips it); [[D-CSUBSET-AGGREGATE-VALUED-CONTROL-EXPR]] (the FC8 path it falls to); plan 23 full-C arc |
| `D-MIR-ENTRY-BLOCK-ALLOCA-HOIST` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c69; NOT §B) — every body-local's storage `Alloca` is pre-emitted into the function ENTRY block (which dominates all uses), not at its decl site.** ROOT: a local DECLARED in an entry-unreachable block (the canonical case: a decl before the first `case` of a `switch`, which the c60 switch-flatten places in a predecessor-less pre-case block) had its `Alloca` emitted into that dead block; the mandatory unreachable-prune (`D-MIR-UNREACHABLE-PRUNE-NORMALIZE`) then dropped the block while a reachable case body still Load/Stored the slot → the `MirFunctionRebuilder` rewrite-map completeness abort (`D-OPT2-REWRITE-MAP-COMPLETENESS`), a SIGABRT compiler crash. FIX (`src/mir/lowering/hir_to_mir.cpp`): `lowerFunction` runs a `collectLocalDecls` pre-pass that emits each local's slot into the (still-open) entry block before lowering the body; the VarDecl site reuses the pre-emitted slot. The jumped-over initializer's Store stays at the (dead) decl site. Corpus `examples/c-subset/switch_decl_before_case` (→42; RED-ON-DISABLE → SIGABRT exit 134). Companion of `D-CSUBSET-ALLOCA-ADDRESS-REMATERIALIZE` (below — the hoist makes alloca addresses live from entry, which that anchor's remat keeps off the register-pressure cliff). |
| `D-AS4-ARM64-LARGE-DISP-LEA-BASE-CLOBBER` | ⏳ **DEFERRED (surfaced + confirmed PRE-EXISTING by the c69 audit, 2026-06-30; NOT introduced by c69).** An arm64 `lea result,[base+disp]` with a displacement > 16 MiB (> 0xFFFFFF) lowers to a `MOVZ Xd; MOVK Xd; ADD Xd, base, Xd` macro that CLOBBERS `base` when regalloc coalesces the base vreg into the result (base == Xd == result). c69's alloca-address rematerialization ([[D-CSUBSET-ALLOCA-ADDRESS-REMATERIALIZE]]) EXPOSED it on `large_frame_beyond_16mib` (a > 16 MiB frame slot) → SIGSEGV on arm64-qemu under the optimized pipeline; FIXED for the alloca-base case by folding a constant-offset alloca Gep into one `lea [sp + slotOff + disp]` (base = sp, the reserved frame register, never the clobberable result). RESIDUAL: a NON-frame `p + hugeConst` (> 16 MiB constant added to a DERIVED pointer whose vreg coalesces with the result) still hits the general Gep lea path, which c69 does NOT touch (audit-verified: no makeMemOffset/emitInst(Lea) added there; alloca bases are routed away first). PRE-EXISTING, c69-INDEPENDENT, astronomically rare, NO sqlite relevance. FIX (when needed): a scratch register for the arm64 large-disp `lea` multi-word variant, OR a regalloc base≠result constraint for it. | (open) | discovered p19 c69 (impl + audit); [[D-CSUBSET-ALLOCA-ADDRESS-REMATERIALIZE]] (c69, whose remat exposed it) + sibling [[D-AS4-ARM64-NEGATIVE-DISP-LEA]] (c42); `src/dss-config/targets/arm64.target.json`; plan 23 full-C arc |
| `D-CSUBSET-ALLOCA-ADDRESS-REMATERIALIZE` | ✅ **CLOSED 2026-06-30 (p19 Cluster G c69; NOT §B) — a body-local alloca's ADDRESS is rematerialized at each use (a fresh `lea_frame_slot` per use) instead of held in one entry-spanning vreg.** ROOT (un-masked by the c69 entry-block alloca-hoist above): hoisting every alloca to entry makes its address-value live from entry; reusing ONE address vreg across that range tips a register-pressure cliff (`examples/c-subset/index_negative`'s `main` takes addresses of locals → `R_SpilledDueToPressure` + `L_VirtualRegInPostRegalloc` scratch-pool exhaustion at the rewrite stage). FIX (the conventional "rematerialize frame addresses" treatment; `src/lir/lowering/mir_to_lir.cpp` + `src/lir/lir_callconv.cpp` + both target JSONs): MIR→LIR emits the `alloca` op ONCE (it reserves the slot, in scan order) and, at each use of the address (`regForValue` chokepoint), a fresh `lea_frame_slot k` (k = the alloca's 0-based scan-order index, on the payload) whose result has a tiny def→use range; lir_callconv materializes each `lea_frame_slot k` into `lea [sp + offsetOf(k)]` — byte-identical to the original alloca's `lea` (offset from the alloca-index→offset map it builds in scan order, asserted congruent with the alloca arm's running offset). The new `lea_frame_slot` virtual op is DISTINCT from `alloca` so the frame-layout slot-count scan is unchanged (exactly ONE slot per original alloca). A CONSTANT-offset Gep whose base IS an alloca (`&local[constIdx]`/`&s.field`) FOLDS its displacement into ONE sp-based `lea [sp + slotOff + disp]` (via an optional ImmInt operand on `lea_frame_slot`) — cheaper (one lea) AND load-bearing on arm64: without the fold, a >16 MiB `disp` in the un-folded `lea_frame_slot` base + `lea [base+disp]` pair lowers to the arm64 MOVZ/MOVK macro `ADD Xd,base,Xd` which clobbers `base` when regalloc coalesces the tiny-range remat base into the Gep result (base==Xd → SIGSEGV, `examples/c-subset/large_frame_beyond_16mib` under the full-release-like pipeline); folding makes the base `sp` (never the result), so the macro is safe. Corpora `examples/c-subset/index_negative` (→42; RED-ON-DISABLE → the scratch-pool exhaustion compile error) + `large_frame_beyond_16mib` (→42 on arm64-qemu; the const-disp fold is what keeps it green under remat). Verified RUN-green on all 3 legs (Win pe compile + WSL x86_64-elf + arm64-qemu). |
| `D-CSUBSET-CHAR-ALU-INTEGER-PROMOTION` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c71; NOT §B) — a SUB-INT (`char`/`signed char`/`short`) operand of a truth-value condition OR of `++`/`--` integer-PROMOTES to `int` (C 6.3.1.1) before the ICmp/Sub.** ROOT (the c70 re-probe compiled sqlite3.c END-TO-END past the VDBE switch, 12m41s → 902× `error[L_UnsupportedLoweringForOpcode]` "ICmp/Sub operand: integer TypeKind ordinal 15 [Char] has no native-width ALU forms" = the [[D-CSUBSET-32BIT-ALU-FORMS]] gate correctly refusing an 8-bit ALU op): a text library scans/classifies/steps `char` on nearly every line. Root-caused (delegated agent) to TWO HIR-lowering sites that build the op DIRECTLY, bypassing the C 6.3.1.1 integer promotion the ORDINARY binary combine site already applies (agent-verified: `a<b`/`c-'0'`/`*z==t` widen fine): (A) `coerceCondition` (cst_to_hir.cpp ~641) minted a SAME-TYPE (Char) zero → `Ne(char,char)` (a Char-typed ICmp) for a bare char truth value (`while(*z)`, `if(c)`, `for(;*z;)`, `?:`/`&&`/`||`); (B) `incDecArithValue` (~3975) → `incDecArithType(Char)=Char` (only Enum was remapped) → `BinaryOp Add/Sub` at opType=Char for `c++`/`c--`; (A') `combineUnaryOp`'s logical `!` (~2722) built a Char-typed `ICmpEq(operand,0)` (the pervasive `if(!*z)` scan idiom) — this 3rd site was FOUND by the independent audit (my initial fix + claims covered only A+B) and FOLDED IN this cycle. FIX (NOT §B — the tier that owns promotion [HIR combine sites] + the correct primitive [`integerPromotedType`→`coerce`→SExt/ZExt] already exist and work; the sites simply skipped them): all THREE sites promote the sub-int operand to `int` via the SAME `integerPromotedType(interner, t, *arith_)` + `coerce` chokepoint the array-index path (~3093) uses — Char/signed→SExt, unsigned(U8/U16)→ZExt via `mapCast`, automatic per kind; (A) mints the zero at the promoted type → `Ne(i32,i32)`; (B)'s EXISTING trailing `coerce(sum, lv.type)` narrows the i32 result back (Trunc) for the store, preserving 8-bit wraparound EXACTLY (C's `(char)((int)c±1)`; signed char 127++→−128); (A') `!` promotes its operand then keeps its Bool result (Bool operand EXCLUDED — its ICmpEq-0 form is already native). `arith_`-gated (block-less toy/tsql unchanged); float conds pass through (`integerPromotedType` no-ops floats → the `Ne` stays an FCmp). WITNESS: corpus `char_alu_integer_promotion` (`while(*z)` scan + plain/signed-char `c++` + signed-char 127→−128 wrap + `if(hi)`/`hi<0` SExt assertion → 42; EVERY observable independent of plain-char signedness [DSS types char SIGNED uniformly; gcc uses UNSIGNED char on arm64] so it matches gcc on all legs, the 2 signedness asserts use `signed char` explicitly; CONSOLIDATED into ONE function of SEQUENTIAL loops so it stays under the pre-existing x86_64 register-pressure cliff that many small inlinable static helpers would tip under -O — the cliff is [[D-CSUBSET-SUBNATIVE-ALU-FORMS]]/regalloc, NOT a c71 defect). Golden test `CharIfConditionLowersAsICmpNeTypedZero`→`CharIfConditionIntegerPromotesToI32BeforeICmpNe` (asserts I32 operands, not Char). RED-ON-DISABLE: the pre-fix compiler emits the exact ordinal-15 (Char) + ordinal-1 (signed char) ICmp/Sub errors on this corpus (WITNESSED on the WSL clone — 315/316, ONLY this corpus red pre-fix). 3-leg gate: Win 501/501; WSL x86_64-elf + qemu arm64 316/316. ★★ INDEPENDENT AUDIT (worktree, all-3-legs gcc-cross-check + the wrong-value/wrong-signedness/double-eval hunt): **SOUND** for A+B (no miscompile — 31 adversarial programs, 23 gcc-cross-checked ×8 legs [gcc x86 signed-char + aarch64 UNSIGNED-char, O0/O2]; objdump confirms SExt(signed)/ZExt(unsigned) both arches; single-eval confirmed; red-on-disable = the ordinal-15/-1 wall; 316/316 + 212/212 no regression; float/int/ptr/enum conds unchanged). The audit's ONE material finding = the un-covered `!` site (A' above) → FOLDED IN this cycle + re-gated (Win 501/501 + WSL/qemu 316/316, gcc-cross-check 42 both arches; `!char` is signedness-agnostic — 0/1 regardless of SExt/ZExt). SIBLINGS NOT fixed (their own sites, deferred/latent): `switch(char)` discriminant ([[D-CSUBSET-NARROW-SWITCH-DISCRIMINANT-CMP]], `A_NoMatchingEncodingVariant` cmp width 8) + unary `-c`/`~c` (Neg/BitNot on char — a result-type change; handle if a re-probe hits them). | (closed — the coerceCondition + incDecArithValue + combineUnaryOp[`!`] integer-promotion, reusing `integerPromotedType`+`coerce`) | p19 Cluster G c71; the c70 re-probe's 902-error frontier; the [[D-CSUBSET-32BIT-ALU-FORMS]] family (the two bypass sites of C 6.3.1.1 promotion); reuses the array-index-promotion chokepoint; `src/hir/lowering/cst_to_hir.cpp` + `tests/mir/test_mir_lowering_c_subset.cpp`; a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-UNARY-SUBINT-PROMOTION` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c72; NOT §B) — unary `~` (BitNot) and `-` (Neg) on a SUB-INT operand integer-PROMOTE the operand to `int` (C 6.5.3.3p3/p4) and carry the PROMOTED result type.** ROOT (the post-c71 re-probe CLEARED the 902 Char [[D-CSUBSET-CHAR-ALU-INTEGER-PROMOTION]] errors → 8 REMAINED on U8/U16 [TypeKind ordinal 6/7 = sqlite's `u8`/`u16`] at a `<deferred>` opcode): sqlite's `~u8`/`-u8` flag math. c71 generalized `combineUnaryOp`'s promotion ONLY to `!` (logical Not); `~`/`-` still built a sub-int-typed `UnaryOp` (`result=operand.type`) → the MIR Not/Neg walled at the LIR sub-native ALU gate. Binary `&`/`|`/`^`/`<<` already promote via `combineBinary` (only the UNARY operators bypassed it). This is EXACTLY the sibling the c71 independent audit flagged as latent. Probe confirmed: `~u8`→ord6, `-u8`→ord6, `~u16`→ord7 all error; `u8 & u8`/`| ^ <<` all OK. FIX (`src/hir/lowering/cst_to_hir.cpp` combineUnaryOp): widen the promotion condition from `Not` to `{Not,BitNot,Neg}` + coerce the operand to `integerPromotedType` (u8/u16→ZExt, signed char/i8→SExt via `mapCast`); the KEY NEW behavior vs c71 — `~`/`-` carry the PROMOTED (int) RESULT type (C 6.5.3.3 — "the result has the promoted type"; threaded via a `promotedTy` local into BOTH the operand coerce and the UnaryOp result), so an assignment back to a narrow lvalue truncates via the normal coerce; `!` keeps its Bool result. Bool EXCLUDED (not gated — keeps `~bool`/`!bool`); float/pointer/≥int operands pass through (`integerPromotedType` no-ops → `-1.5`/`~intConst` unchanged). ZERO combineBinary change. WITNESS: corpus `unary_bitnot_neg_promotion` (`(u8)~(u8)0xF0`=15 + `(~(u16)0xFFF0 & 0xFF)`=15 + `-(u8)1==-1`→+5 + `~(signed char)0==-1`→+7 => 42; observables masked-to-low-byte [signedness-agnostic — u8/u16 ZExt to positive int everywhere] or explicit `signed char`, so gcc agrees all 4 legs; sequential stmts dodge the -O regalloc cliff; RED-ON-DISABLE → `<deferred>: integer TypeKind ordinal 6/7 has no native-width ALU forms`). 3-leg gate Win 502/502 + WSL/qemu 317/317; gcc-cross-check 42 x86(O0/O2)+aarch64 (the result-type change COMPOSES — the `(u8)~m` truncate-back verified). ★★ INDEPENDENT AUDIT (worktree, focused on the c72 delta): **SOUND** — ~4300 observables gcc-cross-checked (x86 signed-char + aarch64 UNSIGNED-char, O0/O2), incl. ALL 256 u8 × 9 contexts + ALL 256 signed-char × 7 contexts; objdump ZExt(u8/u16)/SExt(i8/char) both arches; the result-type change proven GENUINELY-int (unsigned-wide reads + narrow-store truncation catch a stay-as-u8 bug); red-on-disable = the ordinal 6/7/1 wall; 317/317 no regression; float/int/`!`/Bool unchanged. Two pre-existing non-c72 issues ruled out (index_negative arm64 lea [[D-AS4-ARM64-NEGATIVE-DISP-LEA]]; the auditor's own LLP64-long test artifact on pe). | (closed — the combineUnaryOp {Not,BitNot,Neg} promotion + promoted result type for `~`/`-`) | p19 Cluster G c72; the post-c71 re-probe's 8-error frontier; the c71 [[D-CSUBSET-CHAR-ALU-INTEGER-PROMOTION]] unary sibling within the [[D-CSUBSET-32BIT-ALU-FORMS]] family; `src/hir/lowering/cst_to_hir.cpp` (combineUnaryOp); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-BITFIELD-SUBINT-PROMOTION` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c73; NOT §B) — a bit-field whose allocation UNIT is SUB-INT (u8/u16/signed char) computes its extract/insert RMW arithmetic at the PROMOTED int width, then Truncs back.** ROOT (the post-c72 re-probe cleared the 8 unary [[D-CSUBSET-UNARY-SUBINT-PROMOTION]] errors → 2 REMAINED on U8 [ordinal 6] at a `<deferred>` opcode): sub-int BIT-FIELDS (`struct{ u8 a:3; u8 b:5; }`). c71+c72 promoted the char/sub-int ALU at the SOURCE-OPERATOR sites (condition/++--/`!`/`~`/`-`); but a bit-field read/write is SYNTHESIZED at `emitBitfieldExtract` (hir_to_mir.cpp:2233) + `emitBitfieldInsert` (:2288) — `(unit>>off)&mask` / `(unit&~fieldMask)|((rhs&mask)<<off)` — with EVERY op at the field's allocation-UNIT type; for a u8 unit those shift/mask/and/or are u8-typed → the LIR `requireNativeIntWidth` gate walls them. sqlite uses `u8` flag bit-fields pervasively. Probe: `u8 a:3` read/write/`++` all error; `unsigned a:3` (int unit) OK; binary `&|^<<` OK (combineBinary promotes). FIX: both functions — when `unitBytes*8 < 32` — set computeTy=I32 + B=32, cast the loaded unit to i32 (`mapCast(fieldTy,I32)` ZExt/SExt; SExt-vs-ZExt irrelevant, the shift/mask re-derive the field value+sign at width B), run the ops at i32, and Trunc the extract RESULT / merged-unit BACK to the field type for the caller-node contract / Store (identical value, off the gate). A NATIVE unit (>=32-bit: u32/i32/u64 incl. the wide `u64:40` case the :2236 comment protects) keeps computeTy==fieldTy = the ORIGINAL path, byte-identical. The insert rhs is FIELD-TYPED by the caller contract (assignment RHS HIR-coerced; the aggregate-init caller at :2979 now coerces the init value to the field type first — MirBuilder can't query an inst's type mid-build, so the fix casts the rhs FROM the field type). WITNESS: corpus `bitfield_subint_promotion` (u8 read+write+RMW NEIGHBOUR-preserve + 3-bit OVERFLOW-trunc 9&7=1 + signed-char SIGN-EXTEND −3 + AGGREGATE-INIT + u16 unit → 42; observables signedness-agnostic or explicit `signed char` so gcc agrees x86+aarch64; RED-ON-DISABLE → `<deferred>: ordinal 6/7`). 3-leg gate Win 503/503 + WSL/qemu 318/318 (ZERO regression in existing bit-field + golden-MIR tests — the native/wide-unit + init paths); gcc-cross-check 42 x86(signed char)+aarch64(unsigned char). ★★ INDEPENDENT AUDIT (worktree, exhaustive bit-field hunt): **SOUND** — no miscompile on any layout/value/offset/width/signedness/arch. Generated sweeps (EVERY u8 two-field split × values + over-width + neighbour-preserve; signed char at offset 0 AND non-zero offset [exercises `Shl(32-off-W)`]; u16; the adversarial ZExt-vs-SExt edges `u8:8=0x80`→+128 [NOT sign-extended] / `signed char:8=200`→−56 / 1-bit signed=−1) all == gcc across 6 dss legs (pe/x86-elf/arm64-qemu × dbg/rel) + 4 gcc legs (x86+aarch64, O0/O2). objdump: u8 read = movzx+movzx+`and r,0x7`(32-bit)+movzx-trunc, signed char = movsx+shl+sar, `~fieldMask` clear-mask at 32-bit (neighbours preserved); WIDE `u64:40` = 64-bit load + `movabs 0xffffffffff` mask, NO promote/trunc = BYTE-IDENTICAL to pre-c73. Red-on-disable = the ordinal-6 wall. Windows ctest 503/503 incl. `core/test_bitfield_abi_conformance` + golden MIR + all 5 bit-field examples; all run-green x86-elf+arm64. | (closed — the emitBitfieldExtract/Insert sub-int-unit promote + the init-caller field-type coerce) | p19 Cluster G c73; the post-c72 re-probe's 2-error frontier; the SYNTHESIZED-op member of the [[D-CSUBSET-32BIT-ALU-FORMS]] family (siblings c71 [[D-CSUBSET-CHAR-ALU-INTEGER-PROMOTION]] + c72 [[D-CSUBSET-UNARY-SUBINT-PROMOTION]]); `src/mir/lowering/hir_to_mir.cpp` (emitBitfieldExtract/Insert); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-COMPOUND-ASSIGN-VALUE-PROMOTION` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c74; NOT §B) — a compound assignment on a SUB-INT lvalue used in VALUE/expression position integer-PROMOTES the base op to int (C99 `a OP= b` == `a = (T)((a) OP (b))`).** ROOT (the post-c73 re-probe STILL showed the SAME 2 U8 [ordinal 6] `<deferred>` ALU errors — c73 [[D-CSUBSET-BITFIELD-SUBINT-PROMOTION]] fixed u8 BIT-FIELDS, a REAL gap, but NOT sqlite's actual construct; ★ PROCESS MISS — inferred bit-fields from a probe instead of confirming sqlite's site, the re-probe is ground truth): I INSTRUMENTED the LIR ALU gate (mir_to_lir.cpp ~949, print `op#{int(op)} func# operand-ordinals`), compiled sqlite3.c once → **op#18 [=And] operands (U8,I32) → U8** = a compound bitwise-assign on a sub-int lvalue used in VALUE position (`if( (p->flags &= ~MASK) )`, `while((c|=b))`, `return (x&=y)`). c71/c72/c73 promoted the condition/++--/unary/bit-field sites, and the STATEMENT-position compound assign (`x &= y;`) already promoted via `lowerCompoundAssign` (my earlier probes used the statement form — which is why I missed the value form). But a VALUE-position compound assign lowers through TWO OTHER paths — the `lowerExpr` work-stack driver's `finishAssign` (cst_to_hir.cpp:1721, PRIMARY) + the `lowerBinary` Assign arm (~2600, recursive fallback) — both of which built `compoundLhsRead OP rhs` at the LVALUE type with UN-promoted operands → the sub-int ALU op → the gate. FIX (both paths, mirroring `lowerCompoundAssign`): `common = commonArithType(lhs,rhs)` (i32 for sub-int); coerce BOTH operands to `common` + build the BinaryOp at `common`; then `stored = (common != lv.type) ? coerce(opResult, lv.type) : opResult` (narrow back; the store+re-read yield the TRUNCATED value = C's compound-assign value). A NON-arithmetic common (Ptr compound `p += n`, common Invalid) keeps opType==lv.type → the c41 stride-Gep, BYTE-IDENTICAL. Promote-casts are pure conversions → the read-before-rhs value-position order holds. WITNESS: corpus `compound_assign_value_promotion` (`(x&=0x0F)`=15 + `(y<<=2)`=20 + `((z&=0x0F)==0)`+3 + `(w|=2)`=3 + `((s+=9)==1)`+1 [signed char] → 42; the yielded value is the STORED/TRUNCATED value so observables are low-byte [signedness-agnostic or explicit signed char], gcc-agreeing x86+aarch64; RED-ON-DISABLE → the ordinal-6/1 wall). 3-leg gate Win 504/504 + WSL/qemu 319/319 (ZERO regression — this value-position path change is GLOBAL but int/pointer/float/statement compound assigns all pass); gcc-cross-check 42 x86(signed char)+aarch64(unsigned char). ★★ INDEPENDENT AUDIT (worktree — truncated-yield property + no-regression on int/pointer/float compound + single-eval + red-on-disable): **SOUND** — 52 single-observable per-case programs (every op `&= |= ^= <<= >>= += -= *= /= %=` × u8/u16/s8/s16/char, operands chosen so the TRUNCATED stored value ≠ the promoted i32: `(u8 0x40)<<=2`→0 not 256, `(u8 0x10)*=0x11`→16 not 272, `(s8 100)+=100`→−56, `(u16 0x4000)<<=2`→0, plus mixed-sub-int + wider-rhs [common i64]) ALL match gcc on 8 legs (x86 signed-char + aarch64 UNSIGNED-char, O0/O2) — ZERO mismatch; STRUCTURAL: the yield is a fresh `lvRead` of the sub-int slot AFTER the store → by construction the truncated value, NEVER the promoted i32; the conditional narrow ≡ the untouched path's unconditional `coerce` (identity no-op to same type, cst_to_hir.cpp:331; `TypeId::==` on `.v`); the pointer path BYTE-IDENTICAL (`commonArithType` Invalid for Ptr → opType==lv.type → c41 stride-Gep, objdump-confirmed `int* p+=2` scales ×4 both arches); single-eval preserved (`a[i++]+=1`/`*p++&=m`/rhs-once = 204 everywhere). Red-on-disable = the exact ordinal-6 (`<deferred>` U8) + ordinal-1 (Add I8) wall. 319/319 WSL c-subset + HIR/MIR golden/lowering units (toy/tsql/generic/multi_lang byte-neutral). ★ PRE-EXISTING NON-c74 (deferred [[D-CSUBSET-BOOL-COMPOUND-ASSIGN]]): `_Bool b; b|=5`/`b+=3` fails S0003 in BOTH the untouched statement path (`lowerCompoundAssign`) AND value position → NOT a c74 regression (fails identically before/after); unlikely a sqlite blocker (sqlite uses u8/int not `_Bool`) → re-probe is ground truth. | (closed — the finishAssign + lowerBinary value-position compound-assign promotion, mirroring lowerCompoundAssign) | p19 Cluster G c74; the TRUE post-c73 blocker; the value-position member of the [[D-CSUBSET-32BIT-ALU-FORMS]] family (siblings c71/c72/c73); `src/hir/lowering/cst_to_hir.cpp` (finishAssign + lowerBinary Assign arm); a sqlite re-probe frontier; plan 23 full-C arc / SQLite-green |
| `D-AS-REGALLOC-SPILL-RELOAD-SCRATCH` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c75; §B USER — reserve-K, cycle 1 of a 2-cycle plan [[project_v0_0_2_p19_clusterg_c75_c76_regalloc_scratch_plan_2026_07_01]]; c76 = per-point scratch SUPERSEDES this reservation) — a function under GENERAL register pressure (peak simultaneous live values > allocatable GPRs) reserves K caller-saved registers per class as guaranteed spill-reload scratch.** ROOT: the post-c74 MILESTONE re-probe compiled sqlite3.c 12m57s through the WHOLE 8 MB amalgamation → `error[L_VirtualRegInPostRegalloc]` (lir_rewrite.cpp:411): function 1033 (sym 9794), INSTRUMENT-confirmed `op=1 [=arg] isCall=0 nOps=0 nSpilled=0` = a spilled incoming-PARAMETER's result with an EMPTY global scratch pool (general pressure, NOT a wide call). The linear-scan allocator (lir_regalloc.cpp, Poletto-Sarkar) reserved NOTHING for reloads + `pickScratchRegs` (lir_rewrite.cpp:82) built the scratch pool GLOBALLY (allocatable − regs-assigned-to-ANY-range) → a huge func assigns all 15 GPRs → empty pool → a spilled vreg's reload has no register. FIX (`buildFreeLists` + a new `computeReloadReserve`, lir_regalloc.cpp): reserve K caller-saved regs per class (held back from the free lists → never assigned → `pickScratchRegs` picks them up as scratch AUTOMATICALLY, unchanged). ★ K is DERIVED per-function from the max single-instruction register-reload demand (count of same-class VIRTUAL reg operands + result over NON-CALL instructions — calls are the deferred [[D-AS-REGALLOC-WIDE-CALL-OPERAND-COUNT]]) — NEVER a hardcoded count; x86_64 and arm64 compute their own. Caller-saved so a transient reload needs no callee-save; held back from the caller-saved END (allocator's last-choice partition) minimizing codegen impact; the existing L_VirtualRegInPostRegalloc check is the LOUD backstop if K is ever under-provisioned. WITNESS: corpus `regalloc_spill_reload_scratch` (f has 15 params each in TWO inline sums → the first sum's result coexists with all 15 live params → MAXLIVE 16 > 15 GPRs → spill; main calls f with 15 args = a call that FITS → 42; arm64 ~30 GPRs never spills → trivially green + value-checked). RED-ON-DISABLE (WITNESSED — git stash the fix + rebuild): `error[L_VirtualRegInPostRegalloc] function 1 exhausted the per-class scratch pool` on x86_64. 3-leg gate Win 505/505 + WSL/qemu 320/320. Rewrite units re-pinned (the fix intentionally changes allocation): `ExhaustedClass...` → a 20-arg WIDE CALL (the one exhaustion reserve-K genuinely can't fix); `Arm64HighFpr...` precondition (d31 is now reserved SCRATCH not assigned). Knife-edges index_negative + deep_lvalue_chain green. ★★ INDEPENDENT AUDIT found NOT SOUND — a SILENT MISCOMPILE (reserve-K reserved ARG registers [x86_64 SysV r9 = the 6th int arg] as scratch → the rewriter clobbered an incoming param; a 15-param fn returned 149 not 156, the 6th param read as the 5th's value; + a sibling PRE-EXISTING allocator bug assigning arg regs [xmm7] to other vregs → 54 not 42; pre-c75 both were LOUD failures → c75 turned them silent) → FIXED by the companion [[D-AS-REGALLOC-ARG-REGISTER-OCCUPIED]] (arg-exclude the reservation + model incoming arg registers as occupied [entry, argOp) so neither allocation nor scratch reuses them while a param is live); a 3rd fix followed (idiv implicit-reg scratch, part 4 of [[D-AS-REGALLOC-ARG-REGISTER-OCCUPIED]]); the 2nd COMPLETE RE-AUDIT is SOUND. | (closed with the arg-register companion fix — buildFreeLists reserve-K [arg-excluded] + computeReloadReserve; pickScratchRegs unchanged) | p19 Cluster G c75; the post-c74 MILESTONE re-probe frontier (whole-amalgamation compile); §B USER 2-cycle plan [[project_v0_0_2_p19_clusterg_c75_c76_regalloc_scratch_plan_2026_07_01]]; `src/lir/lir_regalloc.cpp`; a sqlite re-probe frontier; plan 12 MIR/LIR / SQLite-green |
| `D-AS-REGALLOC-WIDE-CALL-OPERAND-COUNT` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c76; §B USER option E) — FIXED by a new PRE-REGALLOC pass `lowerWideCallArgs` (`src/lir/lir_wide_call_args.{cpp,hpp}`, invoked in compile_pipeline.cpp before liveness): it splits each Call's SCALAR args beyond the cc's register-passed count (argGprs/argFprs, config-driven, no if(arch)) into `store_outgoing_arg` stack carriers before regalloc, so no Call holds more register-operands than the machine passes in registers → func 2088 clears; computeReloadReserve dropped its isCall exclusion (a call's operands are now bounded). ops[0]=callee (FC4-c2) + ops[1]=sret (FC7-C3) never split. INDEPENDENT AUDIT SOUND (22 probes 3 legs vs gcc O0/O2; the loud L_VirtualRegInPostRegalloc guard stays exact for a genuinely-too-wide callee — NO eviction); Win 515/515 + WSL/qemu 515/515. Value-witnessed by examples/c-subset/wide_call_{int,variadic,indirect,mixed}; the OPERAND-EXPLOSION red-on-disable is the unit `LirRewrite.ExhaustedClassEmitsLoudFailureNotSilentClobber` (small pure-C examples don't reach the explosion). Was: a single CALL instruction whose live register-operand count exceeds the register file exhausts the spill-reload scratch pool, and reserve-K ([[D-AS-REGALLOC-SPILL-RELOAD-SCRATCH]]) CANNOT fix it** (raising K reduces allocatable registers 1:1, so the call's spilled operands always exceed K by one; only reshaping wide outgoing arg lists to incremental stack stores BEFORE regalloc — the root-cause agent's option E — or a rewriter-side evict-a-live-reg escape hatch would fix it). WITNESSED by the c75 corpus's earlier 16-param variant (main's 16-arg call, func 2 exhausted) + the re-pinned `LirRewrite.ExhaustedClassEmitsLoudFailureNotSilentClobber` unit (a 20-arg wide call → loud `L_VirtualRegInPostRegalloc`). ★ NOW a SQLITE BLOCKER (falsifying the earlier 'not a blocker'): the post-c75 re-probe (c75's compiler) CLEARED func 1033 but hit **func 2088 (sym 18765), INSTRUMENT-confirmed `op=26 isCall=1 nOps=9 nSpilled=4`** — a sqlite call with 4 spilled register-operands exceeding K. Per the standing directive [[feedback_address_any_sqlite_green_blocker_2026_06_30]] this is IN-SCOPE, to be handled in the loop AFTER c75 is made sound (the arg-register fix [[D-AS-REGALLOC-ARG-REGISTER-OCCUPIED]] lands first). The loud failure is CORRECT (never a silent clobber) but it BLOCKS sqlite-green → needs option E (reshape wide outgoing arg lists into incremental stack stores BEFORE regalloc) or a rewriter-side evict-a-live-reg escape hatch — a §B fork to bring to the user. Sibling of c75 [[D-AS-REGALLOC-SPILL-RELOAD-SCRATCH]]. | (IN-SCOPE — needs option-E arg-list reshape or rewriter eviction, NOT reserve-K) | p19 Cluster G c75; discovered building the c75 corpus, CONFIRMED as sqlite's func 2088 by the post-c75 re-probe; `src/lir/lir_regalloc.cpp` (computeReloadReserve excludes calls); plan 12 MIR/LIR / SQLite-green |
| `D-AS-REGALLOC-DIRECT-ARG-RELOAD` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c77; USER-chosen) — func 2088 CONFIRMED CLEARED on real sqlite3.c (independent verifier a1b4246e compiled the whole 9.5MB amalgamation for x86_64:elf64: `L_VirtualRegInPostRegalloc` count 0, the ENTIRE regalloc/rewrite phase clean — closing the c75→c77 register-allocation arc; correctness SOUND, 7 probes 3 legs vs gcc O0/O2; the sqlite frontier ADVANCED past regalloc to the x86_64 encoder = c78). A SPILLED register-passed CALL ARG loads DIRECTLY into its ABI arg register, not via a scratch register that callconv then moves.** ROOT (post-c76 re-probe, INSTRUMENTED ground truth, agent a217cf96): sqlite func 2088 = a DIRECT CALL with 6 GPR args, ≥4 SPILL under pressure; the rewriter reloaded each spilled call-arg into a distinct SCRATCH GPR, but SysV has only 3 non-arg caller-saved GPR scratch (rax/r10/r11) vs up to 6 spilling args → exhaust at the 4th → `error[L_VirtualRegInPostRegalloc]`. c76's [[D-AS-REGALLOC-WIDE-CALL-OPERAND-COUNT]] (stacks args BEYOND the register pool) was NECESSARY-NOT-SUFFICIENT — it never touches the IN-POOL args that spill (func 2088 was MISDIAGNOSED as a wide-operand-count problem; the real cause is call-arg reload-scratch SUPPLY). USER chose direct-arg-reload OVER a rewriter eviction escape-hatch (keeps the no-eviction stance). FIX (agent aa22ff13, config-driven no if(arch)): a new POST-REGALLOC operand kind `SpillSlotRef` (slot v + reg class; repurposes a LirOperand padding byte, still 8B; passes verifyLirPostRegalloc since not a Reg; consumed by callconv before assembly) — the rewriter's `classifyCallRegArgs` (mirrors callconv's NSAA arg-cursor EXACTLY, fail-loud on divergence) marks register-passed scalar args; a spilled one becomes a SpillSlotRef instead of a scratch reload (the callee ops[0]/FC4-c2 + sret ops[1]/FC7-C3 + by-value carriers KEEP the scratch path); callconv's emitParallelRegMoves gains a MEM-SOURCE arm — a mem-src move is a pure SINK (never a source → never a cycle member; the cycle-break picks a reg-src victim) emitting `frame_load argReg,[slot]`, composing with the c76 move-cycle machinery UNCHANGED → demand==supply (1 arg reg/arg) → ZERO scratch for call args; REVERTS c76's isCall-exclusion removal in computeReloadReserve. KEEPS the loud L_VirtualRegInPostRegalloc guard (NO eviction) + 2 new fail-loud guards (classifier divergence, SpillSlotRef-in-overflow). WITNESS: `examples/c-subset/spilled_call_arg_reload` (non-inlinable recursive 6-GPR-arg callee, args live across a high-pressure loop → spill; distinct decimal weights → wrong-reload changes the exit; =227; RED-ON-DISABLE under the RELEASE arm → L_VirtualRegInPostRegalloc, the exhaustion being a Mem2Reg-SSA-spill phenomenon). Impl-agent-validated 3 legs vs gcc. ★ CONFIRMED via ground truth (the verifier's real-sqlite compile — the c76 lesson applied: NOT committed until func 2088 actually cleared, unlike c76). | (CLOSED c77; the SpillSlotRef direct-arg-reload) | p19 c77; the post-c76 re-probe frontier; COMPLETES the necessary-not-sufficient c76 [[D-AS-REGALLOC-WIDE-CALL-OPERAND-COUNT]]; root-cause a217cf96 + impl aa22ff13; `src/lir/lir_node.hpp`+`lir_rewrite.cpp`+`lir_callconv.cpp`+`lir_regalloc.cpp`; plan 12 MIR/LIR / SQLite-green |
| `D-AS-REGALLOC-ARG-REGISTER-OCCUPIED` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c75 correctness fix; NOT §B — a clean bug fix) — the allocator + rewriter model an incoming ABI ARGUMENT REGISTER as OCCUPIED over [entry, argOp.late), so neither allocation nor spill-reload scratch reuses it while the incoming parameter is still live in it.** ROOT (the c75 [[D-AS-REGALLOC-SPILL-RELOAD-SCRATCH]] independent AUDIT caught a SILENT MISCOMPILE): a param vreg's live range starts at its `arg` op's late slot, and the `mov home, argreg` materialization is inserted POST-regalloc by callconv — so [entry, argOp) has the incoming arg register appear FREE, and c75's added pressure/reservation reused it. TWO variants (both pre-c75 LOUD `L_VirtualRegInPostRegalloc` failures → c75 turned silent): (V1) c75's reserve-K reservation reserved an arg register (x86_64 SysV's last caller-saved GPR r9 = the 6th int arg) as scratch → the rewriter staged a spill-reload through r9 at entry → clobbered the 6th incoming param (15-param fn → 149 not 156); (V2, PRE-EXISTING, unmasked by c75's pressure) the allocator assigned an FP arg register (xmm7 = 8th FP arg) to a non-param vreg's home → clobbered the incoming FP param (→ 54 not 42). FIX (4 parts, all cc-config-driven, no arch identity, reusing existing machinery): (1) `buildFreeLists` reserves K caller-saved NON-ARG registers — excludes argGprs∪argFprs∪indirectResultRegister; under-reserves (SAFE — loud backstop) when a class has <K non-arg caller-saved (ms_x64 FPR has only xmm4/xmm5=2); (2) new `collectArgRegisterOccupied` + a covered-window `excludedScratch` augment (mirrors implicit-clobber/cross-call) — a range live before an arg register's release (`r.start < releasePos`, not the arg's own vreg) excludes that ordinal from allocation + spill-evict → fixes V2; (3) `rewriteOneFunc` forbids the cc's arg registers (the FC4-c2 `forbiddenBase`) as scratch for a SPILLED `arg`-op result → fixes V1's rewriter side (arg ops are contiguous entry-block-head + operand-less, so the arg-op result store is the sole entry-region reload that can clobber a still-live incoming arg reg). §B: NONE (a clean correctness fix of the ABI invariant 'incoming arg reg occupied [entry, argOp)'), orthogonal to + survives the c76 per-point supersession. (4) `rewriteOneFunc` ALSO forbids the instruction's OWN implicit registers (inputOrdinals∪clobberedOrdinals — e.g. x86 idiv rax+rdx) as reload scratch for its operands+result — the 1st RE-AUDIT caught a spilled idiv DIVISOR reloading into rax (reserve-K reserves the non-arg caller-saved rax), clobbering the dividend the idiv still needs → 121 not 160 (a SILENT miscompile; the mod/remainder path → rdx → 120); driven by the SAME per-opcode schema union collectImplicitClobberPositions already builds for the allocator's crossing-range exclusion, applied at the op where the transient reload happens (no idiv special-case). VALIDATED: the 4 arg repros + 3 corpora (arg_reg_gpr 156 / arg_reg_fp 42 / div_reload_scratch 160) RUN-correct on x86_64-elf(WSL)+pe(Win)+arm64(qemu) vs gcc O0/O2; 3-leg gate Win 508/508 + WSL/qemu 323/323. A 2nd COMPLETE RE-AUDIT (worktree agent a69337) = **SOUND** — all 6 focus areas complete with RUN evidence vs gcc on 3 legs (O0/O2): prior bugs stay fixed; NO next unsafe-scratch class (variable shift `x>>cl` = rcx is COVERED by the SAME generic `info->implicitRegisters` forbidden path — the shift opcodes declare rcx implicit; x86's 7 implicit-reg opcodes ALL handled, arm64 has 0; `mul` has no rdx:rax implicit; return-rax is materialized POST-rewrite; 2-address is post-rewrite; scratch is confined to one instruction's window); all 3 fix-parts RED-ON-DISABLE load-bearing (idiv off → div=121, arg off → 149/54, reservation off → loud L_VirtualRegInPostRegalloc); full ctest Win 508/508 + WSL/qemu 323/323 (qemu needs QEMU_LD_PREFIX+QEMU_STACK_SIZE) + knife-edges 42; under-reserve fails LOUD never silent (Win64 FP exhaustion → L_VirtualRegInPostRegalloc, no artifact). Independently RE-FOUND the pre-existing orthogonal SysV mixed-class stack-arg miscompile [[D-FF3-SYSV-MIXED-CLASS-STACK-OVERFLOW-OFFSET]] (reproduces on c74, NOT caused by c75). | (closed — buildFreeLists arg-exclusion + collectArgRegisterOccupied excludedScratch + rewriter arg-op-result forbidden-scratch) | p19 Cluster G c75 correctness fix; the c75 audit's silent-miscompile finding; `src/lir/lir_regalloc.cpp` + `src/lir/lir_rewrite.cpp`; a foundational fix under BOTH reserve-K and c76 per-point; plan 12 MIR/LIR / SQLite-green |
| `D-AS-REGALLOC-FPR-SPILL-RELOAD-SCRATCH` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c76) / RE-CHARACTERIZED (agent aa0c00e5, 2026-07-01) — the 'FPR spill-reload scratch collapse' was a MISDIAGNOSIS; the REAL defect is a SOUNDNESS BUG in the c76 move-cycle fix ([[D-ML7-2.3]] `L_MoveCycleUnsupported`), fixed as its correctness completion (ships WITH it, same commit).** The c76 move-cycle agent's DESIGN §5 mis-read an 8-FP callee's `movaps %xmmN,%xmm14` body as an FPR scratch-collapse (the FPR analog of c75's GPR reserve-K). The deeper root-cause (agent aa0c00e5, 2069s, 3-leg) DISPROVED it: (a) c75's reserve-K / computeReloadReserve / collectArgRegisterOccupied are ALREADY class-generic and cover FPR by construction (they select cc.argFprs for an FPR-class result) — NO FPR spill-reload gap exists; (b) the `xmm14` body is HARMLESS dead-param coalescing (a LIVE param always gets its own home; `direct8.c` reading all 8 FP params compiles CORRECTLY on the pristine c75 base). The REAL bug is CALLER-side, inside the move-cycle patch: at a cycle-break `emitParallelRegMoves` hands `pickScratchReg` the SHRUNKEN `moves` vector, so a register whose clean move was already EMITTED (now holding its committed argument) OR a dropped IDENTITY-move dst (`dst==src`, an incoming arg already in place) falls OUT of the exclusion → the picker returns it (e.g. xmm0) → the break `movaps scratch,cycSrc` CLOBBERS a committed argument → a SILENT SysV miscompile on a wide FP rotation whose caller sources overlap the low arg registers (rot8=1 not 42). ★ Without this fix the move-cycle patch converts `L_MoveCycleUnsupported` REJECTIONS into silent miscompiles — the two are ONE fix, MUST ship together. FIX (`src/lir/lir_callconv.cpp` `emitParallelRegMoves`, FPRFIX_isolated.diff, APPLIED to c76; config-driven, NO arch/reg names): capture the FULL move-destination footprint ONCE, BEFORE the identity-erase, and fold it into the scratch picker's `reserved` span at the cycle-break (the scratch avoids EVERY committed/identity destination, not just pending-move regs; a caller-saved scratch of the class always remains — SysV xmm8-15 vs the xmm0-7 dests; the `L_MoveCycleUnsupported` fail-loud backstop stays exact). §B: NONE (one agnostic fix; the anticipated reserve-K/arg-occupancy/per-point forks all target the wrong layer — the allocator/reserve layer c75 governs is fine, the bug is a missing exclusion in the post-regalloc move resolver). WITNESS: unit `CycleBreakScratchAvoidsCommittedArgDestination` (replays the emitted moves as a register machine, asserts each arg-dest ends holding its correct source token — name-agnostic; red-on-disable = xmm0 clobbered) + examples `fpr_arg_cycle_scratch` / `mixed_arg_cycle_scratch` (rot8 / simultaneous-GPR+FPR-cycle = 42 on 3 legs vs gcc O0/O2; red-on-disable → 1 on SysV); arg_reg_fp + all c75 witnesses green; Win 510/510. | (IN-SCOPE c76 → CLOSED at commit; the move-cycle-fix scratch-picker soundness completion, NOT an FPR spill-reload bug) | p19 c76; RE-CHARACTERIZED from the move-cycle agent's §5 misdiagnosis by agent aa0c00e5; the correctness completion of the move-cycle fix [[D-ML7-2.3]]; `src/lir/lir_callconv.cpp` (emitParallelRegMoves); plan 12 MIR/LIR / SQLite-green |
| `D-FF3-SYSV-MIXED-CLASS-STACK-OVERFLOW-OFFSET` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c76; USER Option 2) — FIXED: the CALLEE-side incoming overflow-arg offset now follows a monotonic class-SHARED byte cursor (`incomingStackArgByteOffset` in lir_callconv materializeOneFunc) mirroring the caller's overflowIdx, so a GPR-overflow and an FP-overflow arg occupy DISTINCT source-order slots (repro=73 not 64); slot-aligned CCs (Win64) get a congruence assertion. Audit SOUND (515/515 3 legs, gcc-cross-checked; red-on-disable → 64). ★ AUDIT CORRECTION: NOT SysV-only — arm64/AAPCS64 ALSO collides when BOTH classes overflow deeply (>8 GPR + >8 FPR args; the original 7int+9double repro only overflowed FP on arm64 so it masked this); the class-agnostic fix handles both (p05 red-on-disable → arm64=52). Was a SILENT MISCOMPILE (user-reported 2026-07-01; PRE-EXISTING — reproduces on c74 440b74a AND current build-rel [CONFIRMED: repro → 64 not 73], independent of the c75 regalloc arc) — x86-64 SysV (elf64): when a function has BOTH integer/GPR-class params overflowing past the 6 GPR arg registers AND float/FP-class params overflowing past the 8 FP arg registers, the CALLEE reads the overflowed GPR stack arg and the overflowed FP stack arg from the SAME stack offset instead of consecutive slots.** ROOT (disasm-confirmed by the reporter): `int probe(int i1..i7, double d0..d8)` (7 ints + 9 doubles) — the callee emits `mov 0x88(%rsp),%r9` (reads i7) AND `movsd 0x88(%rsp),%xmm4` (reads d8), BOTH at 0x88(%rsp); the caller wrote i7→[rsp+0] and d8→[rsp+8], so the FP overflow arg reads the INT overflow arg's slot → garbage double. The independent per-class overflow counters (GPR-overflow-index, FP-overflow-index) must EACH advance the SHARED incoming-stack cursor, not each restart at 0. WITNESS (CONFIRMED on build-rel): the repro → **DSS x86-elf SysV = 64; gcc -O0/-O2 = 73; arm64 AAPCS64 = 73; Win64 ms_x64 = 73** (only SysV wrong — AAPCS64 + ms_x64 slot-align, so no collision). FIX (config-driven per the SysV ABI's declared stack-overflow discipline — a per-class→shared-cursor rule, NOT an `if (arch==…)` identity branch): the incoming-stack-arg OFFSET assignment — callee-side param stack-slot layout (`src/mir/lowering/hir_to_mir.cpp` ArgOrdinalCounter / stack-overflow handling + `src/lir/lowering/lir_callconv.cpp` recv_by_value_stack_param) and/or caller-side computeMaxOutgoingStackArgs. | (OPEN — a full /dss-cycle: root-cause, config-driven fix, gcc-cross-check 3 legs, a c-subset example witness [mixed int+FP overflow, red-on-disable → SysV≠gcc]) | user-reported + added-as-a-cycle 2026-07-01 (loop's current item NOT stopped); a real silent miscompile but NOT a sqlite-green COMPILE blocker (mixed 7+GPR & 9+FP overflow is rare — sqlite is int/pointer-heavy with few doubles); a potential sqlite RUN-green concern IF a sqlite fn has such a signature (in-scope the moment sqlite RUN hits it per [[feedback_address_any_sqlite_green_blocker_2026_06_30]]); priority HIGH, queued as a dedicated cycle AFTER the current c75-regalloc + func-2088-wide-call arc; `src/mir/lowering/hir_to_mir.cpp` + `src/lir/lowering/lir_callconv.cpp`; the FF3 per-target ABI arc / plan 12 MIR-LIR |
| `D-OPT-RELEASE-FP-CONSTANT-UNLOADED-XMM` | 🔴 **OPEN / SILENT MISCOMPILE (release pipeline only; discovered p19 c76 D-FF3 agent a2848ce5, 2026-07-01; PRE-EXISTING, NOT a c76 defect).** A function with MANY distinct FP CONSTANT args, under the `release` shipped pipeline, miscompiles: after INLINING, the FP-sum references UN-LOADED xmm registers (agent saw xmm3/5/9/13/14/15 holding garbage) → a wrong FP result (the D-FF3 repro's release build = 32 not 73). ROOT (agent-confirmed, disassembly): NOT D-FF3 (under release the callee stack-arg reads ARE at distinct offsets — the D-FF3 fix is correctly applied); the wrong value comes from the FP-CONSTANT materialization / inlining+regalloc path failing to LOAD the constant xmm registers after inlining. PRE-EXISTING: the BASE compiler (D-FF3 reverted) yields the SAME 32 under release; the BASELINE (non-optimized/debug) path is CORRECT (73/142). Only surfaces on the WSL/qemu gate (there the SysV/arm64 legs RUN the optimized differential-verify; Windows ctest is compile-only for non-pe → not exercised). IMPACT on c76's "no-miscompiles" hardening: FP corpus examples with FP-CONSTANT args must OMIT `optimizedPipelines` (documented, per the corpus carve-out) OR use RUNTIME FP args (non-constant — sidesteps the FP-constant materialization bug AND still witnesses the optimizer×feature composition). A potential sqlite RUN-green concern (sqlite uses doubles under the release pipeline) — in-scope the moment sqlite RUN hits it [[feedback_address_any_sqlite_green_blocker_2026_06_30]]. FIX (its own cycle): root-cause why the FP-constant xmm loads are dropped/un-scheduled after inlining (FP-constant materialization ↔ inlining ↔ regalloc interaction). WITNESS (base repro): the D-FF3 examples `mixed_class_stack_overflow`/`_interleaved` with an `optimizedPipelines:[{shippedPipeline:release}]` arm added → RED (32/garbage) on the WSL/qemu run legs; debug arm GREEN (73/142). | (OPEN — a full /dss-cycle: root-cause the FP-constant materialization under inlining+regalloc, config-driven fix, gcc-cross-check 3 legs incl. the optimized run legs, a release-arm corpus witness) | discovered p19 c76 D-FF3 agent a2848ce5 (DESIGN §6, disassembly-confirmed); a DIFFERENT class from the c76 calling-convention fixes; `src/mir`/`src/lir` FP-constant materialization + inlining/regalloc; plan 12 MIR/LIR (or an optimizer plan) / SQLite-green RUN phase |
| `D-RUNTIME-MAIN-ARGC-ARGV` | 🔴 **OPEN / CONFIRMED ON ALL 3 TARGETS (c76 corpus agent a62343ea + parent, 2026-07-01) — DSS's program entry does NOT populate `argc`/`argv` for `int main(int argc, char** argv)`: main reads GARBAGE.** CONFIRMED by `int main(int argc,char**){return argc;}` run with no args (argc should be 1): **pe64=1801535488, x86-elf=224, arm64-qemu=80** — all garbage, all 3 targets. ROOT: the DSS program entry (_start / entry shim) calls main WITHOUT setting up argc/argv (on SysV/arm64 the kernel puts argc + the argv/envp vector on the initial stack at _start; on Windows the CRT must parse the command line) — c-subset programs use `main(void)`, so this NEVER surfaced until c76's corpus tried argc-derived runtime FP args (worked around with a `volatile int one=1;` global). NOT a c76 defect (pre-existing; all c76 examples use main(void)+volatile). Impact on sqlite-green: sqlite3.c is a LIBRARY (a main(void) test driver is unaffected); a sqlite SHELL / test driver using `main(argc,argv)` (e.g. to take a DB path or SQL) WOULD break — a sqlite-RUN concern IF the chosen driver uses argc/argv, in-scope then [[feedback_address_any_sqlite_green_blocker_2026_06_30]]. FIX (its own cycle): per-target program-entry argc/argv setup (SysV/arm64 read argc + argv from the initial stack; Windows parse the command line) → pass to main, config-driven. | (OPEN — a full /dss-cycle: per-target program-entry argc/argv setup, config-driven, gcc-cross-check 3 legs RUN, a `return argc` / argv-deref witness) | discovered p19 c76 corpus agent a62343ea, CONFIRMED 3 legs by parent; a program-entry / _start-shim concern distinct from the c76 calling-convention fixes; program-entry per target; plan 12 or a runtime/CRT plan / SQLite-green RUN phase |
| `D-OPT-RELEASE-SYSV-MIXED-CLASS-REG-ARG-DROP` | 🔴 **OPEN / SILENT MISCOMPILE (release pipeline; discovered p19 c77 verifier a1b4246e, 2026-07-01; PRE-EXISTING, NOT a c77 defect).** On x86-64 SysV (elf64) under the `release` shipped pipeline, a function with a leading GPR (integer/pointer) arg FOLLOWED by an FP arg DROPS the FP arg. Minimal repro `int g(int a, double x){ return (int)x; }` → release SysV returns 0, gcc returns x's value (47); DEBUG is CORRECT; Windows ms_x64 CORRECT — only SysV-release wrong. PROOF pre-existing (NOT c77): the repro has ZERO spilled call args so c77's SpillSlotRef path is never taken; stashing c77 + rebuilding the c76 baseline yields BYTE-IDENTICAL wrong values (p4a=6/p4b=197/z=0 on both). Likely the REGISTER-arg facet of the SysV mixed-class family — DISTINCT from the CLOSED overflow-offset [[D-FF3-SYSV-MIXED-CLASS-STACK-OVERFLOW-OFFSET]] (this is IN-REGISTER args, release-ONLY → an OPTIMIZER×mixed-class-arg interaction, possibly kin to [[D-OPT-RELEASE-FP-CONSTANT-UNLOADED-XMM]]). Impact on sqlite-green: sqlite is int/pointer-heavy (few mixed int+FP fns) → a RUN-green concern IF such a fn runs under release, in-scope then [[feedback_address_any_sqlite_green_blocker_2026_06_30]]; NOT the compile blocker (c78 = the x86_64 encoder gap). FIX (its own cycle): root-cause the release mixed-class register-arg placement/read (why an FP arg after a GPR arg is dropped under the optimizer). | (OPEN — a full /dss-cycle: root-cause the release mixed int+FP register-arg drop, gcc-cross-check 3 legs RUN, a red-on-disable witness) | discovered p19 c77 verifier a1b4246e (proven pre-existing on c76); a release-optimizer × mixed-class-arg concern; `src/mir`/`src/lir` arg placement/read + optimizer; plan 12 or an optimizer plan / SQLite-green RUN phase |
| `D-FFI-SHELL-POSIX-HEADER-BATCH` | ✅ **CLOSED 2026-07-02 (p19 Cluster G c81; config-ONLY, zero C++) — the 6 shipped headers + the file-scope stray `;` that were shell.c's ENTIRE front-end frontier (9 errors → 0; the semantic tier opened = c82).** Context: the USER §B-locked the shell.c CLI route (shell.c + sqlite3.c → the real `sqlite3` binary; multi-TU `--compile a.c b.c` → mergeCuMirs works). The 6 descriptors (src/dss-config/shippedLibs/; every struct glibc-EXACT, verified against /usr/include on x86_64 AND the aarch64 cross headers — identical → flat fields): **signal.json** [elf,macho] (`signal` with REAL fn-ptr types + `raise`; ★ SIGHUP/SIGINT/SIGTRAP/SIGTERM on the PP-VISIBLE `macros` surface — shell.c `#ifdef`-guards every registration, and the RED-ON-DISABLE proved demoting them to semantic `constants` compiles GREEN but runs exit 1 = the silent handler-compile-out gap, now corpus-pinned); **pwd.json** [elf] (getpwuid + passwd 48B); **dirent.json** [elf] (opendir/readdir/closedir + dirent 280B + opaque `DIR` via the stdio-FILE empty-named-struct precedent); **memory.json** UNGATED (string.h alias; first-wins injection dedups the double-include); **utime.json** [elf,macho] (utime + utimbuf — honest minimal, an empty descriptor fails loud by design); **sys/resource.json** [elf] (getrusage + RUSAGE_SELF + rusage 144B; ru_utime/ru_stime intern to the SAME timeval TypeId as sys/time.json → either include order resolves). pwd/dirent/resource are elf-ONLY: Darwin layouts diverge (readdir$INODE64; timeval tv_usec i32) — fail-loud beats silent-wrong. + **P0009**: `SQLITE_EXTENSION_INIT1;` (expands empty → a lone file-scope `;`) — the statement `emptyStmt` rule reused as a 5th `topLevel` alt (c-subset.lang.json; the c30 dual-position precedent; disjoint FIRST, non-speculative, existing Skip mapping). WITNESSES: 7 examples (shipped_signal_handler/getpwuid/getrusage/dirent_readdir/memory_h/utime_h/stray_toplevel_semicolon — 42 on WSL-elf + qemu + gcc; layouts proven THROUGH real libc [root's passwd fields, the 144B rusage fill+tail+sentinel, "." via d_name offset]; red-on-disable per part). Win 531/531. | (closed — the 6 descriptors + the topLevel emptyStmt alt) | p19 Cluster G c81; the shell.c front-end frontier (scoped a1d38b81, built a619eef4); `src/dss-config/shippedLibs/` + `src/dss-config/sources/c-subset.lang.json`; the c14/c51/c53 descriptor + c30 grammar precedents; plan 23 / SQLite-green shell.c route |
| `D-FFI-MACHO-TIMEVAL-TV-USEC-WIDTH` | ⏳ **DEFERRED (flagged by the c81 impl agent, 2026-07-02; LATENT on the unexercised macho leg).** The EXISTING sys/time.json macho variant models `timeval.tv_usec` as i64 where Darwin's is `suseconds_t` = **i32** (+ padding) — the exact divergence class c81 gated its new descriptors elf-only for. Latent (no macho leg runs sqlite yet); becomes real when the macOS leg is exercised. FIX: a macho variant with tv_usec i32 (+ the layout audit of the other macho variants). | (open) | flagged p19 c81 (agent a619eef4); `src/dss-config/shippedLibs/sys/time.json`; the macOS leg / plan 23 |
| `D-CSUBSET-VARIADIC-ARG-ARRAY-DECAY` | ✅ **CLOSED 2026-07-02 (p19 Cluster G c79) — a string literal (any Array-typed arg) in a VARIADIC TAIL decays array→pointer (C 6.5.2.2p6-7) even with NO declared param type to coerce against.** ROOT (INSTRUMENTED ground truth, agent ae1559d5 — 3-tier instrumentation fired exactly ONCE compile-wide): the post-c78 sqlite frontier's last encoder error (`A_NoMatchingEncodingVariant opcode 'mov': LiteralIndex pool slot 33 is not an integer literal` + the A_FunctionEncodeAborted cascade, function 16834 = sqlite3Pragma) was NOT a float — pool slot 33 held the STRING `"NONE"` from `sqlite3VdbeMultiLoad(v,1,"iissssss",…, "NONE")` (PRAGMA foreign_key_list, sqlite3.c ~145852). Every call-arg coercion site targets the callee FnSig's DECLARED param types; an arg BEYOND them got InvalidType and `coerce(arg, InvalidType)` passed through (cst_to_hir.cpp:330) → the literal never got the array→pointer decay Cast → un-decayed Array<Char> → `mir.addConst(string)` → the MIR→LIR string-pool route → `mov r64, Imm64` → the encoder fail-loud. (The error text's 'floats route to rodata' was a red herring; ZERO float literals escape the hir_to_mir ~540 routing, instrumented fact.) FIX (cst_to_hir.cpp, NO §B — reuses the existing decay arm, no new mechanism): a new `coerceCallArg(E, paramType)` — a valid paramType coerces byte-identically; an INVALID paramType (variadic tail / unknown sig) with an Array-typed arg decays via the EXISTING coerce decay arm to pointer-to-elem; swapped into all FOUR call-arg lowering sites (work-stack phase-2 [the sqlite path], pumpCallArgs, the recursive Call arm, the SQL flat-call). Deliberately out of scope: variadic int/float default promotions (ABI-tier width, varargs suites green) + FnSig designators (already correct). WITNESS: `examples/c-subset/variadic_string_literal_arg` (the exact sqlite shape — va_arg reads the decayed char* + verifies BYTES → 42; RED-ON-DISABLE → the exact A_ error pair). Post-fix sqlite: A_* 2→0 (the ENCODER STAGE fully clear); the frontier advanced to 9× K_NoMatchingObjectFormat = D-LK4-RODATA-PRODUCER-RUNTIME-INIT (c80). Validation: repro dss==gcc; 15-shape candidate battery green; WSL ctest 519/520 (the 1 = this anchor-guard row, by design pre-registration); variadic suites green. | (closed — coerceCallArg variadic-tail Array decay at the 4 call-arg sites) | p19 Cluster G c79; the post-c78 sqlite frontier's LAST encoder error; INSTRUMENTED (agent ae1559d5); siblings discovered (NOT sqlite, deferred): [[D-CSUBSET-ARRAY-DECAY-IN-COMPARISON]] + [[D-CSUBSET-ARRAY-DECAY-IN-CONDITION]]; `src/hir/lowering/cst_to_hir.cpp`; plan 23 full-C arc / SQLite-green |
| `D-CSUBSET-ARRAY-DECAY-IN-COMPARISON` | ⏳ **DEFERRED (discovered by the c79 instrumentation sweep, 2026-07-02; PRE-EXISTING, NOT in sqlite3.c).** A string literal / array as a COMPARISON operand (`z == "x"`) does not decay → the same un-decayed-Array `mov` A_ error. `combineBinary` has no array-decay arm for comparisons. Same site family as the TOP run-green deferral [[D-CSUBSET-ARRAY-DECAY-POINTER-IDENTITY]] (`p==arrayName` miscompile) — fix them together when that lands. A prototype exists (agent ae1559d5's fix-comparison-condition.prototype.py in scratchpad/c79floatmov, NOT applied). | (open) | discovered p19 c79; fail-loud (not a miscompile); `src/hir/lowering/cst_to_hir.cpp` combineBinary; plan 23 full-C arc |
| `D-CSUBSET-ARRAY-DECAY-IN-CONDITION` | ⏳ **DEFERRED (discovered by the c79 instrumentation sweep, 2026-07-02; PRE-EXISTING, NOT in sqlite3.c).** A string literal / array in a CONDITION position (`if ("x")` → I_TerminatorTypeMismatch; `p && "x"` → the un-decayed-Array A_ error). `coerceCondition` lacks an Array decay arm (falls through at cst_to_hir.cpp ~714). A trivially-true constant condition is degenerate C but legal. Same prototype as the comparison sibling. | (open) | discovered p19 c79; fail-loud (not a miscompile); `src/hir/lowering/cst_to_hir.cpp` coerceCondition; plan 23 full-C arc |
| `D-CSUBSET-FP-TO-UI-CODEGEN` | ✅ **CLOSED 2026-07-01 (p19 Cluster G c78) — `(unsigned)double` (FPToUI) gains an x86_64 encoding.** Pre-fix fp_to_ui had NO encoding (A_NoEncodingDeclared), 1× on the post-c77 sqlite encoder frontier. FIX: `x86_64.target.json` gives fp_to_ui an encoding block identical to fp_to_si (F2 REX.W 0F 2C /r = CVTTSD2SI for F64 src / F3 REX.W for F32) + `mir_to_lir` FPToUI threads the SOURCE float width (like FPToSI). A double in [0,2^32) reads exactly into the low 32 bits of the signed-64 CVTTSD2SI result → BYTE-IDENTICAL to gcc's `cvttsd2si rax,xmm0`. Witness `examples/c-subset/double_to_unsigned` (=42, x86-only; RED-ON-DISABLE → A_NoEncodingDeclared). RESIDUAL: arm64 fp_to_ui = native FCVTZU (deferred — arm64 declares no fp_to_ui opcode → clean L_RequiredLirOpcodeMissing, own cycle); full-range >2^63 = [[D-CSUBSET-UI-FROM-FP-UNSIGNED-I64]]. | (closed — the x86 fp_to_ui encoding + source-width thread) | p19 c78; the post-c77 encoder frontier; `src/dss-config/targets/x86_64.target.json` + `src/lir/lowering/mir_to_lir.cpp`; plan 12 MIR/LIR / SQLite-green |
| `D-CSUBSET-UI-FROM-FP-UNSIGNED-I64` | ⏳ **DEFERRED (p19 c78; NOT a sqlite blocker) — `(unsigned long)double` where the value ≥ 2^63 needs the conditional-subtract sequence.** x86 CVTTSD2SI is SIGNED; for an unsigned-i64 result ≥ 2^63 gcc emits a compare + subtract-2^63 + OR-back. c78's fp_to_ui ([[D-CSUBSET-FP-TO-UI-CODEGEN]]) covers u32 (and in-range u64) EXACTLY via the signed-64 CVTTSD2SI result; the >2^63 full-range case is deferred (sqlite stays in u32/size_t range → not a blocker). FIX (when needed): the conditional-subtract lowering at MIR→LIR for FPToUI to i64. | (open — the >2^63 unsigned-i64 conditional-subtract) | p19 c78 (the fp_to_ui codegen's residual); `src/lir/lowering/mir_to_lir.cpp` FPToUI; plan 12 MIR/LIR |

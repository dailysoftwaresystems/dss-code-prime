{
  "$comment": "★★★ THE arm64 GAS ASSEMBLY DIALECT — the SECOND assembly language, and the experiment that tests whether `asm.lang.json` is a shared core or a generalization of one dialect (plan 29 P2.6, 2026-08-13).\n\n★★★ THE RESULT OF THAT EXPERIMENT, STATED FIRST BECAUSE IT IS THE WHOLE POINT: `asm.lang.json` NEEDED NO CHANGE. Its seven line-structure rules (`asmLine`, `asmElement`, `asmDirective`, `asmStatement`, `asmStatementTail`, `asmLabelTail`, `asmOperandSeq`) and its four standalone holes (`lineEnd`, `labelSeparator`, `directiveIntroducer`, `asmStandaloneOperand`, plus the two shared with the embedded surface) received this dialect unmodified — not one rule edited, not one hole added, not one `{optional}` widened. What this document writes is a token table, ONE operand production with five subordinate rules, and a vocabulary table. That is the reuse claim, MEASURED rather than asserted, and the governing anchor's own stated test ([[project_inline_asm_own_language_2026_08_12]]: *\"the honest test of the core is whether it needs ANY change\"*).\n⚠ THE CLAIM IS SCOPED TO THE LINE GRAMMAR, WHICH IS WHAT `asm.lang.json` ACTUALLY OWNS. It is NOT a claim that the text→LIR ENGINE needed no change: it did, and those changes are findings in their own right (the sigil-less name resolution below, and the `operandForms` role taxonomy). Conflating 'the shared GRAMMAR held' with 'nothing had to change' is exactly the over-claim this project keeps having to walk back.\n\n★★★ HOW FAR THIS DIALECT IS FROM AT&T, WHICH IS WHY IT IS A HONEST TEST AND NOT A COPY WITH RENAMED RULES. Every axis that a 'shared token table with knobs' design would have had to parameterise is different here, and one of them is not parameterisable at all:\n  • REGISTER SIGIL — AT&T `%rax`, arm64 `x0`. There is NO sigil, which is not a smaller version of a sigil: it makes a register and a symbol LEXICALLY IDENTICAL (see `armName`).\n  • IMMEDIATE SIGIL — AT&T `$42`, arm64 `#42`.\n  • ★★ AND `#` IS AT&T's LINE COMMENT CHARACTER. This single byte is the measurement that made a dialect a LANGUAGE rather than a `.target.json` knob: in `asm-x86_64-att.lang.json` `#` opens a comment that runs to end-of-line; here it opens a VALUE. No token table can hold both, so the two dialects cannot share one — not 'awkwardly', but in principle.\n  • LINE COMMENT — AT&T `#`, arm64 `//` (and `/* */`, which both share).\n  • MEMORY OPERAND — AT&T `disp(base,index,scale)`, arm64 `[base, #off]`. Different opening token, different nesting, different position for the displacement.\n  • OPERAND ORDER — AT&T `destinationLast` (`subq %rcx, %rax`), arm64 `destinationFirst` (`sub x0, x1, x2`). And arm64's three-operand form means the destination is not even one of the inputs, where AT&T's two-address form requires that it is.\n  • MNEMONIC SPELLING — the two tables overlap by coincidence on `mov`/`add`/`sub`/`mul`/`ret` and diverge everywhere else: bitwise OR is `orr`, XOR is `eor`, NOT is `mvn`, shifts are `lsl`/`lsr`/`asr`, load/store are `ldr`/`str`, call is `bl`, branch is `b`.\n\n★★ WHAT THIS DOCUMENT DELIBERATELY DOES NOT KNOW, exactly as its AT&T sibling does not: which registers exist, which opcodes exist, or how anything encodes. `x0` resolves through `TargetSchema::registerByName` and `mov` through `TargetSchema::opcodeByMnemonic`; an unresolvable name is a precise diagnostic naming the target. A register list in this file would be the target identity leak the bar forbids — and it would be a particularly tempting one here, because the sigil-less surface makes 'just declare the register names as tokens' look like the easy fix for `armName`'s ambiguity. It is the wrong fix and it is refused by name; see `armName`.",

  "dssSchemaVersion": 4,

  "language": {
    "$comment": "★ THE NAME CARRIES BOTH AXES (`asm` · `arm64` · `gas`), the sibling convention. `gas` rather than `arm64-att`: the AArch64 assembly surface has no AT&T/Intel-style two-dialect split, so the ASSEMBLER is the axis that distinguishes it (a future `asm-arm64-armasm` for Arm's own tool is the sibling this shape leaves room for).\n⚠ THIS DOCUMENT CLAIMS `.s`/`.S` TOO, AND THAT MAKES THE EXTENSION AMBIGUOUS ACROSS DIALECTS — the ambiguity is REAL rather than a bug in this file, and it is now DEMONSTRATED rather than predicted: with this document shipped, `.s` genuinely names two languages. An assembly file is target-specific BY NATURE (no `.s` compiles for both x86_64 and arm64), so 'which dialect' is answered by the TARGET, not by the extension. Until a driver reads that answer off the target, `--language asm-arm64-gas` names it explicitly, which is what the examples corpus does. Anchored: D-DRIVER-ASM-DIALECT-SELECTED-BY-TARGET (the row predates this file; this file is the second witness it was waiting for).",
    "name": "AsmArm64Gas",
    "version": "0.0.1",
    "fileExtensions": [".s", ".S"]
  },

  "artifactProfiles": ["cli"],

  "$isaComment": "★★★ THE ARCHITECTURE THIS LANGUAGE EMITS (D-ISA-LANGUAGE-BOUND-TO-ARCHITECTURE) — the AT&T twin carries the full argument; read that file's copy and keep the two in step. ★★ THE VALUE IS `aarch64`, MATCHING `arm64.target.json`'s DECLARED `isa` AND NOT THAT TARGET'S `name` (`arm64`) NOR THIS DOCUMENT'S OWN NAME (`asm-arm64-gas`). Both of those are IDENTITIES — the strings a user types — while this is the fact both sides declare about the hardware, and the gate compares only the declared values. That the three spellings differ is the measurement: a rule keyed on any name would have to invent a mapping table, and this one does not have a table at all.",
  "isa": "aarch64",

  "languageReferences": {
    "$comment": "★★★ THE SHARED LINE STRUCTURE, REACHED BY REFERENCE — AND THIS BLOCK IS BYTE-FOR-BYTE THE SAME SHAPE AS THE AT&T DOCUMENT'S, WHICH IS THE MEASUREMENT. `entry: \"asmLine\"` imports exactly the rules reachable from it and NOTHING of the embedded `__asm__(…)` surface (the entry-closure scoping in `mergeLanguageReferences`), so this dialect owes the standalone holes plus the ones shared with the embedded surface — not every hole `asm.lang.json` declares.\n★ THE BOUND TOKENS ARE THE SAME ROLES the AT&T document binds, to DIFFERENT kinds of this document's own. Same roles, different vocabularies, one grammar: the property the mechanism exists to have, now witnessed by a second signatory rather than argued for by one.\n\n★★★ THE TEMPLATE PLACEHOLDER ROLES ARRIVED 2026-08-15 (D-ASM-DIALECT-DECLARES-NO-OPERAND-PLACEHOLDER) AND THIS DIALECT IS THE ONE THAT NEEDED THEM MOST. ✔MEASURED: AArch64 has no register-PINNING constraint letter (this target declares `r`, `w`, `m`, `i` and none of them names a specific register), so an `__asm__` OUTPUT on arm64 can only be written through a placeholder — which means that until these five bindings existed, EVERY aarch64 asm output was unspellable and sqlite's own `hwtime.h` arm (`mrs %0, cntvct_el0`) could not be written at all.\n★★ AND THE BINDING SPLIT IS THE INTERESTING PART, BECAUSE IT IS NOT THE SAME AS THE AT&T DOCUMENT'S. TWO of the five roles bind to kinds this document mints ONLY in its `asm-template` lexer mode (`%` -> PlaceholderSigil and `%l` -> PlaceholderLabelSigil), while `templateOperandIndex` binds to the ORDINARY global `IntLiteral` and `symbolicNameOpen`/`symbolicNameClose` bind to the ORDINARY `BracketOpen`/`BracketClose` this dialect already declares globally for its memory form — `[sp, #-16]` and `%[out]` genuinely use the same byte here, and the AT&T document had to declare a bracket of its own because x86 gas has none. Two dialects, one role set, two different vocabularies AND two different declaration sites: the sharpest witness this mechanism has produced.\n★ No FIRST-set collision follows from reusing the bracket: `armMemory` opens with `BracketOpen` and the placeholder opens with the placeholder sigil, so the two alt branches stay disjoint — and `detectAmbiguousAlternatives` re-runs over the MERGED rule set, so that is checked rather than asserted.",
    "asm": {
      "entry": "asmLine",
      "bindRules": {
        "asmStandaloneOperand": "armOperand"
      },
      "bindTokens": {
        "lineEnd": "LineEnd",
        "labelSeparator": "LabelColon",
        "directiveIntroducer": "DirectiveDot",
        "operandSeparator": "OperandComma",
        "symbolName": "Identifier",
        "templatePlaceholder": "PlaceholderSigil",
        "templateEscape": "PercentEscape",
        "templateLabelPlaceholder": "PlaceholderLabelSigil",
        "templateModifierPlaceholder": "PlaceholderModifierSigil",
        "templateOperandIndex": "IntLiteral",
        "symbolicNameOpen": "BracketOpen",
        "symbolicNameClose": "BracketClose"
      }
    }
  },

  "$tokensComment": "⚠ THIS BANNER SITS AT DOCUMENT LEVEL, NOT INSIDE `tokens`. A key in the `tokens` map is a LEXEME — arbitrary source text — so there is deliberately no `$`-documentation carve-out there and a `$comment` inside the map would declare a token spelled `$comment`. (✔MEASURED on the AT&T document the first time it was loaded: 'value must be an array of meaning objects'.)\n\n★★★ THE NEWLINE IS NOT WHITESPACE — the single most load-bearing line in any assembly dialect, restated here because getting it wrong is silent. Assembly is line-oriented: there is no statement terminator, so the newline IS the terminator. Flag `\\n` as `EmptySpace` and `ret` on one line swallows the next line's `mov` as an operand — a WRONG PARSE, not a parse error. `LineEnd` therefore carries NO flags.\n★ `\\r` STAYS `EmptySpace` so a CRLF file behaves exactly like an LF one.\n\n★★★ `#` IS THE IMMEDIATE SIGIL HERE AND THE LINE COMMENT IN `asm-x86_64-att.lang.json`. THIS IS THE PROOF, and it is a single byte: `mov x0, #100` and `movq $100, %rax  # set it` both contain `#`, one opening a VALUE and one opening a COMMENT. A shared token table parameterised by a `commentPrefixes` knob and an `immediatePrefix` knob would have to give this byte two incompatible meanings AT THE SAME TIME for the two dialects it serves. It cannot, so the table cannot be shared, so a dialect is a LANGUAGE. Every other difference in this file is a difference of degree; this one is a difference of kind.\n\n★★★ THE CONDITIONAL-BRANCH MNEMONICS USED TO BE DECLARED AS LEXEMES HERE, AND THE TWELVE ROWS ARE GONE — D-ASM-DIALECT-IDENTIFIER-CONTINUATION-NOT-CONFIGURABLE, closed 2026-08-13. `b.eq` is ONE mnemonic in gas, but `.` is this dialect's `directiveIntroducer`, so an undeclared `b.eq` tokenized as Identifier(`b`) + DirectiveDot + Identifier(`eq`) and `asmStatement` died at the dot. The workaround leaned on a real tokenizer property (the id-start path probes the declared-lexeme table and a lexeme LONGER than the identifier run wins, so the 4-byte `b.eq` beat the 1-byte run `b`) and it WORKED — its cost was that every dotted spelling appeared TWICE, here and in `instructions[]`, and the pair had to agree. ⇒ replaced by `identifierClass.extraContinue` below, which is gas's own rule and needs no per-spelling declaration at all; see that block's banner. The twelve `instructions[]` rows are unchanged, and they are now the ONLY place a conditional branch is spelled.\n⚠ `b.cs`/`b.cc` ARE gas ALIASES of `b.hs`/`b.lo` and are declared as such — two spellings of one condition, the same shape `.globl`/`.global` already has. Per [[feedback_reference_compilers_are_the_spec]] an alias real gas accepts is not optional.\n\n⚠ NO `!`, NO `@`, NO `%`, NO `;`, NO `=`, NO `+`: pre/post-index writeback (`[sp, #-16]!`), symbol-type sigils (`@function` / `%function`), gas's statement separator and `.set`-style assignment are all real AArch64 gas syntax this dialect does not model yet. They are UNDECLARED rather than half-declared, so they fail loud at the TOKENIZER with 'unexpected character' rather than parsing into something else. Anchored: D-ASM-ARM64-GAS-SURFACE-INCOMPLETE.\n⚠ AND ONE CONSEQUENCE OF THE `directiveIntroducer` CHOICE THAT IS EASY TO MISS: a gas LOCAL label (`.L1:`) opens with the directive dot, so `asmElement` routes it to `asmDirective`. ⓘ UPDATED 2026-08-13 — `asm.lang.json`'s `asmDirective` grew an `{optional asmLabelTail}` slot, so `.L1:` now PARSES here too, from the SHARED grammar and with ZERO bytes changed in this document (✔MEASURED on this dialect: `A0008 unknown assembler directive '.L3'` where it used to be `P0001 expected 'LineEnd' — got ':'`). That the fix reached a second dialect for free is the same reuse property this file's banner claims for the line structure — and it is the first change the shared core has ever needed, which is stated rather than glossed: the core held for a second dialect's VOCABULARY and did not hold for a form both dialects share.\n⚠ THE CONSUMER HALF IS NOT DONE: a directive node carrying a label tail is a LABEL DEFINITION, and the walker still sends it to the directive-vocabulary lookup, so `.L1:` is parseable and not yet definable. Anchored: D-ASM-DOT-PREFIXED-LOCAL-LABELS-UNPARSEABLE (grammar half done) and D-ASM-DOT-PREFIXED-LABEL-NOT-DEFINED-BY-CONSUMER. The corpus still uses plain identifiers (`Lwin`, `Lfail`), which gas accepts identically.\n\n★★★ `%%` IS A TOKEN OF ITS OWN (P5 wave 2, 2026-08-14) — THE ESCAPED PERCENT, DECLARED HERE FOR THE SAME REASON AS IN THE AT&T SIBLING AND WITH A DIFFERENT NEIGHBOUR. An embedded `__asm__` TEMPLATE is not a `.s` line: `%0` is an OPERAND PLACEHOLDER there and a literal percent must be written `%%`. ✔MEASURED 2026-08-14 on THIS port too (`aarch64-linux-gnu-gcc` 13.3.0, source fed as base64): extended `\"A%%0B\"` emits `A%0B` — the `%0` that appears after unescaping is NOT re-read as operand 0. So an unescape-then-scan design would bind `%%0` to operand 0: a silent miscompile the reference compiler does not have.\n★★ THE MECHANISM IS LEXEME **LENGTH**, NOT DECLARATION ORDER — ✔MEASURED in `tokenizer.cpp`'s `longestMatch`, which probes DOWN from the longest declared lexeme, so the 2-byte `%%` beats the 1-byte `%` regardless of row order (and `nlohmann::json` sorts object keys, so file order does not survive the parse at all).\n⚠ AND THE COLLIDING NEIGHBOUR IS DIFFERENT HERE, WHICH IS WHY THIS IS NOT A COPY: `%` is the REGISTER sigil in the AT&T document and the TYPE sigil here (`.type main, %function`). The escape has to be declared per dialect because the byte it escapes means something different in each — the same one-byte argument that made a dialect a language, one level down.\n⚠ NO SHAPE REFERENCES `PercentEscape` YET, which is the correct interim state: a `.s` containing `%%` fails LOUD at the parser, exactly as gas rejects it, so nothing is accepted that the reference assembler refuses while the template surface is still being built.",

  "tokens": {
    " ":  [{ "kind": "Whitespace", "flags": ["EmptySpace"] }],
    "\t": [{ "kind": "Whitespace", "flags": ["EmptySpace"] }],
    "\r": [{ "kind": "Whitespace", "flags": ["EmptySpace"] }],
    "\n": [{ "kind": "LineEnd" }],

    "//": [{ "kind": "LineCommentStart", "flags": ["EmptySpace"],
             "modeOp": "pushMode", "modeArg": "line-comment",
             "stringStyle": { "escapeKind": "none", "endsAt": "\n", "endsAtExclusive": true } }],
    "/*": [{ "kind": "BlockCommentStart", "flags": ["EmptySpace"],
             "modeOp": "pushMode", "modeArg": "block-comment",
             "stringStyle": { "escapeKind": "none", "endsAt": "*/" } }],

    "#":  [{ "kind": "ImmediateSigil" }],
    "%":  [{ "kind": "TypeSigil" }],
    ",": [{ "kind": "OperandComma" }],
    ":": [{ "kind": "LabelColon" }],
    ".": [{ "kind": "DirectiveDot" }],
    "-": [{ "kind": "MinusSign" }],
    "[": [{ "kind": "BracketOpen",  "opensScope": "Bracket" }],
    "]": [{ "kind": "BracketClose", "closesScope": true }]
  },

  "$identifierClassComment": "★★★ `.` CONTINUES AN IDENTIFIER HERE — gas's OWN rule, and the row that DELETED twelve token declarations (D-ASM-DIALECT-IDENTIFIER-CONTINUATION-NOT-CONFIGURABLE, closed 2026-08-13).\n★★ WHAT WAS HERE BEFORE, AND WHY IT HAD TO GO. `b.eq` is ONE mnemonic in gas — you cannot write `b . eq` — but `.` is this dialect's `directiveIntroducer`, so with `b.eq` undeclared the tokenizer produced Identifier(`b`) + DirectiveDot + Identifier(`eq`) and `asmStatement` died at the dot. The workaround was to declare all TWELVE conditional-branch spellings as LEXEMES in the map above, so every one appeared TWICE — once there and once in `instructions[]` — with the two required to agree. Both mismatch directions failed loud, so nothing was silent; the DUPLICATION was the defect, and it scaled with the vocabulary (`cbz`, `tbnz`, `b.al`, `b.vs`… each would have paid it again).\n★★ THE FIX IS THE REFERENCE ASSEMBLER'S ACTUAL RULE RATHER THAN A SPECIAL CASE FOR `b.`: a gas symbol may CONTAIN a dot. ✔MEASURED 2026-08-13 — `aarch64-linux-gnu-as` assembles `foo.bar:` as a label and `.quad DW.ref.foo` as a symbol reference, both rc=0. So `b.eq`, `foo.bar` and `DW.ref.__gxx_personality_v0` all lex as ONE Identifier from ONE declaration, and `instructions[]` matches on the text exactly as before.\n★★★ AND IT DOES NOT COLLIDE WITH THE OTHER TWO `.`-INITIAL CONSTRUCTS, BECAUSE IT ANSWERS A DIFFERENT QUESTION. `extraContinue` says what may appear INSIDE a name that has already started; it says nothing about what may START one, and `.` deliberately cannot (the facet has no `extraStart` key, and asking for one is refused by name at load). So a LEADING `.` is still the `directiveIntroducer` TOKEN, and `.L3:` versus `.text` versus `.section .rodata` are still separated exactly where they were — by `asmDirective`'s `{optional asmLabelTail}` slot and by the directive vocabulary. ⇒ ONE mechanism per question, not three that fight: leading dot ⇒ introducer token; interior dot ⇒ this key.",
  "identifierClass": {
    "extraContinue": "."
  },

  "lexerModes": {
    "$comment": "The two comment bodies, verbatim in shape from the AT&T dialect (which took them verbatim from `c.lang.json`): every character inside becomes an `EmptySpace` CommentChar, and an unterminated body is reported as a comment rather than as a wall of stray tokens. The `endsAtExclusive` on `//` above is what keeps the terminating `LineEnd` OUTSIDE the comment body — without it a comment-terminated line loses its terminator and merges with the next.",
    "line-comment":  { "defaultToken": { "kind": "CommentChar", "flags": ["EmptySpace"] }, "unterminatedAs": "comment" },
    "block-comment": { "defaultToken": { "kind": "CommentChar", "flags": ["EmptySpace"] }, "unterminatedAs": "comment" },

    "$templateComment": "★★★ `asm-template` — THE SECOND LEXICAL SURFACE OF THIS DIALECT, AND THE SIBLING OF THE AT&T DOCUMENT'S MODE OF THE SAME NAME. ⚠⚠ IT IS EMPTY ON PURPOSE (inline-asm P5c, 2026-08-17 — D-SEMANTIC-ASM-TEMPLATE-SIGILS-HARDCODED-BESIDE-A-CONFIG-OWNER, CLOSED). It used to declare three rows — `%%`, `%`, `%l` — while `scanInlineAsmTemplate` compared against the same bytes as C++ literals: two owners of one fact, and nothing able to detect them disagreeing, because the semantic tier has no dialect in scope when it scans. The rows are now SYNTHESIZED by the loader from `asm.lang.json`'s `semantics.inlineAsmTemplateLexemes` (the LANGUAGE's bytes) joined to this document's `bindTokens` (this dialect's kinds) by ROLE NAME; declaring a template lexeme here is now a LOAD ERROR naming both sites. Not a body mode: no `defaultToken`, so the scanner runs its ordinary lexeme match and consults the synthesized table FIRST with the global table as fallback. Entered only by the TEMPLATE PARSE ENTRY, which reads the name from `assembly.templateLexerMode`; a `.s` never enters it.\n\n★★ WHAT EACH SYNTHESIZED ROW BUYS HERE, AND THE THIRD IS THE ONE THAT NEEDS DEFENDING.\n  • `templatePlaceholder` -> `PlaceholderSigil` (`%`) — the operand placeholder. `%` is `TypeSigil` globally here (`.type main, %function`), a DIFFERENT collision from the AT&T document's `RegisterSigil`, which is exactly why the KIND is still per dialect even though the BYTE no longer is: the byte being escaped means something different in each. The same one-byte argument that made a dialect a language, one level down — and it survives the change intact, because what moved is the byte, not the kind.\n  • `templateLabelPlaceholder` -> `PlaceholderLabelSigil` (`%l`) — `asm goto`'s label sigil, two bytes so maximal munch settles `%l[done]` against `%0`. ⚠ It cannot shadow `lsl`/`ldr`/`ldp`: the lexeme starts at the percent.\n⚠⚠ THE `%l` ROW IS THE GRAMMAR HALF ONLY — NOTHING LOWERS A LABEL PLACEHOLDER. It makes `%l[done]` LEX and PARSE; the lowering then refuses it by name, because a label target must resolve to a BLOCK of the function that embedded the template and an operand binding carries a register and no block. See `asm.lang.json`'s `asmTemplateLabelRef` for the full statement.\n  • `templateEscape` -> `PercentEscape` (`%%`) — bound with **NO SHAPE REFERENCING THE KIND IN THIS DIALECT**, and that is deliberate rather than an oversight. AArch64 gas has no register sigil (`x0`, not `%x0`), so there is no `%%<reg>` form to accept — the AT&T document has one and this one does not. What the row buys is the MUNCH GUARD, a correctness property and not a convenience: ✔MEASURED 2026-08-14 on `aarch64-linux-gnu-gcc` 13.3.0 with the source fed as base64, extended `\"A%%0B\"` emits `A%0B` and the `%0` that appears is NOT re-read as operand 0. Without the row `%%0` would lex as PlaceholderSigil + PlaceholderSigil + IntLiteral and a placeholder scanner would find a lone `%` in front of the digit. With it the two bytes are consumed together and `%%0` reaches no shape at all — refused loud, which is what the reference toolchain does one stage later when gas sees the emitted `%0`. ⓘ It is also the one role no shared SHAPE spells; see `asm.lang.json`'s `$templateEscapeHoleComment` for why binding it is still legal.\n  • `symbolicNameOpen`/`symbolicNameClose` -> `BracketOpen`/`BracketClose` (`[`/`]`) — ⚠ THESE TWO ROWS ARE NEW IN THIS MODE AND CHANGE NOTHING, which is stated rather than glossed. `[`/`]` are already GLOBAL in this dialect (the memory form), so before P5c `%[out]` reached them through the mode's global fallback; the synthesis now writes them into the mode as well. ✔MEASURED that the synthesized rows are byte-identical to the global ones — same kinds, `opensScope: Bracket` and `closesScope: true` — so the override and the fallback resolve to the same meaning and no input reads differently. The AT&T sibling, which has NO bracket of its own, genuinely needs the rows.\n★★ THE ORDERING MECHANISM IS LEXEME **LENGTH**, NOT DECLARATION ORDER — ✔MEASURED in `tokenizer.cpp`'s `longestMatchInMode`, which probes DOWN from the longest key; `nlohmann::json` sorts object keys anyway, so a `.json` file's row order does not survive the parse. ⇒ a 'red on reordering' pin cannot exist. The pin that CAN exist now nulls the ROLE in `asm.lang.json` and watches this dialect's row disappear with it — stronger than the old row-deletion pin, which only proved two copies happened to agree.\n\n★★★ AND SINCE P30 THE MODE ALSO CARRIES ONE **COMPOSED** ROW PER WIDTH-VIEW LETTER, WHICH IS THE ONE PLACE THE SYNTHESIS JOINS THREE THINGS INSTEAD OF TWO. The BYTE is still the language's (`templatePlaceholder`), the KIND is still this dialect's (`templateModifierPlaceholder`), and the LETTER is this dialect's `assembly.templateModifiers` — so the loader mints `%` + `w` and friends into this table. ✔MEASURED 2026-08-24 on gcc 13.3.0 both ports that the letter cannot be shared: `%w` is 32 bits on aarch64 and 16 on x86-64. ⚠ Every letter takes the SAME kind, so the placeholder alternation stays FIRST-disjoint however many are declared, and maximal munch settles `%w0` against `%0` exactly as it settles `%l2` — the 2-byte lexeme wins over the 1-byte one. ⚠ A letter composing onto a lexeme another role already owns (`l` is the live example) is REFUSED at load naming both owners.",
    "asm-template": {}
  },

  "numberStyle": {
    "$comment": "gas integer literals: decimal, `0x` hex, `0b` binary and a leading `0` octal — the same set the AT&T dialect declares, because it is a property of GAS rather than of the CPU, and that is a real (small) piece of evidence about where the dialect boundary falls. No float literals: a floating-point CONSTANT in a `.s` appears only inside a `.double`/`.float` directive, which this dialect does not model (an undeclared directive fails loud). Declaring float syntax that cannot be lowered would be the accept-and-ignore this project refuses.",
    "decimal": true,
    "integerPrefixes": [
      { "prefix": "0x", "radix": 16, "digits": "0-9a-fA-F" },
      { "prefix": "0X", "radix": 16, "digits": "0-9a-fA-F" },
      { "prefix": "0b", "radix": 2,  "digits": "01" },
      { "prefix": "0",  "radix": 8,  "digits": "0-7" }
    ],
    "emitKind": { "integer": "IntLiteral" }
  },

  "syncTokens": ["LineEnd"],

  "shapes": {
    "$comment": "★ ONLY THE ROOT AND THE OPERAND PRODUCTION LIVE HERE — six rules, and five of them are the operand. Everything between root and operand (the line, the label chain, the directive, the statement, the operand sequence) arrives from `asm.lang.json`. THAT SIZE IS THE MEASUREMENT: a second dialect's `shapes` block is the size of an operand grammar, not the size of an assembly grammar.",

    "root": {
      "$comment": "An arm64 `.s` translation unit: a run of lines, each terminated.\n★★ THE FINAL NEWLINE IS MANDATORY, for the reason the AT&T document measured and which is a property of the SHARED grammar rather than of either dialect: a trailing `{optional asmElement}` to accept an unterminated last line is refused by the loader as `C_AmbiguousAlternatives` — after any complete line an Identifier could open either another `asmLine` or the unterminated final element, and LL(1) cannot tell them apart. Making the terminator optional inside `asmLine` would remove the ambiguity by removing the LINE DISCIPLINE; `speculative: true` would put backtracking into the one grammar in this tree that has none; appending a newline would be the driver rewriting the user's source.\n⇒ A file whose last line lacks a newline is a LOUD parse error at that line. That this reproduces identically in a second dialect is the evidence it is a shared-grammar property and not an AT&T quirk. Anchored: D-ASM-FINAL-NEWLINE-REQUIRED.",
      "repeat": "asmLine"
    },

    "armOperand": {
      "$comment": "★★ ONE arm64 OPERAND — three FIRST-disjoint forms, no speculation:\n  • `#100` / `#-8`   immediate → ImmediateSigil\n  • `[x1, #16]`      memory    → BracketOpen\n  • `%function`      symbol-type marker → TypeSigil\n  • `x0` / `helper`  a NAME    → Identifier\n★★★ AND THE LAST ARM IS THE FINDING THIS DIALECT EXISTS TO PRODUCE. AT&T needs FIVE arms because its sigils separate the forms lexically; arm64 needs three because it has almost no sigils — and the one it drops entirely, the REGISTER sigil, is the one that was doing the separating. `x0` (a register) and `helper` (a call target) and `Lwin` (a branch target) are THE SAME TOKEN. No grammar can split them, so `armName` is one rule serving three operand ROLES, and the split has to happen where the information actually lives: the TARGET's register table. See `armName`.",
      "alt": [ "armImmediate", "armMemory", "armTypeMarker", "armSectionName", "armName" ]
    },

    "armSectionName": {
      "$comment": "★★★ `.rodata` IN OPERAND POSITION — the shape `.section .rodata` needed and no `armOperand` arm had (D-ASM-SECTION-DIRECTIVE-WITH-OPERAND-UNMODELLED, closed 2026-08-13). ✔MEASURED 2026-08-13 with BOTH reference assemblers: `.section .rodata` assembles rc=0 and produces a `.rodata` PROGBITS+A section, while a bare `.rodata` is `Error: unknown pseudo-op`. So the read-only data section is reachable ONLY through an operand, and until this rule existed the line died at the tokenizer/grammar seam with `expected 'LineEnd' — got '.'`.\n★★ THE DOT IS MANDATORY AND IS NOT DECORATION. ✔MEASURED: gas's `.section rodata` (undotted) creates a section literally NAMED `rodata` with NO allocation flag — a DIFFERENT section from `.rodata`. Accepting the undotted spelling as a synonym would place data somewhere the reference assembler does not, so this rule requires the introducer and the consumer checks that the leading token IS this dialect's introducer (read off the tree, never hardcoded as `.`).\n★ IT IS NOT BOUND TO AN `operandForms` ROLE, exactly like `armTypeMarker` beside it and for the same reason: a role is what an INSTRUCTION operand is lowered through, and this one is read as TEXT by the directive handler and resolved against this dialect's own section-opening directive ROWS. An unroled rule reaching instruction position fails loud in `decodeOperand` — the correct answer for `add x0, .rodata, x1`.\n★ FIRST-DISJOINT WITH EVERY SIBLING ARM: `DirectiveDot` against `ImmediateSigil` / `BracketOpen` / `TypeSigil` / `Identifier|IntLiteral|MinusSign`. And disjoint from `asmLabelTail`'s `LabelColon`, so `asmDirective`'s two consecutive `{optional}` slots stay LL(1) with this arm in the operand set.",
      "sequence": [ "DirectiveDot", "Identifier" ]
    },

    "armTypeMarker": {
      "$comment": "★★ `%function` — THE SYMBOL-TYPE MARKER, AND A THIRD INDEPENDENT AXIS ON WHICH THE TWO DIALECTS DIFFER. ✔MEASURED 2026-08-13 in WSL: `aarch64-linux-gnu-gcc -S` emits `.type main, %function` while x86_64 `gcc -S` emits `.type main, @function`. Same directive, same meaning, different sigil — because `@` is a COMMENT character on 32-bit ARM and the ARM gas port never reclaimed it. ⓘ AArch64 gas ACCEPTS `@function` too (✔MEASURED, assembles rc=0), so this is a case where the dialects overlap and the reference COMPILER's choice is the one worth matching; only `%function` is declared, and an `@function` operand fails loud at the tokenizer rather than being silently accepted, which keeps 'what this dialect reads' honest.\n★ IT IS NOT BOUND TO AN `operandForms` ROLE, deliberately: a role is what an INSTRUCTION operand is lowered through, and this one is read as TEXT by the directive handler and compared against the row's declared `marker`. An unroled rule reaching instruction position fails loud in `decodeOperand` — the correct answer for `add x0, %function, x1`.\n★★ AND THIS IS THE SINGLE CLEANEST REFUTATION OF THE 'ONE TOKEN TABLE WITH KNOBS' DESIGN, because it is the SECOND byte to change meaning across the pair: `%` is the REGISTER sigil in `asm-x86_64-att.lang.json` (`%rax`) and the TYPE sigil here. Together with `#` (comment there, immediate here) that is two of the two sigil bytes inverted — not a knob setting, a different language.",
      "sequence": [ "TypeSigil", "Identifier" ]
    },

    "armName": {
      "$comment": "★★★ A BARE NAME — A REGISTER *OR* A SYMBOL, AND THE GRAMMAR CANNOT TELL WHICH. `mov x0, x1`, `bl helper` and `b Lwin` all put a plain Identifier in operand position, because arm64 gas has no register sigil. This is not a gap in this file; it is the surface gas defines.\n★★ THE RESOLUTION IS A TARGET LOOKUP, WHICH IS WHAT GAS ITSELF DOES: a name that `TargetSchema::registerByName` resolves IS a register; one it does not IS a symbol. That keeps the decision in the vocabulary tier where it belongs, costs this document no register list, and matches the reference assembler's own shadowing rule (a symbol named `x0` loses to the register — ✔ gas behaves this way, and per [[feedback_reference_compilers_are_the_spec]] matching it is the requirement, not a choice).\n★★ HOW THIS DOCUMENT *DECLARES* THE AMBIGUITY, with no new config key: `operandForms` binds BOTH `register` AND `displaced` to THIS rule. Two roles on one rule is not a duplicate — it is the statement 'these two roles are lexically indistinguishable here, resolve them against the target', and it is exactly the fact an engine keyed on rule-identity alone needs to be told. A dialect WITH a sigil (AT&T) binds them to two different rules and gets the strict behaviour it should: `%foo` that names no register is an ERROR there, because the sigil already promised a register.\n⛔ THE FIX THAT MUST NOT BE TAKEN, recorded because it is the obvious one and it looks clean: declaring `x0`…`x30`, `w0`…`w30`, `sp`, `xzr`, `d0`…`d31`, `v0`…`v31` as TOKENS in the map above. It would work, it needs no engine change, and it would put a 130-entry copy of the target's register file into a language document — the precise target identity leak the bar forbids, and the second copy would start drifting the day the target gained a register. Refused by name.\n\n★★★ WIDENED 2026-08-13 FROM `sequence:[Identifier]` TO ALSO ACCEPT A BARE NUMBER — D-ASM-ARM64-BARE-SCALAR-OPERAND-UNPARSEABLE, and the trigger was the data directives below. `.quad 58` puts an `IntLiteral` in operand position, and this alt had NO arm for one, so it died at the parser with `expected 'LineEnd' — got '58'` — while `asm.lang.json`'s OWN `asmDirective` comment asserts that '`.byte 1, 2` (integers)' parses. The shared core was describing a capability one of its two dialects did not have.\n★★ IT IS ALSO WHAT gas ACCEPTS, WHICH IS THE ACTUAL REQUIREMENT rather than a convenience: ✔MEASURED 2026-08-13, `aarch64-linux-gnu-as` assembles `mov x0, 5` and `add x1, x0, 3` rc=0 and encodes them as `#0x5`/`#0x3` — the `#` is OPTIONAL on this port. A dialect refusing a bare immediate was rejecting valid AArch64 gas in the instruction stream too, not only in data.\n★★★ AND IT NEEDED NO NEW ROLE, NO NEW BINDING AND NO ENGINE CHANGE, which is the part worth checking against the bar. The existing `register`+`displaced` pair on this rule already resolves by TARGET LOOKUP, and that lookup answers the new case correctly for free: `registerByName(\"58\")` fails exactly as `registerByName(\"helper\")` does, so a number lands on `displaced` — which `assembly_config.hpp` defines as 'a scalar, optionally followed by a base'. That is precisely what AT&T's `attDisplaced` (`attScalar` + optional memory) already meant, so the two dialects now agree on the shape rather than one having a private hole. `decodeScalar` then reads it with the same token-join it uses for every other scalar, and `armNegNumber` under this alt keeps `.quad -1` signed through the SAME descent that fixed D-ASM-NEGATIVE-SCALAR-LOSES-ITS-SIGN.\n⚠ ONE CONSEQUENCE ACCEPTED KNOWINGLY: `armMemory` uses this rule for its BASE, so `[58]` now PARSES. It does not assemble — `decodeMemory` refuses a base that names no register, loudly — and gas rejects it too, so the refusal moves from the parser to the consumer without becoming silent. Splitting a strict `armMemBase` off would buy a marginally earlier diagnostic and a second rule to keep in sync.",
      "alt": [ "Identifier", "IntLiteral", "armNegNumber" ]
    },

    "armImmediate": {
      "$comment": "`#` + a scalar — `#100`, `#-8`, `#0x1f`, `#some_symbol`. gas also accepts a constant EXPRESSION here (`#(1<<3)`); this dialect models a single scalar and fails loud on anything else rather than accepting an expression it would then have to fold with semantics nobody declared. The AT&T dialect draws the same line at `$`, which is (small) evidence that where a dialect stops is a property of the shared design rather than of either CPU.",
      "sequence": [ "ImmediateSigil", "armScalar" ]
    },

    "armScalar": {
      "$comment": "The value inside an immediate: a number, a negated number, or a symbol name.",
      "alt": [ "IntLiteral", "armNegNumber", "Identifier" ]
    },

    "armNegNumber": {
      "$comment": "`-8` — the ordinary negative displacement, as in `[x29, #-8]`. A separate rule (rather than a signed literal in `numberStyle`) because the sign is GRAMMAR: `-` is also a binary operator in gas expressions, and a signed-literal token would make `8-4` lex as two numbers.",
      "sequence": [ "MinusSign", "IntLiteral" ]
    },

    "armMemory": {
      "$comment": "★★ `[base]` / `[base, #off]` — AND THIS IS THE OTHER AXIS A SHARED TOKEN TABLE COULD NOT HAVE PARAMETERISED. AT&T writes the displacement OUTSIDE and the base inside parens (`-8(%rbp)`); arm64 writes both inside brackets with the displacement SECOND (`[x29, #-8]`). Different opening token, different nesting, different ORDER — three independent facts, not one knob.\n★ The comma inside the brackets can never be confused with the operand-list separator: the choice at this position is between `OperandComma` (an offset follows) and `BracketClose` (the operand ends), which are distinct kinds, and the operand list's own `{repeat}` only resumes after this rule completes. Same argument the AT&T `attMemory` makes about its parens, which is a sign the shape is right rather than a coincidence.\n⚠ NO INDEX-REGISTER FORM (`[x0, x1, lsl #3]`) AND NO WRITEBACK (`[sp, #-16]!`): both are real gas and both are UNDECLARED, so they fail loud rather than parsing into something smaller than what was written. Anchored under D-ASM-ARM64-GAS-SURFACE-INCOMPLETE.",
      "sequence": [ "BracketOpen", "armName", { "optional": "armMemOffset" }, "BracketClose" ]
    },

    "armMemOffset": {
      "$comment": "`, #off` inside a memory operand. Named rather than inlined so the consumer answers 'was there a displacement?' by RuleId — the same node-evidence discipline the shared grammar's fused section arms are built on.",
      "sequence": [ "OperandComma", "armImmediate" ]
    }
  },

  "pipelineEntry": {
    "$comment": "★★★ A `.s` TRANSLATION UNIT ENTERS AT THE ASSEMBLER'S INPUT. `encode` is the fourth tier (operator decision, plan 29 §5) and means post-regalloc, post-two-address-legalize, post-calling-convention. ✔MEASURED in `src/program/compile_pipeline.cpp`: a `.s` must enter after THREE passes and not one — `allocateRegisters` would reassign the programmer's registers, `legalizeTwoAddress` would rewrite the instruction forms, and `materializeCallingConvention` would inject a prologue/epilogue OVER the one the programmer wrote. Entering at `lir` silently reframes every hand-written function.\n★ AND THERE IS NO `optimize: false` SIBLING: entering at `encode` means no MIR module exists, so MIR passes cannot run BY CONSTRUCTION. 'Hand-written assembly is not optimized' is a consequence of the tier, never a second switch that could drift from it.\n★ THE ROW IS DECLARED HERE, IN THE DIALECT, NOT IN `asm.lang.json` — and that placement is load-bearing rather than tidy. A `byRule` row is matched by RULE NAME and every language's root is named `root`, so a `{\"rule\": \"root\"}` row in the shared core would resolve against a HOST language's translation unit and route every C file straight to the assembler, past HIR, MIR and the optimizer: a silent miscompile of an entire host language produced by a config that never mentions C.",
    "byRule": [
      { "rule": "root", "tier": "encode" }
    ]
  },

  "assembly": {
    "$comment": "★★★ THE TEXT→LIR CONTRACT: the rule landmarks the lowering reads, the ONE dialect-wide operand-order fact, and the spelling vocabulary. Everything here is a NAME the engine resolves — no behaviour, no branch.\n\n★★★ `operandOrder` IS `destinationFirst`, AND IT IS THE CLEANEST SINGLE-FACT CONFIRMATION THAT PUTTING IT AT DIALECT SCOPE WAS RIGHT. The reverted `asmSyntax` facet declared a destination flag PER INSTRUCTION on the theory that stores differ; ✔MEASURED false for AT&T, and measured false again here — arm64 is uniformly destination-FIRST including `str x0, [x1, #16]`, where the register written to MEMORY is still written first. Two dialects, both internally uniform, opposite values: exactly one fact, exactly one place to state it.\n★★ AND THE TWO DIALECTS DIFFER IN A SECOND WAY THIS KEY DOES *NOT* HAVE TO CAPTURE, WHICH IS THE POINT OF DERIVING RATHER THAN DECLARING. AT&T's `subq %rcx, %rax` is TWO-ADDRESS (`rax = rax - rcx`); arm64's `sub x0, x1, x2` is THREE-ADDRESS (`x0 = x1 - x2`) and the destination is not an input at all. Neither document says so: `.target.json` already states `requires2Address` per opcode (true on x86_64's `sub`, absent on arm64's), and the lowering derives the operand mapping from that plus `result` plus this one order flag. A per-dialect two-address knob would have been a second source of truth for something the target already owns, and the two would have drifted.\n\n★ `instructions[]` maps an ASSEMBLY SPELLING to TARGET OPCODE NAMES plus the operand WIDTH. ✔MEASURED and this is why the table exists at all: `opcodes[].mnemonic` is NOT assembly — the 80 arm64 entries are DSS's virtual-ISA names (`shr_l`, `jcc`, `store_outgoing_arg`, `lea_extern_got`), while a real `.s` writes `lsr`/`b.eq`/`str`/`adrp`. Several overlap by coincidence (`mov`, `add`, `sub`, `mul`, `ret`, `cmp`, `neg`, `sdiv`, `udiv`, `msub`), which is exactly what would have made reusing them LOOK like it worked — and the coincidence is WIDER here than on x86_64, so the trap is worse: `and` is a real arm64 mnemonic AND a real DSS opcode name, while `orr`/`eor`/`mvn`/`lsl`/`lsr`/`asr`/`ldr`/`str`/`bl`/`b` are all real arm64 mnemonics whose DSS names are something else entirely. A table that happened to be right for ten rows and wrong for ten more is the failure mode this mapping prevents.\n\n★★ `opcodes` IS AN ARRAY, AND arm64's ROWS ARE MOSTLY SINGLETONS — WHICH IS EVIDENCE, NOT A WASTED FACILITY. The array exists because AT&T overloads ONE spelling across THREE DSS opcodes: `movq` is `mov` (`movq %rcx, %rax`), `load` (`movq (%rdi), %rax`) and `store` (`movq %rax, (%rdi)`), elected by the target's own `encoding.variants[].guard.operandKinds`. arm64 does NOT overload: `mov`, `ldr` and `str` are three spellings, so three rows of one opcode each. ⇒ the overloading is a DIALECT property, which is a second independent argument for the array living in the dialect document rather than in the target.\n\n⚠ `width` IS DECLARED 64 ON EVERY ROW, AND THAT IS AN HONEST DEFAULT RATHER THAN A COMPLETE ANSWER. On AT&T the width is in the MNEMONIC SUFFIX (`movq`→64, `movl`→32), so a per-spelling constant is exactly right. On arm64 the width is in the REGISTER NAME — `add x0, x1, x2` is 64-bit and `add w0, w1, w2` is 32-bit with the SAME mnemonic — so a per-spelling constant cannot express the 32-bit form, and every row here therefore describes the x-register form only. ⇒ the moment `w0`…`w30` exist in the target, a `w`-register operand must either DRIVE the width or FAIL LOUD; silently encoding a 64-bit ADD for `add w0, w1, w2` would be a miscompile with no diagnostic. Anchored: D-ASM-DIALECT-WIDTH-SOURCE-IS-DIALECT-DEPENDENT.",

    "unitRule":        "root",
    "lineRule":        "asmLine",
    "elementRule":     "asmElement",
    "directiveRule":   "asmDirective",
    "statementRule":   "asmStatement",
    "labelTailRule":   "asmLabelTail",
    "operandSeqRule":  "asmOperandSeq",

    "$templateSurfaceComment": "★★★ THE FIVE KEYS THAT MAKE AN EMBEDDED `__asm__` TEMPLATE READABLE BY THIS DIALECT (2026-08-15, D-ASM-DIALECT-DECLARES-NO-OPERAND-PLACEHOLDER; third key added P20, fourth and fifth — `templateModifierRule` + `templateModifiers`, the WIDTH-VIEW surface — added P30). OPTIONAL AS A SET, REQUIRED OF EACH OTHER — the loader refuses any proper subset, because a mode with no rule mints placeholder tokens no shape accepts, a rule with no mode declares a shape whose FIRST token nothing ever produces, and a surface with no LABEL rule cannot tell an `asm goto` target from an operand; all three are silent no-ops.\n★ `templateLabelRule` names the LABEL alternative of the placeholder rule, which is how the engine asks `resolveBranchTarget` rather than `resolveRegister` without ever testing the `%l` bytes in C++.\n⚠⚠ THE SET IS MANDATORY FOR THIS DOCUMENT RATHER THAN OPTIONAL — a consequence of `D-CONFIG-ASM-TEMPLATE-CAPABILITY-NO-LONGER-OPTIONAL-FOR-A-SHARED-SURFACE-IMPORTER`, not a new policy: `asm.lang.json`'s `asmLine` closure SPELLS the template holes. The argument lives once, in `assembly_config.hpp`'s `templateLabelRule` docblock.\n★ ON THIS TARGET THE SET IS WHAT MAKES AN ASM OUTPUT POSSIBLE AT ALL — `arm64.target.json` declares the constraint letters `r`/`w`/`m`/`i` and none of them PINS a register, so an output has nowhere to be captured from except the placeholder's own vreg. That is not a gap in the target description: AArch64 genuinely has no register-pinning constraint letter, which is why gcc's own `hwtime.h` writes `mrs %0, cntvct_el0` here and `\"=a\"`/`\"=d\"` on x86.",
    "templateLexerMode":   "asm-template",
    "templateOperandRule": "asmTemplateOperand",
    "templateLabelRule":   "asmTemplateLabelRef",

    "$templateModifiersComment": "★★★ THE WIDTH-VIEW MODIFIER LETTERS OF **THIS** DIALECT, AND THE ONE FACT THAT MAKES THEM DIALECT VOCABULARY RATHER THAN LANGUAGE VOCABULARY: `%w` IS THIRTY-TWO BITS HERE AND SIXTEEN BITS IN `asm-x86_64-att.lang.json`. ✔MEASURED 2026-08-24 by execution on gcc 13.3.0 and clang 19.1.1, at `-O0` and `-O2`, each port fed the same source: `aarch64-linux-gnu-gcc` renders `%w0` as `w0` and `%x0` as `x0`; x86-64 gcc renders `%w0` as `%ax`. ⇒ any single table of letters written in `asm.lang.json` would be wrong for one of its two readers, which is why the SIGIL byte stays language vocabulary and the LETTER ALPHABET is declared here. The loader composes one template-mode lexeme per row out of the two (`%` + `w`), so nothing in this file spells a percent either.\n\n★★ `widthBits` IS THE UNIT THE PIPELINE ALREADY CARRIES — `AsmResolvedRegister::widthBits`, `AsmOperandBinding::widthBits` and `lirInstWidthBits` are the same number — so a modified placeholder states a width the encoders already read, and `%w0` reaches the very path a `.s`-written `mov w0, w1` proves. No `%w`-shaped concept exists anywhere below this row.\n\n★★★ ALL SEVEN LETTERS gcc's AArch64 PORT DOCUMENTS ARE DECLARED, AND EVERY ROW IS **CLASS-SCOPED** — P50, R5+R8 of the operator's design A′ (D-ASM-AARCH64-FP-BARE-OPERAND-WIDTH-DIVERGES-FROM-REFERENCE). ✔MEASURED 2026-09-01, gcc 13.3.0 AND clang 18.1.3 probed SEPARATELY (clang under `-fno-integrated-as`, its integrated assembler rejecting the synthetic probe mnemonic being the instrument and not the finding), `-O0` AND `-O2`, substituted text read out of `-S`: `%b0`/`%h0`/`%s0`/`%d0`/`%q0` on a `\"w\"`-bound `double` render `b0`/`h0`/`s0`/`d0`/`q0` — the five scalar SIMD&FP views `arm64.target.json` declares (`b/h/s/d` as `subOf` rows of the `v` roots; `q` the root's own 128-bit width, spelled scalar). ⚠⚠ THE SENTENCE THIS PARAGRAPH REPLACES SAID THE FIVE FP LETTERS \"RENDER EMPTY\" ON AN `\"r\"` OPERAND AND WAS **WRONG FOR THIS PORT**: re-measured, an FP view letter on an `\"r\"`-bound integer is a HARD ERROR under BOTH references (gcc: *invalid 'asm': incompatible floating point / vector register operand for '%d'*; clang: *invalid operand in inline asm*), and the same error under both on an `\"m\"`-bound operand — a UNANIMOUS refusal, which is exactly what `registerClass` exists to adopt: a wrong-class letter is refused BY NAME, listing the letters the operand's class does declare.\n\n★★★ AND THE REVERSE DIRECTION IS THE OPERATOR'S R8 RULING, NOT A MEASUREMENT GAP. ✔MEASURED in the same run: `%w0`/`%x0` on a `\"w\"`-bound `double` — gcc renders `v0`, clang renders `d0`, both rc=0, no diagnostic from either. Two references, one program, two DIFFERENT registers: the operator ruled the construct DISSOLVED rather than sided with — `w`/`x` are GPR letters, class-scoped letters make them illegal on an FP-class operand, and the divergence disappears along with the program that exhibited it. ⓘ The check runs on the BINDING's class, so an `\"i\"`-bound immediate — where ✔MEASURED the references SPLIT on acceptance (gcc refuses `%d0` on `\"i\"(7)`, clang renders `7`) — skips it and the letter dies, the accepted side of the disjunction.\n\n⚠⚠ `q` IS 128 BITS, AND THE SENTENCE HERE THAT CALLED THAT WIDTH **DECLARED-NOT-CONSUMED** WENT FALSE ON 2026-09-02 — kept in past tense because its PREDICTION came true exactly as written. It read: *`kLirInstFlagWidth128` exists and `lirInstWidthBits` decodes it, but no shipped target declares a width-128 encoding variant, so a `%q0` inside a real instruction is refused loud at the template translator's width-flag map … The letter exists so the day an FP instruction lands in this document, `%q0` starts electing with zero grammar or C++ changes.* ✔MEASURED at the P54 base: that was true. `arm64.target.json` then grew width-128 arms on `popcount_bytes` and `addlanes_bytes` (CNT/ADDV over SIXTEEN byte lanes, byte-verified against gas 2.42 and clang 18.1.3), the template translator's `case 128:` landed in the same change — as its own comment said it would, *in the same change as the first opcode that consumes it* — and a 128-bit template operand now ELECTS. ⓘ The one clause that did NOT come true is the *zero grammar changes* half: the 128-bit forms this target encodes are SIMD, and a SIMD operand is written with a lane ARRANGEMENT (`%0.16b`) rather than a view letter, which needed both grammar and a vocabulary (see `registerArrangements`). ⚠⚠ AND THE CLAUSE THAT FOLLOWED — *`%q0` itself still elects nothing, because every FP mnemonic this dialect declares is scalar or lane-arranged and none takes a `q` operand* — WENT FALSE LATER THE SAME DAY, AND IT WAS NEVER A PROPERTY THE ENGINE COULD HOLD. `%q0` DOES elect now: lane `av` declared the SIMD register move (`move_bytes`, D-ASM-ARM64-GAS-SPELLS-NO-ROUNDING-MODE-OR-VECTOR-MOVE), so `mov` has a width-128 fpr variant — and an ARRANGEMENT is applied as a WIDTH and a CLASS with the suffix then discarded, so `%0.16b` and `%q0` reach election as the SAME query. ✔MEASURED, gas 2.42 and clang 18.1.3 BOTH REJECT `mov q0, q1`, so this was DSS accepting above the union. ⚠⚠ AND THE SENTENCE BEFORE THAT ONE — *No variant can be reachable from one and not the other* — WENT FALSE ON 2026-09-02 (cycle P54, lane `ae`), WHICH IS THE THIRD TIME THIS PARAGRAPH HAS OUTLIVED ITS CODE IN ONE DAY. An arrangement is no longer applied as a width and discarded: it carries its LANE WIDTH into election, and `arm64.target.json` states per FIELD whether that field reads its register as a vector of lanes (`wires[].lanes`/`laneBits`, `destLanes`/`destLaneBits`). `%q0` states NO arrangement, `move_bytes` reads lanes on every field, and `mov %q0, %q1` is refused here exactly as both references refuse it — while `%0.16b` still elects. [[D-ASM-ARRANGEMENT-ERASED-TO-A-WIDTH-BEFORE-ELECTION]] is CLOSED. ⓘ What the LETTER itself still does is unchanged and is now measured on an instruction that really is scalar at 128: `ldr %q0, [x1, #16]` elects `fldr_u`'s Q arm and emits 0x3DC00420, the word both references give. ⚠ Also measured, and it refutes the obvious 'clamp to a declared minimum' scheme outright: an `__int128` on `\"r\"` renders `x0` here and `%rax` on x86-64, so NEITHER reference widens past 64.\n\n⚠ THE SCOPED/UNSCOPED POSTURE IS PER DIALECT AND THE LOADER REFUSES A MIX: this document scopes every letter; `asm-x86_64-att.lang.json` scopes none, because ✔MEASURED x86-64 gcc ACCEPTS a GPR letter on an `\"x\"`-bound xmm operand and renders the bare `%xmm0` (clang refuses; acceptance is decided by the disjunction). One mechanism, two measured postures, each declared where it is true.\n\n⚠ `l` MAY NEVER BE DECLARED HERE: `%l` is the `asm goto` label sigil the LANGUAGE owns, so the composed lexeme would collide. The loader refuses that collision by name rather than letting maximal munch decide it silently.",
    "templateModifierRule": "asmTemplateModifiedRef",
    "templateModifiers": [
      { "letter": "w", "widthBits": 32,  "registerClass": "gpr" },
      { "letter": "x", "widthBits": 64,  "registerClass": "gpr" },
      { "letter": "b", "widthBits": 8,   "registerClass": "fpr" },
      { "letter": "h", "widthBits": 16,  "registerClass": "fpr" },
      { "letter": "s", "widthBits": 32,  "registerClass": "fpr" },
      { "letter": "d", "widthBits": 64,  "registerClass": "fpr" },
      { "letter": "q", "widthBits": 128, "registerClass": "fpr" }
    ],

    "$registerArrangementsComment": "★★★ THE LANE ARRANGEMENTS OF THIS DIALECT — the last gap of D-ASM-DIALECTS-DECLARE-A-REGISTER-CLASS-NO-INSTRUCTION-CAN-NAME, closed 2026-09-02 (cycle P54, lane el). `v1.8b` is the register `v1` read as eight byte lanes, which is SIXTY-FOUR BITS; `v1.16b` is the same register as sixteen lanes, 128. That is the identical fact the `templateModifiers` letters above state — a WIDTH VIEW of a register — written on the other side of the name, which is why it lands in the same field (`AsmResolvedRegister::widthBits`) through the same override and needs no new concept below this document.\n★★★ AND A ROW CARRIES A **LANE WIDTH**, WHICH IS THE FOURTH FIELD AND THE ONE THE PARAGRAPH ABOVE IS MISSING — [[D-ASM-ARRANGEMENT-ERASED-TO-A-WIDTH-BEFORE-ELECTION]], closed 2026-09-02 (cycle P54, lane ae). `.8b`, `.4h`, `.2s` and `.1d` are ALL SIXTY-FOUR BITS and are four different operand shapes, and `v1.8b` differs from `d1` in a way no width can state at all. With only a width and a class, an arrangement was applied and then DISCARDED, so every one of those five spellings reached election as ONE query. ✔MEASURED at the P54 base, both directions and both surfaces, gas 2.42 and clang 18.1.3 REJECTING every one: `cnt d0, d1` compiled and emitted `cnt v0.8b, v1.8b`; `mov d0, d1` emitted `mov v0.8b, v1.8b`; `mov v0, v1` emitted `mov v0.16b, v1.16b`; and — the direction nobody had measured — `fadd v0.8b, v1.8b, v2.8b` compiled and emitted **`fadd d0, d1, d2` (0x1E622820)**, a SCALAR DOUBLE ADD for a byte-lane spelling, with `fmul`/`fsqrt`/`fmov`/`fneg`/`fcmp`/`fabs` behaving the same way. That second direction is a silent WRONG ANSWER rather than an over-acceptance. The lane width now survives into `asm_elect::variantAcceptsRegisterProfile`, where the target's own `wires[].lanes`/`laneBits` and `destLanes`/`destLaneBits` say how each field reads its register.\n★★ EIGHT ARRANGEMENTS ARE DECLARED, AND THE LINE IS \"WHAT A DECLARED OPCODE CAN NAME\" — THE SAME LINE AS BEFORE, DRAWN AGAINST A MEASUREMENT THAT MOVED. The sentence this replaces said `.4h`, `.2s` and `.2d` were *deliberately ABSENT* because no opcode could consume them; that was TRUE for `cnt`/`addv` and FALSE for the vector `mov` that landed hours later in the same cycle. ✔MEASURED, and it is the one gas-vs-clang disagreement in this family: clang 18.1.3 assembles `mov v0.8b/.4h/.2s/.1d` to the SINGLE word 0x0EA11C20 and `mov v0.16b/.8h/.4s/.2d` to 0x4EA11C20, while gas 2.42 refuses everything but `.8b` and `.16b`. The ORR alias `move_bytes` encodes is BITWISE, so its lane size reaches no bit — which is why the target declares `lanes` there with NO `laneBits`, and why one working reference makes all eight required.\n⚠ THE NARROWER REFUSAL THE OLD PARAGRAPH PREFERRED IS NOT LOST, IT MOVED ONE TIER. `cnt v0.4h, v1.4h` now parses and is refused at ELECTION, naming the candidate opcode, because `popcount_bytes` declares `laneBits: 8` on both ends — and ✔MEASURED, gas 2.42 and clang 18.1.3 BOTH REFUSE it too (CNT is byte-only), so the verdict is unchanged and only the message differs. ⓘ `addv h0, v1.4h` = 0x0E71B820 and `fadd v0.2s, v1.2s, v2.2s` = 0x0E22D420 are accepted by both references and stay REFUSED here, loudly: they need target OPCODES this document cannot mint (a halfword ADDV, a vector FADD), which is a capability gap rather than a vocabulary one.\n⚠ THE SUFFIX INCLUDES ITS DOT. The engine never spells a separator byte of its own; a dialect whose arrangement is written some other way declares that other way whole.\n⚠⚠ TWO LEXICAL SURFACES, ONE TABLE, AND THE ASYMMETRY IS MEASURED RATHER THAN CHOSEN: because `identifierClass.extraContinue` declares `.`, a `.s`-written `v1.8b` is ONE `Identifier` token and the dialect's own `armName` already accepts it — while a template's `%0.16b` is FIVE tokens, since an identifier cannot START on a digit. The shared core's `asmTemplateArrangement` exists for the second surface only; both reduce to a concatenated spelling ending in one of the suffixes below.",
    "registerArrangements": [
      { "suffix": ".8b",  "widthBits": 64,  "laneBits": 8,  "registerClass": "fpr" },
      { "suffix": ".4h",  "widthBits": 64,  "laneBits": 16, "registerClass": "fpr" },
      { "suffix": ".2s",  "widthBits": 64,  "laneBits": 32, "registerClass": "fpr" },
      { "suffix": ".1d",  "widthBits": 64,  "laneBits": 64, "registerClass": "fpr" },
      { "suffix": ".16b", "widthBits": 128, "laneBits": 8,  "registerClass": "fpr" },
      { "suffix": ".8h",  "widthBits": 128, "laneBits": 16, "registerClass": "fpr" },
      { "suffix": ".4s",  "widthBits": 128, "laneBits": 32, "registerClass": "fpr" },
      { "suffix": ".2d",  "widthBits": 128, "laneBits": 64, "registerClass": "fpr" }
    ],

    "operandOrder": "destinationFirst",

    "$spellingCaseComment": "★★★ gas IS CASE-INSENSITIVE ON EVERY SPELLING IT DECLARES, AND DSS WAS NOT — D-ASM-DIALECT-MNEMONIC-MATCH-IS-CASE-SENSITIVE. ✔MEASURED 2026-08-15, `aarch64-linux-gnu-as` (GNU Binutils for Ubuntu) 2.42, rc plus the encoded word read back with `aarch64-linux-gnu-objdump -d`: `mov x0,x1` / `MOV X0,X1` / `MoV x0,X1` all encode `aa0103e0`; `mov w0,w1` and `MOV W0,W1` both encode `2a0103e0`; `add x0,x1,x2` and `ADD X0,X1,X2` both `8b020020`; and the two SELECTOR-carrying spellings this dialect declares fold too — `mrs x0, cntvct_el0` and `MRS X0, CNTVCT_EL0` both `d53be040`, `cset x0, eq` and `CSET X0, EQ` both `9a9f17e0`. Directives likewise: `.TEXT`, `.GLOBL`, `.TYPE`, `.SECTION`, `.DATA`, `.XWORD` all rc=0. Refusing what the reference assembler accepts is a defect under [[feedback_reference_compilers_are_the_spec]], so this dialect folds — and it folds ALL FOUR surfaces (mnemonic, register, operand selector, directive) or none: a fold that stopped at the mnemonic would still refuse `MOV X0, X1` at the register, which hides the gap instead of closing it.\n\n★★ THE SELECTOR HALF IS WHY THE KEY IS ON THE DIALECT AND NOT ON THE TARGET. `cntvct_el0` and `eq` are spellings THIS dialect declares in `instructions[].operandSelectors`; `x0` is a spelling the TARGET declares. One key folds both because both are DECLARED VOCABULARY being matched against written text — and `TargetSchema::registerByName` still compares exactly, so nothing about `arm64.target.json` changes and a future dialect for this same CPU may choose the other policy.\n\n⛔ WHAT THIS KEY DOES **NOT** REACH, MEASURED IN THE SAME RUN: (1) SYMBOL/LABEL names — `foo:` and `FOO:` are two defined symbols in one object and `b FOO` against `foo:` leaves `FOO` `*UND*`; (2) the `.type` MARKER — `.type main, %FUNCTION` and `%Function` are BOTH rc=1 *\"unrecognized symbol type\"* while `.TYPE` itself is accepted; (3) the `.section` NAME operand — `.section .RODATA` opens a section literally called `.RODATA`. Folding any of the three would merge two things gas keeps apart, which is a miscompile and not a conformance fix.",
    "spellingCase": "asciiFolded",

    "operandForms": {
      "$comment": "Which of THIS document's rules realizes each operand ROLE the lowering understands. The engine walks ROLES, never rule names — which is what let a dialect whose memory form is `[base, #off]` reuse the lowering unchanged, exactly as the AT&T document predicted it would.\n★★★ TWO ROLES SHARE ONE RULE, AND THAT SHARING IS A DECLARATION RATHER THAN AN ACCIDENT: `register` and `displaced` both name `armName`, which states 'a bare name is either a register or a symbol and only the target can say which'. See `armName` for why no grammar can split them and why declaring the register file here is refused.\n⚠ `indirect` ALSO NAMES `armName`, AND THAT ONE IS A CONTRACT ARTEFACT WORTH NAMING. arm64 has NO indirect-operand marker at all: AT&T writes `jmp *%rax` and puts the indirectness in the OPERAND, arm64 writes `br x0` and puts it in the MNEMONIC (`b` vs `br`, two `instructions[]` rows mapping to `jmp` and `jmp_indirect`). So the honest declaration is 'this dialect has no such form' — but `operandForms` REQUIRES all seven roles, correctly, so that a partial block cannot silently mean 'this operand shape is unrecognized'. It is therefore declared `null` — an EXPLICIT absent, which the loader distinguishes from an omission (an omitted role is still a load error, so a partial block can never silently mean 'this operand shape is unrecognized'). ★ ALIASING IT ONTO `armName` WAS THE FIRST ATTEMPT AND IT WAS WRONG TWICE OVER, which is why the wrong version is recorded here rather than just deleted: it put THREE roles on one rule, tripping the undecidable-pair check (`displaced` and `indirect` are both non-register, so no target lookup can separate them), and it asserted a form this dialect does not have. The role set really was AT&T's operand taxonomy generalized once — that finding was real; the fix was an absent-declaration in the shared loader rather than a per-dialect workaround. Closed: D-ASM-DIALECT-ABSENT-OPERAND-ROLE.",
      "register":   "armName",
      "immediate":  "armImmediate",
      "memory":     "armMemory",
      "displaced":  "armName",
      "indirect":   null,
      "scalar":     "armScalar",
      "negNumber":  "armNegNumber"
    },

    "entryLabels": ["main"],

    "instructions": [
      { "$comment": "★ DATA MOVEMENT. The INTEGER `mov`'s two target variants (`[\"reg\"]` and `[\"imm32\"]`) are variants of the SAME opcode, so the target's own guard elects between `mov x0, x1` and `mov x0, #100` — no dialect involvement, which is the division of labour this table is supposed to have. ⚠ THE SENTENCE HERE THAT CALLED THIS ROW *a singleton* AND `mov` *the one spelling* WHOSE VARIANTS SHARE AN OPCODE WENT FALSE ON 2026-09-02 (cycle P54, lane av): the row now names `move_bytes` as well, because AArch64's SIMD register move is spelled `mov` too and is a DIFFERENT INSTRUCTION — `mov Vd.16b, Vn.16b` is an alias for `orr Vd.16b, Vn.16b, Vn.16b` (✔MEASURED, gas 2.42 and clang 18.1.3 both give 0x4EA11C20 for either spelling). The two opcodes separate by REGISTER CLASS, which is the axis lane `el` added to `electOpcode` in the same cycle: `mov x0, x1` is gpr and elects `mov`, `mov %0.16b, %1.16b` is fpr and elects `move_bytes`. ⓘ The division of labour is unchanged — the DIALECT still states only which opcodes a spelling may denote, and the TARGET still decides which one the operands elect. ★★ NO `width` KEY, AND ITS ABSENCE IS LOAD-BEARING (D-ASM-ARM64-GAS-MOV-ROW-PINS-WIDTH-64, 2026-08-13). This row USED to declare `width: 64`, and `effectiveWidth` reconciles a row-declared width against the width DERIVED from the register operands — so `mov w0, w1` was REFUSED outright (\"the spelling and the registers disagree\"). That is the whole point of the aarch64 model: the REGISTER SPELLING carries the width here, unlike AT&T x86 where the mnemonic SUFFIX does (`movl` vs `movq`) — D-ASM-DIALECT-WIDTH-SOURCE-IS-DIALECT-DEPENDENT. ⚠ THE FINDING THAT MATTERS: the target's W-form ORR variant was added the PREVIOUS cycle specifically so a hand-written `.s` could write `mov w0, w1` — and a `.s` could not write it, because of this key. Neither half was wrong on its own; they were never connected. Every row below whose target opcode declares BOTH an explicit width:32 and an explicit width:64 variant has had the key dropped for the same reason; the rows that KEEP it are the ones whose target opcode has only width-ABSENT variants, where dropping it would let a W-register spelling silently elect a 64-bit word.",
        "spelling": "mov",  "opcodes": ["mov", "move_bytes", "sp_copy"],
        "$commentSpCopy": "★★★ `sp_copy` IS THE THIRD CANDIDATE SINCE 2026-09-03 AND IT IS A DIFFERENT INSTRUCTION, NOT A THIRD ENCODING OF THE SAME ONE ([[D-ASM-ARM64-SP-AND-XZR-SHARE-ENCODING-31-SO-MOV-SP-SILENTLY-BECOMES-ZERO]], cycle P55). AArch64 spells TWO instructions `mov`: a register copy is `ORR Xd, XZR, Xm` (the `mov` opcode above) and a copy naming the STACK POINTER is `ADD <Xd|SP>, <Xn|SP>, #0` (ARM ARM C6.2.209, MOV (to/from SP)), which this target has always declared as `sp_copy` for the VLA frame-pointer capture. ⚠⚠ WITHOUT THIS ROW THE STACK-POINTER SPELLING SILENTLY TOOK THE ORR FORM: ✔MEASURED at the P55 base through the real CLI, `mov x0, sp` emitted 0xAA1F03E0 = `mov x0, xzr`, so x0 received ZERO, and `mov sp, x0` emitted 0xAA0003FF, a NO-OP — rc=0 and no diagnostic, because `sp` and `xzr` share hwEncoding 31. Both references emit 0x910003E0 / 0x9100001F. ★★ THE LIST IS DELIBERATELY **NOT** `opcodesAreRankedEncodings`. Ranking would return at the first match and make the answer depend on declaration order; here the choice is made by REFUSAL on both sides — `mov`'s ORR fields read encoding 31 as the zero register and so refuse `sp`, and `sp_copy` declares `requiresRegRole: \"stackPointer\"` and so refuses every line that names none. `move_bytes` is lane-declaring and refuses a scalar spelling. Exactly one candidate can match any line, and the ambiguity check stays ON to prove it." },

      { "$comment": "★ ARITHMETIC — THREE-ADDRESS, unlike AT&T's two-address forms. Nothing here says so: `.target.json` omits `requires2Address` on these opcodes and the lowering derives the shape. `add x0, x1, x2` therefore produces result=x0, operands=[x1, x2], where `addq %rcx, %rax` produces result=rax, operands=[rax, rcx].",
        "spelling": "add",  "opcodes": ["add", "add_ext_reg"],
        "$commentExtReg": "★★ `add_ext_reg` IS THE SECOND CANDIDATE SINCE 2026-09-03 ([[D-ASM-ARM64-SP-AND-XZR-SHARE-ENCODING-31-SO-MOV-SP-SILENTLY-BECOMES-ZERO]]): AArch64's shifted-register ADD cannot name the stack pointer at all, so `add x0, sp, x1` is the EXTENDED-register encoding — a different instruction under one spelling, exactly as `mov` above. ⚠ THE LIST IS NOT RANKED: `add` refuses `sp` (its fields read encoding 31 as the zero register) and `add_ext_reg` declares `requiresRegRole`, so at most one candidate matches any line and the ambiguity check stays on." },
      { "spelling": "sub",  "opcodes": ["sub", "sub_ext_reg"] },
      { "spelling": "mul",  "opcodes": ["mul"] },
      { "spelling": "sdiv", "opcodes": ["sdiv"] },
      { "spelling": "udiv", "opcodes": ["udiv"] },
      { "spelling": "msub", "opcodes": ["msub"] },
      { "spelling": "neg",  "opcodes": ["neg"] },

      { "$comment": "★ BITWISE — AND THE CLEAREST STRETCH OF THE TABLE WHERE THE SPELLINGS AND THE DSS OPCODE NAMES DIVERGE. gas writes `orr`/`eor`/`mvn`; DSS's virtual ISA calls them `or`/`xor`/`not`. Only `and` coincides, which is precisely the coincidence that makes reusing opcode mnemonics as spellings look workable until it silently is not.\n★★★ THE `width: 64` PINS ARE GONE FROM `and`/`orr`/`eor` — D-ASM-ARM64-BITWISE-W-FORMS-UNDECLARED, closed 2026-08-13, and the ROW ABOVE IS NOW TRUE OF ALL FOUR SPELLINGS RATHER THAN ONE. The pins existed because the target declared `and`/`or`/`xor` with a SINGLE width-ABSENT variant each, and a width-absent variant matches ANY width — so dropping a pin before the target grew its W-form would have let `and w0, w1, w2` elect the 64-bit word. The target now declares AND/ORR/EOR at BOTH widths (0x0A000000 / 0x2A000000 / 0x4A000000 beside 0x8A000000 / 0xAA000000 / 0xCA000000, every one ✔MEASURED against `aarch64-linux-gnu-as` 2.42), so the width comes from the REGISTER SPELLING exactly as `mvn`/`lsl`/`lsr`/`asr` already take it — D-ASM-DIALECT-WIDTH-SOURCE-IS-DIALECT-DEPENDENT, and the same reasoning that removed the `mov` row's pin.\n⚠ WHAT THE PIN WAS ACTUALLY COSTING, stated because it is NOT what the anchor's title says: with `width: 64` declared, `effectiveWidth` reconciled the row against the register-derived width and REFUSED `and w0, w1, w2` outright (\"the spelling and the registers disagree\") — a fail-loud, not a miscompile. The silent half of this defect was never in the dialect at all; it was in the C path, where a 32-bit `int` AND elected the width-absent X-form and emitted a 64-bit instruction. Removing these three pins is what lets a `.s` write the W-form; declaring the target variants is what fixed the C path.",
        "spelling": "and",  "opcodes": ["and"]  },
      { "spelling": "orr",  "opcodes": ["or"]   },
      { "spelling": "eor",  "opcodes": ["xor"]  },
      { "spelling": "mvn",  "opcodes": ["not"] },
      { "spelling": "lsl",  "opcodes": ["shl"] },
      { "spelling": "lsr",  "opcodes": ["shr_l"] },
      { "spelling": "asr",  "opcodes": ["shr_a"] },

      { "$comment": "★ MEMORY. `ldr`/`str` are the SCALED-offset forms and `ldur`/`stur` the UNSCALED ones — four spellings for four DSS opcodes, one each. ⚠ THIS IS WHY THEY ARE NOT ONE ROW WITH `[\"load\", \"load_u\"]`: those two opcodes' variant guards are BYTE-IDENTICAL (`[\"reg\", \"membase\", \"memoffset\"]` at each of widths 64/32/16/8), so `guard.operandKinds` election could not choose between them — it would take the first and silently encode LDR where the programmer wrote LDUR. gas distinguishes them by spelling, so the dialect does too, and the election facility is left to the case that actually needs it (AT&T's `movq`).\n★★★ THESE FOUR ROWS WERE INVERTED WHEN THIS DIALECT SHIPPED (`e5b60f6c`) AND ARE CORRECTED HERE — D-ASM-ARM64-DIALECT-INVERTS-LDR-LDUR-MAPPING. The trap is the `_u` SUFFIX, and it is worth naming because the comment above was RIGHT about gas while the rows below contradicted it. `_u` in DSS's opcode names abbreviates **UNSIGNED-offset** (the scaled `[Xn, #pimm]` form), NOT **unscaled** — read as 'unscaled' it points at exactly the opposite instruction, which is what happened. ✔MEASURED against `arm64.target.json`'s own encodings, which are unambiguous:\n  • DSS `load`  → fixedWord 0xF8400000, wire `imm9`         = **LDUR** Xt,[Xn,#simm9] — unscaled, SIGNED 9-bit;\n  • DSS `store` → fixedWord 0xF8000000, wire `imm9`         = **STUR**;\n  • DSS `load_u`→ fixedWord 0xF9400000, wire `imm12.scaled` = **LDR**  Xt,[Xn,#pimm]  — scaled, UNSIGNED 12-bit;\n  • DSS `store_u`→fixedWord 0xF9000000, wire `imm12.scaled` = **STR**.\nSo gas `ldr`/`str` (scaled, unsigned) are DSS `load_u`/`store_u`, and gas `ldur`/`stur` (unscaled, signed) are DSS `load`/`store`. ⚠ THE OBSERVABLE COST OF THE INVERSION WAS A FAIL-LOUD, NOT A MISCOMPILE, WHICH IS THE ONLY REASON IT SURVIVED A CYCLE: `stur x1, [x0, #-8]` elected the SCALED opcode, whose imm12 field is unsigned, and the encoder refused the negative offset (`A_ImmediateOperandOutOfRange`). The reverse direction was the dangerous one — a `ldr x0,[x0,#8]` reaching the UNSCALED encoder produced a real word with the offset read as a raw byte count rather than a scaled one, i.e. the right instruction shape at the wrong address for every offset that is not a multiple of the access size.",
        "$rankedComment": "★★★ AND SINCE 2026-09-03 `ldr`/`str` ALSO NAME THE **UNSCALED** OPCODES, RANKED BEHIND THE SCALED ONES — [[D-ASM-ARM64-LDR-TO-LDUR-CONVENIENCE-ALIAS-REFUSED]] (cycle P55, lane vd). ⚠⚠ THIS DOES NOT CONTRADICT THE PARAGRAPH ABOVE; IT NARROWS ONE SENTENCE OF IT, AND THE SENTENCE IS RESTATED RATHER THAN DELETED. That paragraph says the four spellings are separate rows because `load` and `load_u` have BYTE-IDENTICAL guards, so 'election could not choose between them — it would take the first and silently encode LDR where the programmer wrote LDUR'. The DANGER it names is real and is still fully defended: `ldur`/`stur` are STILL their own rows naming ONLY the unscaled opcodes, so an explicitly-written `ldur` can never be promoted to `ldr` (✔MEASURED that the references do not promote it either: `ldur h0,[x1,#2]` stays 0x7C402020 even though the scaled form could carry offset 2). What changed is the OTHER direction, and two facts had to arrive before it was safe. FIRST, the guards are no longer byte-identical: every scaled variant now declares `immMin`/`immMax`/`immMultipleOf` mirroring exactly what its `imm12.scaled` slot can carry, so it no longer matches an offset its own encoder would refuse. SECOND, `opcodesAreRankedEncodings` states that these candidates are ENCODINGS OF ONE OPERATION rather than rival readings of the line, so a tie is settled by rank instead of refused as ambiguous.\n★★★ WHY DSS HAD TO ADOPT THE REWRITE, AND IT IS A MEASUREMENT AND NOT A PREFERENCE. ✔MEASURED 2026-09-03, gas 2.42 and clang 18.1.3 probed SEPARATELY, agreeing on every cell, and NEITHER EMITTING A WARNING: `ldr h0,[x1,#1]` = 0x7C401020 = `ldur h0,[x1,#1]`; `ldr s0,[x1,#255]` = 0xBC4FF020; `ldr d0,[x1,#-8]` = 0xFC5F8020; `ldr b0,[x1,#-1]` = 0x3C5FF020; `ldr x0,[x1,#1]` = 0xF8401020; `str h0,[x1,#1]` = 0x7C001020. DSS refused every one of them loudly. Under the standing rule that the union is taken over what WORKS, two references producing a working binary against DSS producing none is a conformance gap, not a taste question.\n★★★ IT IS A CONVENIENCE ALIAS AND **NOT** A MEANING CHANGE, WHICH IS THE QUESTION THE ROW SAID HAD TO BE ANSWERED FIRST. ✔MEASURED: `ldr h0,[x1,#1]` and `ldur h0,[x1,#1]` name the same effective address (x1+1 — LDUR does not scale), the same access size (2 bytes), and the same destination. The two spellings differ in ENCODING, never in what the program does, so this is the same scaled-vs-unscaled ELECTION DSS already performs on its own compiler-generated path (`selectFrameMemOp` swaps an out-of-imm9 frame offset onto the scaled form, which is this same pair in the opposite direction). Adopting it makes the hand-written surface agree with the generated one rather than introducing a new kind of substitution.\n★★★ THE RANK NEVER WIDENS WHAT IS ACCEPTED, AND THE FENCE IS WHERE THE REFERENCES PUT IT. `ldur`'s reach is the UNSCALED signed imm9 (-256..255) while `ldr`'s is the scaled unsigned imm12, so an offset outside BOTH is still refused: ✔MEASURED that gas (`Error: immediate offset out of range`) and clang (`index must be an integer in range [-256, 255]`) refuse `ldr h0,[x1,#257]`, `ldr h0,[x1,#8192]`, `ldr b0,[x1,#4096]`, `ldr q0,[x1,#-264]` and `ldr b0,[x1,#-257]`, and DSS refuses all of them too. ⓘ THE PREFERENCE ORDER IS THE REFERENCES' OWN: scaled first where both encodings fit (✔MEASURED, `ldr h0,[x1,#254]` = the scaled 0x7D41FC20, not the unscaled form).\n⚠ THE ROW'S OWN PREMISE WAS **NARROWER THAN THE TRUTH** AND IS CORRECTED HERE. It framed the trigger as *an offset that is not a multiple of the access size*; ✔MEASURED, the rewrite ALSO fires for every NEGATIVE offset even when it IS a multiple (`ldr d0,[x1,#-8]`, `ldr b0,[x1,#-1]` — and at `b` width EVERY offset is a multiple of 1, so 'non-multiple' cannot be the trigger at all). The real predicate is *the SCALED form cannot represent this displacement*, which is non-negative AND a multiple AND within 4095×accessSize — exactly the three conditions the guards now declare. It is also NOT FP-only: the integer file rewrites identically (`ldr x0,[x1,#1]`, `ldr w0,[x1,#1]`, and gas's `ldrh w0,[x1,#1]` → `ldurh`).",
        "spelling": "ldr",  "opcodes": ["load_u", "fldr_u", "load", "fldur"], "opcodesAreRankedEncodings": true },
      { "$comment": "★★★ THE FP SIBLING IS A SECOND CANDIDATE ON THE **SAME** ROW, NOT A SECOND ROW — added 2026-09-02 (cycle P54, lane el, D-ASM-DIALECTS-DECLARE-A-REGISTER-CLASS-NO-INSTRUCTION-CAN-NAME). gas spells `ldr x0,[x1,#8]` and `ldr d0,[x1,#8]` with ONE mnemonic and so does this row; what separates the two DSS opcodes is the DATA register's CLASS, and until this cycle `electOpcode` could not see it.\n⚠⚠ READ THE BLOCK ABOVE AGAINST THIS ONE: it says these four spellings are separate rows because `load` and `load_u` have BYTE-IDENTICAL guards, and that is still exactly true and still the reason `ldr` and `ldur` are two rows. `load_u` and `fldr_u` are byte-identical in the SAME way — `[\"reg\", \"membase\", \"memoffset\"]` at each width — but they differ in a fact the guards DO carry and the elector had been ignoring: `load_u` writes a `gpr` result and `fldr_u` an `fpr` one (`resultRegClass`). ⇒ the class axis separates them where operand SHAPE cannot, which is the whole point of the axis, and the `ldr`/`ldur` split is untouched because no class distinguishes those two.\n✔MEASURED, gas 2.42 and clang 18.1.3 agreeing on every probe: `ldr d0,[x1,#8]` = 0xFD400420, `ldr s0,[x1,#4]` = 0xBD400420, `ldr q0,[x1,#16]` = 0x3DC00420, `ldr d3,[x7,#24]` = 0xFD400CE3 — the words `fldr_u`'s three variants already declared — against the integer control `ldr x0,[x1,#8]` = 0xF9400420, which is `load_u`'s.",
        "$rankedComment": "The STORE half of `ldr`'s `$rankedComment` — read it there; the two are one decision and the measurement covers both directions (`str h0,[x1,#1]` = 0x7C001020 = `stur h0,[x1,#1]`, `str d0,[x1,#-8]` = 0xFC1F8020, both references agreeing and neither warning).",
        "spelling": "str",  "opcodes": ["store_u", "fstr_u", "store", "fstur"], "opcodesAreRankedEncodings": true },
      { "$comment": "⚠ THE UNSCALED SPELLINGS NAME ONLY THE UNSCALED OPCODES, AND THAT ASYMMETRY IS THE POINT rather than an oversight ([[D-ASM-ARM64-LDR-TO-LDUR-CONVENIENCE-ALIAS-REFUSED]], 2026-09-03). `ldr` denotes the OPERATION and the assembler picks the encoding; `ldur` denotes the ENCODING and must always get it. ✔MEASURED that the references are asymmetric in exactly this way: `ldur h0,[x1,#2]` stays 0x7C402020 and `ldur x0,[x1,#8]` stays 0xF8408020 — a multiple that the scaled form could carry is NOT promoted, on either reference. So adding the scaled opcodes here would make DSS re-encode an instruction the programmer named explicitly, which is the substitution this project refuses; the ranked list on `ldr`/`str` is licensed precisely because those spellings name no encoding.",
        "spelling": "ldur", "opcodes": ["load", "fldur"] },
      { "spelling": "stur", "opcodes": ["store", "fstur"] },
      { "spelling": "adr",  "opcodes": ["lea"],     "width": 64 },

      { "$comment": "★ CONTROL FLOW. `b` is the unconditional branch (DSS `jmp`), `br` the register-indirect one (DSS `jmp_indirect`) — the indirectness is in the MNEMONIC here, where AT&T puts it in the operand (`jmp *%rax`). `bl` is the call and `ret` the return.",
        "spelling": "b",    "opcodes": ["jmp"],          "width": 64 },
      { "spelling": "br",   "opcodes": ["jmp_indirect"], "width": 64 },
      { "spelling": "bl",   "opcodes": ["call"],         "width": 64 },
      { "spelling": "ret",  "opcodes": ["ret"],          "width": 64 },

      { "$comment": "★★ THE CONDITIONAL BRANCHES — one row per condition, each naming a `TargetCondCode` that ALREADY EXISTS. No new vocabulary was minted for this dialect: `eq`/`ne`/`slt`/`sle`/`sgt`/`sge`/`ult`/`ule`/`ugt`/`uge` are the same names `.target.json`'s `condCodeEncoding` maps to AArch64's 4-bit cond field, and the same ones the C front-end's comparisons already produce. ⚠ THE SIGNED/UNSIGNED PAIRING IS THE PART TO GET RIGHT AND IS EASY TO GET WRONG: gas's `lt/le/gt/ge` are SIGNED, and its unsigned peers are spelled `lo/ls/hi/hs` (with `cc`/`cs` as accepted aliases of `lo`/`hs`) rather than with a `u` prefix — so a table built by pattern-matching the letters would map `b.ls` to `sle` and miscompile every unsigned comparison silently.\n⚠ THIS BLOCK USED TO END BY NAMING `cset x0, eq` AS DELIBERATELY ABSENT — 'it needs a condition-code operand ROLE the grammar has no form for'. ✅ THAT IS NO LONGER TRUE: the twelve `cset` rows below declare the condition as a POSITIONAL OPERAND SELECTOR, which needed no new role and no grammar form (D-ASM-ARM64-CONDITION-AS-OPERAND-UNMODELLED, closed 2026-08-14). The diagnosis in the old sentence was right — `eq` really does arrive as a bare Identifier that would decode as a register-or-symbol — and the remedy it assumed (a new operand role) was the wrong one: the operand is not an operand at all, it is part of the mnemonic.",
        "spelling": "b.eq", "opcodes": ["jcc"], "width": 64, "cond": "eq" },
      { "spelling": "b.ne", "opcodes": ["jcc"], "width": 64, "cond": "ne" },
      { "spelling": "b.lt", "opcodes": ["jcc"], "width": 64, "cond": "slt" },
      { "spelling": "b.le", "opcodes": ["jcc"], "width": 64, "cond": "sle" },
      { "spelling": "b.gt", "opcodes": ["jcc"], "width": 64, "cond": "sgt" },
      { "spelling": "b.ge", "opcodes": ["jcc"], "width": 64, "cond": "sge" },
      { "spelling": "b.lo", "opcodes": ["jcc"], "width": 64, "cond": "ult" },
      { "spelling": "b.ls", "opcodes": ["jcc"], "width": 64, "cond": "ule" },
      { "spelling": "b.hi", "opcodes": ["jcc"], "width": 64, "cond": "ugt" },
      { "spelling": "b.hs", "opcodes": ["jcc"], "width": 64, "cond": "uge" },
      { "spelling": "b.cc", "opcodes": ["jcc"], "width": 64, "cond": "ult" },
      { "spelling": "b.cs", "opcodes": ["jcc"], "width": 64, "cond": "uge" },

      { "$comment": "★★★ `cset <Xd>, <cc>` — THE CONDITION AS A POSITIONAL OPERAND SELECTOR, and the close of D-ASM-ARM64-CONDITION-AS-OPERAND-UNMODELLED (operator ruling, plan 29 §4.7, 2026-08-14). The row directly above this block used to say this spelling was DELIBERATELY ABSENT because `eq` 'would arrive as a bare Identifier and decode as a register-or-symbol'. That was true and is now fixed at the seam where it was actually broken: `operandSelectors` says operand 1 is part of the MNEMONIC, so it is consumed by the match and the lowering sees exactly one operand — `x0`, the destination — against `setcc`'s `maxOperands: 0`. No new operand ROLE, no condition-code grammar form, no engine branch.\n★★★ TWELVE ROWS MIRRORING `b.eq`…`b.cs` EXACTLY, AND THE PAIRING IS THE PART THAT IS EASY TO GET WRONG. gas's `lt/le/gt/ge` are SIGNED and its unsigned peers are spelled `lo/ls/hi/hs` (with `cc`/`cs` as accepted aliases of `lo`/`hs`) rather than with a `u` prefix — so a table built by pattern-matching the letters would map `ls` to `sle` and silently miscompile every unsigned comparison. ⚠⚠ `name` BESIDE `cond` IS THEREFORE **NOT ONE FACT WRITTEN TWICE**: `name` is the gas SPELLING and `cond` is the substrate's `TargetCondCode`, and DERIVING one from the other is precisely that letter-matching. Do not 'simplify' it.\n★★★ ✔MEASURED 2026-08-14, `aarch64-linux-gnu-as` 2.42, one word per condition — and every one of the twelve is pinned against these bytes in `tests/asm/test_asm_shipped_dialects.cpp` rather than against a rule I believed: eq 9a9f17e0 · ne 9a9f07e0 · lt 9a9fa7e0 · le 9a9fc7e0 · gt 9a9fd7e0 · ge 9a9fb7e0 · lo 9a9f27e0 · ls 9a9f87e0 · hi 9a9f97e0 · hs 9a9f37e0 · cc 9a9f27e0 · cs 9a9f37e0. ⓘ `cc`/`cs` produce bytes IDENTICAL to `lo`/`hs`, which is what an alias means and is why both spellings are declared rather than one.\n★★ THE INVERSION IS THE TARGET'S AND NOT THIS TABLE'S. `CSET Xd,cond` IS `CSINC Xd,XZR,XZR,invert(cond)`, and `arm64.target.json`'s `setcc` variant already declares `condInvert: true` at `condBitPos: 12` over fixedWord 0x9A9F07E0. So `cond: \"eq\"` (encoding 0) becomes nibble 1 in the wire word — ✔MEASURED to agree with gas on all twelve. A dialect that pre-inverted would double-invert and produce the complement of every condition, silently.\n⚠⚠ THESE ROWS DECLARE **NO** `width`, AND UNTIL 2026-08-15 THEY DECLARED `width: 64` — READ WHY BEFORE PUTTING IT BACK. The old pin was NOT the `nop` row's reasoning; it was the `and`/`orr`/`eor` reasoning, and it was correct FOR AS LONG AS THE TARGET DECLARED ONE width-ABSENT `setcc` VARIANT. ✔MEASURED: gas accepts `cset w0, eq` and encodes 1a9f17e0, a genuinely different instruction from `cset x0, eq` (9a9f17e0) — and a width-absent variant matches ANY width, so a bare row would have derived 32 from the `w` register, elected the ONE 64-bit variant anyway and emitted the 64-bit word. That is a silent miscompile, so the pin shipped and the LOUD refusal it produced was anchored as D-ASM-ARM64-SETCC-W-FORM-UNDECLARED. ✅ THE ANCHOR IS CLOSED: `arm64.target.json` now declares `setcc` at width 64 (0x9A9F07E0) **and** width 32 (0x1A9F07E0), so dropping the pin makes each register spelling ELECT ITS OWN VARIANT instead of misencoding. ⛔ RE-ADDING `width` HERE WOULD RE-BREAK THE W-FORM — `effectiveWidth` would reconcile the declared 64 against the register-derived 32 and refuse `cset w0, eq`, a line gas takes. ⛔ AND DELETING EITHER TARGET VARIANT WOULD RE-OPEN THE MISCOMPILE THE PIN EXISTED FOR; the two edits are one edit, in both directions.",
        "spelling": "cset", "opcodes": ["setcc"], "cond": "eq",  "operandSelectors": [{ "index": 1, "name": "eq" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "ne",  "operandSelectors": [{ "index": 1, "name": "ne" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "slt", "operandSelectors": [{ "index": 1, "name": "lt" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "sle", "operandSelectors": [{ "index": 1, "name": "le" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "sgt", "operandSelectors": [{ "index": 1, "name": "gt" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "sge", "operandSelectors": [{ "index": 1, "name": "ge" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "ult", "operandSelectors": [{ "index": 1, "name": "lo" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "ule", "operandSelectors": [{ "index": 1, "name": "ls" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "ugt", "operandSelectors": [{ "index": 1, "name": "hi" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "uge", "operandSelectors": [{ "index": 1, "name": "hs" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "ult", "operandSelectors": [{ "index": 1, "name": "cc" }] },
      { "spelling": "cset", "opcodes": ["setcc"], "cond": "uge", "operandSelectors": [{ "index": 1, "name": "cs" }] },

      { "$comment": "★ COMPARISON — sets the condition flags a `b.<cc>` then reads. `result: none` in the target, so the lowering must treat BOTH written operands as inputs rather than reading the first one as a destination; that is derivable from `result` and needs no dialect declaration.",
        "spelling": "cmp",  "opcodes": ["cmp", "cmp_ext_reg"] },

      { "$comment": "★★★ `nop` — THE ZERO-OPERAND SPELLING, AND THE SECOND WITNESS FOR IT. `arm64.target.json` has declared this opcode (fixed word 0xD503201F, ✔MEASURED against `aarch64-linux-gnu-gcc` + objdump) with NO producer in any source language: `opcodeByMnemonic` resolved it, its encoder was pinned, and nothing could emit it. The AT&T sibling grows the same row in the same cycle, so the zero-operand path is exercised by BOTH dialects rather than by one — a mechanism witnessed by one signatory is the weaker claim this project keeps having to walk back.\n★★ IT IS ALSO THE ONLY NON-TERMINATOR ZERO-OPERAND INSTRUCTION EITHER DIALECT CAN NOW WRITE. `asmStatement` mints NO tail child for a bare mnemonic, and until this row existed the shipped dialects reached that arm through `ret` alone — a terminator, which takes a different route through the lowering. ⚠ `ret` also hid the case in the ONE direction that matters here: a bare `nop` in the middle of a block must lower to an ordinary instruction and NOT close the block.\n★ `width: 64` FOLLOWS THE `ret` ROW ABOVE. On this dialect a declared width is normally the thing to avoid — the REGISTER SPELLING carries the width here (D-ASM-DIALECT-WIDTH-SOURCE-IS-DIALECT-DEPENDENT), which is why the `mov`/`and`/`orr`/`eor` rows dropped theirs — but that reasoning is about reconciling a declared width against REGISTER operands, and this spelling has none. The target's single encoding variant is width-ABSENT (`guard.operandKinds: []`) and matches any width, so the key can never disagree with an operand that is not there.\n⚠ THIS ROW USED TO END BY NAMING `cntvct` AS DELIBERATELY ABSENT — ✅ NO LONGER TRUE, see the `mrs` row below (D-ASM-ARM64-SYSTEM-REGISTER-AS-OPERAND-UNMODELLED, closed 2026-08-14). The old text's DIAGNOSIS was exactly right and is worth keeping: a plain `{\"spelling\": \"mrs\", \"opcodes\": [\"cntvct\"]}` row would take `mrs x0, cntvct_el0`, read `x0` as the destination per `destinationFirst`, and hand the lowering ONE leftover operand against `maxOperands: 0` — so EVERY line using it would fail loud, which is the read-as-support/deliver-an-error defect the `.cfi_personality` family is kept out of the AT&T table for. What was wrong was the assumed REMEDY (an operand ROLE the grammar has no form for). The operand is not an operand.",
        "spelling": "nop",  "opcodes": ["nop"], "width": 64 },

      { "$comment": "★★★ `mrs <Xd>, cntvct_el0` — THE HARDWARE COUNTER READ, and the close of D-ASM-ARM64-SYSTEM-REGISTER-AS-OPERAND-UNMODELLED (operator ruling, plan 29 §4.7, 2026-08-14). `arm64.target.json` declares `cntvct` (0xD53BE040) as AArch64's answer to RDTSC — the instruction sqlite's own `hwtime.h` reaches for on this CPU — and until this row existed the opcode had NO producer in any source language.\n★★★ WHY THIS IS A SELECTOR AND NOT A SYSREG OPERAND KIND, WHICH IS THE WHOLE ARGUMENT. ✔MEASURED on BOTH shipped targets: x86_64 `rdtsc` is `min/maxOperands 0` with `implicitRegisters.outputs [rax,rdx]`; arm64 `cntvct` is `min/maxOperands 0` with `result: value` and the counter in the fixed word. They differ in RESULT MODEL — real, and fine — but BOTH are zero-operand with WHICH COUNTER baked into the opcode. ⇒ 'read the hardware counter' is ONE VERB across ISAs, and `cntvct_el0` is an AArch64 SPELLING detail exactly as `edx:eax` is an x86 one. Modelling it as a generic `mrs` + a sysreg operand would give the arm64 read a DIFFERENT SHAPE from the x86 read, so every shared consumer — starting with `hwtime.h`'s own lowering — would acquire a per-arch shape distinction: the `if (arch == …)` the bar hard-vetoes, arriving through CONFIG rather than C++, which is the slower and worse way for it to arrive. The target opcode's own $comment already made this argument ('a generic `mrs` could only ever read one system register') and it is the reason this row is spelled `mrs` while naming `cntvct`.\n★★ THE SELECTOR IS CONSUMED BY THE MATCH: operand 1 never becomes an operand, never reaches the target, and is EXCLUDED from the `destinationFirst` reading — so the lowering sees exactly one operand (`x0`, the destination) and ZERO remaining, satisfying `maxOperands: 0` with nothing left over. ⚠ `index` IS DECLARED RATHER THAN ASSUMED BECAUSE THE MIRROR IMAGE PUTS IT FIRST: ✔MEASURED, gas writes `msr tpidr_el0, x0` (d51bd040) — selector at index 0 — and with `destinationFirst` a position-blind key would read the SELECTOR as the destination. NO `msr` ROW SHIPS (nothing needs one, and §A.2 cuts both ways); the point is only that the KEY is not wrong about it.\n⚠⚠ `width: 64` IS REQUIRED, AND §4.7.2(3) PREDICTED IT WOULD NOT BE — the prediction is REFUTED by measurement, which is why that section says not to trust it. The premise holds: `cntvct`'s single encoding variant IS `guard.operandKinds: []`, i.e. width-absent. The INFERENCE does not, because a width-absent variant matches ANY derived width and this dialect derives width from the REGISTER SPELLING. ✔MEASURED 2026-08-14, `aarch64-linux-gnu-as` 2.42: `mrs w0, cntvct_el0` is REJECTED ('operand mismatch') — MRS always writes an X register. Without the pin, `mrs w0, cntvct_el0` would derive width 32, elect the width-absent variant and emit d53be040 anyway: accepting what the reference assembler rejects, which the bar forbids in both directions. With it, `effectiveWidth` reconciles and refuses loudly. This is the `nop` row's situation only in its premise; the conclusion is the opposite because `nop` has no register operand to disagree with.\n★ ✔MEASURED 2026-08-14 with `aarch64-linux-gnu-as` 2.42 and pinned in `tests/asm/test_asm_shipped_dialects.cpp`: `mrs x0, cntvct_el0` → d53be040 — and `mrs x0, tpidr_el0` is a REAL system register gas assembles happily (d53bd040) that this dialect must refuse BY NAME rather than lower as if the operand had not been written. That refusal is what proves the selector SELECTS.\n★ SELECTORS FORECLOSE NOTHING. If a target ever needs a sysreg carried as DATA into a real encoding slot, a generic operand kind COEXISTS with this row — the selector row is a SPECIALIZATION, the same relation `nop` has to a generic form — provided the loader's ambiguity refusal stays in place. Anchored trigger-gated: D-ASM-SYSTEM-REGISTER-AS-ENCODED-DATA-UNMODELLED.",
        "spelling": "mrs",  "opcodes": ["cntvct"], "width": 64,
        "operandSelectors": [{ "index": 1, "name": "cntvct_el0" }] },

      { "$comment": "★★★ SCALAR FLOATING POINT — THE BLOCK THAT MAKES `fpr` A CLASS A DECLARED INSTRUCTION CAN NAME (D-ASM-DIALECTS-DECLARE-A-REGISTER-CLASS-NO-INSTRUCTION-CAN-NAME, arm64 half, 2026-09-02, cycle P54 lane ad). It is the aarch64 mirror of the SSE block `asm-x86_64-att.lang.json` grew in P45, and the defect it closes is the same one stated over a different ISA: `arm64.target.json` declares the constraint letter `w` ⇒ register class `fpr`, and until this block existed NOTHING in this table could write an instruction over that class, so the letter bound a class the dialect could never use.\n★★ ✔MEASURED AT THE CLI BEFORE THE CHANGE, on `arm64:elf64-aarch64-linux-exec`: `__asm__(\"nop\" : \"+w\"(r))` over a `double` compiles rc=0 — the class binds — while `__asm__(\"fadd %d0, %d1, %d2\" : \"=w\"(r) : \"w\"(a), \"w\"(b))` dies rc=1 with `A_AsmTextUnsupported … unknown mnemonic 'fadd'`. ✔MEASURED census at the same base: `arm64.target.json` declared **87** opcodes; this table carried **51** instruction rows covering **29** distinct opcodes; of the 58 unspelled opcodes, ALL TWENTY of the FP/SIMD family were unspelled. The seven rows below take the count to **58** and the coverage to **36**.\n★★★ EVERY SPELLING IS BYTE-VERIFIED BEFORE IT IS DECLARED — GNU as 2.42 AND clang 18.1.3, both agreeing on every probe, base word = the assembled word minus the wired Rd/Rn/Rm fields:\n  `fadd d0,d1,d2` = 0x1E622820 ⇒ 0x1E602800 · `fadd s0,s1,s2` = 0x1E222820 ⇒ 0x1E202800 · `fadd d5,d7,d9` = 0x1E6928E5 (the field-placement pin)\n  `fsub` ⇒ 0x1E603800 / 0x1E203800 · `fmul` ⇒ 0x1E600800 / 0x1E200800 · `fdiv` ⇒ 0x1E601800 / 0x1E201800\n  `fneg d0,d1` = 0x1E614020 ⇒ 0x1E614000 · `fneg s0,s1` ⇒ 0x1E214000 · `fneg d31,d30` = 0x1E6143DF\n  `fcmp d0,d1` = 0x1E612000 ⇒ 0x1E602000 (Rm, not Rn — `result: none`, so BOTH written operands are inputs) · `fcmp s0,s1` ⇒ 0x1E202000 · `fcmp s3,s21` = 0x1E352060\n  `fmov d0,d1` ⇒ 0x1E604000 · `fmov s0,s1` ⇒ 0x1E204000 · `fmov d31,d30` = 0x1E6043DF · `fmov s31,s30` = 0x1E2043DF\nAll fifteen matched the words `arm64.target.json` already declared EXCEPT the two `fmov` narrow arms, which were BOTH wrong — see that opcode's `$variantsComment`; the divergence was invisible until this block made the mnemonic reachable from text.\n★★★ NO `width` KEY ON ANY ROW, AND THAT IS THE DESIGN RATHER THAN AN OMISSION — the same reasoning the `mov`/`add`/`and` rows above record (D-ASM-DIALECT-WIDTH-SOURCE-IS-DIALECT-DEPENDENT). On this dialect the REGISTER VIEW carries the width: `fadd d0,d1,d2` derives 64 and elects the ftype=01 arm, `fadd s0,s1,s2` derives 32 and elects ftype=00, and `fadd h0,h1,h2` derives 16, elects NOTHING (the target declares no width-16 arithmetic arm) and fails loud. A declared width would have to be one number and would refuse the other view.\n⚠⚠ **WHAT IS DELIBERATELY NOT DECLARED, AND THE MEASURED REASON FOR EACH — the near-miss rule this project learned on the x86 half.** gas 2.42 and clang 18.1.3 both accept every spelling below and this dialect refuses all of them by name (`unknown mnemonic`), which is the LOUD half of the bar:\n  (1) THE CONVERSIONS — `fcvt`, `fcvtzs`, `fcvtzu`, `scvtf`, `ucvtf` — and the CROSS-FILE `fmov x0,d1` / `fmov d0,x1` / `fmov w0,s1` / `fmov s0,w1`. The target declares all of them with correct byte-verified words (`fcvt s0,d1` = 0x1E624020 against `fpcvt` width 64, `fcvtzs x0,d1` = 0x9E780020 against `fp_to_si` width 64, `scvtf d0,x1` = 0x9E620020 against `si_to_fp` width 64, `fmov x0,d1` = 0x9E660020 against `movq_xmm_to_gpr`, `fmov d0,x1` = 0x9E670020 against `movq_gpr_to_xmm`). They are unspellable for TWO structural reasons, both measured, neither fixable in this document:\n    (a) A CONVERSION MNEMONIC IS ONE SPELLING OVER N WIDTH PAIRS — ⚠⚠ CLOSED 2026-09-02 (cycle P54, lane el); THIS PARAGRAPH IS KEPT AS THE MEASUREMENT, NOT AS THE STATE OF THE TREE. The five conversion spellings SHIP now, one row each, and the block below carries the closing argument. What follows was true at this block's own base and is the reason the hazard is now pinned rather than merely avoided. `fcvt s0,d1` (32←64) and `fcvt d0,s1` (64←32) are one mnemonic whose two registers deliberately DISAGREE, which `dataRegisterWidth` refuses outright; the row shape that expresses a disagreeing pair is `width`+`destWidth`, which fixes ONE pair per row — and two rows sharing a spelling are refused AT LOAD unless `operandSelectors` separate them (plan 29 §4.7.1), which no register view can do. A single width-less row is WORSE THAN NOTHING, and that is ✔MEASURED rather than argued: with `{\"spelling\": \"fcvtzs\", \"opcodes\": [\"fp_to_si\"]}` declared as a throwaway mutant, `__asm__(\"fcvtzs %w0, %s1\" : \"=r\"(r) : \"w\"(a))` compiles **rc=0** and emits **`fcvtzs x16, s29` (0x9E3803B0)** — the X-form, where gas 2.42 and clang 18.1.3 both give the W-form (`fcvtzs w0, s1` = 0x1E380020). Both registers are 32 bits so the width-honesty gate sees no disagreement, the derived width 32 elects `fp_to_si`'s width-32 arm, and that arm's word carries sf=1. The programmer wrote a 32-bit destination and got a 64-bit one, silently — which is exactly what an undeclared spelling's loud refusal prevents.\n    (b) THE CROSS-FILE `fmov` FORMS ARE ONE SPELLING OVER THREE TARGET OPCODES THAT ELECTION CANNOT SEPARATE — ⚠⚠ CLOSED 2026-09-02 (cycle P54, lane el): `electOpcode` GAINED the register-CLASS axis this paragraph says it lacks, reading banks the target already declared, and the `fmov` row below names all three opcodes. KEPT AS THE MEASUREMENT. `fmov`, `movq_xmm_to_gpr` and `movq_gpr_to_xmm` all accept `[reg]` at width 64, and `electOpcode` keys on operand SHAPE and WIDTH only — never on register CLASS — so a row naming all three is AMBIGUOUS and refused, and a row naming only `fmov` leaves `fmov x0,d1` electing the fpr diagonal. ⓘ That last one is refused rather than mis-encoded, by the ENCODER'S register-class gate (D-OPT-LIR-ARG-REGISTER-CLASS-MISMATCH-FAILLOUD) reading `resultRegClass`/`wires[].regClass` out of this same target — a second, later refusal for the same line, which is why the `fmov` row below ships anyway.\n  (2) THE ARRANGEMENT FORMS — `addv b0, v1.8b` (= `addlanes_bytes`, 0x0E31B800) and `cnt v0.8b, v1.8b` (= `popcount_bytes`, 0x0E205800), both byte-verified. Their operands carry a LANE ARRANGEMENT suffix (`v1.8b`) that this dialect's `armName` rule cannot parse at all — ✔MEASURED, `mov %0.16b, %1.16b` gives `P_UnexpectedToken: expected 'LineEnd' — got '.'` then `— got 'b'`. That is dialect GRAMMAR beside the sigil rules, not a table row — and it was the one gap of this row's three still open when this sentence was written. ⚠ CLOSED 2026-09-02 (cycle P54, lane el): see `assembly.registerArrangements` and the `cnt`/`addv` rows below.\n  (3) THE ORDINARY NEAR MISSES — `fabs`, `fsqrt`, `fmax`, `fmin`, `fmadd`, `fmsub`, `fnmul`, `fcsel`, `fccmp`, `frinta`/`frintz`/`frintm`, and the immediate form `fmov d0, #1.0`. The target declares NO opcode whose template emits their bytes, so there is nothing to bind them to and binding them to a neighbour would silently encode a different instruction. They stay undeclared and refuse loud, pinned by an arm that fails if any ever starts assembling.",
        "spelling": "fadd", "opcodes": ["fadd"] },
      { "spelling": "fsub", "opcodes": ["fsub"] },
      { "spelling": "fmul", "opcodes": ["fmul"] },
      { "spelling": "fdiv", "opcodes": ["fdiv"] },
      { "$comment": "★ ONE SOURCE, ONE DESTINATION — `fneg d0, d1` is 2 written operands against `minOperands`/`maxOperands` 1, because `destinationFirst` spends the first on the result. The same arithmetic every 2-token row in this table does; stated once here because the FP block is the first place a reader meets it beside a 3-token sibling.",
        "spelling": "fneg", "opcodes": ["fneg"] },
      { "$comment": "★ `result: none` on the target, so BOTH written operands are inputs and NEITHER is a destination — the `cmp` row's situation exactly, and the reason the Rm field (not Rn) carries the second register: `fcmp d0, d1` = 0x1E612000, one bit-position different from what a destination-first reading would have produced.",
        "spelling": "fcmp", "opcodes": ["fcmp"] },
      { "$comment": "★★ THE FP REGISTER-REGISTER MOVE, AND THE ROW THAT FOUND A BYTE DIVERGENCE IN ITS OWN TARGET. Declaring it made `arm64.target.json`'s `fmov` reachable from text for the first time and immediately exposed that its width-32 and width-16 arms carried the width-64 word — see that opcode's `$variantsComment` for the measurement and the correction. ⚠⚠ THE CROSS-FILE FORMS **ARE** THIS ROW SINCE 2026-09-02 (cycle P54, lane el), AND THE SENTENCE HERE THAT SAID OTHERWISE WENT FALSE THE DAY AFTER IT WAS WRITTEN. It read: *`fmov x0, d1` elects this diagonal opcode (election is class-blind) and is then refused by the encoder's register-class gate, which is loud but is a refusal one tier later than it should be*. Election is no longer class-blind: it reads `encoding.registerClass` / `wires[].regClass` / `resultRegClass` out of this same target, so this row names all THREE opcodes and each of the six forms elects unambiguously (✔byte-verified, gas 2.42 + clang 18.1.3 agreeing on all eight cross-file probes). The encoder's gate is untouched and is still the last word on a wrong-bank operand. ⓘ `fmov h0, h1` DOES elect, at width 16 — both references refuse that spelling at the default -march and both accept it under `.arch armv8.2-a+fp16`, and this descriptor declares no CPU-feature vocabulary, so the union's answer is the one taken.",
        "spelling": "fmov", "opcodes": ["fmov", "movq_xmm_to_gpr", "movq_gpr_to_xmm"] },

      { "$comment": "★★★ THE CONVERSIONS AND THE ARRANGEMENT FORMS — the two families the P54 lane `ad` measured as STRUCTURALLY UNSPELLABLE and reported rather than worked around. Both are declared here, 2026-09-02 (cycle P54, lane el), and BOTH of the blockers it named were closed at their root rather than routed around. The `$comment` on the scalar-FP block above still records what the tree looked like before; this row set is the answer to it.\n\n★★★ (1) THE CROSS-FILE `fmov` IS NOW ONE ROW NAMING THREE OPCODES. The block above says a row naming all three 'is AMBIGUOUS and refused', which was TRUE when written and is now FALSE: `electOpcode` gained a REGISTER-PROFILE axis reading the banks the target ALREADY declared (`encoding.registerClass`, `wires[].regClass`, `resultRegClass`), so `fmov d0,d1` elects `fmov` (fpr→fpr), `fmov x0,d1` elects `movq_xmm_to_gpr` (fpr→gpr) and `fmov d0,x1` elects `movq_gpr_to_xmm` (gpr→fpr), each unambiguously and none by declaration order. ✔MEASURED, gas 2.42 + clang 18.1.3 agreeing on all eight probes: `fmov x0,d1` = 0x9E660020 · `fmov d0,x1` = 0x9E670020 · `fmov w0,s1` = 0x1E260020 · `fmov s0,w1` = 0x1E270020 · `fmov x7,d3` = 0x9E660067 · `fmov w7,s3` = 0x1E260067 · `fmov d3,x7` = 0x9E6700E3 · `fmov s3,w7` = 0x1E2700E3, against the class-internal controls `fmov d0,d1` = 0x1E604020 and `fmov s0,s1` = 0x1E204020.\n\n★★★ (2) A CONVERSION MNEMONIC IS ONE SPELLING OVER N WIDTH PAIRS, AND THE ANSWER IS `destWidthFromOperands` PLUS A SECOND OPCODE — NOT A SECOND ROW. The block above records the measured hazard verbatim: a width-LESS `fcvtzs` row compiled `fcvtzs %w0, %s1` rc=0 and emitted `fcvtzs x16, s29` (0x9E3803B0), the X-form, where both references give the W-form. That is now UNSAYABLE, in two places at once: the target's `fp_to_si` variants DECLARE `destWidth: 64` and `fp_to_si32`'s declare 32, so a written destination that matches neither elects nothing; and `variantHonorsDeclaredDestWidth` refuses any two-width instruction whose elected variant declares no destination width at all, so a target that simply forgot the key cannot fall back to the old silence.\n⇒ ONE row per mnemonic, `destWidthFromOperands: true`, and the WRITTEN registers state both ends — which is the same thing this dialect already does for `add x0,x1,x2` versus `add w0,w1,w2`, one end at a time.\n✔MEASURED, gas 2.42 + clang 18.1.3 agreeing on all EIGHTEEN conversion probes: `fcvtzs w0,s1` = 0x1E380020 · `w0,d1` = 0x1E780020 · `x0,s1` = 0x9E380020 · `x0,d1` = 0x9E780020 · `w3,s7` = 0x1E3800E3 · `x3,d7` = 0x9E7800E3; `fcvtzu` the same four with opcode bit 001 (0x1E390020 / 0x1E790020 / 0x9E390020 / 0x9E790020); `scvtf s0,w1` = 0x1E220020 · `s0,x1` = 0x9E220020 · `d0,w1` = 0x1E620020 · `d0,x1` = 0x9E620020 · `s3,w7` = 0x1E2200E3 · `d3,x7` = 0x9E6200E3; `ucvtf` the same four with opcode bit 011; `fcvt s0,d1` = 0x1E624020 · `d0,s1` = 0x1E22C020 · `s3,d7` = 0x1E6240E3.\n⚠ AND THE SAME-WIDTH `fcvt` SPELLINGS STAY REFUSED, WHICH IS THE REFERENCES' OWN ANSWER: ✔MEASURED, gas 2.42 AND clang 18.1.3 BOTH REFUSE `fcvt s0, s1` and `fcvt d0, d1`. DSS refuses them too, and for a stated reason rather than by accident — no `fpcvt` variant declares a destination width equal to its source width, so every candidate is eliminated.\n\n★★★ (3) THE ARRANGEMENT FORMS. `cnt` and `addv` are the only two spellings this target can encode over a lane arrangement, and both now parse: see `assembly.registerArrangements`. ✔MEASURED, gas 2.42 + clang 18.1.3 agreeing: `cnt v0.8b, v1.8b` = 0x0E205820 · `cnt v3.8b, v7.8b` = 0x0E2058E3 · `cnt v0.16b, v1.16b` = 0x4E205820 · `addv b0, v1.8b` = 0x0E31B820 · `addv b3, v7.8b` = 0x0E31B8E3 · `addv b0, v1.16b` = 0x4E31B820. `addv` carries `destWidthFromOperands` because it reduces a 64- or 128-bit source into an 8-bit `b` destination — the two ends disagree BY DEFINITION, exactly as a conversion's do.\n\n⚠⚠ THE PARAGRAPH THAT CLOSED THIS BANNER — *WHAT IS STILL DELIBERATELY UNDECLARED* — IS SUPERSEDED, AND MOST OF ITS LIST IS NOW DECLARED. It named `fabs`, `fsqrt`, `fmax`, `fmin`, `fmadd`, `fmsub`, `fnmul`, `fcsel`, `fccmp`, `frinta`/`frintz`/`frintm`, `fmov d0,#1.0`, `mov v0.16b,v1.16b` and the four rounding conversions as spellings this target declared no opcode for, and it said so CORRECTLY on 2026-09-02. Later the same day the operator ruled the vocabulary in, and cycle P54's lane `av` declared 28 of those spellings (see the block below, and [[D-ASM-ARM64-GAS-SPELLS-NO-ROUNDING-MODE-OR-VECTOR-MOVE]]). What is STILL undeclared is a much shorter list with a SHARED reason rather than a per-mnemonic one — every survivor needs an OPERAND FORM this dialect does not model as an instruction operand: `fcsel`/`fccmp`/`fccmpe` need a CONDITION operand (and `fccmp` an `#nzcv` immediate); `fcmp d0,#0.0` and `fmov s0,#1.0` need an FP LITERAL; `fcvtzs w0,s1,#4` needs a fixed-point SHIFT COUNT in a 6-bit field no `EncodingSlotKind` names. `fjcvtzs` and `frint32z`/`frint64z` stay undeclared for the opposite reason: BOTH references REFUSE them at the default -march (✔MEASURED), so declaring them would put DSS ABOVE the union. ⚠⚠ THE CLAUSE THAT SAID **EVERY** fp16 (`h`-view) SCALAR FORM BELONGS IN THAT LIST WAS TWO THINGS WRONG AT ONCE, corrected 2026-09-02 (cycle P54, lane `ae`). (a) It was not true that they were all undeclared: `arm64.target.json` carried a width-16 `fmov` arm — predating this cycle, ✔`git show 30cfe32c` — and `fmov h0, h1` assembled rc=0 to 0x1EE04020 through the very rows this block adds, so DSS was above the union on the one fp16 spelling it could reach. That arm is DELETED. (b) The fp16 posture splits by INSTRUCTION FAMILY, not by width: FP16 ARITHMETIC is `FEAT_FP16` and both references refuse it at the default -march, but FP16 CONVERSION is base ARMv8-A and both ASSEMBLE it — ✔MEASURED, agreeing: `fcvt h0, s1` = 0x1E23C020, `fcvt h0, d1` = 0x1E63C020, `fcvt s0, h1` = 0x1EE24020, `fcvt d0, h1` = 0x1EE2C020. So DSS **was** BELOW the union on those four, which was its own row ([[D-TARGET-ARM64-FP16-CONVERSION-FORMS-UNDECLARED]]) because they needed target opcodes this document cannot mint. ⚠ THAT ROW IS CLOSED (2026-09-02, cycle P55, lane fp): the target now declares `fpcvt_h` beside `fpcvt` and the `fcvt` row below names both, so all four assemble and the four words are pinned in `tests/asm/test_asm_arm64_conversion_dialect_rows.cpp`. ⚠⚠ AND THE HEADLINE ABOVE IT — *FP16 CONVERSION is base ARMv8-A* — IS TOO WIDE BY A FAMILY, ✔MEASURED by that lane: only the float↔float FCVT has half arms in base ARMv8-A. The fp16 **integer** conversions are `FEAT_FP16` and BOTH references refuse them at the default -march (`fcvtzs w0, h1`, `fcvtzs x0, h1`, `scvtf h0, w1`, `scvtf h0, x1`, `fcvtns w0, h1`, `fcvtas w0, h1` — all rejected by both), so the `h` view stays unsayable on every `fcvtzs`/`fcvtzu`/`scvtf`/`ucvtf` and rounding row above, and declaring it there would put DSS ABOVE the union. The near-miss rule itself is unchanged, and `tests/asm/test_asm_arm64_conversion_dialect_rows.cpp` still fails if a spelling with no declared opcode starts assembling.",
        "spelling": "fcvtzs", "opcodes": ["fp_to_si", "fp_to_si32"], "destWidthFromOperands": true },
      { "spelling": "fcvtzu", "opcodes": ["fp_to_ui", "fp_to_ui32"], "destWidthFromOperands": true },
      { "spelling": "scvtf",  "opcodes": ["si_to_fp", "si_to_fp32"], "destWidthFromOperands": true },
      { "spelling": "ucvtf",  "opcodes": ["ui_to_fp", "ui_to_fp32"], "destWidthFromOperands": true },
      { "$comment": "★★ SIX MACHINE INSTRUCTIONS, ONE SPELLING, TWO OPCODES — the second added 2026-09-02 (cycle P55, lane fp) to close [[D-TARGET-ARM64-FP16-CONVERSION-FORMS-UNDECLARED]]. AArch64's FCVT converts between S, D **and H**, and both references assemble all six at the default -march (✔MEASURED, gas 2.42 + clang 18.1.3 probed separately, agreeing on 37 probes). The two opcodes are NOT a dialect decision: an opcode holds at most one arm per SOURCE width and there are two forms at each of the three widths, so the six cannot fit in one — `arm64.target.json`'s `fpcvt` `$comment` carries the derivation. The row is unchanged in SHAPE, which is the point `fcvtzs` makes above: one row per mnemonic, `destWidthFromOperands`, and the WRITTEN registers state both ends. ⓘ The three same-width spellings `fcvt s0,s1`, `fcvt d0,d1` and `fcvt h0,h1` stay refused — ✔MEASURED, both references refuse all three — because no variant of EITHER opcode declares a destination width equal to its source width.", "spelling": "fcvt",   "opcodes": ["fpcvt", "fpcvt_h"],       "destWidthFromOperands": true },

      { "spelling": "cnt",    "opcodes": ["popcount_bytes"] },
      { "spelling": "addv",   "opcodes": ["addlanes_bytes"],         "destWidthFromOperands": true },

      { "$comment": "★★★ THE REST OF THE SCALAR FLOATING-POINT VOCABULARY — TWENTY-EIGHT SPELLINGS ADDED 2026-09-02 (cycle P54, lane av) under [[D-ASM-ARM64-GAS-SPELLS-NO-ROUNDING-MODE-OR-VECTOR-MOVE]], on the operator's ruling that the mnemonics lane `el` measured and left undeclared be declared now.\n\n★★★ THE CENSUS THAT PICKED THEM, BECAUSE THE HONEST ANSWER TO *what is missing* IS NOT A SHORT LIST. ✔MEASURED at this base with 48 inline-asm probes through the real CLI: 46 of them were spellings GNU as 2.42 and clang 18.1.3 BOTH assemble and DSS REFUSED. AArch64 is a large ISA and the remaining set is bounded only by the ISA — integer `madd`/`smulh`/`bic`/`orn`/`eon`/`rev`/`cls`/`adds`/`csel`/`extr`/`ubfx`, the barriers `dmb`/`isb`, the LSE atomics, the lane moves (`ins`/`umov`/`smov`/`dup`) and the whole SIMD arithmetic table are all still refused. ⇒ WHAT SHIPPED IS A FAMILY THAT CLOSES: **every AArch64 scalar floating-point instruction whose operands are registers only, at the S and D forms.** That set is finite, it is what a C program's inline `asm` actually reaches, and it is now complete — a partial fix that read as a complete one is the failure mode this row was written against, so the boundary is stated rather than implied.\n\n★★★ WHY THE EIGHT ROUNDING CONVERSIONS ARE THE POINT OF THE WHOLE ROW SET. `fcvtas`/`fcvtau`/`fcvtms`/`fcvtmu`/`fcvtns`/`fcvtnu`/`fcvtps`/`fcvtpu` differ from the already-declared `fcvtzs`/`fcvtzu` in NOTHING BUT the `rmode` field. Binding one of them to `fp_to_si` would have compiled, linked and RUN, rounding toward zero where the author wrote toward −∞ — a working program with the wrong numbers. Each therefore names its OWN opcode pair, and the two-opcode wide/narrow split is `fcvtzs`'s exactly: one row, `destWidthFromOperands`, and the WRITTEN registers state both ends.\n\n★★ EVERY SPELLING BELOW IS ✔BYTE-VERIFIED AGAINST GNU as 2.42 **AND** clang 18.1.3, PROBED SEPARATELY — 124 probes across the census and the ship list, ZERO disagreements — and each expected word was read back out of an assembled object with `aarch64-linux-gnu-objdump`, never derived from a neighbour and never read out of a manual. The words live on the target opcodes; `tests/asm/test_asm_arm64_rounding_dialect_rows.cpp` pins all 72 through the real lowering.\n\n⚠ NOT ONE OF THESE 28 HAS A C PATH. `mir_to_lir` produces none of them, so every row here is TEXT-REACHABLE ONLY — which is precisely how the `fmov` S-form byte divergence survived unnoticed until P54, and why each word is pinned rather than trusted.\n\n★ NO `width` KEY ON ANY ROW, for the reason the `mov` row states at the top of this table: on aarch64 the REGISTER SPELLING carries the width, and every target opcode named here declares explicit width-32 and width-64 variants, so a declared width could only disagree with the operands.\n\nFP→INTEGER WITH AN EXPLICIT ROUNDING MODE — eight spellings, `destWidthFromOperands` like their `fcvtzs` sibling. ✔`fcvtns w0,s1` = 0x1E200020 · `x0,d1` = 0x9E600020.",
        "spelling": "fcvtns", "opcodes": ["fp_to_si_nearest",   "fp_to_si_nearest32"],   "destWidthFromOperands": true },
      { "spelling": "fcvtnu", "opcodes": ["fp_to_ui_nearest",   "fp_to_ui_nearest32"],   "destWidthFromOperands": true },
      { "spelling": "fcvtas", "opcodes": ["fp_to_si_ties_away", "fp_to_si_ties_away32"], "destWidthFromOperands": true },
      { "spelling": "fcvtau", "opcodes": ["fp_to_ui_ties_away", "fp_to_ui_ties_away32"], "destWidthFromOperands": true },
      { "spelling": "fcvtms", "opcodes": ["fp_to_si_floor",     "fp_to_si_floor32"],     "destWidthFromOperands": true },
      { "spelling": "fcvtmu", "opcodes": ["fp_to_ui_floor",     "fp_to_ui_floor32"],     "destWidthFromOperands": true },
      { "spelling": "fcvtps", "opcodes": ["fp_to_si_ceil",      "fp_to_si_ceil32"],      "destWidthFromOperands": true },
      { "spelling": "fcvtpu", "opcodes": ["fp_to_ui_ceil",      "fp_to_ui_ceil32"],      "destWidthFromOperands": true },

      { "$comment": "★★ ROUND-TO-INTEGRAL, FLOAT IN AND FLOAT OUT — seven spellings, NO `destWidthFromOperands` because both ends are the same width (unlike the `fcvt*` block above, whose two ends disagree by definition). Seven rows and seven opcodes rather than one parameterised row, for the same reason: they differ only in the mode, every one of them returns a NUMBER, and a mis-binding is invisible outside the answer. ✔`frintn d0,d1` = 0x1E644020 · `frinta d0,d1` = 0x1E664020 · `frintm d0,d1` = 0x1E654020 · `frintp s0,s1` = 0x1E24C020 · `frintz s0,s1` = 0x1E25C020 · `frinti d0,d1` = 0x1E67C020 · `frintx s0,s1` = 0x1E274020.",
        "spelling": "frintn", "opcodes": ["fround_nearest"] },
      { "spelling": "frinta", "opcodes": ["fround_ties_away"] },
      { "spelling": "frintm", "opcodes": ["fround_floor"] },
      { "spelling": "frintp", "opcodes": ["fround_ceil"] },
      { "spelling": "frintz", "opcodes": ["fround_trunc"] },
      { "spelling": "frinti", "opcodes": ["fround_current"] },
      { "spelling": "frintx", "opcodes": ["fround_exact"] },

      { "$comment": "★ THE 1-SOURCE ARITHMETIC PAIR that joins the already-declared `fneg` in AArch64's FP data-processing (1 source) group. ✔`fabs d0,d1` = 0x1E60C020 · `fsqrt s0,s1` = 0x1E21C020.",
        "spelling": "fabs",  "opcodes": ["fabs"] },
      { "spelling": "fsqrt", "opcodes": ["fsqrt"] },

      { "$comment": "★★ THE 2-SOURCE ARITHMETIC that joins `fadd`/`fsub`/`fmul`/`fdiv`. ⚠ `fmax` AND `fmaxnm` ARE DIFFERENT INSTRUCTIONS AND ARE NEVER FOLDED: they agree on every ordinary input and disagree on NaN, which is exactly the input a test is least likely to feed. ✔`fmax d0,d1,d2` = 0x1E624820 · `fmin s0,s1,s2` = 0x1E225820 · `fmaxnm d0,d1,d2` = 0x1E626820 · `fminnm s0,s1,s2` = 0x1E227820 · `fnmul s0,s1,s2` = 0x1E228820.",
        "spelling": "fmax",   "opcodes": ["fmax"] },
      { "spelling": "fmin",   "opcodes": ["fmin"] },
      { "spelling": "fmaxnm", "opcodes": ["fmaxnm"] },
      { "spelling": "fminnm", "opcodes": ["fminnm"] },
      { "spelling": "fnmul",  "opcodes": ["fnmul"] },

      { "$comment": "★★ THE FUSED MULTIPLY-ADD SQUARE — the first FOUR-token instructions this dialect spells (`fmadd d0, d1, d2, d3` writes one destination and three sources), which needed nothing from the shared grammar: `asmOperandSeq` already takes as many operands as are written, and the third source rides the `Ra` slot the integer `msub` has used all along. ✔`fmadd d0,d1,d2,d3` = 0x1F420C20 · `fmsub s0,s1,s2,s3` = 0x1F028C20 · `fnmadd d0,d1,d2,d3` = 0x1F620C20 · `fnmsub s0,s1,s2,s3` = 0x1F228C20.",
        "spelling": "fmadd",  "opcodes": ["fmadd"] },
      { "spelling": "fmsub",  "opcodes": ["fmsub"] },
      { "spelling": "fnmadd", "opcodes": ["fnmadd"] },
      { "spelling": "fnmsub", "opcodes": ["fnmsub"] },

      { "$comment": "★ THE SIGNALLING COMPARE — `result: none` on the target, so BOTH written operands are inputs and neither is a destination, exactly as the `fcmp` row above says. ✔`fcmpe d0,d1` = 0x1E612010 · `fcmpe s0,s1` = 0x1E212010, one bit (the E bit, bit 4) away from the quiet `fcmp` words.",
        "spelling": "fcmpe",  "opcodes": ["fcmpe"] }
    ],

    "directives": [
      { "$comment": "★ A DIRECTIVE IS A VERB, NOT A STRING. The engine switches on the closed `verb` set; the SPELLING is this dialect's. gas's `.globl`/`.global` are two spellings of one verb — and that both dialects need exactly that pair is a small confirmation the verb/spelling split is at the right seam. An undeclared directive fails loud naming the spelling and this dialect, never ignored, because a silently-dropped `.globl` produces a binary whose entry symbol is local and a link failure that points nowhere near the cause.",
        "spelling": "text",   "verb": "sectionText" },
      { "$comment": "⚠ BOTH SPELLINGS, AND ON THIS TARGET IT IS `.global` THAT THE REFERENCE COMPILER ACTUALLY EMITS — ✔MEASURED 2026-08-13, `aarch64-linux-gnu-gcc -S` writes `.global main` where x86_64 `gcc -S` writes `.globl main`, and AArch64 gas accepts either (assembles rc=0 both ways). A dialect that declared only the x86 habit would have failed on every aarch64 compiler output while looking complete.",
        "spelling": "globl",  "verb": "globalSymbol" },
      { "spelling": "global", "verb": "globalSymbol" },
      { "$comment": "★★★ `.type NAME, %function` — WHICH LABELS ARE FUNCTIONS, STATED BY THE FILE INSTEAD OF GUESSED BY THE COMPILER. Every label NOT so marked is a BLOCK inside whatever function is open, which is what makes `b Lwin` a branch rather than a call into a second function. The operator's ruling on the corpus: *\"an example that only works because the compiler guesses is the example being wrong.\"*\n★ `marker` IS WHY `.type foo, %object` DOES NOT MINT A FUNCTION — both are `.type`, and only the operand distinguishes them. That this row differs from its x86_64 sibling in exactly ONE string (`%function` vs `@function`) is the evidence the marker belongs to the dialect and not to the engine.",
        "spelling": "type",   "verb": "functionEntry", "marker": "%function" },
      { "$comment": "★★ THE DATA SECTIONS. `.data` and `.bss` are gas directives in their own right and keep their own rows.\n★★★ `.rodata` IS NOT, AND THAT ASYMMETRY IS THE WHOLE OF D-ASM-SECTION-DIRECTIVE-WITH-OPERAND-UNMODELLED (closed 2026-08-13). ✔MEASURED 2026-08-13 against BOTH ports — `aarch64-linux-gnu-as` and x86_64 `as` — a bare `.rodata` is `Error: unknown pseudo-op` while `.section .rodata` assembles rc=0 into a `.rodata` PROGBITS+A section. Declaring `.rodata` as an ordinary row would make this dialect accept a spelling NO reference assembler does, which the bar forbids in both directions; leaving it out made read-only data unreachable. ⇒ the row below declares `operandOnly`, so the spelling exists in the SECTION vocabulary and not in the DIRECTIVE vocabulary, and writing it bare fails loud naming the form that works.\n★★ WHAT THAT BUYS BEYOND `.rodata`, AND WHY IT MINTS NO SECOND TABLE: `.section` DELEGATES to these very rows, so `.section .data` and `.data` reach the identical row by construction (both ✔MEASURED accepted by gas) and `.section .text` reaches the `sectionText` row. A private section-name table hung off the `.section` row would have restated `.data`→data and `.bss`→bss — the same declared-twice shape the identifier-continuation facet just deleted twelve instances of — and the two copies would drift the first time a section was renamed.",
        "spelling": "data",   "verb": "sectionData", "section": "data" },
      { "spelling": "bss",    "verb": "sectionData", "section": "bss"  },
      { "$comment": "★★★ `.section NAME` — THE SECTION IS THE OPERAND. It models the section NAME and NOTHING ELSE, and the boundary is stated rather than discovered: real `.section` also carries flags and a type (✔MEASURED, gas accepts `.section .rodata,\"a\",@progbits` and `.section .note.GNU-stack,\"\",@progbits` rc=0), every one of which changes what the section IS — `\"aw\"` writable, `\"ax\"` executable, `@nobits` zero-fill. DSS derives all of those from the `DataSectionKind`, so honouring the name while dropping the flags would put `.section .rodata,\"aw\"` in a read-only section the program then faults writing. ⇒ a `.section` with more than one operand FAILS LOUD naming the operand it will not interpret. Accept-and-ignore is the one thing not on the table.\n⚠ CONSEQUENCE, STATED SO IT IS NOT REPORTED AS A SURPRISE: the `.section .note.GNU-stack,\"\",@progbits` line that ends nearly every gcc `-S` output is REFUSED — twice over, since `.note.GNU-stack` is also not a section this build can place.\n⚠⚠ AND THE REFUSAL COMES FROM TWO DIFFERENT SITES, WHICH THE SENTENCE ABOVE USED TO ELIDE. 'Both refusals name what they refused' was true of the SECTION NAME and NOT of the flags spelling, and the difference is measurable. ✔MEASURED 2026-08-13 through the real CLI on THIS dialect: `.section .rodata,1` and `.section .rodata,%progbits` reach the directive handler and get `A0008 — '.section' carries 2 operands; this build models the section NAME and nothing else`, quoting the operand and stating the rule. `.section .rodata,@progbits` does NOT — `@` is a token in the AT&T sibling (it writes `.type main, @function`) and is not one here (this document writes `%function`), so it dies at `P000E: got illegal character 0x40`; the quoted form dies the same way at `0x22` on BOTH dialects, since neither declares a string literal. Every form refuses (rc=1, no artifact, the flags are never applied and never ignored) — there is no silent path — but only the lexable forms carry the explanatory message. Pinned from both ends in `tests/asm/test_asm_shipped_dialects.cpp`.",
        "spelling": "section", "verb": "sectionByName" },
      { "$comment": "★ THE READ-ONLY DATA SECTION, reachable ONLY as `.section .rodata`. `operandOnly` is what keeps the bare spelling refused — see the `.data` row's banner for the measurement.",
        "spelling": "rodata", "verb": "sectionData", "section": "rodata", "operandOnly": true },

      { "$comment": "★★★ THE ELEMENT WIDTHS ARE THIS DIALECT'S, AND `.word` IS THE REASON THAT MATTERS. On AArch64 gas `.word` is FOUR bytes; on x86 gas it is TWO. An engine that knew a width for `.word` would silently halve or double every table on one of the two ports, which is precisely why `unitBytes` is dialect data. ✔MEASURED 2026-08-13 with the reference assembler rather than recalled — `aarch64-linux-gnu-as` on a file defining one label per directive, symbol offsets read back with `objdump -t`: `.byte`→1, `.hword`→2, `.short`→2, `.word`→4, `.long`→4, `.quad`→8, `.xword`→8. Consecutive offsets 0,1,3,5,9,13,21,29 — each delta IS the width of the directive above it.",
        "spelling": "byte",   "verb": "emitData", "unitBytes": 1 },
      { "spelling": "hword",  "verb": "emitData", "unitBytes": 2 },
      { "spelling": "short",  "verb": "emitData", "unitBytes": 2 },
      { "spelling": "word",   "verb": "emitData", "unitBytes": 4 },
      { "spelling": "long",   "verb": "emitData", "unitBytes": 4 },
      { "spelling": "quad",   "verb": "emitData", "unitBytes": 8 },
      { "spelling": "xword",  "verb": "emitData", "unitBytes": 8 },

      { "$comment": "★ THREE SPELLINGS, ONE VERB — gas takes `.zero`, `.space` and `.skip` for the same thing, and the FIRST operand is a byte COUNT rather than a value (`.byte 16` writes one byte 0x10; `.zero 16` writes sixteen 0x00). The OPTIONAL SECOND operand is the fill byte.\n★★★ THE VERB WAS SPELLED `reserveZeroBytes` UNTIL 2026-08-13 AND THE RENAME IS THE CLOSE OF D-ASM-SPACE-DIRECTIVE-FILL-BYTE-UNMODELLED. The old name was TRUE while the fill was unmodelled and a second operand was refused; it stops being true the moment `.space 4, 7` writes `07 07 07 07`, and a config verb whose name states the wrong default is a lie the next reader inherits.\n★★ AND `.zero` IS IN THE SAME ROW SET, WHICH IS MEASURED RATHER THAN ASSUMED — the tempting reading is that `.zero` takes no fill and therefore needs its own verb. ✔MEASURED 2026-08-13 with `aarch64-linux-gnu-as` + `objdump -s`: `.space 4, 7` AND `.zero 4, 7` BOTH assemble rc=0 and BOTH emit `07070707`. One verb, an optional fill, three spellings.\n⚠ TWO DELIBERATE DIVERGENCES FROM gas, both erring toward refusing rather than writing bytes nobody named: a fill outside one byte is REFUSED here (✔MEASURED, gas truncates `.space 4, 300` to `0x2c` with a warning and exits 0) — through the SAME `valueFitsUnit` chokepoint `.byte 300` already uses, so it is one module policy and not a second opinion; and a THIRD operand is refused outright. ⓘ A non-zero fill in a ZERO-FILL section (`.bss`) is NOT a divergence: gas exits 0 with `Warning: ignoring fill value in section '.bss'`, and this build warns and ignores it identically — the wire format stores no file bytes there, so there is nowhere for a pattern to live.",
        "spelling": "zero",   "verb": "reserveFillBytes" },
      { "spelling": "space",  "verb": "reserveFillBytes" },
      { "spelling": "skip",   "verb": "reserveFillBytes" },

      { "spelling": "size",   "verb": "ignoredAnnotation" },
      { "spelling": "p2align","verb": "ignoredAnnotation" },
      { "spelling": "align",  "verb": "ignoredAnnotation",
        "$comment": "⚠ `.align` MEANS DIFFERENT THINGS ON DIFFERENT gas TARGETS — a byte count on some, a power of two on others (AArch64 reads it as a power of two, like `.p2align`). It is declared `ignoredAnnotation` because DSS derives function alignment from the format rather than from the source, so the ambiguity never has to be resolved; the day alignment becomes honoured, this row is the one that must grow a target-aware reading rather than a guess." },
      { "$comment": "★★★ THE `.cfi_*` FAMILY — AND ON THIS PORT IT IS AN ADDITION, NOT A REPAIR. D-ASM-CFI-UNWIND-INFO-SILENTLY-DROPPED's own text records the asymmetry and warns about the wrong way to close it: the AT&T dialect carried all eighteen spellings as `ignoredAnnotation` (accepted and DROPPED — the defect), while THIS dialect carried ZERO, so `.cfi_startproc` in an aarch64 `.s` FAILED LOUD as an unknown directive — bar-compliant behaviour that simply could not read the input. Declaring them here as `ignoredAnnotation` 'for consistency with the sibling' would have propagated the defect into a second dialect in the name of tidiness. They arrive as REAL verbs or not at all.\n★★ THE SPELLINGS AND THE MEANINGS ARE IDENTICAL TO THE AT&T PORT, AND THAT IS AN OBSERVATION RATHER THAN AN ASSUMPTION. ✔MEASURED 2026-08-17, `aarch64-linux-gnu-gcc 13.3.0 -O1 -S`: the compiler emits `.cfi_startproc`, `.cfi_def_cfa_offset`, `.cfi_offset 29, -16`, `.cfi_offset 30, -8`, `.cfi_remember_state`, `.cfi_restore 29`, `.cfi_restore_state`, `.cfi_endproc` — the same family, the same numeric operand form, no `#` sigil on the numbers. The two dialects differ on `.type`'s marker (`%function` vs `@function`), on `.word`'s width and on `.global` vs `.globl`; they do NOT differ here, and a row set that invented a difference would be describing the config file rather than gas.\n⚠ THE OPERANDS ARE DWARF REGISTER NUMBERS AND THE COLUMN IS 30, NOT 16. ✔MEASURED 2026-08-17, `aarch64-linux-gnu-as` 2.42 + `readelf --debug-dump=frames`: the CIE prints `Return address column: 30` and `DW_CFA_def_cfa: r31 (sp) ofs 0` — x30 IS an ordinary register that also serves as the RA column, where x86_64's column 16 is synthetic and belongs to no register. That difference is the target's to state (`dwarfReturnAddressColumn` in `arm64.target.json`), never this document's.",
        "spelling": "cfi_startproc",       "verb": "frameStart" },
      { "spelling": "cfi_endproc",         "verb": "frameEnd" },
      { "spelling": "cfi_def_cfa",         "verb": "frameRule", "rule": "def_cfa" },
      { "spelling": "cfi_def_cfa_offset",  "verb": "frameRule", "rule": "def_cfa_offset" },
      { "spelling": "cfi_def_cfa_register","verb": "frameRule", "rule": "def_cfa_register" },
      { "spelling": "cfi_adjust_cfa_offset","verb": "frameRule", "rule": "adjust_cfa_offset" },
      { "spelling": "cfi_offset",          "verb": "frameRule", "rule": "offset" },
      { "spelling": "cfi_rel_offset",      "verb": "frameRule", "rule": "offset", "offsetFromCfa": true },
      { "spelling": "cfi_val_offset",      "verb": "frameRule", "rule": "val_offset" },
      { "spelling": "cfi_register",        "verb": "frameRule", "rule": "register" },
      { "spelling": "cfi_restore",         "verb": "frameRule", "rule": "restore" },
      { "spelling": "cfi_undefined",       "verb": "frameRule", "rule": "undefined" },
      { "spelling": "cfi_same_value",      "verb": "frameRule", "rule": "same_value" },
      { "spelling": "cfi_return_column",   "verb": "frameReturnColumn" },
      { "spelling": "cfi_remember_state",  "verb": "frameRule", "rule": "remember_state" },
      { "spelling": "cfi_restore_state",   "verb": "frameRule", "rule": "restore_state" },
      { "$comment": "⛔ `.cfi_signal_frame` — DECLARED AND REFUSED. ✔MEASURED 2026-08-17: it changes the CIE AUGMENTATION to 'zRS', which is a CIE-level property, and DSS emits one shared CIE per module. See the AT&T row for the full measurement.",
        "spelling": "cfi_signal_frame",    "verb": "unrepresentable" },
      { "$comment": "⛔ `.cfi_escape` — DECLARED AND REFUSED, for the reason the AT&T row measures in full: it states RAW DWARF CFA BYTES nothing can validate or re-encode, and ✔MEASURED 2026-08-17 GNU as emits them verbatim with no length check, so a malformed escape silently EATS the following rules out of the unwind program at rc=0. Declared rather than left undeclared so the refusal names the construct and the next reader finds this note instead of 'adding the missing row'.",
        "spelling": "cfi_escape",          "verb": "unrepresentable" }
    ]
  }
}

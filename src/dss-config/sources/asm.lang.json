{
  "$comment": "★★★ THE ASSEMBLY LANGUAGE — a PEER SOURCE LANGUAGE, not a C feature (plan 29; operator decision 2026-08-12, recorded verbatim in the [[D-LANG-GNU-EXTENDED-INLINE-ASM-UNSUPPORTED]] registry row). The thesis in one line: assembly IS a language, so its grammar belongs beside the other grammars — never nested inside C's.\n\n★★★ TWO ENTRY POINTS, ONE GRAMMAR. This document is designed for BOTH from the start, because getting the shape wrong is what forces a rewrite later:\n  • EMBEDDED — `__asm__ (…)` written inside another language. Entry rule `asmStmt`. The HOST tokenizes, and the operand values are HOST expressions (`\"=a\" (lo)` binds a C lvalue). Reached through the host's `languageReferences` block; the host BINDS every name in `requires` below.\n  • STANDALONE — a `.s` file compiled directly. Entry rule an `asmUnit` root, this document's OWN token table, and asm's own operands (registers, immediates, labels, memory refs).\n★★ THE PROPERTY THAT KEEPS ONE GRAMMAR SERVING BOTH, AND THE ONE TO PROTECT: the two modes share ONE NAME VOCABULARY. Every name in `requires` is satisfied by THIS document's own `shapes`/`tokens` declarations when standalone, and by the host's `bindRules`/`bindTokens` when embedded. Same rules, two satisfaction sources — and the loader states it once, so the two paths CANNOT drift into separate rule sets. An UNSATISFIED name is a load error in BOTH modes, never a silent disable.\n\n⚠ STATUS 2026-08-12 — WHAT IS HERE AND WHAT IS NOT, so the gap is not mistaken for a design. The EMBEDDED half is complete: the twelve rules below, the semantic refusal, and the HIR-lowering row. The STANDALONE half is NOT built — there is no `tokens` block, no `keywords` block, no `root`, no `artifactProfiles`. Everything the standalone half needs is ADDITIVE to this file: adding a `tokens` block that declares the kinds named in `requires.tokens` turns root-mode satisfaction from failing to passing WITHOUT touching one shape, which is precisely the symmetry above doing its job. ✔MEASURED: `src/asm/` consumes LIR (post-regalloc, physical registers only) and has NO text entry point, so a RUNNING `.s` additionally needs the `.target.json` opcode vocabulary (plan 29 P3) plus a text→LIR lowering (P4). That is genuinely large and is not pretended here.\n⇒ CONSEQUENCE, STATED SO IT IS NOT REDISCOVERED: `--language asm` today FAILS LOUD with ONE diagnostic listing every unsatisfied role by name and category. That is the correct answer — a grammar whose vocabulary nothing supplies cannot parse anything — and it doubles as the standalone half's own red-on-disable gate: the day the `tokens` block and the `asmUnit` root land, that diagnostic stops firing by itself, which is the only proof that the additive half actually took.\n\n★★ THE FILE IS `asm.lang.json` — ONE NAMESPACE, ONE RESOLUTION RULE, AND THE STANDALONE CONSTRAINT ENFORCED BY A PROPERTY RATHER THAN BY A SUFFIX. `GrammarSchema::loadShipped(name)` and `languageReferences` both resolve `sources/<name>.lang.json` through the SAME `findShippedConfig` precedence (`DSS_CONFIG_ROOT`, else the cwd ancestor walk) and the same path-like-name rejection. A referenced document is not a second class of file; it is a language document a host reaches into.\n⚠ AN EARLIER DRAFT NAMED THIS FILE `asm.json` to keep a hole-carrying document out of `loadShipped`'s namespace. The CONSTRAINT was right and is kept; the ENFORCEMENT was wrong, because it encoded a TEMPORARY property in a PERMANENT filename — at P2.5 the holes become self-satisfied and the file would have to be renamed, dragging the resolver, the reference name and every comment with it. The loader now checks the PROPERTY (`checkRootModeHolesSatisfied`): `--language asm` FINDS this document and fails with the precise reason — 'language asm cannot be loaded standalone: 15 of its requires names are UNSATISFIED — rules (2): operandExpr, templateText; token kinds (13): asmKeyword, …' — instead of the misleading 'no such language'. ★ THAT CHECK IS SELF-RETIRING: it stops firing the moment the holes are satisfied, with no rename, no flag and no resolver change. ⛔ Do not reintroduce a suffix trick.\n\n★ WHAT THIS DOCUMENT OWNS, AND WHY OWNERSHIP IS THE POINT. It owns its grammar, its `semantics.inlineAsm` refusal, and its `hirLowering` row. A host that references it inherits all three FOR FREE — the refusal 'extended asm is not yet supported' is a property of THIS language's implementation maturity, not of C, and a second host (SQL-in-C, GLSL-in-C++, inline IL) gets it with ZERO new grammar rules. That inheritance is what 'reused' means and is the property the mechanism is tested on.\n\n⚠ NOT OWNED HERE, DELIBERATELY: constraint letters (`=a`, `=d`, `=r`) and register/mnemonic vocabulary. ✔MEASURED — sqlite's `src/hwtime.h` uses `=a`/`=d` on x86 and `=r` on arm64 IN THE SAME HEADER, so they are per-CPU vocabulary and live in `.target.json`. Putting a register list in this file would be exactly the target identity leak the bar forbids.",

  "dssSchemaVersion": 4,

  "language": {
    "$comment": "⚠ NO `fileExtensions` YET, AND THAT IS NOT AN OVERSIGHT. Declaring `.s`/`.S` here would advertise a capability that does not exist: a driver routing a `.s` file to this document today would hit the root-mode unsatisfied-hole errors, which is a true diagnostic pointed at the wrong question. The extensions land in the same commit as the `tokens` block and the `asmUnit` root, so 'this document claims .s' and 'this document can read .s' become true together.",
    "name": "asm",
    "version": "0.0.1"
  },

  "requires": {
    "$comment": "★★ THE HOLES — every name this grammar uses that it does not itself define. Read it as a CONTRACT with two possible signatories (see the header): the host's `bindRules`/`bindTokens` in embedded mode, this document's own `shapes`/`tokens` in standalone mode. Every entry MUST be satisfied; an unsatisfied entry is a load error either way.\n★ THE NAMES ARE ROLE NAMES, NOT SPELLINGS, and that is what makes the grammar portable. Nothing below says `Colon` or `ColonColonOp` or `expression` — those are `c-subset.lang.json`'s vocabulary and would make this file 'a C feature in a different file', which is the outcome the operator rejected by name. A host with a different lexical surface binds the same roles to ITS kinds and gets the same twelve rules.\n★ WHY `sectionSeparatorFused` IS A HOLE OF ITS OWN rather than 'two of `sectionSeparator`': `::` is ONE token under maximal munch in any language that also spells `::` (C23 attribute namespaces do), so the fused boundary is a distinct KIND and must be bound separately. See `asmStmt`'s $comment for the measurement.\n★ `symbolName` serves BOTH the symbolic operand name (`[out] \"=r\"(x)` — GCC 6.47.2.3) and the `asm goto` target labels (6.47.2.7). One role, because in every host that has both they are the same lexical class (a plain identifier); a host where they differ would be the trigger to split this into two holes, and the split would be additive.",
    "rules": [
      "operandExpr",
      "templateText"
    ],
    "tokens": [
      "asmKeyword",
      "volatileQualifier",
      "inlineQualifier",
      "gotoQualifier",
      "sectionSeparator",
      "sectionSeparatorFused",
      "operandSeparator",
      "symbolName",
      "argsOpen",
      "argsClose",
      "symbolicNameOpen",
      "symbolicNameClose",
      "statementEnd"
    ]
  },

  "shapes": {
    "asmStmt": {
      "$comment": "FC17.9(i) D-CSUBSET-INLINE-ASM, P1+P2 (2026-08-12): the GNU inline-asm STATEMENT — the FULL extended form, parsed end to end. `asm [volatile|inline|goto]… ( <template> [: outputs [: inputs [: clobbers [: labels]]]] ) ;`. This is the EMBEDDED entry point: a host reaches the whole cluster through this one rule. FIRST = `asmKeyword`, which in every host is disjoint from every other statement alt → unique-production DIRECT DESCENT, NON-speculative (the sehTryStmt/TryKeyword precedent). The template is `templateText` — bound to a rule that decodes adjacent-concatenated string literals, so multi-piece templates and the L/u/U/u8 prefixes arrive already handled by the HOST's own string chokepoint rather than a second decoder here.\n★★ THE REFUSAL LIVES AT THE SEMANTIC TIER, NOT THE PARSE TIER, AND THAT IS THE POINT. A parse error was the wrong instrument twice over: it cannot say WHICH part of a real asm is unsupported, and it destroys recovery. ✔MEASURED on the real DSS binary, pre-P1: this construct did not merely fail — the parser NEVER RECOVERED, consuming the remainder of the translation unit inside an unterminated `Paren` scope, so every function AFTER the asm statement went undiagnosed and the error count scaled with FILE LENGTH. (53 diagnostics — 50×P0001 + 2×P0002 + 1×P9000, 371 lines of output — on a 10-line file with four trailing functions; 16 on a 4-line reconstruction. ⚠ THE INVARIANT IS THE NON-RECOVERY AND THE SCALING; the bare number is a property of the FILE, not of the construct, and re-quoting it as a fact about `__asm__` is the comment-rot this project keeps getting bitten by.) P1's exit criterion is therefore ONE diagnostic with the functions after it parsed normally — a count that does NOT move with file length ⚠ FOR A GRAMMAR-ACCEPTED asm, which is the exact scope of the claim and the whole of what P1 bought: the refusal moved to the SEMANTIC tier, and that tier only runs on a statement that PARSED. A grammar-REJECTED asm — `(\"\" : : : : : )` with a fifth section, or a missing template — never reaches it and STILL cascades, still scaling with file length. ✔MEASURED with a matched control 2026-08-12: a plain `if (1 {` scales IDENTICALLY, so that is the PRE-EXISTING unterminated-`Paren` recovery gap and NOT an asm regression — scoping this sentence is the honest fix, and the general question of `Paren` recovery is a separate one that must not be smuggled in behind an asm comment. ⚠ Parsing a construct is NOT supporting it: accepting these tokens buys an accurate refusal, nothing more.\n★★ BOUNDARIES, NOT SLOTS — WHY THE TAIL IS A CHAIN OF `…Tail` RULES AND NOT FOUR `{optional}` SLOTS. In any host that also spells `::` as one token (C23 attribute namespaces need it), the TOKENIZER's maximal munch fuses adjacent colons before the parser can ever see two. ✔MEASURED on the real DSS binary through `c-subset.lang.json`: `(\"\" ::: \"memory\")` reaches the parser as ColonColonOp + Colon, and `(\"\" :: \"r\"(x))` as a SINGLE ColonColonOp. Four `{optional}` slots hung off this rule would therefore be UNIMPLEMENTABLE, not merely awkward — for `::` there is no second separator token left to open the next slot. The tail models the BOUNDARY instead: a `sectionSeparator` advances ONE section, a `sectionSeparatorFused` advances TWO at once and forces the section between them EMPTY by construction. Every arm is FIRST-disjoint (the two are distinct KINDS), so nothing here is speculative and the host's statement-level direct descent is untouched.\n★★ EVERY FUSED ARM IS ITS OWN NAMED RULE (asmInputsTailFused / asmClobbersTailFused / asmLabelsTailFused) — an independent design-audit finding, not a style preference. A named rule mints a CST node, so 'which section is this, and was it opened at all?' is a pure RuleId question. With inline `{sequence}` arms a fused jump into an EMPTY label list would leave NO node evidence, making `(\"\" ::::)` structurally indistinguishable from the valid `(\"\" : : : )` — and the 4-colon no-goto forms are ✔MEASURED REJECTED by gcc 13.3.0, clang 18.1.3 and clang 19.1.1 alike (`expected ')' before '::'`), so they would have COMPILED CLEAN. The grammar carries three extra rules so the semantic tier never has to guess from tokens.\n★ THE QUALIFIER RUN IS ORDER-FREE: `{repeat {alt: volatileQualifier|inlineQualifier|gotoQualifier}}`, not a fixed sequence of optionals. ✔MEASURED unanimous across the three references: ANY order and ANY subset of the three is accepted, and `const` is rejected BY NAME (`not a valid 'asm' qualifier`) — i.e. the set is CLOSED and the order is FREE, which is exactly what a repeat-over-alt expresses and what a fixed sequence of optionals cannot. Shipped precedent for this exact construct: `c-subset.lang.json`'s `arrayDeclSuffix` (C99's `int a[static const 10]`). `volatile` is SEMANTICALLY INERT for a no-output asm (implicitly volatile — GCC 6.47.2.1); `goto` is NOT inert — `semantics.inlineAsm.gotoQualifierToken` names its TOKEN role so the labels cross-check stays config-driven.\n★ THE OPERAND VALUE IS `operandExpr` — a FULL expression hole, not an assignment-expression one; asmOperand's $comment carries the measurement.\n⚠ WHAT P1 IS NOT, stated so the widened grammar is not read as widened SUPPORT. It is not codegen: an asm with a NON-EMPTY template still fails loud S_InlineAsmNonEmptyTemplate (D-CSUBSET-INLINE-ASM-TEXT), because real per-target instructions cannot yet be emitted and lowering them to a no-op barrier would be a MISCOMPILE. The empty-template form lowers to MirOpcode::CompilerBarrier via this document's own `hirLowering` asmStmt → InlineAsm row.",
      "sequence": [
        "asmKeyword",
        { "repeat": { "alt": [ "volatileQualifier", "inlineQualifier", "gotoQualifier" ] } },
        "argsOpen", "templateText",
        { "optional": "asmOutputsTail" },
        "argsClose", "statementEnd"
      ]
    },

    "asmOutputsTail": {
      "$comment": "FC17.9(i) P1 (D-CSUBSET-INLINE-ASM-OPERANDS): the FIRST section BOUNDARY after the template — `: <outputs>` — and the head of the boundary CHAIN that models GNU's four optional sections. Two FIRST-disjoint arms: a `sectionSeparator` OPENS the output section here (and `{optional asmInputsTail}` chains the next boundary on), while a `sectionSeparatorFused` means the tokenizer already FUSED this boundary with the next one, so the output section is EMPTY BY CONSTRUCTION and control jumps straight to the named `asmInputsTailFused`. The arms can never both match — the two are distinct token KINDS, not one-then-another characters — so the alt is FIRST-disjoint and NON-speculative, the genericAssoc discipline. The whole rule sits under an `{optional}` in asmStmt, which is what keeps the section-less barrier `__asm__ (\"\")` parsing with an absent tail and zero new nodes.",
      "alt": [
        { "sequence": [ "sectionSeparator", { "optional": "asmOperandList" }, { "optional": "asmInputsTail" } ] },
        "asmInputsTailFused"
      ]
    },

    "asmInputsTailFused": {
      "$comment": "FC17.9(i) P1: the FUSED outputs→inputs boundary — ONE `::` token standing for TWO section boundaries at once, so the section BETWEEN them (outputs) is empty. ★ IT IS A NAMED RULE, NOT AN INLINE `{sequence}` ARM, AND THAT IS LOAD-BEARING: a named rule mints a CST node, so `was this section reached by a fused jump?` becomes a pure RuleId question the semantic tier answers with no token archaeology. Inline it and the fused jump leaves NO node evidence — asmLabelsTailFused's $comment carries the concrete form that would then have compiled CLEAN. ✔MEASURED on the real DSS binary (not inferred from a token table): `__asm__ (\"\" :: \"r\"(x))` arrives at the parser as a SINGLE fused token — `::` is a real token in any host that spells the C23 attribute-namespace separator, and maximal munch fuses the pair in the TOKENIZER, before the parser can ever see two. That is the whole reason a slot-per-section grammar (four `{optional}` slots hanging off asmStmt) cannot express this form: there is no second separator token left to open the inputs slot.",
      "sequence": [ "sectionSeparatorFused", { "optional": "asmOperandList" }, { "optional": "asmClobbersTail" } ]
    },

    "asmInputsTail": {
      "$comment": "FC17.9(i) P1: the INPUTS section boundary — asmOutputsTail's shape one section further along. `sectionSeparator` opens the input operand list and chains `{optional asmClobbersTail}`; a `sectionSeparatorFused` fuses inputs→clobbers (leaving inputs empty) through the named asmClobbersTailFused. Same FIRST-disjoint, non-speculative alt.",
      "alt": [
        { "sequence": [ "sectionSeparator", { "optional": "asmOperandList" }, { "optional": "asmClobbersTail" } ] },
        "asmClobbersTailFused"
      ]
    },

    "asmClobbersTailFused": {
      "$comment": "FC17.9(i) P1: the FUSED inputs→clobbers boundary — a `::` where an inputs section would have opened, so inputs is empty and the clobber list follows immediately. Named for the same node-evidence reason as asmInputsTailFused. ✔MEASURED on the real DSS binary: `__asm__ (\"\" ::: \"memory\")` — the canonical GCC full-memory barrier, and the spelling real headers write — arrives as ONE fused token + ONE plain separator, NOT as three separators: the first two colons fuse, the third stays separate. A grammar that COUNTED colons would section that input wrongly and silently; a grammar that models BOUNDARIES lands it exactly (outputs empty, inputs empty, clobbers = [\"memory\"]).",
      "sequence": [ "sectionSeparatorFused", { "optional": "asmClobberList" }, { "optional": "asmLabelsTail" } ]
    },

    "asmClobbersTail": {
      "$comment": "FC17.9(i) P1: the CLOBBERS section boundary — `sectionSeparator` + `{optional asmClobberList}` + the optional labels tail, or the fused `sectionSeparatorFused` arm (clobbers→labels) via the named asmLabelsTailFused.",
      "alt": [
        { "sequence": [ "sectionSeparator", { "optional": "asmClobberList" }, { "optional": "asmLabelsTail" } ] },
        "asmLabelsTailFused"
      ]
    },

    "asmLabelsTailFused": {
      "$comment": "FC17.9(i) P1: the FUSED clobbers→labels boundary — the LAST fused arm, and the one that proves why EVERY fused arm must be a named rule. ✔MEASURED, all three references unanimous (gcc 13.3.0, clang 18.1.3, clang 19.1.1; `-std=gnu17 -fsyntax-only`, with matched positive controls): the 4-colon no-goto forms such as `__asm__ (\"\" ::::);` are REJECTED — `expected ')' before '::'`. ★ THE HOLE A NAMELESS ARM WOULD LEAVE: if this arm were an inline `{sequence}`, a fused jump into an EMPTY goto-label list would add no node and no distinguishing child, so `(\"\" ::::)` would be structurally INDISTINGUISHABLE from the perfectly valid `(\"\" : : : )` — and would have compiled clean, which is exactly the accept-and-ignore this project forbids. As a NAMED rule the node IS the evidence: an asmLabelsTailFused present with no asmGotoLabelList child is a labels section that was opened and left empty, and the semantic tier refuses it by name. This is a case where the grammar shape exists to make a SEMANTIC question answerable, not to accept more input.",
      "sequence": [ "sectionSeparatorFused", { "optional": "asmGotoLabelList" } ]
    },

    "asmLabelsTail": {
      "$comment": "FC17.9(i) P1: the GOTO-LABELS section boundary — `: <label, label…>`, the FOURTH and last section, meaningful only on an `asm goto` (GCC 6.47.2.7). NO fused arm follows it: there is no fifth section, so a fused separator in this position simply does not match and fails loud at the closing paren — the same refusal the reference compilers give.",
      "sequence": [ "sectionSeparator", { "optional": "asmGotoLabelList" } ]
    },

    "asmOperandList": {
      "$comment": "FC17.9(i) P1: a comma-separated run of ≥1 asmOperand — the shape BOTH the output and the input section use. GCC 6.47.2.2/6.47.2.3 give the two identical SYNTAX and differ only in the constraint string's leading modifier (`=`/`+` vs none), which is a SEMANTIC distinction, so one rule serves both and the section a list belongs to is identified by WHICH boundary rule owns it — never by re-inspecting the string. Non-nullable by construction: an EMPTY section is expressed by omitting this rule under its `{optional}`, never by matching it empty, so `has an operand list` and `has a non-empty operand list` are the same question.",
      "sequence": [ "asmOperand", { "repeat": { "sequence": [ "operandSeparator", "asmOperand" ] } } ]
    },

    "asmOperand": {
      "$comment": "FC17.9(i) P1: ONE GNU asm operand — `[symbolic-name] \"constraint\" ( expression )`. The leading `[name]` is GCC 6.47.2.3's SYMBOLIC OPERAND NAME (`[result] \"=r\"(x)`, written `%[result]` in the template); it is `{optional}` and FIRST-disjoint from the constraint (`symbolicNameOpen` vs whatever opens `templateText`), so no speculation is needed. The constraint is `templateText` — the same string-payload hole the template uses, so adjacent concatenation and literal prefixes decode through the HOST's one string chokepoint rather than a second decoder here. ★ THE VALUE IS A FULL EXPRESSION (`operandExpr`), NOT AN ASSIGNMENT-EXPRESSION, AND THAT IS MEASURED RATHER THAN ASSUMED: `__asm__ (\"\" : : \"r\"(a, b))` is ACCEPTED by gcc 13.3.0, clang 18.1.3 and clang 19.1.1 — the COMMA OPERATOR is legal inside an operand's parens. A host binding this hole to its assignment-expression rule (which exists precisely to STOP before the comma operator) would fail loud on a form all three references take. This is the OPPOSITE call from an argument list — where the comma IS the separator — and the two are consistent, not contradictory: here the separator comma sits OUTSIDE the operand's own parens, so the comma-operator reading and the separator reading can never compete for the same token.",
      "sequence": [
        { "optional": { "sequence": [ "symbolicNameOpen", "symbolName", "symbolicNameClose" ] } },
        "templateText", "argsOpen", "operandExpr", "argsClose"
      ]
    },

    "asmClobberList": {
      "$comment": "FC17.9(i) P1: a comma-separated run of ≥1 CLOBBER string (`\"memory\"`, `\"cc\"`, a register name — GCC 6.47.2.5). `templateText` for the same chokepoint reason as the operand constraint. WHICH clobber names are legal is a per-TARGET question (a register name on one processor is nothing on another), so it is deliberately NOT a grammar vocabulary — putting a register list in this file would be exactly the target identity leak the bar forbids; the check belongs to the target-keyed tier (`.target.json`, plan 29 P3).",
      "sequence": [ "templateText", { "repeat": { "sequence": [ "operandSeparator", "templateText" ] } } ]
    },

    "asmGotoLabelList": {
      "$comment": "FC17.9(i) P1: a comma-separated run of ≥1 goto-target LABEL (GCC 6.47.2.7 `asm goto`). Plain `symbolName`s — in an embedded host they name ordinary labels in the enclosing function and resolve through the SAME label machinery the host's own `goto` already uses, so the parser needs no new vocabulary and no new scope. The two cross-checks — a labels section WITHOUT the `goto` qualifier, and the `goto` qualifier WITHOUT a labels section — are SEMANTIC refusals, not grammatical ones; `semantics.inlineAsm.gotoQualifierToken` names the qualifier role so that check stays config-driven and never hard-codes a spelling.",
      "sequence": [ "symbolName", { "repeat": { "sequence": [ "operandSeparator", "symbolName" ] } } ]
    }
  },

  "semantics": {
    "$inlineAsmComment": "FC17.9(i) (D-CSUBSET-INLINE-ASM, C23 6.8 / GNU 6.47), P1+P2 2026-08-12: the inline-asm SEMANTIC surface. ★★ IT LIVES IN THIS DOCUMENT, NOT IN THE HOST'S, AND THAT IS THE MECHANISM'S POINT. The refusal 'extended asm is not yet supported' is a property of the ASM LANGUAGE's implementation maturity, not of C; a host that references asm inherits it FOR FREE, and a SECOND host inherits it with zero new configuration. The loader merges this block into the host's `semantics` key-wise and REFUSES a collision in either direction — a host may not silently weaken a refusal it did not write.\n★ THE SHAPE IS A NESTED OBJECT (the `inline`/`alignas`/`packed`/`generic` precedent) because the semantic tier needs to name TWELVE grammar landmarks rather than one, and twelve flat sibling keys would have been twelve INDEPENDENTLY OMITTABLE knobs. ⚠ ALL KEYS ARE REQUIRED WHEN THE OBJECT IS PRESENT — a PARTIAL `inlineAsm` declaration is a LOAD ERROR, never a silent half-configuration. A missing `labelsTailFusedRule` would otherwise mean the `asm goto` empty-label form quietly stops being checked and starts compiling clean, which is precisely the accept-and-ignore this project forbids. The object being ABSENT is the OTHER, legal meaning: the language has no inline-asm surface at all (toy/tsql reference nothing) and none of these checks ever runs.\n`rule` = asmStmt. `templateRule` names the string-payload rule the template is captured as — a HOLE (`templateText`), so it resolves to whatever the host bound; Pass 2 decodes it through that host's own adjacent-string chokepoint, and a non-empty / whitespace-only / malformed-escape template still fails loud S_InlineAsmNonEmptyTemplate (real per-target asm TEXT remains the D-CSUBSET-INLINE-ASM-TEXT deferral — P1 parses the extended form, it does not emit it).\n★ THE TAIL RULES COME IN PAIRS, AND BOTH MEMBERS MUST BE NAMED. `outputsTailRule`/`inputsTailRule`/`clobbersTailRule`/`labelsTailRule` are the sections opened by a plain separator; `inputsTailFusedRule`/`clobbersTailFusedRule`/`labelsTailFusedRule` are the sections reached by a FUSED one. The fused forms are not a stylistic duplicate: `::` is one token by maximal munch (✔MEASURED — see asmStmt's $comment), so a fused jump is the ONLY way to express an empty leading section, and naming its rule is what lets this tier answer 'which section does this operand list belong to?' and 'was a section opened and left EMPTY?' by RuleId alone, with no token archaeology and no colon counting.\n`operandListRule`/`clobberListRule`/`gotoLabelListRule` identify the three payload shapes. Each is non-nullable by construction, so PRESENT and NON-EMPTY are the same question — an empty section is an absent list node, never a matched-empty one. `gotoQualifierToken` names the `goto` qualifier's TOKEN ROLE (a hole — never a spelling) so the two cross-checks stay config-driven; a hard-coded spelling here would leak one host's vocabulary into every other.",
    "inlineAsm": {
      "rule": "asmStmt",
      "templateRule": "templateText",
      "outputsTailRule": "asmOutputsTail",
      "inputsTailRule": "asmInputsTail",
      "inputsTailFusedRule": "asmInputsTailFused",
      "clobbersTailRule": "asmClobbersTail",
      "clobbersTailFusedRule": "asmClobbersTailFused",
      "labelsTailRule": "asmLabelsTail",
      "labelsTailFusedRule": "asmLabelsTailFused",
      "operandListRule": "asmOperandList",
      "clobberListRule": "asmClobberList",
      "gotoLabelListRule": "asmGotoLabelList",
      "gotoQualifierToken": "gotoQualifier"
    }
  },

  "pipelineEntry": {
    "$comment": "★★★ WHICH TIER EACH CONSTRUCT ENTERS THE PIPELINE AT (plan 29; operator decision 2026-08-12). Not every construct starts at the top, and for the assembly language that is the ordinary case rather than an exception.\n★★ THIS IS PER-CONSTRUCT, NOT PER-LANGUAGE, AND THE DISTINCTION IS THE WHOLE DESIGN. Plan 29 §1 rejected a language-wide `skipToAssembler` flag and gave the reason precisely: *'the pipeline entry point is a property of the CONSTRUCT, never of the language.'* The embedded `__asm__(…)` proves it — its template text is opaque and belongs to the assembler, but its operand bindings and clobber set are HOST lvalues that MUST survive as IR the register allocator can see. Skip the whole construct to the assembler and the allocator keeps a value live in a register the asm clobbers: corrupted, with no diagnostic. So `asmStmt` enters at `hir` as a payload-carrying node, exactly like every other statement, and the tier boundary runs THROUGH it rather than around it. Now that this language has TWO entry points, a per-entry-point row is what that sentence prescribes.\n★ THE DEFAULT IS THE FULL PIPELINE, AND IT IS OPERATOR-SPECIFIED: a language that declares no `pipelineEntry` block — and a rule absent from `byRule` in a language that does — follows CST→HIR→MIR→LIR exactly as today. `c-subset`, `toy` and `tsql-subset` declare nothing and are unchanged. ⚠ THAT IS A SILENT DEFAULT, WHICH THIS PROJECT NORMALLY REFUSES, AND THREE CLOSED KEY SETS ARE THE ONLY REASON IT IS SAFE HERE: `pipelineEntry` is in the `.lang.json` closed ROOT key set (so `pipelineEntries` is a load error, not a dropped facet); this block's keys are closed (so a typo'd `byRule` is a load error, not 'no overrides'); and `tier` is the closed set {hir, mir, lir} (so `Lir` or `lira` is a load error, not a fallback). DO NOT 'simplify' any of the three — drop one and a misspelling routes hand-written assembly through MIR and the OPTIMIZER, a silent miscompile of the programmer's intent, which is the exact failure this facet exists to prevent. A row naming a rule that does not exist is likewise a LOAD ERROR: an entry declaration with no subject silently does nothing.\n⚠ THERE IS DELIBERATELY NO `optimize: false` SIBLING KEY, AND ADDING ONE WOULD BE A REGRESSION. Entering at LIR means no MIR module exists, so MIR passes cannot run BY CONSTRUCTION — 'no optimization' is a CONSEQUENCE of the tier, not a second switch. Two sources of truth for one property is how they drift apart, and the drift would be silent in the dangerous direction.\n★ THE PLANNED SECOND ROW, AND WHY — `{ \"rule\": \"asmUnit\", \"tier\": \"lir\" }`, landing at P2.5 with `asmUnit` itself. It is NOT written here today on purpose: declaring a pipeline entry for a rule that does not exist would be an unconsumed declaration asserting a capability we do not have (and is a load error by the rule above). The reasoning for it, recorded now so P2.5 does not re-derive it: (a) assembly IS the post-register-allocation, physical-register-only machine tier that LIR already models, so a `.s` translation unit maps onto LIR essentially 1:1 — deriving HIR and MIR for it would be re-deriving what the programmer already wrote; (b) OPTIMIZING hand-written assembly is a miscompile of intent — you cannot know why a human chose a given instruction, order or register, so 'improving' it is changing a decision you cannot read.\n⚠ ONE THING P2.5 MUST SETTLE, RAISED HERE WITH THE EVIDENCE RATHER THAN DISCOVERED LATER: `lir` is currently ONE name for two distinct points. ✔MEASURED in `src/program/compile_pipeline.cpp` — `lowerMirModuleToAssembly` takes a `Mir&` and produces VREG-based LIR at its step 4, after which come wide-call materialization, liveness, REGISTER ALLOCATION, and only then encoding; plan 13 §0 records that `src/asm/` consumes LIR that is already post-regalloc and physical-register-only. A hand-written `.s` names PHYSICAL registers, so it must enter AFTER allocation — running regalloc over it would rewrite the programmer's register choices, which is the same miscompile-of-intent as optimizing it. So the `asmUnit` row means 'post-regalloc LIR', and whether that is spelled `lir` (with the consumer knowing an LIR entry is physical) or split into a fourth tier name is a decision for P2.5, not a detail to settle silently.",
    "byRule": [
      { "rule": "asmStmt", "tier": "hir" }
    ]
  },

  "hirLowering": {
    "ruleMappings": [
      { "rule": "asmStmt", "hirKind": "InlineAsm",
        "$comment": "FC17.9(i) (D-CSUBSET-INLINE-ASM): the empty-template `asm [volatile|inline|goto]… (\"\")` lowers to a core InlineAsm leaf → MirOpcode::CompilerBarrier (HIR→MIR). ★ THIS ROW TRAVELS WITH THE GRAMMAR: the loader CONCATENATES a referenced document's `ruleMappings` onto the host's (element-wise, because the block is a collection of independent rule-scoped rows) and refuses a duplicate `rule` across the join — so a host inherits the lowering without writing it, and cannot shadow it by accident. UNCONDITIONAL: the gate lives in the semantic tier (semantics.inlineAsm → S_InlineAsmNonEmptyTemplate and the P1 extended-form refusals), which aborts before codegen on any asm this compiler cannot yet emit, so a rejected asm's MIR is never emitted — clean separation, mirroring staticAssertDecl (semantic gates the condition; the lowering row is unconditional). ⚠ P1 widened what PARSES (operand/clobber sections and `asm goto` labels) WITHOUT widening what LOWERS: this row still describes the empty-template barrier ONLY, and the semantic tier is the thing that keeps that true." }
    ]
  }
}

{
  "header": "string.h",
  "standard": "c89",
  "$comment": "★ c82 (shell.c): `typedefs`/`constants` re-provide size_t + NULL — C89 7.21.1 REQUIRES string.h to provide both without stddef.h; widths/values identical to stddef.json, first-wins dedup makes the multi-header provision moot (stdio/stdlib/time re-provide too, exactly like real libc headers). strdup is POSIX (C23 adopted it) → per-symbol [elf,macho]; the msvcrt spelling is _strdup (the plain-name legacy alias is unverified) — ungate when a pe consumer verifies it. ★ TF-C49 (SQLITE_THREADSAFE=0): `strerror` is C89 7.11.6.2 (`char *strerror(int)`) exported by libc on ALL formats (glibc/msvcrt/libSystem `T`) → NO format gate. SQLite's unixLogErrorAtLine takes the `#else` (non-threadsafe) branch `zErr = strerror(iErrno)` when SQLITE_THREADSAFE=0 (the default THREADSAFE build takes the `zErr = \"\"` branch → references NEITHER strerror nor strerror_r, why the default corpus compiled clean without it). strerror_r stays UN-shipped: its `#if SQLITE_THREADSAFE && defined(HAVE_STRERROR_R)` branch needs HAVE_STRERROR_R, an autoconf probe DSS never defines → no consumer (would be a speculative build). ★ D-CSUBSET-DARWIN-BSD-SYMBOL-CLUSTER: strlcpy + strlcat — the BSD bounded copy/concat pair, SDK _string.h:201-202 `size_t strlcpy(char *dst, const char *src, size_t size)` (strlcat same shape), exported by libSystem (link-verified on this machine). Per-symbol [macho] by the no-over-ship rule: every measured consumer lives in sqlite os_unix.c's `#if defined(__APPLE__) && SQLITE_ENABLE_LOCKING_STYLE` proxy/AFP-locking region — strlcpy ×7 (first: proxyGetLockPath os_unix.c:7434) + strlcat ×2 (os_unix.c:7443) — and glibc only gained the pair in 2.38, so an elf arm would also be an unconsumed portability claim (the strdup gate's inverse). The descriptor SHADOWS the real _string.h totally (angle-include never reads the SDK text), so each name is S0001 without its row. RETURN is the BSD semantics — the length of the string it TRIED to create (strlen(src); resp. initial dst length + strlen(src)) — NOT the dest pointer like strcpy/strcat; the u64 result is load-bearing and pinned by tests/ffi RealStringJsonStrlcpyStrlcatMachoOnly + the shipped_strlcpy_strlcat_macho corpus exit arithmetic (5*8+2=42 folds only if both returns cross the FFI correctly).",
  "library": { "pe": "msvcrt.dll", "elf": "libc.so.6", "macho": "/usr/lib/libSystem.B.dylib" },
  "typedefs": [
    { "name": "size_t", "variants": [
        { "when": { "dataModel": "LP64" },  "type": "u64 \"unsigned long\"" },
        { "when": { "dataModel": "LLP64" }, "type": "u64 \"unsigned long long\"" }
    ] }
  ],
  "constants": [
    { "name": "NULL", "value": 0, "type": "i64" }
  ],
  "symbols": [
    { "name": "strdup",  "signature": "fn(ptr<char>) -> ptr<char>",            "kind": "function", "linkage": "external", "availableObjectFormats": ["elf", "macho"] },
    { "name": "strlcpy", "signature": "fn(ptr<char>, ptr<char>, u64) -> u64",  "kind": "function", "linkage": "external", "availableObjectFormats": ["macho"] },
    { "name": "strlcat", "signature": "fn(ptr<char>, ptr<char>, u64) -> u64",  "kind": "function", "linkage": "external", "availableObjectFormats": ["macho"] },
    { "name": "strlen",  "signature": "fn(ptr<char>) -> u64",                  "kind": "function", "linkage": "external" },
    { "name": "strcmp",  "signature": "fn(ptr<char>, ptr<char>) -> i32",       "kind": "function", "linkage": "external" },
    { "name": "strncmp", "signature": "fn(ptr<char>, ptr<char>, u64) -> i32",  "kind": "function", "linkage": "external" },
    { "name": "strcpy",  "signature": "fn(ptr<char>, ptr<char>) -> ptr<char>", "kind": "function", "linkage": "external" },
    { "name": "strncpy", "signature": "fn(ptr<char>, ptr<char>, u64) -> ptr<char>", "kind": "function", "linkage": "external" },
    { "name": "strcat",  "signature": "fn(ptr<char>, ptr<char>) -> ptr<char>", "kind": "function", "linkage": "external" },
    { "name": "strncat", "signature": "fn(ptr<char>, ptr<char>, u64) -> ptr<char>", "kind": "function", "linkage": "external" },
    { "name": "strchr",  "signature": "fn(ptr<char>, i32) -> ptr<char>",       "kind": "function", "linkage": "external" },
    { "name": "strrchr", "signature": "fn(ptr<char>, i32) -> ptr<char>",       "kind": "function", "linkage": "external" },
    { "name": "strstr",  "signature": "fn(ptr<char>, ptr<char>) -> ptr<char>", "kind": "function", "linkage": "external" },
    { "name": "memcpy",  "signature": "fn(ptr<void>, ptr<void>, u64) -> ptr<void>", "kind": "function", "linkage": "external" },
    { "name": "memmove", "signature": "fn(ptr<void>, ptr<void>, u64) -> ptr<void>", "kind": "function", "linkage": "external" },
    { "name": "memset",  "signature": "fn(ptr<void>, i32, u64) -> ptr<void>",  "kind": "function", "linkage": "external" },
    { "name": "memcmp",  "signature": "fn(ptr<void>, ptr<void>, u64) -> i32",  "kind": "function", "linkage": "external" },
    { "name": "memchr",  "signature": "fn(ptr<void>, i32, u64) -> ptr<void>",  "kind": "function", "linkage": "external" },
    { "name": "strspn",  "signature": "fn(ptr<char>, ptr<char>) -> u64",       "kind": "function", "linkage": "external" },
    { "name": "strcspn", "signature": "fn(ptr<char>, ptr<char>) -> u64",       "kind": "function", "linkage": "external" },
    { "name": "strerror","signature": "fn(i32) -> ptr<char>",                  "kind": "function", "linkage": "external" },
    { "name": "_strdup", "signature": "fn(ptr<char>) -> ptr<char>", "kind": "function", "linkage": "external", "availableObjectFormats": ["pe"] }
  ]
}

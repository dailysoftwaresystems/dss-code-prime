{
  "$comment": "ELF64 SHARED LIBRARY (ET_DYN, .so) for x86_64 Linux -- c150, the D-LK1-4 shared-library half (plan 14 / roadmap A1 part 1). Consumed by `src/link/format/elf.cpp` via the format-blind linker engine: `elf.type = dyn` routes to the SAME `encodeElfExecDynamic` substrate the exec image uses, with the dyn divergences (e_type=ET_DYN, e_entry=0, no PT_PHDR/PT_INTERP/.interp, base-0 VAs the loader slides, real-named .dynsym EXPORTS findable via .hash, R_X86_64_RELATIVE for internal absolute data slots, got-indirect extern data). A FOREIGN toolchain consumes the output: `gcc main.c -L. -lfoo` links against the DSS-emitted libfoo.so and the program runs. Differs from elf64-x86_64-linux-exec.format.json in: `elf.type` (dyn vs exec), NO `interpreter` (a .so is loaded by an already-running ld.so; validate() rejects one here), text `virtualAddress` = pageAlign (base-0 image by construction -- validate() pins the equality), NO `entryPoint`/`processExit`/`entryCallingConvention`/`processArgs` (a .so has no process entry; the linker synthesizes no trampoline -- the PIE-executable follow-up is exactly ET_DYN plus that entry machinery), `dataImportBinding` got-indirect (copy relocations are exec-only ELF), NO `tlsAccess` and NO tdata/tbss rows (TLS in a .so needs the general-dynamic model -- D-LK-DYN-TLS-MODEL; every thread_local fails loud at the MIR->LIR gate / linker section gate), `artifactProfiles` [\"lib\"], and an OPTIONAL `elf.soname` knob (absent here: per-artifact identity does not belong in a shipped format schema; DT_SONAME is emitted only when a schema declares it -- absent matches `gcc -shared` without -Wl,-soname, where the consumer's linker records the filename).",

  "dssObjectFormatVersion": 1,

  "$stackReserveUnsupportedReasonComment": "D-SQLITE-PE64-FULL-TIER-STACK-DEPTH: ELF has NO image field for a stack SIZE on ANY flavor -- PT_GNU_STACK encodes stack EXECUTABILITY, not size. The kernel sizes the initial thread's stack from the process limit (`ulimit -s` / setrlimit RLIMIT_STACK) and further threads from pthread_attr_setstacksize, so nothing the linker writes can change it. A `stackReserve` / `--stack-reserve` request is therefore REFUSED with that remedy rather than silently dropped -- which is also why the sqlite full tier passes on Linux (8 MiB by default) and overflowed on Windows at 1 MiB.",

  "stackReserveUnsupportedReason": "runtime-controlled",

  "format": {
    "name": "elf64-x86_64-linux-dyn",
    "version": "1.0",
    "kind": "elf"
  },

  "$dataModelComment": "Same LP64 identity as the exec/rel siblings (64-bit Linux).",
  "dataModel": "LP64",
  "$headerNameMatchingComment": "D-PP-HEADER-CASE-INSENSITIVE-PE: Linux/POSIX header names are matched BYTE-EXACT -- `#include <Stdio.h>` does NOT name `stdio.h`, and a conforming toolchain must reject it. REQUIRED closed-enum field: the loader rejects a missing or unknown value. DSS performs the comparison ITSELF (core/types/include_path_resolve.cpp) rather than calling fs::exists and inheriting the BUILD HOST's convention -- without that, an elf target built on Windows/macOS silently ACCEPTED `<Stdio.h>` and emitted a real object (MEASURED 2026-08-04).",
  "headerNameMatching": "case-sensitive",
  "$cSymbolDecorationComment": "D-FFI-CMANGLING-RULE-NOT-CONFIG-DRIVEN: System V / ELF applies NO decoration to a C identifier -- the linker-visible name IS the identifier. MEASURED 2026-08-06 (`nm -D /lib/x86_64-linux-gnu/libc.so.6`, WSL Ubuntu): glibc exports `exit@@GLIBC_2.2.5`, `printf@@GLIBC_2.2.5` and `fstat64@@GLIBC_2.33` under exactly their C spellings. THE SAME COMMAND IS ALSO WHY `none` IS A POSITIVE ANSWER AND NOT AN ABSENCE: `_exit@@GLIBC_2.2.5` is exported TOO and is a DIFFERENT FUNCTION -- POSIX `_exit(2)`, which does NOT flush stdio -- so a format that wrongly claimed `leading-underscore` would not merely produce an ugly name, it would silently bind every `exit` to the non-flushing wrapper and lose buffered output. This key is the single owner of a fact that previously had TWO: the closed C++ table `kCManglingRules` (src/ffi/mangling/c_mangle.cpp, keyed on ObjectFormatKind) and, redundantly, a literal encoding in the descriptors themselves -- the macho64-*-exec files spell `processExit.importMangledName` as `_exit` while their elf/pe siblings spell it `exit`. Two owners in two languages, with nothing forcing them to agree. RED-ON-DISABLE, stated precisely for the tier that exists today (MEASURED 2026-08-06 by actually removing the key from a shipped file and running the suite): DELETE this key and the format is REFUSED AT LOAD -- TWO errors at /cSymbolDecoration, one C_MissingField from the loader and one from ObjectFormatData::validate(), which loadFromText runs before returning -- so every test and example that loads this format goes red, naming the file. CHANGING the value is now an equally REAL lever, and this sentence was corrected the moment it became true: before step C4 the engine still read a C++ table and the value here changed no emitted binary. C4 landed (same cycle, TF-C122), and the effect is MEASURED 2026-08-06 -- flipping macho64-arm64-darwin-exec from `leading-underscore` to `none` and RECOMPILING NOTHING made the engine emit `printf` where it had emitted `_printf`, reddening tests/ffi/test_c_mangle.cpp's SchemeIsReadFromTheShippedSchemaNotACppTable. That is the whole point of the anchor: the answer lives in this file, so editing this file changes what the compiler emits. Two further pins guard the value: tests/link/test_c_symbol_decoration.cpp asserts the per-kind answer across all 24 shipped files and cross-checks it against this family's own processExit.importMangledName.",
  "cSymbolDecoration": { "scheme": "none" },
  "$cCallingConventionComment": "D-FFI-ABI-CATALOG-SELECTS-CALLING-CONVENTION-BY-FORMAT-IDENTITY: the platform C ABI for code compiled into this format, NAMING a row in the paired target's callingConventions[] array. This selection used to be re-derived in C++ by `kAbiCatalog`, a closed table keyed on (target name, ObjectFormatKind) — while this very string was already written here. Two owners of one fact; the table is deleted and this is the single owner. System V AMD64 (`rdi,rsi,rdx,rcx,r8,r9`).",
  "cCallingConvention": { "convention": "sysv_amd64" },
  "$outputExtensionComment": "D-PROGRAM-TIER-RETAINS-FORMAT-IDENTITY-BRANCHES: the extension the driver appends to an artifact this format produces. This used to be derived in C++ by `program/target_spec.cpp::outputExtensionFor`, a five-arm `switch (fmt.kind())` with a NESTED per-flavor switch inside each arm -- seven identity mentions computing a NAMING fact, which is not an engine behaviour at all. The nest existed because one ObjectFormatKind spans several artifact flavors; a DOCUMENT is exactly one flavor, so the whole derivation collapses to this one string. REQUIRED on every format. An EMPTY value is a DECLARED answer and not an omission -- a Unix executable carries no extension -- which is why the in-memory field is an `optional` and the loader's required check asks `contains`, never `empty`.",
  "outputExtension": ".so",
  "$archiveFlavorComment": "D-PROGRAM-TIER-RETAINS-FORMAT-IDENTITY-BRANCHES: WHICH `ar` variant this format's ECOSYSTEM uses. Declared on every native format, not only the `container: archive` ones, because that is what the deleted C++ derivation answered: `(format.kind() == ObjectFormatKind::Pe) ? Coff : SysV` was handed WHATEVER format the caller had, and `linkAndWriteStaticArchive` is reachable with the RELOCATABLE format of an ecosystem as well as its archive one (MEASURED -- restricting the key to archives reddened link/test_ar_writer and program/test_static_link). NOT cosmetic: the coff flavor carries the little-endian second linker member link.exe requires to resolve members at all. A closed VERB rather than an open name, because an archive layout is an ENGINE capability -- only variants `writeArArchive` realizes can be honored.",
  "archiveFlavor": "sysv",
  "$targetArchComment": "D-PROGRAM-TIER-RETAINS-FORMAT-IDENTITY-BRANCHES: the .target.json NAME this format is for. `crossValidateTargetFormat` compares it against TargetSchema::name() -- two DECLARED NAMES -- where it used to run a six-arm `switch (format.kind())` reading a DIFFERENT field per identity (elf.machine / pe.machine / macho.cputype) and compare each against a hardcoded C++ table, kTargetArchMachineCodes, keyed on (target name x format kind). THAT TABLE IS DELETED, and the argument is ONE OWNER rather than tidiness: `arm64 is EM_AARCH64 = 183` had two owners -- this document's own machine field and that C++ row -- with nothing forcing them to agree, which is the identical shape as kCManglingRules and kAbiCatalog. The number now lives only in the document that emits it, and the pairing question is answered by a name this document states outright. Adding a new arch is now JSON-ONLY.",
  "targetArch": "x86_64",
  "$predefinedMacrosComment": "TF-C97 (D-PP-FORMAT-DATA-MODEL-PREDEFINES): the C-visible face of this file's `dataModel` above, DERIVED FROM THAT VALUE AND NEVER FROM THIS FILE'S NAME -- the loader synthesizes no macro name and the engine performs no format-identity branch; it merges whatever rows are declared here with the language's `preprocess.predefinedMacros` and the target's `predefinedMacros`, applying `availableObjectFormats` exactly once. DECLARED ON THE FORMAT because the data model is per-(OS x format), not per-CPU: the same x86_64 target is LP64 here and LLP64 under pe64-x86_64-windows-*, so a target-side row would be wrong on one of its own formats -- which is exactly what both .target.json files say when they record `__LP64__`/`_LP64` as DELIBERATELY NOT SHIPPED and defer to the format. BOTH spellings, each value 1: MEASURED 2026-07-30 with `/usr/bin/clang -dM -E -x c /dev/null -target <triple>` (Apple clang 21.0.0), __LP64__ and _LP64 are BOTH defined as 1 on arm64-apple-darwin, x86_64-apple-darwin, aarch64-linux-gnu and x86_64-unknown-linux-gnu, and NEITHER is defined on x86_64-pc-windows-msvc. COLLISION POLICY: a name declared by this file AND the language or target config is FATAL (C_ConflictingPredefinedMacro) -- no side silently wins. WHAT IT UNBLOCKS, MEASURED: the macOS SDK gates 234 occurrences across 91 headers on __LP64__, so without these rows DSS compiled the SDK's ILP32 arms while its own widths were LP64 (sizeof(long)==8), and $SDK/usr/include/mach/port.h:113-114 then asserted the user32 struct size 12 instead of user64's 16 -- sqlite mem1.c's three error[S0029]. RED-ON-DISABLE: delete this key and examples/c/predefined_lp64_macho stops exiting 42.",
  "predefinedMacros": [
    { "name": "__LP64__", "kind": "constant", "value": "1", "programRedefinition": "ordinary", "impliedSurface": { "kind": "claims-nothing", "reason": "arch-property" } },
    { "name": "_LP64",    "kind": "constant", "value": "1", "programRedefinition": "ordinary", "impliedSurface": { "kind": "claims-nothing", "reason": "arch-property" } }
  ],


  "$bitFieldStrategyComment": "D-CSUBSET-BITFIELD-ABI-EXACT: SysV/GNU LSB-first bit-field packing, same as the ELF siblings.",
  "bitFieldStrategy": "gnu_packed",
  "$unnamedBitFieldAlignmentComment": "D-CSUBSET-ZERO-WIDTH-BITFIELD-ALIGNMENT: an UNNAMED bit-field contributes NO alignment to the enclosing composite. MEASURED P45 -- `struct { char c; unsigned :0; char d; }` is 5/1 here, against 8/4 on the AAPCS64 ELF formats that share this very same `gnu_packed` strategy.  The zero-width field still BREAKS the packing cursor to its declared type's unit boundary (uncapped by `#pragma pack`); only the alignment contribution is dropped. ~ Declared on the FORMAT and not the target for exactly the reason `bitFieldStrategy` is: one CPU target serves several ABIs. There is NO target-side fallback for this key -- omitting it does not inherit, it leaves the axis undeclared and the layout engine fails loud the moment a zero-width bit-field needs it.",
  "unnamedBitFieldAlignment": "ignored",

  "$externCallDispatchComment": "Same call-site shape as the exec image: the walker synthesizes a PLT stub per extern FUNCTION (jmp through the GOT slot, GLOB_DAT eager-bound) and the call site is a plain direct `call rel32` to the stub -- pc-relative, so the code needs no fixup under the ET_DYN load slide. D-FFI-EXTERN-CALL-DISPATCH.",
  "$runtimeLibrariesComment": "D-CSUBSET-PACKED-ATOMIC-MEMBER: this format's ROLE -> IMAGE table. It did not exist here before P53 because no spine block on this flavor named a role; `atomicsRuntime` below is the first, and the loader REFUSES a row no block names (inert config), so the two land together or not at all.",
  "runtimeLibraries": [
    { "role": "atomicsRuntime", "image": "libatomic.so.1" }
  ],
  "$atomicsRuntimeComment": "D-CSUBSET-PACKED-ATOMIC-MEMBER: the GENERIC C11 atomics entry points an UNDER-ALIGNED `_Atomic` scalar access lowers to a CALL of, and the role naming the image that owns them. ★ THE NAMES ARE DECLARED HERE, ALREADY MANGLED FOR THIS FORMAT, for the reason `processExit.importMangledName` is: the C-level spelling is `__atomic_load`, but the SYMBOL a Mach-O image carries is `___atomic_load` — this format's own `cSymbolDecoration` leading underscore — and a lowerer re-deriving that rule would be the hardcoded-`msvcrt.dll` defect one tier over. ⚠⚠ THE GENERIC, RUNTIME-SIZED ENTRIES, NEVER THE SIZED `__atomic_load_4` FAMILY: ✔MEASURED 2026-09-02 on the native arm64 VPS, the sized entry SIGBUSes on the very access this block routes around (it is an IFUNC resolving to the inline realization), while the generic one dispatches through libatomic's lock table and returns the value — rc 42 against rc 135. A probe that calls the sized entry measures \"no working reference exists\", which is false. WHY A ROW DISTINCT FROM `cLibrary` HERE: ✔MEASURED (`nm -D --defined-only`) on both Linux legs, `libc.so.6` and `libgcc_s.so.1` export ZERO `__atomic_*` symbols while `libatomic.so.1` exports the generic pair as ordinary global text at the default version `@@LIBATOMIC_1.0` (every SIZED variant is an IFUNC) — linking without `-latomic` fails `undefined reference to __atomic_store`, so on ELF this genuinely is a second image. RED-ON-DISABLE: delete this block and an under-aligned `_Atomic` access refuses at compile time under a `traps` target, naming this key.",
  "atomicsRuntime": {
    "role": "atomicsRuntime",
    "loadMangledName": "__atomic_load",
    "storeMangledName": "__atomic_store"
  },
  "externCallDispatch": "direct-plt",
  "$weakDefinitionComment": "D-CONFIG-WEAK-DEFINITION-DIALECT-NOT-DECLARED + D-LK-WEAK-DEFINITION-DIALECT-UNCONSULTED-BY-ELF-AND-MACHO-WRITERS: HOW this format spells a WEAK DEFINITION -- a symbol several translation units may define, of which the linker keeps one. ELF puts the weakness in the SYMBOL's own BINDING field: STB_WEAK (2) in the high nibble of Elf64_Sym.st_info, gABI 4.18. No section flag and no separate per-symbol flag bit is involved, which is exactly what distinguishes `symbol-binding` from COFF's `comdat` (a per-SECTION duplicate-resolution policy) and from Mach-O's `symbol-flag` (N_WEAK_DEF set ALONGSIDE an ordinary binding). Written by `stbForBinding` in src/link/format/elf.cpp. THIS IS A DIALECT ROW, NOT A CAPABILITY FLAG: it says how this format spells something it CAN express, never that it cannot -- see D-LK-PE-ALTERNATENAME-DECLARE-AND-REFUSE for why an implementation gap recorded as a format incapability is a false fact in the place most likely to be trusted later. `elf::encode` READS this key -- ONCE, covering every ELF flavor, because the alias pass reaches stbForBinding on ET_REL, ET_EXEC and ET_DYN alike -- and refuses loud on a dialect it has no encoder for, so the declaration cannot drift away from what is actually emitted. The loader additionally refuses this key at LOAD when the declared dialect is not one the resolved backend's walker writes (ObjectFormatBackend::weakDefinitionDialects), so a mis-declaration fails at its source rather than at the first weak definition.",
  "weakDefinition": { "dialect": "symbol-binding" },

  "$dataImportBindingComment": "c150 (D-LK1-4 + D-LK-EXTERN-DATA-IMPORT): extern library DATA objects bind GOT-INDIRECT in a shared library -- the walker reserves a GOT slot per imported object and emits R_X86_64_GLOB_DAT against it; ld.so writes the object's address into the slot and the c117/c149 GotIndirect lowering (lea-of-slot + deref) reads it. The exec sibling declares the SAME value now: copy-relocation was DELETED from the vocabulary (D-LK-ELF-COPY-RELOC-CLAIMS-ONE-NAME-OF-AN-ALIAS-SET — it claimed one name of an aliased libc object and split it silently), so there is no longer an ELF data-import model that a .so has to be protected from; the loader's closed-enum reject refuses the spelling on ANY flavour.",
  "dataImportBinding": "got-indirect",

  "$supportedDataSectionsComment": "rodata (read-only, R+X PT_LOAD #1 -- reloc-FREE items only: a reloc-bearing rodata item would need a load-time write into a read-only page, D-LK-DYN-RODATA-ITEM-RELOC fails loud), data (mutable initialized globals, R+W PT_LOAD #2), bss (zero-init, NOBITS tail of PT_LOAD #2), relro (reloc-bearing CONST data, c145 -- merged into `.data` by the dynamic arm so its R_X86_64_RELATIVE slots land in writable pages, exactly why gcc places such data in .data.rel.ro). NO tdata/tbss: thread-locals in a shared library need the general-dynamic TLS model (D-LK-DYN-TLS-MODEL, not implemented); the linker's pre-walker gate rejects them loudly by this set's absence.",
  "supportedDataSections": ["rodata", "data", "bss", "relro"],

  "$artifactProfilesComment": "This format SERVES the shared-library profile (plan 06 AP3). The c language declares lib in its own set, so a lib-profile project may select this format; the cli profile is NOT served (a .so is not a runnable console binary). \u2605 `module` JOINED THIS SET 2026-08-15 (plan 06 \u00a75.1 B.12, as CORRECTED): a module IS a library, so it is served by every format that serves `lib` or `staticlib` and by nothing else. `artifactProfile` says what a project builds ITSELF as; `DependencyComposition` says how it is CONSUMED (`module` is SourceMerge \u2014 a consumer takes its SOURCES and ignores its artifact). Those are orthogonal, so a module builds standalone like any other library and the static-vs-dynamic choice is made by the `<target>:<format>` spec the author writes in `targets[]`, exactly as it already is for `lib` vs `staticlib` \u2014 no new rule and no engine change.",
  "artifactProfiles": ["lib", "module"],

  "elf": {
    "$comment": "Elf64_Ehdr identity for a shared library. type = ET_DYN (3). e_entry = 0 (no process entry). class/data/osabi/machine identical to the exec sibling. pageAlign = 4096 (x86_64 Linux 4 KB pages). NO interpreter (validate() rejects one on dyn -- a .so carries no PT_INTERP). NO soname by default (see the top-level $comment). bindNow = true: eager binding, DT_FLAGS_1 = DF_1_NOW + R_X86_64_GLOB_DAT in .rela.dyn -- the same v1 stance as the exec image (lazy .rela.plt/JUMP_SLOT is D-LK6-11).",
    "class": "elf64",
    "data": "lsb",
    "osabi": "sysv",
    "abiVersion": 0,
    "machine": 62,
    "type": "dyn",
    "pageAlign": 4096,
    "bindNow": true
  },
  "$staticInitializersComment": "D-C-GNU-CONSTRUCTOR-ATTRIBUTE-IS-WARNED-AND-IGNORED-NOT-RUN: THIS FORMAT DECLARES NO STATIC-INITIALIZER CHANNEL, AND THE ABSENCE IS THE ANSWER RATHER THAN AN OMISSION -- recorded here so nobody 'completes' the table by adding one.\\nThis is an IMAGE flavor that is NOT an EXEC flavor: `isExecFlavor()` is false, so DSS injects no entry trampoline and there is no DSS-synthesized entry to call anything. Nor will the platform: ld.so runs `DT_INIT_ARRAY`, a DYNAMIC TAG the ELF writer does not emit (MEASURED on a DSS image -- `readelf -d` lists eleven tags and none is an init tag), and a PE DLL's initializers run from `DllMain`/`_DllMainCRTStartup`, which DSS does not synthesize either.\\nSo both runners would be FALSE here, and declaring the SECTIONS with no runner would emit a table nothing walks -- the silent shape this anchor exists to remove. With nothing declared, a program that schedules an initializer for this format is REFUSED by the linker, naming the format and the remedy. Making it work needs a real capability first (emit DT_INIT_ARRAY/DT_INIT_ARRAYSZ, or synthesize a DllMain), not a config row. Contrast macho64-*-dylib, which DOES declare `imageLoader`: dyld really does run `__mod_init_func` in a dylib, so that arm is a capability DSS has.",


  "sections": [
    {
      "$comment": "Executable code. virtualAddress = 4096 = elf.pageAlign, PINNED by validate(): ET_DYN VAs are BASE-RELATIVE (the loader slides the whole image), and the walker computes baseImageVa = text.virtualAddress - pageAlign, so the equality makes the image base 0 BY CONSTRUCTION -- the gcc .so convention (page zero holds Ehdr + PHT + dynamic metadata; .text opens the second page). sh_flags = SHF_ALLOC|SHF_EXECINSTR (6).",
      "kind": "text",
      "name": ".text",
      "type": 1,
      "flags": 6,
      "addrAlign": 16,
      "entrySize": 0,
      "virtualAddress": 4096
    },
    {
      "$comment": "Read-only initialized data (string literals, reloc-free const globals). Folded into the R+X PT_LOAD #1 after .text (same as the exec image). virtualAddress = 0 -- computed. A reloc-BEARING const global goes to relro instead (c145) -- in a .so that distinction is LOAD-BEARING, not a hardening nicety: RELATIVE fixups must land in writable pages.",
      "kind": "rodata",
      "name": ".rodata",
      "type": 1,
      "flags": 2,
      "addrAlign": 8,
      "entrySize": 0,
      "virtualAddress": 0
    },
    {
      "$comment": "Writable initialized data. R+W PT_LOAD #2 (W^X preserved). virtualAddress = 0 -- computed.",
      "kind": "data",
      "name": ".data",
      "type": 1,
      "flags": 3,
      "addrAlign": 8,
      "entrySize": 0,
      "virtualAddress": 0
    },
    {
      "$comment": "Zero-initialized data. SHT_NOBITS (8); memsz-only tail of PT_LOAD #2. virtualAddress = 0 -- computed.",
      "kind": "bss",
      "name": ".bss",
      "type": 8,
      "flags": 3,
      "addrAlign": 8,
      "entrySize": 0,
      "virtualAddress": 0
    },
    {
      "$comment": "Symbol table (debugger support; the LOADER reads .dynsym, which the walker emits unconditionally on this arm). sh_entsize = 24.",
      "kind": "symtab",
      "name": ".symtab",
      "type": 2,
      "flags": 0,
      "addrAlign": 8,
      "entrySize": 24,
      "virtualAddress": 0
    },
    {
      "$comment": "Symbol-name string table.",
      "kind": "strtab",
      "name": ".strtab",
      "type": 3,
      "flags": 0,
      "addrAlign": 1,
      "entrySize": 0,
      "virtualAddress": 0
    },
    {
      "$comment": "Section-header string table.",
      "kind": "shstrtab",
      "name": ".shstrtab",
      "type": 3,
      "flags": 0,
      "addrAlign": 1,
      "entrySize": 0,
      "virtualAddress": 0
    }
  ],

  "relocations": [
    {
      "$comment": "The pc-relative kinds are the only ones legal in ET_DYN `.text` (an absolute fixup in code would need DT_TEXTREL -- D-LK-DYN-TEXT-ABS-RELOC fails loud). kind numbers match the target schema rows (the cross-reference unifier requires both sides).",
      "name": "R_X86_64_PC32",
      "kind": 1,
      "nativeId": 2
    },
    {
      "$comment": "abs64 -- legal in DATA items only on this arm: the walker patches the slot with the base-relative value AND emits an R_X86_64_RELATIVE row for it (the load slide completes the address).",
      "name": "R_X86_64_64",
      "kind": 2,
      "nativeId": 1
    },
    {
      "name": "R_X86_64_32",
      "kind": 3,
      "nativeId": 10
    }
  ]
}

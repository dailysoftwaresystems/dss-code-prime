{
  "$comment": "STATIC LIBRARY (`ar` archive of relocatable members) for elf64-x86_64-linux. c171 (D-FF1-AR-STATICLIB-DRIVER-WIRING). IDENTICAL to the `elf64-x86_64-linux` relocatable format -- its ELF ET_REL x86_64 SysV members are written by the SAME relocatable writer + the same sections[]/relocations[] rows -- PLUS exactly `container: \"archive\"` + `artifactProfiles: [\"staticlib\"]` + the `-staticlib` name. The archive framing itself is FORMAT-BLIND (src/link/format/ar.cpp): the SysV `.a` armap for ELF/Mach-O, the Microsoft `.lib` 2nd linker member for PE (flavor threaded from kind() in linkAndWriteStaticArchive). No writer code is format-specific to the archive. Kept in sync with the relocatable sibling by hand: any section/reloc change there must mirror here.",
  "dssObjectFormatVersion": 1,
  "$absentKeyRationaleComment": "Machine-read by tests/link/test_object_format_family_symmetry.cpp: every root key a SIBLING of this format declares and this file does not must appear below with a non-empty reason, and every entry below must correspond to a real absence. Both directions fail loud, so this table can neither be skipped nor rot after a gap is closed.",
  "$absentKeyRationale": {
    "externAddrBinding": "DELIBERATE -- identical to the `elf64-x86_64-linux` relocatable sibling this file is hand-kept in sync with, for the identical reason (D-LK-ARM64-EXTERN-DATA-ADDR-PIE-GOT): x86_64's `&extern` value form is a PC-relative rel32 and is already PIE-safe, so the GOT indirection the aarch64 archive needs would be wrong here. An ARCH-SPECIFIC ABI fact, not an unfinished copy."
  },
  "$stackReserveUnsupportedReasonComment": "D-SQLITE-PE64-FULL-TIER-STACK-DEPTH: ELF has NO image field for a stack SIZE on ANY flavor -- PT_GNU_STACK encodes stack EXECUTABILITY, not size. The kernel sizes the initial thread's stack from the process limit (`ulimit -s` / setrlimit RLIMIT_STACK) and further threads from pthread_attr_setstacksize, so nothing the linker writes can change it. A `stackReserve` / `--stack-reserve` request is therefore REFUSED with that remedy rather than silently dropped -- which is also why the sqlite full tier passes on Linux (8 MiB by default) and overflowed on Windows at 1 MiB.",
  "stackReserveUnsupportedReason": "runtime-controlled",

  "format": {
    "name": "elf64-x86_64-linux-staticlib",
    "version": "1.0",
    "kind": "elf"
  },
  "$containerComment": "D-FF1-AR-STATICLIB-DRIVER-WIRING (c171): `container: archive` marks this as a STATIC-LIBRARY format. The driver (compileOneTarget) dispatches on THIS field -- never the artifactProfile name (the artifact_profile.hpp veto) -- routing each CU's relocatable member (.o) into ONE `ar` archive via linkAndWriteStaticArchive. This is the user's Option-1 format-container decision: output SHAPE stays 100% format-driven (cli->exec, lib->shared, staticlib->archive), exactly as elf.type / pe.type / macho.filetype select the single-file shapes.",
  "container": "archive",
  "$artifactProfilesComment": "This format SERVES the `staticlib` artifact profile (plan 06). It is the FIRST format to declare it -- the bare relocatable sibling serves NO profile (a `.o` is an internal cross-CU intermediate / a static-archive member, never a project's final artifact). enforceArtifactProfileFormat validates a `staticlib` project against this set. \u2605 `module` JOINED THIS SET 2026-08-15 (plan 06 \u00a75.1 B.12, as CORRECTED): a module IS a library, so it is served by every format that serves `lib` or `staticlib` and by nothing else. `artifactProfile` says what a project builds ITSELF as; `DependencyComposition` says how it is CONSUMED (`module` is SourceMerge \u2014 a consumer takes its SOURCES and ignores its artifact). Those are orthogonal, so a module builds standalone like any other library and the static-vs-dynamic choice is made by the `<target>:<format>` spec the author writes in `targets[]`, exactly as it already is for `lib` vs `staticlib` \u2014 no new rule and no engine change.",
  "artifactProfiles": [
    "staticlib",
    "module"
  ],
  "$dataModelComment": "FC3 c1: 64-bit linux/darwin are LP64 (int 32, long 64, ptr 64). Consumed by the semantic tier (buildCuMir threads format.dataModel() into analyze()): c long binds I64 here. REQUIRED closed-enum field; the loader rejects a missing or unknown value (a silent default would bake wrong widths).",
  "dataModel": "LP64",
  "$headerNameMatchingComment": "D-PP-HEADER-CASE-INSENSITIVE-PE: Linux/POSIX header names are matched BYTE-EXACT -- `#include <Stdio.h>` does NOT name `stdio.h`, and a conforming toolchain must reject it. REQUIRED closed-enum field: the loader rejects a missing or unknown value. DSS performs the comparison ITSELF (core/types/include_path_resolve.cpp) rather than calling fs::exists and inheriting the BUILD HOST's convention -- without that, an elf target built on Windows/macOS silently ACCEPTED `<Stdio.h>` and emitted a real object (MEASURED 2026-08-04).",
  "headerNameMatching": "case-sensitive",
  "$cSymbolDecorationComment": "D-FFI-CMANGLING-RULE-NOT-CONFIG-DRIVEN: System V / ELF applies NO decoration to a C identifier -- the linker-visible name IS the identifier. MEASURED 2026-08-06 (`nm -D /lib/x86_64-linux-gnu/libc.so.6`, WSL Ubuntu): glibc exports `exit@@GLIBC_2.2.5`, `printf@@GLIBC_2.2.5` and `fstat64@@GLIBC_2.33` under exactly their C spellings. THE SAME COMMAND IS ALSO WHY `none` IS A POSITIVE ANSWER AND NOT AN ABSENCE: `_exit@@GLIBC_2.2.5` is exported TOO and is a DIFFERENT FUNCTION -- POSIX `_exit(2)`, which does NOT flush stdio -- so a format that wrongly claimed `leading-underscore` would not merely produce an ugly name, it would silently bind every `exit` to the non-flushing wrapper and lose buffered output. This key is the single owner of a fact that previously had TWO: the closed C++ table `kCManglingRules` (src/ffi/mangling/c_mangle.cpp, keyed on ObjectFormatKind) and, redundantly, a literal encoding in the descriptors themselves -- the macho64-*-exec files spell `processExit.importMangledName` as `_exit` while their elf/pe siblings spell it `exit`. Two owners in two languages, with nothing forcing them to agree. RED-ON-DISABLE, stated precisely for the tier that exists today (MEASURED 2026-08-06 by actually removing the key from a shipped file and running the suite): DELETE this key and the format is REFUSED AT LOAD -- TWO errors at /cSymbolDecoration, one C_MissingField from the loader and one from ObjectFormatData::validate(), which loadFromText runs before returning -- so every test and example that loads this format goes red, naming the file. CHANGING the value is now an equally REAL lever, and this sentence was corrected the moment it became true: before step C4 the engine still read a C++ table and the value here changed no emitted binary. C4 landed (same cycle, TF-C122), and the effect is MEASURED 2026-08-06 -- flipping macho64-arm64-darwin-exec from `leading-underscore` to `none` and RECOMPILING NOTHING made the engine emit `printf` where it had emitted `_printf`, reddening tests/ffi/test_c_mangle.cpp's SchemeIsReadFromTheShippedSchemaNotACppTable. That is the whole point of the anchor: the answer lives in this file, so editing this file changes what the compiler emits. Two further pins guard the value: tests/link/test_c_symbol_decoration.cpp asserts the per-kind answer across all 24 shipped files and cross-checks it against this family's own processExit.importMangledName.",
  "cSymbolDecoration": { "scheme": "none" },
  "$cCallingConventionComment": "D-FFI-ABI-CATALOG-SELECTS-CALLING-CONVENTION-BY-FORMAT-IDENTITY: the platform C ABI for code compiled into this format, NAMING a row in the paired target's callingConventions[] array. This selection used to be re-derived in C++ by `kAbiCatalog`, a closed table keyed on (target name, ObjectFormatKind) — while this very string was already written here. Two owners of one fact; the table is deleted and this is the single owner. System V AMD64 (`rdi,rsi,rdx,rcx,r8,r9`).",
  "cCallingConvention": { "convention": "sysv_amd64" },
  "$outputExtensionComment": "D-PROGRAM-TIER-RETAINS-FORMAT-IDENTITY-BRANCHES: the extension the driver appends to an artifact this format produces. This used to be derived in C++ by `program/target_spec.cpp::outputExtensionFor`, a five-arm `switch (fmt.kind())` with a NESTED per-flavor switch inside each arm -- seven identity mentions computing a NAMING fact, which is not an engine behaviour at all. The nest existed because one ObjectFormatKind spans several artifact flavors; a DOCUMENT is exactly one flavor, so the whole derivation collapses to this one string. REQUIRED on every format. An EMPTY value is a DECLARED answer and not an omission -- a Unix executable carries no extension -- which is why the in-memory field is an `optional` and the loader's required check asks `contains`, never `empty`.",
  "outputExtension": ".a",
  "$targetArchComment": "D-PROGRAM-TIER-RETAINS-FORMAT-IDENTITY-BRANCHES: the .target.json NAME this format is for. `crossValidateTargetFormat` compares it against TargetSchema::name() -- two DECLARED NAMES -- where it used to run a six-arm `switch (format.kind())` reading a DIFFERENT field per identity (elf.machine / pe.machine / macho.cputype) and compare each against a hardcoded C++ table, kTargetArchMachineCodes, keyed on (target name x format kind). THAT TABLE IS DELETED, and the argument is ONE OWNER rather than tidiness: `arm64 is EM_AARCH64 = 183` had two owners -- this document's own machine field and that C++ row -- with nothing forcing them to agree, which is the identical shape as kCManglingRules and kAbiCatalog. The number now lives only in the document that emits it, and the pairing question is answered by a name this document states outright. Adding a new arch is now JSON-ONLY.",
  "targetArch": "x86_64",
  "$archiveMemberExtensionComment": "D-PROGRAM-TIER-RETAINS-FORMAT-IDENTITY-BRANCHES: how each relocatable MEMBER inside this archive is named. `program.cpp` used to read `kind() == ObjectFormatKind::Pe ? \".obj\" : \".o\"`. Presence-PAIRED with `container: archive`: required here, and REFUSED on any format that packages no members, because a fact about an artifact a format never produces reads as authority and is never consulted. Cross-checked by test against the `outputExtension` its own relocatable sibling declares -- two documents that must agree and have no other reason to.",
  "archiveMemberExtension": ".o",
  "$archiveFlavorComment": "D-PROGRAM-TIER-RETAINS-FORMAT-IDENTITY-BRANCHES: WHICH `ar` variant writes this archive. `compile_pipeline.cpp` used to read `(format.kind() == ObjectFormatKind::Pe) ? Coff : SysV`, defended in a comment as \"the closed enum, the existing agnostic dispatch axis\" -- which is precisely the rationalization the veto rejects, since a closed enum keyed on IDENTITY is an identity branch however it is spelled. NOT cosmetic: the coff flavor carries the little-endian second linker member that link.exe requires to resolve members at all, so a wrong answer produces a `.lib` whose members silently cannot be found. A closed VERB rather than an open name, because an archive layout is an ENGINE capability -- only variants `writeArArchive` realizes can be honored -- which is the opposite of the calling-convention NAME one key above, and deliberately so.",
  "archiveFlavor": "sysv",
  "$predefinedMacrosComment": "TF-C97 (D-PP-FORMAT-DATA-MODEL-PREDEFINES): the C-visible face of this file's `dataModel` above, DERIVED FROM THAT VALUE AND NEVER FROM THIS FILE'S NAME -- the loader synthesizes no macro name and the engine performs no format-identity branch; it merges whatever rows are declared here with the language's `preprocess.predefinedMacros` and the target's `predefinedMacros`, applying `availableObjectFormats` exactly once. DECLARED ON THE FORMAT because the data model is per-(OS x format), not per-CPU: the same x86_64 target is LP64 here and LLP64 under pe64-x86_64-windows-*, so a target-side row would be wrong on one of its own formats -- which is exactly what both .target.json files say when they record `__LP64__`/`_LP64` as DELIBERATELY NOT SHIPPED and defer to the format. BOTH spellings, each value 1: MEASURED 2026-07-30 with `/usr/bin/clang -dM -E -x c /dev/null -target <triple>` (Apple clang 21.0.0), __LP64__ and _LP64 are BOTH defined as 1 on arm64-apple-darwin, x86_64-apple-darwin, aarch64-linux-gnu and x86_64-unknown-linux-gnu, and NEITHER is defined on x86_64-pc-windows-msvc. COLLISION POLICY: a name declared by this file AND the language or target config is FATAL (C_ConflictingPredefinedMacro) -- no side silently wins. WHAT IT UNBLOCKS, MEASURED: the macOS SDK gates 234 occurrences across 91 headers on __LP64__, so without these rows DSS compiled the SDK's ILP32 arms while its own widths were LP64 (sizeof(long)==8), and $SDK/usr/include/mach/port.h:113-114 then asserted the user32 struct size 12 instead of user64's 16 -- sqlite mem1.c's three error[S0029]. RED-ON-DISABLE: delete this key and examples/c/predefined_lp64_macho stops exiting 42.",
  "predefinedMacros": [
    { "name": "__LP64__", "kind": "constant", "value": "1", "programRedefinition": "ordinary", "impliedSurface": { "kind": "claims-nothing", "reason": "arch-property" } },
    { "name": "_LP64",    "kind": "constant", "value": "1", "programRedefinition": "ordinary", "impliedSurface": { "kind": "claims-nothing", "reason": "arch-property" } }
  ],
  "$bitFieldStrategyComment": "D-CSUBSET-BITFIELD-ABI-EXACT: C bit-field allocation is OS/format-determined (the x86_64 CPU target serves BOTH this ELF-SysV format and PE-MS), so the strategy lives here, not on the target. SysV/Itanium/GNU LSB-first packing into the declared-type unit — byte-ABI-exact to gcc (verified by the conformance witness). Different-sized adjacent bit-fields may SHARE a unit (unlike MSVC).",
  "bitFieldStrategy": "gnu_packed",
  "$longDoubleFormatComment": "FC17.9(e) D-CSUBSET-LONG-DOUBLE: SysV x86_64 `long double` = x87 80-bit, 16/16. See elf64-x86_64-linux-exec.",
  "longDoubleFormat": "x87-80",
  "$externCallDispatchComment": "D-LK-OBJECT-EXTERN-CALL-RELOCATABLE: extern calls in a RELOCATABLE object lower to a PLAIN DIRECT `call` (E8 disp32) + a rel32 relocation against the UNDEFINED extern symbol — the SAME call-site shape as the exec `direct-plt`, except the FINAL (foreign) linker synthesizes the PLT/GOT and resolves the symbol, not DSS. So `direct-plt` is the right dispatch here too; the relocatable writer emits the extern as an SHN_UNDEF `.symtab` entry + a `.rela.text` reloc rather than a DSS-synthesized stub. The undefined-extern CALL reloc is emitted as R_X86_64_PLT32 (the linker-may-use-PLT variant of PC32, same S+A-P formula) so a foreign PIE link resolves it through a linker-built PLT instead of erroring on a bare PC32 against an undefined symbol.",
  "externCallDispatch": "direct-plt",
  "$weakDefinitionComment": "D-CONFIG-WEAK-DEFINITION-DIALECT-NOT-DECLARED + D-LK-WEAK-DEFINITION-DIALECT-UNCONSULTED-BY-ELF-AND-MACHO-WRITERS: HOW this format spells a WEAK DEFINITION -- a symbol several translation units may define, of which the linker keeps one. ELF puts the weakness in the SYMBOL's own BINDING field: STB_WEAK (2) in the high nibble of Elf64_Sym.st_info, gABI 4.18. No section flag and no separate per-symbol flag bit is involved, which is exactly what distinguishes `symbol-binding` from COFF's `comdat` (a per-SECTION duplicate-resolution policy) and from Mach-O's `symbol-flag` (N_WEAK_DEF set ALONGSIDE an ordinary binding). Written by `stbForBinding` in src/link/format/elf.cpp. THIS IS A DIALECT ROW, NOT A CAPABILITY FLAG: it says how this format spells something it CAN express, never that it cannot -- see D-LK-PE-ALTERNATENAME-DECLARE-AND-REFUSE for why an implementation gap recorded as a format incapability is a false fact in the place most likely to be trusted later. `elf::encode` READS this key -- ONCE, covering every ELF flavor, because the alias pass reaches stbForBinding on ET_REL, ET_EXEC and ET_DYN alike -- and refuses loud on a dialect it has no encoder for, so the declaration cannot drift away from what is actually emitted. The loader additionally refuses this key at LOAD when the declared dialect is not one the resolved backend's walker writes (ObjectFormatBackend::weakDefinitionDialects), so a mis-declaration fails at its source rather than at the first weak definition.",
  "weakDefinition": { "dialect": "symbol-binding" },
  "$supportedDataSectionsComment": "D-LK-OBJECT-DATA-SECTION-RELOCATABLE: a relocatable object DOES carry data — a global lands in `.rodata`/`.data`/`.bss`/`.data.rel.ro` with `sh_addr=0` and a section-relative `.symtab` entry (STT_OBJECT, st_shndx = the data section, st_value = offset) the FINAL linker binds. Same `AssembledModule.dataItems` producer path as exec (format-blind); the ET_REL writer emits the section bytes + data symbols + relocs against them instead of resolving to absolute VAs. D-LK-RELRO-CONST-DATA-RELOCATABLE (c145): a CONST global that carries LOAD-TIME relocations (a const function-pointer table / `int *const p = &x;` — sqlite's VFS method tables + `aSyscall[]`) lands in `relro` (ELF `.data.rel.ro`) and the writer emits its OWN relocations into `.rela.data.rel.ro` (R_X86_64_64); a reloc-bearing MUTABLE pointer global lands in `.data` and gets `.rela.data`. TLS (`tdata`/`tbss`) is deferred to a follow-up.",
  "supportedDataSections": [
    "rodata",
    "data",
    "bss",
    "relro"
  ],
  "elf": {
    "$comment": "Elf64_Ehdr identity fields. `class` = ELFCLASS64; `data` = ELFDATA2LSB (little-endian); `osabi` = ELFOSABI_NONE (SysV, conventional for relocatable .o); `machine` = EM_X86_64 (62).",
    "class": "elf64",
    "data": "lsb",
    "osabi": "sysv",
    "abiVersion": 0,
    "machine": 62
  },
  "sections": [
    {
      "$comment": "Executable code. sh_type = SHT_PROGBITS (1); sh_flags = SHF_ALLOC | SHF_EXECINSTR (0x6); addralign = 16 (x86_64 psABI).",
      "kind": "text",
      "name": ".text",
      "type": 1,
      "flags": 6,
      "addrAlign": 16,
      "entrySize": 0
    },
    {
      "$comment": "Relocation table for .text. sh_type = SHT_RELA (4); sh_flags = SHF_INFO_LINK (0x40); addralign = 8; entrysize = 24 (Elf64_Rela). sh_link/sh_info are set per-link.",
      "kind": "reloc",
      "name": ".rela.text",
      "type": 4,
      "flags": 64,
      "addrAlign": 8,
      "entrySize": 24
    },
    {
      "$comment": "Symbol table. sh_type = SHT_SYMTAB (2); addralign = 8; entrysize = 24 (Elf64_Sym).",
      "kind": "symtab",
      "name": ".symtab",
      "type": 2,
      "flags": 0,
      "addrAlign": 8,
      "entrySize": 24
    },
    {
      "$comment": "Symbol-name string table. sh_type = SHT_STRTAB (3); addralign = 1.",
      "kind": "strtab",
      "name": ".strtab",
      "type": 3,
      "flags": 0,
      "addrAlign": 1,
      "entrySize": 0
    },
    {
      "$comment": "Section-header string table. sh_type = SHT_STRTAB (3); addralign = 1. e_shstrndx of the ELF header points at this section.",
      "kind": "shstrtab",
      "name": ".shstrtab",
      "type": 3,
      "flags": 0,
      "addrAlign": 1,
      "entrySize": 0
    },
    {
      "$comment": "D-LK-OBJECT-EXTERN-SYMBOL-NAMES: empty non-executable-stack marker for foreign-toolchain interop. sh_type = SHT_PROGBITS (1); sh_flags = 0 (NO SHF_EXECINSTR) is what tells `ld` the stack need not be executable; addralign = 1; size 0. The ELF writer emits it (appended last, ET_REL only) iff this row is present.",
      "kind": "note",
      "name": ".note.GNU-stack",
      "type": 1,
      "flags": 0,
      "addrAlign": 1,
      "entrySize": 0
    },
    {
      "$comment": "D-LK-OBJECT-DATA-SECTION-RELOCATABLE: read-only initialized data (string literals, const globals). SHT_PROGBITS(1); SHF_ALLOC(2). In a relocatable object sh_addr=0 (the writer forces it) — the final linker assigns the VA. No virtualAddress field (exec-only).",
      "kind": "rodata",
      "name": ".rodata",
      "type": 1,
      "flags": 2,
      "addrAlign": 8,
      "entrySize": 0
    },
    {
      "$comment": "D-LK-OBJECT-DATA-SECTION-RELOCATABLE: writable initialized data (mutable globals, `int counter = 7;`). SHT_PROGBITS(1); SHF_ALLOC|SHF_WRITE(3). sh_addr=0 in a relocatable object.",
      "kind": "data",
      "name": ".data",
      "type": 1,
      "flags": 3,
      "addrAlign": 8,
      "entrySize": 0
    },
    {
      "$comment": "D-LK-OBJECT-DATA-SECTION-RELOCATABLE: zero-initialized data (tentative globals `int g;`). SHT_NOBITS(8) — occupies no file bytes. SHF_ALLOC|SHF_WRITE(3). sh_addr=0 in a relocatable object.",
      "kind": "bss",
      "name": ".bss",
      "type": 8,
      "flags": 3,
      "addrAlign": 8,
      "entrySize": 0
    },
    {
      "$comment": "D-LK-RELRO-CONST-DATA-RELOCATABLE (c145): relocated-read-only const data — a CONST global that carries LOAD-TIME relocations (a const function-pointer table / `int *const p = &x;` — sqlite's VFS method tables + `aSyscall[]`). This is the gcc `.data.rel.ro` contract: SHT_PROGBITS(1) + SHF_ALLOC|SHF_WRITE(3) — identical flags to `.data` (the loader must WRITE the resolved target VA into each slot during load-time relocation; it cannot sit in read-only `.rodata`, which would force DT_TEXTREL under PIE). A foreign linker places it in the GNU_RELRO segment (read-only AFTER relocation). sh_addr=0 in a relocatable object. The writer emits this section's OWN relocations into `.rela.data.rel.ro` (R_X86_64_64, abs64) — the ET_REL writer synthesizes the `.rela.<section>` name from this row's name, so no separate reloc-table row is declared here.",
      "kind": "relro",
      "name": ".data.rel.ro",
      "type": 1,
      "flags": 3,
      "addrAlign": 8,
      "entrySize": 0
    },
    {
      "$comment": "D-LK-MERGED-FOREIGN-FUNCTIONS-CARRY-NO-UNWIND-INFO-IN-THE-IMAGE: PER-FUNCTION UNWIND METADATA -- the DWARF call-frame table `gcc -c` and `clang -c` emit BY DEFAULT for every function that has a frame. Its absence from this list is why a function merged out of an ordinary Linux `.a` used to arrive in a DSS image with NO unwind description at all: a backtrace stopped at it and an exception thrown through it terminated, and unlike the Mach-O arm this one did not even say so. ★ THE VOCABULARY IS THE SWITCH, NOT A NAME TEST: `elf_object_reader.cpp` finds this section by its universal `kind` and never by the string `.eh_frame`, which is the same shape the Mach-O `__LD,__compact_unwind` row uses; a format that declares no `unwind` row carries nothing, exactly as before. ✔MEASURED 2026-08-24 against gcc 13.3.0 (`gcc -c` and `aarch64-linux-gnu-gcc -c`, no flags, `readelf -S`/`-r`): sh_type = SHT_PROGBITS (1); sh_flags = SHF_ALLOC (2) -- allocated but NOT writable and NOT executable; sh_addralign = 8; and the FDE function references live in `.rela.eh_frame` as R_X86_64_PC32 / R_AARCH64_PREL32 against the code section symbol, one per function. ⚠ entrySize is 0 AND THAT IS A STATEMENT, NOT AN OMISSION: a CIE/FDE record is self-describing and VARIABLE-LENGTH (its first field is its own length), so there is no record width to declare and no record COUNT derivable by division -- the Mach-O `__compact_unwind` row's fixed 32 is the property of THAT encoding, not of unwind sections in general. ⚠ THIS ROW MAKES DSS CARRY THE INFORMATION, NOT THE BYTES: the reader decodes each FDE back into the neutral `CfiFunction` vocabulary and hands it to whichever format writer is emitting, so the merged function's unwind table is re-emitted rather than copied. `.eh_frame` in the IMAGE stays writer-owned (elf.cpp synthesizes every byte of it), which is why this row appears only on the RELOCATABLE documents.",
      "kind": "unwind",
      "name": ".eh_frame",
      "type": 1,
      "flags": 2,
      "addrAlign": 8,
      "entrySize": 0
    },
    {
      "$comment": "D-LK-OBJECT-CARRIES-NO-SUMMARY-OR-MIR-SECTION. The per-module LINK-TIME-OPTIMIZATION SUMMARY: a small digest (defined symbols, referenced symbols, per-symbol cost) that a whole-program pass reads for EVERY object it is given. sh_type = SHT_PROGBITS (1). sh_flags = 0 -- deliberately NOT SHF_ALLOC, so the section occupies file bytes and NO image bytes: `ld` leaves a non-ALLOC section out of every PT_LOAD, so a summary-carrying object links into a byte-identical program with a foreign linker. addralign 8 so a reader may map records without a copy. PAIRED with the `.dss.mir` row below, and the SPLIT is the whole economy -- the global pass reads every summary WITHOUT PAGING IN ONE BYTE OF MIR, then reads the MIR of only the modules it chose. One combined section would make that impossible.",
      "kind": "lto-summary",
      "name": ".dss.summary",
      "type": 1,
      "flags": 0,
      "addrAlign": 8,
      "entrySize": 0
    },
    {
      "$comment": "D-LK-OBJECT-CARRIES-NO-SUMMARY-OR-MIR-SECTION. The module's serialized MIR body -- the LARGE half of the pair, read only for the modules the summary pass selected. Same SHT_PROGBITS / non-ALLOC / align-8 declaration as the summary row above and for the same reasons; the two differ in PAYLOAD, never in wire treatment. \u26a0 Not `debug`: stripping debug info must never throw away the optimizer's input.",
      "kind": "lto-ir",
      "name": ".dss.mir",
      "type": 1,
      "flags": 0,
      "addrAlign": 8,
      "entrySize": 0
    }
  ],
  "relocations": [
    {
      "$comment": "PC-relative 32-bit (call / jmp rel32). ELF type R_X86_64_PC32 = 2. `kind` = 1 matches x86_64.target.json's `rel32` row. `pltNativeId` = 4 (R_X86_64_PLT32): when this rel32 targets an UNDEFINED extern (a `call` to a libc function the FINAL linker resolves), the writer emits PLT32 instead so a foreign PIE link routes it through a linker-built PLT — a bare PC32 against an undefined symbol errors under -pie (D-LK-OBJECT-EXTERN-CALL-RELOCATABLE). Same S+A-P formula, same -4 addend.",
      "name": "R_X86_64_PC32",
      "kind": 1,
      "nativeId": 2,
      "pltNativeId": 4
    },
    {
      "$comment": "Absolute 64-bit. ELF type R_X86_64_64 = 1. `kind` = 2 matches x86_64.target.json's `abs64` row.",
      "name": "R_X86_64_64",
      "kind": 2,
      "nativeId": 1
    },
    {
      "$comment": "Absolute 32-bit zero-extend. ELF type R_X86_64_32 = 10. `kind` = 3 matches x86_64.target.json's `abs32` row.",
      "name": "R_X86_64_32",
      "kind": 3,
      "nativeId": 10
    },
    {
      "$comment": "D-UNWIND-NO-EH-FRAME-IN-RELOCATABLE-OBJECTS: the DWARF FDE `initial_location` pointer in a relocatable `.eh_frame` (DW_EH_PE_pcrel|sdata4). SAME ELF wire type as the kind-1 row above (R_X86_64_PC32 = 2); the `_UNBIASED` suffix is a DSS-side disambiguator, NOT a psABI spelling -- the format loader requires row names to be unique and both rows emit r_type 2. The two DSS kinds differ ONLY in the implicit addend the emitter bakes in (kind 1 `rel32` = -4, correct for a call site whose displacement is relative to the instruction END; kind 5 `pcrel32` = 0, correct for a DATA word). No `pltNativeId`: an FDE pointer never names an extern call, and routing it through a PLT stub would make the unwinder's PC lookup land on jump-stub bytes. ✔MEASURED 2026-08-13 against `gcc -c`: `readelf -r` shows `R_X86_64_PC32  .text + <fnOffset>` on every FDE.",
      "name": "R_X86_64_PC32_UNBIASED",
      "kind": 5,
      "nativeId": 2,
      "emitOnly": true
    }
  ]
}

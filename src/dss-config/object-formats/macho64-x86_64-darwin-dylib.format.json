{
  "$absentKeyRationaleComment": "Machine-read by tests/link/test_object_format_family_symmetry.cpp: every root key a SIBLING of this format declares and this file does not must appear below with a non-empty reason, and every entry below must correspond to a real absence. Both directions fail loud, so this table can neither be skipped nor rot after a gap is closed. CREATED 2026-08-19 (cycle P12) the moment the arm64 dylib sibling opted into tlsAccess -- before that this file had no absent sibling key and therefore no table.",
  "$absentKeyRationale": {
    "tlsAccess": "DELIBERATE: the arm64 dylib sibling's TLS opt-in (D-LK3-DYLIB-TLS-MODEL, closed 2026-08-19) rides the SHARED macho-tlv walker machinery, but x86_64 Darwin TLS depends on the x86_64 EXEC's own TLS opt-in (macho64-x86_64-darwin-exec deliberately declares no tlsAccess -- its $absentKeyRationale, pinned by Linker.TdataItemOnNonOptedInFormatsRejectsAtAcceptsGate, and MEASURED zero _Thread_local consumers in the staged upstream corpus; the sibling's Rosetta run leg landed 2026-08-19, so nothing here waits on runtime reachability). A _Thread_local TU on this format walls loud (K_FormatLacksThreadLocalSupport / the supportedDataSections gate), which is the correct outcome until an x86_64 Darwin TLS cycle lands."
  },
  "$comment": "Mach-O 64-bit DYNAMIC LIBRARY (MH_DYLIB, .dylib) for x86_64 macOS -- c171 conformance (the x86_64 sibling of macho64-arm64-darwin-dylib). CONFIG-ONLY for the writer: EVERY dylib divergence in `src/link/format/macho.cpp` is FILETYPE-keyed (`isDylib = filetype==Dylib`), never cputype-keyed -- MH_DYLIB routes unconditionally to `encodeExecDynamic` (the x86_64 macOS dynamic writer is in fact the ORIGINAL one, D-LK6-5); the sole cputype dispatch in the dynamic path is `__stubs` (`machoStubSizeFor` x86_64=6-byte FF 25 disp32, already supported); the export trie, ad-hoc code signing (execSegFlags is a caller-supplied per-flavor arg -- dylib=0, arch-neutral), and segment layout (`kPageSize = im.segmentPageSize`) are all arch-neutral. So this needs NO writer code, only this schema. Differs from macho64-arm64-darwin-dylib in ONLY the arch-specific fields: cputype (16777223 x86_64 vs 16777228 arm64), cpusubtype (3 vs 0), segmentPageSize (4096 x86_64 4 KiB pages vs 16384 Apple-Silicon 16 KiB), text virtualAddress (= segmentPageSize), longDoubleFormat (x87-80, x86_64's 80-bit extended -- arm64 uses IEEE-128 and omits it), and the reloc rows (X86_64_RELOC_* vs ARM64_RELOC_*). **RUNTIME IS macOS-x86_64/Rosetta-GATED**: there is NO macOS-x86_64 CI leg (macos-latest is Apple Silicon), so this ships with STRUCTURAL byte-pins ONLY (the writer emits it correctly -- proven at the byte level), and its exit-code RUN witness rides a future macOS-x86_64/Rosetta leg (the D-LK1-MACHO-X8664-DARWIN-DATA-SECTION untested-form discipline; follow-up D-LK-MACHO-X8664-DYLIB-RUNTIME). Code signing is KEPT (ad-hoc SHA-256) though x86_64 macOS does not REQUIRE it (unlike Apple Silicon) -- a signed dylib loads everywhere and mirrors modern ld64, which ad-hoc-signs all output.",

  "dssObjectFormatVersion": 1,

  "$stackReserveUnsupportedReasonComment": "D-SQLITE-PE64-FULL-TIER-STACK-DEPTH: a shared library carries an image header but NO field for a stack size (there is no LC_MAIN in a MH_DYLIB / no stack-size field in an ELF ET_DYN), and a loaded library never owns a thread's stack anyway, so a `stackReserve` / `--stack-reserve` request is REFUSED with an actionable remedy rather than silently dropped.",

  "stackReserveUnsupportedReason": "no-image-field",

  "$dataModelComment": "LP64 (64-bit Darwin: int 32, long 64, ptr 64) -- same as the x86_64 exec/object siblings.",
  "dataModel": "LP64",
  "$headerNameMatchingComment": "D-PP-HEADER-CASE-INSENSITIVE-PE: macOS ships on APFS/HFS+ volumes that are CASE-INSENSITIVE by default (the installer default since 10.13; a case-sensitive volume is opt-in), and Apple's own SDK headers rely on it -- `<TargetConditionals.h>` / `<CoreFoundation/CoreFoundation.h>` are routinely spelled with divergent case. REQUIRED closed-enum field. DSS ASCII-folds the comparison ITSELF per path component rather than delegating to fs::exists, so a Darwin cross-build from a case-sensitive Linux host resolves exactly as it does natively.",
  "headerNameMatching": "case-insensitive",
  "$cSymbolDecorationComment": "D-FFI-CMANGLING-RULE-NOT-CONFIG-DRIVEN: Apple's C ABI prefixes EVERY C symbol with exactly ONE leading underscore, on both 32- and 64-bit Mach-O -- the rule has no bitness axis and no per-arch axis (the arm64 and x86_64 slices of the universal libSystem decorate identically), which is why it is declared per FORMAT and not per target. EVIDENCE, EACH LABELLED. MEASURED-BY-EXECUTION (recorded TF-C113, the macOS cross-build arc): a Windows host cross-emitted a mach-o arm64 binary whose only process-exit path is the `_exit` import, and it RAN on the operator's Apple Silicon Mac and exited correctly -- an undecorated `exit` would have been an unresolved dyld bind, not a cosmetic difference. MEASURED (recorded TF-C121): libSystem's x86_64 slice exports BOTH `_fstat` and `_fstat$INODE64`, i.e. the C identifier `fstat` reaches the linker as `_fstat`. NOT RE-MEASURED THIS CYCLE, stated rather than buried: the Mac is a personal machine and was not declared reachable, so nothing here rests on a probe taken today. This key is the single owner of a fact that previously had TWO: the closed C++ table `kCManglingRules` (src/ffi/mangling/c_mangle.cpp, keyed on ObjectFormatKind) and, redundantly, a literal encoding in the descriptors themselves -- the macho64-*-exec files spell `processExit.importMangledName` as `_exit` while their elf/pe siblings spell it `exit`. Two owners in two languages, with nothing forcing them to agree. RED-ON-DISABLE, stated precisely for the tier that exists today (MEASURED 2026-08-06 by actually removing the key from a shipped file and running the suite): DELETE this key and the format is REFUSED AT LOAD -- TWO errors at /cSymbolDecoration, one C_MissingField from the loader and one from ObjectFormatData::validate(), which loadFromText runs before returning -- so every test and example that loads this format goes red, naming the file. CHANGING the value is now an equally REAL lever, and this sentence was corrected the moment it became true: before step C4 the engine still read a C++ table and the value here changed no emitted binary. C4 landed (same cycle, TF-C122), and the effect is MEASURED 2026-08-06 -- flipping macho64-arm64-darwin-exec from `leading-underscore` to `none` and RECOMPILING NOTHING made the engine emit `printf` where it had emitted `_printf`, reddening tests/ffi/test_c_mangle.cpp's SchemeIsReadFromTheShippedSchemaNotACppTable. That is the whole point of the anchor: the answer lives in this file, so editing this file changes what the compiler emits. Two further pins guard the value: tests/link/test_c_symbol_decoration.cpp asserts the per-kind answer across all 24 shipped files and cross-checks it against this family's own processExit.importMangledName.",
  "cSymbolDecoration": { "scheme": "leading-underscore" },
  "$predefinedMacrosComment": "TF-C97 (D-PP-FORMAT-DATA-MODEL-PREDEFINES): the C-visible face of this file's `dataModel` above, DERIVED FROM THAT VALUE AND NEVER FROM THIS FILE'S NAME -- the loader synthesizes no macro name and the engine performs no format-identity branch; it merges whatever rows are declared here with the language's `preprocess.predefinedMacros` and the target's `predefinedMacros`, applying `availableObjectFormats` exactly once. DECLARED ON THE FORMAT because the data model is per-(OS x format), not per-CPU: the same x86_64 target is LP64 here and LLP64 under pe64-x86_64-windows-*, so a target-side row would be wrong on one of its own formats -- which is exactly what both .target.json files say when they record `__LP64__`/`_LP64` as DELIBERATELY NOT SHIPPED and defer to the format. BOTH spellings, each value 1: MEASURED 2026-07-30 with `/usr/bin/clang -dM -E -x c /dev/null -target <triple>` (Apple clang 21.0.0), __LP64__ and _LP64 are BOTH defined as 1 on arm64-apple-darwin, x86_64-apple-darwin, aarch64-linux-gnu and x86_64-unknown-linux-gnu, and NEITHER is defined on x86_64-pc-windows-msvc. COLLISION POLICY: a name declared by this file AND the language or target config is FATAL (C_ConflictingPredefinedMacro) -- no side silently wins. WHAT IT UNBLOCKS, MEASURED: the macOS SDK gates 234 occurrences across 91 headers on __LP64__, so without these rows DSS compiled the SDK's ILP32 arms while its own widths were LP64 (sizeof(long)==8), and $SDK/usr/include/mach/port.h:113-114 then asserted the user32 struct size 12 instead of user64's 16 -- sqlite mem1.c's three error[S0029]. RED-ON-DISABLE: delete this key and examples/c-subset/predefined_lp64_macho stops exiting 42.",
  "predefinedMacros": [
    { "name": "__LP64__", "kind": "constant", "value": "1", "impliedSurface": { "kind": "claims-nothing", "reason": "arch-property" } },
    { "name": "_LP64",    "kind": "constant", "value": "1", "impliedSurface": { "kind": "claims-nothing", "reason": "arch-property" } }
  ],


  "$longDoubleFormatComment": "x86_64 macOS `long double` is the 80-bit x87 extended type (16-byte aligned) -- the same value the macho64-x86_64-darwin-exec sibling declares. ⚠ CORRECTED 2026-08-06: this comment used to end '(arm64 macOS uses IEEE-754 binary128 and its dylib omits this field.)', which was wrong TWICE and is worth recording rather than quietly deleting. Apple arm64 `long double` is `f64` (long double IS double), NOT binary128 -- binary128 is LINUX arm64, a different platform on the same CPU; and the arm64 dylib's omission was a GAP, not a design: it was the only arm64 Darwin format missing the axis, so `long double` compiled on the arm64 `.o` and `.exec` and was REJECTED on the `.dylib` (S0056) from the same source. The sibling now declares `f64` and the sentence that rationalised its absence is gone.",
  "longDoubleFormat": "x87-80",

  "$bitFieldStrategyComment": "D-CSUBSET-BITFIELD-ABI-EXACT: Darwin x86_64 uses the generic LSB-first bit-field allocation -- same gnu_packed as the x86_64 macho siblings.",
  "bitFieldStrategy": "gnu_packed",

  "format": {
    "name": "macho64-x86_64-darwin-dylib",
    "version": "1.0",
    "kind": "macho"
  },

  "$externCallDispatchComment": "The walker synthesizes a `__stubs` entry per extern FUNCTION (x86_64 = 6-byte FF 25 disp32 through the `__got` slot, lazy/eager per bindNow) and the call site is a plain direct `call rel32` to the stub -- pc-relative, slide-safe under the dyld base slide. D-FFI-EXTERN-CALL-DISPATCH.",
  "externCallDispatch": "direct-plt",

  "$dataImportBindingComment": "extern library DATA objects bind GOT-INDIRECT -- the walker reserves a `__got` slot per imported object; dyld's bind opcodes write the object's address into the slot and the GotIndirect lowering reads it. Copy relocations are not a Mach-O mechanism, and are no longer a DSS one either: the value was deleted from `DataImportBinding` (D-LK-ELF-COPY-RELOC-CLAIMS-ONE-NAME-OF-AN-ALIAS-SET).",
  "dataImportBinding": "got-indirect",

  "$supportedDataSectionsComment": "rodata (__TEXT,__const -- read-only), data (__DATA,__data), bss (__DATA,__bss zero-fill), relro (reloc-bearing const, __DATA,__const -- merged so its rebase slots land in writable pages). NO tdata/tbss: thread_local in a dylib needs the TLV descriptor model (D-LK3-DYLIB-TLS-MODEL); fails loud by this set's absence.",
  "supportedDataSections": ["rodata", "data", "bss", "relro"],

  "$artifactProfilesComment": "This format SERVES the shared-library profile (plan 06 AP3). The cli profile is the exec sibling. \u2605 `module` JOINED THIS SET 2026-08-15 (plan 06 \u00a75.1 B.12, as CORRECTED): a module IS a library, so it is served by every format that serves `lib` or `staticlib` and by nothing else. `artifactProfile` says what a project builds ITSELF as; `DependencyComposition` says how it is CONSUMED (`module` is SourceMerge \u2014 a consumer takes its SOURCES and ignores its artifact). Those are orthogonal, so a module builds standalone like any other library and the static-vs-dynamic choice is made by the `<target>:<format>` spec the author writes in `targets[]`, exactly as it already is for `lib` vs `staticlib` \u2014 no new rule and no engine change.",
  "artifactProfiles": ["lib", "module"],

  "macho": {
    "$comment": "mach_header_64 identity for an x86_64 MH_DYLIB. cputype = 16777223 (0x01000007 = CPU_TYPE_X86_64), cpusubtype = 3 (CPU_SUBTYPE_X86_64_ALL, same as the x86_64 exec). filetype = dylib (MH_DYLIB = 6). flags = 1048709 (MH_NOUNDEFS|MH_DYLDLINK|MH_TWOLEVEL|MH_NO_REEXPORTED_DYLIBS -- no MH_PIE on a library; same value as the arm64 dylib).",
    "cputype": 16777223,
    "cpusubtype": 3,
    "filetype": "dylib",
    "flags": 1048709
  },

  "image": {
    "$comment": "Mach-O image fields for an x86_64 MH_DYLIB. pageZeroSize = 0 -- a dylib carries NO __PAGEZERO (validate() enforces): __TEXT.vmaddr = 0 and dyld slides the whole base-0 image. segmentPageSize = 4096 (0x1000) -- x86_64 macOS uses 4 KiB segment alignment (Apple Silicon's dylib uses 16384). NO dylinkerPath. installName = the LC_ID_DYLIB identity a client records (@rpath convention; the walker NEVER derives it from the output file name -- c150 DT_SONAME discipline). loadDylibs = [libSystem] (the home of any libc import). bindNow = true. buildVersion macos 11.0. codeSignature = ad-hoc SHA-256: KEPT for symmetry with the arm64 dylib + modern ld64, though x86_64 macOS does not REQUIRE signing; the walker sets execSegFlags = 0 for the dylib flavor.",
    "pageZeroSize": 0,
    "segmentPageSize": 4096,
    "installName": "@rpath/libdss.dylib",
    "loadDylibs": ["/usr/lib/libSystem.B.dylib"],
    "bindNow": true,
    "buildVersion": {"platform": "macos", "minOs": "11.0", "sdk": "11.0"},
    "codeSignature": {"kind": "adhoc", "hashAlgorithm": "sha256", "pageSize": 4096, "identifier": "com.dss.dylib"}
  },

  "sections": [
    {
      "$comment": "Executable code. virtualAddress = 4096 = image.segmentPageSize, the base-0 dylib convention (page zero holds the header + load commands; __text opens the second page). type = 0x80000400 = S_ATTR_PURE_INSTRUCTIONS|S_ATTR_SOME_INSTRUCTIONS (the section_64.flags DSS's macho writer reads from `type`).",
      "kind": "text",
      "name": "__text",
      "segment": "__TEXT",
      "type": 2147484672,
      "flags": 0,
      "addrAlign": 16,
      "entrySize": 0,
      "virtualAddress": 4096
    },
    {
      "$comment": "Read-only initialized data. __TEXT,__const. virtualAddress = 0 -- computed. A reloc-bearing const global routes to relro (also __DATA,__const via the pair key).",
      "kind": "rodata",
      "name": "__const",
      "segment": "__TEXT",
      "type": 0,
      "flags": 0,
      "addrAlign": 8,
      "entrySize": 0,
      "virtualAddress": 0
    },
    {
      "$comment": "Writable initialized data. __DATA,__data. virtualAddress = 0 -- computed.",
      "kind": "data",
      "name": "__data",
      "segment": "__DATA",
      "type": 0,
      "flags": 0,
      "addrAlign": 8,
      "entrySize": 0,
      "virtualAddress": 0
    },
    {
      "$comment": "Zero-initialized data. __DATA,__bss. type = 1 = S_ZEROFILL (no file bytes). virtualAddress = 0 -- computed.",
      "kind": "bss",
      "name": "__bss",
      "segment": "__DATA",
      "type": 1,
      "flags": 0,
      "addrAlign": 8,
      "entrySize": 0,
      "virtualAddress": 0
    }
  ],

  "relocations": [
    {
      "$comment": "X86_64_RELOC_BRANCH -- the extern-call reloc on a __stubs entry (a plain rel32 branch). kind = 1 matches x86_64.target.json's `rel32` row (nativeId is the packed (type<<28)|(length<<25)|(pcrel<<24)). ⚠⚠ VALUE CORRECTED 2026-08-20 (D-CONFIG-MACHO-X86_64-EXEC-DYLIB-RELOC-NATIVEID-CONTRADICTS-ITS-OWN-ROW): this row declared nativeId 369098752 = 0x16000000, which under the packing THIS COMMENT ITSELF states -- (type<<28)|(length<<25)|(pcrel<<24) -- decodes to r_type=1 (X86_64_RELOC_SIGNED), r_length=3 (8 bytes), r_pcrel=0. Every one of those three fields contradicts the row's own name and stated intent: BRANCH is r_type=2, 4 bytes, PC-relative. The `-darwin` and `-staticlib` siblings have always carried the correct 620756992 = 0x25000000 = (2<<28)|(2<<25)|(1<<24), and the arm64 family agrees on that same packed value for its own BRANCH26. ⓘ THE DEFECT WAS LATENT, NOT LIVE, AND IT IS WORTH SAYING WHICH: relocation_info records are emitted only by the MH_OBJECT writer, which reads the `-darwin`/`-staticlib` documents, so nothing consumed this number. It is corrected rather than left alone because the exec format serves a real `cli` profile with a real run leg, and a wrong declared value that no consumer reads today is precisely the shape that becomes a silent miscompile the day one does. ★ `isCall` (D-LK-MACHO-ISDATA-NO-CALL-SIGNAL, 2026-08-20): this wire relocation can ONLY target executable code, so an object reader that meets an undefined extern through it has PROVEN the extern is a function rather than a data object -- the only classification signal a symbol-name-only reference offers. It is declared HERE, on the format row, and not derived from the target's formula, because a formula describes ARITHMETIC and not ROLE: `S + A - P` is the identical arithmetic for a call and for a PC-relative data reference, so the x86_64 target's `linear` is honest and simply carries no role, while aarch64's `aarch64_call26` looked like a role only because that CPU's branch has its own instruction encoding. ✔MEASURED 2026-08-20: every macho64-x86_64 archive member calling any library function was unreadable for exactly this reason while the arm64 sibling read the same source cleanly. The target document cannot carry it -- it is PER-CPU and shared with ELF, where the same `rel32` genuinely serves data references too.",
      "name": "X86_64_RELOC_BRANCH",
      "kind": 1,
      "nativeId": 620756992,
      "isCall": true
    },
    {
      "$comment": "X86_64_RELOC_UNSIGNED (length 3 = 8 bytes) -- an abs64 data pointer; the walker emits a dyld rebase for it under the base slide. kind = 2.",
      "name": "X86_64_RELOC_UNSIGNED_8",
      "kind": 2,
      "nativeId": 100663296
    },
    {
      "$comment": "X86_64_RELOC_UNSIGNED (length 2 = 4 bytes) -- an abs32 data slot. kind = 3. ⚠ VALUE CORRECTED 2026-08-20 (D-CONFIG-MACHO-X86_64-EXEC-DYLIB-RELOC-NATIVEID-CONTRADICTS-ITS-OWN-ROW): declared 33554432 = 0x02000000, which is r_length=1 = TWO bytes, for a row named _4 and (in the dylib sibling) commented \"length 2 = 4 bytes\". The correct packing is (0<<28)|(2<<25)|(0<<24) = 0x04000000 = 67108864, which is what the `-darwin`/`-staticlib` siblings carry. Same latency as the BRANCH row above: the MH_OBJECT writer is the only consumer of these numbers and it reads a different document.",
      "name": "X86_64_RELOC_UNSIGNED_4",
      "kind": 3,
      "nativeId": 67108864
    }
  ]
}

{
  "$comment": "WebAssembly v1 (v1) module format. First WASM schema (plan 14 LK8, 2026-05-30). Consumed by `src/link/format/wasm.cpp` via the format-blind linker engine. LK8 ships the linker SKELETON only — the walker emits the 8-byte module preamble (magic 'asm' + version 1) per WebAssembly spec §5.5; plan 18 (MIR→WAT→.wasm) fills the type / import / function / table / memory / global / export / start / element / code / data sections. No `sections[]` declared because the LK8 skeleton emits no section payloads; plan 18 expands this JSON with a WASM-specific section vocabulary when its MIR→WAT walker lands. No `relocations[]` because WASM has no traditional native-style relocations — intra-module references are Function/Table indices resolved by the runtime (per plan 18 §2.1).",

  "dssObjectFormatVersion": 1,

  "format": {
    "name": "wasm32-v1",
    "version": "1.0",
    "kind": "wasm"
  },

  "$predefinedMacrosComment": "TF-C97 (D-PP-FORMAT-DATA-MODEL-PREDEFINES): THIS FORMAT DELIBERATELY DECLARES NO `predefinedMacros` -- `dataModel` above is ILP32, and `__LP64__`/`_LP64` name the LP64 model specifically, so neither may be defined here. The absence is an ANSWER, not an omission, and is pinned as one by tests/link/test_object_format_schema.cpp. (This format is DECLARED-ONLY anyway -- the semantic consumer fails loud when an ILP32 format is actually selected -- so no C program on this leg can observe it yet; the row exists so the file STATES its answer instead of defaulting into one.)",
  "$dataModelComment": "FC3 c1: 32-bit data model (int 32, long 32, ptr 32) -- DECLARED-ONLY: the semantic consumer fails loud (S_UnsupportedDataModel) when an ILP32 format is actually selected, so no untested width path runs. REQUIRED closed-enum field like every format.",
  "dataModel": "ILP32",
  "$headerNameMatchingComment": "D-PP-HEADER-CASE-INSENSITIVE-PE: REQUIRED closed-enum field on EVERY format, including the declared-only skeletons. This format ships no C header surface of its own, so it declares the CONSERVATIVE value: byte-exact matching can only ever REJECT a spelling loudly, never silently ACCEPT one the target would reject. Revisit if/when a real header convention is defined for it.",
  "headerNameMatching": "case-sensitive",
  "$cSymbolDecorationComment": "D-FFI-CMANGLING-RULE-NOT-CONFIG-DRIVEN: WASM applies NO name decoration -- it reaches host functions through the two-level IMPORT NAMESPACE (module name plus field name) recorded in the import section, so there is no flat linker symbol to decorate in the first place. DOCUMENTED (WebAssembly core spec, import section) and consistent with the tree's own `kCManglingRules` row, `false` for wasm since FF4. DECLARED rather than omitted because the key is REQUIRED on EVERY format, unconditionally: `none` is the DECLARED answer for a format whose C-symbol channel is namespaced, and the required rule is exactly what stops a future wasm-family file from inheriting a scheme by silence. RED-ON-DISABLE, stated precisely for the tier that exists today (MEASURED 2026-08-06 by actually removing the key from a shipped file and running the suite): DELETE this key and the format is REFUSED AT LOAD -- TWO errors at /cSymbolDecoration, one C_MissingField from the loader and one from ObjectFormatData::validate(), which loadFromText runs before returning -- so every test and example that loads this format goes red, naming the file. CHANGING the value is now an equally REAL lever, and this sentence was corrected the moment it became true: before step C4 the engine still read a C++ table and the value here changed no emitted binary. C4 landed (same cycle, TF-C122), and the effect is MEASURED 2026-08-06 -- flipping macho64-arm64-darwin-exec from `leading-underscore` to `none` and RECOMPILING NOTHING made the engine emit `printf` where it had emitted `_printf`, reddening tests/ffi/test_c_mangle.cpp's SchemeIsReadFromTheShippedSchemaNotACppTable. That is the whole point of the anchor: the answer lives in this file, so editing this file changes what the compiler emits. Two further pins guard the value: tests/link/test_c_symbol_decoration.cpp asserts the per-kind answer across all 24 shipped files and cross-checks it against this family's own processExit.importMangledName.",
  "cSymbolDecoration": { "scheme": "none" }
}
